name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff mypy

      - name: Run Ruff (linting)
        run: ruff check --output-format=github

      - name: Run Ruff (formatting check)
        run: ruff format --diff

      - name: Run mypy (Tier 1 modules)
        run: mypy src/f1d/shared --config-file pyproject.toml

  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.9', '3.10', '3.11', '3.12', '3.13']

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov

      - name: Run Tier 1 tests with coverage (10% threshold)
        # Note: Threshold is low because coverage measures ALL shared modules,
        # not just the tested ones. Individual tested modules have 70%+ coverage.
        run: |
          pytest tests/unit/test_panel_ols.py \
                 tests/unit/test_iv_regression.py \
                 tests/unit/test_data_validation.py \
                 --cov=src/f1d/shared \
                 --cov-report=term-missing \
                 --cov-report=xml:coverage-tier1.xml \
                 --cov-fail-under=10 \
                 -v
        continue-on-error: false

      - name: Run Tier 2 tests with coverage (10% threshold)
        # Note: Threshold is low because coverage measures ALL shared modules
        run: |
          pytest tests/integration/ \
                 tests/unit/test_path_utils.py \
                 tests/unit/test_chunked_reader.py \
                 --cov=src/f1d/shared \
                 --cov-report=term-missing \
                 --cov-report=xml:coverage-tier2.xml \
                 --cov-fail-under=10 \
                 -v
        continue-on-error: false

      - name: Run full test suite with coverage (40% overall)
        run: |
          pytest tests/ \
            -m "not e2e" \
            --cov=src/f1d \
            --cov-report=xml \
            --cov-report=html \
            --cov-report=json \
            --cov-report=term-missing \
            --cov-fail-under=40 \
            -v

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.python-version }}
          path: |
            coverage.xml
            htmlcov/
          retention-days: 30

  e2e-test:
    name: E2E Tests
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      - name: Run E2E tests
        run: pytest tests/ -m e2e -v --timeout=1200
