---
phase: 73-cicd-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "pyproject.toml contains all tool configurations in single file"
    - "Dependencies are declared with version pins"
    - "Tool configurations match standards in DOC_TOOLING_STANDARD.md"
  artifacts:
    - path: "pyproject.toml"
      provides: "Single source of configuration for all tools"
      contains: ["[project.optional-dependencies]", "[project.urls]", "[tool.ruff]", "[tool.mypy]"]
  key_links:
    - from: "pyproject.toml"
      to: "GitHub Actions"
      via: "tool config sections"
      pattern: "\\[tool\\.(ruff|mypy|pytest)\\]"
    - from: "pyproject.toml"
      to: "pre-commit"
      via: "tool config sections"
      pattern: "\\[tool\\.(ruff|mypy)\\]"
---

<objective>
Enhance pyproject.toml with complete PEP 621 metadata and all tool configurations consolidated.

Purpose: Establish single source of truth for all project configuration, eliminating config drift between tools.
Output: Complete pyproject.toml with dependencies, tool configs, and project metadata.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pyproject.toml
@docs/DOC_TOOLING_STANDARD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PEP 621 project metadata</name>
  <files>pyproject.toml</files>
  <action>
    Add missing PEP 621 required fields to [project] section:
    - Add `authors` with name/email (use placeholder if not available)
    - Add `keywords` relevant to project (thesis, data-processing, finance)
    - Add `classifiers` for Python 3.9-3.13 support (already has 3.9-3.11)

    Add [project.optional-dependencies] section:
    ```toml
    [project.optional-dependencies]
    dev = [
        "pytest>=8.0",
        "pytest-cov>=5.0",
        "pytest-mock>=3.12",
        "ruff>=0.9.0",
        "mypy>=1.14",
        "pre-commit>=3.8",
    ]
    test = [
        "pytest>=8.0",
        "pytest-cov>=5.0",
        "pytest-mock>=3.12",
    ]
    ```

    Add [project.urls] section:
    ```toml
    [project.urls]
    Homepage = "https://github.com/user/f1d"
    Repository = "https://github.com/user/f1d.git"
    Issues = "https://github.com/user/f1d/issues"
    ```

    Ensure dependencies list includes all from requirements.txt:
    - structlog is already present
    - Verify pydantic and pydantic-settings are present
    - Verify scipy, statsmodels, scikit-learn are in dependencies
  </action>
  <verify>
    grep -E "^\[project\.(optional-dependencies|urls)\]" pyproject.toml && echo "Sections present"
    grep -E "^(ruff|mypy|pytest|pre-commit)" pyproject.toml && echo "Dev dependencies present"
  </verify>
  <done>
    pyproject.toml has complete [project] section with optional-dependencies and urls subsections
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance ruff configuration per TOOL-05</name>
  <files>pyproject.toml</files>
  <action>
    Update [tool.ruff.lint] section to match DOC_TOOLING_STANDARD.md TOOL-05:
    - Change `select` to include more rules: ["E", "W", "F", "I", "B", "C4", "UP", "ARG", "SIM"]
    - Keep existing `ignore = ["E501"]`
    - Add `ignore = ["E501", "B008"]` (function calls in argument defaults)

    Add [tool.ruff.lint.per-file-ignores]:
    ```toml
    [tool.ruff.lint.per-file-ignores]
    "__init__.py" = ["E402", "F401"]
    "tests/**" = ["S101", "ARG"]
    "2_Scripts/**" = ["ALL"]  # Legacy scripts - not enforcing
    ```

    Add [tool.ruff.lint.isort]:
    ```toml
    [tool.ruff.lint.isort]
    known-first-party = ["f1d"]
    ```

    Keep existing [tool.ruff.format] configuration (already correct).
  </action>
  <verify>
    grep -E "known-first-party" pyproject.toml && echo "isort config present"
    grep -E "(C4|UP|ARG|SIM)" pyproject.toml && echo "Extended rules present"
  </verify>
  <done>
    ruff configuration matches DOC_TOOLING_STANDARD.md TOOL-05 with extended rule set
  </done>
</task>

<task type="auto">
  <name>Task 3: Add bandit security configuration</name>
  <files>pyproject.toml</files>
  <action>
    Add [tool.bandit] configuration for security scanning:
    ```toml
    # ==============================================================================
    # Bandit Configuration (Security Scanning)
    # ==============================================================================

    [tool.bandit]
    exclude_dirs = [
        "tests/",
        ".___archive/",
        "2_Scripts/",  # Legacy scripts
    ]
    skips = [
        "B101",  # Allow assert in non-test code (legacy scripts)
    ]
    ```

    This configuration will be used by pre-commit hooks in 73-03.
  </action>
  <verify>
    grep -E "^\[tool\.bandit\]" pyproject.toml && echo "Bandit config present"
  </verify>
  <done>
    Bandit security scanner configuration added to pyproject.toml
  </done>
</task>

</tasks>

<verification>
1. pyproject.toml contains [project.optional-dependencies] with dev/test groups
2. pyproject.toml contains [project.urls] with project links
3. ruff configuration has extended rule set (C4, UP, ARG, SIM)
4. ruff has isort configuration with known-first-party
5. bandit configuration section exists
6. All changes maintain backward compatibility with existing CI workflow
</verification>

<success_criteria>
- pyproject.toml validation passes (valid TOML syntax)
- All required sections present per DOC_TOOLING_STANDARD.md TOOL-01
- Tool configurations are complete for ruff, mypy, pytest, coverage, bandit
</success_criteria>

<output>
After completion, create `.planning/phases/073-cicd-pipeline/73-01-SUMMARY.md`
</output>
