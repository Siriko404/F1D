---
phase: 70-type-hints-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/f1d/shared/__init__.py
  - src/f1d/shared/centering.py
  - src/f1d/shared/chunked_reader.py
  - src/f1d/shared/cli_validation.py
  - src/f1d/shared/data_loading.py
  - src/f1d/shared/data_validation.py
  - src/f1d/shared/dependency_checker.py
  - src/f1d/shared/diagnostics.py
  - src/f1d/shared/dual_writer.py
  - src/f1d/shared/env_validation.py
  - src/f1d/shared/financial_utils.py
  - src/f1d/shared/industry_utils.py
  - src/f1d/shared/iv_regression.py
  - src/f1d/shared/latex_tables.py
  - src/f1d/shared/metadata_utils.py
  - src/f1d/shared/observability/__init__.py
  - src/f1d/shared/observability/anomalies.py
  - src/f1d/shared/observability/files.py
  - src/f1d/shared/observability/logging.py
  - src/f1d/shared/observability/memory.py
  - src/f1d/shared/observability/stats.py
  - src/f1d/shared/observability/throughput.py
  - src/f1d/shared/observability_utils.py
  - src/f1d/shared/panel_ols.py
  - src/f1d/shared/path_utils.py
  - src/f1d/shared/regression_helpers.py
  - src/f1d/shared/regression_utils.py
  - src/f1d/shared/regression_validation.py
  - src/f1d/shared/reporting_utils.py
  - src/f1d/shared/string_matching.py
  - src/f1d/shared/subprocess_validation.py
autonomous: true

must_haves:
  truths:
    - "All Tier 1 shared module functions have type annotations on parameters"
    - "All Tier 1 shared module functions have type annotations on return values"
    - "mypy --strict passes on src/f1d/shared/ with zero errors"
    - "Type aliases are defined for complex types (DataFrameDict, VariableList, etc.)"
  artifacts:
    - path: "src/f1d/shared/path_utils.py"
      provides: "Path validation with complete type hints"
      contains: "def validate_output_path(path: Path,"
    - path: "src/f1d/shared/panel_ols.py"
      provides: "Panel OLS regression with complete type hints"
      contains: "pd.DataFrame"
    - path: "src/f1d/shared/data_validation.py"
      provides: "Data validation with complete type hints"
      contains: "List[str]"
  key_links:
    - from: "src/f1d/shared/*.py"
      to: "typing module"
      via: "import from typing"
      pattern: "from typing import"
    - from: "src/f1d/shared/*.py"
      to: "pandas types"
      via: "import pandas"
      pattern: "import pandas as pd"
---

<objective>
Add comprehensive type hints to all Tier 1 (Core Shared) modules in src/f1d/shared/ to achieve 100% type coverage with mypy strict mode compliance.

Purpose: Tier 1 modules are the foundation of the codebase and require complete type safety for CI blocking enforcement. This ensures all shared utilities have verified type contracts.

Output: 30 Python files with complete type annotations on all public functions and methods.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/CODE_QUALITY_STANDARD.md
@pyproject.toml

## Tier 1 Modules (from ARCHITECTURE_STANDARD.md)

Location: `src/f1d/shared/`
Coverage Target: 100%
mypy Mode: `strict = true`
Enforcement: CI blocking

### Type Hint Requirements (CODE-02)

For Tier 1 modules:
- All parameters must have type annotations
- All return types must be annotated
- Use `Optional[T]` for parameters that can be None
- Use `Union[A, B]` for parameters accepting multiple types
- Use type aliases for complex types (DataFrameDict, VariableList)

### Type Aliases to Define

```python
from typing import Dict, List, Any

DataFrameDict = Dict[str, pd.DataFrame]
VariableList = List[str]
RegressionResults = Dict[str, Any]
```

### Example Type Hints

```python
from pathlib import Path
from typing import Dict, List, Optional, Union
import pandas as pd

def validate_data(
    df: pd.DataFrame,
    required_cols: List[str],
    strict: bool = True,
) -> Dict[str, Any]:
    ...

def load_dataset(
    path: Path,
    columns: Optional[List[str]] = None,
) -> pd.DataFrame:
    ...
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add type hints to core utility modules</name>
  <files>
    src/f1d/shared/path_utils.py
    src/f1d/shared/dual_writer.py
    src/f1d/shared/cli_validation.py
    src/f1d/shared/env_validation.py
    src/f1d/shared/dependency_checker.py
    src/f1d/shared/subprocess_validation.py
  </files>
  <action>
    Add complete type hints to all functions and methods in these utility modules.

    For each module:
    1. Add `from typing import ...` imports for required types (List, Dict, Optional, Union, Any, Callable)
    2. Add type annotations to all function parameters
    3. Add return type annotations to all functions
    4. Use `Optional[T]` for optional parameters with None defaults
    5. Use `Path` from pathlib for path parameters

    Example for path_utils.py (already partially done):
    - `validate_output_path(path: Path, must_exist: bool = False, must_be_writable: bool = True) -> Path`
    - `ensure_output_dir(path: Path) -> Path`
    - `get_latest_output_dir(output_base: Path, required_file: Optional[str] = None) -> Path`

    Do NOT change any function behavior - only add type annotations.
  </action>
  <verify>
    Run `mypy src/f1d/shared/path_utils.py src/f1d/shared/dual_writer.py src/f1d/shared/cli_validation.py src/f1d/shared/env_validation.py src/f1d/shared/dependency_checker.py src/f1d/shared/subprocess_validation.py --strict` and verify zero errors.
  </verify>
  <done>
    All 6 utility modules have type hints on all public functions and mypy --strict passes with zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add type hints to data handling modules</name>
  <files>
    src/f1d/shared/data_loading.py
    src/f1d/shared/data_validation.py
    src/f1d/shared/chunked_reader.py
    src/f1d/shared/metadata_utils.py
    src/f1d/shared/string_matching.py
  </files>
  <action>
    Add complete type hints to data handling modules.

    For each module:
    1. Add type imports: `from typing import Dict, List, Optional, Union, Any, Iterator`
    2. Use `pd.DataFrame` and `pd.Series` for pandas types
    3. For chunked_reader.py, use `Iterator[pd.DataFrame]` for generators
    4. For data_validation.py, return types like `Dict[str, Any]` for validation results
    5. Define type aliases at module level if needed:
       - `ValidationResult = Dict[str, Any]`
       - `ColumnList = List[str]`

    Example patterns:
    - `def load_parquet(path: Path, columns: Optional[List[str]] = None) -> pd.DataFrame:`
    - `def validate_columns(df: pd.DataFrame, required: List[str]) -> List[str]:` (returns list of missing columns)
    - `def read_chunks(path: Path, chunk_size: int = 10000) -> Iterator[pd.DataFrame]:`

    Do NOT change any function behavior - only add type annotations.
  </action>
  <verify>
    Run `mypy src/f1d/shared/data_loading.py src/f1d/shared/data_validation.py src/f1d/shared/chunked_reader.py src/f1d/shared/metadata_utils.py src/f1d/shared/string_matching.py --strict` and verify zero errors.
  </verify>
  <done>
    All 5 data handling modules have type hints on all public functions and mypy --strict passes with zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add type hints to econometric modules</name>
  <files>
    src/f1d/shared/panel_ols.py
    src/f1d/shared/iv_regression.py
    src/f1d/shared/regression_helpers.py
    src/f1d/shared/regression_utils.py
    src/f1d/shared/regression_validation.py
    src/f1d/shared/centering.py
  </files>
  <action>
    Add complete type hints to econometric regression modules.

    For each module:
    1. Define type aliases for complex types:
       ```python
       from typing import Dict, List, Any, Optional, Union, Tuple
       import pandas as pd
       import numpy as np
       from numpy.typing import NDArray

       RegressionResults = Dict[str, Any]
       VariableList = List[str]
       CoefficientDict = Dict[str, float]
       ```
    2. Use `NDArray[np.float64]` for numpy arrays (from numpy.typing)
    3. Use `Tuple[float, float]` for confidence intervals
    4. For regression results, use `Dict[str, Any]` or define a TypedDict

    Example patterns:
    - `def run_panel_ols(df: pd.DataFrame, dependent: str, exog: VariableList, entity_effects: bool = True, time_effects: bool = True) -> RegressionResults:`
    - `def cluster_standard_errors(results: Any, cluster_col: str, df: pd.DataFrame) -> NDArray[np.float64]:`
    - `def get_coefficient_table(results: RegressionResults) -> pd.DataFrame:`

    Do NOT change any function behavior - only add type annotations.
  </action>
  <verify>
    Run `mypy src/f1d/shared/panel_ols.py src/f1d/shared/iv_regression.py src/f1d/shared/regression_helpers.py src/f1d/shared/regression_utils.py src/f1d/shared/regression_validation.py src/f1d/shared/centering.py --strict` and verify zero errors.
  </verify>
  <done>
    All 6 econometric modules have type hints on all public functions and mypy --strict passes with zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add type hints to financial and industry utilities</name>
  <files>
    src/f1d/shared/financial_utils.py
    src/f1d/shared/industry_utils.py
    src/f1d/shared/reporting_utils.py
    src/f1d/shared/latex_tables.py
    src/f1d/shared/diagnostics.py
  </files>
  <action>
    Add complete type hints to financial and reporting utility modules.

    For each module:
    1. financial_utils.py: Use `pd.DataFrame` for dataframes, `List[str]` for column lists
    2. industry_utils.py: Use `Dict[str, str]` for industry mappings
    3. reporting_utils.py: Use `TextIO` from typing for file handles
    4. latex_tables.py: Use `str` for LaTeX output, `Dict[str, Any]` for table configs
    5. diagnostics.py: Use `Dict[str, Any]` for diagnostic results

    Example patterns:
    - `def compute_financial_ratio(df: pd.DataFrame, numerator: str, denominator: str) -> pd.Series:`
    - `def format_latex_table(df: pd.DataFrame, caption: str, label: str) -> str:`
    - `def run_diagnostics(df: pd.DataFrame, columns: Optional[List[str]] = None) -> Dict[str, Any]:`

    Do NOT change any function behavior - only add type annotations.
  </action>
  <verify>
    Run `mypy src/f1d/shared/financial_utils.py src/f1d/shared/industry_utils.py src/f1d/shared/reporting_utils.py src/f1d/shared/latex_tables.py src/f1d/shared/diagnostics.py --strict` and verify zero errors.
  </verify>
  <done>
    All 5 utility modules have type hints on all public functions and mypy --strict passes with zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 5: Add type hints to observability package</name>
  <files>
    src/f1d/shared/observability/__init__.py
    src/f1d/shared/observability/anomalies.py
    src/f1d/shared/observability/files.py
    src/f1d/shared/observability/logging.py
    src/f1d/shared/observability/memory.py
    src/f1d/shared/observability/stats.py
    src/f1d/shared/observability/throughput.py
    src/f1d/shared/observability_utils.py
  </files>
  <action>
    Add complete type hints to the observability package.

    Note: observability modules already have `strict = true` in mypy overrides, so they may have partial type hints. Verify and complete any missing annotations.

    For each module:
    1. stats.py: Use `Dict[str, Union[int, float, str]]` for stats dictionaries
    2. anomalies.py: Use `List[Dict[str, Any]]` for anomaly reports
    3. files.py: Use `Path` for file paths, `Dict[str, Any]` for file info
    4. logging.py: Use `logging.Logger` from standard library
    5. memory.py: Use `float` for memory values in GB/MB
    6. throughput.py: Use `float` for timing values

    Example patterns:
    - `def compute_stats(df: pd.DataFrame, columns: Optional[List[str]] = None) -> Dict[str, Any]:`
    - `def detect_anomalies(df: pd.DataFrame, threshold: float = 3.0) -> List[Dict[str, Any]]:`
    - `def log_memory_usage(logger: logging.Logger, operation: str) -> None:`

    Do NOT change any function behavior - only add type annotations.
  </action>
  <verify>
    Run `mypy src/f1d/shared/observability/ src/f1d/shared/observability_utils.py --strict` and verify zero errors.
  </verify>
  <done>
    All 7 observability modules have type hints on all public functions and mypy --strict passes with zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 6: Verify Tier 1 mypy strict compliance</name>
  <files>
    src/f1d/shared/__init__.py
  </files>
  <action>
    Run comprehensive mypy verification on all Tier 1 modules.

    1. Update src/f1d/shared/__init__.py to export type aliases if not already present:
       ```python
       from typing import Dict, List, Any

       # Type aliases for use across the codebase
       DataFrameDict = Dict[str, "pd.DataFrame"]
       VariableList = List[str]
       RegressionResults = Dict[str, Any]
       ```

    2. Run mypy on entire shared package:
       ```bash
       mypy src/f1d/shared/ --strict --ignore-missing-imports
       ```

    3. Fix any remaining type errors discovered during the full package scan.

    4. Document any unavoidable type: ignore comments with explanation.

    Common issues to watch for:
    - Missing return type annotations
    - `Any` types that could be more specific
    - Third-party library types (pandas, numpy) needing ignore-missing-imports
  </action>
  <verify>
    Run `mypy src/f1d/shared/ --strict` and confirm zero errors. Also run `mypy src/f1d/shared/ --strict --ignore-missing-imports` to verify no pandas/numpy type issues.
  </verify>
  <done>
    mypy --strict passes on entire src/f1d/shared/ package with zero errors, confirming 100% Tier 1 type coverage.
  </done>
</task>

</tasks>

<verification>
1. Run `mypy src/f1d/shared/ --strict` - must pass with zero errors
2. Verify all 30 Tier 1 modules have type annotations on public functions
3. Check that type aliases (DataFrameDict, VariableList) are defined in shared/__init__.py
4. Confirm no function has missing parameter or return type annotations
</verification>

<success_criteria>
- [ ] All 30 Tier 1 modules have complete type hints
- [ ] mypy --strict passes on src/f1d/shared/ with zero errors
- [ ] Type aliases (DataFrameDict, VariableList, RegressionResults) defined
- [ ] No function missing parameter or return type annotations
- [ ] All type errors resolved or documented with type: ignore comments
</success_criteria>

<output>
After completion, create `.planning/phases/70-type-hints-implementation/70-01-SUMMARY.md`
</output>
