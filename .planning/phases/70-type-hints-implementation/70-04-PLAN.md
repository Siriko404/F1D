---
phase: 70-type-hints-implementation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/f1d/shared/observability/stats.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "stats.py passes mypy --strict without errors"
    - "All Tier 1 observability modules have 100% type hint coverage"
  artifacts:
    - path: "src/f1d/shared/observability/stats.py"
      provides: "Type-safe stats module with TypedDict"
      contains: "TypedDict or Union types for heterogeneous dicts"
  key_links:
    - from: "src/f1d/shared/observability/stats.py"
      to: "mypy --strict"
      via: "Type annotations"
---

<objective>
Refactor stats.py to resolve 131 mypy strict mode errors by using TypedDict for heterogeneous dictionary structures.

Purpose: Close Gap 1 from 70-VERIFICATION.md - stats.py is the only Tier 1 module failing strict mode.
Output: stats.py passes mypy --strict
</objective>

<context>
@.planning/phases/70-type-hints-implementation/70-VERIFICATION.md
@.planning/phases/70-type-hints-implementation/70-01-SUMMARY.md

Gap reason: stats.py has 131 mypy strict errors due to heterogeneous dictionary structures where functions return Dict[str, Any] but internal assignments use specific types that conflict (e.g., assigning lists where floats expected).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze stats.py type errors</name>
  <files>src/f1d/shared/observability/stats.py</files>
  <action>
Run mypy on stats.py to get detailed error list:
```
python -m mypy src/f1d/shared/observability/stats.py --strict --ignore-missing-imports 2>&1 | head -150
```

Categorize the 131 errors by pattern:
- Dict[str, float] assignments with list values
- Dict[str, Any] with heterogeneous value types
- Missing return type annotations
- Incompatible type assignments
  </action>
  <verify>mypy shows specific error patterns (line numbers, types)</verify>
  <done>Error categorization complete with identified patterns</done>
</task>

<task type="auto">
  <name>Task 2: Define TypedDict classes for stats dictionary structures</name>
  <files>src/f1d/shared/observability/stats.py</files>
  <action>
For each heterogeneous dict pattern identified in Task 1:
1. Create TypedDict class with explicit value types
2. Use Union types where values can be multiple types
3. Add Total=False for optional keys

Example patterns to implement:
- StatsDict = TypedDict('StatsDict', {'count': int, 'mean': float, 'values': List[float], ...})
- Use Literal for string keys with known values

Replace Dict[str, Any] returns with properly typed TypedDict or Union
  </action>
  <verify>TypedDict classes defined at module level, mypy shows reduced error count</verify>
  <done>TypedDict definitions replace Dict[str, Any] for heterogeneous structures</done>
</task>

<task type="auto">
  <name>Task 3: Fix remaining type errors and verify strict mode passes</name>
  <files>src/f1d/shared/observability/stats.py</files>
  <action>
1. Add missing return type annotations to all functions
2. Fix type: ignore comments that can be removed with proper typing
3. Ensure all internal dict assignments match declared types

Run mypy after each fix:
```
python -m mypy src/f1d/shared/observability/stats.py --strict --ignore-missing-imports
```

Iterate until 0 errors
  </action>
  <verify>mypy --strict passes with 0 errors</verify>
  <done>stats.py passes mypy --strict mode</done>
</task>

</tasks>

<verification>
Run full Tier 1 verification:
```
python -m mypy src/f1d/shared/observability/*.py --strict --ignore-missing-imports
```
Expected: All 7 observability modules pass strict mode
</verification>

<success_criteria>
- stats.py passes mypy --strict with 0 errors
- All 7 observability modules (anomalies, files, logging, memory, throughput, stats, __init__) pass mypy --strict
- Tier 1 modules achieve 100% type hint coverage
</success_criteria>

<output>
After completion, create `.planning/phases/70-type-hints-implementation/70-04-SUMMARY.md`
</output>
