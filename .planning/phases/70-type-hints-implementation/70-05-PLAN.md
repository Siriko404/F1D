---
phase: 70-type-hints-implementation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/f1d/sample/*.py
  - src/f1d/financial/v1/*.py
  - src/f1d/financial/v2/*.py
  - src/f1d/econometric/v2/*.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Tier 2 modules achieve 80%+ type hint coverage (mypy moderate mode passes)"
    - "All sample, financial, and econometric modules have type annotations"
  artifacts:
    - path: "src/f1d/sample/*.py"
      provides: "Type-annotated sample stage modules"
    - path: "src/f1d/financial/v1/*.py"
      provides: "Type-annotated financial v1 modules"
    - path: "src/f1d/financial/v2/*.py"
      provides: "Type-annotated financial v2 modules"
    - path: "src/f1d/econometric/v2/*.py"
      provides: "Type-annotated econometric v2 modules"
  key_links:
    - from: "Tier 2 modules"
      to: "pyproject.toml mypy config"
      via: "moderate mode settings"
---

<objective>
Fix remaining type errors in Tier 2 modules to achieve mypy moderate mode compliance (80%+ coverage).

Purpose: Close Gap 2 from 70-VERIFICATION.md - 843 errors in 39 Tier 2 files prevent full codebase type checking.
Output: mypy passes on Tier 2 modules with moderate mode
</objective>

<context>
@.planning/phases/70-type-hints-implementation/70-VERIFICATION.md
@.planning/phases/70-type-hints-implementation/70-02-SUMMARY.md

Gap reason: Full codebase mypy run shows 843 errors in 39 Tier 2 files. Issues include:
- Missing type annotations for stats dictionaries
- Heterogeneous dict typing issues in sample and econometric modules
- Collection[str] used where dict types expected
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze Tier 2 mypy errors</name>
  <files>
    - src/f1d/sample/*.py
    - src/f1d/financial/v1/*.py
    - src/f1d/financial/v2/*.py
    - src/f1d/econometric/v2/*.py
  </files>
  <action>
Run mypy on Tier 2 modules to get error summary:
```
python -m mypy src/f1d/sample/ src/f1d/financial/ src/f1d/econometric/ --ignore-missing-imports 2>&1 | tail -50
```

Get count by module:
```
python -m mypy src/f1d/sample/ src/f1d/financial/ src/f1d/econometric/ --ignore-missing-imports 2>&1 | grep "error" | wc -l
```

Categorize by module and error type (missing annotations, heterogeneous dicts, etc.)
  </action>
  <verify>Error count and categorization by module</verify>
  <done>Error breakdown by module and type identified</done>
</task>

<task type="auto">
<name>Task 2: Fix sample stage type errors</name>
<files>src/f1d/sample/*.py</files>
<action>
Focus on most frequent error patterns in sample/:
1. Add return types to all public functions
2. Fix stats dict typing (Dict[str, Any] or TypedDict)
3. Add type annotations for loader functions returning pd.DataFrame

Run mypy after fixes:
```
python -m mypy src/f1d/sample/ --ignore-missing-imports
```
  </action>
<verify>mypy error count reduced in sample/</verify>
<done>sample/ modules pass mypy moderate mode</done>
</task>

<task type="auto">
  <name>Task 3: Fix financial v1/v2 stage type errors</name>
  <files>
    - src/f1d/financial/v1/*.py
    - src/f1d/financial/v2/*.py
  </files>
<action>
Focus on:
1. Add return types to public functions
2. Fix industry mapping dict types (use Dict[int, Tuple[int, Optional[str]]])
3. Add pd.DataFrame return types for load_* functions
4. Fix Any types that should be more specific

Run mypy after fixes:
```
python -m mypy src/f1d/financial/ --ignore-missing-imports
```
  </action>
<verify>mypy error count reduced in financial/</verify>
<done>financial/ modules pass mypy moderate mode</done>
</task>

<task type="auto">
  <name>Task 4: Fix econometric v2 stage type errors</name>
  <files>src/f1d/econometric/v2/*.py</files>
<action>
Focus on:
1. Add return types to regression functions
2. Fix stats dict typing (Collection[str] -> Dict[str, Any])
3. Add type annotations for linearmodels returns
4. Fix heterogeneous dict issues in 4.1_H1CashHoldingsRegression.py

Run mypy after fixes:
```
python -m mypy src/f1d/econometric/ --ignore-missing-imports
```
  </action>
<verify>mypy error count reduced in econometric/</verify>
<done>econometric/ modules pass mypy moderate mode</done>
</task>

</tasks>

<verification>
Run full Tier 2 verification:
```
python -m mypy src/f1d/sample/ src/f1d/financial/ src/f1d/econometric/ --ignore-missing-imports
```
Expected: 0 errors or minimal errors acceptable for moderate mode
</verification>

<success_criteria>
- mypy passes on all Tier 2 modules with --ignore-missing-imports
- Tier 2 modules achieve 80%+ type hint coverage
- Full codebase mypy run shows significantly reduced error count (<100 errors)
</success_criteria>

<output>
After completion, create `.planning/phases/70-type-hints-implementation/70-05-SUMMARY.md`
</output>
