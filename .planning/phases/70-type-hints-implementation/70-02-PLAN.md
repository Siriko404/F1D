---
phase: 70-type-hints-implementation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/f1d/sample/*.py
  - src/f1d/text/*.py
  - src/f1d/financial/v1/*.py
  - src/f1d/financial/v2/*.py
  - src/f1d/econometric/v1/*.py
  - src/f1d/econometric/v2/*.py
autonomous: true

must_haves:
  truths:
    - "All Tier 2 stage modules have type annotations on public functions"
    - "mypy passes on Tier 2 modules with moderate mode (80%+ coverage)"
    - "Stage scripts can import shared types from f1d.shared"
    - "Regression scripts have typed function signatures"
  artifacts:
    - path: "src/f1d/sample/1.5_Utils.py"
      provides: "Sample utilities with type hints"
      contains: "def "
    - path: "src/f1d/financial/v2/3.2_H2Variables.py"
      provides: "H2 variable construction with type hints"
      contains: "pd.DataFrame"
    - path: "src/f1d/econometric/v2/"
      provides: "V2 regression scripts with type hints"
  key_links:
    - from: "src/f1d/*/v*/*.py"
      to: "f1d.shared types"
      via: "from f1d.shared import"
      pattern: "from f1d\\.shared"
    - from: "Tier 2 modules"
      to: "pandas types"
      via: "import pandas"
      pattern: "import pandas as pd"
---

<objective>
Add type hints to all Tier 2 (Stage-Specific) modules in src/f1d/{sample,text,financial,econometric}/ to achieve 80%+ type coverage with moderate mypy compliance.

Purpose: Tier 2 modules are stage-specific and allow gradual type adoption. Focus on public function signatures and key data flow points while allowing some flexibility for complex internal logic.

Output: ~53 Python files across sample, text, financial, and econometric packages with type annotations on public functions.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/CODE_QUALITY_STANDARD.md

## Tier 2 Modules (from ARCHITECTURE_STANDARD.md)

Location: `src/f1d/{sample,text,financial,econometric}/`
Coverage Target: 80%+
mypy Mode: `disallow_untyped_defs = false`, `warn_return_any = false`
Enforcement: Warning (not CI blocking)

### Type Hint Requirements (CODE-02)

For Tier 2 modules:
- Public functions should have type hints (80%+ target)
- Internal/private functions may omit type hints
- Complex internal logic can use `Any` for flexibility
- Focus on function signatures that are called from other modules

### Type Hint Strategy

1. **Sample stage (6 files):** Add hints to all utility functions
2. **Text stage (1 file):** Add hints to __init__.py exports
3. **Financial V1 (5 files):** Add hints to main processing functions
4. **Financial V2 (14 files):** Add hints to hypothesis variable functions
5. **Econometric V1 (~5 files):** Add hints to regression execution functions
6. **Econometric V2 (~5 files):** Add hints to hypothesis regression functions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add type hints to sample stage modules</name>
  <files>
    src/f1d/sample/__init__.py
    src/f1d/sample/1.0_BuildSampleManifest.py
    src/f1d/sample/1.1_CleanMetadata.py
    src/f1d/sample/1.2_LinkEntities.py
    src/f1d/sample/1.3_BuildTenureMap.py
    src/f1d/sample/1.4_AssembleManifest.py
    src/f1d/sample/1.5_Utils.py
  </files>
  <action>
    Add type hints to sample stage modules with 80%+ coverage focus.

    For each module:
    1. Add type imports at module level:
       ```python
       from typing import Dict, List, Optional, Any
       from pathlib import Path
       import pandas as pd
       from f1d.shared import DataFrameDict, VariableList
       ```

    2. Add type hints to all PUBLIC functions (those called from other modules or __main__):
       - Main processing functions (build_*, construct_*, process_*)
       - Utility functions in 1.5_Utils.py
       - Data loading functions

    3. Internal helper functions may omit type hints for faster iteration.

    Example patterns:
    - `def build_sample_manifest(config: Dict[str, Any]) -> pd.DataFrame:`
    - `def clean_metadata(df: pd.DataFrame, column_rules: Dict[str, Any]) -> pd.DataFrame:`
    - `def link_entities(manifest_df: pd.DataFrame, linking_config: Dict[str, Any]) -> pd.DataFrame:`
    - `def build_tenure_map(manifest_df: pd.DataFrame) -> pd.DataFrame:`

    Focus on function signatures that matter for understanding data flow.
  </action>
  <verify>
    Run `mypy src/f1d/sample/ --ignore-missing-imports` and verify it passes without blocking errors (warnings acceptable).
  </verify>
  <done>
    All 7 sample modules have type hints on public functions with 80%+ coverage. mypy passes in moderate mode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add type hints to text stage modules</name>
  <files>
    src/f1d/text/__init__.py
  </files>
  <action>
    Add type hints to text stage package.

    Currently text/ only has __init__.py. If it exports functions:
    1. Add type hints to any exported functions
    2. Document expected types in docstrings

    If text/__init__.py is empty or only imports, ensure it has proper type exports:
    ```python
    from typing import Dict, List, Any
    import pandas as pd
    ```
  </action>
  <verify>
    Run `mypy src/f1d/text/ --ignore-missing-imports` and verify it passes.
  </verify>
  <done>
    Text stage module has appropriate type hints on any exported functions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add type hints to financial V1 modules</name>
  <files>
    src/f1d/financial/__init__.py
    src/f1d/financial/v1/__init__.py
    src/f1d/financial/v1/3.0_BuildFinancialFeatures.py
    src/f1d/financial/v1/3.1_FirmControls.py
    src/f1d/financial/v1/3.2_MarketVariables.py
    src/f1d/financial/v1/3.3_EventFlags.py
    src/f1d/financial/v1/3.4_Utils.py
  </files>
  <action>
    Add type hints to financial V1 stage modules.

    For each module:
    1. Add type imports:
       ```python
       from typing import Dict, List, Optional, Any
       from pathlib import Path
       import pandas as pd
       import numpy as np
       from numpy.typing import NDArray
       from f1d.shared import DataFrameDict, VariableList
       ```

    2. Focus on main processing functions:
       - build_financial_features()
       - construct_firm_controls()
       - compute_market_variables()
       - add_event_flags()

    3. Utility functions should have hints for key parameters.

    Example patterns:
    - `def build_financial_features(df: pd.DataFrame, config: Dict[str, Any]) -> pd.DataFrame:`
    - `def construct_firm_controls(df: pd.DataFrame, control_vars: VariableList) -> pd.DataFrame:`
    - `def compute_market_variables(df: pd.DataFrame, price_col: str = "prc") -> pd.DataFrame:`
  </action>
  <verify>
    Run `mypy src/f1d/financial/v1/ --ignore-missing-imports` and verify it passes without blocking errors.
  </verify>
  <done>
    All 6 financial V1 modules have type hints on public functions with 80%+ coverage.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add type hints to financial V2 modules</name>
  <files>
    src/f1d/financial/v2/__init__.py
    src/f1d/financial/v2/3.1_H1Variables.py
    src/f1d/financial/v2/3.2_H2Variables.py
    src/f1d/financial/v2/3.2a_AnalystDispersionPatch.py
    src/f1d/financial/v2/3.3_H3Variables.py
    src/f1d/financial/v2/3.5_H5Variables.py
    src/f1d/financial/v2/3.6_H6Variables.py
    src/f1d/financial/v2/3.7_H7IlliquidityVariables.py
    src/f1d/financial/v2/3.8_H8TakeoverVariables.py
    src/f1d/financial/v2/3.9_H2_BiddleInvestmentResidual.py
    src/f1d/financial/v2/3.10_H2_PRiskUncertaintyMerge.py
    src/f1d/financial/v2/3.11_H9_StyleFrozen.py
    src/f1d/financial/v2/3.12_H9_PRiskFY.py
    src/f1d/financial/v2/3.13_H9_AbnormalInvestment.py
  </files>
  <action>
    Add type hints to financial V2 hypothesis-specific modules.

    For each module:
    1. All follow similar patterns - construct hypothesis-specific variables
    2. Add hints to main construction functions:
       - `def construct_h1_variables(df: pd.DataFrame, config: Dict[str, Any]) -> pd.DataFrame:`
       - `def construct_h2_variables(df: pd.DataFrame) -> pd.DataFrame:`
       - etc.

    3. Focus on the main entry point function for each script.

    4. Use `Dict[str, Any]` for config dictionaries since they vary by hypothesis.

    Example patterns:
    - `def construct_h1_variables(df: pd.DataFrame, config: Optional[Dict[str, Any]] = None) -> pd.DataFrame:`
    - `def compute_biddle_residual(df: pd.DataFrame, investment_col: str, controls: VariableList) -> pd.Series:`
    - `def merge_prisk_uncertainty(df: pd.DataFrame, prisk_path: Path) -> pd.DataFrame:`
  </action>
  <verify>
    Run `mypy src/f1d/financial/v2/ --ignore-missing-imports` and verify it passes without blocking errors.
  </verify>
  <done>
    All 13 financial V2 modules have type hints on public functions with 80%+ coverage.
  </done>
</task>

<task type="auto">
  <name>Task 5: Add type hints to econometric V1 and V2 modules</name>
  <files>
    src/f1d/econometric/__init__.py
    src/f1d/econometric/v1/__init__.py
    src/f1d/econometric/v1/*.py
    src/f1d/econometric/v2/__init__.py
    src/f1d/econometric/v2/*.py
  </files>
  <action>
    Add type hints to econometric V1 and V2 regression modules.

    For each module:
    1. Econometric modules call shared.regression functions, so import types:
       ```python
       from typing import Dict, List, Optional, Any
       from pathlib import Path
       import pandas as pd
       from f1d.shared import RegressionResults, VariableList
       from f1d.shared.panel_ols import run_panel_ols
       ```

    2. Focus on main regression execution functions:
       - `def run_ceo_clarity_regression(df: pd.DataFrame, config: Dict[str, Any]) -> RegressionResults:`
       - `def run_hypothesis_test(df: pd.DataFrame, hypothesis: str, spec: Dict[str, Any]) -> pd.DataFrame:`

    3. Output functions (LaTeX table generation) should have:
       - `def export_regression_table(results: List[RegressionResults], output_path: Path) -> None:`

    4. Allow `Any` for linearmodels return types since they are complex.

    Example patterns:
    - `def estimate_model(df: pd.DataFrame, spec: Dict[str, Any]) -> RegressionResults:`
    - `def run_hypothesis_suite(df: pd.DataFrame, hypotheses: VariableList) -> Dict[str, RegressionResults]:`
  </action>
  <verify>
    Run `mypy src/f1d/econometric/ --ignore-missing-imports` and verify it passes without blocking errors.
  </verify>
  <done>
    All econometric V1 and V2 modules have type hints on public functions with 80%+ coverage.
  </done>
</task>

<task type="auto">
  <name>Task 6: Verify Tier 2 mypy compliance</name>
  <files>
    (no files modified - verification only)
  </files>
  <action>
    Run comprehensive mypy verification on all Tier 2 modules.

    1. Run mypy on each stage package with moderate settings:
       ```bash
       mypy src/f1d/sample/ --ignore-missing-imports
       mypy src/f1d/text/ --ignore-missing-imports
       mypy src/f1d/financial/ --ignore-missing-imports
       mypy src/f1d/econometric/ --ignore-missing-imports
       ```

    2. Count warnings vs errors:
       - Warnings are acceptable for Tier 2
       - Errors should be fixed or have type: ignore with explanation

    3. Estimate coverage:
       - Spot-check 5-10 files for function signature coverage
       - Verify public functions have hints
       - Internal functions may omit hints

    4. Document any modules that need special handling.

    Expected outcome: mypy passes with warnings allowed, no blocking errors.
  </action>
  <verify>
    Run `mypy src/f1d/sample/ src/f1d/text/ src/f1d/financial/ src/f1d/econometric/ --ignore-missing-imports` and confirm no blocking errors (warnings acceptable for Tier 2).
  </verify>
  <done>
    All Tier 2 modules pass mypy with moderate mode. Estimated 80%+ type hint coverage on public functions.
  </done>
</task>

</tasks>

<verification>
1. Run `mypy src/f1d/sample/ src/f1d/text/ src/f1d/financial/ src/f1d/econometric/ --ignore-missing-imports` - must pass without blocking errors
2. Verify public functions in each stage have type hints
3. Spot-check 10 random files for coverage estimate
4. Confirm imports from f1d.shared work correctly
</verification>

<success_criteria>
- [ ] All Tier 2 modules pass mypy with moderate mode (warnings OK)
- [ ] Estimated 80%+ type hint coverage on public functions
- [ ] Sample stage (7 files) has typed public functions
- [ ] Financial stages (19 files) have typed public functions
- [ ] Econometric stages (~10 files) have typed public functions
- [ ] No blocking mypy errors in Tier 2 modules
</success_criteria>

<output>
After completion, create `.planning/phases/70-type-hints-implementation/70-02-SUMMARY.md`
</output>
