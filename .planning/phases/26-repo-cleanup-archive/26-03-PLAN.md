---
phase: 26-repo-cleanup-archive
plan: 03
type: execute
wave: 2
depends_on: [26-02]
files_modified:
  - DEPENDENCIES.md
  - INTEGRATION_REPORT.md
  - prd.md
  - UPGRADE_GUIDE.md
  - presentation.pdf
  - 2_Scripts_20251212.rar
  - 2_Scripts_20261201.rar
  - BACKUP_20260114_191340/
  - nul
  - .gitignore
autonomous: false

must_haves:
  truths:
    - Root directory cleaned to only contain standard files (README.md, 1_Inputs/, 2_Scripts/, 3_Logs/, 4_Outputs/, requirements.txt, pyproject.toml, config/)
    - Non-standard root files archived to appropriate categories
    - User approved disposition of documentation files (DEPENDENCIES.md, INTEGRATION_REPORT.md, prd.md, UPGRADE_GUIDE.md)
    - Large presentation.pdf archived or kept per user decision
    - .gitignore updated to exclude archive directory
  artifacts:
    - path: "root/"
      provides: "Clean root directory following CLAUDE.md naming convention"
      contains: "Only README.md, 1_Inputs/, 2_Scripts/, 3_Logs/, 4_Outputs/, requirements.txt, pyproject.toml, config/"
    - path: ".gitignore"
      provides: "Git ignore patterns for archive"
      contains: ".___archive/"
  key_links:
    - from: ".___archive/docs/"
      to: "Root documentation files (archived)"
      via: "User decision and mv/git mv commands"
      pattern: "mv.*\.md.*___archive"
    - from: ".___archive/backups/"
      to: "Root backup files (archived)"
      via: "mv command"
      pattern: "mv.*\.rar.*backups"
---

<objective>
Clean up root directory by archiving non-standard files following CLAUDE.md naming conventions

**Purpose:** The root directory currently contains multiple files that violate the project's strict naming convention (CLAUDE.md 1.2). Only README.md, 1_Inputs/, 2_Scripts/, 3_Logs/, 4_Outputs/, requirements.txt, pyproject.toml, and config/ should be in root. This plan archives all non-standard files after user approval.

**Why this matters:** A cluttered root directory violates the project's design principle of minimal yet functional structure. Following naming conventions makes the repository immediately understandable to new developers and reviewers.

**Output:** Clean root directory containing only standard files per CLAUDE.md, with non-standard files archived to appropriate categories, and .gitignore updated to exclude archive directory.
</objective>

<execution_context>
@C:\Users\sinas\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\sinas\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/26-repo-cleanup-archive/26-RESEARCH.md
@.planning/phases/26-repo-cleanup-archive/26-02-SUMMARY.md (from plan 02)
@CLAUDE.md

# Research Highlights
From 26-RESEARCH.md:
- Root should only contain: README.md, 1_Inputs/, 2_Scripts/, 3_Logs/, 4_Outputs/
- Standard Python projects also have: requirements.txt, pyproject.toml
- Non-standard files in root: DEPENDENCIES.md, INTEGRATION_REPORT.md, prd.md, UPGRADE_GUIDE.md, presentation.pdf, nul, 2_Scripts_20251212.rar, 2_Scripts_20261201.rar, BACKUP_20260114_191340/
- User decision needed for documentation files (value vs. clutter tradeoff)
- Update .gitignore to exclude .___archive/ directory

From CLAUDE.md section 1.2:
Pattern: `<Stage>.<Step>[.<Substep>]_<PascalCaseName>[.<ext>]`
Regex: `^(1|2|3|4)\.(\d+)(?:\.(\d+))?_[A-Z][A-Za-z0-9]*(?:_[A-Z][A-Za-z0-9]*)*(?:\.[A-Za-z0-9]+)?$`
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <what-built>Analysis of non-standard files in root directory requiring disposition decisions</what-built>
  <how-to-verify>
List all non-standard files currently in root:

```bash
cd "C:\Users\sinas\OneDrive\Desktop\Projects\Thesis_Bmad\Data\Data\Datasets\Datasets\Data_Processing\F1D"

# List root files
echo "=== Current root directory contents ==="
ls -1 | grep -v "^\(1_Inputs\|2_Scripts\|3_Logs\|4_Outputs\|config\|\.planning\|\.git\|\.claude\|___archive\|README\.md\|requirements\.txt\|pyproject\.toml\)$"

echo ""
echo "=== File sizes ==="
ls -lh *.md *.pdf *.rar 2>/dev/null | grep -v "README.md"
```

Files requiring decisions:
1. **DEPENDENCIES.md** (tracked) - Detailed dependency documentation
2. **INTEGRATION_REPORT.md** (untracked) - Integration gap analysis from earlier phases
3. **prd.md** (tracked) - Product requirements document
4. **UPGRADE_GUIDE.md** (untracked) - Upgrade procedures
5. **presentation.pdf** (tracked, 1.5MB) - Research presentation
6. **nul** (untracked) - Unknown purpose (investigate first)
7. **2_Scripts_20251212.rar** (untracked) - Backup archive
8. **2_Scripts_20261201.rar** (untracked) - Backup archive
9. **BACKUP_20260114_191340/** (untracked) - Backup directory

Review each file's content and purpose before deciding.
  </how-to-verify>
  <decision>Disposition of non-standard root files (archive vs. keep)</decision>
  <context>
The project's CLAUDE.md specifies root should only contain:
- README.md (main documentation)
- 1_Inputs/, 2_Scripts/, 3_Logs/, 4_Outputs/ (directories)
- requirements.txt, pyproject.toml (Python project standards)
- config/ (configuration)

All other files should be archived. However, some documentation files may have value:

**DEPENDENCIES.md** - Detailed dependency rationale (vs. requirements.txt which just lists versions)
**INTEGRATION_REPORT.md** - Historical integration gaps (may be superseded)
**prd.md** - Product requirements (may belong in .planning/ instead)
**UPGRADE_GUIDE.md** - Operational procedures (useful reference)
**presentation.pdf** - Large file, retrieve from archive if needed for thesis
  </context>
  <options>
    <option id="option-archive-all">
      <name>Archive all non-standard files</name>
      <pros>
        - Root directory is pristine, follows CLAUDE.md exactly
        - All files preserved in archive for later retrieval
        - No ambiguity about what belongs in root
      </pros>
      <cons>
        - Loses easy reference to documentation files
        - Need to retrieve from archive for DEPENDENCIES.md, UPGRADE_GUIDE.md
      </cons>
    </option>
    <option id="option-keep-docs">
      <name>Keep documentation files in root</name>
      <pros>
        - DEPENDENCIES.md, UPGRADE_GUIDE.md remain accessible
        - Documentation files are common in root
      </pros>
      <cons>
        - Violates CLAUDE.md naming convention
        - Root directory cluttered with 5+ extra files
        - Sets precedent for more files in root
      </cons>
    </option>
    <option id="option-move-docs-planning">
      <name>Move docs to .planning/ directory</name>
      <pros>
        - Documentation preserved and accessible
        - Root directory stays clean
        - prd.md already belongs in .planning/
      </pros>
      <cons>
        - .planning/ is for planning docs, not reference docs
        - Mixes planning with operational documentation
      </cons>
    </option>
    <option id="option-archive-everything-but-prd">
      <name>Archive everything except prd.md (move to .planning/)</name>
      <pros>
        - Clean root, prd.md moved to appropriate location
        - All reference docs archived but retrievable
        - Follows project conventions
      </pros>
      <cons>
        - Loses easy access to DEPENDENCIES.md, UPGRADE_GUIDE.md
      </cons>
    </option>
  </options>
  <resume-signal>Select: option-archive-all, option-keep-docs, option-move-docs-planning, or option-archive-everything-but-prd</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Archive non-standard root files per user decision</name>
  <files>Root directory (non-standard files), .___archive/docs/, .___archive/backups/</files>
  <action>
Archive root files based on user's decision from Task 1:

**If user selected option-archive-all or option-archive-everything-but-prd:**

1. Move backup archives to backups/:
```bash
# Compressed backups
mv 2_Scripts_20251212.rar .___archive/backups/scripts/
mv 2_Scripts_20261201.rar .___archive/backups/scripts/

# Directory backup
mv BACKUP_20260114_191340 .___archive/backups/root/
```

2. Move documentation files to docs/ (option-archive-all) or keep (option-keep-docs):
```bash
# For option-archive-all:
if [ "%USER_DECISION%" = "option-archive-all" ]; then
    # Check git tracking first
    for file in DEPENDENCIES.md prd.md presentation.pdf; do
        if git ls-files --error-unmatch "$file" >/dev/null 2>&1; then
            git mv "$file" .___archive/docs/
        else
            mv "$file" .___archive/docs/
        fi
    done

    # Untracked files
    mv INTEGRATION_REPORT.md .___archive/docs/reports/
    mv UPGRADE_GUIDE.md .___archive/docs/
fi
```

3. For option-archive-everything-but-prd:
```bash
# Move prd.md to .planning/
mv prd.md .planning/

# Archive everything else
for file in DEPENDENCIES.md presentation.pdf; do
    if git ls-files --error-unmatch "$file" >/dev/null 2>&1; then
        git mv "$file" .___archive/docs/
    else
        mv "$file" .___archive/docs/
    fi
done

mv INTEGRATION_REPORT.md .___archive/docs/reports/
mv UPGRADE_GUIDE.md .___archive/docs/
```

4. Handle nul file (investigate first):
```bash
# Check if nul is referenced by any script
if ! grep -r "nul" 2_Scripts/ --exclude-dir={ARCHIVE*,4_Econometric} >/dev/null 2>&1; then
    # Not referenced, safe to archive
    mv nul .___archive/test_outputs/temp/ 2>/dev/null || rm nul
else
    echo "WARNING: nul is referenced by scripts - keep in root or investigate"
fi
```

**If user selected option-keep-docs:**
Only move backup files and unknown files, keep documentation in root.

**If user selected option-move-docs-planning:**
Move documentation to .planning/docs/ instead of archiving.
</action>
  <verify>
```bash
# Verify root directory is clean
echo "=== Non-standard files in root ==="
ls -1 | grep -v "^\(1_Inputs\|2_Scripts\|3_Logs\|4_Outputs\|config\|\.planning\|\.git\|\.claude\|___archive\|README\.md\|requirements\.txt\|pyproject\.toml\)$"

# Should be empty or only show files user explicitly chose to keep

# Verify archived files exist
test -d .___archive/backups/scripts/ && echo "OK: Backup scripts archived"
test -d .___archive/backups/root/ && echo "OK: Backup root archived"
test -d .___archive/docs/ && ls -1 .___archive/docs/*.md | head -5
```
  </verify>
  <done>Root directory contains only standard files per CLAUDE.md (README.md, 1_Inputs/, 2_Scripts/, 3_Logs/, 4_Outputs/, requirements.txt, pyproject.toml, config/) plus any files user explicitly chose to keep. All non-standard files moved to appropriate archive categories.</done>
</task>

<task type="auto">
  <name>Task 3: Update .gitignore to exclude archive directory</name>
  <files>.gitignore</files>
  <action>
Add .___archive/ to .gitignore to prevent archived files from appearing as untracked:

1. Check current .gitignore:
```bash
cat .gitignore | grep -v "^#" | grep -v "^$"
```

2. Add archive to .gitignore (if not already present):
```bash
if ! grep -q "^\.___archive" .gitignore; then
    echo "" >> .gitignore
    echo "# Archive directory - contains old/unused files" >> .gitignore
    echo ".___archive/" >> .gitignore

    echo "Added .___archive/ to .gitignore"
else
    echo ".___archive/ already in .gitignore"
fi
```

3. Verify git status is clean:
```bash
git status
```

Expected: Archive files should not appear as "Untracked files".
</action>
  <verify>
```bash
# Verify .___archive/ in .gitignore
grep ".___archive" .gitignore

# Verify git status doesn't show archive files
git status --short | grep -c "___archive"
# Should output 0 (no archive files in status)
```
  </verify>
  <done>.gitignore updated to include .___archive/, git status no longer shows archived files as untracked, repository is clean.</done>
</task>

</tasks>

<verification>
After completing all tasks, verify root directory compliance:

```bash
# 1. List root directory (should only show standard files)
echo "=== Root directory contents ==="
ls -1

# 2. Verify CLAUDE.md compliance
echo ""
echo "=== Checking CLAUDE.md compliance ==="
standard="README.md 1_Inputs 2_Scripts 3_Logs 4_Outputs requirements.txt pyproject.toml config"
for item in $standard; do
    if ls -1 | grep -q "^$item$"; then
        echo "✓ $item"
    else
        echo "✗ $item (missing)"
    fi
done

# 3. Show archived documentation
echo ""
echo "=== Archived documentation ==="
ls -1 .___archive/docs/*.md 2>/dev/null | head -10

# 4. Verify .gitignore
echo ""
echo "=== Git ignore check ==="
grep ".___archive" .gitignore && echo "OK: Archive ignored"
```

Expected output:
- Root contains only standard files
- All 8 standard items present
- Archived docs in .___archive/docs/
- .gitignore contains .___archive/
</verification>

<success_criteria>
Phase 26-03 is complete when:
1. Root directory contains only standard files per CLAUDE.md (or explicit user-approved additions)
2. All backup files (*.rar, BACKUP_*/) moved to .___archive/backups/
3. Documentation files archived or moved per user decision
4. .gitignore updated to exclude .___archive/
5. git status shows no untracked archive files
6. User is satisfied with root directory cleanliness
</success_criteria>

<output>
After completion, create `.planning/phases/26-repo-cleanup-archive/26-03-SUMMARY.md` with:
- User's decision on documentation files (which option selected)
- List of files archived (by category)
- List of files kept in root (with justification)
- .gitignore changes made
- Final root directory contents (ls -1 output)
- Any user concerns or additional requests
</output>
