---
phase: 60-code-organization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/shared/observability/__init__.py
  - 2_Scripts/shared/observability/logging.py
  - 2_Scripts/shared/observability/stats.py
  - 2_Scripts/shared/observability/files.py
  - 2_Scripts/shared/observability/memory.py
  - 2_Scripts/shared/observability/throughput.py
  - 2_Scripts/shared/observability/anomalies.py
  - 2_Scripts/shared/observability_utils.py (becomes package import wrapper)
autonomous: true

must_haves:
  truths:
    - "observability_utils.py split into focused modules (logging, stats, files, memory, throughput, anomalies)"
    - "Old imports continue to work via __init__.py re-exports"
    - "All 59 calling scripts still function correctly"
    - "New preferred imports work: from shared.observability import DualWriter"
  artifacts:
    - path: "2_Scripts/shared/observability/__init__.py"
      provides: "Backward-compatible package initialization with re-exports"
      contains: "__all__", "from .", "DualWriter"
    - path: "2_Scripts/shared/observability/logging.py"
      provides: "DualWriter class and logging utilities"
      contains: "class DualWriter", "def write"
    - path: "2_Scripts/shared/observability/stats.py"
      provides: "Statistics and analysis functions"
      contains: "def print_stat", "def analyze_missing_values", "def print_stats_summary"
    - path: "2_Scripts/shared/observability/files.py"
      provides: "File utility functions"
      contains: "def compute_file_checksum"
    - path: "2_Scripts/shared/observability/memory.py"
      provides: "Memory tracking utilities"
      contains: "def get_process_memory_mb"
    - path: "2_Scripts/shared/observability/throughput.py"
      provides: "Performance measurement functions"
      contains: "def calculate_throughput"
    - path: "2_Scripts/shared/observability/anomalies.py"
      provides: "Anomaly detection functions"
      contains: "def detect_anomalies_zscore", "def detect_anomalies_iqr"
  key_links:
    - from: "2_Scripts/shared/observability/__init__.py"
      to: "2_Scripts/shared/observability/*.py"
      via: "Re-export imports (from .logging import DualWriter)"
      pattern: "from \\."
    - from: "All calling scripts (59 files)"
      to: "2_Scripts/shared/observability/__init__.py"
      via: "Existing import statements (backward compatible)"
      pattern: "from shared\\.observability_utils import|from shared import observability_utils"
---

<objective>
Split the monolithic observability_utils.py (4,668 lines, 60+ functions) into focused modules while maintaining 100% backward compatibility.

Purpose: Improve code maintainability by organizing related functions into logical modules without breaking any existing imports. The 4,668-line file is difficult to navigate and maintain.

Output: shared/observability/ package with 7 modules (init, logging, stats, files, memory, throughput, anomalies) and backward-compatible imports.
</objective>

<execution_context>
@C:\Users\sinas\.claude\get-shit-done/workflows/execute-plan.md
@C:\Users\sinas\.claude\get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Module structure based on function analysis:
# - Lines 37-54: compute_file_checksum -> files.py
# - Lines 55-110: print_stat, analyze_missing_values -> stats.py
# - Lines 111-147: print_stats_summary, save_stats -> stats.py
# - Lines 163-184: get_process_memory_mb, calculate_throughput -> memory.py, throughput.py
# - Lines 213-269: detect_anomalies_zscore, detect_anomalies_iqr -> anomalies.py
# - Lines 4630-4668: DualWriter class -> logging.py
# - Lines 323-4629: Step-specific stats functions -> stats.py (keep together)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create observability package structure</name>
  <files>
    - 2_Scripts/shared/observability/__init__.py
    - 2_Scripts/shared/observability/logging.py
    - 2_Scripts/shared/observability/stats.py
    - 2_Scripts/shared/observability/files.py
    - 2_Scripts/shared/observability/memory.py
    - 2_Scripts/shared/observability/throughput.py
    - 2_Scripts/shared/observability/anomalies.py
  </files>
  <action>
    Create the observability package directory and extract functions from observability_utils.py:

    **1. Create 2_Scripts/shared/observability/__init__.py:**
    - Re-export all public symbols from submodules
    - Maintain backward compatibility with: from shared.observability_utils import DualWriter
    - Include __all__ list defining public API
    - Add deprecation warning for old import path (optional, logged warning only)

    **2. Create 2_Scripts/shared/observability/logging.py (~500 lines):**
    - Extract DualWriter class (lines 4630-4668)
    - Add module docstring explaining logging utilities
    - Import dependencies: logging, sys

    **3. Create 2_Scripts/shared/observability/files.py (~200 lines):**
    - Extract compute_file_checksum function (lines 37-54)
    - Add module docstring explaining file utilities
    - Import dependencies: hashlib, pathlib.Path

    **4. Create 2_Scripts/shared/observability/memory.py (~150 lines):**
    - Extract get_process_memory_mb function (lines 163-182)
    - Add module docstring explaining memory tracking
    - Import dependencies: psutil

    **5. Create 2_Scripts/shared/observability/throughput.py (~300 lines):**
    - Extract calculate_throughput function (lines 184-212)
    - Add module docstring explaining performance measurement
    - Import dependencies: logging (already configured)

    **6. Create 2_Scripts/shared/observability/anomalies.py (~400 lines):**
    - Extract detect_anomalies_zscore (lines 213-268)
    - Extract detect_anomalies_iqr (lines 269-322)
    - Add module docstring explaining anomaly detection
    - Import dependencies: pandas, numpy

    **7. Create 2_Scripts/shared/observability/stats.py (~800+ lines):**
    - Extract all remaining stats functions (lines 55-4629, excluding above)
    - Functions: print_stat, analyze_missing_values, print_stats_summary, save_stats, compute_*_stats
    - Add module docstring explaining statistics utilities
    - Import dependencies: pandas, pathlib, typing

    IMPORTANT: Copy function signatures and implementations exactly - no logic changes
  </action>
  <verify>
    - All 7 files created in 2_Scripts/shared/observability/
    - Each module has proper imports and docstrings
    - __init__.py contains re-exports for backward compatibility
  </verify>
  <done>Package structure created with 7 modules</done>
</task>

<task type="auto">
  <name>Task 2: Update observability_utils.py to package imports</name>
  <files>2_Scripts/shared/observability_utils.py</files>
  <action>
    Modify existing observability_utils.py to become a thin compatibility wrapper:
    1. Keep the file (don't delete - maintain backward compatibility)
    2. Replace function implementations with re-exports from observability package
    3. Add deprecation notice in module docstring
    4. Ensure all existing imports continue to work

    Pattern:
    ```python
    # observability_utils.py (updated)
    """
    [DEPRECATED] Use: from shared.observability import X
    This module provides backward compatibility.
    """
    from shared.observability import (
        DualWriter,
        compute_file_checksum,
        print_stat,
        analyze_missing_values,
        # ... all public symbols
    )
    ```

    KEY: Do NOT delete observability_utils.py - it becomes a compatibility shim
  </action>
  <verify>
    - observability_utils.py still exists
    - File imports from observability package
    - Old import statements still work
  </verify>
  <done>observability_utils.py now re-exports from package</done>
</task>

<task type="auto">
  <name>Task 3: Verify backward compatibility with test imports</name>
  <files>tests/unit/test_observability_imports.py (create new test file)</files>
  <action>
    Create a test to verify backward compatibility:
    1. Test old import: from shared.observability_utils import DualWriter, compute_file_checksum, print_stat, calculate_throughput, detect_anomalies_zscore, get_process_memory_mb
    2. Test new import: from shared.observability import DualWriter, compute_file_checksum, print_stat, calculate_throughput, detect_anomalies_zscore, get_process_memory_mb
    3. Verify both import paths return the same objects (same identity via `is`)
    4. Test that imported functions are callable

    Run: pytest tests/unit/test_observability_imports.py -v
  </action>
  <verify>
    - Test file created
    - All import tests pass
    - Old and new imports return identical objects
  </verify>
  <done>Backward compatibility verified via tests</done>
</task>

<task type="auto">
  <name>Task 4: Verify all calling scripts still work</name>
  <files>2_Scripts/**/*.py (59 files that import observability_utils)</files>
  <action>
    Verify that no existing scripts break from the refactor:
    1. Get list of files importing observability_utils: grep -r "from shared.observability_utils import\|from shared import observability_utils" 2_Scripts/ --include="*.py" -l
    2. For each file, run Python syntax check: python -m py_compile [file]
    3. Run a quick import test on each to ensure no ImportError

    Quick verification script:
    ```python
    import sys
    from pathlib import Path

    scripts_dir = Path("2_Scripts")
    for py_file in scripts_dir.rglob("*.py"):
        content = py_file.read_text()
        if "observability_utils" in content:
            try:
                compile(content, str(py_file), 'exec')
                print(f"OK: {py_file}")
            except SyntaxError as e:
                print(f"ERROR: {py_file}: {e}")
    ```

    NOTE: Full pipeline execution verification (ORG-03-10) is deferred to Phase 63 (TEST-01-04) as it requires running end-to-end pipeline tests which are expensive and outside the scope of this refactoring. This task verifies structural compatibility only.
  </action>
  <verify>
    - All 59 files compile without syntax errors
    - No ImportError when importing modules
    - Import test shows all files can import successfully
  </verify>
  <done>All calling scripts verified structurally compatible (full pipeline test deferred to Phase 63)</done>
</task>

</tasks>

<verification>
Overall verification steps:
1. shared/observability/ package exists with 7 modules
2. observability_utils.py still exists as compatibility wrapper
3. Old imports work: from shared.observability_utils import DualWriter
4. New imports work: from shared.observability import DualWriter
5. All 59 calling scripts compile successfully
6. Unit tests verify backward compatibility
</verification>

<success_criteria>
1. Package created with logical module separation
2. Zero breaking changes to existing imports
3. Test suite verifies backward compatibility
4. All 4,668 lines of original code preserved (just reorganized)
</success_criteria>

<output>
After completion, create `.planning/phases/60-code-organization/60-03-SUMMARY.md`
</output>
