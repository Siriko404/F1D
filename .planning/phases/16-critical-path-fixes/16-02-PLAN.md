---
phase: 16-critical-path-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_full_pipeline.py
  - .github/workflows/test.yml
autonomous: true
must_haves:
  truths:
    - "An integration test runs all 17 scripts sequentially (Steps 1-4)"
    - "Test fails if any script exits with non-zero code"
    - "Test verifies existence of key output artifacts"
    - "Test runs in under 20 minutes (or has appropriate timeout)"
  artifacts:
    - path: "tests/integration/test_full_pipeline.py"
      contains: "def test_full_pipeline_execution"
  key_links:
    - from: "tests/integration/test_full_pipeline.py"
      to: "2_Scripts/"
      via: "subprocess.run"
---

<objective>
Create an End-to-End (E2E) integration test that verifies the full pipeline execution from Step 1 to Step 4.

Purpose: Close the critical testing gap identified in the milestone audit. Currently, no test verifies that data flows correctly between all steps.
Output: A new pytest integration test `tests/integration/test_full_pipeline.py`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.0.0-MILESTONE-AUDIT.md
@.planning/phases/16-critical-path-fixes/16-RESEARCH.md

The pipeline has 17 core scripts across 4 steps.
The test should execute them in the defined order (1.0 -> 1.4 -> 2.1... -> 4.3).
Use `subprocess.run` with `timeout` as recommended in research.
Use `shared.path_utils` or `pathlib` for robust path resolution.
</context>

<tasks>

<task type="auto">
  <name>Create full pipeline integration test</name>
  <files>tests/integration/test_full_pipeline.py</files>
  <action>
    Create `tests/integration/test_full_pipeline.py` with `test_full_pipeline_execution` function.
    
    1. Define the ordered list of 17 scripts to execute.
    2. Iterate through scripts and run each using `subprocess.run`.
    3. Assert returncode == 0 for each script.
    4. Implement a fail-fast mechanism (stop on first failure).
    5. Add a generous timeout (e.g., 20 mins total or per script).
    6. Verify critical output files exist after execution (e.g., final dataset).
    
    Mark with `@pytest.mark.e2e` and `@pytest.mark.slow`.
  </action>
  <verify>
    pytest tests/integration/test_full_pipeline.py --collect-only
  </verify>
  <done>
    Test file exists and is discoverable by pytest.
  </done>
</task>

<task type="auto">
  <name>Register E2E marker in pytest config</name>
  <files>pyproject.toml or pytest.ini</files>
  <action>
    Check if `pytest.ini` or `pyproject.toml` exists.
    Add `e2e` to the markers list to avoid warnings.
    
    Example:
    markers = [
        "e2e: End-to-end pipeline tests",
        ...
    ]
  </action>
  <verify>
    pytest --markers | grep e2e
  </verify>
  <done>
    Marker registered successfully.
  </done>
</task>

</tasks>

<verification>
Run the test collection to ensure it's valid.
Note: Actual execution might take too long for this interaction, so verification focuses on structure and validity.
</verification>

<success_criteria>
- [ ] tests/integration/test_full_pipeline.py created
- [ ] Test covers all 17 scripts in correct order
- [ ] Test uses subprocess.run with timeout
- [ ] pytest marker registered
</success_criteria>

<output>
After completion, create .planning/phases/16-critical-path-fixes/16-02-SUMMARY.md
</output>
