---
phase: 18-complete-phase-13-refactoring
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [2_Scripts/1_Sample/1.2_LinkEntities.py]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "1.2_LinkEntities.py uses shared.string_matching.match_company_names() instead of inline RapidFuzz calls"
    - "Direct rapidfuzz imports (from rapidfuzz import fuzz, process) removed"
    - "Line count reduced from 1090 to <1050 lines"
    - "Script executes successfully with identical outputs"
  artifacts:
    - path: "2_Scripts/1_Sample/1.2_LinkEntities.py"
      provides: "Refactored entity linking script using shared string matching"
      imports:
        - "from shared.string_matching import match_company_names"
      not_imports:
        - "from rapidfuzz import fuzz, process"
  key_links:
    - from: "2_Scripts/1_Sample/1.2_LinkEntities.py"
      to: "2_Scripts/shared/string_matching.py"
      via: "match_company_names() function"
      pattern: "match_company_names\("
---

<objective>
Fix 1.2_LinkEntities.py refactoring by replacing inline RapidFuzz.process.extractOne() calls with match_company_names() from shared.string_matching module.

Purpose: Close Gap 1 from VERIFICATION.md - script currently has inline RapidFuzz calls instead of using the shared match_company_names() function, and line count increased from 1043 to 1090 instead of decreasing.

Output: Refactored 1.2_LinkEntities.py with line count <1050, using shared string matching module, producing identical outputs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-complete-phase-13-refactoring/18-VERIFICATION.md

@2_Scripts/1_Sample/1.2_LinkEntities.py (lines 65-69, 91-93, 873-878)
@2_Scripts/shared/string_matching.py (match_company_names function at line 104)
@.planning/phases/18-complete-phase-13-refactoring/18-01-PLAN.md (original plan that was not executed)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import match_company_names and remove direct rapidfuzz imports</name>
  <files>2_Scripts/1_Sample/1.2_LinkEntities.py</files>
  <action>
  In 2_Scripts/1_Sample/1.2_LinkEntities.py:

  1. Update the existing import from shared.string_matching (lines 65-69):
     Change:
       from shared.string_matching import (
           load_matching_config,
           get_scorer,
           RAPIDFUZZ_AVAILABLE,
       )
     To:
       from shared.string_matching import (
           match_company_names,
           load_matching_config,
           RAPIDFUZZ_AVAILABLE,
       )

  2. Remove the direct rapidfuzz imports (lines 91-93):
     Delete these lines:
       # Load RapidFuzz directly for fuzzy matching operations
       if RAPIDFUZZ_AVAILABLE:
           from rapidfuzz import fuzz, process

  3. Remove usage of get_scorer variable if only used for process.extractOne() calls (check entire file)

  Gap reason: VERIFICATION.md Gap 1 - "Script still has inline RapidFuzz.process.extractOne() calls (lines 873-878) and does not import or use match_company_names()"
  </action>
  <verify>grep -n "from rapidfuzz import" 2_Scripts/1_Sample/1.2_LinkEntities.py && echo "FAIL: Direct rapidfuzz imports still present" || echo "OK: Direct rapidfuzz imports removed"</verify>
  <done>match_company_names imported, direct rapidfuzz imports removed</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline process.extractOne() call with match_company_names()</name>
  <files>2_Scripts/1_Sample/1.2_LinkEntities.py</files>
  <action>
  In 2_Scripts/1_Sample/1.2_LinkEntities.py, replace the inline RapidFuzz matching logic (lines 870-883):

  Existing code:
  ```
  for i, (idx, row) in enumerate(tier3_candidates.iterrows(), 1):
      query_name = row["company_name_norm"]

      # Get the scorer function from config
      scorer = get_scorer(scorer_name)

      result = process.extractOne(
          query_name,
          choice_list,
          scorer=scorer,
          score_cutoff=fuzzy_threshold,
      )

      if result is not None:
          best_match, best_score, _ = result
          ccm_data = choices[best_match]
          matched_records.append({
              "company_id": row["company_id"],
              # ... rest of record
          })
  ```

  Replace with match_company_names() call:
  ```
  for i, (idx, row) in enumerate(tier3_candidates.iterrows(), 1):
      query_name = row["company_name_norm"]

      # Use shared string matching function
      best_match, best_score = match_company_names(
          query=query_name,
          candidates=choice_list,
          threshold=fuzzy_threshold,
          scorer_name=scorer_name,
          preprocess=True,
      )

      if best_score > 0:  # Match found (returns 0.0 if no match above threshold)
          ccm_data = choices[best_match]
          matched_records.append({
              "company_id": row["company_id"],
              # ... rest of record
          })
  ```

  Note: match_company_names() returns (best_match, score) tuple, so extract both values. If score == 0.0, no match above threshold.

  Gap reason: VERIFICATION.md Gap 1 - "Lines 873-878 still use process.extractOne() inline"
  </action>
  <verify>grep -n "process.extractOne" 2_Scripts/1_Sample/1.2_LinkEntities.py && echo "FAIL: process.extractOne still present" || echo "OK: process.extractOne removed"</verify>
  <done>Inline process.extractOne() replaced with match_company_names()</done>
</task>

<task type="auto">
  <name>Task 3: Verify line count reduction and script execution</name>
  <files>2_Scripts/1_Sample/1.2_LinkEntities.py</files>
  <action>
  After refactoring, verify:

  1. Count lines:
     wc -l 2_Scripts/1_Sample/1.2_LinkEntities.py
     Target: Line count must be <1050 (was 1090, reduction of at least 40 lines)

  2. Verify syntax:
     python -m py_compile 2_Scripts/1_Sample/1.2_LinkEntities.py

  3. Check for any remaining rapidfuzz imports or process.extractOne calls:
     grep -r "from rapidfuzz" 2_Scripts/1_Sample/1.2_LinkEntities.py
     grep -r "process.extractOne" 2_Scripts/1_Sample/1.2_LinkEntities.py
     Both should return empty (no matches)

  Gap reason: VERIFICATION.md Gap 1 - "Line count increased from 1043 to 1090 instead of decreasing"
  </action>
  <verify>
  LINE_COUNT=$(wc -l < 2_Scripts/1_Sample/1.2_LinkEntities.py) && echo "Line count: $LINE_COUNT" && [ "$LINE_COUNT" -lt 1050 ] && echo "OK: Line count <1050" || echo "FAIL: Line count >=1050"
  </verify>
  <done>Line count <1050, script syntax valid, no remaining rapidfuzz inline usage</done>
</task>

</tasks>

<verification>
After completion, verify Gap 1 is closed:
1. 1.2_LinkEntities.py imports match_company_names from shared.string_matching
2. Direct rapidfuzz imports removed (grep confirms no "from rapidfuzz import")
3. Inline process.extractOne() calls replaced with match_company_names()
4. Line count reduced from 1090 to <1050
5. Script has valid Python syntax
</verification>

<success_criteria>
Gap 1 closed:
- ✅ match_company_names imported and used
- ✅ No direct rapidfuzz imports
- ✅ No process.extractOne() inline calls
- ✅ Line count <1050 (target reduction)
- ✅ Script syntax valid (compiles successfully)
</success_criteria>

<output>
After completion, create `.planning/phases/18-complete-phase-13-refactoring/18-04-SUMMARY.md`
</output>
