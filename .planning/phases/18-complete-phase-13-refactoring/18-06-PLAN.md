---
phase: 18-complete-phase-13-refactoring
plan: 06
type: tdd
wave: 1
depends_on: []
files_modified: [tests/unit/test_regression_helpers.py]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Unit test file tests/unit/test_regression_helpers.py exists with comprehensive tests"
    - "build_regression_sample() tested with various filter configurations"
    - "_check_missing_values() helper tested with missing and complete data"
    - "_assign_industry_codes() helper tested with SIC fixtures"
    - "All tests pass when run with pytest"
  artifacts:
    - path: "tests/unit/test_regression_helpers.py"
      provides: "Unit tests for regression_helpers.py functions"
      test_classes:
        - "TestBuildRegressionSample"
        - "TestCheckMissingValues"
        - "TestAssignIndustryCodes"
  key_links:
    - from: "tests/unit/test_regression_helpers.py"
      to: "2_Scripts/shared/regression_helpers.py"
      via: "Import build_regression_sample, _check_missing_values, _assign_industry_codes"
      pattern: "from shared.regression_helpers import"
---

<objective>
Create comprehensive unit tests for regression_helpers.py functions to close the test coverage gap identified in VERIFICATION.md.

Purpose: Close Gap 3 from VERIFICATION.md - "All extracted functions tested with unit tests" is PARTIAL. Observability and string_matching have tests, but regression_helpers.py has no dedicated unit test file.

Output: tests/unit/test_regression_helpers.py with tests for build_regression_sample(), _check_missing_values(), and _assign_industry_codes() functions, all passing with pytest.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/18-complete-phase-13-refactoring/18-VERIFICATION.md

@2_Scripts/shared/regression_helpers.py (build_regression_sample, _check_missing_values, _assign_industry_codes)
@tests/unit/test_observability_helpers.py (reference pattern for testing)
@tests/unit/test_fuzzy_matching.py (reference pattern for testing)
@.planning/phases/18-complete-phase-13-refactoring/18-02-SUMMARY.md (build_regression_sample implementation details)
</context>

<feature>
  <name>Regression Helpers Unit Tests</name>
  <files>
    - tests/unit/test_regression_helpers.py
    - 2_Scripts/shared/regression_helpers.py
  </files>
  <behavior>
    Test suite for regression_helpers.py functions:
    
    1. build_regression_sample():
       - Test with empty filter configuration
       - Test with eq filter (exact match)
       - Test with gt/lt filters (range filters)
       - Test with in/not_in filters (list filters)
       - Test with year_range filter
       - Test with missing values in required variables
       - Test with FF12 industry classification
       - Test with FF48 industry classification
       - Test error handling (missing required columns, invalid filters)
    
    2. _check_missing_values():
       - Test with no missing values
       - Test with missing values in some columns
       - Test with empty DataFrame
       - Test with columns not in DataFrame (should be handled gracefully)
       - Verify return type is dict of missing counts
    
    3. _assign_industry_codes():
       - Test with FF12 classification
       - Test with FF48 classification
       - Test with classification=None (should skip)
       - Test with missing SIC column (should skip)
       - Test with valid SIC codes
       - Test error handling (missing SIC lookup file)
  </behavior>
  <implementation>
    Follow test pattern from test_observability_helpers.py:
    
    1. Use pytest fixtures for test DataFrames
    2. Use tempfile for SIC lookup fixtures if needed
    3. Test both happy paths and error cases
    4. Verify return types and structure
    5. Test deterministic behavior (same input produces same output)
    6. Use minimal, focused test cases (avoid complex fixtures)
    
    SIC lookup fixtures:
    - Create minimal CSV fixtures in tests/fixtures/ directory
    - Or mock pd.read_parquet/read_csv to return test DataFrames
    - Use pytest-mock or unittest.mock for external file dependencies
    
    Test file structure:
    ```python
    import pytest
    import pandas as pd
    import sys
    from pathlib import Path
    
    sys.path.insert(0, str(Path(__file__).parent.parent.parent / '2_Scripts'))
    
    from shared.regression_helpers import (
        build_regression_sample,
        _check_missing_values,
        _assign_industry_codes,
    )
    
    class TestCheckMissingValues:
        # Tests for _check_missing_values()
    
    class TestAssignIndustryCodes:
        # Tests for _assign_industry_codes()
    
    class TestBuildRegressionSample:
        # Tests for build_regression_sample()
    ```
  </implementation>
</feature>

<red-green-refactor-cycle>

**RED - Write failing tests:**

1. Create tests/unit/test_regression_helpers.py
2. Write test classes for each function
3. Include test methods for various scenarios
4. Run pytest - all tests MUST fail (test before implementation cycle, but implementation exists so tests may pass initially - this validates implementation)

**GREEN - Ensure all tests pass:**

1. Run pytest tests/unit/test_regression_helpers.py -v
2. All tests MUST pass
3. Fix any failing tests by adjusting test expectations (not implementation)

**REFACTOR (if needed):**

1. Clean up test code if obvious improvements exist
2. Ensure tests are maintainable and clear
3. Only refactor tests, not implementation code

</red-green-refactor-cycle>

<verification>
After completion, verify Gap 3 is closed:
1. tests/unit/test_regression_helpers.py exists with 3+ test classes
2. build_regression_sample() has tests for various filter configurations (eq, gt/lt, in/not_in, year_range, missing values)
3. _check_missing_values() has tests for missing values and edge cases
4. _assign_industry_codes() has tests for FF12/FF48 classification and error handling
5. pytest tests/unit/test_regression_helpers.py -v runs all tests and passes (exit code 0)
</verification>

<success_criteria>
Gap 3 closed:
- ✅ tests/unit/test_regression_helpers.py created
- ✅ Tests for build_regression_sample() with various filters
- ✅ Tests for _check_missing_values() helper
- ✅ Tests for _assign_industry_codes() helper
- ✅ All tests pass (pytest exit code 0)
</success_criteria>

<output>
After completion, create `.planning/phases/18-complete-phase-13-refactoring/18-06-SUMMARY.md`
</output>
