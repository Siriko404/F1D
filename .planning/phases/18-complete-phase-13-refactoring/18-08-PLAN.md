---
phase: 18-complete-phase-13-refactoring
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py
autonomous: true
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "4.2_LiquidityRegressions.py reduced to <800 lines (target: -16 lines)"
    - "Comment lines consolidated from 33 to ~17 lines"
    - "Script maintains readability and functionality"
  artifacts:
    - path: "2_Scripts/4_Econometric/4.2_LiquidityRegressions.py"
      provides: "Refactored script with consolidated comments"
      max_lines: 800
  key_links:
    - from: "2_Scripts/4_Econometric/4.2_LiquidityRegressions.py"
      to: "2_Scripts/4_Econometric/4.2_LiquidityRegressions.py"
      via: "Comment consolidation"
      pattern: "consolidated.*section.*headers"
---

<objective>
Reduce 4.2_LiquidityRegressions.py line count to <800 lines by consolidating verbose section headers and comments.

Purpose: Complete line count reduction for 4.2_LiquidityRegressions.py by consolidating verbose comments and section headers (target: -16 lines).
Output: 4.2 script at <800 lines with maintained readability
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-complete-phase-13-refactoring/18-VERIFICATION.md
@.planning/phases/18-complete-phase-13-refactoring/18-05-SUMMARY.md

# Current state
4.2_LiquidityRegressions.py: 816 lines (need -16 lines to reach <800)
Current comment lines: 33 lines (4% of script)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate verbose section headers in 4.2</name>
  <files>2_Scripts/4_Econometric/4.2_LiquidityRegressions.py</files>
  <action>
Reduce verbose section headers and comments in 4.2_LiquidityRegressions.py:

1. Find and consolidate consecutive separator lines (e.g., multiple "# ======" lines):
   - Replace sequences like:
     ```
     # ============================================================================
     # ============================================================================
     ```
   - With single separator:
     ```
     # ============================================================================
     ```

2. Consolidate verbose comment blocks into single descriptive lines:
   - Combine multi-line explanatory comments where redundant
   - Keep essential comments for clarity, remove verbose restatements

3. Reduce excessive blank lines (max 1-2 consecutive blank lines between sections)

4. Target reduction: -16 lines (from 816 to 800)

Consolidation strategy:
- Section headers: Keep one line separator between major sections
- Function docstrings: Keep unchanged (essential for documentation)
- Inline comments: Keep only where clarifying complex logic
- Remove: Redundant section titles, duplicate separators, obvious comments
  </action>
  <verify>
python -c "
import subprocess
result = subprocess.run(['wc', '-l', '2_Scripts/4_Econometric/4.2_LiquidityRegressions.py'], capture_output=True, text=True)
lines = int(result.stdout.strip().split()[0])
print(f'Current line count: {lines}')
assert lines <= 800, f'Line count {lines} exceeds target 800'
"
</verify>
  <done>4.2 line count ≤ 800 lines (target: -16 lines reduction)</done>
</task>

<task type="auto">
  <name>Task 2: Verify script functionality and syntax</name>
  <files>2_Scripts/4_Econometric/4.2_LiquidityRegressions.py</files>
  <action>
Run Python syntax check to ensure no errors introduced by comment consolidation:

1. Syntax validation:
python -c "import py_compile; py_compile.compile('2_Scripts/4_Econometric/4.2_LiquidityRegressions.py', doraise=True)"

2. Verify function definitions remain intact:
python -c "
import ast
with open('2_Scripts/4_Econometric/4.2_LiquidityRegressions.py', 'r') as f:
    tree = ast.parse(f.read())
funcs = [node.name for node in ast.walk(tree) if isinstance(node, ast.FunctionDef)]
print(f'Functions found: {len(funcs)}')
assert 'run_first_stage' in funcs, 'Missing run_first_stage function'
assert 'run_ols_regression' in funcs, 'Missing run_ols_regression function'
assert 'run_iv_regression' in funcs, 'Missing run_iv_regression function'
"

3. Verify no code logic was accidentally removed (only comments/blank lines reduced)
  </action>
  <verify>python -c "import py_compile; py_compile.compile('2_Scripts/4_Econometric/4.2_LiquidityRegressions.py', doraise=True)"</verify>
  <done>Script has valid Python syntax, all functions intact</done>
</task>

<task type="auto">
  <name>Task 3: Verify comment line reduction achieved</name>
  <files>2_Scripts/4_Econometric/4.2_LiquidityRegressions.py</files>
  <action>
Verify that comment lines have been reduced:

python -c "
with open('2_Scripts/4_Econometric/4.2_LiquidityRegressions.py', 'r', encoding='utf-8') as f:
    lines = f.readlines()

# Count comment lines (starting with # and longer than 2 chars)
comment_lines = sum(1 for line in lines if line.strip().startswith('#') and len(line.strip()) > 2)
print(f'Comment lines after consolidation: {comment_lines}')
assert comment_lines <= 20, f'Too many comment lines: {comment_lines} (target: ~17)'
print('Comment reduction achieved')
"
  </action>
  <verify>python -c "with open('2_Scripts/4_Econometric/4.2_LiquidityRegressions.py', 'r') as f: lines = f.readlines(); comment_lines = sum(1 for line in lines if line.strip().startswith('#') and len(line.strip()) > 2); print(f'Comment lines: {comment_lines}'); assert comment_lines <= 20"</verify>
  <done>Comment lines reduced to ≤20 (from original 33)</done>
</task>

</tasks>

<verification>
1. Verify 4.2 line count ≤ 800: `wc -l 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py`
2. Verify script syntax: `python -c "import py_compile; py_compile.compile('2_Scripts/4_Econometric/4.2_LiquidityRegressions.py', doraise=True)"`
3. Verify functions intact: `grep -c "^def " 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py`
4. Verify comment reduction: Python script to count comment lines (should be ≤20)
</verification>

<success_criteria>
4.2_LiquidityRegressions.py line count reduced to ≤800 lines (target: -16 lines from 816)
Comment lines reduced from 33 to ≤20 while maintaining readability
Script functionality intact (all functions present, no syntax errors)
</success_criteria>

<output>
After completion, create `.planning/phases/18-complete-phase-13-refactoring/18-08-SUMMARY.md`
</output>
