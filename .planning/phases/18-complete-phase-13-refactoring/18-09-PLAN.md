---
phase: 18-complete-phase-13-refactoring
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py
autonomous: true
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "4.1.1_EstimateCeoClarity_CeoSpecific.py reduced to <800 lines (target: -5 lines)"
    - "Script maintains readability and functionality after line reduction"
    - "No code logic removed, only cosmetic changes"
  artifacts:
    - path: "2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py"
      provides: "Refactored script with minor line reduction"
      max_lines: 800
  key_links:
    - from: "2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py"
      to: "2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py"
      via: "Comment/blank line consolidation"
      pattern: "consolidated.*comments|reduced.*blank.*lines"
---

<objective>
Reduce 4.1.1_EstimateCeoClarity_CeoSpecific.py line count to <800 lines by consolidating verbose comments and reducing blank lines.

Purpose: Complete Phase 18 line count reduction target for 4.1.1 by removing ~5 lines through comment consolidation and blank line reduction. The script is currently at 805 lines (5 lines over the <800 target). Previous extraction of prepare_regression_data reduced it from 847 to 805 (-42 lines), leaving only -5 lines needed to reach target.

Output: 4.1.1 script at <800 lines with maintained readability and functionality
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-complete-phase-13-refactoring/18-VERIFICATION.md
@.planning/phases/18-complete-phase-13-refactoring/18-07-SUMMARY.md

# Current state
4.1.1_EstimateCeoClarity_CeoSpecific.py: 805 lines (need -5 lines to reach <800)
Previous work: prepare_regression_data() extracted to shared module, reducing line count from 847 to 805
Gap reason: Script is only 5 lines over target, likely due to verbose comments or excessive blank lines
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze 4.1.1 for line reduction opportunities</name>
  <files>2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py</files>
  <action>
  Analyze the script to identify opportunities for minor line reduction (target: -5 lines):

  1. Count comment lines and identify verbose patterns:
     python -c "
  with open('2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py', 'r', encoding='utf-8') as f:
      lines = f.readlines()

  # Count comment lines
  comment_lines = [i+1 for i, line in enumerate(lines) if line.strip().startswith('#') and len(line.strip()) > 2]
  print(f'Total comment lines: {len(comment_lines)}')

  # Count consecutive blank lines (sequences of 2+ blank lines)
  blank_sequences = []
  in_sequence = False
  start = 0
  for i, line in enumerate(lines):
      if line.strip() == '':
          if not in_sequence:
              in_sequence = True
              start = i
      else:
          if in_sequence:
              seq_len = i - start
              if seq_len > 1:
                  blank_sequences.append((start, seq_len))
              in_sequence = False

  # Check for sequence at end of file
  if in_sequence:
      seq_len = len(lines) - start
      if seq_len > 1:
          blank_sequences.append((start, seq_len))

  excess_blanks = sum(seq_len - 1 for _, seq_len in blank_sequences)
  print(f'Excess blank lines (sequences > 1): {excess_blanks}')

  # Find duplicate separator lines
  separators = [i+1 for i, line in enumerate(lines) if '=' in line and set(line.strip()) == {'='}]
  print(f'Separator lines found: {len(separators)}')
  "
  </action>
  <verify>
  python -c "
  import subprocess
  result = subprocess.run(['wc', '-l', '2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py'], capture_output=True, text=True)
  print(f'Current line count: {result.stdout.strip()}')
  "
  </verify>
  <done>Analysis complete with identified reduction opportunities</done>
</task>

<task type="auto">
  <name>Task 2: Apply minimal line reduction to 4.1.1</name>
  <files>2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py</files>
  <action>
  Apply minimal, conservative changes to reduce line count by ~5 lines:

  1. Consolidate consecutive blank lines (reduce to max 1 blank line between sections):
     - Find sequences of 2+ blank lines
     - Reduce to single blank line
     - Preserve logical section separation

  2. Consolidate duplicate separator lines:
     - If multiple "==========" lines appear consecutively, reduce to single line
     - Keep single separators between major sections

  3. Remove trailing blank lines at end of file (if more than 1):
     - Keep at most 1 blank line at end of file

  4. Consolidate redundant comment headers:
     - If section has both "# =======" and "# SECTION NAME", consolidate to single line
     - Example: Replace:
       ```
       # ============================================================================
       # Load Data
       ```
       With:
       ```
       # ============================================================================
       # Load Data
       ```

  5. Preserve essential elements:
     - All function docstrings (don't modify)
     - All import statements
     - All code logic (don't modify)
     - Single-line comments that clarify complex logic

  Target reduction: ~5 lines (from 805 to 800 or below)
  Conservative approach: Start with blank line consolidation only, move to other methods if more reduction needed
  </action>
  <verify>
  python -c "
  import subprocess
  result = subprocess.run(['wc', '-l', '2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py'], capture_output=True, text=True)
  lines = int(result.stdout.strip().split()[0])
  print(f'Current line count: {lines}')
  assert lines <= 800, f'Line count {lines} exceeds target 800 (need {lines - 800} more lines removed)'
  print('SUCCESS: Line count below 800 target')
  "
  </verify>
  <done>4.1.1 line count ≤ 800 lines (target: -5 lines reduction)</done>
</task>

<task type="auto">
  <name>Task 3: Verify script syntax and functionality</name>
  <files>2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py</files>
  <action>
  Run Python syntax check to ensure no errors introduced by line reduction:

  1. Syntax validation:
  python -c "import py_compile; py_compile.compile('2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py', doraise=True)"

  2. Verify function definitions remain intact (count should match previous):
  python -c "
  import ast
  with open('2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py', 'r') as f:
      tree = ast.parse(f.read())
  funcs = [node.name for node in ast.walk(tree) if isinstance(node, ast.FunctionDef)]
  print(f'Functions found: {len(funcs)}')
  # Check for key functions
  assert any('prepare_regression_data' not in name for name in funcs), 'prepare_regression_data should be imported, not defined'
  print('Key imports and functions intact')
  "

  3. Verify no code logic was removed (only comments/blank lines reduced)
  python -c "
  with open('2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py', 'r') as f:
      content = f.read()
  # Check that key code patterns still exist
  assert 'prepare_regression_data' in content, 'Missing prepare_regression_data call'
  assert 'df = prepare_regression_data' in content, 'Missing prepare_regression_data usage'
  assert 'from shared.regression_helpers import' in content, 'Missing shared module import'
  print('Code logic intact')
  "
  </action>
  <verify>python -c "import py_compile; py_compile.compile('2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py', doraise=True)"</verify>
  <done>Script has valid Python syntax and all code logic intact</done>
</task>

</tasks>

<verification>
1. Verify 4.1.1 line count ≤ 800: `wc -l 2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py`
2. Verify script syntax: `python -c "import py_compile; py_compile.compile('2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py', doraise=True)"`
3. Verify functions intact: `grep "^def " 2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py`
4. Verify shared module import: `grep "from shared.regression_helpers import" 2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py`
5. Verify code logic unchanged: Check that prepare_regression_data() call still exists
</verification>

<success_criteria>
4.1.1_EstimateCeoClarity_CeoSpecific.py line count reduced to ≤800 lines (target: -5 lines from 805)
Script syntax valid with no errors introduced
All code logic intact (only comments and blank lines reduced)
Essential readability maintained through section separators and key comments
</success_criteria>

<output>
After completion, create `.planning/phases/18-complete-phase-13-refactoring/18-09-SUMMARY.md`
</output>
