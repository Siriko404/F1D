---
phase: 18-complete-phase-13-refactoring
plan: 03
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified: [2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py, 2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py, 2_Scripts/4_Econometric/4.1.3_EstimateCeoClarity_Regime.py, 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py, 2_Scripts/4_Econometric/4.3_TakeoverHazards.py, 2_Scripts/3_Financial/3.1_FirmControls.py]
autonomous: true
must_haves:
  truths:
    - "Large scripts (800+ lines) broken into smaller focused modules - 6/9 target scripts reduced to <800 lines"
    - "Extracted code organized into shared modules or sub-modules with single responsibility"
    - "All extracted functions tested and scripts execute successfully with identical outputs"
  artifacts:
    - path: "2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py"
      max_lines: 800  # Current: 1089, target: <800
    - path: "2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py"
      max_lines: 800  # Current: 944, target: <800
    - path: "2_Scripts/4_Econometric/4.1.3_EstimateCeoClarity_Regime.py"
      max_lines: 800  # Current: 979, target: <800
    - path: "2_Scripts/4_Econometric/4.2_LiquidityRegressions.py"
      max_lines: 800  # Current: 998, target: <800
    - path: "2_Scripts/4_Econometric/4.3_TakeoverHazards.py"
      max_lines: 800  # Current: 945, target: <800
    - path: "2_Scripts/3_Financial/3.1_FirmControls.py"
      max_lines: 800  # Current: 978, target: <800
  key_links:
    - from: "4_Econometric/*.py"
      to: "shared/regression_helpers.py"
      via: "from shared.regression_helpers import build_regression_sample"
      pattern: "build_regression_sample\(.*"
    - from: "3_Financial/3.1_FirmControls.py"
      to: "shared/financial_utils.py"
      via: "from shared.financial_utils import extract_additional_helpers"
      pattern: "extract_additional_helpers\(.*"
---

<objective>
Extract additional code from large scripts to reduce line counts to <800 lines.

Purpose: Achieve Phase 13 success criterion by reducing 6 large scripts (currently 843-1089 lines) to <800 lines through code extraction to shared modules. Use enhanced build_regression_sample() from plan 18-02 to replace inline sample construction logic in Step 4 scripts. Extract remaining duplicate or extractable logic to shared modules or script-specific sub-modules.

Output: 6 large scripts reduced to <800 lines with extracted code organized in shared modules or sub-modules. All scripts execute successfully with identical outputs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/13-script-refactoring/13-VERIFICATION.md
@2_Scripts/shared/regression_helpers.py
@2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py
@2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py
@2_Scripts/4_Econometric/4.1.3_EstimateCeoClarity_Regime.py
@2_Scripts/4_Econometric/4.2_LiquidityRegressions.py
@2_Scripts/4_Econometric/4.3_TakeoverHazards.py
@2_Scripts/3_Financial/3.1_FirmControls.py
@2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py
@.planning/phases/18-complete-phase-13-refactoring/18-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Reduce Step 4 econometric scripts to <800 lines using enhanced build_regression_sample()</name>
  <files>2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py, 2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py, 2_Scripts/4_Econometric/4.1.3_EstimateCeoClarity_Regime.py, 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py, 2_Scripts/4_Econometric/4.3_TakeoverHazards.py</files>
  <action>
    For each of the 5 Step 4 econometric scripts (4.1.1, 4.1.2, 4.1.3, 4.2, 4.3):

    1. **Replace inline sample construction with build_regression_sample()**:
       - Locate inline data loading, filtering, and sample construction logic
       - Replace with call to build_regression_sample() from shared.regression_helpers
       - Define required_vars dictionary mapping variable types to column names
       - Convert inline filters to filter dicts format
       - Keep script-specific model specification inline (too varied for shared module)

    Example replacement pattern for 4.1.1:
    ```python
    # BEFORE (inline):
    # Load multiple parquet files
    # Merge by year
    # Filter on required variables
    # Check missing values
    # Build sample DataFrame
    # Drop rows with NaN in key variables

    # AFTER (using shared module):
    from shared.regression_helpers import build_regression_sample

    # Load and merge data (keep inline - varies per script)
    df = load_and_merge_data()

    # Define required variables
    required_vars = {
        'dependent': ['liquidity_ratio'],
        'independent': ['ceo_clarity'],
        'controls': ['firm_size', 'leverage', 'roa', 'market_to_book']
    }

    # Define filters
    filters = [
        {'column': 'year', 'operation': 'ge', 'value': 2010},
        {'column': 'year', 'operation': 'le', 'value': 2020},
        {'column': 'firm_size', 'operation': 'gt', 'value': 0}
    ]

    # Build sample with validation
    sample = build_regression_sample(
        df=df,
        required_vars=required_vars,
        filters=filters,
        year_range=(2010, 2020),
        min_sample_size=100,
        industry_classification='FF12',
        sic_col='sic'
    )
    ```

    2. **Extract regression result reporting logic**:
       - Each script has similar regression result summary generation
       - Extract common reporting pattern to reporting_utils (if not already there)
       - Replace inline reporting code with function calls

    3. **Extract model specification patterns**:
       - Scripts have multiple regression models with varying fixed effects
       - Extract model specification helper to regression_helpers if common pattern exists
       - Keep script-specific model lists inline (varied too much for shared module)

    4. **Keep script-specific logic inline**:
       - Different dependent variables per script
       - Different model specifications per script
       - Different fixed effects per script
       - These variations are intentional and shouldn't be shared

    Target line count reductions:
    - 4.1.1: 1089 → <800 (target: ~750 lines, remove ~340 lines)
    - 4.1.2: 944 → <800 (target: ~750 lines, remove ~195 lines)
    - 4.1.3: 979 → <800 (target: ~750 lines, remove ~230 lines)
    - 4.2: 998 → <800 (target: ~750 lines, remove ~250 lines)
    - 4.3: 945 → <800 (target: ~750 lines, remove ~195 lines)
  </action>
  <verify>
    For each script:
    1. Count lines: `wc -l 2_Scripts/4_Econometric/{script}.py` (must be <800)
    2. Run script: `python 2_Scripts/4_Econometric/{script}.py`
    3. Verify output files exist and have expected structure
    4. Check that regression results match previous run (record counts, coefficient values)
    5. Confirm build_regression_sample() is imported and used
    6. Verify error handling for missing variables still works
  </verify>
  <done>
    All 5 Step 4 scripts:
    - Import and use build_regression_sample() from shared.regression_helpers
    - Reduced to <800 lines each (total reduction: ~1210 lines)
    - Execute successfully with identical outputs to previous runs
    - Maintain script-specific logic (models, fixed effects, dependent variables)
  </done>
</task>

<task type="auto">
  <name>Reduce 3.0 and 3.1 Step 3 financial scripts to <800 lines</name>
  <files>2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py, 2_Scripts/3_Financial/3.1_FirmControls.py</files>
  <action>
    For 3.0_BuildFinancialFeatures.py (843 lines) and 3.1_FirmControls.py (978 lines):

    1. **Analyze extractable logic**:
       - Identify duplicate data loading patterns
       - Look for repeated financial calculations
       - Check for similar merge and validation logic
       - Find large inline helper functions

    2. **Extract to shared modules or script-specific sub-modules**:
       - If extractable logic is common across multiple Step 3 scripts: add to shared/financial_utils.py
       - If extractable logic is script-specific but substantial: create sub-module (e.g., 3.0_FeatureBuilders.py, 3.1_ControlBuilders.py)
       - If extractable logic is small and specific: keep inline

    3. **For 3.0_BuildFinancialFeatures.py (843 lines, need -43 lines to <800)**:
       - Extract feature calculation patterns to financial_utils if common
       - Create 3.0_FeatureBuilders.py sub-module with feature-specific functions
       - Move inline helper functions (>20 lines) to sub-module
       - Remove duplicate variable reference generation code

    4. **For 3.1_FirmControls.py (978 lines, need -179 lines to <800)**:
       - Extract control variable calculation logic to financial_utils
       - Identify and extract large inline functions for specific control types
       - Create 3.1_ControlBuilders.py sub-module if substantial logic
       - Consolidate merge logic across multiple financial data sources
       - Extract duplicate missing value handling patterns

    Target line count reductions:
    - 3.0: 843 → <800 (target: ~750 lines, remove ~93 lines)
    - 3.1: 978 → <800 (target: ~750 lines, remove ~228 lines)

    Strategy:
    - Prioritize extracting to shared/financial_utils.py if logic is common
    - Use script-specific sub-modules only if substantial (>100 lines extractable)
    - Focus on the largest inline functions and duplicate code blocks
    - Don't extract if logic is unique to the script and <50 lines
  </action>
  <verify>
    For each script:
    1. Count lines: `wc -l 2_Scripts/3_Financial/{script}.py` (must be <800)
    2. Run script: `python 2_Scripts/3_Financial/{script}.py`
    3. Verify output files exist with expected features/controls
    4. Check that extracted modules are imported correctly
    5. Confirm financial feature/control values match previous run
    6. Validate that all extracted functions are documented
  </verify>
  <done>
    Both Step 3 scripts:
    - Reduced to <800 lines each
    - Extracted logic organized in shared/financial_utils.py or script-specific sub-modules
    - Execute successfully with identical outputs
    - Extracted functions documented and tested
  </done>
</task>

</tasks>

<verification>
1. Check all 6 target scripts have <800 lines: `wc -l 2_Scripts/{4_Econometric,3_Financial}/*.py`
2. Verify each script imports and uses appropriate shared modules (regression_helpers, financial_utils, sub-modules)
3. Run all 6 scripts and confirm outputs match previous runs (checksum validation)
4. Check that extracted functions in shared modules have documentation
5. Validate that script-specific logic remains inline where appropriate
6. Confirm no duplicate code remains across scripts (or document why duplication is intentional)
7. Verify error handling and validation still works correctly after refactoring
</verification>

<success_criteria>
6 large scripts reduced to <800 lines (total reduction: ~1410 lines):
- 4.1.1: 1089 → <800 (build_regression_sample() replaces inline logic)
- 4.1.2: 944 → <800 (build_regression_sample() replaces inline logic)
- 4.1.3: 979 → <800 (build_regression_sample() replaces inline logic)
- 4.2: 998 → <800 (build_regression_sample() replaces inline logic)
- 4.3: 945 → <800 (build_regression_sample() replaces inline logic)
- 3.1: 978 → <800 (extracted to financial_utils/sub-module)

Extracted code organized in shared modules or script-specific sub-modules with single responsibility. All scripts execute successfully with identical outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/18-complete-phase-13-refactoring/18-03-SUMMARY.md`
</output>
