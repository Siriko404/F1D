---
phase: 09-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/shared/env_validation.py
  - 2_Scripts/shared/__init__.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Environment variable schema is defined for future use"
    - "Validation function checks required variables, types, and defaults"
    - "Schema includes WRDS credentials and API configuration (optional)"
    - "Validation raises clear errors on missing or invalid env vars"
    - "No breaking changes to existing code (env vars not currently used)"
  artifacts:
    - path: "2_Scripts/shared/env_validation.py"
      provides: "Environment variable schema and validation"
      exports: ["ENV_SCHEMA", "EnvValidationError", "validate_env_schema", "load_and_validate_env"]
  key_links:
    - from: "2_Scripts/shared/env_validation.py"
      to: "os.environ"
      via: "Direct read from environment"
      pattern: "os.environ.get"
---

<objective>
Implement environment variable validation schema for future use when adding external services (WRDS credentials, API keys).

Purpose: Addresses security concern SEC-02 (No Environment Variable Validation). While the current codebase doesn't use environment variables or .env files, adding them in the future for WRDS credentials or API keys would introduce security risks if not validated properly. This plan creates the validation infrastructure ready for future use.

Output: A reusable environment validation module with schema definition for common configuration variables.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-security-hardening/09-RESEARCH.md
@.planning/codebase/CONCERNS.md

# Research patterns to follow
Pattern 2: Environment Variable Schema Validation (from 09-RESEARCH.md)
- Define ENV_SCHEMA dictionary with required flags, types, defaults
- validate_env_schema() checks required vars, validates types, applies defaults
- load_and_validate_env() is the entry point (calls validate_env_schema)
- Raises EnvValidationError on failures with clear error messages

# Important: Do NOT modify existing scripts
- Current codebase doesn't use env vars yet
- This is preparatory infrastructure for future use
</context>

<tasks>

<task type="auto">
  <name>Create environment validation module</name>
  <files>2_Scripts/shared/env_validation.py</files>
  <action>
Create 2_Scripts/shared/env_validation.py with:

1. ENV_SCHEMA dictionary defining:
   - WRDS_USERNAME (optional, str)
   - WRDS_PASSWORD (optional, str) - with warning about plaintext
   - API_TIMEOUT_SECONDS (optional, int, default=30)
   - MAX_RETRIES (optional, int, default=3)
   - Add documentation field "description" for each variable

2. EnvValidationError exception class:
   - Inherits from Exception
   - Used for all validation failures

3. validate_env_schema(schema) function:
   - Iterate through schema entries
   - Check required vars are present (or use default)
   - Validate types (int, float, bool, str, Path)
   - Apply defaults for optional vars
   - Return validated dict

4. load_and_validate_env() function:
   - Call validate_env_schema(ENV_SCHEMA)
   - Print success message with variable count
   - Catch EnvValidationError, print to stderr, exit with code 1

Follow Pattern 2 from 09-RESEARCH.md exactly.

Use Python standard library only (os, sys, pathlib, typing).

Add docstrings explaining this is for future .env support.
</action>
  <verify>
python -c "from shared.env_validation import ENV_SCHEMA, EnvValidationError, load_and_validate_env; print('Module loaded successfully')"
  </verify>
  <done>
env_validation.py created with schema and validation functions ready for future use
  </done>
</task>

</tasks>

<verification>
1. Verify env_validation.py can be imported without errors
2. Verify ENV_SCHEMA defines all expected variables
3. Verify validate_env_schema() applies defaults correctly
4. Verify validate_env_schema() raises EnvValidationError on missing required vars
5. Verify load_and_validate_env() exits with non-zero code on failure
6. Test: Set WRDS_USERNAME env var, run validation, verify it's loaded
7. Test: Unset all env vars, run validation, verify defaults are applied
</verification>

<success_criteria>
1. ENV_SCHEMA includes WRDS_USERNAME, WRDS_PASSWORD, API_TIMEOUT_SECONDS, MAX_RETRIES
2. validate_env_schema() validates types correctly (int, str, float, bool)
3. Defaults are applied for optional variables
4. Clear error messages on validation failures
5. No changes to existing scripts (env vars not currently used)
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-hardening/09-02-SUMMARY.md`
</output>
