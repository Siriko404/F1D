---
phase: 09-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/shared/subprocess_validation.py
  - 2_Scripts/shared/__init__.py
  - 2_Scripts/1_Sample/1.0_BuildSampleManifest.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All subprocess executions validate script paths before running"
    - "Paths are validated to be within expected directory (2_Scripts/)"
    - "Absolute paths are used for subprocess execution"
    - "Path traversal attacks (../malicious.py) are prevented"
    - "Existing pipeline functionality is preserved"
  artifacts:
    - path: "2_Scripts/shared/subprocess_validation.py"
      provides: "Subprocess path validation functions"
      exports: ["validate_script_path", "run_validated_subprocess"]
    - path: "2_Scripts/1_Sample/1.0_BuildSampleManifest.py"
      provides: "Orchestrator with validated subprocess execution"
      contains: "validate_script_path"
  key_links:
    - from: "2_Scripts/1_Sample/1.0_BuildSampleManifest.py"
      to: "shared/subprocess_validation.py"
      via: "from shared.subprocess_validation import validate_script_path"
      pattern: "from shared.subprocess_validation import"
---

<objective>
Add subprocess path validation to prevent path traversal attacks and ensure only intended scripts can be executed.

Purpose: Addresses security concern SEC-01 (Subprocess Execution Without Validation) by validating all script paths before subprocess execution. The orchestrator script 1.0_BuildSampleManifest.py runs subscripts based on configuration, which could be vulnerable if paths are tampered with or if configuration contains malicious entries.

Output: A shared validation module and updated orchestrator script with secure subprocess execution.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-security-hardening/09-RESEARCH.md
@.planning/codebase/CONCERNS.md

# Research patterns to follow
Pattern 1: Subprocess Path Validation (from 09-RESEARCH.md)
- Validate script_path is within allowed directory
- Use absolute paths (Path.resolve())
- Check file extension (.py)
- Raise specific errors (ValueError, FileNotFoundError)
</context>

<tasks>

<task type="auto">
  <name>Create subprocess validation module</name>
  <files>2_Scripts/shared/subprocess_validation.py</files>
  <action>
Create 2_Scripts/shared/subprocess_validation.py with:

1. validate_script_path(script_path, allowed_dir) function:
   - Resolve script_path to absolute path (Path.resolve())
   - Check if file exists (raise FileNotFoundError if not)
   - Check if path is within allowed_dir using relative_to() (raise ValueError if not)
   - Check file extension is .py (raise ValueError if not)
   - Return validated absolute Path

2. run_validated_subprocess(script_path, allowed_dir, ...) function:
   - Call validate_script_path()
   - Execute subprocess.run() with validated absolute path
   - Return subprocess.CompletedProcess

Follow Pattern 1 from 09-RESEARCH.md exactly.

Use Python standard library only (pathlib, subprocess, sys).
</action>
  <verify>
python -c "from shared.subprocess_validation import validate_script_path, run_validated_subprocess; print('Module loaded successfully')"
  </verify>
  <done>
subprocess_validation.py created with validate_script_path() and run_validated_subprocess() functions
  </done>
</task>

<task type="auto">
  <name>Update orchestrator to use path validation</name>
  <files>2_Scripts/1_Sample/1.0_BuildSampleManifest.py</files>
  <action>
Update 2_Scripts/1_Sample/1.0_BuildSampleManifest.py to use subprocess validation:

1. Add import:
   from shared.subprocess_validation import validate_script_path

2. Define allowed directory constant near imports:
   ALLOWED_SCRIPT_DIR = Path(__file__).parent  # 2_Scripts/

3. In the subprocess execution loop (around line 164):
   BEFORE: subprocess.run([sys.executable, str(script_path)], ...)

   AFTER:
   try:
       # Validate script path before execution
       validated_path = validate_script_path(script_path, ALLOWED_SCRIPT_DIR)
   except (ValueError, FileNotFoundError) as e:
       print_dual(f"ERROR: Script validation failed: {e}")
       success = False
       break

   # Execute with validated absolute path
   result = subprocess.run([sys.executable, str(validated_path)], ...)

4. Ensure error handling preserves existing behavior:
   - Print error message to both terminal and log (using print_dual)
   - Set success = False and break on validation failure
   - Continue with existing returncode checking logic

Do NOT modify:
- Script configuration or step definitions
- Output handling logic
- Any other orchestrator behavior
  </action>
  <verify>
# Run orchestrator in dry-run mode (if available) or verify code inspection
python -c "
import re
with open('2_Scripts/1_Sample/1.0_BuildSampleManifest.py', 'r') as f:
    content = f.read()
    assert 'from shared.subprocess_validation import' in content
    assert 'validate_script_path' in content
    assert 'ALLOWED_SCRIPT_DIR' in content
    print('Validation: Path validation added to orchestrator')
"
  </verify>
  <done>
Orchestrator script validates all subprocess paths before execution
  </done>
</task>

</tasks>

<verification>
1. Verify subprocess_validation.py can be imported without errors
2. Verify orchestrator imports and uses validate_script_path()
3. Verify error handling preserves existing pipeline behavior
4. Run a test: Attempt to run a script outside 2_Scripts/ (should fail with clear error)
5. Run existing pipeline: Ensure no regressions in execution
</verification>

<success_criteria>
1. All subprocess.run() calls in 1.0_BuildSampleManifest.py use validated paths
2. Path validation prevents execution of scripts outside 2_Scripts/ directory
3. Absolute paths are used for all subprocess executions
4. Clear error messages on validation failures
5. Existing pipeline executes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-hardening/09-01-SUMMARY.md`
</output>
