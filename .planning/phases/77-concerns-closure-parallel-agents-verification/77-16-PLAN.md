---
phase: 77-concerns-closure-parallel-agents-verification
plan: 16
type: execute
wave: 1
depends_on: []
files_modified:
  - src/f1d/econometric/v1/4.3_TakeoverHazards.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Type errors in 4.3_TakeoverHazards.py reduced from 32 to <5"
    - "lifelines library type issues documented with type ignores"
    - "mypy passes on 4.3_TakeoverHazards.py with documented ignores"
    - "Survival analysis function signatures properly annotated"
  artifacts:
    - path: "src/f1d/econometric/v1/4.3_TakeoverHazards.py"
      provides: "Type-safe survival analysis script with documented lifelines issues"
      min_lines: 700
  key_links:
    - from: "src/f1d/econometric/v1/4.3_TakeoverHazards.py"
      to: "mypy"
      via: "Type checking verification"
      pattern: "mypy.*4\\.3_TakeoverHazards\\.py"
---

<objective>
Reduce type errors in 4.3_TakeoverHazards.py from 32 to <5 using scoped type ignores for lifelines library integration issues.

Purpose: Address lifelines library type stub gaps. The lifelines library lacks official type stubs, causing 32 type errors that cannot be fixed with annotations alone.

Output: Type-safe 4.3_TakeoverHazards.py with mypy passing using documented type ignores.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-concerns-closure-parallel-agents-verification/77-VERIFICATION.md
@.planning/phases/77-concerns-closure-parallel-agents-verification/77-03-PLAN.md
@.planning/codebase/type_ignore_audit.md

# Survival analysis script with lifelines integration
@src/f1d/econometric/v1/4.3_TakeoverHazards.py

# Type ignore audit showing lifelines issues
@.planning/codebase/type_ignore_audit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze lifelines type errors in 4.3_TakeoverHazards.py</name>
  <files>src/f1d/econometric/v1/4.3_TakeoverHazards.py</files>
  <action>
    Run mypy on 4.3_TakeoverHazards.py to identify and categorize type errors:

    1. Run: mypy src/f1d/econometric/v1/4.3_TakeoverHazards.py --show-error-codes --no-error-summary 2>&1 | tee .planning/codebase/takeover_hazards_errors_analysis.txt
    2. Categorize errors by type:
       - lifelines library import errors (import-untyped)
       - CoxPHFitter method call errors (call-arg, attr-defined)
       - Optional import fallback errors

    3. Identify which errors are from lifelines vs. fixable annotation issues

    Reference type_ignore_audit.md Category 2 (Optional Import Fallbacks) and Category 3 (Function Call Arguments).

    Primary issue: lifelines library lacks official type stubs.
  </action>
  <verify>
    test -f .planning/codebase/takeover_hazards_errors_analysis.txt
    grep -c "error:" .planning/codebase/takeover_hazards_errors_analysis.txt | grep -q "3[0-2]"
  </verify>
  <done>
    Type errors analyzed with ~32 errors confirmed, mostly lifelines-related
  </done>
</task>

<task type="auto">
  <name>Task 2: Add type annotations to survival analysis functions</name>
  <files>src/f1d/econometric/v1/4.3_TakeoverHazards.py</files>
  <action>
    Add proper type annotations to run_cox_ph() and run_fine_gray() functions:

    1. Import typing helpers (ensure present):
       ```python
       from typing import Dict, Any, Optional, Union, List, TypedDict
       ```

    2. Add return type annotations:
       ```python
       def run_cox_ph(
           df: pd.DataFrame,
           duration_col: str,
           event_col: str,
           covariates: List[str],
       ) -> Dict[str, Any]:
           """Run Cox proportional hazards model."""
           # Implementation
           return {
               "coefficients": {...},
               "confidence_intervals": {...},
               ...
           }
       ```

    3. Add TypedDict for return structure if helpful:
       ```python
       class SurvivalResult(TypedDict):
           coefficients: Dict[str, float]
           conf_int_lower: Dict[str, float]
           conf_int_upper: Dict[str, float]
           summary: str

       def run_cox_ph(...) -> SurvivalResult:
           # ...
       ```

    4. Fix any non-lifelines related errors (missing returns, etc.)

    Do NOT change business logic or survival analysis implementation from 77-03.
  </action>
  <verify>
    mypy src/f1d/econometric/v1/4.3_TakeoverHazards.py --show-error-codes 2>&1 | grep -v "import-untyped\\|attr-defined\\|call-arg" | grep -c "error:" | head -1 returns value < 10
  </verify>
  <done>
    Function signatures annotated, non-lifelines errors fixed
  </done>
</task>

<task type="auto">
  <name>Task 3: Add scoped type ignores for lifelines library issues</name>
  <files>src/f1d/econometric/v1/4.3_TakeoverHazards.py</files>
  <action>
    For lifelines-related errors that cannot be fixed (no official type stubs):

    1. Add scoped `# type: ignore[code]` comments at each error location:
       ```python
       from lifelines import CoxPHFitter  # type: ignore[import-untyped]  # lifelines lacks stubs

       cph = CoxPHFitter()  # type: ignore[attr-defined]  # lifelines stubs unavailable
       results = run_cox_ph(df, ...)  # type: ignore[call-arg]  # Complex lifelines signature
       ```

    2. Create TYPE ERROR BASELINE comment block at top of file:
       ```python
       # TYPE ERROR BASELINE: N remaining errors (down from 32)
       # TODO(77-16): lifelines library lacks official type stubs (https://github.com/CamDavidsonPilon/lifelines/issues/XXX)
       # Workaround: Scoped type: ignore comments for lifelines integration
       # - lines XX: import-untyped - lifelines import
       # - lines YY: attr-defined - CoxPHFitter attributes
       # - lines ZZ: call-arg - Method signature mismatches
       ```

    3. For Optional import fallback at top of file:
       ```python
       try:
           from lifelines import CoxPHFitter
       except ImportError:
           CoxPHFitter = None  # type: ignore[misc,assignment]  # Optional dependency fallback
       ```

    Goal: Reduce from 32 to <5 total documented errors (all lifelines-related).
  </action>
  <verify>
    mypy src/f1d/econometric/v1/4.3_TakeoverHazards.py succeeds (exit code 0 with type ignores)
    grep "type: ignore" src/f1d/econometric/v1/4.3_TakeoverHazards.py | grep -c "lifelines" > 0
  </verify>
  <done>
    lifelines errors documented with scoped type ignores, mypy passes
  </done>
</task>

</tasks>

<verification>
1. mypy passes on 4.3_TakeoverHazards.py (with documented type ignores)
2. Type error count reduced from 32 to <5
3. All lifelines-related errors have scoped type: ignore comments
4. TYPE ERROR BASELINE comment block documents lifelines limitation
5. Survival analysis functionality unchanged
</verification>

<success_criteria>
- Type errors in 4.3_TakeoverHazards.py reduced from 32 to <5
- mypy passes on 4.3_TakeoverHazards.py
- All lifelines errors documented with inline rationale
- Non-lifelines errors fixed with proper annotations
- No behavioral changes to survival analysis functions
</success_criteria>

<output>
After completion, create `.planning/phases/77-concerns-closure-parallel-agents-verification/77-16-SUMMARY.md`
</output>
