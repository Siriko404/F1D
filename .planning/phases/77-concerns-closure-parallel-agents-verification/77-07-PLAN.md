---
phase: 77-concerns-closure-parallel-agents-verification
plan: 07
type: execute
wave: 2
depends_on: []
files_modified:
  - src/f1d/shared/observability/stats.py
autonomous: true

must_haves:
  truths:
    - "Type errors in stats.py reduced from 56 to <10"
    - "mypy passes on stats.py with minimal errors"
    - "Type annotations added for complex nested dictionary structures"
    - "No functionality changes - only type improvements"
  artifacts:
    - path: "src/f1d/shared/observability/stats.py"
      provides: "Type-safe statistics module with reduced errors"
      min_lines: 5171
  key_links:
    - from: "src/f1d/shared/observability/stats.py"
      to: "mypy"
      via: "Type checking verification"
      pattern: "mypy.*stats\\.py"
---

<objective>
Reduce type errors in stats.py from 56 to <10 to achieve ROADMAP criterion 4 (mypy < 10 errors).

Purpose: Address "Stats Module Type Errors (56 errors)" concern in CONCERNS.md. The stats.py module has 47 of the 56 total type errors, primarily due to complex nested dictionary structures with inconsistent typing.

Output: Type-safe stats.py module with mypy passing with <10 errors.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

# Stats module with type errors
@src/f1d/shared/observability/stats.py

# Type error baseline
@.planning/codebase/type_errors_baseline.txt
@.planning/codebase/type_errors_summary.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze type errors in stats.py</name>
  <files>src/f1d/shared/observability/stats.py</files>
  <action>
    Run mypy on stats.py to identify and categorize type errors:

    1. Run: mypy src/f1d/shared/observability/stats.py --show-error-codes
    2. Capture output to .planning/codebase/stats_errors_analysis.txt
    3. Categorize errors by type:
       - Dictionary key/value type mismatches
       - Missing return type annotations
       - Any type usage
       - Union type issues

    4. Identify quick wins (errors fixable with simple annotations)
    5. Identify complex issues (require refactoring)

    Focus on errors that can be fixed without breaking functionality.
  </action>
  <verify>
    test -f .planning/codebase/stats_errors_analysis.txt
    grep -c "error:" .planning/codebase/stats_errors_analysis.txt > 0
  </verify>
  <done>
    Type errors analyzed and categorized
  </done>
</task>

<task type="auto">
  <name>Task 2: Add type annotations to high-impact functions</name>
  <files>src/f1d/shared/observability/stats.py</files>
  <action>
    Add proper type annotations to functions with most type errors:

    1. For each function in stats_errors_analysis.txt:
       - Add explicit return type annotation
       - Add type hints for complex dictionary return values
       - Use TypedDict for nested structures where appropriate

    2. Pattern for fixing dictionary returns:
       ```python
       from typing import TypedDict, Dict, Any, Union

       class StatsResult(TypedDict):
           mean: float
           median: float
           std: float
           count: int

       def compute_statistics(data: List[float]) -> StatsResult:
           # Implementation
           return {"mean": m, "median": med, "std": s, "count": c}
       ```

    3. Focus on top 10 functions causing most errors

    Do NOT change business logic or function behavior.
  </action>
  <verify>
    mypy src/f1d/shared/observability/stats.py --show-error-codes | grep -c "error:" returns < 10
  </verify>
  <done>
    Type annotations added, error count reduced to <10
  </done>
</task>

<task type="auto">
  <name>Task 3: Add type ignore comments for remaining errors</name>
  <files>src/f1d/shared/observability/stats.py</files>
  <action>
    For remaining errors that cannot be easily fixed:

    1. Add scoped `# type: ignore[code]` comments with specific error codes
    2. Document why each type ignore is needed in inline comments
    3. Create tracking comment block at top of file

    Pattern:
    ```python
    # TYPE ERROR BASELINE: 9 remaining errors (down from 56)
    # TODO(78): Fix remaining TypedDict issues in stats module
    # - lines 1234: assignment[arg-type] - complex nested dict
    # - lines 1456: return[value] - conditional return types

    def complex_function(data):
        result = {}  # type: ignore[assignment]  # TODO: fix nested dict type
        return result
    ```

    Only use type: ignore for genuine complexity, not laziness.
  </action>
  <verify>
    grep -c "# type: ignore" src/f1d/shared/observability/stats.py >= 0
    mypy src/f1d/shared/observability/stats.py succeeds (exit code 0 with type ignores)
  </verify>
  <done>
    Remaining errors documented with scoped type ignores
  </done>
</task>

</tasks>

<verification>
1. mypy passes on stats.py (with documented type ignores)
2. Type error count reduced from 56 to <10
3. No functionality changes
4. Type ignore comments are scoped and documented
</verification>

<success_criteria>
- Type errors in stats.py reduced from 56 to <10
- mypy passes on stats.py
- All type ignores documented with specific error codes
- No behavioral changes to stats functions
</success_criteria>

<output>
After completion, create `.planning/phases/77-concerns-closure-parallel-agents-verification/77-07-SUMMARY.md`
</output>
