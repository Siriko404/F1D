---
phase: 77-concerns-closure-parallel-agents-verification
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - src/f1d/text/verify_step2.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Type errors in verify_step2.py reduced from 30 to <5"
    - "Collection[str] indexing issues resolved"
    - "mypy passes on verify_step2.py with minimal errors"
    - "Dictionary operations have proper type hints"
  artifacts:
    - path: "src/f1d/text/verify_step2.py"
      provides: "Type-safe verification script with reduced errors"
      min_lines: 400
  key_links:
    - from: "src/f1d/text/verify_step2.py"
      to: "mypy"
      via: "Type checking verification"
      pattern: "mypy.*verify_step2\\.py"
---

<objective>
Reduce type errors in verify_step2.py from 30 to <5 to address Criterion 4 of Phase 77 verification (mypy < 10 errors total).

Purpose: Fix the second-highest error module in text module. The verify_step2.py script has Collection[str] indexing issues and missing type annotations.

Output: Type-safe verify_step2.py with mypy passing with <5 errors.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-concerns-closure-parallel-agents-verification/77-VERIFICATION.md
@.planning/phases/77-concerns-closure-parallel-agents-verification/77-07-PLAN.md
@.planning/phases/77-concerns-closure-parallel-agents-verification/77-13-PLAN.md

# Verification script with type errors
@src/f1d/text/verify_step2.py

# Type ignore audit for reference
@.planning/codebase/type_ignore_audit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze and categorize verify_step2.py type errors</name>
  <files>src/f1d/text/verify_step2.py</files>
  <action>
    Run mypy on verify_step2.py to identify and categorize type errors:

    1. Run: mypy src/f1d/text/verify_step2.py --show-error-codes --no-error-summary 2>&1 | tee .planning/codebase/verify_step2_errors_analysis.txt
    2. Categorize errors by type:
       - Collection[str] indexing issues (primary per VERIFICATION.md)
       - Dictionary key/value type mismatches
       - Missing return type annotations
       - Pandas operations

    3. Identify quick wins vs. complex issues

    Focus on Collection[str] indexing errors which are the primary issue per 77-VERIFICATION.md.
  </action>
  <verify>
    test -f .planning/codebase/verify_step2_errors_analysis.txt
    grep -c "error:" .planning/codebase/verify_step2_errors_analysis.txt | grep -q "2[0-9]\\|30"
  </verify>
  <done>
    Type errors analyzed with ~30 errors confirmed, primary issue identified as Collection indexing
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Collection[str] indexing and add type annotations</name>
  <files>src/f1d/text/verify_step2.py</files>
  <action>
    Fix Collection indexing errors and add proper type annotations:

    1. Import typing helpers (add if missing):
       ```python
       from typing import Dict, List, Any, Optional, Union, cast
       ```

    2. For Collection[str] indexing issues, change patterns like:
       ```python
       # Before (causes indexing error):
       def process_files(files: Collection[str]):
           for file in files:
               name = file.stem  # Error: Collection has no attribute `stem`

       # After (fixed):
       def process_files(files: List[Path] | Collection[Path]):
           for file in files:
               name = file.stem
       ```
       OR use specific types instead of Collection where possible.

    3. Add return type annotations to functions missing them:
       ```python
       def analyze_missing_values(df: pd.DataFrame) -> Dict[str, Dict[str, Any]]:
           # Implementation
           return missing
       ```

    4. For dictionary operations with dynamic keys, use explicit types:
       ```python
       result: Dict[str, Any] = {}
       result[key] = value  # type: ignore[index]  # If unavoidable
       ```

    Pattern reference from 77-07: Use typing.cast for type inference issues.

    Do NOT change business logic.
  </action>
  <verify>
    mypy src/f1d/text/verify_step2.py --show-error-codes 2>&1 | grep -c "error:" | head -1 returns value < 10
  </verify>
  <done>
    Collection indexing issues fixed, type annotations added
  </done>
</task>

<task type="auto">
  <name>Task 3: Add type ignore comments for remaining errors</name>
  <files>src/f1d/text/verify_step2.py</files>
  <action>
    For remaining errors that cannot be easily fixed:

    1. Add scoped `# type: ignore[code]` comments with specific error codes
    2. Document why each type ignore is needed in inline comments
    3. Create TYPE ERROR BASELINE comment block at top of file

    Pattern:
    ```python
    # TYPE ERROR BASELINE: N remaining errors (down from 30)
    # Remaining type errors are from pandas operations or library limitations
    ```

    Goal: Reduce from 30 to <5 total errors.
  </action>
  <verify>
    mypy src/f1d/text/verify_step2.py succeeds (exit code 0)
  </verify>
  <done>
    Remaining errors documented, mypy passes on verify_step2.py
  </done>
</task>

</tasks>

<verification>
1. mypy passes on verify_step2.py
2. Type error count reduced from 30 to <5
3. Collection[str] indexing issues resolved
4. No functionality changes
5. Type ignores documented with inline rationale
</verification>

<success_criteria>
- Type errors in verify_step2.py reduced from 30 to <5
- mypy passes on verify_step2.py
- Collection indexing issues resolved
- All type ignores documented
- No behavioral changes
</success_criteria>

<output>
After completion, create `.planning/phases/77-concerns-closure-parallel-agents-verification/77-14-SUMMARY.md`
</output>
