---
phase: 77-concerns-closure-parallel-agents-verification
plan: 09
type: execute
wave: 2
depends_on: []
files_modified:
  - requirements.txt
  - pyproject.toml
autonomous: true

must_haves:
  truths:
    - "types-pandas and types-psutil added to dependencies"
    - "import-untyped errors eliminated from mypy output"
    - "mypy configuration includes type stub packages"
    - "All external dependencies have type stubs where available"
  artifacts:
    - path: "requirements.txt"
      provides: "Updated dependencies with type stubs"
    - path: "pyproject.toml"
      provides: "Updated mypy configuration"
  key_links:
    - from: "pyproject.toml"
      to: "types-pandas, types-psutil"
      via: "mypy type stub packages"
      pattern: "types-"
---

<objective>
Add full type stub coverage to address "Full Type Stub Coverage" concern in CONCERNS.md.

Purpose: Missing type stubs for pandas and psutil cause `import-untyped` errors in mypy. Adding types-pandas and types-psutil packages enables stricter type checking.

Output: Complete type stub coverage with all import-untyped errors eliminated.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

# Current dependencies
@requirements.txt
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add type stub packages to requirements.txt</name>
  <files>requirements.txt</files>
  <action>
    Update requirements.txt to include type stub packages:

    1. Add type stub packages for untyped dependencies:
       - types-pandas
       - types-psutil
       - types-requests (if not present)
       - types-PyYAML (if not present)

    2. Verify the packages are compatible with Python version
    3. Add comments explaining purpose:
       ```txt
       # Type stubs for mypy strict checking
       types-pandas>=2.0.0  # Type stubs for pandas
       types-psutil>=6.0.0  # Type stubs for psutil
       ```

    Keep existing dependencies unchanged.
  </action>
  <verify>
    grep -c "types-pandas" requirements.txt returns 1
    grep -c "types-psutil" requirements.txt returns 1
  </verify>
  <done>
    Type stub packages added to requirements.txt
  </done>
</task>

<task type="auto">
  <name>Task 2: Update pyproject.toml mypy configuration</name>
  <files>pyproject.toml</files>
  <action>
    Update pyproject.toml mypy configuration:

    1. Update [tool.mypy] section to include type stub packages:
       ```toml
       [tool.mypy]
       # Type stub packages
       plugins = ["numpy.typing.mypy_plugin"]

       # Strictness settings
       strict = false  # Gradually enable strict mode
       warn_return_any = true
       warn_unused_configs = true
       disallow_untyped_defs = false  # Phase 78 target
       ```

    2. Add per-module overrides if needed:
       ```toml
       [[tool.mypy.overrides]]
       module = "pandas.*, psutil.*"
       ignore_missing_imports = false  # Now have type stubs
       ```

    3. Keep existing mypy configuration that works
  </action>
  <verify>
    grep -c "types-pandas" pyproject.toml > 0 or grep -c "plugins" pyproject.toml > 0
    python -c "import tomllib; f=open('pyproject.toml','rb'); print(tomllib.load(f)['tool']['mypy'])"
  </verify>
  <done>
    pyproject.toml updated for type stub usage
  </done>
</task>

<task type="auto">
  <name>Task 3: Install and verify type stubs</name>
  <files>requirements.txt, pyproject.toml</files>
  <action>
    Install type stub packages and verify mypy:

    1. Install new type stub packages:
       ```bash
       pip install types-pandas types-psutil
       ```

    2. Run mypy to verify import-untyped errors are reduced:
       ```bash
       mypy src/f1d/ --show-error-codes | grep "import-untyped"
       ```

    3. Check remaining type errors:
       ```bash
       mypy src/f1d/ 2>&1 | tee .planning/codebase/mypy_with_stubs.log
       ```

    4. Update type_errors_baseline.txt if needed

    Type stubs should eliminate import-untyped warnings for pandas and psutil.
  </action>
  <verify>
    pip show types-pandas returns package info
    pip show types-psutil returns package info
    mypy src/f1d/ completes with fewer import-untyped errors
  </verify>
  <done>
    Type stubs installed and verified, import-untyped errors reduced
  </done>
</task>

</tasks>

<verification>
1. types-pandas and types-psutil in requirements.txt
2. pyproject.toml configured for type stubs
3. pip show confirms packages are installed
4. mypy runs with reduced import-untyped errors
</verification>

<success_criteria>
- Type stub packages added to requirements.txt
- pyproject.toml updated for type stub usage
- import-untyped errors eliminated for pandas and psutil
- mypy completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/77-concerns-closure-parallel-agents-verification/77-09-SUMMARY.md`
</output>
