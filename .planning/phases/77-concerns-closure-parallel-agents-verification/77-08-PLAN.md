---
phase: 77-concerns-closure-parallel-agents-verification
plan: 08
type: execute
wave: 2
depends_on: []
files_modified:
  - src/f1d/financial/v1/3.0_BuildFinancialFeatures.py
  - src/f1d/financial/v1/3.1_FirmControls.py
  - src/f1d/financial/v1/3.2_MarketVariables.py
  - src/f1d/financial/v1/3.3_EventFlags.py
  - src/f1d/econometric/v1/4.1_DescriptiveStats.py
  - src/f1d/econometric/v1/4.2_CorrelationMatrix.py
  - src/f1d/shared/chunked_reader.py
autonomous: true

must_haves:
  truths:
    - "V1 legacy scripts have test coverage"
    - "Tests verify V1 financial scripts execute correctly"
    - "Tests verify V1 econometric scripts execute correctly"
    - "Coverage for V1 scripts > 40%"
  artifacts:
    - path: "tests/unit/test_v1_financial_features.py"
      provides: "V1 financial features tests"
      min_lines: 80
    - path: "tests/unit/test_v1_econometric_stats.py"
      provides: "V1 econometric stats tests"
      min_lines: 60
    - path: "tests/regression/test_v1_scripts_integration.py"
      provides: "V1 integration tests"
      min_lines: 80
  key_links:
    - from: "tests/unit/test_v1_*.py"
      to: "src/f1d/*/v1/*.py"
      via: "from f1d.*.v1 import"
      pattern: "from f1d\\..*\\.v1\\."
---

<objective>
Add test coverage for V1 legacy code to address "V1 Legacy Code test coverage" concern in CONCERNS.md.

Purpose: V1 scripts are still used but excluded from coverage calculation. Add tests to verify V1 financial and econometric scripts work correctly, ensuring bugs are detected.

Output: Unit and integration tests for V1 legacy scripts with >40% coverage.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

# V1 scripts to test
@src/f1d/financial/v1/*.py
@src/f1d/econometric/v1/*.py

# Existing test patterns
@tests/unit/test_financial_utils.py
@tests/regression/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests for V1 financial scripts</name>
  <files>tests/unit/test_v1_financial_features.py</files>
  <action>
    Create tests/unit/test_v1_financial_features.py:

    1. Test V1 financial scripts:
       - 3.0_BuildFinancialFeatures.py: Test feature building logic
       - 3.1_FirmControls.py: Test firm control variable calculation
       - 3.2_MarketVariables.py: Test market variable construction
       - 3.3_EventFlags.py: Test event flag creation

    2. For each script, test:
       - Data loading with fixtures
       - Feature calculation correctness
       - Output format matches expected schema
       - Error handling for missing inputs

    Pattern:
    ```python
    import pytest
    from f1d.financial.v1.c30 import build_financial_features
    import pandas as pd

    def test_build_financial_features_creates_expected_columns():
        df = pd.DataFrame({"gvkey": ["001"], "fyear": [2020]})
        result = build_financial_features(df)
        assert "market_cap" in result.columns
        assert "book_value" in result.columns
    ```

    Use fixtures from tests/conftest.py or create sample data fixtures.
  </action>
  <verify>
    pytest tests/unit/test_v1_financial_features.py -v returns PASSED
    Coverage for src/f1d/financial/v1/ increases
  </verify>
  <done>
    V1 financial tests created and passing
  </done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for V1 econometric scripts</name>
  <files>tests/unit/test_v1_econometric_stats.py</files>
  <action>
    Create tests/unit/test_v1_econometric_stats.py:

    1. Test V1 econometric scripts:
       - 4.1_DescriptiveStats.py: Test statistics calculation
       - 4.2_CorrelationMatrix.py: Test correlation matrix generation
       - 4.3_TakeoverHazards.py: Test survival analysis (covered in 77-03)
       - 4.0_LoadData.py: Test data loading

    2. For each script, test:
       - Statistical calculations with known inputs
       - Output formats (dict/DataFrame/JSON)
       - Edge cases (empty data, NaN handling)
       - Integration with survival analysis functions

    Use pytest fixtures for sample financial data.
  </action>
  <verify>
    pytest tests/unit/test_v1_econometric_stats.py -v returns PASSED
    Coverage for src/f1d/econometric/v1/ increases
  </verify>
  <done>
    V1 econometric tests created and passing
  </done>
</task>

<task type="auto">
  <name>Task 3: Create integration tests for V1 scripts</name>
  <files>tests/regression/test_v1_scripts_integration.py</files>
  <action>
    Create tests/regression/test_v1_scripts_integration.py:

    1. Test V1 end-to-end workflows:
       - Load data -> build features -> compute stats
       - Verify output files are created
       - Verify data flow between V1 scripts

    2. Test with minimal fixtures:
       - Use tests/fixtures/v1_sample_data.parquet if available
       - Otherwise create small test dataset

    3. Test dry-run execution:
       - Run each V1 script with --dry-run flag
       - Verify scripts complete without errors

    Pattern:
    ```python
    import pytest
    import subprocess

    V1_SCRIPTS = [
        "src/f1d/financial/v1/3.0_BuildFinancialFeatures.py",
        # ... all V1 scripts
    ]

    @pytest.mark.parametrize("script", V1_SCRIPTS)
    def test_v1_script_dryrun(script):
        result = subprocess.run(
            ["python", script, "--dry-run"],
            capture_output=True,
            timeout=60
        )
        assert result.returncode == 0
    ```
  </action>
  <verify>
    pytest tests/regression/test_v1_scripts_integration.py -v returns PASSED
    All V1 scripts complete dry-run successfully
  </verify>
  <done>
    V1 integration tests created and passing
  </done>
</task>

</tasks>

<verification>
1. Unit tests for V1 financial scripts pass
2. Unit tests for V1 econometric scripts pass
3. Integration tests verify V1 workflows
4. Coverage for V1 scripts > 40%
5. All V1 scripts pass dry-run tests
</verification>

<success_criteria>
- 2 unit test files created (financial, econometric)
- 1 integration test file created
- Coverage > 40% for V1 scripts
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/77-concerns-closure-parallel-agents-verification/77-08-SUMMARY.md`
</output>
