---
phase: 77-concerns-closure-parallel-agents-verification
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - src/f1d/text/tokenize_and_count.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Type errors in tokenize_and_count.py reduced from 86 to <10"
    - "mypy passes on tokenize_and_count.py with minimal errors"
    - "Complex dictionary return types properly annotated"
    - "Pandas operations have proper type hints"
  artifacts:
    - path: "src/f1d/text/tokenize_and_count.py"
      provides: "Type-safe tokenization script with reduced errors"
      min_lines: 600
  key_links:
    - from: "src/f1d/text/tokenize_and_count.py"
      to: "mypy"
      via: "Type checking verification"
      pattern: "mypy.*tokenize_and_count\\.py"
---

<objective>
Reduce type errors in tokenize_and_count.py from 86 to <10 to address Criterion 4 of Phase 77 verification (mypy < 10 errors total).

Purpose: Fix the highest-error module identified in 77-VERIFICATION.md. The tokenize_and_count.py script has complex dictionary structures, pandas operations, and missing type annotations.

Output: Type-safe tokenize_and_count.py with mypy passing with <10 errors.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-concerns-closure-parallel-agents-verification/77-VERIFICATION.md
@.planning/phases/77-concerns-closure-parallel-agents-verification/77-07-PLAN.md

# Text module with type errors
@src/f1d/text/tokenize_and_count.py

# Type ignore audit for reference
@.planning/codebase/type_ignore_audit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze type errors in tokenize_and_count.py</name>
  <files>src/f1d/text/tokenize_and_count.py</files>
  <action>
    Run mypy on tokenize_and_count.py to identify and categorize type errors:

    1. Run: mypy src/f1d/text/tokenize_and_count.py --show-error-codes --no-error-summary 2>&1 | tee .planning/codebase/tokenize_errors_analysis.txt
    2. Categorize errors by type:
       - Dictionary key/value type mismatches
       - Missing return type annotations on functions
       - Pandas DataFrame operations
       - Collection/list indexing issues
       - Assignment type mismatches

    3. Identify quick wins (errors fixable with simple annotations)
    4. Identify issues requiring type: ignore (library limitations)

    Focus on the highest-impact error categories first.
  </action>
  <verify>
    test -f .planning/codebase/tokenize_errors_analysis.txt
    grep -c "error:" .planning/codebase/tokenize_errors_analysis.txt | grep -q "8[0-6]"
  </verify>
  <done>
    Type errors analyzed and categorized with 86 errors confirmed
  </done>
</task>

<task type="auto">
  <name>Task 2: Add type annotations to high-impact functions</name>
  <files>src/f1d/text/tokenize_and_count.py</files>
  <action>
    Add proper type annotations to functions with the most type errors:

    1. Import typing helpers at top of file (add if missing):
       ```python
       from typing import Dict, List, Any, Optional, Union, Tuple, TypedDict, cast
       import pandas as pd
       import numpy as np
       ```

    2. For functions returning complex dictionaries, use TypedDict or explicit Dict types:
       ```python
       # Define TypedDict for common return types
       class TokenStats(TypedDict):
           total_tokens: int
           vocab_hits: int
           categories: Dict[str, int]

       def compute_token_stats(df: pd.DataFrame) -> TokenStats:
           # Implementation
           return {"total_tokens": n, "vocab_hits": h, "categories": c}
       ```

    3. For pandas operations, use cast() where mypy cannot infer types:
       ```python
       result = cast(pd.DataFrame, df.groupby("col").agg(...))
       ```

    4. Add return types to all functions missing them (focus on top 10 by error count)

    Pattern reference from 77-07: Use typing.cast for pandas type inference issues.

    Do NOT change business logic or function behavior.
  </action>
  <verify>
    mypy src/f1d/text/tokenize_and_count.py --show-error-codes 2>&1 | grep -c "error:" | head -1 returns value < 20
  </verify>
  <done>
    Type annotations added, error count significantly reduced
  </done>
</task>

<task type="auto">
  <name>Task 3: Add type ignore comments for remaining errors</name>
  <files>src/f1d/text/tokenize_and_count.py</files>
  <action>
    For remaining errors that cannot be easily fixed (library limitations, complex pandas patterns):

    1. Add scoped `# type: ignore[code]` comments with specific error codes
    2. Document why each type ignore is needed in inline comments
    3. Create TYPE ERROR BASELINE comment block at top of file

    Pattern:
    ```python
    # TYPE ERROR BASELINE: N remaining errors (down from 86)
    # TODO(77-13): Remaining type errors are library limitations
    # - lines XXX: assignment[pandas] - Complex DataFrame operations
    # - lines YYY: index[dict] - Dynamic key assignment

    # Inline comment pattern
    result[name] = value  # type: ignore[index]  # Dynamic key, library limitation
    ```

    4. Use type: ignore only for genuine library limitations, not fixable annotations

    Reference type_ignore_audit.md Category 5 (Pandas operations) and Category 6 (Dictionary indexing).

    Goal: Reduce from 86 to <10 total errors.
  </action>
  <verify>
    mypy src/f1d/text/tokenize_and_count.py succeeds (exit code 0 with type ignores)
    grep -c "type: ignore" src/f1d/text/tokenize_and_count.py >= 0
  </verify>
  <done>
    Remaining errors documented with scoped type ignores, mypy passes
  </done>
</task>

</tasks>

<verification>
1. mypy passes on tokenize_and_count.py (with documented type ignores)
2. Type error count reduced from 86 to <10
3. No functionality changes
4. Type ignore comments are scoped with error codes and inline rationale
5. TYPE ERROR BASELINE comment block added at top of file
</verification>

<success_criteria>
- Type errors in tokenize_and_count.py reduced from 86 to <10
- mypy passes on tokenize_and_count.py
- All type ignores documented with specific error codes and inline comments
- No behavioral changes to tokenize_and_count functions
- Error analysis document created in .planning/codebase/tokenize_errors_analysis.txt
</success_criteria>

<output>
After completion, create `.planning/phases/77-concerns-closure-parallel-agents-verification/77-13-SUMMARY.md`
</output>
