---
phase: 77-concerns-closure-parallel-agents-verification
plan: 15
type: execute
wave: 1
depends_on: []
files_modified:
  - src/f1d/text/construct_variables.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Type errors in construct_variables.py reduced from 19 to <5"
    - "Complex pandas operations properly typed"
    - "mypy passes on construct_variables.py with minimal errors"
    - "Variable construction functions have proper return types"
  artifacts:
    - path: "src/f1d/text/construct_variables.py"
      provides: "Type-safe variable construction script"
      min_lines: 500
  key_links:
    - from: "src/f1d/text/construct_variables.py"
      to: "mypy"
      via: "Type checking verification"
      pattern: "mypy.*construct_variables\\.py"
---

<objective>
Reduce type errors in construct_variables.py from 19 to <5 to address Criterion 4 of Phase 77 verification (mypy < 10 errors total).

Purpose: Fix the third text module file with complex pandas operations and missing type annotations.

Output: Type-safe construct_variables.py with mypy passing with <5 errors.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-concerns-closure-parallel-agents-verification/77-VERIFICATION.md
@.planning/phases/77-concerns-closure-parallel-agents-verification/77-07-PLAN.md
@.planning/phases/77-concerns-closure-parallel-agents-verification/77-13-PLAN.md
@.planning/phases/77-concerns-closure-parallel-agents-verification/77-14-PLAN.md

# Variable construction script with type errors
@src/f1d/text/construct_variables.py

# Type ignore audit for reference
@.planning/codebase/type_ignore_audit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze construct_variables.py type errors</name>
  <files>src/f1d/text/construct_variables.py</files>
  <action>
    Run mypy on construct_variables.py to identify and categorize type errors:

    1. Run: mypy src/f1d/text/construct_variables.py --show-error-codes --no-error-summary 2>&1 | tee .planning/codebase/construct_variables_errors_analysis.txt
    2. Categorize errors by type:
       - Complex pandas operations (primary per VERIFICATION.md)
       - Dictionary key/value type mismatches
       - Missing return type annotations

    3. Identify fixable vs. requires-type-ignore issues

    Focus on pandas operation errors which are the primary issue per 77-VERIFICATION.md.
  </action>
  <verify>
    test -f .planning/codebase/construct_variables_errors_analysis.txt
    grep -c "error:" .planning/codebase/construct_variables_errors_analysis.txt | grep -q "1[0-9]\\|19"
  </verify>
  <done>
    Type errors analyzed with ~19 errors confirmed
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix pandas operations and add type annotations</name>
  <files>src/f1d/text/construct_variables.py</files>
  <action>
    Fix pandas operation errors and add proper type annotations:

    1. Import typing helpers (add if missing):
       ```python
       from typing import Dict, List, Any, Optional, Union, cast
       import pandas as pd
       ```

    2. For pandas operations that mypy cannot infer, use cast():
       ```python
       # Before (causes error):
       result = df.groupby("col").agg({"val": "mean"})

       # After (fixed):
       result = cast(pd.DataFrame, df.groupby("col").agg({"val": "mean"}))
       ```

    3. Add return type annotations:
       ```python
       def construct_linguistic_variables(df: pd.DataFrame) -> pd.DataFrame:
           # Implementation
           return result_df
       ```

    4. For complex dictionary returns, use explicit types:
       ```python
       def compute_stats() -> Dict[str, Any]:
           return {"mean": m, "std": s, "count": c}
       ```

    Pattern reference from 77-07: Use typing.cast for pandas type inference issues.

    Do NOT change business logic.
  </action>
  <verify>
    mypy src/f1d/text/construct_variables.py --show-error-codes 2>&1 | grep -c "error:" | head -1 returns value < 10
  </verify>
  <done>
    Pandas operations fixed with cast(), type annotations added
  </done>
</task>

<task type="auto">
  <name>Task 3: Add type ignore comments for remaining errors</name>
  <files>src/f1d/text/construct_variables.py</files>
  <action>
    For remaining errors that cannot be easily fixed (pandas library limitations):

    1. Add scoped `# type: ignore[code]` comments with specific error codes
    2. Document why each type ignore is needed
    3. Create TYPE ERROR BASELINE comment block at top of file

    Goal: Reduce from 19 to <5 total errors.
  </action>
  <verify>
    mypy src/f1d/text/construct_variables.py succeeds (exit code 0)
  </verify>
  <done>
    Remaining errors documented, mypy passes
  </done>
</task>

</tasks>

<verification>
1. mypy passes on construct_variables.py
2. Type error count reduced from 19 to <5
3. Pandas operations properly typed
4. No functionality changes
5. Type ignores documented
</verification>

<success_criteria>
- Type errors in construct_variables.py reduced from 19 to <5
- mypy passes on construct_variables.py
- Pandas operation types properly annotated
- All type ignores documented
- No behavioral changes
</success_criteria>

<output>
After completion, create `.planning/phases/77-concerns-closure-parallel-agents-verification/77-15-SUMMARY.md`
</output>
