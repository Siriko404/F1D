---
phase: 77-concerns-closure-parallel-agents-verification
plan: 11
type: execute
wave: 3
depends_on: [77-02]
files_modified:
  - src/f1d/sample/1.1_CleanMetadata.py
  - src/f1d/sample/1.2_LinkEntities.py
  - src/f1d/sample/1.3_BuildTenureMap.py
  - src/f1d/sample/1.4_AssembleManifest.py
  - src/f1d/financial/v1/3.0_BuildFinancialFeatures.py
  - src/f1d/financial/v1/3.1_FirmControls.py
  - src/f1d/financial/v1/3.2_MarketVariables.py
  - src/f1d/shared/chunked_reader.py
  - src/f1d/econometric/v1/4.3_TakeoverHazards.py
autonomous: true

must_haves:
  truths:
    - "Type ignore comments reduced from ~40 to <20"
    - "Remaining type ignores have documented rationale"
    - "mypy passes on all files with documented type ignores"
    - "No silent type errors hidden without documentation"
  artifacts:
    - path: ".planning/codebase/type_ignore_audit.md"
      provides: "Documentation for all type ignore comments"
      min_lines: 100
  key_links:
    - from: ".planning/codebase/type_ignore_audit.md"
      to: "src/f1d/**/*.py"
      via: "Type ignore documentation"
      pattern: "# type: ignore"
---

<objective>
Reduce and document type ignore comments to address "Type Ignore Comments (Medium Priority)" concern in CONCERNS.md.

Purpose: 40+ `# type: ignore` comments suppress mypy errors. After 77-02 eliminates dynamic import comments, address remaining type ignores by fixing underlying issues or documenting rationale.

Output: Reduced and fully documented type ignore comments with <20 remaining.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONCERNS.md

# Files with type ignores (after 77-02 removes dynamic import comments)
@src/f1d/sample/*.py
@src/f1d/shared/chunked_reader.py
@src/f1d/econometric/v1/4.3_TakeoverHazards.py
@src/f1d/financial/v1/*.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all type ignore comments</name>
  <files>.planning/codebase/type_ignore_audit.md</files>
  <action>
    Create comprehensive audit of all type ignore comments:

    1. Scan codebase for type ignores:
       ```bash
       grep -rn "# type: ignore" src/f1d/ > .planning/codebase/all_type_ignores.txt
       ```

    2. Categorize each type ignore:
       - Dynamic imports (should be removed by 77-02)
       - Complex pandas operations (document, keep)
       - Third-party library issues (document, keep)
       - Fixable type errors (fix in Task 2)
       - Legacy code patterns (document, defer)

    3. Create .planning/codebase/type_ignore_audit.md with:
       - Total count of type ignores
       - Categories and counts
       - Action plan for each category
       - Priority fixes (quick wins)

    Format:
    ```markdown
    # Type Ignore Audit (2026-02-14)

    ## Summary
    - Total type ignores: 42
    - After 77-02 (dynamic imports): 34 remaining
    - Target: <20

    ## Categories
    1. Fixable (8): Simple type annotations missing
    2. Pandas complexity (15): DataFrame operations
    3. Third-party issues (6): psutil, etc.
    4. Legacy (5): V1 code patterns
    ```
  </action>
  <verify>
    test -f .planning/codebase/type_ignore_audit.md
    grep -c "type: ignore" .planning/codebase/all_type_ignores.txt > 0
  </verify>
  <done>
    Type ignore audit created with categorization
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix fixable type errors</name>
  <files>src/f1d/sample/1.1_CleanMetadata.py, src/f1d/sample/1.2_LinkEntities.py, src/f1d/sample/1.3_BuildTenureMap.py, src/f1d/sample/1.4_AssembleManifest.py, src/f1d/shared/chunked_reader.py</files>
  <action>
    Fix type errors that can be resolved with simple annotations:

    1. For each "fixable" type ignore from audit:
       - Remove the `# type: ignore` comment
       - Add proper type annotation
       - Run mypy to verify fix

    2. Common fixes:
       ```python
       # Before
       def process_data(df):  # type: ignore[arg-type]
           return df

       # After
       def process_data(df: pd.DataFrame) -> pd.DataFrame:
           return df
       ```

    3. Target: Fix at least 8-10 type ignores

    Only fix clear-cut cases. Leave complex pandas operations for documentation.
  </action>
  <verify>
    mypy src/f1d/sample/ src/f1d/shared/chunked_reader.py --show-error-codes | grep "type: ignore" | wc -l shows reduction
    git diff shows type annotations added
  </verify>
  <done>
    Fixable type errors resolved with proper annotations
  </done>
</task>

<task type="auto">
  <name>Task 3: Document remaining type ignores with rationale</name>
  <files>src/f1d/sample/*.py, src/f1d/shared/chunked_reader.py, src/f1d/financial/v1/*.py, src/f1d/econometric/v1/4.3_TakeoverHazards.py</files>
  <action>
    Add inline documentation for all remaining type ignores:

    1. Replace bare `# type: ignore` with scoped, documented versions:
       ```python
       # Before
       result = complex_pandas_op(df)  # type: ignore

       # After
       # type: ignore[arg-type] - pandas typing too strict for this pattern
       result = complex_pandas_op(df)
       ```

    2. Add module-level tracking comments:
       ```python
       """
       TYPE ERROR BASELINE: 12 remaining type ignores

       Documented reasons:
       - Lines 45-67: pandas.DataFrame operations (typing limitation)
       - Line 123: psutil.Process memory_info (third-party stub issue)
       - Lines 200-210: Legacy V1 pattern, defer to Phase 78
       """
       ```

    3. Update type_ignore_audit.md with final count and status

    Ensure every remaining type ignore has a documented rationale.
  </action>
  <verify>
    grep -c "# type: ignore" src/f1d/ returns < 20
    grep -c "pandas typing" src/f1d/**/*.py > 0
    grep -c "TYPE ERROR BASELINE" src/f1d/**/*.py > 0
  </verify>
  <done>
    All remaining type ignores documented with rationale
  </done>
</task>

</tasks>

<verification>
1. Type ignore audit created
2. Fixable type errors resolved
3. Remaining type ignores < 20
4. All type ignores have documented rationale
5. mypy passes with documented type ignores
</verification>

<success_criteria>
- Type ignore audit document created
- Type ignores reduced from ~40 to <20
- All remaining type ignores documented
- No bare type: ignore without rationale
</success_criteria>

<output>
After completion, create `.planning/phases/77-concerns-closure-parallel-agents-verification/77-11-SUMMARY.md`
</output>
