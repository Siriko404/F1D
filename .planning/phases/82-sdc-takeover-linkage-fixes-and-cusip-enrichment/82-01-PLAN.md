---
phase: 82-sdc-takeover-linkage-fixes
plan: 01
type: execute
wave: 1
depends_on: [81]
autonomous: true
---

<objective>
Fix SDC takeover event linkage bugs discovered during Phase 81 audit and improve CUSIP coverage through CCM enrichment.

Purpose: Enable accurate takeover event detection for H8 hypothesis analysis.
Output: Working takeover event flags with ~2% match rate (up from 0%).
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix anomaly detection bug in BuildFinancialFeatures</name>
  <files>src/f1d/financial/v1/3.0_BuildFinancialFeatures.py</files>
  <action>
    Fix index misalignment bug in `detect_anomalies_zscore()` and `detect_anomalies_iqr()` functions:

    **Bug:** `series = df[col].dropna()` creates series with different index than `df`, then `df[anomaly_mask]` fails.

    **Fix:** Change `df[anomaly_mask]` to `series[anomaly_mask]` on lines 177 and 233.
  </action>
  <verify>
    - Script completes without IndexingError
    - stats.json is generated successfully
  </verify>
  <done>
    Anomaly detection functions work correctly with data containing nulls.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix manifest path in financial scripts</name>
  <files>
    src/f1d/financial/v1/3.0_BuildFinancialFeatures.py
    src/f1d/financial/v1/3.1_FirmControls.py
    src/f1d/financial/v1/3.2_MarketVariables.py
    src/f1d/financial/v1/3.3_EventFlags.py
  </files>
  <action>
    Fix all financial scripts to load manifest from correct directory:

    **Bug:** Scripts load from `1.0_BuildSampleManifest` (no CUSIP column)
    **Fix:** Change to `1.4_AssembleManifest` (has CUSIP column)

    Update `get_latest_output_dir()` calls in all 4 scripts.
  </action>
  <verify>
    - All scripts load manifest with CUSIP column
    - "Calls with CUSIP" shows > 0 in EventFlags output
  </verify>
  <done>
    All financial scripts load manifest with CUSIP column included.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add CUSIP to manifest assembly</name>
  <files>src/f1d/sample/1.4_AssembleManifest.py</files>
  <action>
    Add `cusip` column to manifest output:

    1. Add `cusip` to columns list when loading metadata (line 189-202)
    2. Add CCM file path to `setup_paths()`
    3. Add CCM CUSIP enrichment logic to fill missing CUSIPs from CCM data
  </action>
  <verify>
    - Manifest has `cusip` column
    - CUSIP coverage is 100% (after CCM enrichment)
  </verify>
  <done>
    Manifest includes CUSIP with 100% coverage (linked metadata + CCM fallback).
  </done>
</task>

<task type="auto">
  <name>Task 4: Fix empty string CUSIP handling in EventFlags</name>
  <files>src/f1d/financial/v1/3.3_EventFlags.py</files>
  <action>
    Fix `load_manifest()` to handle empty string CUSIPs:

    **Bug:** Empty strings pass `notna()` check but fail SDC matching
    **Fix:** Replace empty strings with None after extracting CUSIP6
  </action>
  <verify>
    - "Calls with CUSIP" count matches actual non-empty CUSIP count
    - Takeover events > 0
  </verify>
  <done>
    Empty string CUSIPs are properly handled as null values.
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify takeover event detection</name>
  <files>.planning/verification/82-takeover-audit.json</files>
  <action>
    Run EventFlags script and verify takeover events:

    1. Run `python src/f1d/financial/v1/3.3_EventFlags.py`
    2. Verify takeover events ~2,300+ (2% match rate)
    3. Verify Friendly/Uninvited classification
    4. Create audit JSON with results
  </action>
  <verify>
    - Takeover events: 2,000+ (up from 0)
    - Match rate: ~2%
    - Friendly and Uninvited events present
  </verify>
  <done>
    Takeover event detection working with expected match rate.
  </done>
</task>

</tasks>

<verification>
1. All 5 bugs fixed with commits
2. Takeover events: 2,000+ (up from 0)
3. CUSIP coverage: 100% (up from 82.7%)
4. Audit JSON generated with verification metrics
</verification>

<success_criteria>
- Zero takeover events bug FIXED
- Anomaly detection crash FIXED
- CUSIP coverage improved to 100%
- Takeover match rate ~2%
</success_criteria>

<output>
After completion, create `.planning/phases/82-sdc-takeover-linkage-fixes-and-cusip-enrichment/82-01-SUMMARY.md`
</output>
