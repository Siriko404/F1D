---
phase: 75-gap-closure
plan: 05
type: execute
wave: 3
depends_on: ["75-01", "75-02", "75-03", "75-04"]
files_modified:
  - .planning/v6.0-MILESTONE-AUDIT.md
  - .planning/STATE.md
autonomous: false

must_haves:
  truths:
    - "Zero sys.path.insert() calls in src/f1d/ codebase"
    - "Zero from shared.* imports in tests/"
    - "LoggingSettings integrated with configure_logging()"
    - "All sample scripts import using f1d.shared.* namespace"
    - "All tests execute without PYTHONPATH manipulation"
    - "mypy passes on all migrated files"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated project state"
    - path: ".planning/v6.1-MILESTONE-AUDIT.md"
      provides: "Verification report"
  key_links:
    - from: "All migrated files"
      to: "src/f1d/shared/"
      via: "proper f1d.shared.* imports"
---

<objective>
Perform final verification of all gap closure work and update project state to mark v6.1 milestone complete.

Purpose: Ensure all gaps identified in v6.0 audit have been successfully closed and document the verification results.

Output: Updated STATE.md, v6.1 milestone audit report, and verified compliance.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v6.0-MILESTONE-AUDIT.md

# Success Criteria (from ROADMAP)

1. Zero `sys.path.insert()` calls in src/f1d/ codebase
2. Zero `from shared.*` imports in tests/ (all use `from f1d.shared.*`)
3. LoggingSettings integrated with configure_logging() for unified config
4. All sample scripts import using proper `f1d.shared.*` namespace
5. All tests execute without PYTHONPATH manipulation
6. mypy passes on all migrated files
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify import pattern compliance</name>
  <files></files>
  <action>
Run comprehensive verification checks for import patterns across the codebase.

Execute the following verification commands and document results:

1. Check for sys.path.insert in src/f1d/:
```bash
grep -r "sys.path.insert" src/f1d/ --include="*.py" || echo "PASS: No sys.path.insert found"
```

2. Check for legacy shared.* imports in tests/:
```bash
grep -r "from shared\." tests/ --include="*.py" || echo "PASS: No legacy imports found"
```

3. Check for legacy shared.* imports in src/f1d/:
```bash
grep -r "from shared\." src/f1d/ --include="*.py" || echo "PASS: No legacy imports found"
```

4. Verify proper f1d.shared.* imports in sample scripts:
```bash
grep -c "from f1d.shared\." src/f1d/sample/*.py
```

5. Verify proper imports in tests:
```bash
grep -c "from f1d.shared\." tests/**/*.py 2>/dev/null | head -20
```

Document the count of files verified and any issues found.
  </action>
  <verify>All grep commands return expected results (0 legacy patterns, positive count of proper patterns)</verify>
  <done>Import pattern verification complete with documented results</done>
</task>

<task type="auto">
  <name>Task 2: Run mypy on all migrated files</name>
  <files></files>
  <action>
Run mypy type checking on all files modified during Phase 75.

Execute:
```bash
mypy src/f1d/sample/ --no-error-summary 2>&1 | tail -5
mypy src/f1d/shared/logging/ --no-error-summary 2>&1 | tail -5
```

Document:
- Number of files checked
- Number of errors (if any)
- Whether pre-existing errors were introduced by this phase

Note: Pre-existing type errors in sample/ are acceptable (Tier 3 modules).
Focus on ensuring no NEW errors were introduced by the migration.
  </action>
  <verify>mypy runs without errors on logging module; sample scripts may have pre-existing errors</verify>
  <done>mypy verification complete, no new errors introduced</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite</name>
  <files></files>
  <action>
Run the complete test suite to verify all tests execute correctly.

Execute:
```bash
pytest tests/ --collect-only -q 2>&1 | tail -3
pytest tests/unit/ -v --tb=short 2>&1 | tail -20
pytest tests/integration/ -v --tb=short 2>&1 | tail -20
```

Document:
- Total tests collected
- Tests passed/failed/skipped
- Any import errors or collection errors
- Any environment-related failures

The goal is to verify tests execute, not necessarily that all pass (some may have data dependencies).
  </action>
  <verify>pytest collects all tests without import errors</verify>
  <done>Test suite executes without collection/import errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 75 gap closure:
- 5 sample scripts migrated to f1d.shared.* imports
- 18+ test files migrated to f1d.shared.* imports
- LoggingSettings integrated with configure_logging()
- Econometric test compatibility resolved (if applicable)
- All verification checks passed</what-built>
  <how-to-verify>
1. Review the verification output from Tasks 1-3
2. Confirm all success criteria from ROADMAP are met:
   - [ ] Zero sys.path.insert() calls in src/f1d/
   - [ ] Zero from shared.* imports in tests/
   - [ ] LoggingSettings integrated with configure_logging()
   - [ ] Sample scripts use f1d.shared.* namespace
   - [ ] Tests execute without PYTHONPATH manipulation
   - [ ] mypy passes on migrated files
3. Optionally run a sample script manually to verify it works
  </how-to-verify>
  <resume-signal>Type "approved" to complete phase or describe issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Update project state and create milestone audit</name>
  <files>
    .planning/STATE.md
    .planning/v6.1-MILESTONE-AUDIT.md
  </files>
  <action>
Update project state to reflect Phase 75 completion and create v6.1 milestone audit.

**Update .planning/STATE.md:**
- Change Phase 75 status from PENDING to COMPLETE
- Update current position to show v6.1 complete
- Add completion date
- Update progress metrics

**Create .planning/v6.1-MILESTONE-AUDIT.md:**
Create a milestone audit report documenting:
- All gaps from v6.0 audit and their resolution status
- Verification results for each success criterion
- Any remaining technical debt (if applicable)
- Overall milestone status (PASSED/NEEDS_ATTENTION)

Template:
```markdown
---
milestone: v6.1
audited: [date]
status: passed
gaps_closed: [count]/[total]
---

# v6.1 Milestone Audit Report

**Milestone:** Architecture Compliance Gap Closure
**Audited:** [date]
**Status:** PASSED

## Gap Closure Summary

| Gap | Status | Resolution |
|-----|--------|------------|
| sys.path.insert() in sample scripts | CLOSED | Migrated to f1d.shared.* imports |
| Legacy imports in tests | CLOSED | Migrated to f1d.shared.* imports |
| LoggingSettings integration | CLOSED | configure_logging() accepts settings |
| pandas/numpy compatibility | CLOSED/NOT_NEEDED | [resolution] |

## Success Criteria Verification

[Results from verification tasks]

## Recommendation

[Status and any follow-up items]
```
  </action>
  <verify>STATE.md shows Phase 75 COMPLETE; v6.1-MILESTONE-AUDIT.md exists</verify>
  <done>Project state updated and milestone audit created</done>
</task>

</tasks>

<verification>
1. Verify .planning/STATE.md shows Phase 75 COMPLETE
2. Verify .planning/v6.1-MILESTONE-AUDIT.md exists with audit results
3. Confirm all 6 success criteria from ROADMAP are documented as met
4. Verify git status shows all changes committed
</verification>

<success_criteria>
- All gap closure verification tasks completed
- All 6 phase success criteria verified as TRUE
- STATE.md updated with Phase 75 COMPLETE status
- v6.1-MILESTONE-AUDIT.md created documenting results
- Project marked as v6.1 complete
</success_criteria>

<output>
After completion, create `.planning/phases/75-gap-closure/75-05-SUMMARY.md`
</output>
