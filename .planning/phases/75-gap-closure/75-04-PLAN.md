---
phase: 75-gap-closure
plan: 04
type: execute
wave: 3
depends_on: ["75-02", "75-03"]
files_modified:
  - tests/conftest.py
  - pyproject.toml
  - tests/unit/econometric/test_panel_ols.py
  - tests/unit/econometric/test_iv_regression.py
autonomous: true

must_haves:
  truths:
    - "panel_ols and iv_regression tests execute without pandas/numpy compatibility errors"
    - "Test environment has compatible pandas/numpy versions"
    - "Tier 1 regression tests pass without xfails due to environment issues"
  artifacts:
    - path: "pyproject.toml"
      provides: "Pinned dependency versions"
      contains: "pandas"
    - path: "tests/conftest.py"
      provides: "Test fixtures and configuration"
    - path: "tests/unit/econometric/test_panel_ols.py"
      provides: "Panel OLS tests"
    - path: "tests/unit/econometric/test_iv_regression.py"
      provides: "IV regression tests"
  key_links:
    - from: "tests/unit/econometric/"
      to: "src/f1d/econometric/"
      via: "imports and test execution"
---

<objective>
Resolve pandas/numpy version compatibility issues blocking panel_ols and iv_regression tests.

Purpose: Close the environmental gap identified in v6.0 audit where panel_ols and iv_regression tests were blocked by pandas/numpy version incompatibilities. Tests should execute without environment-related xfails.

Output: Compatible dependency constraints and working econometric tests.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v6.0-MILESTONE-AUDIT.md

# Problem Context

From v6.0 audit:
- "panel_ols and iv_regression coverage blocked by pandas/numpy env compatibility"
- Tests likely failing due to API changes between pandas versions

# Investigation Required

This plan requires investigation to identify the specific compatibility issue:
1. Check current pandas/numpy versions in pyproject.toml
2. Run the affected tests to see the actual error
3. Determine if it's a version pinning issue or code update needed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate pandas/numpy compatibility issue</name>
  <files>pyproject.toml</files>
  <action>
Investigate the specific pandas/numpy compatibility issue affecting econometric tests.

Steps:
1. Check current dependency versions in pyproject.toml
2. Identify if panel_ols and iv_regression test files exist
3. Run the tests to capture the exact error message:
   ```bash
   pytest tests/unit/econometric/ -v --tb=long 2>&1 | head -100
   ```
4. If tests don't exist, check tests/regression/ for similar files
5. Document the specific error and root cause

Common pandas/numpy compatibility issues:
- DataFrame.append() removed in pandas 2.0 (use pd.concat)
- Indexing changes (df[column] behavior)
- Deprecated functions (e.g., pd.np access)
- numpy deprecation warnings becoming errors

If the issue is dependency version, document what version range works.
If the issue is code, identify the specific functions/lines that need updating.
  </action>
  <verify>Investigation documented with specific error messages and root cause</verify>
  <done>Root cause of compatibility issue identified and documented</done>
</task>

<task type="auto">
  <name>Task 2: Fix compatibility issue in code or dependencies</name>
  <files>
    pyproject.toml
    src/f1d/econometric/*.py
    tests/unit/econometric/*.py
  </files>
  <action>
Based on investigation results, apply the appropriate fix.

**If dependency version issue:**
- Update pyproject.toml with compatible version constraints
- Example: `pandas = ">=1.5.0,<3.0.0"` or pin to specific known-working version

**If code compatibility issue:**
- Update deprecated pandas/numpy API usage in econometric modules
- Common fixes:
  - `df.append(other)` -> `pd.concat([df, other])`
  - `pd.np.function()` -> `np.function()`
  - `df.ix[]` -> `df.loc[]` or `df.iloc[]`
  - Use `df.to_numpy()` instead of `df.values` where appropriate

**If test infrastructure issue:**
- Update conftest.py fixtures if needed
- Ensure test data generation uses compatible APIs

Make minimal changes to resolve the issue - do not refactor beyond what's needed.
  </action>
  <verify>pytest tests/unit/econometric/ -v passes without pandas/numpy related errors</verify>
  <done>Econometric tests execute without compatibility errors</done>
</task>

<task type="auto">
  <name>Task 3: Verify all econometric tests pass</name>
  <files>tests/unit/econometric/</files>
  <action>
Run the full econometric test suite and verify all tests pass.

Steps:
1. Run: `pytest tests/unit/econometric/ -v --tb=short`
2. Verify no xfails or skips due to environment issues
3. If tests still fail with different errors, document them for future work
4. Run mypy on econometric modules to ensure no type errors introduced:
   `mypy src/f1d/econometric/`

Record test results:
- Number of tests collected
- Number of tests passed
- Number of tests failed (should be 0)
- Number of tests skipped (should be 0 or documented)
  </action>
  <verify>pytest tests/unit/econometric/ shows all tests passing</verify>
  <done>All econometric tests pass without environment-related failures</done>
</task>

</tasks>

<verification>
1. Run: `pytest tests/unit/econometric/ -v` - all tests pass
2. Run: `mypy src/f1d/econometric/` - passes (may have pre-existing warnings)
3. Verify no pandas/numpy deprecation warnings in test output
4. Check that pyproject.toml has appropriate version constraints if needed
</verification>

<success_criteria>
- Root cause of pandas/numpy compatibility issue identified
- Appropriate fix applied (code update or dependency constraint)
- All econometric tests execute without environment-related errors
- No xfails or skips due to pandas/numpy issues
- mypy passes on modified files
</success_criteria>

<output>
After completion, create `.planning/phases/75-gap-closure/75-04-SUMMARY.md`
</output>
