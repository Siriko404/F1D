---
phase: 75-gap-closure
plan: 02
type: execute
wave: 2
depends_on: ["75-01"]
files_modified:
  - tests/unit/test_calculate_throughput.py
  - tests/unit/test_financial_utils_exceptions.py
  - tests/unit/test_metadata_utils.py
  - tests/unit/test_industry_utils.py
  - tests/unit/test_regression_helpers.py
  - tests/unit/test_subprocess_validation.py
  - tests/unit/test_env_validation.py
  - tests/unit/test_subprocess_validation_edge_cases.py
  - tests/unit/test_env_validation_edge_cases.py
  - tests/unit/test_edge_cases.py
  - tests/unit/test_data_validation_edge_cases.py
  - tests/integration/test_full_pipeline.py
  - tests/integration/test_pipeline_step1.py
  - tests/integration/test_pipeline_step2.py
  - tests/integration/test_pipeline_step3.py
  - tests/integration/test_error_propagation.py
  - tests/regression/test_h7_h8_data_coverage.py
  - tests/regression/test_output_stability.py
  - tests/regression/generate_baseline_checksums.py
autonomous: true

must_haves:
  truths:
    - "All test files import using f1d.shared.* namespace"
    - "Tests execute without PYTHONPATH manipulation"
    - "pytest discovers and runs all tests with standard configuration"
  artifacts:
    - path: "tests/unit/*.py"
      provides: "Unit tests with proper imports"
      pattern: "from f1d\\.shared\\."
    - path: "tests/integration/*.py"
      provides: "Integration tests with proper imports"
      pattern: "from f1d\\.shared\\."
    - path: "tests/regression/*.py"
      provides: "Regression tests with proper imports"
      pattern: "from f1d\\.shared\\."
  key_links:
    - from: "tests/**/*.py"
      to: "src/f1d/shared/"
      via: "import statements"
      pattern: "from f1d\\.shared\\."
---

<objective>
Migrate all legacy test files from `from shared.*` imports to proper `from f1d.shared.*` namespace imports.

Purpose: Achieve consistent import patterns across the entire test suite. Legacy tests with sys.path manipulation and old import patterns create technical debt and confusion.

Output: 18+ test files with proper src-layout imports.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v6.0-MILESTONE-AUDIT.md

# Migration Pattern
1. Remove `sys.path.insert(0, ...)` lines
2. Change `from shared.*` to `from f1d.shared.*`
3. Remove any `2_Scripts` path references
4. Tests should rely on pytest's configured pythonpath in pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate unit test files (11 files)</name>
  <files>
    tests/unit/test_calculate_throughput.py
    tests/unit/test_financial_utils_exceptions.py
    tests/unit/test_metadata_utils.py
    tests/unit/test_industry_utils.py
    tests/unit/test_regression_helpers.py
    tests/unit/test_subprocess_validation.py
    tests/unit/test_env_validation.py
    tests/unit/test_subprocess_validation_edge_cases.py
    tests/unit/test_env_validation_edge_cases.py
    tests/unit/test_edge_cases.py
    tests/unit/test_data_validation_edge_cases.py
  </files>
  <action>
For each unit test file, apply the following migration:

1. Remove any `sys.path.insert(0, ...)` lines (typically at the top after imports)
2. Remove any `from pathlib import Path` imports used only for sys.path manipulation
3. Change all `from shared.*` imports to `from f1d.shared.*`
4. Remove any references to `2_Scripts` directory

Pattern replacement:
- `from shared.observability_utils` -> `from f1d.shared.observability_utils`
- `from shared.path_utils` -> `from f1d.shared.path_utils`
- `from shared.data_validation` -> `from f1d.shared.data_validation`
- `from shared.financial_utils` -> `from f1d.shared.financial_utils`
- `from shared.metadata_utils` -> `from f1d.shared.metadata_utils`
- `from shared.industry_utils` -> `from f1d.shared.industry_utils`
- `from shared.subprocess_validation` -> `from f1d.shared.subprocess_validation`
- `from shared.env_validation` -> `from f1d.shared.env_validation`

Do NOT change test logic or assertions - only import statements.
  </action>
  <verify>grep -l "from shared\." tests/unit/*.py returns empty OR only files not in migration list</verify>
  <done>All 11 unit test files use f1d.shared.* imports with no sys.path manipulation</done>
</task>

<task type="auto">
  <name>Task 2: Migrate integration test files (5 files)</name>
  <files>
    tests/integration/test_full_pipeline.py
    tests/integration/test_pipeline_step1.py
    tests/integration/test_pipeline_step2.py
    tests/integration/test_pipeline_step3.py
    tests/integration/test_error_propagation.py
  </files>
  <action>
For each integration test file, apply the same migration pattern as Task 1.

1. Remove any `sys.path.insert(0, ...)` lines
2. Change all `from shared.*` imports to `from f1d.shared.*`
3. Remove any references to `2_Scripts` directory

Note: Some integration tests may have additional complexity like subprocess execution - preserve that logic, only modify import statements.

Do NOT change test logic or assertions - only import statements.
  </action>
  <verify>grep -l "from shared\." tests/integration/*.py returns empty OR only .log files</verify>
  <done>All 5 integration test files use f1d.shared.* imports with no sys.path manipulation</done>
</task>

<task type="auto">
  <name>Task 3: Migrate regression test files (3 files)</name>
  <files>
    tests/regression/test_h7_h8_data_coverage.py
    tests/regression/test_output_stability.py
    tests/regression/generate_baseline_checksums.py
  </files>
  <action>
For each regression test file, apply the same migration pattern.

1. Remove any `sys.path.insert(0, ...)` lines
2. Change all `from shared.*` imports to `from f1d.shared.*`
3. Remove any references to `2_Scripts` directory

Note: generate_baseline_checksums.py may be a utility script rather than a pytest test - still migrate its imports.

Do NOT change test logic or assertions - only import statements.
  </action>
  <verify>grep -l "from shared\." tests/regression/*.py returns empty</verify>
  <done>All 3 regression test files use f1d.shared.* imports with no sys.path manipulation</done>
</task>

</tasks>

<verification>
1. Run: `grep -r "sys.path.insert" tests/` - should return empty (excluding log files)
2. Run: `grep -r "from shared\." tests/` - should return empty (excluding log files)
3. Run: `pytest tests/ --collect-only` - should collect all tests without import errors
4. Run: `pytest tests/unit/ -v --tb=short` - unit tests should execute without import errors
</verification>

<success_criteria>
- Zero `sys.path.insert()` calls in tests/ directory
- Zero `from shared.*` imports in tests/ directory
- All imports use `from f1d.shared.*` pattern
- pytest can collect all tests without import errors
- Unit tests pass (integration/regression may have environment dependencies)
</success_criteria>

<output>
After completion, create `.planning/phases/75-gap-closure/75-02-SUMMARY.md`
</output>
