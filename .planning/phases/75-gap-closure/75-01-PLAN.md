---
phase: 75-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/f1d/sample/1.0_BuildSampleManifest.py
  - src/f1d/sample/1.1_CleanMetadata.py
  - src/f1d/sample/1.2_LinkEntities.py
  - src/f1d/sample/1.3_BuildTenureMap.py
  - src/f1d/sample/1.4_AssembleManifest.py
autonomous: true

must_haves:
  truths:
    - "Sample scripts import using f1d.shared.* namespace without sys.path manipulation"
    - "Running sample scripts directly works without PYTHONPATH setup"
    - "All shared module imports resolve correctly from installed package"
  artifacts:
    - path: "src/f1d/sample/1.1_CleanMetadata.py"
      provides: "Sample script with proper imports"
      pattern: "from f1d\\.shared\\."
    - path: "src/f1d/sample/1.2_LinkEntities.py"
      provides: "Sample script with proper imports"
      pattern: "from f1d\\.shared\\."
    - path: "src/f1d/sample/1.3_BuildTenureMap.py"
      provides: "Sample script with proper imports"
      pattern: "from f1d\\.shared\\."
    - path: "src/f1d/sample/1.4_AssembleManifest.py"
      provides: "Sample script with proper imports"
      pattern: "from f1d\\.shared\\."
    - path: "src/f1d/sample/1.0_BuildSampleManifest.py"
      provides: "Sample script with proper imports"
      pattern: "from f1d\\.shared\\."
  key_links:
    - from: "src/f1d/sample/*.py"
      to: "src/f1d/shared/"
      via: "import statements"
      pattern: "from f1d\\.shared\\."
---

<objective>
Migrate all sample scripts from legacy `sys.path.insert()` workarounds to proper `f1d.shared.*` imports.

Purpose: Eliminate sys.path manipulation anti-pattern that breaks when running scripts outside the development environment. Sample scripts should work with the installed f1d package.

Output: 5 sample scripts with proper src-layout imports.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v6.0-MILESTONE-AUDIT.md

# Migration Pattern (from Phase 69-74)
- Replace `sys.path.insert(0, str(...))` with standard imports
- Change `from shared.*` to `from f1d.shared.*`
- Remove fallback import blocks that manipulate sys.path
- Keep script functionality unchanged
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 1.1_CleanMetadata.py to proper imports</name>
  <files>src/f1d/sample/1.1_CleanMetadata.py</files>
  <action>
Remove all sys.path.insert() calls and migrate imports to f1d.shared.* namespace.

Changes required:
1. Remove lines 36-37: `sys.path.insert(0, str(Path(__file__).parent.parent))`
2. Change line 60: `from shared.chunked_reader` -> `from f1d.shared.chunked_reader`
3. Change line 61-74: `from shared.observability_utils` -> `from f1d.shared.observability_utils`
4. Remove lines 77-86: The try/except fallback block for data_validation import
5. Replace with direct import: `from f1d.shared.data_validation import load_validated_parquet`
6. Remove lines 89-105: The try/except fallback block for path_utils import
7. Replace with direct import: `from f1d.shared.path_utils import ensure_output_dir, validate_input_file, validate_output_path`
8. Change line 149: `from shared.dependency_checker` -> `from f1d.shared.dependency_checker`

Do NOT change any script logic or functionality - only import statements.
  </action>
  <verify>grep -c "sys.path.insert" src/f1d/sample/1.1_CleanMetadata.py returns 0</verify>
  <done>File has zero sys.path.insert() calls and all imports use f1d.shared.* namespace</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 1.2_LinkEntities.py to proper imports</name>
  <files>src/f1d/sample/1.2_LinkEntities.py</files>
  <action>
Remove all sys.path.insert() calls and migrate imports to f1d.shared.* namespace.

Changes required:
1. Remove lines 57 and 75: `sys.path.insert(0, ...)` calls
2. Remove line 114: `_sys.path.insert(0, ...)` in fallback block
3. Change line 77: `from shared.chunked_reader` -> `from f1d.shared.chunked_reader`
4. Change line 78: `from shared.industry_utils` -> `from f1d.shared.industry_utils`
5. Change line 79-95: `from shared.observability_utils` -> `from f1d.shared.observability_utils`
6. Change line 96-102: `from shared.string_matching` -> `from f1d.shared.string_matching`
7. Remove try/except fallback blocks for path_utils (lines 104-120)
8. Replace with direct import: `from f1d.shared.path_utils import ensure_output_dir, validate_input_file, validate_output_path`
9. Change line 161: `from shared.dependency_checker` -> `from f1d.shared.dependency_checker`

Do NOT change any script logic or functionality - only import statements.
  </action>
  <verify>grep -c "sys.path.insert" src/f1d/sample/1.2_LinkEntities.py returns 0</verify>
  <done>File has zero sys.path.insert() calls and all imports use f1d.shared.* namespace</done>
</task>

<task type="auto">
  <name>Task 3: Migrate remaining sample scripts (1.0, 1.3, 1.4)</name>
  <files>
    src/f1d/sample/1.3_BuildTenureMap.py
    src/f1d/sample/1.4_AssembleManifest.py
    src/f1d/sample/1.0_BuildSampleManifest.py
  </files>
  <action>
Apply the same migration pattern to the remaining 3 sample scripts.

For 1.3_BuildTenureMap.py:
- Remove line 44: sys.path.insert(0, ...)
- Change all `from shared.*` to `from f1d.shared.*`

For 1.4_AssembleManifest.py:
- Remove line 44: sys.path.insert(0, ...)
- Change all `from shared.*` to `from f1d.shared.*`

For 1.0_BuildSampleManifest.py:
- This file uses try/except fallbacks with sys.path manipulation inside
- Remove all try/except fallback blocks with sys.path.insert
- Change all `from shared.*` to `from f1d.shared.*`

Do NOT change any script logic or functionality - only import statements.
  </action>
  <verify>
grep -c "sys.path.insert" src/f1d/sample/1.3_BuildTenureMap.py returns 0 AND
grep -c "sys.path.insert" src/f1d/sample/1.4_AssembleManifest.py returns 0 AND
grep -c "sys.path.insert" src/f1d/sample/1.0_BuildSampleManifest.py returns 0
  </verify>
  <done>All 3 files have zero sys.path.insert() calls and use f1d.shared.* namespace</done>
</task>

</tasks>

<verification>
1. Run: `grep -r "sys.path.insert" src/f1d/sample/` - should return empty
2. Run: `grep -r "from shared\." src/f1d/sample/` - should return empty
3. Run: `grep -r "from f1d\.shared\." src/f1d/sample/` - should show all imports
4. Verify mypy passes: `mypy src/f1d/sample/` (may have pre-existing type errors unrelated to this change)
</verification>

<success_criteria>
- Zero `sys.path.insert()` calls in src/f1d/sample/*.py files
- All imports use `from f1d.shared.*` pattern
- No `from shared.*` legacy imports remain
- Import statements are syntactically correct (verified by mypy or python -c)
</success_criteria>

<output>
After completion, create `.planning/phases/75-gap-closure/75-01-SUMMARY.md`
</output>
