---
phase: 13-script-refactoring
plan: 07
type: execute
wave: 5
depends_on: ["13-06"]
files_modified: ["2_Scripts/shared/README.md"]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Shared modules documented in README"
  artifacts:
    - path: "2_Scripts/shared/README.md"
      contains: "regression_validation.py section with API reference"
    - path: "2_Scripts/shared/README.md"
      contains: "string_matching.py section with API reference"
  key_links:
    - from: "2_Scripts/shared/README.md"
      to: "2_Scripts/shared/regression_validation.py"
      via: "Documentation section"
      pattern: "regression_validation.py"
    - from: "2_Scripts/shared/README.md"
      to: "2_Scripts/shared/string_matching.py"
      via: "Documentation section"
      pattern: "string_matching.py"
---

<objective>
Document regression_validation.py and string_matching.py modules in shared/README.md

Purpose: Complete documentation for all 9 shared modules
Output: Updated shared/README.md with regression_validation and string_matching sections
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-script-refactoring/13-VERIFICATION.md
@.planning/phases/13-script-refactoring/13-03-PLAN.md
@.planning/phases/13-script-refactoring/13-04-PLAN.md

# Module implementations
@2_Scripts/shared/regression_validation.py
@2_Scripts/shared/string_matching.py
@2_Scripts/shared/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression_validation.py documentation to shared/README.md</name>
  <files>2_Scripts/shared/README.md</files>
  <action>
Read 2_Scripts/shared/README.md to understand the existing documentation pattern and structure. Find where to insert the regression_validation.py section (typically after existing module sections or in alphabetical order).

Add a new section for regression_validation.py following the existing documentation pattern:
- Module name and purpose
- Brief description of what the module does
- List of exported functions with their signatures
- Brief description of each function's purpose
- Example usage if applicable

The regression_validation.py module contains these functions:
- validate_columns(df, required_columns)
- validate_data_types(df, column_types)
- validate_no_missing_independent(df, independent_vars)
- validate_regression_data(df, dependent_var, independent_vars, required_columns=None)
- validate_sample_size(df, min_size=30)
- check_multicollinearity(df, independent_vars, threshold=10.0)

Each function validates regression inputs and raises RegressionValidationError if validation fails with clear error messages.

Insert this section before any conclusion or summary sections, maintaining consistent formatting with existing module documentation sections.
</action>
  <verify>grep -A 30 "regression_validation.py" 2_Scripts/shared/README.md | head -31</verify>
  <done>regression_validation.py section added to shared/README.md with all 6 functions documented</done>
</task>

<task type="auto">
  <name>Task 2: Add string_matching.py documentation to shared/README.md</name>
  <files>2_Scripts/shared/README.md</files>
  <action>
Read the updated shared/README.md to see where the regression_validation.py section was added. Add a new section for string_matching.py following the same documentation pattern.

The string_matching.py module contains these functions:
- load_matching_config(config_path='config/project.yaml')
- get_scorer(scorer_name='token_sort_ratio')
- match_company_names(target_names, candidate_names, threshold=92.0, scorer=None)
- match_many_to_many(target_names, candidate_names, threshold=92.0, scorer=None)
- _match_many_to_many_fallback(target_names, candidate_names, threshold=92.0, scorer=None)

This module provides config-driven fuzzy name matching using RapidFuzz library. It reads thresholds from config/project.yaml string_matching section and matches company/entity names with configurable scoring algorithms.

Insert this section adjacent to other utility modules (e.g., near path_utils or symlink_utils), maintaining consistent formatting with existing module documentation sections.
</action>
  <verify>grep -A 30 "string_matching.py" 2_Scripts/shared/README.md | head -31</verify>
  <done>string_matching.py section added to shared/README.md with all 5 public functions documented</done>
</task>

</tasks>

<verification>
1. Verify that shared/README.md now contains both regression_validation.py and string_matching.py sections
2. Verify that all exported functions are documented with their signatures
3. Verify that documentation follows the same pattern as other module sections
4. Run: grep -c "### " 2_Scripts/shared/README.md to count module sections (should be 9 total now)
</verification>

<success_criteria>
- shared/README.md contains documentation for regression_validation.py with all 6 functions listed
- shared/README.md contains documentation for string_matching.py with all 5 public functions listed
- Documentation format is consistent with existing module sections
- Total of 9 modules documented in shared/README.md
</success_criteria>

<output>
After completion, create `.planning/phases/13-script-refactoring/13-07-SUMMARY.md`
</output>
