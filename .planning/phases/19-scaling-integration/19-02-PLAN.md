---
phase: 19-scaling-infrastructure-testing-integration
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - 2_Scripts/2_Text/2.1_TokenizeAndCount.py
  - 2_Scripts/2_Text/2.2_ConstructVariables.py
  - 2_Scripts/2_Text/2.3_VerifyStep2.py
autonomous: true

must_haves:
  truths:
    - Step 2 scripts use PyArrow column pruning to reduce memory footprint
    - Only required columns are loaded from Parquet files
    - MemoryAwareThrottler infrastructure is documented as available for future integration
  artifacts:
    - path: "2_Scripts/2_Text/2.1_TokenizeAndCount.py"
      provides: "Tokenization with column-optimized reads"
      contains: "pd.read_parquet(input_path, columns="
    - path: "2_Scripts/2_Text/2.2_ConstructVariables.py"
      provides: "Variable construction with column-optimized reads"
      contains: "pd.read_parquet("
    - path: "2_Scripts/2_Text/2.3_VerifyStep2.py"
      provides: "Verification with column-optimized reads"
      contains: "pd.read_parquet(vars_path, columns="
  key_links:
    - from: "2.1, 2.2, 2.3 scripts"
      to: "chunked_reader.py"
      via: "Import comment"
      pattern: "chunked_reader.*throttl|MemoryAwareThrottler.*future"
---

<objective>
Add PyArrow column pruning to Step 2 scripts and document throttling infrastructure readiness

Purpose: Reduce memory footprint in Step 2 scripts by loading only required columns, while documenting that chunked_reader throttling infrastructure is available for future integration
Output: Step 2 scripts with column-optimized Parquet reads
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v1.0.0-MILESTONE-AUDIT.md
@2_Scripts/SCALING.md
@.planning/phases/15-scaling-preparation/15-02-SUMMARY.md

# Column pruning pattern established in Phase 15-02 for scripts 1.2, 1.4, 3.2
# Step 2 scripts (2.1, 2.2, 2.3) read large transcript files and benefit from column pruning
# Full process_in_chunks() throttling integration requires significant refactoring; column pruning provides immediate memory optimization
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add column pruning to 2.1_TokenizeAndCount.py</name>
  <files>2_Scripts/2_Text/2.1_TokenizeAndCount.py</files>
  <action>
    1. Identify required columns for each pd.read_parquet() call:
       - Line 419: df = pd.read_parquet(input_path) - needs: file_name, speaker_text (and other columns used after load)
       - Line 504: df = pd.read_parquet(input_path) - needs: file_name, speaker_text
       - Line 575: manifest = pd.read_parquet(manifest_path) - already has columns=["file_name"]

    2. Trace column usage through process_year() function:
       - Start from line 419, check which columns are used: file_name (filtering), speaker_text (vectorization), and metadata columns

    3. Add columns= parameter to lines 419 and 504:
       ```python
       # Before:
       df = pd.read_parquet(input_path)

       # After:
       df = pd.read_parquet(input_path, columns=['file_name', 'speaker_text', 'speaker_number', 'context', 'role', 'speaker_name', 'employer'])
       ```
       (Adjust column list based on actual usage traced through the function)

    4. Add comment at top of file after imports:
       ```python
       # Note: MemoryAwareThrottler from shared/chunked_reader.py is available for future chunked processing.
       # Current implementation uses column pruning for memory optimization, avoiding complex refactoring required for process_in_chunks().
       ```

    Reference: Phase 15-02 pattern, SCALING.md lines 73-96
  </action>
  <verify>
    # Check that columns= parameters are added:
    grep -n "pd.read_parquet" 2_Scripts/2_Text/2.1_TokenizeAndCount.py
    # Verify comment about throttling exists:
    grep -n "MemoryAwareThrottler\|chunked.*throttl" 2_Scripts/2_Text/2.1_TokenizeAndCount.py
  </verify>
  <done>
    - 2.1 has columns= parameter on all pd.read_parquet() calls (except line 575 which already has it)
    - Column list includes only columns actually used in processing
    - Comment documents throttling infrastructure availability
  </done>
</task>

<task type="auto">
  <name>Task 2: Add column pruning to 2.2_ConstructVariables.py</name>
  <files>2_Scripts/2_Text/2.2_ConstructVariables.py</files>
  <action>
    1. Identify required columns for each pd.read_parquet() call:
       - Line 304: df = pd.read_parquet(input_path) - trace column usage in construct_variables()
       - Line 431: df = pd.read_parquet(in_path) - trace column usage
       - Line 611: first_year_df = pd.read_parquet(first_year_file) - trace column usage

    2. Trace column usage for each read:
       - Read lines after each read, identify which columns are accessed
       - Include only those columns in columns= parameter

    3. Add columns= parameter to lines 304, 431, 611:
       ```python
       # Example pattern:
       df = pd.read_parquet(input_path, columns=['col1', 'col2', 'col3'])
       ```

    4. Add comment about throttling at top of file (same pattern as Task 1)

    Reference: Phase 15-02 pattern
  </action>
  <verify>
    grep -n "pd.read_parquet.*columns=" 2_Scripts/2_Text/2.2_ConstructVariables.py
  </verify>
  <done>
    - 2.2 has columns= parameter on all pd.read_parquet() calls
    - Column lists include only required columns
    - Comment documents throttling infrastructure availability
  </done>
</task>

<task type="auto">
  <name>Task 3: Add column pruning to 2.3_VerifyStep2.py</name>
  <files>2_Scripts/2_Text/2.3_VerifyStep2.py</files>
  <action>
    1. Identify required columns for pd.read_parquet() at line 186:
       - df = pd.read_parquet(vars_path) - trace column usage in verification logic

    2. Add columns= parameter:
       ```python
       df = pd.read_parquet(vars_path, columns=['col1', 'col2', 'col3'])
       ```

    3. Add comment about throttling at top of file (same pattern as Task 1)
  </action>
  <verify>
    grep -n "pd.read_parquet.*columns=" 2_Scripts/2_Text/2.3_VerifyStep2.py
  </verify>
  <done>
    - 2.3 has columns= parameter on pd.read_parquet() call
    - Column list includes only required columns
    - Comment documents throttling infrastructure availability
  </done>
</task>

</tasks>

<verification>
Run verification commands for each script:
- All pd.read_parquet() calls have columns= parameter (except those already having it)
- Column lists include only columns actually used
- Comments document MemoryAwareThrottler availability
</verification>

<success_criteria>
1. 2.1_TokenizeAndCount.py has columns= on 2 pd.read_parquet() calls (lines 419, 504)
2. 2.2_ConstructVariables.py has columns= on 3 pd.read_parquet() calls (lines 304, 431, 611)
3. 2.3_VerifyStep2.py has columns= on 1 pd.read_parquet() call (line 186)
4. All column lists include only columns actually used by each function
5. Each file has comment documenting throttling infrastructure availability
</success_criteria>

<output>
After completion, create `.planning/phases/19-scaling-infrastructure-testing-integration/19-02-SUMMARY.md`
</output>
