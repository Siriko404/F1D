---
phase: 61-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/**/*.py (for adding headers)
  - docs/SCRIPT_DOCSTANDARD.md (to be created)
autonomous: true

must_haves:
  truths:
    - "All 68 pipeline scripts have standardized headers"
    - "Headers include Purpose, Inputs, Outputs, Dependencies sections"
    - "Headers include Deterministic flag, Author/Date fields"
    - "Shared utility modules have module-level docstrings"
    - "Docstring format is consistent across scripts"
  artifacts:
    - path: "docs/SCRIPT_DOCSTANDARD.md"
      provides: "Standardized header template and docstring format"
      min_lines: 60
      contains: ["Purpose:", "Inputs:", "Outputs:", "Dependencies:", "Deterministic:", "Author:", "Date:"]
  key_links:
    - from: "2_Scripts/**/*.py"
      to: "docs/SCRIPT_DOCSTANDARD.md"
      via: "Docstring format reference"
      pattern: "Standardized header"
---

<objective>
Verify script header compliance, document the standard, and ADD missing standardized headers to non-compliant scripts. This task focuses on DOC-02 requirements: ensuring all scripts follow consistent header format and creating documentation for the standard.

Purpose: Verify and remediate script docstring compliance, then document the header standard
Output: Documented header standard in docs/SCRIPT_DOCSTANDARD.md, 100% compliant scripts, and verification report
</objective>

<execution_context>
@C:\Users\sinas\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sinas\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/requirements.md (DOC-02 requirements)
@2_Scripts/1_Sample/1.1_CleanMetadata.py (header example)
@2_Scripts/shared/panel_ols.py (shared module example)
@2_Scripts/shared/observability/stats.py (observability example)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit script header compliance across all Python files</name>
  <files>2_Scripts/**/*.py</files>
  <action>
    Audit all 68 Python scripts in 2_Scripts/ for DOC-02 header compliance:

    For each .py file, check for:
    1. Shebang line: `#!/usr/bin/env python3`
    2. Module docstring with:
       - STEP/Script identifier
       - Description
       - Inputs section (or equivalent)
       - Outputs section (or equivalent)
       - Dependencies section (for shared modules)
       - Deterministic flag

    Create a compliance report script that:
    - Lists files with compliant headers
    - Lists files with missing or non-compliant headers
    - Categorizes by directory (1_Sample, 2_Text, 3_Financial, 4_Econometric, shared)

    Use grep to count compliance:
    ```bash
    # Count shebang lines
    grep -l "^#!/usr/bin/env python3" 2_Scripts/**/*.py | wc -l

    # Count Deterministic flags
    grep -l "Deterministic:" 2_Scripts/**/*.py | wc -l
    ```

    Output: A list `NONCOMPLIANT_HEADERS.txt` with files needing header additions.
  </action>
  <verify>Generate compliance report showing header statistics and list of non-compliant files</verify>
  <done>Compliance report created with header format analysis and list of files needing updates</done>
</task>

<task type="auto">
  <name>Task 2: Create SCRIPT_DOCSTANDARD.md with header template including Author/Date</name>
  <files>docs/SCRIPT_DOCSTANDARD.md</files>
  <action>
    Create docs/SCRIPT_DOCSTANDARD.md documenting the standard header format:

    Include:
    1. Purpose of the header standard
    2. Template for pipeline scripts (Steps 1-4) including Author/Date fields
    3. Template for shared utility modules including Author/Date fields
    4. Field definitions (what each section should contain)
    5. Examples from actual scripts
    6. Compliance checklist

    Template structure based on existing patterns with Author/Date added:
    ```python
    #!/usr/bin/env python3
    """
    ==============================================================================
    STEP X.Y: {Script Name}
    ==============================================================================
    ID: X.Y_ScriptName
    Description: {One-line description}

    Purpose: {Detailed purpose (what and why)}

    Inputs:
        - 4_Outputs/{PreviousStep}/latest/{file.parquet}
        - 1_Inputs/{data_file}

    Outputs:
        - 4_Outputs/{CurrentStep}/{timestamp}/{output.parquet}
        - stats.json
        - {timestamp}.log

    Dependencies:
        - Requires Step X.{Y-1}
        - Uses: shared.module1, shared.module2

    Deterministic: true

    Author: {Your Name}
    Date: {YYYY-MM-DD}
    ==============================================================================
    """
    ```

    First create docs/ directory if it doesn't exist.

    NOTE: The Author/Date fields address DOC-02-03 requirement.
  </action>
  <verify>docs/SCRIPT_DOCSTANDARD.md exists and contains template with all required fields including Author/Date</verify>
  <done>Header standard documented with templates and examples including Author/Date fields</done>
</task>

<task type="auto">
  <name>Task 3: ADD missing standardized headers to non-compliant scripts</name>
  <files>2_Scripts/**/*.py (files from NONCOMPLIANT_HEADERS.txt)</files>
  <action>
    For each file listed in NONCOMPLIANT_HEADERS.txt from Task 1, ADD the standardized header:

    For each non-compliant file:
    1. Read the file to identify existing content
    2. Add shebang line if missing: `#!/usr/bin/env python3`
    3. Add module docstring at top with:
       - STEP identifier based on file path
       - Description based on script name/purpose
       - Inputs section (list input files or "None" if utility)
       - Outputs section (list output files or "None" if utility)
       - Dependencies section (list required modules/scripts)
       - Deterministic flag (true for pipeline scripts, varies for utilities)
       - Author: "Thesis Author" (placeholder)
       - Date: current date in YYYY-MM-DD format

    Use Edit tool to add headers to each non-compliant file.
    Preserve existing content; insert header before existing code.

    Example header insertion pattern:
    ```python
    #!/usr/bin/env python3
    """
    ==============================================================================
    STEP 3.1: Firm Controls Construction
    ==============================================================================
    ID: 3.1_FirmControls
    Description: Constructs firm-level control variables for regression analysis

    Purpose: Calculate size, book-to-market, leverage, ROA, and other control
             variables from Compustat quarterly data.

    Inputs:
        - 4_Outputs/2_Text/latest/linguistic_variables.parquet
        - 1_Inputs/compustat/quarterly/compustat_q.parquet

    Outputs:
        - 4_Outputs/3_Financial/{timestamp}/firm_controls.parquet
        - stats.json
        - {timestamp}.log

    Dependencies:
        - Requires Step 2.2
        - Uses: shared.financial_utils, shared.observability

    Deterministic: true

    Author: Thesis Author
    Date: 2026-02-11
    ==============================================================================
    """

    # Existing code continues below...
    ```

    This task addresses DOC-02-01 requirement to ADD standardized headers.
  </action>
  <verify>Run grep to confirm all 68 scripts now have shebang and standardized headers</verify>
  <done>All 68 pipeline scripts have standardized headers with all required fields including Author/Date</done>
</task>

<task type="auto">
  <name>Task 4: Verify and ADD module-level docstrings to shared utility modules</name>
  <files>2_Scripts/shared/**/*.py</files>
  <action>
    Check each shared utility module for module-level docstrings and ADD any that are missing:

    Modules to verify (26 total):
    1. observability/__init__.py
    2. observability/logging.py
    3. observability/stats.py
    4. observability/files.py
    5. observability/memory.py
    6. observability/throughput.py
    7. observability/anomalies.py
    8. panel_ols.py
    9. regression_utils.py
    10. regression_helpers.py
    11. iv_regression.py
    12. diagnostics.py
    13. financial_utils.py
    14. data_loading.py
    15. data_validation.py
    16. path_utils.py
    17. cli_validation.py
    18. dependency_checker.py
    19. string_matching.py
    20. chunked_reader.py
    21. centering.py
    22. latex_tables.py
    23. reporting_utils.py
    24. regression_validation.py
    25. dual_writer.py
    26. metadata_utils.py

    For each module:
    1. Check if module-level docstring exists
    2. If missing, ADD docstring with:
       - Module purpose (what problem it solves)
       - Main functions/classes exported
       - Dependencies (external libraries)
       - Usage example (if applicable)
    3. If incomplete, enhance missing sections

    Example module docstring template:
    ```python
    """
    ==============================================================================
    SHARED MODULE: Panel OLS with Fixed Effects
    ==============================================================================
    ID: shared/panel_ols
    Description: Panel OLS regression with firm + year + industry fixed effects

    Purpose: Phases 33-35 (H1/H2/H3 Regressions) need standardized panel
             regression infrastructure with proper fixed effects, interaction
             terms, and multicollinearity diagnostics.

    Main Functions:
        - run_panel_ols(): Execute panel regression with fixed effects
        - compute_vif(): Calculate variance inflation factors
        - check_collinearity(): Detect perfect multicollinearity

    Dependencies:
        - linearmodels: PanelOLS for fixed effects estimation
        - pandas: DataFrame operations
        - numpy: Numerical operations

    Author: Thesis Author
    Date: 2026-02-11
    ==============================================================================
    """
    ```

    This task addresses DOC-02-05 requirement to ADD module-level docstrings.
  </action>
  <verify>All 26 shared utility modules have module-level docstrings with Purpose, Main Functions, Dependencies</verify>
  <done>Shared module docstring report created; all modules have complete docstrings</done>
</task>

<task type="auto">
  <name>Task 5: Generate docstring compliance summary</name>
  <files>docs/DOCSTRING_COMPLIANCE.md</files>
  <action>
    Generate comprehensive compliance summary in docs/DOCSTRING_COMPLIANCE.md:

    Include:
    1. Overall statistics (total scripts, compliant count, percentage)
    2. Before/After comparison (compliance rate before Task 3 vs. after)
    3. Breakdown by directory (1_Sample, 2_Text, 3_Financial, 4_Econometric, shared)
    4. List of scripts that had headers added
    5. List of shared modules that had docstrings added/enhanced
    6. Confirmation of 100% compliance

    Format as Markdown with tables for readability.
  </action>
  <verify>docs/DOCSTRING_COMPLIANCE.md exists with statistics showing 100% compliance</verify>
  <done>Docstring compliance summary created showing 100% header compliance across all 68 scripts</done>
</task>

</tasks>

<verification>
After completion, verify:
1. docs/SCRIPT_DOCSTANDARD.md exists with header template including Author/Date
2. docs/DOCSTRING_COMPLIANCE.md exists showing 100% compliance
3. All 68 scripts have standardized headers (shebang + docstring)
4. All 26 shared modules have module-level docstrings
5. DOC-02-01, DOC-02-02, DOC-02-03, DOC-02-04, DOC-02-05 all satisfied
</verification>

<success_criteria>
- SCRIPT_DOCSTANDARD.md documents the header format with Author/Date fields
- All 68 pipeline scripts have compliant headers
- All 26 shared utility modules have complete module-level docstrings
- DOCSTRING_COMPLIANCE.md reports 100% compliance
- DOC-02-01 through DOC-02-05 requirements fully implemented
</success_criteria>

<output>
After completion, create `.planning/phases/61-documentation/61-02-SUMMARY.md` with:
- Summary of docstring audit findings
- Before/After compliance statistics
- List of scripts that had headers added
- List of shared modules that had docstrings added
- Confirmation of 100% DOC-02 compliance
- Reference to created documentation files
</output>
