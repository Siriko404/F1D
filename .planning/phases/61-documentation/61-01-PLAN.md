---
phase: 61-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true

must_haves:
  truths:
    - "User can understand project purpose from README.md"
    - "User can install dependencies using README instructions"
    - "User can run pipeline using README quick start guide"
    - "User understands directory structure from README"
    - "User knows data sources and availability from README"
    - "User knows computational requirements from README"
  artifacts:
    - path: "README.md"
      provides: "Project overview, installation, quick start, structure, data sources"
      min_lines: 300
      contains: ["# F1D", "## Installation", "## Quick Start", "## Pipeline Structure", "## Data Sources"]
  key_links:
    - from: "README.md"
      to: "config/project.yaml"
      via: "Configuration reference"
      pattern: "config/project.yaml"
    - from: "README.md"
      to: ".planning/ROADMAP.md"
      via: "Project phases reference"
      pattern: "ROADMAP.md"
---

<objective>
Verify and enhance the repository-level README.md for the F1D Data Processing Pipeline. The README exists but needs verification against DOC-01 requirements to ensure all necessary information is present for academic reviewers and users.

Purpose: Create comprehensive documentation for users to understand, install, and run the pipeline
Output: Verified/enhanced README.md meeting all DOC-01 requirements
</objective>

<execution_context>
@C:\Users\sinas\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\sinas\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@.planning/requirements.md (DOC-01 requirements)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify README completeness against DOC-01 requirements</name>
  <files>README.md</files>
  <action>
    Read the current README.md and verify it contains all DOC-01-01 through DOC-01-08 elements:

    1. DOC-01-01: Comprehensive README.md in project root - verify exists
    2. DOC-01-02: Project overview and purpose - verify present in first section
    3. DOC-01-03: Installation and setup instructions - verify includes dependencies, Python version
    4. DOC-01-04: Quick start guide - verify includes example commands
    5. DOC-01-05: Directory structure explanation - verify includes tree or description
    6. DOC-01-06: Data sources and availability statement - verify includes WRDS/commercial data note
    7. DOC-01-07: Computational requirements - verify includes RAM, runtime estimates
    8. DOC-01-08: Reference to thesis/paper - verify citation info exists

    For each missing element, note what needs to be added.
  </action>
  <verify>Check that README.md contains sections for all 8 DOC-01 requirements</verify>
  <done>All DOC-01 requirements verified present in README.md</done>
</task>

<task type="auto">
  <name>Task 2: Enhance README with any missing DOC-01 elements</name>
  <files>README.md</files>
  <action>
    Based on Task 1 findings, add any missing DOC-01 elements to README.md:

    - If DOC-01-06 incomplete: Add detailed data sources section (WRDS, CRSP, Compustat, IBES, SDC, CCCL, LM Dictionary)
    - If DOC-01-07 incomplete: Add "Computational Requirements" section with:
      * Python version: 3.8+ (tested on 3.8-3.13)
      * RAM: Minimum 8GB, recommended 16GB
      * Runtime: ~20-30 minutes for full pipeline
    - If DOC-01-08 incomplete: Add "Citation" or "Contact" section

    Preserve existing content; only add missing sections. Use consistent Markdown formatting.
  </action>
  <verify>Run grep to verify key sections exist in README.md</verify>
  <done>README.md contains all 8 DOC-01 required elements</done>
</task>

<task type="auto">
  <name>Task 3: Validate README usability with smoke tests</name>
  <files>README.md</files>
  <action>
    Validate that README instructions are correct and executable:

    1. Verify all code blocks in README are syntactically correct
    2. Verify all file paths mentioned in README exist
    3. Verify config/project.yaml is referenced correctly
    4. Verify that "## Quick Start" commands use correct relative paths
    5. Check that directory structure description matches actual structure

    Use Bash to validate paths:
    ```bash
    # Check referenced paths exist
    test -f config/project.yaml || echo "Missing: config/project.yaml"
    test -d 2_Scripts || echo "Missing: 2_Scripts/"
    test -d 4_Outputs || echo "Missing: 4_Outputs/"
    ```
  </action>
  <verify>All referenced paths and commands in README.md are valid</verify>
  <done>README.md instructions are executable and paths are correct</done>
</task>

</tasks>

<verification>
After completion, verify:
1. README.md exists in project root
2. Contains sections: Project Overview, Installation, Quick Start, Pipeline Structure, Data Sources, Computational Requirements
3. All file paths referenced in README exist
4. Code examples are syntactically correct
5. DOC-01-01 through DOC-01-08 all satisfied
</verification>

<success_criteria>
- README.md contains all 8 DOC-01 required elements
- All file paths referenced in README exist
- Code blocks in README are syntactically valid
- README provides sufficient information for new user to install and run pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/61-documentation/61-01-SUMMARY.md` with:
- Summary of DOC-01 verification findings
- List of any enhancements made to README.md
- Confirmation of which DOC-01 requirements were already present vs. added
</output>
