---
phase: 24-complete-refactoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [2_Scripts/shared/metadata_utils.py]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "shared/metadata_utils.py module exists with load_variable_descriptions() function"
    - "load_variable_descriptions() correctly loads variable descriptions from reference files"
    - "Function has type hints and comprehensive docstring"
    - "Module follows project contract header pattern"
  artifacts:
    - path: "2_Scripts/shared/metadata_utils.py"
      provides: "Variable description loading from reference files"
      min_lines: 40
      contains: "def load_variable_descriptions"
  key_links:
    - from: "2_Scripts/1_Sample/1.2_LinkEntities.py"
      to: "2_Scripts/shared/metadata_utils.py"
      via: "Extraction of inline load_variable_descriptions() function (lines 237-258)"
      pattern: "from shared.metadata_utils import load_variable_descriptions"
---

<objective>
Create shared/metadata_utils.py module with load_variable_descriptions() function

Purpose: Extract reusable variable description loading from 1.2_LinkEntities.py to enable line count reduction through code sharing

Output: New shared module with extractable metadata loading logic
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PHASE_24/24-RESEARCH.md
@.planning/STATE.md
@.planning/ROADMAP.md

# Source code to extract from
@2_Scripts/1_Sample/1.2_LinkEntities.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create metadata_utils.py module with load_variable_descriptions()</name>
  <files>2_Scripts/shared/metadata_utils.py</files>
  <action>
    Create new module 2_Scripts/shared/metadata_utils.py with:

    1. Contract header at top:
    ```
    """
    ID: metadata_utils
    Description: Variable description loading utilities
    Declared Inputs: Variable reference files (tab-separated with headers)
    Declared Outputs: load_variable_descriptions() function
    deterministic: true
    """
    ```

    2. Extract load_variable_descriptions() function from 1.2_LinkEntities.py (lines 237-258):
       - Copy the exact function implementation
       - Add type hints: (ref_files: Dict[str, Path]) -> Dict[str, Dict[str, str]]
       - Enhance docstring with Args, Returns, Raises sections
       - Keep function logic unchanged (preserve behavior)
       - Graceful exception handling (skip files that fail to load)

    3. Required imports:
       ```python
       from pathlib import Path
       from typing import Dict
       ```

    4. Ensure deterministic behavior:
       - No nondeterministic operations
       - Pure function (no external state)
       - Consistent output for same inputs

    Reference research for exact implementation pattern.
  </action>
  <verify>python -m py_compile 2_Scripts/shared/metadata_utils.py && echo "Module compiles successfully"</verify>
  <done>shared/metadata_utils.py exists with load_variable_descriptions() function that compiles without errors</done>
</task>

</tasks>

<verification>
After completion:
1. Verify module compiles: `python -m py_compile 2_Scripts/shared/metadata_utils.py`
2. Verify function signature: `python -c "from shared.metadata_utils import load_variable_descriptions; help(load_variable_descriptions)"`
3. Check line count is ~40-60 lines (not bloated)
4. Verify contract header exists at top of file
5. Ensure type hints present on all function parameters
</verification>

<success_criteria>
- shared/metadata_utils.py module created with load_variable_descriptions() function
- Function loads variable descriptions from reference files
- Module follows project contract header pattern
- Function has comprehensive docstring and type hints
- Module compiles without errors
- Function gracefully handles file load errors
</success_criteria>

<output>
After completion, create `.planning/phases/24-complete-refactoring/24-02-SUMMARY.md`
</output>
