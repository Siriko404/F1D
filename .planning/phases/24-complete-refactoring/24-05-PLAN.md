---
phase: 24-complete-refactoring
plan: 05
type: execute
wave: 2
depends_on: []
files_modified: [2_Scripts/3_Financial/3.1_FirmControls.py]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "3.1_FirmControls.py consolidated inline code to reduce line count"
    - "Script compiles without errors after consolidation"
    - "Line count reduced to <800 lines (currently 801)"
    - "No functional changes - only inline consolidation"
  artifacts:
    - path: "2_Scripts/3_Financial/3.1_FirmControls.py"
      provides: "Firm controls with consolidated inline code"
      max_lines: 800
  key_links:
    - from: "2_Scripts/3_Financial/3.1_FirmControls.py"
      to: "self (inline consolidation)"
      via: "Remove verbose comments, consolidate similar blocks, compact code"
      pattern: "No external imports - inline refactoring only"
---

<objective>
Refactor 3.1_FirmControls.py with inline consolidation to reduce line count below 800

Purpose: Perform minor inline code consolidation (not extraction) to reduce script size from 801 to <800 lines

Output: 3.1_FirmControls.py with consolidated inline code, no functional changes
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PHASE_24/24-RESEARCH.md
@.planning/STATE.md
@.planning/ROADMAP.md

# Script to refactor
@2_Scripts/3_Financial/3.1_FirmControls.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate inline code in 3.1 to reduce line count</name>
  <files>2_Scripts/3_Financial/3.1_FirmControls.py</files>
  <action>
    Refactor 2_Scripts/3_Financial/3.1_FirmControls.py with INLINE consolidation (no extraction):

    1. Read script to identify consolidation opportunities:
       - Verbose multi-line comments (can be condensed)
       - Duplicate variable names or assignments
       - Similar code blocks that can be merged
       - Excessive blank lines (remove non-structural blank lines)

    2. Perform consolidation (target: ~2-10 lines reduction):
       - Condense verbose comments to single-line summaries
       - Remove duplicate blank lines (keep structural separation only)
       - Consolidate similar operations where possible
       - **DO NOT** extract functions (this is inline consolidation only)
       - **DO NOT** change script logic or behavior

    3. Verify script compiles: `python -m py_compile 2_Scripts/3_Financial/3.1_FirmControls.py`

    4. Check final line count: `wc -l 2_Scripts/3_Financial/3.1_FirmControls.py`
       - Target: <800 lines (currently 801)
       - Minimum reduction: 2 lines (just 1 over target)
       - Target reduction: 5-10 lines for safety margin

    5. If still over 800 after consolidation:
       - Identify small extractable functions (10-20 lines)
       - Extract to shared/financial_utils.py if already there
       - Or create small inline helper functions

    Use custom Python script for complex edits if needed.
  </action>
  <verify>python -m py_compile 2_Scripts/3_Financial/3.1_FirmControls.py && echo "Script compiles successfully" && wc -l 2_Scripts/3_Financial/3.1_FirmControls.py | awk '{if ($1 < 800) print "Line count <800: SUCCESS"; else print "Line count: "$1" (still over 800)"}'</verify>
  <done>3.1_FirmControls.py line count <800, script compiles, no functional changes</done>
</task>

</tasks>

<verification>
After completion:
1. Verify script compiles: `python -m py_compile 2_Scripts/3_Financial/3.1_FirmControls.py`
2. Verify line count <800: `wc -l 2_Scripts/3_Financial/3.1_FirmControls.py`
3. Verify no new imports added (inline consolidation only): `grep "^from shared" 2_Scripts/3_Financial/3.1_FirmControls.py` (should show existing imports only)
4. Verify no new functions defined (inline consolidation only): compare function definitions before/after
5. Verify script executes correctly (dry run with minimal inputs)
</verification>

<success_criteria>
- 3.1_FirmControls.py line count reduced to <800 (from 801)
- Script compiles without errors
- No functional changes (inline consolidation only)
- No new shared module imports added
- No new functions extracted
</success_criteria>

<output>
After completion, create `.planning/phases/24-complete-refactoring/24-05-SUMMARY.md`
</output>
