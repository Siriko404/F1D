---
phase: 19-scaling-infrastructure-testing-integration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_full_pipeline.py
  - tests/integration/test_observability_integration.py
  - tests/integration/test_pipeline_step1.py
  - tests/integration/test_pipeline_step2.py
autonomous: true

must_haves:
  truths:
    - Integration tests use absolute paths resolved from __file__ reference
    - Tests run correctly from any working directory
    - CI/CD workflow executes integration tests without path errors
  artifacts:
    - path: "tests/integration/test_full_pipeline.py"
      contains: "Path(__file__)|pathlib.Path(__file__).parent"
      provides: "Absolute path resolution"
    - path: "tests/integration/test_observability_integration.py"
      contains: "Path(__file__)|pathlib.Path(__file__).parent"
      provides: "Absolute path resolution"
    - path: "tests/integration/test_pipeline_step1.py"
      contains: "Path(__file__)|pathlib.Path(__file__).parent"
      provides: "Absolute path resolution"
    - path: "tests/integration/test_pipeline_step2.py"
      contains: "Path(__file__)|pathlib.Path(__file__).parent"
      provides: "Absolute path resolution"
  key_links:
    - from: "Integration test files"
      to: "Root directory"
      via: "__file__.parent.parent"
      pattern: "Path(__file__).parent(\.parent)+"
---

<objective>
Fix integration test path resolution to work from any directory

Purpose: Ensure integration tests resolve paths correctly when invoked from different working directories (e.g., pytest from tests/ or repo root)
Output: Integration tests using absolute paths derived from __file__
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v1.0.0-MILESTONE-AUDIT.md

# Integration tests use relative paths like Path("2_Scripts/...") and Path("4_Outputs/...")
# These fail when pytest is invoked from different directories (tests/ integration tests directory, not repo root)
# Solution: Use Path(__file__).parent.parent to resolve to repo root, then construct absolute paths
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix path resolution in test_full_pipeline.py</name>
  <files>tests/integration/test_full_pipeline.py</files>
  <action>
    1. Add import at top (after existing imports):
       ```python
       from pathlib import Path
       ```

    2. Add REPO_ROOT constant near top of test class or module:
       ```python
       # Get repository root from test file location
       REPO_ROOT = Path(__file__).parent.parent.parent
       ```

    3. Replace relative paths with absolute paths:
       - Line 116: script_name = Path(script_path).stem (keep as-is, uses script_path parameter)
       - Line 119: return Path(f"4_Outputs/{script_name}/latest") → return REPO_ROOT / f"4_Outputs/{script_name}/latest"
       - Line 121: return Path(f"4_Outputs/2_Textual_Analysis/{script_name}/latest") → return REPO_ROOT / f"4_Outputs/2_Textual_Analysis/{script_name}/latest"
       - Line 123: return Path(f"4_Outputs/3_Financial/{script_name}/latest") → return REPO_ROOT / f"4_Outputs/3_Financial/{script_name}/latest"
       - Line 125: return Path(f"4_Outputs/4_Econometric/{script_name}/latest") → return REPO_ROOT / f"4_Outputs/4_Econometric/{script_name}/latest"
       - Line 241: final_outputs_dir = Path("4_Outputs/4_Econometric/4.3_TakeoverHazards/latest") → final_outputs_dir = REPO_ROOT / "4_Outputs/4_Econometric/4.3_TakeoverHazards/latest"
       - Line 268: step1_output = Path("4_Outputs/1.4_AssembleManifest/latest") → step1_output = REPO_ROOT / "4_Outputs/1.4_AssembleManifest/latest"
       - Line 272: step2_output = Path("4_Outputs/2_Textual_Analysis/2.2_ConstructVariables/latest") → step2_output = REPO_ROOT / "4_Outputs/2_Textual_Analysis/2.2_ConstructVariables/latest"
       - Line 283: step3_output = Path("4_Outputs/3_Financial/3.3_EventFlags/latest") → step3_output = REPO_ROOT / "4_Outputs/3_Financial/3.3_EventFlags/latest"
       - Line 287: step4_output = Path("4_Outputs/4_Econometric/4.3_TakeoverHazards/latest") → step4_output = REPO_ROOT / "4_Outputs/4_Econometric/4.3_TakeoverHazards/latest"

    4. Verify script_path parameter is already absolute or converted:
       - Line 151: script_path_obj = Path(script_path) - keep as-is (should be absolute from caller)

    Pattern: Replace Path("path/to/file") with REPO_ROOT / "path/to/file"
  </action>
  <verify>
    # Verify REPO_ROOT constant exists:
    grep -n "REPO_ROOT.*Path(__file__)" tests/integration/test_full_pipeline.py
    # Verify no remaining relative paths:
    grep -n 'Path("2_Scripts/\|Path("4_Outputs/' tests/integration/test_full_pipeline.py | grep -v "script_path"
  </verify>
  <done>
    - REPO_ROOT constant defined as Path(__file__).parent.parent.parent
    - All Path("4_Outputs/...") replaced with REPO_ROOT / "4_Outputs/..."
    - Tests work from any working directory
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix path resolution in test_observability_integration.py</name>
  <files>tests/integration/test_observability_integration.py</files>
  <action>
    1. Check if pathlib.Path is already imported (if not, add import)

    2. Add REPO_ROOT constant:
       ```python
       REPO_ROOT = Path(__file__).parent.parent.parent
       ```

    3. Replace relative paths:
       - Line 26: script_path = Path("2_Scripts/1_Sample/1.1_CleanMetadata.py") → script_path = REPO_ROOT / "2_Scripts/1_Sample/1.1_CleanMetadata.py"
       - Line 58: script_path = Path(script_path_str) - check if script_path_str is full path from test parameter, if so keep as-is

    4. Check context around line 58 to understand script_path_str usage pattern
  </action>
  <verify>
    grep -n 'Path("2_Scripts/\|Path("4_Outputs/' tests/integration/test_observability_integration.py
  </verify>
  <done>
    - REPO_ROOT constant defined
    - All relative paths replaced with absolute paths
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix path resolution in test_pipeline_step1.py</name>
  <files>tests/integration/test_pipeline_step1.py</files>
  <action>
    1. Check if pathlib.Path is already imported

    2. Add REPO_ROOT constant:
       ```python
       REPO_ROOT = Path(__file__).parent.parent.parent
       ```

    3. Replace relative paths:
       - Line 19: config_path = Path("config/project.yaml") → config_path = REPO_ROOT / "config/project.yaml"
       - Line 38: script_path = Path("2_Scripts/1_Sample/1.1_CleanMetadata.py") → script_path = REPO_ROOT / "2_Scripts/1_Sample/1.1_CleanMetadata.py"
       - Line 54: output_dir = Path("4_Outputs/1.1_CleanMetadata/latest") → output_dir = REPO_ROOT / "4_Outputs/1.1_CleanMetadata/latest"
       - Line 71: stats_path = Path("4_Outputs/1.1_CleanMetadata/latest/stats.json") → stats_path = REPO_ROOT / "4_Outputs/1.1_CleanMetadata/latest/stats.json"
       - Line 102: stats_path = Path("4_Outputs/1.1_CleanMetadata/latest/stats.json") → stats_path = REPO_ROOT / "4_Outputs/1.1_CleanMetadata/latest/stats.json"
       - Line 103: output_path = Path("4_Outputs/1.1_CleanMetadata/latest/cleaned_metadata.parquet") → output_path = REPO_ROOT / "4_Outputs/1.1_CleanMetadata/latest/cleaned_metadata.parquet"
  </action>
  <verify>
    grep -n 'Path("2_Scripts/\|Path("4_Outputs/\|Path("config/' tests/integration/test_pipeline_step1.py
  </verify>
  <done>
    - REPO_ROOT constant defined
    - All relative paths replaced with absolute paths
  </done>
</task>

<task type="auto">
  <name>Task 4: Fix path resolution in test_pipeline_step2.py</name>
  <files>tests/integration/test_pipeline_step2.py</files>
  <action>
    1. Check if pathlib.Path is already imported

    2. Add REPO_ROOT constant:
       ```python
       REPO_ROOT = Path(__file__).parent.parent.parent
       ```

    3. Replace relative paths:
       - Line 18: script_path = Path("2_Scripts/2_Text/2.1_TokenizeAndCount.py") → script_path = REPO_ROOT / "2_Scripts/2_Text/2.1_TokenizeAndCount.py"
  </action>
  <verify>
    grep -n 'Path("2_Scripts/\|Path("4_Outputs/' tests/integration/test_pipeline_step2.py
  </verify>
  <done>
    - REPO_ROOT constant defined
    - All relative paths replaced with absolute paths
  </done>
</task>

</tasks>

<verification>
For each test file:
1. REPO_ROOT constant exists as Path(__file__).parent.parent.parent
2. No Path("2_Scripts/") patterns remain (REPO_ROOT / "2_Scripts/" used instead)
3. No Path("4_Outputs/") patterns remain (REPO_ROOT / "4_Outputs/" used instead)
4. No Path("config/") patterns remain (REPO_ROOT / "config/" used instead)

Run test from different directories to verify:
- pytest tests/integration/test_full_pipeline.py (from repo root)
- cd tests/integration && pytest test_full_pipeline.py (from test directory)
</verification>

<success_criteria>
1. test_full_pipeline.py has REPO_ROOT constant and uses it for all file paths
2. test_observability_integration.py has REPO_ROOT constant and uses it for all file paths
3. test_pipeline_step1.py has REPO_ROOT constant and uses it for all file paths
4. test_pipeline_step2.py has REPO_ROOT constant and uses it for all file paths
5. All four test files have no remaining Path("2_Scripts/"), Path("4_Outputs/"), or Path("config/") patterns
6. Tests pass when run from both repo root and tests/integration/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/19-scaling-infrastructure-testing-integration/19-04-SUMMARY.md`
</output>
