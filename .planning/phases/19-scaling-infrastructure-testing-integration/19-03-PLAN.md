---
phase: 19-scaling-infrastructure-testing-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py
  - 2_Scripts/3_Financial/3.1_FirmControls.py
  - 2_Scripts/3_Financial/3.3_EventFlags.py
autonomous: true

must_haves:
  truths:
    - Step 3 scripts use PyArrow column pruning to reduce memory footprint
    - Only required columns are loaded from Parquet files
    - MemoryAwareThrottler infrastructure is documented as available for future integration
  artifacts:
    - path: "2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py"
      contains: "pd.read_parquet(|MemoryAwareThrottler|chunked.*throttl"
      provides: "Financial features with column-optimized reads"
    - path: "2_Scripts/3_Financial/3.1_FirmControls.py"
      contains: "pd.read_parquet(|MemoryAwareThrottler|chunked.*throttl"
      provides: "Firm controls with column-optimized reads"
    - path: "2_Scripts/3_Financial/3.3_EventFlags.py"
      contains: "pd.read_parquet(|MemoryAwareThrottler|chunked.*throttl"
      provides: "Event flags with column-optimized reads"
  key_links:
    - from: "3.0, 3.1, 3.3 scripts"
      to: "chunked_reader.py"
      via: "Import comment"
      pattern: "chunked_reader.*throttl|MemoryAwareThrottler.*future"
---

<objective>
Add PyArrow column pruning to Step 3 scripts

Purpose: Reduce memory footprint in Step 3 scripts by loading only required columns from Parquet files
Output: Step 3 scripts with column-optimized Parquet reads
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v1.0.0-MILESTONE-AUDIT.md
@2_Scripts/SCALING.md
@.planning/phases/15-scaling-preparation/15-02-SUMMARY.md

# Column pruning pattern established in Phase 15-02 for script 3.2 (already has column pruning)
# Step 3 scripts (3.0, 3.1, 3.3) still read large financial datasets without column pruning
# 3.1 already has one column-pruned read (line 174: compustat_file), but needs it on other reads
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add column pruning to 3.0_BuildFinancialFeatures.py</name>
  <files>2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py</files>
  <action>
    1. Identify required columns for pd.read_parquet() at line 340:
       - df = pd.read_parquet(manifest_path) - trace column usage in build_features()

    2. Trace column usage through build_features() function:
       - Find which columns from manifest are accessed (file_name, start_date, permno, etc.)
       - Include only those columns in columns= parameter

    3. Add columns= parameter to line 340:
       ```python
       df = pd.read_parquet(manifest_path, columns=['col1', 'col2', 'col3'])
       ```

    4. Add comment about throttling at top of file:
       ```python
       # Note: MemoryAwareThrottler from shared/chunked_reader.py is available for future chunked processing.
       # Current implementation uses column pruning for memory optimization.
       ```

    Reference: Phase 15-02 pattern for 3.2 (similar script structure)
  </action>
  <verify>
    grep -n "pd.read_parquet.*columns=" 2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py
    grep -n "MemoryAwareThrottler\|chunked.*throttl" 2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py
  </verify>
  <done>
    - 3.0 has columns= parameter on pd.read_parquet() call
    - Column list includes only columns actually used
    - Comment documents throttling infrastructure availability
  </done>
</task>

<task type="auto">
  <name>Task 2: Add column pruning to 3.1_FirmControls.py</name>
  <files>2_Scripts/3_Financial/3.1_FirmControls.py</files>
  <action>
    1. Identify required columns for each pd.read_parquet() call:
       - Line 143: df = pd.read_parquet(manifest_file) - needs: file_name, gvkey, start_date, year
       - Line 174: df = pd.read_parquet(compustat_file, columns=required_cols) - already has column pruning ✓
       - Line 208: df = pd.read_parquet(ibes_file) - needs: [trace usage]
       - Line 229: df = pd.read_parquet(cccl_file) - needs: [trace usage]
       - Line 344: ccm = pd.read_parquet(ccm_file) - needs: [trace usage]

    2. Trace column usage for each function call:
       - Read lines after each read, identify which columns are accessed
       - Include only those columns in columns= parameter

    3. Add columns= parameter to lines 143, 208, 229, 344:
       ```python
       # Example pattern:
       df = pd.read_parquet(manifest_file, columns=['file_name', 'gvkey', 'start_date', 'year'])
       ```

    4. Add comment about throttling at top of file

    Reference: Phase 15-02 pattern, line 174 already shows correct pattern
  </action>
  <verify>
    grep -n "pd.read_parquet.*columns=" 2_Scripts/3_Financial/3.1_FirmControls.py
    grep -n "MemoryAwareThrottler\|chunked.*throttl" 2_Scripts/3_Financial/3.1_FirmControls.py
    # Verify all 5 reads have columns= parameter (line 174 already has it)
  </verify>
  <done>
    - 3.1 has columns= parameter on all 5 pd.read_parquet() calls
    - Column lists include only required columns
    - Comment documents throttling infrastructure availability
  </done>
</task>

<task type="auto">
  <name>Task 3: Add column pruning to 3.3_EventFlags.py</name>
  <files>2_Scripts/3_Financial/3.3_EventFlags.py</files>
  <action>
    1. Identify required columns for each pd.read_parquet() call:
       - Line 236: df = pd.read_parquet(manifest_file) - needs: [trace usage in build_event_flags()]
       - Line 255: df = pd.read_parquet(sdc_file) - needs: [trace usage]

    2. Trace column usage through build_event_flags() function:
       - Find which columns from manifest are accessed
       - Find which columns from sdc data are accessed

    3. Add columns= parameter to lines 236 and 255:
       ```python
       df = pd.read_parquet(manifest_file, columns=['col1', 'col2', 'col3'])
       df = pd.read_parquet(sdc_file, columns=['col1', 'col2', 'col3'])
       ```

    4. Add comment about throttling at top of file

    Reference: Phase 15-02 pattern
  </action>
  <verify>
    grep -n "pd.read_parquet.*columns=" 2_Scripts/3_Financial/3.3_EventFlags.py
    grep -n "MemoryAwareThrottler\|chunked.*throttl" 2_Scripts/3_Financial/3.3_EventFlags.py
  </verify>
  <done>
    - 3.3 has columns= parameter on both pd.read_parquet() calls
    - Column list includes only columns actually used
    - Comment documents throttling infrastructure availability
  </done>
</task>

</tasks>

<verification>
Run verification commands for each script:
- 3.0: 1 pd.read_parquet() call has columns= parameter
- 3.1: All 5 pd.read_parquet() calls have columns= parameter
- 3.3: Both pd.read_parquet() calls have columns= parameter
- All column lists include only columns actually used
- Comments document MemoryAwareThrottler availability (verified by grep for throttling references)
</verification>

<success_criteria>
1. 3.0_BuildFinancialFeatures.py has columns= on 1 pd.read_parquet() call (line 340)
2. 3.1_FirmControls.py has columns= on 5 pd.read_parquet() calls (lines 143, 174, 208, 229, 344)
3. 3.3_EventFlags.py has columns= on 2 pd.read_parquet() calls (lines 236, 255)
4. All column lists include only columns actually used by each function
5. Each file has comment documenting throttling infrastructure availability
</success_criteria>

<output>
After completion, create `.planning/phases/19-scaling-infrastructure-testing-integration/19-03-SUMMARY.md`
</output>
