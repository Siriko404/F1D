---
phase: 23-tech-debt-cleanup
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "4.4_GenerateSummaryStats.py is restored from deletion (0 bytes -> working script)"
    - "4.4_GenerateSummaryStats.py imports DualWriter from shared.observability_utils"
    - "4.4_GenerateSummaryStats.py has no inline DualWriter class definition"
    - "4.4_GenerateSummaryStats.py compiles without errors"
  artifacts:
    - path: "2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py"
      provides: "Restored summary statistics generation script"
      contains: "from shared.observability_utils import DualWriter"
      not_contains: "class DualWriter:"
      min_lines: 900
  key_links:
    - from: "4.4_GenerateSummaryStats.py"
      to: "shared.observability_utils.DualWriter"
      via: "Import statement"
      pattern: "from shared.observability_utils import.*DualWriter"
---

<objective>
Restore 4.4_GenerateSummaryStats.py (deleted in commit 2adb9ac) and refactor to use shared imports

Purpose: CRITICAL GAP - 4.4_GenerateSummaryStats.py was completely deleted (918 lines → 0 bytes) during Plan 23-05 execution. This plan restores the file from commit 03b75e0 and refactors it to import DualWriter from shared.observability_utils instead of inline definition.

Output: Fully functional 4.4_GenerateSummaryStats.py with shared module imports
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-tech-debt-cleanup/23-tech-debt-cleanup-VERIFICATION.md

# Gap Context from VERIFICATION.md:
# - "4.4_GenerateSummaryStats.py went from 918 lines with inline code to 0 bytes (deleted)"
# - Status: CRITICAL BLOCKER - file completely deleted instead of refactored
# - Gap reason: "File was 918 lines before commit 2adb9ac, now 0 bytes. Script completely deleted instead of refactored."
# - Missing: "RESTORE 4.4_GenerateSummaryStats.py - file was completely deleted (0 bytes), needs full restoration with imports from shared.observability_utils"

# Restoration Target:
# - Commit 03b75e0: "fix(23-04): Add comprehensive error handling to 4.4_GenerateSummaryStats.py"
# - File had inline DualWriter class at line 99 (original 918-line version)
# - File had import from shared.symlink_utils (update_latest_link)
# - Needs to add: from shared.observability_utils import DualWriter
# - Needs to remove: inline DualWriter class definition (lines 99-~130)

# Pattern reference (from other migrated scripts):
# 4.1_EstimateCeoClarity.py imports: from shared.observability_utils import DualWriter, compute_file_checksum, print_stat, analyze_missing_values, print_stats_summary, save_stats
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore 4.4_GenerateSummaryStats.py from commit 03b75e0</name>
  <files>2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py</files>
  <action>
  Restore the deleted 4.4_GenerateSummaryStats.py file from commit 03b75e0 (before deletion in commit 2adb9ac).

  Step 1: Use git show to extract the file content:
  ```bash
  git show 03b75e0:2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py > 2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py
  ```

  Step 2: Verify file restoration:
  ```bash
  wc -l 2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py
  ```
  Should show ~918 lines (not 0).

  Step 3: Verify file has content:
  ```bash
  head -20 2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py
  ```
  Should show the doc header with "STEP 4.4: Generate Summary Statistics".

  The restored file will have:
  - Inline DualWriter class (needs removal in Task 2)
  - Import from shared.symlink_utils (keep this)
  - Inline statistics helper functions (keep these for now - unique to this script)
  - Comprehensive error handling (added in commit 03b75e0, keep this)
  </action>
  <verify>
  test -s 2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py && wc -l 2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py | grep -q "918" && echo "4.4 restored with 918 lines"
  grep -q "STEP 4.4: Generate Summary Statistics" 2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py && echo "4.4 has correct header"
  </verify>
  <done>
  4.4_GenerateSummaryStats.py restored from commit 03b75e0 (918 lines)
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor 4.4 to import DualWriter from shared.observability_utils</name>
  <files>2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py</files>
  <action>
  Remove inline DualWriter class from restored 4.4_GenerateSummaryStats.py and add import from shared.observability_utils.

  Step 1: Find inline DualWriter class (should be around line 99):
  ```bash
  grep -n "class DualWriter:" 2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py
  ```
  Should show line number (likely 99).

  Step 2: Add import for DualWriter from shared.observability_utils:
  Find existing imports section (after line ~40 where imports from shared.symlink_utils are).
  Add this import line:
  ```python
  from shared.observability_utils import DualWriter
  ```
  Place it after the existing `from shared.symlink_utils import update_latest_link` import.

  Step 3: Remove inline DualWriter class definition:
  - Find `class DualWriter:` line (around line 99 in restored file)
  - Remove entire class definition from `class DualWriter:` to the next top-level function or class
  - The DualWriter class typically spans ~40 lines (ends before next function definition)
  - Look for blank line after class or next `def ` to determine end of class

  Example removal pattern:
  ```python
  # BEFORE (lines to remove):
  class DualWriter:
      """Write output to both console and log file."""

      def __init__(self, log_path):
          ...

      def write(self, message):
          ...

      def close(self):
          ...

  # AFTER: Removed, using import from shared.observability_utils
  ```

  Step 4: Verify no inline DualWriter remains:
  ```bash
  grep -n "class DualWriter:" 2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py
  ```
  Should return nothing (no inline DualWriter class).

  Step 5: Verify import was added:
  ```bash
  grep -n "from shared.observability_utils import DualWriter" 2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py
  ```
  Should show line number (in imports section).
  </action>
  <verify>
  ! grep -q "class DualWriter:" 2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py && echo "No inline DualWriter in 4.4"
  grep -q "from shared.observability_utils import DualWriter" 2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py && echo "4.4 imports DualWriter from shared"
  python -m py_compile 2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py && echo "4.4 compiles successfully"
  </verify>
  <done>
  4.4_GenerateSummaryStats.py refactored to import DualWriter from shared.observability_utils, no inline code remains
  </done>
</task>

</tasks>

<verification>
1. 4.4_GenerateSummaryStats.py is restored (not 0 bytes, ~918 lines)
2. 4.4_GenerateSummaryStats.py imports DualWriter from shared.observability_utils
3. 4.4_GenerateSummaryStats.py has no inline DualWriter class definition
4. 4.4_GenerateSummaryStats.py compiles without syntax errors
5. File has proper doc header and imports from shared.symlink_utils preserved
6. Statistics helper functions (unique to 4.4) preserved
</verification>

<success_criteria>
1. 4.4_GenerateSummaryStats.py is a working script (not deleted)
2. Script imports DualWriter from shared.observability_utils
3. No inline DualWriter class definition
4. Script compiles and can be imported without errors
5. All original functionality preserved (error handling, statistics functions)
</success_criteria>

<output>
After completion, create `.planning/phases/23-tech-debt-cleanup/23-07-SUMMARY.md`
</output>
