---
phase: 23-tech-debt-cleanup
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/2_Text/2.1_TokenizeAndCount.py
  - 2_Scripts/2_Text/2.2_ConstructVariables.py
  - 2_Scripts/3_Financial/3.4_Utils.py
  - 2_Scripts/4_Econometric/4.3_TakeoverHazards.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "2.1_TokenizeAndCount.py has no inline DualWriter class"
    - "2.2_ConstructVariables.py has no inline DualWriter class"
    - "3.4_Utils.py has no inline DualWriter class"
    - "4.3_TakeoverHazards.py has no inline DualWriter class"
    - "All 4 scripts import DualWriter from shared.observability_utils"
    - "No inline DualWriter class remains anywhere in codebase"
  artifacts:
    - path: "2_Scripts/2_Text/2.1_TokenizeAndCount.py"
      provides: "Tokenize script with shared imports"
      contains: "from shared.observability_utils import.*DualWriter"
      not_contains: "class DualWriter:"
    - path: "2_Scripts/2_Text/2.2_ConstructVariables.py"
      provides: "Variables script with shared imports"
      contains: "from shared.observability_utils import.*DualWriter"
      not_contains: "class DualWriter:"
    - path: "2_Scripts/3_Financial/3.4_Utils.py"
      provides: "Utilities script with shared imports"
      contains: "from shared.observability_utils import.*DualWriter"
      not_contains: "class DualWriter:"
    - path: "2_Scripts/4_Econometric/4.3_TakeoverHazards.py"
      provides: "Takeover hazards script with shared imports"
      contains: "from shared.observability_utils import.*DualWriter"
      not_contains: "class DualWriter:"
  key_links:
    - from: "All 4 scripts"
      to: "shared.observability_utils.DualWriter"
      via: "Import statement"
      pattern: "from shared.observability_utils import.*DualWriter"
---

<objective>
Remove inline DualWriter class from remaining 4 scripts and import from shared.observability_utils

Purpose: Close gaps from VERIFICATION.md - 4 scripts still have inline DualWriter class definitions despite Plan 23-05 claiming they were removed:
- 2.1_TokenizeAndCount.py (line 69 in setup_logging)
- 2.2_ConstructVariables.py (line 54 in setup_logging)
- 3.4_Utils.py (lines 14-29 at module level)
- 4.3_TakeoverHazards.py (line 111)

These scripts need the inline DualWriter class removed and imports from shared.observability_utils added.

Output: All 4 scripts import DualWriter from shared.observability_utils with no inline code
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-tech-debt-cleanup/23-tech-debt-cleanup-VERIFICATION.md

# Gap Context from VERIFICATION.md:
# - "DualWriter removal from 2.1_TokenizeAndCount.py (line 69 in setup_logging)"
# - "DualWriter removal from 2.2_ConstructVariables.py (line 54 in setup_logging)"
# - "DualWriter removal from 3.4_Utils.py (lines 14-29)"
# - "DualWriter removal from 4.3_TakeoverHazards.py (line 111)"
# - Reason: "Plans 23-05 and 23-06 summaries claimed work was done, but verification shows files in 2_Text and 3_Financial directories were not actually modified."
# - Root cause: "Plans 23-05 and 23-06 summaries claimed work was done, but verification shows files in 2_Text and 3_Financial directories were not actually modified."

# DualWriter locations (from verification):
# 2.1_TokenizeAndCount.py: line 69 (inside setup_logging function)
# 2.2_ConstructVariables.py: line 54 (inside setup_logging function)
# 3.4_Utils.py: lines 14-29 (module level)
# 4.3_TakeoverHazards.py: line 111

# Pattern reference (from successfully migrated scripts):
# 4.1_EstimateCeoClarity.py: from shared.observability_utils import DualWriter, compute_file_checksum, print_stat, analyze_missing_values, print_stats_summary, save_stats
# 1_Sample scripts: from shared.observability_utils import DualWriter (some also import utility functions)

# Shared module location:
# 2_Scripts/shared/observability_utils.py contains DualWriter class (line 303)
# 2_Scripts/shared/dual_writer.py re-exports DualWriter from observability_utils
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove inline DualWriter from 2.1_TokenizeAndCount.py and 2.2_ConstructVariables.py</name>
  <files>
    2_Scripts/2_Text/2.1_TokenizeAndCount.py
    2_Scripts/2_Text/2.2_ConstructVariables.py
  </files>
  <action>
  Remove inline DualWriter class from both 2.1 and 2.2 scripts and add import from shared.observability_utils.

  For 2.1_TokenizeAndCount.py:
  Step 1: Find inline DualWriter class (line 69 in setup_logging function):
  ```bash
  grep -n "class DualWriter:" 2_Scripts/2_Text/2.1_TokenizeAndCount.py
  ```

  Step 2: Add import for DualWriter in existing imports section:
  - Find where other shared imports are (if any)
  - Add: `from shared.observability_utils import DualWriter`
  - Place after existing imports, before function definitions

  Step 3: Remove inline DualWriter class:
  - The class is defined inside setup_logging function (not at module level)
  - Find lines from `class DualWriter:` to end of class
  - Class ends when setup_logging function continues or another function starts
  - Remove all lines of the DualWriter class definition
  - Preserve the rest of setup_logging function

  Example pattern (setup_logging function structure):
  ```python
  def setup_logging(...):
      # ... setup code ...
      class DualWriter:  # <- REMOVE from here
          # ... DualWriter implementation ...
      # <- REMOVE to here
      # ... rest of setup_logging function ...
  ```

  For 2.2_ConstructVariables.py (same process):
  Step 1: Find inline DualWriter class (line 54 in setup_logging function):
  ```bash
  grep -n "class DualWriter:" 2_Scripts/2_Text/2.2_ConstructVariables.py
  ```

  Step 2: Add import: `from shared.observability_utils import DualWriter`

  Step 3: Remove inline DualWriter class (inside setup_logging function, same pattern as 2.1)

  Verification after both files:
  ```bash
  grep -n "class DualWriter:" 2_Scripts/2_Text/2.1_TokenizeAndCount.py 2_Scripts/2_Text/2.2_ConstructVariables.py
  ```
  Should return nothing (no inline DualWriter in either file).

  ```bash
  grep -n "from shared.observability_utils import DualWriter" 2_Scripts/2_Text/2.1_TokenizeAndCount.py 2_Scripts/2_Text/2.2_ConstructVariables.py
  ```
  Should show both files importing from shared module.
  </action>
  <verify>
  ! grep -q "class DualWriter:" 2_Scripts/2_Text/2.1_TokenizeAndCount.py && echo "2.1: No inline DualWriter"
  ! grep -q "class DualWriter:" 2_Scripts/2_Text/2.2_ConstructVariables.py && echo "2.2: No inline DualWriter"
  grep -q "from shared.observability_utils import.*DualWriter" 2_Scripts/2_Text/2.1_TokenizeAndCount.py && echo "2.1 imports DualWriter from shared"
  grep -q "from shared.observability_utils import.*DualWriter" 2_Scripts/2_Text/2.2_ConstructVariables.py && echo "2.2 imports DualWriter from shared"
  python -m py_compile 2_Scripts/2_Text/2.1_TokenizeAndCount.py && echo "2.1 compiles"
  python -m py_compile 2_Scripts/2_Text/2.2_ConstructVariables.py && echo "2.2 compiles"
  </verify>
  <done>
  2.1_TokenizeAndCount.py and 2.2_ConstructVariables.py import DualWriter from shared.observability_utils, no inline code
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove inline DualWriter from 3.4_Utils.py and 4.3_TakeoverHazards.py</name>
  <files>
    2_Scripts/3_Financial/3.4_Utils.py
    2_Scripts/4_Econometric/4.3_TakeoverHazards.py
  </files>
  <action>
  Remove inline DualWriter class from 3.4_Utils and 4.3 scripts and add import from shared.observability_utils.

  For 3.4_Utils.py:
  Step 1: Find inline DualWriter class (lines 14-29 at module level):
  ```bash
  grep -n "class DualWriter:" 2_Scripts/3_Financial/3.4_Utils.py
  ```

  Step 2: Add import for DualWriter in existing imports section:
  - Find existing imports (at top of file, after sys.path setup)
  - Add: `from shared.observability_utils import DualWriter`
  - Place with other shared imports (if any)

  Step 3: Remove inline DualWriter class:
  - Class is at module level (not inside a function)
  - Find lines from `class DualWriter:` to next blank line or next function definition
  - Typically spans ~15-20 lines (lines 14-29 per verification)
  - Remove entire class definition

  Example pattern (module level):
  ```python
  # Imports...
  from shared.symlink_utils import update_latest_link

  class DualWriter:  # <- REMOVE from here
      # ... DualWriter implementation ...
  # <- REMOVE to here

  def other_function(...):  # <- Keep this function
      ...
  ```

  For 4.3_TakeoverHazards.py (same process):
  Step 1: Find inline DualWriter class (line 111):
  ```bash
  grep -n "class DualWriter:" 2_Scripts/4_Econometric/4.3_TakeoverHazards.py
  ```

  Step 2: Add import: `from shared.observability_utils import DualWriter`
  - Place in imports section after other imports

  Step 3: Remove inline DualWriter class:
  - Find and remove entire class definition
  - Look for next function or class to determine end of DualWriter class

  Verification after both files:
  ```bash
  grep -n "class DualWriter:" 2_Scripts/3_Financial/3.4_Utils.py 2_Scripts/4_Econometric/4.3_TakeoverHazards.py
  ```
  Should return nothing (no inline DualWriter in either file).

  ```bash
  grep -n "from shared.observability_utils import DualWriter" 2_Scripts/3_Financial/3.4_Utils.py 2_Scripts/4_Econometric/4.3_TakeoverHazards.py
  ```
  Should show both files importing from shared module.
  </action>
  <verify>
  ! grep -q "class DualWriter:" 2_Scripts/3_Financial/3.4_Utils.py && echo "3.4_Utils: No inline DualWriter"
  ! grep -q "class DualWriter:" 2_Scripts/4_Econometric/4.3_TakeoverHazards.py && echo "4.3: No inline DualWriter"
  grep -q "from shared.observability_utils import.*DualWriter" 2_Scripts/3_Financial/3.4_Utils.py && echo "3.4_Utils imports DualWriter from shared"
  grep -q "from shared.observability_utils import.*DualWriter" 2_Scripts/4_Econometric/4.3_TakeoverHazards.py && echo "4.3 imports DualWriter from shared"
  python -m py_compile 2_Scripts/3_Financial/3.4_Utils.py && echo "3.4_Utils compiles"
  python -m py_compile 2_Scripts/4_Econometric/4.3_TakeoverHazards.py && echo "4.3 compiles"
  </verify>
  <done>
  3.4_Utils.py and 4.3_TakeoverHazards.py import DualWriter from shared.observability_utils, no inline code
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify no inline DualWriter remains anywhere in codebase</name>
  <files>All scripts in 2_Scripts/</files>
  <action>
  Comprehensive verification that all inline DualWriter classes have been removed from the entire codebase.

  Verification 1 - Search all directories for inline DualWriter:
  ```bash
  grep -r "class DualWriter:" 2_Scripts/1_Sample/ 2_Scripts/2_Text/ 2_Scripts/3_Financial/ 2_Scripts/4_Econometric/ --include="*.py" 2>/dev/null | grep -v ARCHIVE | grep -v __pycache__
  ```

  Verification 2 - Check root level 2.3_Report.py explicitly:
  ```bash
  grep -q "class DualWriter:" 2_Scripts/2.3_Report.py && echo "FAIL: Inline DualWriter in 2.3_Report.py" || echo "OK: No inline DualWriter in 2.3_Report.py"
  ```

  Verification 3 - Count scripts importing from shared.observability_utils:
  ```bash
  grep -r "from shared.observability_utils import.*DualWriter" 2_Scripts/1_Sample/ 2_Scripts/2_Text/ 2_Scripts/3_Financial/ 2_Scripts/4_Econometric/ --include="*.py" 2>/dev/null | grep -v ARCHIVE | wc -l
  grep -q "from shared.observability_utils import.*DualWriter" 2_Scripts/2.3_Report.py && echo "2.3_Report.py imports DualWriter"
  ```

  Should return 12 (for subdirectories) + 1 (for 2.3_Report.py) = 13 total scripts importing DualWriter from shared module.

  Verification 4 - Verify each gap file specifically:
  ```bash
  echo "=== Gap file verification ==="
  ! grep -q "class DualWriter:" 2_Scripts/2_Text/2.1_TokenizeAndCount.py && echo "✓ 2.1: No inline DualWriter"
  ! grep -q "class DualWriter:" 2_Scripts/2_Text/2.2_ConstructVariables.py && echo "✓ 2.2: No inline DualWriter"
  ! grep -q "class DualWriter:" 2_Scripts/3_Financial/3.4_Utils.py && echo "✓ 3.4_Utils: No inline DualWriter"
  ! grep -q "class DualWriter:" 2_Scripts/4_Econometric/4.3_TakeoverHazards.py && echo "✓ 4.3: No inline DualWriter"
  grep -q "from shared.observability_utils import.*DualWriter" 2_Scripts/2_Text/2.1_TokenizeAndCount.py && echo "✓ 2.1: Imports from shared"
  grep -q "from shared.observability_utils import.*DualWriter" 2_Scripts/2_Text/2.2_ConstructVariables.py && echo "✓ 2.2: Imports from shared"
  grep -q "from shared.observability_utils import.*DualWriter" 2_Scripts/3_Financial/3.4_Utils.py && echo "✓ 3.4_Utils: Imports from shared"
  grep -q "from shared.observability_utils import.*DualWriter" 2_Scripts/4_Econometric/4.3_TakeoverHazards.py && echo "✓ 4.3: Imports from shared"
  ```

  All checks should pass.
  </action>
  <verify>
  ! grep -r "class DualWriter:" 2_Scripts/1_Sample/ 2_Scripts/2_Text/ 2_Scripts/3_Financial/ 2_Scripts/4_Econometric/ --include="*.py" 2>/dev/null | grep -v ARCHIVE | grep -v __pycache__ | grep -q "class DualWriter:" && echo "No inline DualWriter in subdirectories" || echo "FAIL: Inline DualWriter still present"
  ! grep -q "class DualWriter:" 2_Scripts/2.3_Report.py && echo "No inline DualWriter in 2.3_Report.py" || echo "FAIL: Inline DualWriter in 2.3_Report.py"
  grep -r "from shared.observability_utils import.*DualWriter" 2_Scripts/1_Sample/ 2_Scripts/2_Text/ 2_Scripts/3_Financial/ 2_Scripts/4_Econometric/ --include="*.py" 2>/dev/null | grep -v ARCHIVE | wc -l | grep -q "12" && echo "All 12 subdirectory scripts import from shared"
  grep -q "from shared.observability_utils import.*DualWriter" 2_Scripts/2.3_Report.py && echo "2.3_Report.py imports DualWriter"
  </verify>
  <done>
  All 5 gap closure tasks complete (4.4 restored, 4 scripts refactored), no inline DualWriter remains anywhere in codebase
  </done>
</task>

</tasks>

<verification>
1. 2.1_TokenizeAndCount.py has no inline DualWriter class and imports from shared
2. 2.2_ConstructVariables.py has no inline DualWriter class and imports from shared
3. 3.4_Utils.py has no inline DualWriter class and imports from shared
4. 4.3_TakeoverHazards.py has no inline DualWriter class and imports from shared
5. No inline DualWriter class remains anywhere in active scripts (non-ARCHIVE, non-__pycache__)
6. All 13 scripts (12 subdirectories + 2.3_Report.py) import DualWriter from shared.observability_utils
7. All 4 gap scripts compile without syntax errors
</verification>

<success_criteria>
1. All 4 gap scripts import DualWriter from shared.observability_utils
2. No inline DualWriter class definitions remain anywhere in codebase
3. All 4 scripts compile without syntax errors
4. Code duplication eliminated (DualWriter consolidated to shared module)
5. Phase 23 gap closure complete (all gaps from VERIFICATION.md closed)
</success_criteria>

<output>
After completion, create `.planning/phases/23-tech-debt-cleanup/23-08-SUMMARY.md`
</output>
