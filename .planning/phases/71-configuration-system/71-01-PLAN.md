---
phase: 71-configuration-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - src/f1d/shared/config/__init__.py
  - src/f1d/shared/config/base.py
  - src/f1d/shared/config/paths.py
autonomous: true

must_haves:
  truths:
    - "pydantic-settings is installed and importable"
    - "Base configuration classes exist with proper field validation"
    - "Path settings resolve to absolute paths from project root"
    - "Configuration can be loaded from YAML file"
  artifacts:
    - path: "src/f1d/shared/config/__init__.py"
      provides: "Configuration module exports"
      exports: ["ProjectConfig", "DataSettings", "LoggingSettings"]
    - path: "src/f1d/shared/config/base.py"
      provides: "Base pydantic-settings classes for all configuration"
      min_lines: 150
    - path: "src/f1d/shared/config/paths.py"
      provides: "Path resolution and validation"
      min_lines: 50
  key_links:
    - from: "src/f1d/shared/config/base.py"
      to: "config/project.yaml"
      via: "from_yaml classmethod"
      pattern: "yaml.safe_load"
---

<objective>
Implement pydantic-settings base configuration with type-safe validation.

Purpose: Establish the foundation for type-safe configuration management that validates all settings at load time and supports environment variable overrides.
Output: Configuration module with base settings classes that can load from config/project.yaml
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference: Configuration standard patterns
@docs/CONFIG_TESTING_STANDARD.md

# Reference: Existing configuration file structure
@config/project.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pydantic-settings dependency to requirements.txt</name>
  <files>requirements.txt</files>
  <action>
Add pydantic and pydantic-settings to requirements.txt with version constraints:

1. Read current requirements.txt
2. Add the following lines if not present:
   - `pydantic>=2.0,<3.0` - Core pydantic library for data validation
   - `pydantic-settings>=2.0,<3.0` - Settings management with env var support

Place these in alphabetical order within the appropriate section. Do NOT remove any existing dependencies.
  </action>
  <verify>grep -E "pydantic" requirements.txt</verify>
  <done>requirements.txt contains pydantic>=2.0 and pydantic-settings>=2.0</done>
</task>

<task type="auto">
  <name>Task 2: Create config module structure</name>
  <files>src/f1d/shared/config/__init__.py</files>
  <action>
Create the config package directory and __init__.py with module structure:

1. Create directory: src/f1d/shared/config/
2. Create __init__.py with re-exports:
   - Import ProjectConfig, DataSettings, LoggingSettings, DeterminismSettings from .base
   - Import PathsSettings from .paths
   - Define __all__ list

This establishes the import pattern: `from f1d.shared.config import ProjectConfig`
  </action>
  <verify>python -c "from f1d.shared.config import ProjectConfig; print('OK')"</verify>
  <done>config module is importable (will error until base.py created, but structure is ready)</done>
</task>

<task type="auto">
  <name>Task 3: Create base configuration classes</name>
  <files>src/f1d/shared/config/base.py</files>
  <action>
Create base pydantic-settings classes following CONFIG_TESTING_STANDARD.md patterns:

Create the following classes in src/f1d/shared/config/base.py:

1. **DataSettings** (BaseSettings):
   - year_start: int with Field(ge=2000, le=2030)
   - year_end: int with Field(ge=2000, le=2030)
   - Add @model_validator to ensure year_end >= year_start

2. **LoggingSettings** (BaseSettings):
   - level: str with pattern "^(DEBUG|INFO|WARNING|ERROR|CRITICAL)$"
   - format: str default "%(asctime)s [%(levelname)s] %(message)s"
   - timestamp_format: str default "%Y-%m-%d %H:%M:%S"

3. **DeterminismSettings** (BaseSettings):
   - random_seed: int = Field(default=42, ge=0)
   - thread_count: int = Field(default=1, ge=1, le=32)
   - sort_inputs: bool = True

4. **ChunkProcessingSettings** (BaseSettings):
   - max_memory_percent: float = Field(default=80.0, ge=0, le=100)
   - base_chunk_size: int = Field(default=10000, ge=1)
   - enable_throttling: bool = True
   - log_memory_status: bool = True

5. **ProjectSettings** (BaseSettings):
   - name: str = "F1D_Clarity"
   - version: str
   - description: str

6. **ProjectConfig** (BaseSettings):
   - model_config with env_prefix="F1D_", env_nested_delimiter="__", extra="ignore"
   - project: ProjectSettings
   - data: DataSettings
   - logging: LoggingSettings = LoggingSettings()
   - determinism: DeterminismSettings = DeterminismSettings()
   - chunk_processing: ChunkProcessingSettings = ChunkProcessingSettings()
   - @classmethod from_yaml(path: Path) -> "ProjectConfig"
   - def validate_paths(self, base: Path) -> Dict[str, Path]

Use Google-style docstrings for all classes. Import from pydantic, pydantic_settings, and yaml.
  </action>
  <verify>python -m mypy src/f1d/shared/config/base.py --ignore-missing-imports</verify>
  <done>base.py passes mypy, all classes defined with proper field validators</done>
</task>

<task type="auto">
  <name>Task 4: Create path resolution utilities</name>
  <files>src/f1d/shared/config/paths.py</files>
  <action>
Create PathsSettings class with path resolution logic:

1. **PathsSettings** (BaseSettings):
   - inputs: str = "1_Inputs"
   - scripts: str = "2_Scripts"
   - logs: str = "3_Logs"
   - outputs: str = "4_Outputs"
   - lm_dictionary: Optional[str] = None
   - unified_info: Optional[str] = None
   - speaker_data_pattern: Optional[str] = None

2. Add resolve(self, base: Path) -> Dict[str, Path] method that:
   - Converts all string paths to absolute Path objects
   - Handles pattern strings (speaker_data_pattern with {year})
   - Returns dictionary mapping path names to resolved Path objects

3. Add validate_paths(self, base: Path) method that:
   - Checks that input paths exist
   - Creates output and log directories if they don't exist
   - Returns validated paths dict

Include proper type hints and Google-style docstrings.
  </action>
  <verify>python -m mypy src/f1d/shared/config/paths.py --ignore-missing-imports</verify>
  <done>paths.py passes mypy, PathsSettings with resolve() method working</done>
</task>

<task type="auto">
  <name>Task 5: Verify configuration loading from YAML</name>
  <files>src/f1d/shared/config/__init__.py</files>
  <action>
Update __init__.py and test configuration loading:

1. Ensure all exports are correct in __init__.py:
   - ProjectConfig, DataSettings, LoggingSettings, DeterminismSettings from .base
   - PathsSettings from .paths

2. Create a simple test script to verify:
   ```python
   from pathlib import Path
   from f1d.shared.config import ProjectConfig

   config = ProjectConfig.from_yaml(Path("config/project.yaml"))
   print(f"Project: {config.project.name}")
   print(f"Data range: {config.data.year_start}-{config.data.year_end}")
   print(f"Log level: {config.logging.level}")
   ```

3. Run the test and verify output matches project.yaml values
  </action>
  <verify>python -c "from f1d.shared.config import ProjectConfig; c = ProjectConfig.from_yaml(Path('config/project.yaml')); print(c.project.name)"</verify>
  <done>Configuration loads successfully from project.yaml with all values accessible</done>
</task>

</tasks>

<verification>
Run mypy on config module:
```bash
python -m mypy src/f1d/shared/config/ --ignore-missing-imports
```

Test configuration loading:
```bash
python -c "from f1d.shared.config import ProjectConfig; from pathlib import Path; c = ProjectConfig.from_yaml(Path('config/project.yaml')); assert c.data.year_start == 2002"
```
</verification>

<success_criteria>
- pydantic-settings>=2.0 added to requirements.txt
- src/f1d/shared/config/ module created with proper structure
- ProjectConfig class loads from config/project.yaml
- All field validators work correctly (year range, log level pattern, etc.)
- Configuration values match those in project.yaml
- mypy passes on all config module files
</success_criteria>

<output>
After completion, create `.planning/phases/71-configuration-system/71-01-SUMMARY.md`
</output>
