---
phase: 71-configuration-system
plan: 02
type: execute
wave: 2
depends_on: [71-01]
files_modified:
  - src/f1d/shared/config/step_configs.py
  - src/f1d/shared/config/datasets.py
  - src/f1d/shared/config/hashing.py
  - src/f1d/shared/config/string_matching.py
  - src/f1d/shared/config/env.py
  - src/f1d/shared/config/__init__.py
autonomous: true

must_haves:
  truths:
    - "Step-specific configuration classes exist for all pipeline steps"
    - "Dataset configuration validates context_filter and role_filter settings"
    - "Hashing settings validate algorithm and chunk_size"
    - "String matching settings validate threshold ranges"
    - "Environment variables can override configuration values"
  artifacts:
    - path: "src/f1d/shared/config/step_configs.py"
      provides: "Step-specific configuration classes"
      exports: ["Step00Config", "Step01Config", "Step02Config", "Step03Config", "Step04Config", "Step07Config", "Step08Config", "Step09Config"]
    - path: "src/f1d/shared/config/datasets.py"
      provides: "Dataset configuration classes"
      exports: ["DatasetConfig", "DatasetsConfig"]
    - path: "src/f1d/shared/config/env.py"
      provides: "Environment variable handling with secrets"
      exports: ["EnvConfig"]
  key_links:
    - from: "src/f1d/shared/config/step_configs.py"
      to: "src/f1d/shared/config/base.py"
      via: "BaseSettings inheritance"
      pattern: "class.*Settings.*BaseSettings"
    - from: "src/f1d/shared/config/env.py"
      to: ".env"
      via: "SettingsConfigDict env_file"
      pattern: "env_file.*\.env"
---

<objective>
Create step-specific and supporting configuration classes with environment variable support.

Purpose: Extend the configuration system to cover all sections in project.yaml including step-specific settings, dataset configurations, and environment variable overrides.
Output: Complete configuration module covering all project.yaml sections
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference: Configuration standard patterns
@docs/CONFIG_TESTING_STANDARD.md

# Reference: Existing configuration file structure
@config/project.yaml

# Reference: Prior plan SUMMARY
@.planning/phases/71-configuration-system/71-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create step-specific configuration classes</name>
  <files>src/f1d/shared/config/step_configs.py</files>
  <action>
Create configuration classes for each step section in project.yaml.

Reference the existing project.yaml step_XX sections and create:

1. **StepOutputSettings** (common output pattern):
   - output_subdir: str
   - outputs: Dict[str, str] (for file names)

2. **Step00Config** (BaseSettings):
   - enabled: bool = True
   - output_subdir: str
   - outputs: StepOutputSettings (nested)

3. **Step00bConfig** and **Step00cConfig** (similar pattern)

4. **Step01Config**:
   - enabled: bool = True
   - output_subdir: str
   - selection: Dict[str, int] (uncertainty_threshold, weak_modal_threshold)
   - normalization: Dict[str, str] (case, pattern)
   - outputs: StepOutputSettings

5. **Step02Config**:
   - enabled: bool = True
   - output_subdir: str
   - context_filter: str = "qa"
   - managerial_roles: List[str]
   - exclusion_keywords: List[str]
   - duplicate_selection: Dict[str, List[str]]
   - outputs: StepOutputSettings

6. **Step03Config**:
   - enabled: bool = True
   - output_subdir: str
   - tokenization: Dict[str, Any]
   - top_matches_count: int
   - compiler: Dict[str, str]
   - outputs: StepOutputSettings

7. **Step04Config**, **Step07Config**, **Step08Config**, **Step09Config** with their specific fields

Add **StepsConfig** container class that holds all step configs.
Use Dict[str, Any] for heterogeneous nested configurations.
  </action>
  <verify>python -m mypy src/f1d/shared/config/step_configs.py --ignore-missing-imports</verify>
  <done>All step config classes pass mypy and match project.yaml structure</done>
</task>

<task type="auto">
  <name>Task 2: Create dataset configuration classes</name>
  <files>src/f1d/shared/config/datasets.py</files>
  <action>
Create dataset configuration classes:

1. **DatasetConfig** (BaseSettings):
   - description: str
   - context_filter: Optional[str] = None
   - role_filter: Optional[str] = None
   - enabled: bool = True

2. **DatasetsConfig** (BaseSettings):
   - model_config: extra="allow" (for dynamic dataset names)
   - manager_qa: DatasetConfig
   - manager_pres: DatasetConfig
   - analyst_qa: DatasetConfig
   - entire_call: DatasetConfig

Add a get_dataset(name: str) -> Optional[DatasetConfig] method to DatasetsConfig for safe access.

Include a get_enabled_datasets() -> List[str] method that returns names of enabled datasets.
  </action>
  <verify>python -m mypy src/f1d/shared/config/datasets.py --ignore-missing-imports</verify>
  <done>Dataset config classes pass mypy, can load from project.yaml</done>
</task>

<task type="auto">
  <name>Task 3: Create hashing and string matching configs</name>
  <files>src/f1d/shared/config/hashing.py, src/f1d/shared/config/string_matching.py</files>
  <action>
Create configuration classes for hashing and string_matching sections:

**hashing.py:**

1. **HashingConfig** (BaseSettings):
   - algorithm: str = "sha256"
   - chunk_size: int = Field(default=65536, ge=1024)

Add @field_validator for algorithm to ensure it's a valid hashlib algorithm.

**string_matching.py:**

1. **CompanyNameMatchingConfig** (BaseSettings):
   - default_threshold: float = Field(default=92.0, ge=0, le=100)
   - min_threshold: float = Field(default=85.0, ge=0, le=100)
   - scorer: str = "token_sort_ratio"
   - preprocess: bool = True

2. **EntityNameMatchingConfig** (BaseSettings):
   - default_threshold: float = Field(default=85.0, ge=0, le=100)
   - min_threshold: float = Field(default=70.0, ge=0, le=100)
   - scorer: str = "WRatio"
   - preprocess: bool = True

3. **StringMatchingConfig** (BaseSettings):
   - company_name: CompanyNameMatchingConfig
   - entity_name: EntityNameMatchingConfig
   - batch_size: int = Field(default=1000, ge=1)
   - enable_parallel: bool = False

Add cross-field validator to ensure min_threshold <= default_threshold.
  </action>
  <verify>python -m mypy src/f1d/shared/config/hashing.py src/f1d/shared/config/string_matching.py --ignore-missing-imports</verify>
  <done>Hashing and string matching configs pass mypy with threshold validation</done>
</task>

<task type="auto">
  <name>Task 4: Create environment variable support</name>
  <files>src/f1d/shared/config/env.py</files>
  <action>
Create EnvConfig class for environment variable and secrets handling following CONFIG_TESTING_STANDARD.md CONF-02:

1. **EnvConfig** (BaseSettings):
   - model_config with env_file=".env", env_file_encoding="utf-8", extra="ignore"

   Attributes:
   - wrds_username: Optional[str] = None
   - wrds_password: Optional[SecretStr] = None
   - api_timeout_seconds: int = Field(default=30, ge=1, le=300)
   - max_retries: int = Field(default=3, ge=0, le=10)

2. Add get_wrds_password(self) -> Optional[str] method that:
   - Returns None if password not set
   - Uses .get_secret_value() to extract password only when needed

3. Create module-level singleton: `env = EnvConfig()`

Also create .env.example file in project root with template:
```bash
# .env.example
# WRDS credentials (optional - for data download)
WRDS_USERNAME=
WRDS_PASSWORD=

# API settings
API_TIMEOUT_SECONDS=30
MAX_RETRIES=3
```
  </action>
  <verify>python -m mypy src/f1d/shared/config/env.py --ignore-missing-imports</verify>
  <done>EnvConfig passes mypy, SecretStr protects passwords, .env.example created</done>
</task>

<task type="auto">
  <name>Task 5: Update ProjectConfig to include all sections</name>
  <files>src/f1d/shared/config/base.py, src/f1d/shared/config/__init__.py</files>
  <action>
Update ProjectConfig to include all configuration sections:

1. Add imports to base.py:
   - from .step_configs import StepsConfig
   - from .datasets import DatasetsConfig
   - from .hashing import HashingConfig
   - from .string_matching import StringMatchingConfig

2. Add fields to ProjectConfig:
   - steps: Optional[StepsConfig] = None
   - datasets: Optional[DatasetsConfig] = None
   - hashing: HashingConfig = HashingConfig()
   - string_matching: Optional[StringMatchingConfig] = None
   - analyst_detection: Optional[Dict[str, str]] = None

3. Update __init__.py to export all new classes:
   - Add StepsConfig, DatasetsConfig, HashingConfig, StringMatchingConfig, EnvConfig

4. Create a convenience function:
   ```python
   def load_config(path: Path | None = None) -> ProjectConfig:
       """Load configuration from project.yaml with env var overrides."""
       if path is None:
           path = Path("config/project.yaml")
       return ProjectConfig.from_yaml(path)
   ```
  </action>
  <verify>python -c "from f1d.shared.config import load_config; c = load_config(); print(c.project.name, c.datasets.manager_qa.enabled)"</verify>
  <done>ProjectConfig loads all sections from project.yaml, load_config() convenience function works</done>
</task>

</tasks>

<verification>
Run mypy on all config modules:
```bash
python -m mypy src/f1d/shared/config/ --ignore-missing-imports
```

Test full configuration loading:
```bash
python -c "
from pathlib import Path
from f1d.shared.config import load_config
c = load_config(Path('config/project.yaml'))
assert c.data.year_start == 2002
assert c.logging.level == 'INFO'
assert c.datasets.manager_qa.enabled == True
print('All sections loaded correctly')
"
```
</verification>

<success_criteria>
- All step_XX sections have corresponding config classes
- Dataset configuration validates correctly
- Hashing and string_matching configs have threshold validation
- EnvConfig handles secrets with SecretStr
- .env.example template file exists
- ProjectConfig includes all configuration sections
- load_config() function provides convenient access
- All files pass mypy
</success_criteria>

<output>
After completion, create `.planning/phases/71-configuration-system/71-02-SUMMARY.md`
</output>
