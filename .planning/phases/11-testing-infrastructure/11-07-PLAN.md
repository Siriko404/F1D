---
phase: 11-testing-infrastructure
plan: 07
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03", "11-04", "11-05", "11-06"]
files_modified:
  - .github/workflows/test.yml
  - .github/workflows/README.md
  - .planning/phases/11-testing-infrastructure/CI-CD-PLACEHOLDER.md
autonomous: true
user_setup:
  - service: github
    why: "CI/CD for automated testing"
    env_vars: []
    dashboard_config:
      - task: "Enable GitHub Actions in repository settings"
        location: "Repository Settings -> Actions -> General -> Allow all actions and reusable workflows"
        note: "This is optional - CI/CD-PLACEHOLDER.md documents deferral to Phase 12 option"
autonomous: true
must_haves:
  truths:
    - "GitHub Actions workflow file exists (.github/workflows/test.yml)"
    - "GitHub Actions workflow runs pytest on push and pull request"
    - "Coverage reporting is configured in workflow"
    - "CI/CD enablement instructions documented in README.md"
    - "CI-CD placeholder documents both enablement and deferral options"
    - "All tests pass in CI/CD environment (or locally before enablement)"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "GitHub Actions workflow for automated testing"
      contains: "pytest, coverage, test execution"
    - path: ".github/workflows/README.md"
      provides: "CI/CD documentation including enablement instructions"
      contains: "Enabling GitHub Actions section"
    - path: ".planning/phases/11-testing-infrastructure/CI-CD-PLACEHOLDER.md"
      provides: "Documentation of CI/CD setup options"
      contains: "Option 1: Enable GitHub Actions Now, Option 2: Defer to Phase 12"
  key_links:
    - from: ".github/workflows/test.yml"
      to: "tests/"
      via: "test execution in CI/CD environment"
      pattern: "pytest.*tests.*--cov"
    - from: ".github/workflows/README.md"
      to: ".github/workflows/test.yml"
      via: "documentation of workflow purpose and setup"
      pattern: "test.yml.*pytest.*coverage"
---

<objective>
Configure CI/CD with GitHub Actions for automated testing and create placeholder for deferred setup.

Purpose: Set up GitHub Actions workflow to run tests on push and pull request, generate coverage reports, and ensure tests pass in isolated environment. Also create CI-CD-PLACEHOLDER.md to document that CI/CD setup can be deferred to Phase 12 if GitHub Actions are not yet enabled.

Output: GitHub Actions workflow that runs tests and generates coverage reports, plus CI-CD-PLACEHOLDER.md documenting deferred setup option.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-testing-infrastructure/11-RESEARCH.md
@.planning/phases/11-testing-infrastructure/11-01-SUMMARY.md
@.planning/phases/11-testing-infrastructure/11-02-SUMMARY.md
@.planning/phases/11-testing-infrastructure/11-03-SUMMARY.md
@.planning/phases/11-testing-infrastructure/11-04-SUMMARY.md
@.planning/phases/11-testing-infrastructure/11-05-SUMMARY.md
@.planning/phases/11-testing-infrastructure/11-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions workflow for testing</name>
  <files>.github/workflows/test.yml</files>
  <action>
    Create .github/workflows/test.yml with GitHub Actions workflow for automated testing.

    The workflow should:
    - Run on push to main/master branch
    - Run on pull request
    - Set up Python environment
    - Install dependencies from requirements.txt
    - Run tests with pytest
    - Generate coverage report
    - Upload coverage to codecov (optional) or save as artifact

    Example workflow:

    ```yaml
    name: Tests

    on:
      push:
        branches: [ main, master ]
      pull_request:
        branches: [ main, master ]

    jobs:
      test:
        runs-on: ubuntu-latest

        steps:
        - name: Checkout code
          uses: actions/checkout@v4

        - name: Set up Python
          uses: actions/setup-python@v5
          with:
            python-version: '3.13'

        - name: Cache pip dependencies
          uses: actions/cache@v4
          with:
            path: ~/.cache/pip
            key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
            restore-keys: |
              ${{ runner.os }}-pip-

        - name: Install dependencies
          run: |
            python -m pip install --upgrade pip
            pip install -r requirements.txt
            pip install pytest pytest-cov

        - name: Run tests with coverage
          run: |
            pytest tests/ --cov=2_Scripts --cov-report=xml --cov-report=html --cov-report=term -v

        - name: Upload coverage to Codecov (optional)
          uses: codecov/codecov-action@v4
          with:
            file: ./coverage.xml
            fail_ci_if_error: false
          continue-on-error: true

        - name: Upload coverage as artifact
          uses: actions/upload-artifact@v4
          with:
            name: coverage-report
            path: htmlcov/
            retention-days: 30

        - name: Upload test results as artifact
          if: always()
          uses: actions/upload-artifact@v4
          with:
            name: test-results
            path: |
              .pytest_cache/
              test-results/
            retention-days: 30
          continue-on-error: true
    ```

    Note: This is a basic workflow. For production use, consider:
    - Multiple Python versions (3.10, 3.11, 3.12, 3.13)
    - Linting (pylint, flake8, black)
    - Type checking (mypy)
    - Codecov integration for coverage tracking
    - Slack/email notifications on failures

    Reference: https://docs.github.com/en/actions
  </action>
  <verify>
    Run `cat .github/workflows/test.yml` to verify workflow exists
  </verify>
  <done>
    GitHub Actions workflow created with test execution and coverage reporting
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CI/CD documentation</name>
  <files>.github/workflows/README.md</files>
  <action>
    Create .github/workflows/README.md documenting the CI/CD setup.

    Example:

    ```markdown
    # CI/CD Configuration

    ## Workflows

    ### test.yml

    Runs automated tests on every push and pull request.

    **Trigger conditions:**
    - Push to `main` or `master` branch
    - Pull request to `main` or `master` branch

    **Steps:**
    1. Checkout code
    2. Set up Python 3.13
    3. Cache pip dependencies
    4. Install dependencies (requirements.txt)
    5. Run tests with pytest and coverage
    6. Upload coverage to Codecov (optional)
    7. Upload coverage report as artifact
    8. Upload test results as artifact

    **Artifacts:**
    - `coverage-report`: HTML coverage report (retained for 30 days)
    - `test-results`: Test cache and results (retained for 30 days)

    ## Running Tests Locally

    Before pushing, run tests locally:

    ```bash
    # Install dependencies
    pip install -r requirements.txt
    pip install pytest pytest-cov

    # Run all tests
    pytest tests/ -v

    # Run with coverage
    pytest tests/ --cov=2_Scripts --cov-report=html --cov-report=term

    # Run only unit tests (faster)
    pytest tests/unit/ -v

    # Run only integration tests
    pytest tests/integration/ -v -m integration

    # Run tests excluding integration (faster)
    pytest -m "not integration"
    ```

    ## Coverage Reporting

    Coverage targets:
    - Shared modules: 80%+
    - Main pipeline scripts: 50%+

    Coverage is generated in two formats:
    - HTML: `htmlcov/index.html` (view in browser)
    - Terminal: Printed to console
    - XML: `coverage.xml` (for Codecov)

    ## Troubleshooting

    **Tests fail in CI but pass locally:**
    - Check Python version (CI uses 3.13)
    - Check dependencies (CI installs from requirements.txt)
    - Check environment variables (CI may have different env)

    **Coverage not uploading to Codecov:**
    - Ensure Codecov token is set in GitHub Secrets
    - Check codecov-action logs for errors

    **Workflow not triggering:**
    - Check workflow YAML syntax (yamllint)
    - Check trigger conditions (branch names)
    - Check GitHub Actions permissions (Settings > Actions > General)
    ```

    Reference: https://docs.github.com/en/actions
  </action>
  <verify>
    Run `cat .github/workflows/README.md` to verify documentation exists
  </verify>
  <done>
    CI/CD documentation created
  </done>
</task>

<task type="auto">
  <name>Task 3: Create GitHub Actions enablement note</name>
  <files>.github/workflows/README.md</files>
  <action>
    Append to .github/workflows/README.md a note about enabling GitHub Actions in repository settings.

    Add this section to the README:

    ```markdown
    ## Enabling GitHub Actions

    The workflow is created but GitHub Actions may not be enabled yet. To enable:

    1. Go to repository on GitHub
    2. Navigate to Settings > Actions > General
    3. Under "Actions permissions", select:
       - "Allow all actions and reusable workflows" OR
       - "Allow [your organization] and select non-[your organization] actions and reusable workflows"
    4. Click "Save"

    Once enabled, the workflow will automatically run on push and pull requests.

    If you prefer not to enable GitHub Actions yet, see CI-CD-PLACEHOLDER.md for deferred setup options.
    ```

    Reference: https://docs.github.com/en/actions/security-guides/automatic-token-authentication
  </action>
  <verify>
    Run `cat .github/workflows/README.md | grep -A 10 "Enabling GitHub Actions"` to verify section exists
  </verify>
  <done>
    GitHub Actions enablement note added to README
  </done>
</task>

<task type="auto">
  <name>Task 4: Create CI/CD placeholder for deferred setup</name>
  <files>.planning/phases/11-testing-infrastructure/CI-CD-PLACEHOLDER.md</files>
  <action>
    Create .planning/phases/11-testing-infrastructure/CI-CD-PLACEHOLDER.md documenting that CI/CD setup can be deferred to Phase 12.

    Note: This is NOT a decision to defer - it's a placeholder documenting the option. GitHub Actions workflow is created in Task 1, so CI/CD is ready to use once enabled.

    Example:

    ```markdown
    # CI/CD Setup Options

    ## GitHub Actions Workflow Created

    A GitHub Actions workflow has been created in `.github/workflows/test.yml`.

    ## Two Options Available

    ### Option 1: Enable GitHub Actions Now (Recommended)

    The workflow is ready to use. To enable:

    1. Go to repository on GitHub
    2. Navigate to Settings > Actions > General
    3. Under "Actions permissions", select "Allow all actions and reusable workflows"
    4. Click "Save"

    Once enabled, the workflow will automatically run on push and pull requests.

    ### Option 2: Defer to Phase 12

    If you prefer not to enable GitHub Actions yet, that's fine. The workflow is ready when you are.

    Phase 12 may include:
    - Additional workflows (linting, type checking)
    - Coverage reporting integration (Codecov)
    - Monitoring and observability setup
    - Notification systems (Slack, email)

    ## Next Steps

    - Option 1: Enable GitHub Actions in repository settings and push code to trigger first workflow run
    - Option 2: Run tests locally with `pytest tests/` and wait for Phase 12 CI/CD enhancements

    ## References

    - Phase 11 plans: `.planning/phases/11-testing-infrastructure/`
    - GitHub Actions documentation: https://docs.github.com/en/actions
    - pytest documentation: https://docs.pytest.org/
    ```

    Reference: Open Question 1 from 11-RESEARCH.md (CI/CD Integration)
  </action>
  <verify>
    Run `cat .planning/phases/11-testing-infrastructure/CI-CD-PLACEHOLDER.md` to verify placeholder exists
  </verify>
  <done>
    CI-CD placeholder created documenting both enablement and deferral options
  </done>
</task>

</tasks>

<verification>
- .github/workflows/test.yml exists and contains pytest execution
- .github/workflows/README.md documents CI/CD setup including enablement instructions
- .planning/phases/11-testing-infrastructure/CI-CD-PLACEHOLDER.md documents both enablement and deferral options
- Workflow YAML is valid and would run on push/PR once enabled
- Coverage reporting is configured in workflow
</verification>

<success_criteria>
1. GitHub Actions workflow created that runs tests on push/PR (ready to use once enabled)
2. Coverage reporting configured (HTML, XML, terminal)
3. Artifacts uploaded for coverage and test results
4. Documentation created in .github/workflows/README.md including enablement steps
5. CI-CD-PLACEHOLDER.md documents both enablement and deferral options
6. All tests pass in CI/CD environment (or local environment before enablement)
</success_criteria>

<output>
After completion, create `.planning/phases/11-testing-infrastructure/11-07-SUMMARY.md`
</output>
