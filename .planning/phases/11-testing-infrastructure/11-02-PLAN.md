---
phase: 11-testing-infrastructure
plan: 02
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - tests/unit/test_chunked_reader.py
  - tests/unit/test_data_validation.py
  - tests/unit/test_env_validation.py
  - tests/unit/test_subprocess_validation.py
  - tests/unit/test_fuzzy_matching.py
  - tests/unit/test_tenure_calculation.py
  - tests/unit/test_regression_models.py
autonomous: true
must_haves:
  truths:
    - "Unit tests for all shared modules exist (chunked_reader, data_validation, env_validation, subprocess_validation)"
    - "Unit tests for key pipeline functions exist (fuzzy matching, tenure calculation)"
    - "Unit tests for regression model functions exist (CEO clarity, liquidity regressions, takeover hazards)"
    - "Tests use fixtures for test data (DataFrames, Parquet files)"
    - "Coverage for shared modules is 80%+"
    - "All unit tests pass"
  artifacts:
    - path: "tests/unit/test_data_validation.py"
      provides: "Unit tests for data_validation module"
      contains: "test_validate_dataframe_schema_success, test_validate_dataframe_schema_missing_columns"
    - path: "tests/unit/test_env_validation.py"
      provides: "Unit tests for env_validation module"
      contains: "test_validate_env_schema_success, test_validate_env_type_validation"
    - path: "tests/unit/test_subprocess_validation.py"
      provides: "Unit tests for subprocess_validation module"
      contains: "test_validate_script_path_success, test_validate_script_path_outside_directory"
    - path: "tests/unit/test_fuzzy_matching.py"
      provides: "Unit tests for fuzzy matching functions"
      contains: "test_fuzzy_match_by_cusip, test_fuzzy_match_by_ticker"
    - path: "tests/unit/test_tenure_calculation.py"
      provides: "Unit tests for tenure calculation functions"
      contains: "test_calculate_tenure_months, test_calculate_tenure_years"
    - path: "tests/unit/test_regression_models.py"
      provides: "Unit tests for regression model functions"
      contains: "test_estimate_ceo_clarity_basic, test_liquidity_regression_model, test_takeover_hazard_estimation"
  key_links:
    - from: "pytest --cov"
      to: "2_Scripts/shared/"
      via: "coverage collection on shared modules"
      pattern: "coverage.*shared.*80%"
---

<objective>
Add comprehensive unit tests for shared modules and key pipeline functions.

Purpose: Achieve 80%+ coverage for shared modules and test key pipeline functions (fuzzy matching, tenure calculation, regression models) to prevent regressions and ensure code quality.

Output: Unit tests for all shared modules and key pipeline functions with 80%+ coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-testing-infrastructure/11-RESEARCH.md
@.planning/phases/11-testing-infrastructure/11-01-SUMMARY.md
@2_Scripts/shared/data_validation.py
@2_Scripts/shared/env_validation.py
@2_Scripts/shared/subprocess_validation.py
@2_Scripts/shared/chunked_reader.py
@2_Scripts/1_Sample/1.2_LinkEntities.py
@2_Scripts/1_Sample/1.3_BuildTenureMap.py
@2_Scripts/4_Financial-Econometric/4.1_EstimateCeoClarity.py
@2_Scripts/4_Financial-Econometric/4.2_LiquidityRegressions.py
@2_Scripts/4_Financial-Econometric/4.3_TakeoverHazards.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for data_validation module</name>
  <files>tests/unit/test_data_validation.py</files>
  <action>
    Create tests/unit/test_data_validation.py with tests for data_validation module.

    Test functions:
    - `test_validate_dataframe_schema_success` - Should not raise for valid DataFrame with correct schema
    - `test_validate_dataframe_schema_missing_columns` - Should raise DataValidationError for missing required columns
    - `test_validate_dataframe_schema_column_type_mismatch` - Should raise for wrong column types
    - `test_validate_dataframe_schema_value_range_violation` - Should raise for values outside range
    - `test_load_validated_parquet_with_schema` - Should load and validate parquet file
    - `test_load_validated_parquet_without_schema` - Should load without validation if schema=None

    Use fixtures from conftest.py (sample_dataframe, sample_parquet_file).

    Example from RESEARCH.md Pattern 6:

    ```python
    from 2_Scripts.shared.data_validation import (
        validate_dataframe_schema,
        DataValidationError,
        load_validated_parquet,
    )

    def test_validate_dataframe_schema_success(sample_dataframe):
        """Test schema validation passes for valid DataFrame."""
        schema = {
            "required_columns": ["file_name", "Total_Words"],
            "column_types": {"file_name": "object", "Total_Words": "int"},
        }
        # Should not raise
        validate_dataframe_schema(sample_dataframe, "test_schema", Path("test.parquet"))


    def test_validate_dataframe_schema_missing_columns():
        """Test schema validation fails for missing columns."""
        df = pd.DataFrame({"col1": [1, 2]})
        schema = {"required_columns": ["missing_col"]}

        with pytest.raises(DataValidationError, match="Missing columns"):
            validate_dataframe_schema(df, "test_schema", Path("test.parquet"))
    ```

    Reference: Pattern 3 (Parametrization) and Pattern 6 (Testing Schema Validation) from 11-RESEARCH.md
  </action>
  <verify>
    Run `pytest tests/unit/test_data_validation.py -v` to verify all tests pass
    Run `pytest --cov=2_Scripts/shared/data_validation --cov-report=term` to verify coverage
  </verify>
  <done>
    Unit tests for data_validation module pass with 80%+ coverage
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for env_validation module</name>
  <files>tests/unit/test_env_validation.py</files>
  <action>
    Create tests/unit/test_env_validation.py with tests for env_validation module.

    Test functions:
    - `test_validate_env_schema_success` - Should validate all env vars with defaults
    - `test_validate_env_schema_missing_required` - Should raise for missing required env var
    - `test_validate_env_type_validation_int` - Should convert string to int
    - `test_validate_env_type_validation_float` - Should convert string to float
    - `test_validate_env_type_validation_bool` - Should convert string to bool
    - `test_load_and_validate_env` - Should call validate_env_schema and exit on failure

    Use monkeypatch fixture to set/unset environment variables.

    Reference: Pattern 3 (Parametrization) from 11-RESEARCH.md
  </action>
  <verify>
    Run `pytest tests/unit/test_env_validation.py -v` to verify all tests pass
    Run `pytest --cov=2_Scripts/shared/env_validation --cov-report=term` to verify coverage
  </verify>
  <done>
    Unit tests for env_validation module pass with 80%+ coverage
  </done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for subprocess_validation module</name>
  <files>tests/unit/test_subprocess_validation.py</files>
  <action>
    Create tests/unit/test_subprocess_validation.py with tests for subprocess_validation module.

    Test functions:
    - `test_validate_script_path_success` - Should validate and return absolute path for valid script
    - `test_validate_script_path_not_absolute` - Should convert relative path to absolute
    - `test_validate_script_path_outside_directory` - Should raise ValueError for path outside allowed directory
    - `test_validate_script_path_not_in_allowed_dir` - Should raise for path not under allowed directory
    - `test_run_validated_subprocess_success` - Should run script and return result
    - `test_run_validated_subprocess_validation_failure` - Should not run if path validation fails

    Use monkeypatch for subprocess.run to avoid actually running subprocesses in tests.

    Reference: Pattern 3 (Parametrization) and Pattern 6 (Testing Subprocess Validation) from 11-RESEARCH.md
  </action>
  <verify>
    Run `pytest tests/unit/test_subprocess_validation.py -v` to verify all tests pass
    Run `pytest --cov=2_Scripts/shared/subprocess_validation --cov-report=term` to verify coverage
  </verify>
  <done>
    Unit tests for subprocess_validation module pass with 80%+ coverage
  </done>
</task>

<task type="auto">
  <name>Task 4: Add unit tests for fuzzy matching functions</name>
  <files>tests/unit/test_fuzzy_matching.py</files>
  <action>
    Create tests/unit/test_fuzzy_matching.py with tests for fuzzy matching functions in 1.2_LinkEntities.py.

    Identify key fuzzy matching functions from 1.2_LinkEntities.py:
    - Fuzzy matching by CUSIP
    - Fuzzy matching by ticker
    - Fuzzy matching by company name (using rapidfuzz if available)

    Test functions:
    - `test_fuzzy_match_by_cusip_exact_match` - Should match exact CUSIP
    - `test_fuzzy_match_by_cusip_no_match` - Should return None for no match
    - `test_fuzzy_match_by_ticker_exact_match` - Should match exact ticker
    - `test_fuzzy_match_by_ticker_case_insensitive` - Should match ticker regardless of case
    - `test_fuzzy_match_by_name_similarity` - Should match by name similarity (if rapidfuzz available)

    Create sample DataFrames for testing (small datasets with known matches).

    Reference: Pattern 3 (Parametrization) from 11-RESEARCH.md
  </action>
  <verify>
    Run `pytest tests/unit/test_fuzzy_matching.py -v` to verify all tests pass
    Run `pytest --cov=2_Scripts/1_Sample/1.2_LinkEntities --cov-report=term` to verify coverage of fuzzy matching functions
  </verify>
  <done>
    Unit tests for fuzzy matching functions pass
  </done>
</task>

<task type="auto">
  <name>Task 5: Add unit tests for tenure calculation functions</name>
  <files>tests/unit/test_tenure_calculation.py</files>
  <action>
    Create tests/unit/test_tenure_calculation.py with tests for tenure calculation functions in 1.3_BuildTenureMap.py.

    Identify key tenure calculation functions from 1.3_BuildTenureMap.py:
    - Calculate tenure in months
    - Calculate tenure in years
    - Build tenure map from call dates

    Test functions:
    - `test_calculate_tenure_months_single_call` - Should calculate 0 months for single call
    - `test_calculate_tenure_months_multiple_calls` - Should calculate correct months between calls
    - `test_calculate_tenure_months_same_day` - Should handle multiple calls on same day
    - `test_calculate_tenure_years_conversion` - Should convert months to years
    - `test_tenure_boundary_values` - Should handle edge cases (first call, last call)

    Create sample DataFrames for testing (small datasets with known tenure values).

    Reference: Pattern 3 (Parametrization) from 11-RESEARCH.md
  </action>
  <verify>
    Run `pytest tests/unit/test_tenure_calculation.py -v` to verify all tests pass
    Run `pytest --cov=2_Scripts/1_Sample/1.3_BuildTenureMap --cov-report=term` to verify coverage of tenure calculation functions
  </verify>
  <done>
    Unit tests for tenure calculation functions pass
  </done>
</task>

<task type="auto">
  <name>Task 6: Add unit tests for regression model functions</name>
  <files>tests/unit/test_regression_models.py</files>
  <action>
    Create tests/unit/test_regression_models.py with tests for regression model functions in Step 4 scripts.

    Identify key regression model functions from Step 4 scripts:
    - 4.1_EstimateCeoClarity.py: CEO clarity estimation functions
    - 4.2_LiquidityRegressions.py: Liquidity regression functions
    - 4.3_TakeoverHazards.py: Takeover hazard estimation functions

    Test functions:
    - `test_estimate_ceo_clarity_basic` - Should estimate CEO clarity from linguistic measures
    - `test_estimate_ceo_clarity_edge_cases` - Should handle missing values, zero variance
    - `test_liquidity_regression_model` - Should fit liquidity regression model
    - `test_liquidity_regression_coefficients` - Should extract and validate coefficients
    - `test_takeover_hazard_estimation` - Should estimate takeover hazard probability
    - `test_takeover_hazard_edge_cases` - Should handle boundary values

    Create sample DataFrames for testing (small datasets with known regression results).

    Use @pytest.mark.skipif to skip tests if Step 4 scripts don't exist.

    Reference: Pattern 3 (Parametrization) and Pattern 6 (Testing with Edge Cases) from 11-RESEARCH.md
  </action>
  <verify>
    Run `pytest tests/unit/test_regression_models.py -v` to verify all tests pass (or skip gracefully if files not found)
    Run `pytest --cov=2_Scripts/4_Financial-Econometric --cov-report=term` to verify coverage of regression model functions
  </verify>
  <done>
    Unit tests for regression model functions pass (or skip gracefully if files not found)
  </done>
</task>

<task type="auto">
  <name>Task 7: Verify coverage targets for shared modules</name>
  <files>
    tests/unit/test_data_validation.py
    tests/unit/test_env_validation.py
    tests/unit/test_subprocess_validation.py
    tests/unit/test_chunked_reader.py
  </files>
  <action>
    Run coverage report for all shared modules and verify 80%+ coverage target.

    Run:
    ```bash
    pytest tests/unit/ --cov=2_Scripts/shared --cov-report=term --cov-report=html
    ```

    Check coverage percentages for each module:
    - chunked_reader.py: 80%+
    - data_validation.py: 80%+
    - env_validation.py: 80%+
    - subprocess_validation.py: 80%+

    If any module is below 80%, add additional tests to cover untested code paths.

    Review HTML coverage report (htmlcov/index.html) to identify untested lines and add tests.

    Reference: Success Criteria from 11-RESEARCH.md (80%+ coverage for shared modules)
  </action>
  <verify>
    Run `pytest tests/unit/ --cov=2_Scripts/shared --cov-report=term` and verify all modules show 80%+ coverage
    Open htmlcov/index.html and verify coverage report is complete
  </verify>
  <done>
    All shared modules achieve 80%+ coverage
  </done>
</task>

</tasks>

<verification>
- pytest tests/unit/ -v passes all tests
- pytest tests/unit/test_data_validation.py passes
- pytest tests/unit/test_env_validation.py passes
- pytest tests/unit/test_subprocess_validation.py passes
- pytest tests/unit/test_fuzzy_matching.py passes
- pytest tests/unit/test_tenure_calculation.py passes
- pytest tests/unit/test_regression_models.py passes (or skips gracefully if files not found)
- pytest tests/unit/ --cov=2_Scripts/shared --cov-report=term shows 80%+ coverage for all shared modules
- HTML coverage report generated in htmlcov/
</verification>

<success_criteria>
1. Unit tests for all shared modules (chunked_reader, data_validation, env_validation, subprocess_validation) exist and pass
2. Unit tests for key pipeline functions (fuzzy matching, tenure calculation) exist and pass
3. Unit tests for regression model functions (CEO clarity, liquidity regressions, takeover hazards) exist and pass
4. Tests use fixtures for test data (sample_dataframe, sample_parquet_file)
5. Tests use @pytest.mark.parametrize for edge cases
6. Coverage for shared modules is 80%+
7. All unit tests pass: `pytest tests/unit/`
</success_criteria>

<output>
After completion, create `.planning/phases/11-testing-infrastructure/11-02-SUMMARY.md`
</output>
