---
phase: 11-testing-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/conftest.py
  - tests/unit/test_chunked_reader.py
autonomous: true
must_haves:
  truths:
    - "pytest discovers all tests in tests/ directory"
    - "pytest configuration is in pyproject.toml"
    - "Shared fixtures are defined in conftest.py"
    - "Existing test_chunked_reader.py moved from 2_Scripts/shared/ to tests/unit/"
    - "pytest --collect-only shows all tests"
    - "pytest --cov can generate coverage reports"
  artifacts:
    - path: "pyproject.toml"
      provides: "pytest configuration and discovery rules"
      contains: "[tool.pytest.ini_options]"
    - path: "tests/conftest.py"
      provides: "Shared fixtures for test data management"
      exports: ["test_data_dir", "sample_dataframe", "sample_parquet_file"]
    - path: "tests/unit/"
      provides: "Unit test directory structure"
      contains: "test_*.py files"
    - path: "tests/integration/"
      provides: "Integration test directory structure"
    - path: "tests/regression/"
      provides: "Regression test directory structure"
    - path: "tests/fixtures/"
      provides: "Test data files directory"
    - path: "tests/fixtures/sample_parquet/"
      provides: "Sample parquet files for testing"
    - path: "tests/fixtures/sample_yaml/"
      provides: "Sample YAML files for testing"
  key_links:
    - from: "pytest --collect-only"
      to: "tests/"
      via: "test discovery via testpaths configuration"
      pattern: "pytest.*collect.*tests"
    - from: "pytest --cov=2_Scripts"
      to: "2_Scripts/"
      via: "coverage collection on all scripts"
      pattern: "coverage.*source.*2_Scripts"
    - from: "tests/conftest.py"
      to: "tests/"
      via: "fixture injection into test functions"
      pattern: "fixture.*test_data_dir"
---

<objective>
Set up pytest framework with modern pyproject.toml configuration and organize test structure.

Purpose: Establish the foundation for comprehensive testing with pytest, following modern configuration patterns (pyproject.toml instead of pytest.ini), and creating proper directory structure for unit, integration, and regression tests.

Output: Configured pytest framework with test discovery, shared fixtures, and organized test directory structure.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-testing-infrastructure/11-RESEARCH.md
@2_Scripts/shared/test_chunked_reader.py
@config/project.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml with pytest configuration</name>
  <files>pyproject.toml</files>
  <action>
    Create pyproject.toml with pytest configuration following modern practices:

    ```toml
    [tool.pytest.ini_options]
    minversion = "8.0"
    addopts = [
        "-ra",  # Show extra test summary info
        "-q",   # Quiet mode (less output)
        "--strict-markers",  # Error on unknown markers
        "--strict-config",   # Error on invalid config options
        "--import-mode=importlib",  # Use importlib for import mode (recommended)
    ]
    testpaths = [
        "tests",
    ]
    python_files = [
        "test_*.py",
        "*_test.py",
    ]
    python_classes = [
        "Test*",
    ]
    python_functions = [
        "test_*",
    ]
    markers = [
        "slow: marks tests as slow (deselect with '-m \"not slow\"')",
        "integration: marks tests as integration tests",
        "regression: marks tests as regression tests",
        "unit: marks tests as unit tests",
    ]

    [tool.coverage.run]
    source = ["2_Scripts"]
    omit = [
        "*/tests/*",
        "*/__pycache__/*",
        "*/ARCHIVE*/*",
        "*/ARCHIVE_OLD*/*",
    ]

    [tool.coverage.report]
    exclude_lines = [
        "pragma: no cover",
        "def __repr__",
        "raise AssertionError",
        "raise NotImplementedError",
        "if __name__ == .__main__.:",
        "if TYPE_CHECKING:",
    ]
    ```

    Add pytest dependencies to requirements.txt:
    ```
    pytest>=8.0.0
    pytest-cov>=7.0.0
    ```

    Reference: Pattern 1 from 11-RESEARCH.md
  </action>
  <verify>
    Run `pytest --version` to verify pytest is installed
    Run `pytest --collect-only` to verify test discovery (should show no tests yet)
  </verify>
  <done>
    pytest configuration in pyproject.toml, pytest discovers tests/ directory
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests/ directory structure</name>
  <files>
    tests/conftest.py
    tests/unit/
    tests/integration/
    tests/regression/
    tests/fixtures/
    tests/fixtures/sample_parquet/
    tests/fixtures/sample_yaml/
    tests/fixtures/sample_csv/
  </files>
  <action>
    Create the following directory structure:
    - tests/
      - conftest.py (shared fixtures)
      - unit/ (unit tests)
      - integration/ (integration tests)
      - regression/ (regression tests)
      - fixtures/ (test data files)
        - sample_parquet/
        - sample_yaml/
        - sample_csv/

    Create conftest.py with shared fixtures following Pattern 2 from 11-RESEARCH.md:

    ```python
    # tests/conftest.py - Shared fixtures
    import pytest
    from pathlib import Path
    import pandas as pd
    import pyarrow.parquet as pq

    @pytest.fixture(scope="session")
    def test_data_dir():
        """Path to test data directory."""
        return Path(__file__).parent / "fixtures"


    @pytest.fixture
    def sample_dataframe():
        """Create a sample DataFrame for testing."""
        return pd.DataFrame({
            "file_name": ["test1.docx", "test2.docx"],
            "Total_Words": [100, 200],
            "MaQaUnc_pct": [0.5, 0.75],
        })


    @pytest.fixture
    def sample_parquet_file(tmp_path, sample_dataframe):
        """Create a temporary Parquet file for testing."""
        file_path = tmp_path / "test_data.parquet"
        sample_dataframe.to_parquet(file_path)
        return file_path


    @pytest.fixture
    def sample_config_path(test_data_dir):
        """Path to sample project.yaml for testing."""
        config_path = test_data_dir / "sample_yaml" / "project.yaml"
        if not config_path.exists():
            pytest.skip(f"Sample config not found: {config_path}")
        return config_path
    ```

    Copy config/project.yaml to tests/fixtures/sample_yaml/project.yaml as sample test data.

    Reference: Pattern 2 from 11-RESEARCH.md
  </action>
  <verify>
    Run `ls -la tests/` to verify directory structure
    Run `pytest --collect-only` to verify fixtures are loaded
  </verify>
  <done>
    tests/ directory structure created with conftest.py containing shared fixtures
  </done>
</task>

<task type="auto">
  <name>Task 3: Move existing test to tests/unit/</name>
  <files>
    tests/unit/test_chunked_reader.py
  </files>
  <action>
    Move 2_Scripts/shared/test_chunked_reader.py to tests/unit/test_chunked_reader.py

    Update imports in the test file to use the new path:
    - Change `from chunked_reader import ...` to `from 2_Scripts.shared.chunked_reader import ...`

    Delete 2_Scripts/shared/test_chunked_reader.py after successful move (or keep as reference with comment).

    The test file should now follow pytest conventions (tests in tests/ directory, not in application code).

    Reference: Anti-Pattern 1 from 11-RESEARCH.md (tests in application code directory)
  </action>
  <verify>
    Run `pytest tests/unit/test_chunked_reader.py -v` to verify tests run
    Run `pytest --collect-only` to verify test is discovered
  </verify>
  <done>
    test_chunked_reader.py moved to tests/unit/ and all tests pass
  </done>
</task>

<task type="auto">
  <name>Task 4: Configure coverage reporting</name>
  <files>
    pyproject.toml
  </files>
  <action>
    Ensure pyproject.toml has coverage configuration (added in Task 1).

    Create README in tests/ directory to document test structure:
    ```markdown
    # Test Directory Structure

    ## Directories
    - `unit/` - Unit tests for individual functions and modules
    - `integration/` - Integration tests for end-to-end pipeline steps
    - `regression/` - Regression tests for output stability
    - `fixtures/` - Test data files (Parquet, YAML, CSV)

    ## Running Tests
    ```bash
    # Run all tests
    pytest

    # Run with coverage
    pytest --cov=2_Scripts --cov-report=html --cov-report=term

    # Run specific test category
    pytest -m unit
    pytest -m integration
    pytest -m regression

    # Run specific test file
    pytest tests/unit/test_chunked_reader.py -v

    # Run tests excluding slow tests
    pytest -m "not slow"
    ```

    ## Coverage Targets
    - Shared modules: 80%+
    - Main pipeline scripts: 50%+
    ```

    Reference: Pitfall 2 from 11-RESEARCH.md (no test data management) - README provides guidance
  </action>
  <verify>
    Run `pytest --cov=2_Scripts --cov-report=term` to verify coverage reporting works
    Run `pytest --cov=2_Scripts --cov-report=html` to generate HTML coverage report
  </verify>
  <done>
    Coverage reporting configured and HTML report generated successfully
  </done>
</task>

</tasks>

<verification>
- pytest --version shows version 8.0+
- pytest --collect-only shows all tests in tests/ directory
- tests/ directory structure exists with unit/, integration/, regression/, fixtures/
- conftest.py provides shared fixtures (test_data_dir, sample_dataframe, sample_parquet_file)
- test_chunked_reader.py moved from 2_Scripts/shared/ to tests/unit/
- pytest tests/unit/test_chunked_reader.py passes
- pytest --cov=2_Scripts generates coverage report
- HTML coverage report generated in htmlcov/
</verification>

<success_criteria>
1. pytest configured in pyproject.toml with test discovery rules
2. tests/ directory structure created with conftest.py
3. Shared fixtures defined for test data management
4. test_chunked_reader.py moved to tests/unit/
5. All tests pass: `pytest tests/`
6. Coverage reporting works: `pytest --cov=2_Scripts --cov-report=html`
7. HTML coverage report generated in htmlcov/
</success_criteria>

<output>
After completion, create `.planning/phases/11-testing-infrastructure/11-01-SUMMARY.md`
</output>
