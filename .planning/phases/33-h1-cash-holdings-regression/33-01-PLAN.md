---
phase: 33-h1-cash-holdings-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/4_Econometric_V2/4.1_H1CashHoldingsRegression.py
  - 4_Outputs/4_Econometric_V2/{timestamp}/H1_Regression_Results.parquet
  - 4_Outputs/4_Econometric_V2/{timestamp}/stats.json
  - 4_Outputs/4_Econometric_V2/{timestamp}/H1_RESULTS.md
  - 3_Logs/4_Econometric_V2/{timestamp}_H1.log
autonomous: true

must_haves:
  truths:
    - "Speech uncertainty measures from Step 2 are merged with H1 variables at firm-year level"
    - "OLS regression runs with CashHoldings_{t+1} as DV and Uncertainty*Leverage interaction"
    - "Standard errors are clustered at firm level for all specifications"
    - "Coefficient table reports beta1, beta3 with significance tests for H1 hypothesis"
    - "stats.json contains R-squared, N, F-stat, all coefficients for all 24 regressions"
  artifacts:
    - path: "2_Scripts/4_Econometric_V2/4.1_H1CashHoldingsRegression.py"
      provides: "H1 regression script with data loading, aggregation, regression execution"
      min_lines: 700
    - path: "4_Outputs/4_Econometric_V2/{timestamp}/H1_Regression_Results.parquet"
      provides: "All regression coefficients, SEs, p-values, diagnostics for 24 regressions"
    - path: "4_Outputs/4_Econometric_V2/{timestamp}/stats.json"
      provides: "Variable distributions, merge rates, regression summaries, execution metadata"
    - path: "4_Outputs/4_Econometric_V2/{timestamp}/H1_RESULTS.md"
      provides: "Human-readable summary of key regression findings"
  key_links:
    - from: "4.1_H1CashHoldingsRegression.py"
      to: "shared/panel_ols.py"
      via: "import run_panel_ols"
      pattern: "from shared.panel_ols import run_panel_ols"
    - from: "4.1_H1CashHoldingsRegression.py"
      to: "shared/centering.py"
      via: "import center_continuous"
      pattern: "from shared.centering import center_continuous"
    - from: "4.1_H1CashHoldingsRegression.py"
      to: "H1_CashHoldings.parquet"
      via: "get_latest_output_dir load"
      pattern: "H1_CashHoldings.parquet"
    - from: "4.1_H1CashHoldingsRegression.py"
      to: "linguistic_variables_*.parquet"
      via: "speech uncertainty data load"
      pattern: "linguistic_variables_\\d{4}.parquet"
---

<objective>
Run and validate panel OLS regressions for H1 hypothesis: whether speech uncertainty predicts higher cash holdings and whether leverage moderates this effect.

Purpose: Test the core H1 hypothesis (vague managers hoard more cash; leverage disciplines this behavior) using 6 uncertainty measures across 4 regression specifications (24 total regressions).

Output: 
- `4.1_H1CashHoldingsRegression.py` script with full regression pipeline
- `H1_Regression_Results.parquet` with all coefficients and diagnostics
- `stats.json` with execution metadata and regression summaries
- `H1_RESULTS.md` with human-readable findings
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-h1-cash-holdings-regression/33-CONTEXT.md
@.planning/phases/33-h1-cash-holdings-regression/33-RESEARCH.md

# Existing infrastructure (Phase 32)
@2_Scripts/shared/panel_ols.py
@2_Scripts/shared/centering.py
@2_Scripts/shared/diagnostics.py

# Pattern reference (Phase 29)
@2_Scripts/3_Financial_V2/3.1_H1Variables.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create H1 Cash Holdings Regression Script</name>
  <files>
    2_Scripts/4_Econometric_V2/4.1_H1CashHoldingsRegression.py
  </files>
  <action>
Create `4.1_H1CashHoldingsRegression.py` (target ~800-1000 lines) following existing project patterns (3.1_H1Variables.py, 4.1_EstimateCeoClarity.py).

**Script Structure:**

1. **Contract Header** (lines 1-50):
   - ID: 4.1_H1CashHoldingsRegression
   - Description: Panel OLS regressions for H1 (Speech Uncertainty & Cash Holdings)
   - Declared Inputs: H1_CashHoldings.parquet, linguistic_variables_*.parquet
   - Declared Outputs: H1_Regression_Results.parquet, stats.json, H1_RESULTS.md
   - Deterministic: true

2. **Imports and Configuration** (lines 51-120):
   - Standard imports: sys, os, argparse, pathlib, datetime, pandas, numpy, yaml, json
   - Add parent to sys.path for shared imports
   - Import from shared: path_utils (validate_output_path, ensure_output_dir, get_latest_output_dir), observability_utils (DualWriter, print_stat, save_stats), panel_ols (run_panel_ols), centering (center_continuous), diagnostics (check_multicollinearity, MulticollinearityError)

3. **Constants** (lines 121-160):
   ```python
   UNCERTAINTY_MEASURES = [
       'Manager_QA_Uncertainty_pct',
       'CEO_QA_Uncertainty_pct',
       'Manager_QA_Weak_Modal_pct',
       'CEO_QA_Weak_Modal_pct',
       'Manager_Pres_Uncertainty_pct',
       'CEO_Pres_Uncertainty_pct',
   ]
   
   CONTROL_VARS = [
       'firm_size', 'tobins_q', 'roa', 'capex_at',
       'dividend_payer', 'ocf_volatility', 'current_ratio'
   ]
   
   SPECS = {
       'primary': {'entity_effects': True, 'time_effects': True, 'double_cluster': False},
       'pooled': {'entity_effects': False, 'time_effects': False, 'double_cluster': False},
       'year_only': {'entity_effects': False, 'time_effects': True, 'double_cluster': False},
       'double_cluster': {'entity_effects': True, 'time_effects': True, 'double_cluster': True},
   }
   ```

4. **Path Setup Functions** (lines 161-220):
   - `load_config()` — read config/project.yaml
   - `setup_paths(config, timestamp)` — resolve all input/output paths using get_latest_output_dir
     - H1 variables: `4_Outputs/3_Financial_V2/` (find latest with H1_CashHoldings.parquet)
     - Speech uncertainty: `4_Outputs/2_Textual_Analysis/2.2_Variables/` (find latest)
     - Output: `4_Outputs/4_Econometric_V2/{timestamp}/`
     - Logs: `3_Logs/4_Econometric_V2/{timestamp}_H1.log`

5. **Data Loading Functions** (lines 221-320):
   - `load_h1_variables(h1_dir)` — load H1_CashHoldings.parquet, ensure gvkey is str.zfill(6)
   - `load_speech_uncertainty(speech_dir, uncertainty_cols)` — load all linguistic_variables_*.parquet files, concatenate, select only needed columns (file_name, gvkey, start_date + uncertainty_cols)

6. **Aggregation Function** (lines 321-380):
   - `aggregate_speech_to_firmyear(speech_df, uncertainty_cols)`:
     - Extract fiscal_year from start_date: `pd.to_datetime(df['start_date']).dt.year`
     - Group by ['gvkey', 'fiscal_year']
     - Aggregate: mean of uncertainty columns, count of file_name as 'n_calls'
     - Return DataFrame at firm-year level
     - Log: "Aggregated {N} calls to {M} firm-years, mean {X} calls per firm-year"

7. **Data Preparation Function** (lines 381-480):
   - `prepare_regression_data(h1_df, speech_agg_df, uncertainty_cols)`:
     - Merge H1 variables with aggregated speech data on ['gvkey', 'fiscal_year'], how='inner'
     - Log merge rate: "Merged: {N} obs ({X}% of H1 data)"
     - Create lead DV: sort by ['gvkey', 'fiscal_year'], then `df.groupby('gvkey')['cash_holdings'].shift(-1)`
     - Drop NaN in cash_holdings_lead
     - Log: "Lead variable created, dropped {N} obs (last year per firm)"
     - Return regression-ready DataFrame

8. **Single Regression Function** (lines 481-600):
   - `run_single_h1_regression(df, uncertainty_var, spec_name, spec_config, control_vars)`:
     - Copy DataFrame to avoid side effects
     - Center variables: `center_continuous(df, [uncertainty_var, 'leverage'])` — creates uncertainty_var_c, leverage_c
     - Create interaction: `df[f'{uncertainty_var}_x_leverage'] = df[f'{uncertainty_var}_c'] * df['leverage_c']`
     - Build exog list: [uncertainty_c, leverage_c, interaction] + control_vars
     - Pre-flight VIF check on CONTROL_VARS only (not interaction terms) — STRICT mode, fail_on_violation=True
     - Call run_panel_ols():
       - dependent='cash_holdings_lead'
       - exog=exog list
       - entity_col='gvkey', time_col='fiscal_year'
       - entity_effects, time_effects per spec_config
       - cov_type='clustered'
       - cluster_cols=['gvkey'] if not double_cluster, else ['gvkey', 'fiscal_year']
     - Extract results: coefficients, R2, N, F-stat
     - Perform one-tailed hypothesis tests:
       - beta1 > 0 (uncertainty coefficient): p_one_tail = p_two_tail / 2 if beta > 0, else 1 - p_two_tail/2
       - beta3 < 0 (interaction coefficient): p_one_tail = p_two_tail / 2 if beta < 0, else 1 - p_two_tail/2
     - Return dict with all results, means used for centering, hypothesis test outcomes

9. **Main Regression Loop** (lines 601-720):
   - `run_all_h1_regressions(reg_df, uncertainty_measures, specs, control_vars)`:
     - Initialize results list
     - For each uncertainty_var in uncertainty_measures:
       - For each spec_name, spec_config in specs:
         - Print: "Running: {uncertainty_var} x {spec_name}"
         - result = run_single_h1_regression(...)
         - Append to results
         - Print: "  N={N}, R2={R2:.4f}, beta1={beta1:.4f} (p={p1:.4f}), beta3={beta3:.4f} (p={p3:.4f})"
     - Return list of all 24 regression results

10. **Output Functions** (lines 721-850):
    - `save_regression_results(results, output_dir)`:
      - Convert results to DataFrame with columns: spec, uncertainty_var, variable, coefficient, se, t_stat, p_value, n_obs, r_squared, r_squared_within, f_stat, f_pvalue, hypothesis_test
      - Save as H1_Regression_Results.parquet
    - `generate_results_markdown(results, output_dir)`:
      - Create H1_RESULTS.md with:
        - Header: "# H1 Cash Holdings Regression Results"
        - Summary table: Primary specification results for all 6 uncertainty measures
        - Hypothesis test outcomes: which measures support beta1>0 and beta3<0
        - Key findings narrative
    - `save_stats(stats, output_dir)`:
      - Structure per RESEARCH.md pattern: step_id, timestamp, input, processing, output, regressions, timing, memory

11. **CLI and Main** (lines 851-950):
    - ArgumentParser with --help, --dry-run
    - main() function:
      - Parse args
      - Generate timestamp
      - Load config
      - Setup paths
      - Initialize DualWriter for logging
      - Print script header with git SHA, config snapshot
      - Load H1 variables
      - Load speech uncertainty
      - Aggregate to firm-year
      - Prepare regression data
      - Run all regressions
      - Save outputs
      - Print summary statistics
      - Print completion message

**Key Implementation Notes:**

- Use existing shared modules (panel_ols.py, centering.py, diagnostics.py) — do NOT reimplement
- STRICT diagnostics: if VIF > 5 on any control variable, script must exit with error (per CONTEXT.md)
- Console output should be standard verbosity: announce each regression, show key metrics
- Ensure gvkey is zero-padded 6-character string throughout
- Handle edge case: firms with only 1 call in a year (n_calls=1 is valid, not an error)
- Handle edge case: H1 data has multiple obs per gvkey-year from firm controls merge — take mean of controls before regression
  </action>
  <verify>
    - Script exists at 2_Scripts/4_Econometric_V2/4.1_H1CashHoldingsRegression.py
    - Script has proper contract header with ID, inputs, outputs, deterministic flag
    - python 2_Scripts/4_Econometric_V2/4.1_H1CashHoldingsRegression.py --help shows usage
    - Script imports from shared modules (panel_ols, centering, diagnostics)
    - UNCERTAINTY_MEASURES list contains all 6 required measures
    - SPECS dict contains all 4 specifications (primary, pooled, year_only, double_cluster)
  </verify>
  <done>
    Script file created with complete regression pipeline. Script can be invoked with --help. All shared module imports are correct. Script is ready for execution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute H1 Regressions and Validate Outputs</name>
  <files>
    4_Outputs/4_Econometric_V2/{timestamp}/H1_Regression_Results.parquet
    4_Outputs/4_Econometric_V2/{timestamp}/stats.json
    4_Outputs/4_Econometric_V2/{timestamp}/H1_RESULTS.md
    3_Logs/4_Econometric_V2/{timestamp}_H1.log
  </files>
  <action>
Execute the H1 regression script and validate all outputs meet requirements.

**Execution:**
```bash
python 2_Scripts/4_Econometric_V2/4.1_H1CashHoldingsRegression.py
```

**Expected Console Output (verify these appear):**
1. Script header with timestamp, git SHA
2. Data loading messages:
   - "Loaded H1 variables: {N} rows"
   - "Loaded speech uncertainty: {N} calls"
   - "Aggregated to {N} firm-years"
   - "Merged: {N} obs"
   - "Lead variable created, dropped {N} obs"
3. For each of 24 regressions:
   - "Running: {uncertainty_var} x {spec_name}"
   - "  N={N}, R2={R2:.4f}, beta1={beta1} (p={p1}), beta3={beta3} (p={p3})"
4. Output file confirmations
5. Execution summary (timing, memory)

**Output Validation:**

1. **H1_Regression_Results.parquet:**
   - Load and verify: `df = pd.read_parquet(output_dir / 'H1_Regression_Results.parquet')`
   - Should have 24 x ~10 = ~240 rows (24 regressions x ~10 variables each)
   - Required columns: spec, uncertainty_var, variable, coefficient, se, t_stat, p_value
   - Verify all 6 uncertainty measures appear
   - Verify all 4 specs appear

2. **stats.json:**
   - Load and verify structure
   - Required keys: step_id, timestamp, input, processing, output, regressions
   - regressions key should have 24 entries
   - Each regression entry should have: N, rsquared, f_statistic, coefficients, hypothesis_tests

3. **H1_RESULTS.md:**
   - Verify file exists and is readable
   - Contains summary table with all primary specification results
   - Contains hypothesis test outcomes

4. **Requirement Verification:**
   - **H1-06 (Merge):** stats.json shows merge_rate > 0
   - **H1-07 (OLS spec):** Regression runs with DV=cash_holdings_lead, includes interaction term
   - **H1-08 (Clustered SE):** All specs use cov_type='clustered' (verify in logs)
   - **H1-09 (Hypothesis tests):** stats.json contains hypothesis_tests with beta1>0 and beta3<0 outcomes
   - **H1-10 (Output):** All 3 output files exist with correct content

**Troubleshooting (if script fails):**
- If VIF error: Check which control variable failed, investigate multicollinearity
- If merge rate is low: Check gvkey format consistency between datasets
- If linearmodels error: Verify linearmodels is installed and df is properly indexed
  </action>
  <verify>
    - Script completes without error
    - H1_Regression_Results.parquet exists in 4_Outputs/4_Econometric_V2/{timestamp}/
    - stats.json exists with 24 regression entries
    - H1_RESULTS.md exists with summary tables
    - Log file exists in 3_Logs/4_Econometric_V2/
    - stats.json shows merge_rate > 50% (at least half of H1 data matched)
    - All 6 uncertainty measures appear in results
    - All 4 specification types appear in results
  </verify>
  <done>
    Script executed successfully. All 24 regressions completed. H1_Regression_Results.parquet contains coefficients for all specifications. stats.json contains regression summaries and hypothesis test outcomes. H1_RESULTS.md provides human-readable summary. Requirements H1-06 through H1-10 are satisfied.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Requirement H1-06 (Merge):** Verify in stats.json that speech uncertainty was successfully merged with H1 variables at firm-year level. Check merge_rate field.

2. **Requirement H1-07 (OLS spec):** Verify regression results contain:
   - Dependent variable: cash_holdings_lead (t+1)
   - Independent variables: uncertainty_c, leverage_c, uncertainty_x_leverage
   - Fixed effects: Firm + Year for primary spec
   - Controls: firm_size, tobins_q, roa, capex_at, dividend_payer, ocf_volatility, current_ratio

3. **Requirement H1-08 (Clustered SE):** Verify all regressions used clustered standard errors at firm level (check logs for cov_type='clustered', cluster_cols=['gvkey']).

4. **Requirement H1-09 (Hypothesis tests):** Check stats.json for hypothesis_tests entries showing:
   - beta1 > 0 test (uncertainty coefficient positive = vagueness increases cash)
   - beta3 < 0 test (interaction coefficient negative = leverage attenuates)

5. **Requirement H1-10 (Output):** Verify all outputs exist:
   - H1_Regression_Results.parquet with all coefficients
   - stats.json with R-squared, N, F-stat, all coefficients
   - H1_RESULTS.md with human-readable summary
</verification>

<success_criteria>
- [ ] Script 4.1_H1CashHoldingsRegression.py exists and runs without error
- [ ] 24 regressions completed (6 uncertainty measures x 4 specifications)
- [ ] H1_Regression_Results.parquet exists with all coefficients and diagnostics
- [ ] stats.json exists with regression summaries and hypothesis test outcomes
- [ ] H1_RESULTS.md exists with human-readable summary
- [ ] Requirements H1-06, H1-07, H1-08, H1-09, H1-10 all verified as satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/33-h1-cash-holdings-regression/33-01-SUMMARY.md`
</output>
