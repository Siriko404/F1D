---
phase: 74-testing-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 74-01
files_modified:
  - tests/unit/test_financial_utils.py
  - tests/unit/test_panel_ols.py
  - tests/unit/test_iv_regression.py
  - tests/unit/test_data_validation.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Tier 1 modules (financial_utils, panel_ols, iv_regression) have 90%+ test coverage"
    - "Tests use factory fixtures from 74-01 instead of inline fixtures"
    - "All critical code paths in Tier 1 modules are tested"
    - "Edge cases and error conditions are covered"
  artifacts:
    - path: "tests/unit/test_financial_utils.py"
      provides: "Unit tests for financial calculations"
      contains: "def test_"
    - path: "tests/unit/test_panel_ols.py"
      provides: "Unit tests for panel OLS regression"
      contains: "def test_"
    - path: "tests/unit/test_iv_regression.py"
      provides: "Unit tests for IV/2SLS regression"
      contains: "def test_"
    - path: "tests/unit/test_data_validation.py"
      provides: "Unit tests for data validation"
      contains: "def test_"
  key_links:
    - from: "tests/unit/*.py"
      to: "src/f1d/shared/*.py"
      via: "import statements"
      pattern: "from f1d\\.shared"
    - from: "tests/unit/*.py"
      to: "tests/factories/*.py"
      via: "pytest fixture injection"
      pattern: "_factory"
---

<objective>
Add comprehensive unit tests for Tier 1 modules to achieve 90% coverage target.

Purpose: Ensure critical financial and econometric functions are thoroughly tested with edge cases covered.

Output: Updated test files with factory fixture usage and improved coverage for Tier 1 modules.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context (factory fixtures)
@.planning/phases/74-testing-infrastructure/74-01-PLAN.md

# Tier 1 modules to test
@src/f1d/shared/financial_utils.py
@src/f1d/shared/panel_ols.py
@src/f1d/shared/iv_regression.py
@src/f1d/shared/data_validation.py

# Existing tests
@tests/unit/test_financial_utils.py
@tests/unit/test_panel_ols.py
@tests/unit/test_iv_regression.py
@tests/unit/test_data_validation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update and enhance financial_utils tests</name>
  <files>tests/unit/test_financial_utils.py</files>
  <action>
Enhance tests/unit/test_financial_utils.py to achieve 90% coverage:

1. First, run coverage to identify gaps:
   pytest tests/unit/test_financial_utils.py --cov=src/f1d/shared/financial_utils --cov-report=term-missing

2. Update existing fixtures to use factory fixtures from 74-01:
   - Replace inline sample_compustat_df with sample_compustat_factory
   - Replace inline sample_compustat_row with sample_financial_row_factory

3. Add missing test cases for uncovered code paths:
   - Test all functions in financial_utils.py that are currently untested
   - Add edge case tests (empty DataFrames, missing columns, NaN values)
   - Add error condition tests (invalid inputs, missing required fields)

4. Specific test additions based on common gaps:
   - Test calculate_firm_controls with various Compustat column combinations
   - Test error handling when gvkey not found in Compustat data
   - Test behavior with negative values (debt, assets)
   - Test division by zero protection in financial ratios

5. Ensure all tests use f1d.shared import pattern:
   - from f1d.shared.financial_utils import function_name

Do NOT remove existing tests - only add new tests and update fixtures.
  </action>
  <verify>
pytest tests/unit/test_financial_utils.py --cov=src/f1d/shared/financial_utils --cov-report=term-missing --cov-fail-under=90
  </verify>
  <done>
test_financial_utils.py achieves 90%+ coverage, uses factory fixtures, all code paths tested.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update and enhance panel_ols tests</name>
  <files>tests/unit/test_panel_ols.py</files>
  <action>
Enhance tests/unit/test_panel_ols.py to achieve 90% coverage:

1. First, run coverage to identify gaps:
   pytest tests/unit/test_panel_ols.py --cov=src/f1d/shared/panel_ols --cov-report=term-missing

2. Update existing fixtures to use factory fixtures:
   - Replace inline sample_panel_data with sample_panel_data_factory

3. Add missing test cases:
   - Test run_panel_ols with entity_effects=True/False combinations
   - Test run_panel_ols with time_effects=True/False combinations
   - Test collinearity detection (VIF calculation)
   - Test multicollinearity error handling
   - Test behavior with missing dependent/independent columns
   - Test behavior with insufficient observations

4. Edge case tests:
   - Single firm (no entity variation)
   - Single time period (no time variation)
   - Perfect collinearity between independent variables
   - Missing data (NaN in columns)

5. Update imports to use src-layout pattern:
   - from f1d.shared.panel_ols import run_panel_ols, CollinearityError

6. Add skip marker for linearmodels if not available:
   - @pytest.mark.skipif(not LINEARMODELS_AVAILABLE, reason="linearmodels not available")
  </action>
  <verify>
pytest tests/unit/test_panel_ols.py --cov=src/f1d/shared/panel_ols --cov-report=term-missing --cov-fail-under=90
  </verify>
  <done>
test_panel_ols.py achieves 90%+ coverage, uses factory fixtures, handles linearmodels availability gracefully.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update and enhance iv_regression tests</name>
  <files>tests/unit/test_iv_regression.py</files>
  <action>
Enhance tests/unit/test_iv_regression.py to achieve 90% coverage:

1. First, run coverage to identify gaps:
   pytest tests/unit/test_iv_regression.py --cov=src/f1d/shared/iv_regression --cov-report=term-missing

2. Update existing fixtures to use factory fixtures:
   - Replace inline panel data with sample_panel_data_factory
   - Create additional instrument variables as needed

3. Add missing test cases:
   - Test run_iv_regression with various instrument combinations
   - Test first-stage F-statistic calculation
   - Test weak instrument detection
   - Test overidentification tests (if applicable)
   - Test error handling for invalid inputs

4. Edge case tests:
   - Insufficient instruments
   - Weak instruments (low first-stage F)
   - Missing columns (endogenous, instruments, dependent)
   - Perfect multicollinearity among instruments

5. Update imports:
   - from f1d.shared.iv_regression import run_iv_regression, IVError

6. Add appropriate skip markers for linearmodels dependency
  </action>
  <verify>
pytest tests/unit/test_iv_regression.py --cov=src/f1d/shared/iv_regression --cov-report=term-missing --cov-fail-under=90
  </verify>
  <done>
test_iv_regression.py achieves 90%+ coverage, uses factory fixtures, handles dependencies gracefully.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update and enhance data_validation tests</name>
  <files>tests/unit/test_data_validation.py</files>
  <action>
Enhance tests/unit/test_data_validation.py to achieve 90% coverage:

1. First, run coverage to identify gaps:
   pytest tests/unit/test_data_validation.py --cov=src/f1d/shared/data_validation --cov-report=term-missing

2. Update existing fixtures to use factory fixtures where applicable

3. Add missing test cases:
   - Test validate_dataframe with various column requirements
   - Test check_required_columns with present/missing columns
   - Test validate_dtypes with various data types
   - Test validation error messages for clarity

4. Edge case tests:
   - Empty DataFrame
   - DataFrame with all NaN columns
   - DataFrame with mixed types in columns
   - Very large DataFrames (performance consideration)

5. Error handling tests:
   - Missing required columns raises appropriate error
   - Wrong dtype columns raise appropriate error
   - Error messages include column names

6. Update imports:
   - from f1d.shared.data_validation import validate_dataframe, ValidationError
  </action>
  <verify>
pytest tests/unit/test_data_validation.py --cov=src/f1d/shared/data_validation --cov-report=term-missing --cov-fail-under=90
  </verify>
  <done>
test_data_validation.py achieves 90%+ coverage, all validation functions tested, clear error messages verified.
  </done>
</task>

</tasks>

<verification>
Run all Tier 1 unit tests with coverage:
pytest tests/unit/test_financial_utils.py tests/unit/test_panel_ols.py tests/unit/test_iv_regression.py tests/unit/test_data_validation.py --cov=src/f1d/shared --cov-report=term-missing

Verify overall coverage meets tier requirements:
- financial_utils: 90%+
- panel_ols: 90%+
- iv_regression: 90%+
- data_validation: 90%+
</verification>

<success_criteria>
- All four Tier 1 test files updated to use factory fixtures
- Each module achieves 90%+ test coverage
- Edge cases and error conditions covered
- Tests use src-layout import pattern (from f1d.shared.*)
- All tests pass with pytest
</success_criteria>

<output>
After completion, create `.planning/phases/74-testing-infrastructure/74-02-SUMMARY.md`
</output>
