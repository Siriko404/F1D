---
phase: 74-testing-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 74-01
files_modified:
  - tests/integration/test_config_integration.py
  - tests/integration/test_logging_integration.py
  - tests/integration/test_observability_integration.py
  - tests/unit/test_path_utils.py
  - tests/unit/test_chunked_reader.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Tier 2 modules have 80%+ test coverage"
    - "Integration tests verify multi-module interactions"
    - "Configuration, logging, and observability integrations tested"
    - "path_utils and chunked_reader tested with real file operations"
  artifacts:
    - path: "tests/integration/test_config_integration.py"
      provides: "Integration tests for configuration system"
      contains: "def test_"
    - path: "tests/integration/test_logging_integration.py"
      provides: "Integration tests for logging system"
      contains: "def test_"
    - path: "tests/unit/test_path_utils.py"
      provides: "Unit tests for path utilities"
      contains: "def test_"
    - path: "tests/unit/test_chunked_reader.py"
      provides: "Unit tests for chunked file reading"
      contains: "def test_"
  key_links:
    - from: "tests/integration/*.py"
      to: "src/f1d/shared/**/*.py"
      via: "multi-module imports"
      pattern: "from f1d\\."
    - from: "tests/unit/*.py"
      to: "src/f1d/shared/path_utils.py"
      via: "import"
      pattern: "path_utils"
---

<objective>
Add comprehensive integration tests for Tier 2 modules to achieve 80% coverage target.

Purpose: Verify multi-module interactions work correctly and Tier 2 modules have adequate test coverage.

Output: Updated integration tests and new unit tests for path_utils and chunked_reader achieving 80% coverage.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context (factory fixtures)
@.planning/phases/74-testing-infrastructure/74-01-PLAN.md

# Tier 2 modules to test
@src/f1d/shared/path_utils.py
@src/f1d/shared/chunked_reader.py
@src/f1d/shared/config/loader.py
@src/f1d/shared/logging/handlers.py

# Existing integration tests
@tests/integration/test_config_integration.py
@tests/integration/test_logging_integration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance config integration tests</name>
  <files>tests/integration/test_config_integration.py</files>
  <action>
Enhance tests/integration/test_config_integration.py to achieve 80% coverage:

1. First, run coverage to identify gaps:
   pytest tests/integration/test_config_integration.py --cov=src/f1d/shared/config --cov-report=term-missing

2. Update existing fixtures to use factory fixtures from 74-01:
   - Replace inline config creation with sample_config_yaml_factory
   - Replace inline env vars with sample_env_vars_factory

3. Add integration test cases:
   - Test full config loading pipeline: YAML -> ProjectConfig -> validated instance
   - Test environment variable override of YAML values
   - Test config caching behavior
   - Test config reload on file change (if applicable)

4. Test multi-module integration:
   - Config provides paths to data_loading module
   - Config provides logging settings to logging module
   - Config validation integrates with pydantic validation

5. Edge case tests:
   - Missing config file handling
   - Invalid YAML syntax handling
   - Missing required fields handling
   - Type coercion (string to int, etc.)

6. Update imports:
   - from f1d.shared.config import ProjectConfig, load_config
   - from f1d.shared.config.loader import ConfigLoader
  </action>
  <verify>
pytest tests/integration/test_config_integration.py --cov=src/f1d/shared/config --cov-report=term-missing --cov-fail-under=80
  </verify>
  <done>
test_config_integration.py achieves 80%+ coverage for config module, uses factory fixtures, tests multi-module integration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance logging integration tests</name>
  <files>tests/integration/test_logging_integration.py</files>
  <action>
Enhance tests/integration/test_logging_integration.py to achieve 80% coverage:

1. First, run coverage to identify gaps:
   pytest tests/integration/test_logging_integration.py --cov=src/f1d/shared/logging --cov-report=term-missing

2. Update fixtures to use factory fixtures where applicable

3. Add integration test cases:
   - Test dual output (console + file) logging
   - Test structlog context binding across function calls
   - Test JSON formatting for file output
   - Test ConsoleRenderer for console output
   - Test log level filtering

4. Test multi-module integration:
   - Logging configured from config module settings
   - Context binding with operation IDs
   - Error logging with stack traces

5. Edge case tests:
   - Logging with very long messages
   - Logging with unicode characters
   - Logging with None/null values in context
   - Log file rotation (if applicable)

6. Verify log output files:
   - Check JSON format in file output
   - Verify operation IDs propagate through logs
   - Check timestamp formatting
  </action>
  <verify>
pytest tests/integration/test_logging_integration.py --cov=src/f1d/shared/logging --cov-report=term-missing --cov-fail-under=80
  </verify>
  <done>
test_logging_integration.py achieves 80%+ coverage for logging module, tests dual output and context binding.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create path_utils unit tests</name>
  <files>tests/unit/test_path_utils.py</files>
  <action>
Create or enhance tests/unit/test_path_utils.py to achieve 80% coverage:

1. First, check if file exists and run coverage:
   pytest tests/unit/test_path_utils.py --cov=src/f1d/shared/path_utils --cov-report=term-missing

2. If file does not exist, create it with this structure:
   - Module docstring explaining test coverage
   - Import path_utils functions from f1d.shared.path_utils
   - Test class organization

3. Test cases for path_utils functions:
   - Test resolve_data_path with various inputs
   - Test path validation (exists/not exists)
   - Test path normalization (relative/absolute)
   - Test path joining operations
   - Test file extension handling

4. Edge case tests:
   - Paths with spaces
   - Paths with unicode characters
   - Very long paths
   - Invalid path characters (platform-specific)
   - Symbolic links (if applicable)

5. Test with tmp_path fixture for file operations:
   - Create temporary files/directories
   - Test path resolution to temp locations
   - Clean up after tests

6. Use src-layout imports:
   - from f1d.shared.path_utils import resolve_data_path, validate_path
  </action>
  <verify>
pytest tests/unit/test_path_utils.py --cov=src/f1d/shared/path_utils --cov-report=term-missing --cov-fail-under=80
  </verify>
  <done>
test_path_utils.py exists with 80%+ coverage, tests all path utilities with edge cases.
  </done>
</task>

<task type="auto">
  <name>Task 4: Enhance chunked_reader unit tests</name>
  <files>tests/unit/test_chunked_reader.py</files>
  <action>
Enhance tests/unit/test_chunked_reader.py to achieve 80% coverage:

1. First, run coverage to identify gaps:
   pytest tests/unit/test_chunked_reader.py --cov=src/f1d/shared/chunked_reader --cov-report=term-missing

2. Update fixtures to use factory fixtures for test data generation

3. Test cases for chunked_reader functions:
   - Test read_in_chunks with various chunk sizes
   - Test chunk iteration
   - Test memory efficiency (large files)
   - Test with different file formats (Parquet, CSV)

4. Edge case tests:
   - Empty file
   - File with single row
   - File with row count exactly matching chunk size
   - Very large chunk sizes
   - Chunk size of 1

5. Test chunk processing:
   - Process chunks in loop
   - Accumulate results across chunks
   - Error handling within chunks

6. Update imports:
   - from f1d.shared.chunked_reader import read_in_chunks, ChunkedReader

7. Add performance consideration note:
   - Mark slow tests with @pytest.mark.slow
   - Use smaller test files for unit tests
  </action>
  <verify>
pytest tests/unit/test_chunked_reader.py --cov=src/f1d/shared/chunked_reader --cov-report=term-missing --cov-fail-under=80
  </verify>
  <done>
test_chunked_reader.py achieves 80%+ coverage, tests all chunking operations with edge cases, slow tests marked.
  </done>
</task>

</tasks>

<verification>
Run all Tier 2 tests with coverage:
pytest tests/integration/test_config_integration.py tests/integration/test_logging_integration.py tests/unit/test_path_utils.py tests/unit/test_chunked_reader.py --cov=src/f1d/shared --cov-report=term-missing

Verify tier coverage:
- config module: 80%+
- logging module: 80%+
- path_utils: 80%+
- chunked_reader: 80%+
</verification>

<success_criteria>
- All Tier 2 test files updated/created to use factory fixtures
- Each module achieves 80%+ test coverage
- Integration tests verify multi-module interactions
- Unit tests cover edge cases
- Tests use src-layout import pattern
- All tests pass with pytest
</success_criteria>

<output>
After completion, create `.planning/phases/74-testing-infrastructure/74-03-SUMMARY.md`
</output>
