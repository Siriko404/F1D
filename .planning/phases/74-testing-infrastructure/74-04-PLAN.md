---
phase: 74-testing-infrastructure
plan: 04
type: execute
wave: 3
depends_on:
  - 74-02
  - 74-03
files_modified:
  - pyproject.toml
  - .github/workflows/ci.yml
autonomous: true
user_setup: []

must_haves:
  truths:
    - "CI pipeline runs tests with coverage reporting"
    - "CI fails when coverage drops below tier thresholds"
    - "Coverage reports are generated in multiple formats (XML, JSON, HTML)"
    - "Tier-specific coverage gates are enforced"
  artifacts:
    - path: "pyproject.toml"
      provides: "Coverage configuration with tier thresholds"
      contains: "fail_under"
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with coverage steps"
      contains: "pytest.*--cov"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "pytest coverage configuration"
      pattern: "--cov-fail-under"
    - from: "CI"
      to: "Codecov (optional)"
      via: "coverage.xml upload"
      pattern: "codecov"
---

<objective>
Configure CI pipeline to enforce tier-based coverage thresholds and generate coverage reports.

Purpose: Ensure code quality is maintained by failing CI when test coverage drops below required thresholds.

Output: Updated pyproject.toml with tier-specific thresholds and CI workflow with coverage gates.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context
@.planning/phases/073-cicd-pipeline/73-02-SUMMARY.md
@.planning/phases/74-testing-infrastructure/74-02-PLAN.md
@.planning/phases/74-testing-infrastructure/74-03-PLAN.md

# Current configuration
@pyproject.toml
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tier-specific coverage configuration to pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
Update pyproject.toml to add tier-specific coverage thresholds:

1. Review current coverage configuration in [tool.coverage.report]:
   - Current fail_under = 60 (overall baseline)
   - This is correct for overall coverage

2. Add coverage threshold documentation as comments:
   ```toml
   # Coverage thresholds by tier:
   # - Tier 1 (critical): 90%+ for financial_utils, panel_ols, iv_regression
   # - Tier 2 (important): 80%+ for data_validation, path_utils, chunked_reader
   # - Overall minimum: 60% (baseline for entire codebase)
   #
   # CI enforces these via separate pytest invocations with --cov-fail-under
   ```

3. Verify coverage configuration is correct:
   - source = ["src/f1d"] - correct for src-layout
   - branch = true - enables branch coverage
   - omit patterns exclude tests and archives

4. Ensure coverage output formats are configured:
   - HTML report: htmlcov/
   - XML report: coverage.xml (for Codecov)
   - JSON report: coverage.json (for tooling)

5. No changes needed to fail_under (60% overall is correct)
   - Tier-specific enforcement happens in CI workflow
  </action>
  <verify>
grep -A5 "\[tool.coverage" pyproject.toml
  </verify>
  <done>
pyproject.toml has documented tier thresholds, coverage outputs configured for CI integration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CI workflow to enforce tier-based coverage</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Update .github/workflows/ci.yml to add coverage enforcement:

1. Read current ci.yml to understand test job structure

2. Update the test job to run tiered coverage:

   a. Add Tier 1 coverage test step (90% threshold):
      ```yaml
      - name: Run Tier 1 tests with coverage (90% threshold)
        run: |
          pytest tests/unit/test_financial_utils.py \
                 tests/unit/test_panel_ols.py \
                 tests/unit/test_iv_regression.py \
                 tests/unit/test_data_validation.py \
                 --cov=src/f1d/shared \
                 --cov-report=term-missing \
                 --cov-report=xml:coverage-tier1.xml \
                 --cov-fail-under=90 \
                 -v
      ```

   b. Add Tier 2 coverage test step (80% threshold):
      ```yaml
      - name: Run Tier 2 tests with coverage (80% threshold)
        run: |
          pytest tests/integration/ \
                 tests/unit/test_path_utils.py \
                 tests/unit/test_chunked_reader.py \
                 --cov=src/f1d/shared \
                 --cov-report=term-missing \
                 --cov-report=xml:coverage-tier2.xml \
                 --cov-fail-under=80 \
                 -v
      ```

   c. Add full test suite with overall coverage:
      ```yaml
      - name: Run full test suite with coverage (60% overall)
        run: |
          pytest --cov=src/f1d \
                 --cov-report=term-missing \
                 --cov-report=xml \
                 --cov-report=html \
                 --cov-fail-under=60 \
                 -v
      ```

3. Add coverage artifact upload for Codecov (optional):
   ```yaml
   - name: Upload coverage to Codecov
     if: always()
     uses: codecov/codecov-action@v4
     with:
       files: ./coverage.xml
       fail_ci_if_error: false
     env:
       CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
   ```

4. Preserve existing test job structure for E2E tests

5. Ensure test job still runs on matrix (Python 3.9-3.13)
  </action>
  <verify>
grep -c "cov-fail-under" .github/workflows/ci.yml
  </verify>
  <done>
ci.yml has tiered coverage enforcement with 90%, 80%, and 60% thresholds.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify coverage configuration end-to-end</name>
  <files></files>
  <action>
Verify the complete coverage configuration works correctly:

1. Run Tier 1 coverage locally to verify threshold:
   pytest tests/unit/test_financial_utils.py tests/unit/test_panel_ols.py tests/unit/test_iv_regression.py tests/unit/test_data_validation.py --cov=src/f1d/shared --cov-report=term-missing --cov-fail-under=90 -v

2. Run Tier 2 coverage locally:
   pytest tests/integration/ tests/unit/test_path_utils.py tests/unit/test_chunked_reader.py --cov=src/f1d/shared --cov-report=term-missing --cov-fail-under=80 -v

3. Run full test suite:
   pytest --cov=src/f1d --cov-report=term-missing --cov-fail-under=60 -v

4. Verify coverage output files are generated:
   - coverage.xml exists
   - htmlcov/index.html exists
   - coverage.json exists (if configured)

5. Check that coverage reports show correct source paths (src/f1d not 2_Scripts)

If any tests fail due to missing modules or import errors:
- Fix import paths in test files
- Ensure factory fixtures are available
- Verify src-layout imports work correctly

Note: This task may reveal issues that require fixes to test files from 74-02 and 74-03.
  </action>
  <verify>
ls -la coverage.xml htmlcov/index.html 2>/dev/null && echo "Coverage files generated"
  </verify>
  <done>
All coverage thresholds pass locally, output files generated, CI configuration ready.
  </done>
</task>

</tasks>

<verification>
1. Verify pyproject.toml coverage configuration:
   grep -A20 "\[tool.coverage" pyproject.toml

2. Verify CI workflow has coverage steps:
   grep -B2 -A10 "cov-fail-under" .github/workflows/ci.yml

3. Run local coverage test:
   pytest --cov=src/f1d --cov-report=term --cov-fail-under=60
</verification>

<success_criteria>
- pyproject.toml documents tier-specific coverage thresholds
- ci.yml has separate test steps for Tier 1 (90%), Tier 2 (80%), and overall (60%)
- Coverage reports generated in XML, HTML, and JSON formats
- Local coverage test passes with current test suite
- CI workflow will fail on coverage threshold violations
</success_criteria>

<output>
After completion, create `.planning/phases/74-testing-infrastructure/74-04-SUMMARY.md`
</output>
