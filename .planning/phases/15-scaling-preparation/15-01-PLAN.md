---
phase: 15-scaling-preparation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [2_Scripts/shared/parallel_utils.py]
autonomous: true

must_haves:
  truths:
    - "Parallel workers can generate independent random streams"
    - "Random streams are deterministic (same seed = same results)"
    - "No adjacent seed collision (worker streams don't overlap)"
    - "Utility provides SeedSequence spawning per NumPy best practices"
  artifacts:
    - path: "2_Scripts/shared/parallel_utils.py"
      provides: "Deterministic random number generation for parallel workers"
      exports: ["spawn_worker_rng", "get_deterministic_random"]
      min_lines: 30
    - path: "2_Scripts/shared/README.md"
      contains: "parallel_utils.py usage"
  key_links:
    - from: "parallel_utils.py"
      to: "numpy.random.SeedSequence"
      via: "SeedSequence spawning pattern"
      pattern: "SeedSequence.*spawn"
---

<objective>
Create deterministic random number generation utilities for parallel processing to ensure reproducible results.

Purpose: Parallel processing must be deterministic for research replication. Using NumPy's SeedSequence spawning ensures each worker gets an independent random stream while maintaining overall reproducibility.

Output: New shared/parallel_utils.py module with spawn_worker_rng() function for deterministic RNG in parallel workers.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-scaling-preparation/15-RESEARCH.md
@.planning/TECHNICAL_REMEDIATION_ROADMAP.md

# Existing infrastructure
@config/project.yaml (thread_count: 1, random_seed: 42)
@2_Scripts/shared/README.md (shared modules pattern)
</context>

<tasks>

<task type="auto">
  <name>Create parallel_utils.py with deterministic RNG spawning</name>
  <files>2_Scripts/shared/parallel_utils.py</files>
  <action>
    Create 2_Scripts/shared/parallel_utils.py with the following functions:

    1. spawn_worker_rng(worker_id: int, root_seed: int) -> numpy.random.Generator
       - Creates deterministic RNG stream for a specific worker
       - Uses SeedSequence spawning pattern: [worker_id, root_seed]
       - Returns numpy.random.Generator instance
       - Includes docstring explaining determinism guarantees

    2. get_deterministic_random(seed: int) -> numpy.random.Generator
       - Convenience function for single-threaded deterministic RNG
       - Returns numpy.random.Generator with provided seed
       - Includes docstring with usage example

    Import numpy at top. Add module docstring explaining purpose and reference to NumPy parallel RNG documentation.

    Reference: 15-RESEARCH.md Pattern 1 (Deterministic Parallelization with Seed Propagation)

    Avoid: manual seed addition (root_seed + worker_id) - adjacent seeds produce similar streams
  </action>
  <verify>python -c "from shared.parallel_utils import spawn_worker_rng, get_deterministic_random; rng1 = get_deterministic_random(42); print(rng1.integers(0, 100, size=5))"</verify>
  <done>parallel_utils.py exists with two functions, imports numpy, proper docstrings, references NumPy documentation</done>
</task>

<task type="auto">
  <name>Update shared/README.md with parallel_utils documentation</name>
  <files>2_Scripts/shared/README.md</files>
  <action>
    Add section to 2_Scripts/shared/README.md:

    ## Deterministic Parallel RNG (parallel_utils.py)

    Provides deterministic random number generation for parallel processing.

    **Functions:**
    - `spawn_worker_rng(worker_id, root_seed)`: Create deterministic RNG for parallel workers
    - `get_deterministic_random(seed)`: Create deterministic RNG for single-threaded use

    **Usage:**
    ```python
    from shared.parallel_utils import spawn_worker_rng

    # In parallel worker function
    rng = spawn_worker_rng(worker_id, root_seed=42)
    result = rng.uniform(size=1000)
    ```

    **Determinism:** Uses NumPy's SeedSequence spawning to ensure independent streams while maintaining reproducibility.

    Include reference to NumPy parallel RNG docs.
  </action>
  <verify>grep -q "parallel_utils.py" 2_Scripts/shared/README.md && grep -q "SeedSequence" 2_Scripts/shared/README.md</verify>
  <done>README.md includes parallel_utils section with usage examples and determinism explanation</done>
</task>

<task type="auto">
  <name>Add unit tests for parallel_utils</name>
  <files>tests/unit/test_parallel_utils.py</files>
  <action>
    Create tests/unit/test_parallel_utils.py with the following tests:

    1. test_spawn_worker_rng_reproducibility:
       - Same worker_id + same root_seed produces same random sequence
       - Assert: arrays are equal (np.array_equal)

    2. test_spawn_worker_rng_independence:
       - Different worker_ids produce different random sequences
       - Assert: arrays are not equal

    3. test_get_deterministic_random_reproducibility:
       - Same seed produces same sequence
       - Assert: arrays are equal

    4. test_seed_sequence_no_collision:
       - Spawn multiple workers, ensure no duplicate sequences
       - Assert: all 100 random sequences are unique

    Use pytest fixtures and parametrize where appropriate. Import pytest and numpy.

    Reference: 15-RESEARCH.md Testing Strategy (benchmark determinism)
  </action>
  <verify>pytest tests/unit/test_parallel_utils.py -v</verify>
  <done>test_parallel_utils.py exists with 4 tests, all pass, covers reproducibility, independence, no collision</done>
</task>

</tasks>

<verification>
Run: python -c "from shared.parallel_utils import spawn_worker_rng; rng1 = spawn_worker_rng(0, 42); rng2 = spawn_worker_rng(0, 42); assert (rng1.integers(0, 100, size=10) == rng2.integers(0, 100, size=10)).all(), 'Reproducibility failed'"

Verify that same worker_id + root_seed produces identical random sequences.
</verification>

<success_criteria>
1. parallel_utils.py created with spawn_worker_rng and get_deterministic_random functions
2. Functions use SeedSequence spawning pattern (not manual seed addition)
3. Unit tests verify reproducibility, independence, and no collision
4. shared/README.md documents usage and determinism guarantees
5. pytest tests all pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-scaling-preparation/15-01-SUMMARY.md`
</output>
