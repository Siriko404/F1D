---
phase: 15-scaling-preparation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/1_Sample/1.2_LinkEntities.py
  - 2_Scripts/1_Sample/1.4_AssembleManifest.py
  - 2_Scripts/3_Financial/3.2_MarketVariables.py
autonomous: true

must_haves:
  truths:
    - "Scripts only read columns they need from Parquet files"
    - "Memory usage reduced by avoiding unused column loading"
    - "Results identical to full-column loads (same data, less memory)"
  artifacts:
    - path: "2_Scripts/1_Sample/1.2_LinkEntities.py"
      contains: "columns="
      modification: "Added column pruning to Parquet reads"
    - path: "2_Scripts/1_Sample/1.4_AssembleManifest.py"
      contains: "columns="
      modification: "Added column pruning to Parquet reads"
    - path: "2_Scripts/3_Financial/3.2_MarketVariables.py"
      contains: "columns="
      modification: "Added column pruning to Parquet reads"
  key_links:
    - from: "Parquet read statements"
      to: "selected columns only"
      via: "pd.read_parquet(columns=[...])"
      pattern: "read_parquet.*columns"
---

<objective>
Add column pruning to critical scripts that read large Parquet files to reduce memory footprint and I/O overhead.

Purpose: Loading entire Parquet files when only a subset of columns is needed wastes memory and I/O bandwidth. Column pruning is especially important for large intermediate datasets in Step 1 and Step 3.

Output: Updated scripts with column-specific Parquet reads, reducing memory usage for large datasets.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-scaling-preparation/15-RESEARCH.md
@.planning/TECHNICAL_REMEDIATION_ROADMAP.md

# Existing infrastructure
@2_Scripts/shared/chunked_reader.py (read_selected_columns function)
@2_Scripts/shared/README.md (shared modules pattern)

# Critical scripts with large Parquet loads
@2_Scripts/1_Sample/1.2_LinkEntities.py (entity linking)
@2_Scripts/1_Sample/1.4_AssembleManifest.py (manifest assembly)
@2_Scripts/3_Financial/3.2_MarketVariables.py (market data)
</context>

<tasks>

<task type="auto">
  <name>Add column pruning to 1.2_LinkEntities.py</name>
  <files>2_Scripts/1_Sample/1.2_LinkEntities.py</files>
  <action>
    Identify all pd.read_parquet() and pq.read_table() calls in 1.2_LinkEntities.py.

    For each call, trace which columns are used after the read (grep for column references).

    Replace full reads with column-specific reads:

    Example pattern:
    ```python
    # Before:
    df = pd.read_parquet("file.parquet")

    # After:
    df = pd.read_parquet("file.parquet", columns=['col1', 'col2', 'col3'])
    ```

    For reads followed immediately by merge/filter, keep all columns used in those operations.

    Add inline comment: "# Column pruning: only reading needed columns"

    Reference: 15-RESEARCH.md Pattern 2 (PyArrow Column Pruning)

    Verification: Script should produce identical results to baseline (no data loss).
  </action>
  <verify>python 2_Scripts/1_Sample/1.2_LinkEntities.py > 3_Logs/1.2_LinkEntities/test.log 2>&1 && echo "Script executes successfully"</verify>
  <done>All Parquet reads use columns parameter, results identical to baseline, script executes without errors</done>
</task>

<task type="auto">
  <name>Add column pruning to 1.4_AssembleManifest.py</name>
  <files>2_Scripts/1_Sample/1.4_AssembleManifest.py</files>
  <action>
    Identify all pd.read_parquet() and pq.read_table() calls in 1.4_AssembleManifest.py.

    For each call, trace which columns are used after the read.

    Replace full reads with column-specific reads:

    ```python
    # Before:
    df = pd.read_parquet("file.parquet")

    # After:
    df = pd.read_parquet("file.parquet", columns=['col1', 'col2', 'col3'])
    ```

    For large intermediate files (e.g., speaker data, enriched calls), prioritize column pruning - these are likely the largest files.

    Add inline comment: "# Column pruning: only reading needed columns"

    Reference: 15-RESEARCH.md Pattern 2 (PyArrow Column Pruning)
  </action>
  <verify>python 2_Scripts/1_Sample/1.4_AssembleManifest.py > 3_Logs/1.4_AssembleManifest/test.log 2>&1 && echo "Script executes successfully"</verify>
  <done>All Parquet reads use columns parameter, results identical to baseline, script executes without errors</done>
</task>

<task type="auto">
  <name>Add column pruning to 3.2_MarketVariables.py</name>
  <files>2_Scripts/3_Financial/3.2_MarketVariables.py</files>
  <action>
    Identify all pd.read_parquet() and pq.read_table() calls in 3.2_MarketVariables.py.

    For each call, trace which columns are used after the read.

    Replace full reads with column-specific reads:

    ```python
    # Before:
    df = pd.read_parquet("file.parquet")

    # After:
    df = pd.read_parquet("file.parquet", columns=['col1', 'col2', 'col3'])
    ```

    Market data files (CRSP data) are often large - prioritize these for column pruning.

    Add inline comment: "# Column pruning: only reading needed columns"

    Reference: 15-RESEARCH.md Pattern 2 (PyArrow Column Pruning)
  </action>
  <verify>python 2_Scripts/3_Financial/3.2_MarketVariables.py > 3_Logs/3.2_MarketVariables/test.log 2>&1 && echo "Script executes successfully"</verify>
  <done>All Parquet reads use columns parameter, results identical to baseline, script executes without errors</done>
</task>

</tasks>

<verification>
Run each updated script and verify:
1. Script executes without errors
2. Output checksum matches baseline (optional - run pipeline to compare)
3. Memory usage reduced (observe during execution)

Check: grep -r "read_parquet.*columns" 2_Scripts/1_Sample/1.2_LinkEntities.py 2_Scripts/1_Sample/1.4_AssembleManifest.py 2_Scripts/3_Financial/3.2_MarketVariables.py | wc -l (should show column pruning applied)
</verification>

<success_criteria>
1. All three scripts use columns parameter for Parquet reads
2. Scripts execute successfully without errors
3. Results identical to baseline (no data loss from column pruning)
4. Memory usage reduced (qualitative improvement)
</success_criteria>

<output>
After completion, create `.planning/phases/15-scaling-preparation/15-02-SUMMARY.md`
</output>
