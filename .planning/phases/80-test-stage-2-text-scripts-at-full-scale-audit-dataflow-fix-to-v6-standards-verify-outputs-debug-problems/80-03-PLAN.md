---
phase: 80-test-stage-2-text-scripts
plan: 03
type: execute
wave: 3
depends_on: ["80-02"]
files_modified:
  - .planning/verification/80-schema-validation.json
  - .planning/verification/80-data-profile.json
autonomous: true
must_haves:
  truths:
    - "word_counts.parquet has expected columns (file_name, word, count)"
    - "linguistic_variables.parquet has file_name and all linguistic measures"
    - "All numeric values are in valid ranges"
    - "No unexpected null values in required columns"
    - "Row counts match expected values from Stage 1"
  artifacts:
    - path: ".planning/verification/80-schema-validation.json"
      provides: "Schema validation results per output"
    - path: ".planning/verification/80-data-profile.json"
      provides: "Data profile statistics per output"
  key_links:
    - from: "Script docstrings"
      to: "Output schemas"
      via: "Inputs/Outputs section"
---

<objective>
Validate output schemas and data quality for all Stage 2 outputs.

Purpose: Verify that outputs match expected schemas and contain logically consistent values.
Output: JSON validation reports for schema and data quality.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate output schemas</name>
  <files>.planning/verification/80-schema-validation.json</files>
  <action>
    For each output file, validate schema against expected columns:

    **2.1_TokenizeAndCount output (word_counts.parquet):**
    Expected columns (from docstring):
    - file_name (link to manifest)
    - word (tokenized word)
    - count (word occurrence count)
    - Additional columns as implemented

    **2.2_ConstructVariables output (linguistic_variables.parquet):**
    Expected columns (from docstring):
    - file_name (link to manifest)
    - word_count (total words)
    - unique_words (vocabulary size)
    - avg_word_length (mean word length)
    - readability measures (as implemented)
    - complexity measures (as implemented)
    - All columns from Stage 1 manifest (merged)

    Create validation script using pandas to:
    1. Load each parquet file
    2. Check for expected columns
    3. Check column data types
    4. Report missing/unexpected columns

    Store results in `.planning/verification/80-schema-validation.json`
  </action>
  <verify>
    - JSON file exists with valid structure
    - All Stage 2 output files validated
    - Any schema issues documented
  </verify>
  <done>
    Schema validation complete for all Stage 2 outputs.
    All outputs have expected columns with correct types.
    Any deviations documented for review.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate data quality and logical consistency</name>
  <files>.planning/verification/80-data-profile.json</files>
  <action>
    For each output file, validate data quality:

    **2.1_TokenizeAndCount checks:**
    - file_name values exist in Stage 1 manifest
    - word column not null
    - count column > 0
    - No negative counts
    - Reasonable vocabulary size per transcript

    **2.2_ConstructVariables checks:**
    - file_name unique (one row per call)
    - Row count matches Stage 1 manifest (112,968)
    - word_count > 0 for all rows
    - unique_words <= word_count
    - avg_word_length in reasonable range (3-10)
    - No null values in key columns

    Create data profile JSON with:
    - Row counts
    - Null rates per column
    - Value distributions for key columns
    - Any anomalies detected

    Store in `.planning/verification/80-data-profile.json`
  </action>
  <verify>
    - JSON file exists with valid structure
    - All quality checks documented
    - Any issues flagged for review
  </verify>
  <done>
    Data quality validation complete.
    All outputs contain logically consistent values.
    Data profile statistics captured for audit report.
  </done>
</task>

<task type="auth">
  <name>Task 3: Remediate validation failures</name>
  <files>
    4_Outputs/2_Textual_Analysis/*/*.parquet
  </files>
  <action>
    If Tasks 1-2 found any validation failures, fix them:

    Common issues and fixes:
    1. **Missing columns in schema**: Add missing columns to script output
    2. **Wrong data types**: Cast columns to correct types in script
    3. **Null values in required columns**: Check data source, add validation
    4. **Row count mismatch**: Debug filtering logic in script
    5. **Duplicate keys**: Check for data duplication in processing

    Steps:
    1. Review validation errors from 80-schema-validation.json
    2. Identify root cause (script bug vs data issue)
    3. Implement fix in source script
    4. Re-run affected script(s) from Plan 80-02
    5. Re-run validation (Task 1-2) to confirm fix
    6. Document all fixes in 80-data-profile.json "fixes_applied" section

    If no validation failures found, this task is NO-OP (still mark complete).
  </action>
  <verify>
    - If issues existed: All are fixed and validation passes
    - If no issues: Task completes with "no issues to fix" status
    - Re-validation confirms all quality checks pass
  </verify>
  <done>
    All validation failures remediated (or none existed).
    Outputs pass all schema and quality checks.
    Fixes documented in audit trail.
  </done>
</task>

</tasks>

<verification>
1. Schema validation JSON exists with all Stage 2 outputs documented
2. Data profile JSON exists with quality metrics
3. No critical schema mismatches (missing required columns)
4. Data quality issues documented (if any)
5. Any validation failures found in Tasks 1-2 are fixed in Task 3
</verification>

<success_criteria>
- All Stage 2 outputs have expected schemas
- All outputs have logically consistent values
- Any validation issues found are fixed
- Data quality metrics documented in JSON format
</success_criteria>

<output>
After completion, create `.planning/phases/80-test-stage-2-text-scripts-at-full-scale-audit-dataflow-fix-to-v6-standards-verify-outputs-debug-problems/80-03-SUMMARY.md`
</output>
