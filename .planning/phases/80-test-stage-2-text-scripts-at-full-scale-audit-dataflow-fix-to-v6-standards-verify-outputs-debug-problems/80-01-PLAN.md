---
phase: 80-test-stage-2-text-scripts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/verification/80-standards-audit.json
autonomous: true
must_haves:
  truths:
    - "All Stage 2 scripts use f1d.shared.* namespace imports"
    - "No sys.path.insert() calls in any Stage 2 script"
    - "mypy passes on Stage 2 scripts with 0 errors"
  artifacts:
    - path: ".planning/verification/80-standards-audit.json"
      provides: "Standards compliance audit results"
  key_links:
    - from: "src/f1d/text/*.py"
      to: "f1d.shared.*"
      via: "import statements"
      pattern: "from f1d\\.shared\\."
---

<objective>
Verify V6.1 architecture standards compliance for all Stage 2 text scripts before full-scale testing.

Purpose: Ensure codebase meets quality standards before investing time in execution testing.
Output: JSON audit report documenting compliance status per script.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit V6.1 namespace import compliance</name>
  <files>.planning/verification/80-standards-audit.json</files>
  <action>
    Create verification directory if not exists, then audit all Stage 2 scripts for V6.1 compliance:

    1. Check that all imports use `f1d.shared.*` namespace (not legacy `from shared.*`)
    2. Check for zero `sys.path.insert()` calls
    3. Run `mypy src/f1d/text/ --no-error-summary` and capture results

    Scripts to audit:
    - src/f1d/text/tokenize_and_count.py
    - src/f1d/text/construct_variables.py
    - src/f1d/text/report_step2.py
    - src/f1d/text/verify_step2.py

    Use grep to check for:
    - `from f1d.shared.` (should be present)
    - `from shared.` (should be absent - legacy pattern)
    - `sys.path.insert` (should be absent)

    Create structured JSON output at `.planning/verification/80-standards-audit.json` with:
    - Per-script compliance status (imports, sys.path, mypy errors)
    - Overall compliance summary
    - Any issues found with file:line references
  </action>
  <verify>
    - `cat .planning/verification/80-standards-audit.json` shows valid JSON
    - JSON contains results for all 4 scripts
    - mypy command returns 0 errors (or errors are documented)
  </verify>
  <done>
    JSON audit file exists with compliance status for all Stage 2 scripts.
    V6.1 compliance verified: f1d.shared.* imports, zero sys.path.insert, mypy 0 errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify --dry-run execution for all scripts</name>
  <files>.planning/verification/80-dry-run-results.json</files>
  <action>
    Execute --dry-run for each script to validate prerequisites without actual processing:

    ```bash
    python -m f1d.text.tokenize_and_count --dry-run
    python -m f1d.text.construct_variables --dry-run
    python src/f1d/text/report_step2.py --dry-run 2>/dev/null || echo "report_step2 may not support --dry-run"
    python src/f1d/text/verify_step2.py --dry-run 2>/dev/null || echo "verify_step2 may not support --dry-run"
    ```

    For each script, capture:
    - Exit code (0 = success)
    - stdout/stderr output
    - Any missing prerequisite files

    Note: report_step2.py and verify_step2.py may not have --dry-run support (document status).

    Store results in `.planning/verification/80-dry-run-results.json`
  </action>
  <verify>
    - tokenize_and_count dry-run passes or documents prerequisites needed
    - construct_variables dry-run passes or documents prerequisites needed
    - report_step2 and verify_step2 status documented (may not support --dry-run)
  </verify>
  <done>
    All Stage 2 scripts execute --dry-run successfully or have documented prerequisite gaps.
    Prerequisites for full execution understood and documented.
  </done>
</task>

<task type="auth">
  <name>Task 3: Fix V6.1 compliance issues</name>
  <files>
    src/f1d/text/*.py
  </files>
  <action>
    If Task 1 found any V6.1 compliance issues (legacy imports, sys.path.insert, mypy errors), fix them:

    Common fixes:
    1. Replace `from shared.*` with `from f1d.shared.*`
    2. Remove `sys.path.insert()` calls
    3. Fix mypy errors with type hints or type:ignore comments

    After fixing, re-run:
    ```bash
    mypy src/f1d/text/ --no-error-summary
    ```

    Verify all scripts pass with 0 errors.

    If no issues found in Task 1, this task is NO-OP (still mark complete).
  </action>
  <verify>
    - If issues existed: All are fixed and mypy returns 0 errors
    - If no issues: Task completes with "no issues to fix" status
  </verify>
  <done>
    All V6.1 compliance issues fixed (or none existed).
    Scripts fully compliant with architecture standards.
  </done>
</task>

</tasks>

<verification>
1. `.planning/verification/80-standards-audit.json` exists with valid JSON
2. All Stage 2 scripts show f1d.shared.* imports
3. Zero sys.path.insert() calls found
4. mypy passes with 0 errors on Stage 2 scripts
5. `.planning/verification/80-dry-run-results.json` exists with valid JSON
6. Any V6.1 issues found in Task 1 are fixed in Task 3
</verification>

<success_criteria>
- V6.1 compliance verified for all Stage 2 text scripts
- Any compliance issues found are fixed
- Dry-run validation completed for all scripts
</success_criteria>

<output>
After completion, create `.planning/phases/80-test-stage-2-text-scripts-at-full-scale-audit-dataflow-fix-to-v6-standards-verify-outputs-debug-problems/80-01-SUMMARY.md`
</output>
