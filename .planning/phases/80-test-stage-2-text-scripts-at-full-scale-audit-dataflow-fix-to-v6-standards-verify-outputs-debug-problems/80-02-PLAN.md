---
phase: 80-test-stage-2-text-scripts
plan: 02
type: execute
wave: 2
depends_on: ["80-01"]
files_modified:
  - 4_Outputs/2_Textual_Analysis/2.1_TokenizeAndCount/
  - 4_Outputs/2_Textual_Analysis/2.2_ConstructVariables/
  - .planning/verification/80-execution-audit.json
autonomous: true
must_haves:
  truths:
    - "2.1_TokenizeAndCount executes and produces word_counts.parquet"
    - "2.2_ConstructVariables executes and produces linguistic_variables.parquet"
    - "report_step2 executes successfully"
    - "verify_step2 executes successfully"
  artifacts:
    - path: "4_Outputs/2_Textual_Analysis/2.1_TokenizeAndCount/*/word_counts.parquet"
      provides: "Word frequency counts per transcript"
    - path: "4_Outputs/2_Textual_Analysis/2.2_ConstructVariables/*/linguistic_variables.parquet"
      provides: "Call-level linguistic measures"
  key_links:
    - from: "4_Outputs/1.4_AssembleManifest/*/master_sample_manifest.parquet"
      to: "2.1_TokenizeAndCount"
      via: "load_validated_parquet"
    - from: "1_Inputs/transcript/*.txt"
      to: "2.1_TokenizeAndCount"
      via: "file reading"
    - from: "2.1 output"
      to: "2.2_ConstructVariables"
      via: "get_latest_output_dir"
---

<objective>
Execute the complete Stage 2 text processing pipeline at full scale using Stage 1 outputs.

Purpose: Generate production text analysis outputs for all 112,968 earnings calls.
Output: Complete set of Stage 2 outputs in 4_Outputs/2_Textual_Analysis/ directories.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute 2.1_TokenizeAndCount at full scale</name>
  <files>
    4_Outputs/2_Textual_Analysis/2.1_TokenizeAndCount/*/word_counts.parquet
    4_Outputs/2_Textual_Analysis/2.1_TokenizeAndCount/*/report_step_2_1.md
  </files>
  <action>
    Execute Step 2.1 to tokenize transcripts and count words:

    ```bash
    python -m f1d.text.tokenize_and_count
    ```

    Expected behavior:
    - Load master_sample_manifest.parquet from latest Stage 1 output
    - Read transcript .txt files from 1_Inputs/transcript/
    - Tokenize text using sklearn/sklearn-like tokenization
    - Count word occurrences per transcript
    - Handle missing transcript files gracefully (log warnings)

    Expected outputs:
    - word_counts.parquet with columns: file_name, word, count, etc.
    - Row count: varies based on vocabulary (expect ~1M-10M rows)

    Capture execution metrics:
    - Wall-clock time
    - Transcripts processed
    - Total word count records
    - Missing transcript count (if any)
    - Memory usage

    On error: STOP immediately, document error, and prepare for fix.
  </action>
  <verify>
    - Exit code 0
    - Output directory exists: `ls 4_Outputs/2_Textual_Analysis/2.1_TokenizeAndCount/`
    - word_counts.parquet exists and has > 0 rows
    - report_step_2_1.md exists
    - Processing completed within reasonable time (< 1 hour expected)
    - Memory usage within acceptable limits (< 8GB expected)
  </verify>
  <done>
    2.1_TokenizeAndCount executed successfully.
    All transcripts from Stage 1 manifest processed (112,968 calls).
    Word count file created with vocabulary statistics.
    Expected: ~1M-10M word count rows depending on vocabulary.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute 2.2_ConstructVariables at full scale</name>
  <files>
    4_Outputs/2_Textual_Analysis/2.2_ConstructVariables/*/linguistic_variables.parquet
    4_Outputs/2_Textual_Analysis/2.2_ConstructVariables/*/report_step_2_2.md
  </files>
  <action>
    Execute Step 2.2 to construct linguistic variables:

    ```bash
    python -m f1d.text.construct_variables
    ```

    Expected behavior:
    - Load word_counts.parquet from latest 2.1 output
    - Aggregate word-level stats into call-level measures
    - Calculate linguistic variables (complexity, readability, sentiment, etc.)
    - Merge with Stage 1 manifest metadata

    Expected outputs:
    - linguistic_variables.parquet with columns:
      - file_name (link to manifest)
      - word_count, unique_words, avg_word_length
      - readability measures, linguistic complexity, etc.
    - Row count: matches Stage 1 manifest (112,968)

    Capture execution metrics:
    - Wall-clock time
    - Input word count rows
    - Output call-level rows
    - Variables calculated
    - Memory usage

    On error: STOP immediately, document error, and prepare for fix.
  </action>
  <verify>
    - Exit code 0
    - Output directory exists: `ls 4_Outputs/2_Textual_Analysis/2.2_ConstructVariables/`
    - linguistic_variables.parquet exists with ~112,968 rows
    - report_step_2_2.md exists
    - Row count exactly matches Stage 1 manifest (112,968 ± 1%)
    - Processing completed within reasonable time (< 30 minutes expected)
  </verify>
  <done>
    2.2_ConstructVariables executed successfully.
    Linguistic variables constructed for all earnings calls.
    Output row count: 112,968 (matches Stage 1 manifest exactly).
    All call-level linguistic measures calculated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Execute report_step2.py</name>
  <files>
    4_Outputs/2_Textual_Analysis/*/report_step2*.md
  </files>
  <action>
    Execute report_step2.py to generate Stage 2 summary report:

    ```bash
    python src/f1d/text/report_step2.py
    ```

    Expected behavior:
    - Load Stage 2 outputs
    - Generate summary statistics and visualizations
    - Create markdown report

    Note: This script may not have standard CLI arguments. Check behavior.

    Capture:
    - Exit code
    - Output location
    - Any errors
  </action>
  <verify>
    - Script executes without errors
    - Report file generated (if applicable)
  </verify>
  <done>
    report_step2.py executed successfully.
    Stage 2 summary report generated.
  </done>
</task>

<task type="auto">
  <name>Task 4: Execute verify_step2.py and create execution audit</name>
  <files>
    .planning/verification/80-execution-audit.json
  </files>
  <action>
    Execute verify_step2.py to verify Stage 2 outputs:

    ```bash
    python src/f1d/text/verify_step2.py
    ```

    Expected behavior:
    - Verify output file existence
    - Check schema compliance
    - Validate data quality

    After execution, create `.planning/verification/80-execution-audit.json` with:
    - Per-script execution status (pass/fail)
    - Wall-clock time per script
    - Row counts at each stage
    - Any errors encountered
    - Output file locations

    Structure similar to 79-execution-audit.json.
  </action>
  <verify>
    - verify_step2.py executes without errors
    - JSON audit file exists with all 4 scripts documented
    - Execution metrics captured
  </verify>
  <done>
    All Stage 2 scripts executed successfully at full scale.
    Outputs verified and execution audit JSON created.
  </done>
</task>

</tasks>

<verification>
1. All Stage 2 script output directories exist under 4_Outputs/2_Textual_Analysis/
2. Each output directory contains expected parquet files
3. word_counts.parquet has > 0 rows
4. linguistic_variables.parquet has ~112,968 rows
5. Execution audit JSON documents all script results
</verification>

<success_criteria>
- All Stage 2 scripts execute without errors
- All expected output files created
- Row counts within expected ranges
- Execution audit JSON created for tracking
</success_criteria>

<output>
After completion, create `.planning/phases/80-test-stage-2-text-scripts-at-full-scale-audit-dataflow-fix-to-v6-standards-verify-outputs-debug-problems/80-02-SUMMARY.md`
</output>
