---
phase: 81-test-stage-3-financial-scripts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/verification/81-standards-audit.json
autonomous: true
must_haves:
  truths:
    - "All Stage 3 scripts use f1d.shared.* namespace imports"
    - "No sys.path.insert() calls in any Stage 3 script"
    - "mypy passes on Stage 3 scripts with 0 errors"
  artifacts:
    - path: ".planning/verification/81-standards-audit.json"
      provides: "Standards compliance audit results"
  key_links:
    - from: "src/f1d/financial/**/*.py"
      to: "f1d.shared.*"
      via: "import statements"
      pattern: "from f1d\\.shared\\."
---

<objective>
Verify V6.1 architecture standards compliance for all Stage 3 financial scripts before full-scale testing.

Purpose: Ensure codebase meets quality standards before investing time in execution testing.
Output: JSON audit report documenting compliance status per script.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit V6.1 namespace import compliance</name>
  <files>.planning/verification/81-standards-audit.json</files>
  <action>
    Create verification directory if not exists, then audit all Stage 3 scripts for V6.1 compliance:

    1. Check that all imports use `f1d.shared.*` namespace (not legacy `from shared.*`)
    2. Check for zero `sys.path.insert()` calls
    3. Run `mypy src/f1d/financial/ --no-error-summary` and capture results

    Scripts to audit (V1):
    - src/f1d/financial/v1/3.0_BuildFinancialFeatures.py
    - src/f1d/financial/v1/3.1_FirmControls.py
    - src/f1d/financial/v1/3.2_MarketVariables.py
    - src/f1d/financial/v1/3.3_EventFlags.py
    - src/f1d/financial/v1/3.4_Utils.py

    Scripts to audit (V2 - H1-H9 hypothesis scripts):
    - src/f1d/financial/v2/3.1_H1Variables.py
    - src/f1d/financial/v2/3.2_H2Variables.py
    - src/f1d/financial/v2/3.3_H3Variables.py
    - src/f1d/financial/v2/3.5_H5Variables.py
    - src/f1d/financial/v2/3.6_H6Variables.py
    - src/f1d/financial/v2/3.7_H7IlliquidityVariables.py
    - src/f1d/financial/v2/3.8_H8TakeoverVariables.py
    - Plus auxiliary scripts (AnalystDispersionPatch, BiddleInvestmentResidual, etc.)

    Use grep to check for:
    - `from f1d.shared.` (should be present)
    - `from shared.` (should be absent - legacy pattern)
    - `sys.path.insert` (should be absent)

    Create structured JSON output at `.planning/verification/81-standards-audit.json` with:
    - Per-script compliance status (imports, sys.path, mypy errors)
    - Overall compliance summary
    - Any issues found with file:line references
  </action>
  <verify>
    - `cat .planning/verification/81-standards-audit.json` shows valid JSON
    - JSON contains results for all Stage 3 scripts
    - mypy command returns 0 errors (or errors are documented)
  </verify>
  <done>
    JSON audit file exists with compliance status for all Stage 3 scripts.
    V6.1 compliance verified: f1d.shared.* imports, zero sys.path.insert, mypy 0 errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify --dry-run execution for V1 scripts</name>
  <files>.planning/verification/81-dry-run-results.json</files>
  <action>
    Execute --dry-run for each V1 script to validate prerequisites without actual processing:

    ```bash
    python -m f1d.financial.v1.build_financial_features --dry-run
    python -m f1d.financial.v1.firm_controls --dry-run
    python -m f1d.financial.v1.market_variables --dry-run
    python -m f1d.financial.v1.event_flags --dry-run
    ```

    For each script, capture:
    - Exit code (0 = success)
    - stdout/stderr output
    - Any missing prerequisite files

    Note: Script module names may vary - check __main__.py or actual module names.

    Store results in `.planning/verification/81-dry-run-results.json`
  </action>
  <verify>
    - All V1 scripts execute --dry-run successfully or have documented prerequisite gaps
    - Prerequisites for full execution understood and documented
  </verify>
  <done>
    All Stage 3 V1 scripts execute --dry-run successfully or have documented prerequisite gaps.
    Prerequisites for full execution understood and documented.
  </done>
</task>

<task type="auth">
  <name>Task 3: Fix V6.1 compliance issues</name>
  <files>
    src/f1d/financial/**/*.py
  </files>
  <action>
    If Task 1 found any V6.1 compliance issues (legacy imports, sys.path.insert, mypy errors), fix them:

    Common fixes:
    1. Replace `from shared.*` with `from f1d.shared.*`
    2. Remove `sys.path.insert()` calls
    3. Fix mypy errors with type hints or type:ignore comments

    After fixing, re-run:
    ```bash
    mypy src/f1d/financial/ --no-error-summary
    ```

    Verify all scripts pass with 0 errors.

    If no issues found in Task 1, this task is NO-OP (still mark complete).
  </action>
  <verify>
    - If issues existed: All are fixed and mypy returns 0 errors
    - If no issues: Task completes with "no issues to fix" status
  </verify>
  <done>
    All V6.1 compliance issues fixed (or none existed).
    Scripts fully compliant with architecture standards.
  </done>
</task>

</tasks>

<verification>
1. `.planning/verification/81-standards-audit.json` exists with valid JSON
2. All Stage 3 scripts show f1d.shared.* imports
3. Zero sys.path.insert() calls found
4. mypy passes with 0 errors on Stage 3 scripts
5. `.planning/verification/81-dry-run-results.json` exists with valid JSON
6. Any V6.1 issues found in Task 1 are fixed in Task 3
</verification>

<success_criteria>
- V6.1 compliance verified for all Stage 3 financial scripts
- Any compliance issues found are fixed
- Dry-run validation completed for V1 scripts
</success_criteria>

<output>
After completion, create `.planning/phases/81-test-stage-3-financial-scripts-at-full-scale-audit-dataflow-fix-to-v6-standards-verify-outputs-debug-problems/81-01-SUMMARY.md`
</output>
