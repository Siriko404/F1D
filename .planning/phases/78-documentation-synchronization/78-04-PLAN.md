---
phase: 78-documentation-synchronization
plan: 04
type: execute
wave: 2
depends_on: ["78-01"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "No broken internal links in documentation"
    - "All relative paths resolve correctly"
    - "README links to existing files"
  artifacts: []
  key_links: []
---

<objective>
Verify all internal documentation links are valid and fix any broken references.

Purpose: Ensure documentation integrity - no 404 links or missing file references.
Output: Report of link status and any fixes applied.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check internal documentation links</name>
  <files>README.md, 2_Scripts/shared/README.md, docs/*.md</files>
  <action>
Scan all markdown files for internal links and verify they exist:

1. Check links in README.md:
   - `DEPENDENCIES.md` - verify exists
   - `UPGRADE_GUIDE.md` - verify exists
   - `ROADMAP.md` - verify exists (should be .planning/ROADMAP.md)
   - `2_Scripts/SCALING.md` - verify exists
   - `config/project.yaml` - verify exists

2. Check links in 2_Scripts/shared/README.md:
   - `../SCALING.md` - verify exists
   - Any pyarrow/pandas documentation links (external - skip)

3. Check docs/*.md files for internal references

4. For each broken link:
   - If file moved, update the path
   - If file doesn't exist, either create stub or remove reference

Run this command to find all markdown links:
```bash
grep -r "\[.*\](.*)" --include="*.md" | grep -v "http" | head -50
```
  </action>
  <verify>All internal links resolve to existing files</verify>
  <done>Documentation link audit complete, broken links fixed</done>
</task>

<task type="auto">
  <name>Task 2: Fix ROADMAP.md reference in README.md</name>
  <files>README.md</files>
  <action>
The README.md references ROADMAP.md but it should be .planning/ROADMAP.md:

1. Find the line: `See ROADMAP.md for detailed phase-by-phase documentation.`
2. Update to: `See .planning/ROADMAP.md for detailed phase-by-phase documentation.`

3. Also check: `- **ROADMAP.md**: Project phases, plans, and progress tracking`
4. Update to: `- **.planning/ROADMAP.md**: Project phases, plans, and progress tracking`
  </action>
  <verify>grep -c ".planning/ROADMAP" README.md returns at least 2</verify>
  <done>ROADMAP.md paths corrected in README.md</done>
</task>

<task type="auto">
  <name>Task 3: Verify SCALING.md reference</name>
  <files>README.md, 2_Scripts/shared/README.md</files>
  <action>
Check if SCALING.md exists in 2_Scripts/:

1. Run: `ls 2_Scripts/SCALING.md`
2. If exists, verify README.md links to it correctly
3. If doesn't exist, either:
   a. Create a minimal SCALING.md stub that redirects to main README
   b. Remove references from README.md

The README.md currently references:
- `[2_Scripts/SCALING.md](2_Scripts/SCALING.md)`
- `[SCALING.md](2_Scripts/SCALING.md)`
  </action>
  <verify>SCALING.md link resolves correctly or is removed</verify>
  <done>SCALING.md reference is valid</done>
</task>

</tasks>

<verification>
1. All internal links in README.md resolve
2. All internal links in shared/README.md resolve
3. No broken relative paths in documentation
</verification>

<success_criteria>
- No broken internal documentation links
- All referenced files exist
- README.md links are correct
</success_criteria>

<output>
After completion, create `.planning/phases/78-documentation-synchronization/78-04-SUMMARY.md`
</output>
