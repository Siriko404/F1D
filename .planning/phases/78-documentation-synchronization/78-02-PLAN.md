---
phase: 78-documentation-synchronization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/1_Sample/README.md
  - 2_Scripts/2_Text/README.md
  - 2_Scripts/3_Financial/README.md
  - 2_Scripts/4_Econometric/README.md
  - 2_Scripts/3_Financial_V2/README.md
  - 2_Scripts/4_Econometric_V2/README.md
autonomous: true

must_haves:
  truths:
    - "All legacy script folder READMEs have deprecation notices"
    - "READMEs point developers to the new src/f1d/* locations"
    - "Last updated dates are current"
  artifacts:
    - path: "2_Scripts/1_Sample/README.md"
      provides: "Sample construction documentation with deprecation notice"
      contains: "DEPRECATED|archived|migrated"
    - path: "2_Scripts/3_Financial/README.md"
      provides: "Financial V1 documentation with deprecation notice"
      contains: "DEPRECATED|archived|migrated"
    - path: "2_Scripts/4_Econometric/README.md"
      provides: "Econometric V1 documentation with deprecation notice"
      contains: "DEPRECATED|archived|migrated"
  key_links:
    - from: "2_Scripts/*/README.md"
      to: "src/f1d/*"
      via: "migration notice"
      pattern: "src/f1d"
---

<objective>
Add deprecation notices to all legacy script folder READMEs, directing developers to the new src-layout locations.

Purpose: Ensure developers understand that scripts have been migrated to the src/f1d/* namespace and the old 2_Scripts folders are legacy reference locations.
Output: Updated READMEs with clear deprecation notices.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deprecation notice to Sample README</name>
  <files>2_Scripts/1_Sample/README.md</files>
  <action>
Add a deprecation notice at the top of the file (after the title, before the Purpose section):

```markdown
> **Note:** This folder contains legacy scripts kept for reference. The active versions have been migrated to `src/f1d/sample/` as part of the v6.1 architecture standard. New development should use the `f1d.sample.*` namespace imports.
```

Also update:
1. Change `*Last updated: 2026-02-11*` to `*Last updated: 2026-02-14*`
2. Change `*Phase: 60-code-organization*` to `*Phase: 78-documentation-synchronization*`
3. Add `*Status: LEGACY - Migrated to src/f1d/sample/*` after the current status line
  </action>
  <verify>grep -c "src/f1d/sample" "2_Scripts/1_Sample/README.md" returns at least 1</verify>
  <done>README has deprecation notice pointing to src/f1d/sample/</done>
</task>

<task type="auto">
  <name>Task 2: Add deprecation notice to Text README</name>
  <files>2_Scripts/2_Text/README.md</files>
  <action>
Add a deprecation notice at the top of the file (after the title):

```markdown
> **Note:** This folder contains legacy scripts kept for reference. The active versions have been migrated to `src/f1d/text/` as part of the v6.1 architecture standard. New development should use the `f1d.text.*` namespace imports.
```

Also update:
1. Update last updated date to 2026-02-14
2. Update phase to 78-documentation-synchronization
3. Add migration status line
  </action>
  <verify>grep -c "src/f1d/text" "2_Scripts/2_Text/README.md" returns at least 1</verify>
  <done>README has deprecation notice pointing to src/f1d/text/</done>
</task>

<task type="auto">
  <name>Task 3: Add deprecation notice to Financial V1 README</name>
  <files>2_Scripts/3_Financial/README.md</files>
  <action>
Add a deprecation notice at the top of the file (after the title):

```markdown
> **Note:** This folder contains legacy V1 scripts kept for reference. The active versions have been migrated to `src/f1d/financial/v1/` as part of the v6.1 architecture standard. New development should use the `f1d.financial.*` namespace imports.
```

Also update:
1. Update last updated date to 2026-02-14
2. Update phase to 78-documentation-synchronization
3. Change status to: `**Status:** LEGACY - Migrated to src/f1d/financial/v1/`
  </action>
  <verify>grep -c "src/f1d/financial" "2_Scripts/3_Financial/README.md" returns at least 1</verify>
  <done>README has deprecation notice pointing to src/f1d/financial/</done>
</task>

<task type="auto">
  <name>Task 4: Add deprecation notice to Econometric V1 README</name>
  <files>2_Scripts/4_Econometric/README.md</files>
  <action>
Add a deprecation notice at the top of the file (after the title):

```markdown
> **Note:** This folder contains legacy V1 scripts kept for reference. The active versions have been migrated to `src/f1d/econometric/v1/` as part of the v6.1 architecture standard. New development should use the `f1d.econometric.*` namespace imports.
```

Also update:
1. Update last updated date to 2026-02-14
2. Update phase to 78-documentation-synchronization
3. Change status to: `**Status:** LEGACY - Migrated to src/f1d/econometric/v1/`
  </action>
  <verify>grep -c "src/f1d/econometric" "2_Scripts/4_Econometric/README.md" returns at least 1</verify>
  <done>README has deprecation notice pointing to src/f1d/econometric/</done>
</task>

<task type="auto">
  <name>Task 5: Add deprecation notices to V2 READMEs</name>
  <files>2_Scripts/3_Financial_V2/README.md, 2_Scripts/4_Econometric_V2/README.md</files>
  <action>
Add deprecation notices to both V2 READMEs:

For 2_Scripts/3_Financial_V2/README.md:
```markdown
> **Note:** This folder contains legacy V2 scripts kept for reference. The active versions have been migrated to `src/f1d/financial/v2/` as part of the v6.1 architecture standard. New development should use the `f1d.financial.*` namespace imports.
```

For 2_Scripts/4_Econometric_V2/README.md:
```markdown
> **Note:** This folder contains legacy V2 scripts kept for reference. The active versions have been migrated to `src/f1d/econometric/v2/` as part of the v6.1 architecture standard. New development should use the `f1d.econometric.*` namespace imports.
```

Update dates and phases in both files.
  </action>
  <verify>grep -c "src/f1d/financial/v2" "2_Scripts/3_Financial_V2/README.md" returns at least 1 and grep -c "src/f1d/econometric/v2" "2_Scripts/4_Econometric_V2/README.md" returns at least 1</verify>
  <done>Both V2 READMEs have deprecation notices</done>
</task>

</tasks>

<verification>
1. All 6 READMEs in 2_Scripts/ subfolders contain "src/f1d" references
2. All READMEs have updated dates (2026-02-14)
3. All READMEs have deprecation/migration notices
</verification>

<success_criteria>
- All legacy script folder READMEs have deprecation notices
- READMEs clearly indicate the new src/f1d/* locations
- Developers can find the active code locations
</success_criteria>

<output>
After completion, create `.planning/phases/78-documentation-synchronization/78-02-SUMMARY.md`
</output>
