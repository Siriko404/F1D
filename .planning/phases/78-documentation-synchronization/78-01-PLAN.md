---
phase: 78-documentation-synchronization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - 2_Scripts/shared/README.md
autonomous: true

must_haves:
  truths:
    - "Main README.md reflects v6.1 architecture with f1d.shared.* namespace"
    - "shared/README.md uses current import patterns in all code examples"
    - "No sys.path.insert() patterns in documentation code examples"
    - "pip install -e . mentioned as prerequisite for running scripts"
  artifacts:
    - path: "README.md"
      provides: "Project overview with v6.1 architecture"
      min_lines: 1400
    - path: "2_Scripts/shared/README.md"
      provides: "Shared utilities documentation with correct imports"
      contains: "from f1d.shared"
  key_links:
    - from: "README.md"
      to: "v6.1 architecture"
      via: "import patterns"
      pattern: "f1d\\.shared"
---

<objective>
Update main README.md and shared/README.md to reflect v6.1 migrated state with proper namespace imports.

Purpose: Ensure new developers can clone the repo and run scripts without PYTHONPATH tricks, using the modern `f1d.shared.*` namespace.
Output: Updated documentation files with correct import patterns.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update shared/README.md import patterns</name>
  <files>2_Scripts/shared/README.md</files>
  <action>
Replace ALL legacy import patterns in the shared/README.md file:

1. Replace `from shared.*` with `from f1d.shared.*` in ALL code examples:
   - `from shared.chunked_reader` -> `from f1d.shared.chunked_reader`
   - `from shared.data_validation` -> `from f1d.shared.data_validation`
   - `from shared.regression_utils` -> `from f1d.shared.regression_utils`
   - `from shared.regression_validation` -> `from f1d.shared.regression_validation`
   - `from shared.financial_utils` -> `from f1d.shared.financial_utils`
   - `from shared.reporting_utils` -> `from f1d.shared.reporting_utils`
   - `from shared.path_utils` -> `from f1d.shared.path_utils`
   - `from shared.dependency_checker` -> `from f1d.shared.dependency_checker`
   - `from shared.symlink_utils` -> `from f1d.shared.symlink_utils`
   - `from shared.string_matching` -> `from f1d.shared.string_matching`
   - `from shared.observability_utils` -> `from f1d.shared.observability_utils`

2. Remove the `sys.path.insert(0, str(Path(__file__).parent))` line from the "Module Usage Example" section (around line 1409)

3. Add a note at the top of the file that `pip install -e .` is required before using these imports

4. Update any references to module paths from `shared/` to `src/f1d/shared/`
  </action>
  <verify>grep -c "from shared\." "2_Scripts/shared/README.md" returns 0 (no legacy imports remaining)</verify>
  <done>All code examples use f1d.shared.* namespace imports, no sys.path.insert patterns remain</done>
</task>

<task type="auto">
  <name>Task 2: Update main README.md Quick Start and Installation sections</name>
  <files>README.md</files>
  <action>
Update the README.md to reflect v6.1 architecture and installation requirements:

1. In the "Installation" section (around line 6-31), add after the prerequisites:
   ```bash
   # Install the package in editable mode (required for f1d.shared.* imports)
   pip install -e .
   ```

2. In the "Quick Start" section (around line 1396-1417):
   - Add `pip install -e .` as step 2 before installing other dependencies
   - Add a note: "The package must be installed in editable mode for scripts to use `from f1d.shared.*` imports"

3. Update the "Troubleshooting" section (around line 651-660) to include:
   - "If you see 'ModuleNotFoundError: No module named f1d', run `pip install -e .`"

4. In the "Pipeline Structure" section, add a brief note about the src-layout architecture and f1d.shared.* namespace

5. Verify the "Execution Instructions" section does not have any sys.path manipulation examples
  </action>
  <verify>grep -c "pip install -e ." README.md returns at least 2 (added in multiple relevant sections)</verify>
  <done>README.md clearly documents the pip install -e . requirement and v6.1 architecture</done>
</task>

<task type="auto">
  <name>Task 3: Add architecture section to README.md</name>
  <files>README.md</files>
  <action>
Add a new "Architecture" section to README.md after the "Pipeline Structure" section:

1. Insert after the "Pipeline Structure" section (around line 1428):
   ```markdown
   ## Architecture

   The F1D pipeline uses a src-layout package structure following PyPA recommendations:

   - **Package:** `f1d` - Installable via `pip install -e .`
   - **Shared modules:** `f1d.shared.*` - Reusable utilities (config, logging, data validation, etc.)
   - **Stage scripts:** `f1d.sample.*`, `f1d.text.*`, `f1d.financial.*`, `f1d.econometric.*`

   All scripts use proper namespace imports:
   ```python
   from f1d.shared.config import get_settings
   from f1d.shared.logging import configure_logging
   from f1d.shared.path_utils import ensure_output_dir
   ```

   No `sys.path.insert()` or PYTHONPATH manipulation is required when the package is installed in editable mode.

   ### v6.1 Milestone

   The v6.1 milestone (Phases 75-77) completed full architecture compliance:
   - Zero `sys.path.insert()` calls in entire codebase
   - Zero legacy `from shared.*` imports
   - mypy passes with 0 errors on 101 source files
   - 1000+ tests with namespace imports
   ```

2. Ensure this section provides clear guidance for new developers on the import patterns
  </action>
  <verify>grep -c "## Architecture" README.md returns at least 1</verify>
  <done>Architecture section documents v6.1 standards and namespace imports</done>
</task>

</tasks>

<verification>
1. Run: `grep -c "from shared\." 2_Scripts/shared/README.md` - should be 0
2. Run: `grep -c "pip install -e ." README.md` - should be >= 2
3. Run: `grep -c "## Architecture" README.md` - should be 1
4. Verify no `sys.path.insert` patterns in documentation code examples
</verification>

<success_criteria>
- All code examples in shared/README.md use `from f1d.shared.*` imports
- README.md clearly documents the pip install -e . requirement
- Architecture section explains the v6.1 namespace structure
- New developer can clone, `pip install -e .`, and run scripts without PYTHONPATH tricks
</success_criteria>

<output>
After completion, create `.planning/phases/78-documentation-synchronization/78-01-SUMMARY.md`
</output>
