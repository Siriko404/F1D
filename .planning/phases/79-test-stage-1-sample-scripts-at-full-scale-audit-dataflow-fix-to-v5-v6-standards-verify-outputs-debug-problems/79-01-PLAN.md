---
phase: 79-test-stage-1-sample-scripts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/verification/79-standards-audit.json
autonomous: true
must_haves:
  truths:
    - "All 5 sample scripts use f1d.shared.* namespace imports"
    - "No sys.path.insert() calls in any sample script"
    - "mypy passes on sample scripts with 0 errors"
  artifacts:
    - path: ".planning/verification/79-standards-audit.json"
      provides: "Standards compliance audit results"
  key_links:
    - from: "src/f1d/sample/*.py"
      to: "f1d.shared.*"
      via: "import statements"
      pattern: "from f1d\\.shared\\."
---

<objective>
Verify V6.1 architecture standards compliance for all Stage 1 sample scripts before full-scale testing.

Purpose: Ensure codebase meets quality standards before investing time in execution testing.
Output: JSON audit report documenting compliance status per script.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/phase-79/79-RESEARCH.md
@.planning/research/phase-79/V5_V6_STANDARDS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit V6.1 namespace import compliance</name>
  <files>.planning/verification/79-standards-audit.json</files>
  <action>
    Create verification directory if not exists, then audit all 5 sample scripts for V6.1 compliance:

    1. Check that all imports use `f1d.shared.*` namespace (not legacy `from shared.*`)
    2. Check for zero `sys.path.insert()` calls
    3. Run `mypy src/f1d/sample/ --no-error-summary` and capture results

    Scripts to audit:
    - src/f1d/sample/1.0_BuildSampleManifest.py
    - src/f1d/sample/1.1_CleanMetadata.py
    - src/f1d/sample/1.2_LinkEntities.py
    - src/f1d/sample/1.3_BuildTenureMap.py
    - src/f1d/sample/1.4_AssembleManifest.py

    Use grep to check for:
    - `from f1d.shared.` (should be present)
    - `from shared.` (should be absent - legacy pattern)
    - `sys.path.insert` (should be absent)

    Create structured JSON output at `.planning/verification/79-standards-audit.json` with:
    - Per-script compliance status (imports, sys.path, mypy errors)
    - Overall compliance summary
    - Any issues found with file:line references
  </action>
  <verify>
    - `cat .planning/verification/79-standards-audit.json` shows valid JSON
    - JSON contains results for all 5 scripts
    - mypy command returns 0 errors (or errors are documented)
  </verify>
  <done>
    JSON audit file exists with compliance status for all 5 sample scripts.
    V6.1 compliance verified: f1d.shared.* imports, zero sys.path.insert, mypy 0 errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify --dry-run execution for all scripts</name>
  <files>.planning/verification/79-dry-run-results.json</files>
  <action>
    Execute --dry-run for each script to validate prerequisites without actual processing:

    ```bash
    python -m f1d.sample.1_0_BuildSampleManifest --dry-run
    python -m f1d.sample.1_1_CleanMetadata --dry-run
    python -m f1d.sample.1_2_LinkEntities --dry-run
    python -m f1d.sample.1_3_BuildTenureMap --dry-run
    python -m f1d.sample.1_4_AssembleManifest --dry-run
    ```

    For each script, capture:
    - Exit code (0 = success)
    - stdout/stderr output
    - Any missing prerequisite files

    Note: 1.4_AssembleManifest may fail dry-run if 1.2 and 1.3 outputs don't exist yet (expected on fresh run).

    Store results in `.planning/verification/79-dry-run-results.json`
  </action>
  <verify>
    - 1.0_BuildSampleManifest dry-run passes (exit 0)
    - 1.1_CleanMetadata dry-run passes (exit 0)
    - 1.2_LinkEntities dry-run may need 1.1 output (document status)
    - 1.3_BuildTenureMap dry-run passes (no step dependencies)
    - 1.4_AssembleManifest dry-run needs 1.2 and 1.3 outputs (document status)
  </verify>
  <done>
    All 5 scripts execute --dry-run successfully or have documented prerequisite gaps.
    Scripts 1.0, 1.1, 1.3 pass dry-run unconditionally.
    Scripts 1.2, 1.4 dry-run status documented with required prior outputs.
  </done>
</task>

</tasks>

<verification>
1. `.planning/verification/79-standards-audit.json` exists with valid JSON
2. All 5 scripts show f1d.shared.* imports
3. Zero sys.path.insert() calls found
4. mypy passes with 0 errors on sample scripts
5. `.planning/verification/79-dry-run-results.json` exists with valid JSON
</verification>

<success_criteria>
- V6.1 compliance verified for all Stage 1 sample scripts
- Dry-run validation completed for all scripts
- Any issues documented for remediation in subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/79-test-stage-1-sample-scripts-at-full-scale-audit-dataflow-fix-to-v5-v6-standards-verify-outputs-debug-problems/79-01-SUMMARY.md`
</output>
