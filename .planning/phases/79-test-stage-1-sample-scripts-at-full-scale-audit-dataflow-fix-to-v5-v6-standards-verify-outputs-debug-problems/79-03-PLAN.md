---
phase: 79-test-stage-1-sample-scripts
plan: 03
type: execute
wave: 3
depends_on: ["79-02"]
files_modified:
  - .planning/verification/79-schema-validation.json
  - .planning/verification/79-data-profile.json
autonomous: true
must_haves:
  truths:
    - "metadata_cleaned.parquet has expected columns from Unified-info"
    - "metadata_linked.parquet has gvkey, link_method, link_quality columns"
    - "tenure_monthly.parquet has gvkey, year, month, ceo_id columns"
    - "master_sample_manifest.parquet has file_name, gvkey, ceo_id columns"
    - "All numeric values are in valid ranges"
    - "No unexpected null values in required columns"
  artifacts:
    - path: ".planning/verification/79-schema-validation.json"
      provides: "Schema validation results per output"
    - path: ".planning/verification/79-data-profile.json"
      provides: "Data profile statistics per output"
  key_links:
    - from: "Script docstrings"
      to: "Output schemas"
      via: "Inputs/Outputs section"
---

<objective>
Validate output schemas and data quality for all Stage 1 outputs.

Purpose: Verify that outputs match expected schemas and contain logically consistent values.
Output: JSON validation reports for schema and data quality.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/phase-79/79-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate output schemas</name>
  <files>.planning/verification/79-schema-validation.json</files>
  <action>
    For each output file, validate schema against expected columns:

    **1.1_CleanMetadata output (metadata_cleaned.parquet):**
    Expected columns (from Unified-info):
    - file_name (unique identifier)
    - company_id, permno, cusip
    - start_date, event_type
    - All original Unified-info columns (cleaned)

    **1.2_LinkEntities output (metadata_linked.parquet):**
    Expected columns:
    - All columns from metadata_cleaned
    - gvkey (Compustat firm identifier)
    - conm (Company name from CCM)
    - sic (Industry code)
    - link_method (which tier matched)
    - link_quality (score 80/90/100)
    - fuzzy_score (if Tier 3 matched, nullable)
    - ff12_code, ff12_name, ff48_code, ff48_name

    **1.3_BuildTenureMap output (tenure_monthly.parquet):**
    Expected columns:
    - gvkey, year, month, date
    - ceo_id (execid)
    - ceo_name (exec_fullname)
    - prev_ceo_id
    - prev_ceo_name

    **1.4_AssembleManifest output (master_sample_manifest.parquet):**
    Expected columns:
    - file_name (unique call identifier)
    - gvkey, start_date, conm, sic
    - ff12_code, ff12_name, ff48_code, ff48_name
    - ceo_id, ceo_name
    - prev_ceo_id, prev_ceo_name

    Create validation script using pandas to:
    1. Load each parquet file
    2. Check for expected columns
    3. Check column data types
    4. Report missing/unexpected columns

    Store results in `.planning/verification/79-schema-validation.json`
  </action>
  <verify>
    - JSON file exists with valid structure
    - All 4 output files validated
    - Any schema issues documented
  </verify>
  <done>
    Schema validation complete for all Stage 1 outputs.
    All outputs have expected columns with correct types.
    Any deviations documented for review.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate data quality and logical consistency</name>
  <files>.planning/verification/79-data-profile.json</files>
  <action>
    For each output file, validate data quality:

    **1.1_CleanMetadata checks:**
    - No duplicate file_name values
    - start_date in range 2002-2018
    - event_type == '1' for all rows
    - No null file_name values

    **1.2_LinkEntities checks:**
    - link_quality in {80, 90, 100} or null
    - link_method in {'tier1', 'tier2', 'tier3'} or null
    - gvkey is 6-digit string (zero-padded)
    - Match rate >= 70% (non-null gvkey)

    **1.3_BuildTenureMap checks:**
    - year in valid range
    - month in 1-12
    - ceo_id not null
    - gvkey is 6-digit string
    - No duplicate (gvkey, year, month) combinations

    **1.4_AssembleManifest checks:**
    - file_name unique
    - gvkey not null
    - ceo_id not null
    - year derived from start_date matches join

    Create data profile JSON with:
    - Row counts
    - Null rates per column
    - Value distributions for key columns
    - Any anomalies detected

    Store in `.planning/verification/79-data-profile.json`
  </action>
  <verify>
    - JSON file exists with valid structure
    - All quality checks documented
    - Any issues flagged for review
  </verify>
  <done>
    Data quality validation complete.
    All outputs contain logically consistent values.
    Data profile statistics captured for audit report.
  </done>
</task>

</tasks>

<verification>
1. Schema validation JSON exists with all 4 outputs documented
2. Data profile JSON exists with quality metrics
3. No critical schema mismatches (missing required columns)
4. Data quality issues documented (if any)
</verification>

<success_criteria>
- All Stage 1 outputs have expected schemas
- All outputs have logically consistent values
- Data quality metrics documented in JSON format
</success_criteria>

<output>
After completion, create `.planning/phases/79-test-stage-1-sample-scripts-at-full-scale-audit-dataflow-fix-to-v5-v6-standards-verify-outputs-debug-problems/79-03-SUMMARY.md`
</output>
