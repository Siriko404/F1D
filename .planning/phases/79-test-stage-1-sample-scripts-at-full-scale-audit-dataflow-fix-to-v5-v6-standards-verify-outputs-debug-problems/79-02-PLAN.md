---
phase: 79-test-stage-1-sample-scripts
plan: 02
type: execute
wave: 2
depends_on: ["79-01"]
files_modified:
  - 4_Outputs/1.1_CleanMetadata/
  - 4_Outputs/1.2_LinkEntities/
  - 4_Outputs/1.3_BuildTenureMap/
  - 4_Outputs/1.4_AssembleManifest/
  - 4_Outputs/1.0_BuildSampleManifest/
  - .planning/verification/79-execution-audit.json
autonomous: true
must_haves:
  truths:
    - "1.1_CleanMetadata executes and produces metadata_cleaned.parquet"
    - "1.2_LinkEntities executes and produces metadata_linked.parquet"
    - "1.3_BuildTenureMap executes and produces tenure_monthly.parquet"
    - "1.4_AssembleManifest executes and produces master_sample_manifest.parquet"
    - "1.0_BuildSampleManifest orchestrates all steps successfully"
  artifacts:
    - path: "4_Outputs/1.1_CleanMetadata/*/metadata_cleaned.parquet"
      provides: "Cleaned earnings call metadata"
    - path: "4_Outputs/1.2_LinkEntities/*/metadata_linked.parquet"
      provides: "Linked metadata with GVKEY and industry codes"
    - path: "4_Outputs/1.3_BuildTenureMap/*/tenure_monthly.parquet"
      provides: "Monthly CEO tenure panel"
    - path: "4_Outputs/1.4_AssembleManifest/*/master_sample_manifest.parquet"
      provides: "Final sample universe"
  key_links:
    - from: "1_Inputs/Unified-info.parquet"
      to: "1.1_CleanMetadata"
      via: "load_validated_parquet"
    - from: "1.1 output"
      to: "1.2_LinkEntities"
      via: "get_latest_output_dir"
    - from: "1_Inputs/Execucomp/comp_execucomp.parquet"
      to: "1.3_BuildTenureMap"
      via: "load_validated_parquet"
    - from: "1.2 output + 1.3 output"
      to: "1.4_AssembleManifest"
      via: "merge on gvkey/year/month"
---

<objective>
Execute the complete Stage 1 sample pipeline at full scale (2002-2018, all quarters).

Purpose: Generate production outputs for the full historical range to verify pipeline correctness.
Output: Complete set of Stage 1 outputs in 4_Outputs/ directories.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/phase-79/79-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute 1.1_CleanMetadata at full scale</name>
  <files>
    4_Outputs/1.1_CleanMetadata/*/metadata_cleaned.parquet
    4_Outputs/1.1_CleanMetadata/*/report_step_1_1.md
  </files>
  <action>
    Execute step 1.1 to clean and filter raw metadata:

    ```bash
    python -m f1d.sample.1_1_CleanMetadata
    ```

    Expected behavior:
    - Load 1_Inputs/Unified-info.parquet (~55MB)
    - Remove exact duplicate rows
    - Resolve file_name collisions (keep earliest timestamp)
    - Filter for event_type='1' (earnings calls only)
    - Filter for years 2002-2018

    Capture execution metrics:
    - Wall-clock time
    - Input row count
    - Output row count (expected: ~200K-300K records)
    - Memory usage

    On error: STOP immediately, document error, and prepare for fix.
  </action>
  <verify>
    - Exit code 0
    - Output directory exists: `ls 4_Outputs/1.1_CleanMetadata/`
    - metadata_cleaned.parquet exists and has > 0 rows
    - report_step_1_1.md exists
  </verify>
  <done>
    1.1_CleanMetadata executed successfully.
    Output files created in timestamped directory under 4_Outputs/1.1_CleanMetadata/.
    Row count matches expected range (~200K-300K earnings calls 2002-2018).
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute 1.2_LinkEntities at full scale</name>
  <files>
    4_Outputs/1.2_LinkEntities/*/metadata_linked.parquet
    4_Outputs/1.2_LinkEntities/*/report_step_1_2.md
  </files>
  <action>
    Execute step 1.2 to link calls to CRSP/Compustat CCM:

    ```bash
    python -m f1d.sample.1_2_LinkEntities
    ```

    Expected behavior:
    - Load metadata_cleaned.parquet from latest 1.1 output
    - Load 1_Inputs/CRSPCompustat_CCM/CRSPCompustat_CCM.parquet
    - Execute 4-tier linking strategy:
      - Tier 1: PERMNO + Date Range (quality=100)
      - Tier 2: CUSIP8 + Date Range (quality=90)
      - Tier 3: Fuzzy Name Match (quality=80, threshold=92)
    - Map SIC to FF12/FF48 industries using 1_Inputs/FF1248/*.zip

    Capture execution metrics:
    - Match rate per tier
    - Total match rate (expected: 70-90%)
    - Unmatched record count
    - Execution time

    On error: STOP immediately, document error, and prepare for fix.
  </action>
  <verify>
    - Exit code 0
    - Output directory exists: `ls 4_Outputs/1.2_LinkEntities/`
    - metadata_linked.parquet exists and has > 0 rows
    - gvkey column exists and has non-null values
    - Total match rate >= 70%
  </verify>
  <done>
    1.2_LinkEntities executed successfully.
    Output files created with gvkey assignments for majority of records.
    Match rate within expected range (70-90%).
  </done>
</task>

<task type="auto">
  <name>Task 3: Execute 1.3_BuildTenureMap at full scale</name>
  <files>
    4_Outputs/1.3_BuildTenureMap/*/tenure_monthly.parquet
    4_Outputs/1.3_BuildTenureMap/*/report_step_1_3.md
  </files>
  <action>
    Execute step 1.3 to build CEO tenure panel:

    ```bash
    python -m f1d.sample.1_3_BuildTenureMap
    ```

    Expected behavior:
    - Load 1_Inputs/Execucomp/comp_execucomp.parquet (~44MB)
    - Filter for CEO records (ceoann='CEO' or becameceo not null)
    - Build tenure episodes per (gvkey, execid)
    - Link predecessor CEOs
    - Expand to monthly panel
    - Resolve overlaps (keep most recent CEO)

    Active CEO handling:
    - If CEO's max year >= latest dataset year: impute end_date as 2025-12-31
    - Otherwise: use last fiscal year end

    Capture execution metrics:
    - Total CEO episodes
    - Total monthly records
    - Unique CEOs
    - Unique firms

    On error: STOP immediately, document error, and prepare for fix.
  </action>
  <verify>
    - Exit code 0
    - Output directory exists: `ls 4_Outputs/1.3_BuildTenureMap/`
    - tenure_monthly.parquet exists and has > 0 rows
    - Columns include: gvkey, year, month, ceo_id, ceo_name, prev_ceo_id
  </verify>
  <done>
    1.3_BuildTenureMap executed successfully.
    Monthly CEO tenure panel created for all firms in Execucomp.
  </done>
</task>

<task type="auto">
  <name>Task 4: Execute 1.4_AssembleManifest at full scale</name>
  <files>
    4_Outputs/1.4_AssembleManifest/*/master_sample_manifest.parquet
    4_Outputs/1.4_AssembleManifest/*/report_step_1_4.md
  </files>
  <action>
    Execute step 1.4 to assemble final manifest:

    ```bash
    python -m f1d.sample.1_4_AssembleManifest
    ```

    Expected behavior:
    - Load metadata_linked.parquet from latest 1.2 output
    - Load tenure_monthly.parquet from latest 1.3 output
    - Join on (gvkey, year, month) with 6-digit zero-padded gvkey
    - Filter unmatched calls
    - Apply minimum call threshold (>= 5 calls per CEO)
    - Sort by file_name for determinism

    Join key normalization:
    ```python
    metadata["gvkey"] = metadata["gvkey"].apply(
        lambda x: str(int(x)).zfill(6) if pd.notna(x) else None
    )
    tenure["gvkey"] = tenure["gvkey"].astype(str).str.zfill(6)
    ```

    Capture execution metrics:
    - Join match rate
    - Records before/after CEO filtering
    - Final manifest row count (expected: ~100K-150K)

    On error: STOP immediately, document error, and prepare for fix.
  </action>
  <verify>
    - Exit code 0
    - Output directory exists: `ls 4_Outputs/1.4_AssembleManifest/`
    - master_sample_manifest.parquet exists and has > 0 rows
    - Columns include: file_name, gvkey, ceo_id, ceo_name
    - Row count >= 50000 (reasonable lower bound)
  </verify>
  <done>
    1.4_AssembleManifest executed successfully.
    Final sample manifest created with CEO-filtered earnings calls.
  </done>
</task>

<task type="auto">
  <name>Task 5: Execute 1.0_BuildSampleManifest orchestrator</name>
  <files>
    4_Outputs/1.0_BuildSampleManifest/*/master_sample_manifest.parquet
    4_Outputs/1.0_BuildSampleManifest/*/report_step_1_0.md
    .planning/verification/79-execution-audit.json
  </files>
  <action>
    Execute the orchestrator to verify end-to-end pipeline:

    ```bash
    python -m f1d.sample.1_0_BuildSampleManifest
    ```

    This will:
    - Run all 4 substeps in sequence
    - Copy final manifest to orchestrator output directory
    - Generate consolidated report

    Note: Since steps 1.1-1.4 already have outputs, this should complete quickly
    (copying existing outputs). If it re-runs everything, that's also valid.

    After execution, create `.planning/verification/79-execution-audit.json` with:
    - Per-script execution status (pass/fail)
    - Wall-clock time per script
    - Row counts at each stage
    - Any errors encountered
  </action>
  <verify>
    - Exit code 0
    - Output directory exists: `ls 4_Outputs/1.0_BuildSampleManifest/`
    - master_sample_manifest.parquet exists
    - JSON audit file exists with all 5 scripts documented
  </verify>
  <done>
    Complete Stage 1 pipeline executed successfully at full scale.
    All 5 scripts pass with expected outputs.
    Execution audit JSON created for Phase 79 tracking.
  </done>
</task>

</tasks>

<verification>
1. All 5 script output directories exist under 4_Outputs/
2. Each output directory contains expected parquet files
3. Each output directory contains report_step_*.md
4. Final manifest has >= 50000 rows
5. Execution audit JSON documents all script results
</verification>

<success_criteria>
- All 5 Stage 1 scripts execute without errors
- All expected output files created
- Row counts within expected ranges at each transformation stage
- Execution audit JSON created for tracking
</success_criteria>

<output>
After completion, create `.planning/phases/79-test-stage-1-sample-scripts-at-full-scale-audit-dataflow-fix-to-v5-v6-standards-verify-outputs-debug-problems/79-02-SUMMARY.md`
</output>
