---
phase: 08-tech-debt-cleanup
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - 2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py
  - 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py
autonomous: true
must_haves:
  truths:
    - "No bare except: blocks remain in econometric scripts"
    - "All error handling uses specific exception types"
    - "Errors are logged with context (operation, file, parameters)"
    - "Scripts exit with non-zero codes on critical failures"
  artifacts:
    - path: "2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py"
      provides: "Fixed error handling with specific exceptions"
      contains: "except FileNotFoundError|except PermissionError|except OSError"
      not_contains: "except:\\s*pass"
      min_lines: 10
    - path: "2_Scripts/4_Econometric/4.2_LiquidityRegressions.py"
      provides: "Fixed error handling with specific exceptions"
      contains: "except FileNotFoundError|except PermissionError|except OSError"
      not_contains: "except:\\s*pass"
      min_lines: 10
  key_links:
    - from: "Error handling blocks"
      to: "sys.exit(1)"
      via: "Explicit exception handling"
      pattern: "except.*as e:\\s*print.*file=sys.stderr\\s*sys.exit\\(1\\)"
---

<objective>
Fix inconsistent error handling in econometric scripts that still have bare except blocks

Purpose: While Phase 7 improved error handling in utility files, econometric scripts still have bare `except:` blocks with `pass` statements that silently hide errors. This makes debugging difficult and can lead to incorrect results. Following Phase 7 patterns, we replace bare except with specific exception types, error logging, and exit codes.

Output: Econometric scripts with proper error handling (specific exceptions, logging, exit codes)
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-tech-debt-cleanup/08-RESEARCH.md
@.planning/phases/07-critical-bug-fixes/07-01-SUMMARY.md

# Research pattern: Error Handling Standardization (Following Phase 7)
# Replace bare except: and except Exception: with specific exceptions
# Use Phase 7 patterns: FileNotFoundError, PermissionError, OSError, ValueError
# Always log error context and exit with sys.exit(1) on critical failures
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix error handling in 4.1.2_EstimateCeoClarity_Extended.py</name>
  <files>2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py</files>
  <action>
Fix bare except blocks in 4.1.2_EstimateCeoClarity_Extended.py.

First, find all bare except blocks:
```bash
grep -n "except:" 2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py -A1
```

This shows two bare except blocks (likely around file operations or data loading).

For each bare except block, replace with specific exception handling following Phase 7 pattern:

Example replacement:
```python
# BEFORE (bad):
try:
    risky_operation()
except:
    pass  # Silent failure

# AFTER (good):
import sys
try:
    risky_operation()
except FileNotFoundError as e:
    print(f"ERROR: File not found: {e}", file=sys.stderr)
    print(f"  Operation: [describe what operation was being attempted]", file=sys.stderr)
    sys.exit(1)
except PermissionError as e:
    print(f"ERROR: Permission denied: {e}", file=sys.stderr)
    print(f"  Operation: [describe what operation was being attempted]", file=sys.stderr)
    sys.exit(1)
except OSError as e:
    print(f"ERROR: OS error: {e}", file=sys.stderr)
    print(f"  Operation: [describe what operation was being attempted]", file=sys.stderr)
    sys.exit(1)
```

Key principles:
1. Import sys at module level (if not already imported)
2. Catch specific exceptions (FileNotFoundError, PermissionError, OSError, or relevant types)
3. Log error to stderr with context (what operation failed)
4. Exit with sys.exit(1) on critical failures
5. Include relevant parameters (file paths, variable names) in error messages

Apply this to both bare except blocks found in the file.
  </action>
  <verify>
grep -c "except:" 2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py
grep -q "except FileNotFoundError\|except PermissionError\|except OSError" 2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py && echo "Specific exceptions found"
  </verify>
  <done>
File has no bare except: blocks, all error handling uses specific exception types with logging and exit codes
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix error handling in 4.2_LiquidityRegressions.py</name>
  <files>2_Scripts/4_Econometric/4.2_LiquidityRegressions.py</files>
  <action>
Fix bare except blocks in 4.2_LiquidityRegressions.py.

First, find all bare except blocks:
```bash
grep -n "except:" 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py -A1
```

This shows two bare except blocks (likely around file operations or data loading).

For each bare except block, replace with specific exception handling following the same pattern as Task 1:

Example replacement:
```python
# BEFORE (bad):
try:
    risky_operation()
except:
    pass  # Silent failure

# AFTER (good):
import sys
try:
    risky_operation()
except FileNotFoundError as e:
    print(f"ERROR: File not found: {e}", file=sys.stderr)
    print(f"  Operation: [describe what operation was being attempted]", file=sys.stderr)
    sys.exit(1)
except PermissionError as e:
    print(f"ERROR: Permission denied: {e}", file=sys.stderr)
    print(f"  Operation: [describe what operation was being attempted]", file=sys.stderr)
    sys.exit(1)
except OSError as e:
    print(f"ERROR: OS error: {e}", file=sys.stderr)
    print(f"  Operation: [describe what operation was being attempted]", file=sys.stderr)
    sys.exit(1)
```

Apply this to both bare except blocks found in the file.
  </action>
  <verify>
grep -c "except:" 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py
grep -q "except FileNotFoundError\|except PermissionError\|except OSError" 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py && echo "Specific exceptions found"
  </verify>
  <done>
File has no bare except: blocks, all error handling uses specific exception types with logging and exit codes
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify no bare except blocks remain in econometric scripts</name>
  <files>2_Scripts/4_Econometric/</files>
  <action>
Verify that all econometric scripts have been checked for bare except blocks:

```bash
grep -r "except:" 2_Scripts/4_Econometric/ --include="*.py" -n
```

This should show no bare except blocks remaining. Any remaining "except:" should be for specific exceptions like "except Exception:" (which is acceptable if it's the only option, though specific exceptions are preferred).

If any bare except: blocks are found in other econometric scripts, note them for future cleanup (this task only addresses the two files explicitly identified).
  </action>
  <verify>
grep -r "except:" 2_Scripts/4_Econometric/ --include="*.py" | grep -v "except.*:" | wc -l
  </verify>
  <done>
No bare except: blocks remain in 4.1.2_EstimateCeoClarity_Extended.py and 4.2_LiquidityRegressions.py
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify error handling patterns match Phase 7</name>
  <files>2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py, 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py</files>
  <action>
Verify that error handling patterns match Phase 7 standards:

```bash
# Check for specific exceptions
grep -q "except FileNotFoundError" 2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py
grep -q "sys.exit(1)" 2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py
grep -q "file=sys.stderr" 2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py

grep -q "except FileNotFoundError\|except PermissionError\|except OSError" 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py
grep -q "sys.exit(1)" 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py
grep -q "file=sys.stderr" 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py
```

Verify that:
1. sys is imported
2. Specific exception types are used
3. Errors are logged to stderr
4. sys.exit(1) is called on critical failures
  </action>
  <verify>
grep -q "import sys" 2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py && echo "sys imported in 4.1.2"
grep -q "import sys" 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py && echo "sys imported in 4.2"
  </verify>
  <done>
Error handling in both files follows Phase 7 patterns (specific exceptions, stderr logging, exit codes)
  </done>
</task>

</tasks>

<verification>
1. 4.1.2_EstimateCeoClarity_Extended.py has no bare except: blocks
2. 4.2_LiquidityRegressions.py has no bare except: blocks
3. Both files use specific exception types (FileNotFoundError, PermissionError, OSError)
4. Both files log errors to stderr with context
5. Both files call sys.exit(1) on critical failures
6. Both files have sys imported at module level
</verification>

<success_criteria>
1. No bare except: blocks remain in targeted econometric scripts
2. All error handling uses specific exception types
3. Errors are logged with context (operation, parameters)
4. Scripts exit with non-zero codes on critical failures
5. Error handling patterns match Phase 7 standards
6. Code compiles without syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-tech-debt-cleanup/08-04-SUMMARY.md`
</output>
