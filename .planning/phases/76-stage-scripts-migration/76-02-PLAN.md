---
phase: 76-stage-scripts-migration
plan: 02
type: execute
wave: 2
depends_on: ["76-01"]
files_modified:
  - src/f1d/financial/v1/3.0_BuildFinancialFeatures.py
  - src/f1d/financial/v1/3.1_FirmControls.py
  - src/f1d/financial/v1/3.2_MarketVariables.py
  - src/f1d/financial/v1/3.3_EventFlags.py
autonomous: true

must_haves:
  truths:
    - "Financial v1 stage scripts import using f1d.shared.* namespace"
    - "Zero sys.path.insert() calls in financial/v1/ modules"
  artifacts:
    - path: "src/f1d/financial/v1/*.py"
      provides: "Financial v1 stage scripts with proper imports"
      pattern: "from f1d\\.shared\\."
---

<objective>
Migrate all financial v1 stage scripts from legacy sys.path.insert() workarounds to proper f1d.shared.* imports.

Purpose: Complete the architecture migration for Tier 3 financial v1 stage scripts. These 4 files were not migrated in Phase 69.

Output: 4 financial v1 scripts with proper src-layout imports.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Migration Pattern
- Remove `sys.path.insert(0, str(...))` lines
- Change `from shared.*` to `from f1d.shared.*`
- Keep script functionality unchanged
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all financial v1 scripts</name>
  <files>
    src/f1d/financial/v1/3.0_BuildFinancialFeatures.py
    src/f1d/financial/v1/3.1_FirmControls.py
    src/f1d/financial/v1/3.2_MarketVariables.py
    src/f1d/financial/v1/3.3_EventFlags.py
  </files>
  <action>
For each file:
1. Remove all `sys.path.insert(0, ...)` lines
2. Change all `from shared.*` imports to `from f1d.shared.*`
3. Remove any `2_Scripts` path references in import logic
4. Keep script functionality unchanged
  </action>
  <verify>grep -c "sys.path.insert" returns 0 for all 4 files</verify>
  <done>All 4 files migrated to f1d.shared.* imports</done>
</task>

</tasks>

<verification>
1. Run: `grep -r "sys.path.insert" src/f1d/financial/v1/` - should return empty
2. Run: `grep -r "from shared\." src/f1d/financial/v1/` - should return empty
3. Run: `python -c "from f1d.financial.v1 import *"` - should not raise ImportError
</verification>

<success_criteria>
- Zero `sys.path.insert()` calls in src/f1d/financial/v1/*.py files
- All imports use `from f1d.shared.*` pattern
- No `from shared.*` legacy imports remain
- Python can import the modules without errors
</success_criteria>

<output>
After completion, create `.planning/phases/76-stage-scripts-migration/76-02-SUMMARY.md`
</output>
