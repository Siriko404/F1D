---
phase: 76-stage-scripts-migration
plan: 04
type: execute
wave: 3
depends_on: ["76-02", "76-03"]
files_modified:
  - tests/performance/test_performance_h2_variables.py
  - tests/performance/test_performance_link_entities.py
  - .planning/STATE.md
autonomous: false

must_haves:
  truths:
    - "Zero sys.path.insert() calls in tests/performance/"
    - "All stage scripts in src/f1d/ use f1d.shared.* namespace"
    - "Full ROADMAP criterion #1 compliance achieved"
  artifacts:
    - path: "tests/performance/*.py"
      provides: "Performance tests with proper imports"
    - path: ".planning/v6.1-MILESTONE-AUDIT.md"
      provides: "Updated milestone audit"
---

<objective>
Migrate performance tests and perform final verification to complete Phase 76 and achieve full ROADMAP compliance.

Purpose: Close the last remaining sys.path.insert() usage in the codebase and verify all success criteria are met.

Output: Migrated performance tests, updated STATE.md, verified compliance.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Current State After Plans 76-01 to 76-03
- Financial v2 scripts: Migrated (13 files)
- Financial v1 scripts: Migrated (4 files)
- Econometric scripts: Migrated (19 files)
- Performance tests: Still need migration (2 files)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate performance tests</name>
  <files>
    tests/performance/test_performance_h2_variables.py
    tests/performance/test_performance_link_entities.py
  </files>
  <action>
For each performance test file:
1. Remove all `sys.path.insert(0, ...)` lines
2. Change all `from shared.*` imports to `from f1d.shared.*`
3. Remove any `2_Scripts` path references
4. Keep test functionality unchanged

Note: These tests may import from 2_Scripts for subprocess execution - preserve that logic, only fix import patterns for f1d.shared modules.
  </action>
  <verify>grep -c "sys.path.insert" returns 0 for both files</verify>
  <done>Both performance test files migrated</done>
</task>

<task type="auto">
  <name>Task 2: Verify all sys.path.insert() eliminated</name>
  <files></files>
  <action>
Run comprehensive verification:

```bash
# Check src/f1d/
grep -r "sys.path.insert" src/f1d/ --include="*.py" || echo "PASS: No sys.path.insert in src/f1d/"

# Check tests/
grep -r "sys.path.insert" tests/ --include="*.py" || echo "PASS: No sys.path.insert in tests/"

# Count total
echo "Total sys.path.insert in codebase:"
grep -r "sys.path.insert" src/f1d/ tests/ --include="*.py" 2>/dev/null | wc -l
```
  </action>
  <verify>All grep commands return 0 results</verify>
  <done>Full codebase verified clean of sys.path.insert()</done>
</task>

<task type="auto">
  <name>Task 3: Verify all legacy imports eliminated</name>
  <files></files>
  <action>
Run verification for legacy import patterns:

```bash
# Check for legacy imports in src/f1d/
grep -r "from shared\." src/f1d/ --include="*.py" || echo "PASS: No legacy imports in src/f1d/"

# Check for legacy imports in tests/
grep -r "from shared\." tests/ --include="*.py" || echo "PASS: No legacy imports in tests/"
```
  </action>
  <verify>All grep commands return 0 results</verify>
  <done>Full codebase verified clean of legacy imports</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 76 stage scripts migration:
- 13 financial v2 scripts migrated
- 4 financial v1 scripts migrated
- 19 econometric scripts migrated
- 2 performance tests migrated
- Zero sys.path.insert() in entire codebase
- Zero legacy from shared.* imports</what-built>
  <how-to-verify>
1. Review verification output from Tasks 2-3
2. Confirm all ROADMAP success criteria are met:
   - [ ] Zero sys.path.insert() calls in src/f1d/financial/
   - [ ] Zero sys.path.insert() calls in src/f1d/econometric/
   - [ ] Zero sys.path.insert() calls in tests/performance/
   - [ ] All stage scripts use f1d.shared.* namespace
   - [ ] mypy passes on migrated files (no new errors)
3. Optionally run a sample stage script to verify it works
  </how-to-verify>
  <resume-signal>Type "approved" to complete phase or describe issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Update project state and milestone audit</name>
  <files>
    .planning/STATE.md
    .planning/v6.1-MILESTONE-AUDIT.md
  </files>
  <action>
Update project state to reflect Phase 76 completion and v6.1 milestone completion.

**Update .planning/STATE.md:**
- Change Phase 76 status from PENDING to COMPLETE
- Update current position to show v6.1 COMPLETE
- Add completion date
- Update progress metrics

**Update .planning/v6.1-MILESTONE-AUDIT.md:**
- Change status from "partial" to "passed"
- Update gaps_closed to reflect Phase 76 work
- Update remaining_gaps to 0
- Add Phase 76 to phase verification summary
- Update recommendation to PASSED
  </action>
  <verify>STATE.md shows Phase 76 COMPLETE; milestone audit shows PASSED</verify>
  <done>Project state and milestone audit updated</done>
</task>

</tasks>

<verification>
1. Verify zero sys.path.insert() in entire codebase
2. Verify zero legacy imports in entire codebase
3. Verify .planning/STATE.md shows Phase 76 COMPLETE
4. Verify .planning/v6.1-MILESTONE-AUDIT.md shows status: passed
</verification>

<success_criteria>
- Zero `sys.path.insert()` calls in src/f1d/financial/
- Zero `sys.path.insert()` calls in src/f1d/econometric/
- Zero `sys.path.insert()` calls in tests/performance/
- All stage scripts use f1d.shared.* namespace
- mypy passes on migrated files (no new errors)
- STATE.md and milestone audit updated
</success_criteria>

<output>
After completion, create `.planning/phases/76-stage-scripts-migration/76-04-SUMMARY.md`
</output>
