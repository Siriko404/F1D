---
phase: 25.1-fix-pipeline-scripts-to-run-sequentially-and-individually-manually-not-with-any-orchestrator-script
plan: 02
subsystem: pipeline-validation
tags: [argparse, prerequisite-checking, dependency-validation, CLI-flags]

# Dependency graph
requires:
  - phase: 25.1-01
    provides: dependency_checker.py module with validate_prerequisites() function
provides:
  - CLI validation with argparse (--help, --dry-run flags) for all Step 1 scripts
  - Prerequisite checking for sequential execution of Step 1 scripts
  - Clear error messages when inputs/prerequisites missing
  - Manual execution capability without orchestrator dependency
affects:
  - phase: 25.1-03
    reason: Uses same pattern for remaining pipeline scripts

# Tech tracking
tech-stack:
  added: [argparse (stdlib)]
  patterns: [CLI-argument-parsing, prerequisite-validation, dependency-checking]

key-files:
  created: []
  modified:
    - 2_Scripts/1_Sample/1.1_CleanMetadata.py
    - 2_Scripts/1_Sample/1.2_LinkEntities.py
    - 2_Scripts/1_Sample/1.3_BuildTenureMap.py
    - 2_Scripts/1_Sample/1.4_AssembleManifest.py

key-decisions:
  - "Use argparse from Python stdlib instead of external CLI libraries"
  - "All scripts support --dry-run for validation-only execution"
  - "Prerequisite checks use dependency_checker.validate_prerequisites() from shared module"
  - "Scripts validate both input files and prerequisite step outputs"

patterns-established:
  - "Pattern: CLI validation template - parse_arguments() + check_prerequisites() + wrap main()"
  - "Pattern: Dry-run mode allows validation without executing full processing"
  - "Pattern: Clear error messages from dependency_checker with actionable next steps"

# Metrics
duration: 10 min
completed: 2026-01-24
---

# Phase 25.1 Plan 2: CLI Validation for Step 1 Sample Construction Scripts Summary

**Argparse CLI validation with prerequisite checking enabling manual sequential execution of Step 1 pipeline scripts**

## Performance

- **Duration:** 10 min
- **Started:** 2026-01-24
- **Completed:** 2026-01-24
- **Tasks:** 4
- **Files modified:** 4

## Accomplishments

- Added argparse CLI validation to all 4 Step 1 scripts (1.1, 1.2, 1.3, 1.4)
- Implemented prerequisite checking using dependency_checker.validate_prerequisites() from shared module
- Enabled --dry-run mode for validation-only execution on all scripts
- Scripts now validate input files and prerequisite step outputs before processing
- Clear error messages with actionable next steps when prerequisites missing
- Manual sequential execution now possible without orchestrator dependency

## Task Commits

Each task was committed atomically:

1. **Task 1: Add argparse and validation to 1.1_CleanMetadata.py** - `c4a4f76` (feat)
2. **Task 2: Add argparse and validation to 1.2_LinkEntities.py** - `14a8ccc` (feat)
3. **Task 3: Add argparse and validation to 1.3_BuildTenureMap.py** - `c6acfd7` (feat)
4. **Task 4: Add argparse and validation to 1.4_AssembleManifest.py** - `c726ab7` (feat)

**Plan metadata:** (created after this commit)

## Files Created/Modified

- `2_Scripts/1_Sample/1.1_CleanMetadata.py` - Added argparse, parse_arguments(), check_prerequisites(), dry-run mode
- `2_Scripts/1_Sample/1.2_LinkEntities.py` - Added argparse, parse_arguments(), check_prerequisites(), dry-run mode
- `2_Scripts/1_Sample/1.3_BuildTenureMap.py` - Added argparse, parse_arguments(), check_prerequisites(), dry-run mode, fixed duplicate imports
- `2_Scripts/1_Sample/1.4_AssembleManifest.py` - Added argparse, parse_arguments(), check_prerequisites(), dry-run mode, added load_config() and setup_paths() functions

## Decisions Made

- Use argparse from Python stdlib (no external dependencies needed for CLI)
- All scripts support --help flag showing usage information
- All scripts support --dry-run flag for validation-only execution
- Prerequisite checks validate both input files and prior step outputs via latest/ symlinks
- Error messages from dependency_checker provide clear next steps when prerequisites missing

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- 1.3_BuildTenureMap.py had duplicate load_config() function definition - Fixed by removing duplicate
- 1.3_BuildTenureMap.py had duplicate imports (sys, Path) - Fixed by removing duplicates
- 1.4_AssembleManifest.py had missing load_config() and setup_paths() functions - Added these functions
- 1.4_AssembleManifest.py had duplicate imports (sys, Path) - Fixed by removing duplicates
- 1.3_BuildTenureMap.py and 1.4_AssembleManifest.py had missing print_dual() function - Added function

All issues were minor code cleanup issues discovered during implementation and fixed inline.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Step 1 scripts (1.1, 1.2, 1.3, 1.4) now support:
- Manual execution: `python 2_Scripts/1_Sample/1.X_*.py`
- Help mode: `python 2_Scripts/1_Sample/1.X_*.py --help`
- Dry-run validation: `python 2_Scripts/1_Sample/1.X_*.py --dry-run`

Prerequisite checking ensures sequential execution order:
- 1.1 validates Unified-info.parquet exists
- 1.2 validates CRSPCompustat_CCM directory and 1.1 output (metadata_cleaned.parquet)
- 1.3 validates Execucomp data and 1.2 output (metadata_linked.parquet)
- 1.4 validates both 1.2 and 1.3 outputs (metadata_linked.parquet, tenure_monthly.parquet)

Ready for next plan (25.1-03) to add CLI validation to remaining pipeline scripts.

---
*Phase: 25.1-fix-pipeline-scripts-to-run-sequentially-and-individually-manually-not-with-any-orchestrator-script*
*Completed: 2026-01-24*
