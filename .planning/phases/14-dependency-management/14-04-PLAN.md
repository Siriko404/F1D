---
phase: 14-dependency-management
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, DEPENDENCIES.md, README.md]
autonomous: true

must_haves:
  truths:
    - "RapidFuzz documented as optional dependency in DEPENDENCIES.md"
    - "Installation instructions provided for RapidFuzz"
    - "Performance impact documented (string matching speedup)"
    - "Graceful degradation pattern explained (warnings, fallback)"
  artifacts:
    - path: "requirements.txt"
      provides: "RapidFuzz optional dependency specification"
      contains: "rapidfuzz>=3.14.0"
    - path: "DEPENDENCIES.md"
      provides: "RapidFuzz documentation"
      contains: "RapidFuzz"
    - path: "README.md"
      provides: "Installation instructions for optional deps"
      contains: "pip install rapidfuzz"
  key_links:
    - from: "requirements.txt"
      to: "DEPENDENCIES.md"
      via: "RapidFuzz version reference in documentation"
      pattern: "rapidfuzz.*3\\.14\\.0"
    - from: "DEPENDENCIES.md"
      to: "2_Scripts/shared/string_matching.py"
      via: "Code reference for graceful degradation"
      pattern: "RAPIDFUZZ_AVAILABLE"
    - from: "README.md"
      to: "DEPENDENCIES.md"
      via: "Installation instructions cross-reference"
      pattern: "See DEPENDENCIES.md"
---

<objective>
Document RapidFuzz as an optional dependency with installation instructions and performance impact.

Purpose: Clarify RapidFuzz's role as optional dependency, provide clear installation guidance, and explain graceful degradation behavior.
Output: Comprehensive documentation in DEPENDENCIES.md and installation instructions in README.md.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/TECHNICAL_REMEDIATION_ROADMAP.md
@requirements.txt
@2_Scripts/shared/string_matching.py

# Concern: RapidFuzz Optional Dependency (Dependencies at Risk #4)
# Risk: Tier 3 fuzzy matching depends on rapidfuzz which is not in requirements.txt
# Impact: If rapidfuzz unavailable, entity linking loses Tier 3 matches reducing match rate

# Research findings (from planning context):
# - Current state: rapidfuzz>=3.14.0 in requirements.txt (optional)
# - Code pattern: Graceful degradation with warnings (RAPIDFUZZ_AVAILABLE flag)
# - Implementation: 2_Scripts/shared/string_matching.py warns if missing, falls back to no-match
# - Decision: Keep as optional with improved documentation (not making required)

# Scripts using RapidFuzz:
# - 2_Scripts/1_Sample/1.2_LinkEntities.py (Tier 3 fuzzy name matching)
# - 2_Scripts/shared/string_matching.py (core matching utilities)

# Graceful degradation pattern:
# - If RapidFuzz not installed: RAPIDFUZZ_AVAILABLE = False
# - Import warning: "RapidFuzz not installed. Fuzzy matching will be disabled. Install: pip install rapidfuzz"
# - Function behavior: Returns (query, 0.0) instead of fuzzy match if unavailable

# Impact analysis:
# - Tier 3 fuzzy matching: Disabled without RapidFuzz
# - Match rate: Lower (Tier 1 and Tier 2 still work)
# - Performance: Slower sequential processing if manually implemented later
# - Functionality: Pipeline completes, but with fewer entity matches

# Documentation requirements:
# 1. Document as optional in DEPENDENCIES.md
# 2. Explain graceful degradation behavior
# 3. Provide installation instructions
# 4. Document performance impact (fuzzy matching speedup)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify RapidFuzz optional dependency in requirements.txt</name>
  <files>requirements.txt</files>
  <action>
    Verify rapidfuzz>=3.14.0 is in requirements.txt as optional dependency.

    Add comment above the line explaining its optional nature:
    "# Optional: RapidFuzz for fuzzy string matching (Tier 3 entity linking)"
    "# Install for improved entity match rates: pip install rapidfuzz>=3.14.0"
    "# Pipeline runs without it (graceful degradation), but match rate is lower"

    If the comment already exists, ensure it's clear and comprehensive.

    This specification ensures:
    1. Users know it's optional (won't break install if unavailable)
    2. Installation instructions are clear
    3. Impact of missing RapidFuzz is documented
  </action>
  <verify>
    grep "rapidfuzz>=3.14.0" requirements.txt && grep "Optional" requirements.txt
  </verify>
  <done>requirements.txt includes rapidfuzz>=3.14.0 with clear optional annotation</done>
</task>

<task type="auto">
  <name>Task 2: Document RapidFuzz in DEPENDENCIES.md</name>
  <files>DEPENDENCIES.md</files>
  <action>
    Update DEPENDENCIES.md to add comprehensive RapidFuzz documentation:

    Add to "Optional Dependencies" section (create if not exists):

    ### RapidFuzz
    - **Version**: >=3.14.0 (optional, not pinned)
    - **Purpose**: Fuzzy string matching for entity linking (Tier 3 matching)
    - **Required**: No (graceful degradation)
    - **Impact if Missing**:
      - Tier 3 fuzzy name matching disabled
      - Lower entity match rate (Tier 1 and Tier 2 still work)
      - Pipeline completes successfully but with fewer matches
    - **Performance**:
      - Without: Tier 3 matching returns no matches (fast but incomplete)
      - With: Fuzzy matching with configurable thresholds (slower but more matches)
      - Typical speedup: 10-50x for large company name datasets vs. manual implementation
    - **Graceful Degradation**:
      - Import warning logged if unavailable
      - Functions return (query, 0.0) instead of fuzzy matches
      - No errors or pipeline failures
    - **Usage**:
      - 2_Scripts/1_Sample/1.2_LinkEntities.py (Tier 3 fuzzy matching)
      - 2_Scripts/shared/string_matching.py (core matching utilities)
    - **Installation**: `pip install rapidfuzz>=3.14.0`

    Add to "Version Pinning Rationale" section:
    - RapidFuzz: Optional, not pinned (>=3.14.0 allows automatic upgrades)
    - Graceful degradation pattern allows pipeline to run without it
    - Recommended for production use (higher match rates)

    Add new section "Optional Dependencies" if it doesn't exist, explaining the optional dependency pattern.
  </action>
  <verify>
    grep -q "RapidFuzz" DEPENDENCIES.md && grep -q "Optional" DEPENDENCIES.md && grep -q "graceful degradation" DEPENDENCIES.md
  </verify>
  <done>DEPENDENCIES.md documents RapidFuzz as optional with graceful degradation</done>
</task>

<task type="auto">
  <name>Task 3: Add RapidFuzz installation instructions to README.md</name>
  <files>README.md</files>
  <action>
    Create or update README.md to include RapidFuzz installation instructions:

    Add to "Installation" section:

    ### Optional Dependencies

    **RapidFuzz** (recommended for improved entity matching):
    ```bash
    pip install rapidfuzz>=3.14.0
    ```

    RapidFuzz enables Tier 3 fuzzy name matching in entity linking, which significantly improves match rates for company names with spelling variations or abbreviations. The pipeline runs without RapidFuzz (graceful degradation), but match rates will be lower.

    See DEPENDENCIES.md for details on optional dependencies and their impact.

    If README.md doesn't exist, create a basic one with:
    1. Project overview (brief)
    2. Installation (dependencies, optional deps)
    3. Quick start (running a script)
    4. Documentation references (DEPENDENCIES.md, UPGRADE_GUIDE.md)

    Keep README.md concise and user-focused (not technical deep dives).
  </action>
  <verify>
    test -f README.md && grep -q "RapidFuzz" README.md && grep -q "pip install" README.md
  </verify>
  <done>README.md includes RapidFuzz installation instructions</done>
</task>

</tasks>

<verification>
1. Verify requirements.txt includes rapidfuzz>=3.14.0 with optional annotation
2. Verify DEPENDENCIES.md documents RapidFuzz as optional with graceful degradation
3. Verify README.md includes installation instructions for optional dependencies
4. Verify all documentation cross-references each other
</verification>

<success_criteria>
1. RapidFuzz documented as optional in DEPENDENCIES.md
2. Graceful degradation pattern explained clearly
3. Installation instructions provided in README.md
4. Performance impact documented (higher match rates with RapidFuzz)
5. No confusion about whether RapidFuzz is required
</success_criteria>

<output>
After completion, create `.planning/phases/14-dependency-management/14-04-SUMMARY.md`
</output>
