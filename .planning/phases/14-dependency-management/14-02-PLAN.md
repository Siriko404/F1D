---
phase: 14-dependency-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, DEPENDENCIES.md]
autonomous: true

must_haves:
  truths:
    - "PyArrow pinned to 21.0.0 in requirements.txt"
    - "DEPENDENCIES.md documents PyArrow compatibility rationale"
    - "PyArrow version validated for Python 3.8-3.13 support"
    - "Performance impact documented for current version"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned PyArrow version"
      contains: "pyarrow==21.0.0"
    - path: "DEPENDENCIES.md"
      provides: "PyArrow compatibility documentation"
      contains: "PyArrow"
  key_links:
    - from: "requirements.txt"
      to: "DEPENDENCIES.md"
      via: "PyArrow version reference in documentation"
      pattern: "pyarrow.*21\\.0\\.0"
    - from: "DEPENDENCIES.md"
      to: "https://pypi.org/project/pyarrow/21.0.0"
      via: "Python version compatibility reference"
      pattern: "Python.*3\\.8"
---

<objective>
Pin PyArrow to 21.0.0 and document compatibility with Python 3.8-3.13 and performance characteristics.

Purpose: Ensure PyArrow version supports all target Python versions and document performance characteristics for future upgrades.
Output: Pinned version, compatibility documentation, and performance baseline.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/TECHNICAL_REMEDIATION_ROADMAP.md
@requirements.txt

# Concern: PyArrow Performance Degradation (Dependencies at Risk #2)
# Risk: pyarrow 21.0.0 may have performance regressions in newer versions
# Impact: Parquet read/write speed could slow down significantly

# Research findings (from planning context):
# - Current version: 21.0.0
# - Recommended: Keep pinned to 21.0.0
# - Rationale: PyArrow 23.0.0 requires Python >= 3.10 (incompatible with target 3.8-3.13)
# - Compatibility: 21.0.0 supports Python 3.8+ (perfect for target range)
# - Performance: Current version performs well; benchmark before future upgrades

# Scripts using PyArrow:
# - All scripts use pandas.read_parquet() which leverages PyArrow engine
# - Parquet is primary file format for intermediate and final outputs

# Performance testing requirement:
# - Before upgrading PyArrow, benchmark read/write performance
# - Compare with baseline (current version performance)
# - Document any performance regressions in UPGRADE_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify PyArrow 21.0.0 pin and document rationale</name>
  <files>requirements.txt</files>
  <action>
    Verify PyArrow is pinned to 21.0.0 in requirements.txt (should already be there).

    Add comment above the line explaining the pinning rationale:
    "Pinned to 21.0.0 for Python 3.8-3.13 compatibility. 23.0.0+ requires Python >= 3.10. See DEPENDENCIES.md for upgrade strategy and performance notes."

    If not present, add: pyarrow==21.0.0 with the comment above.

    This pinning ensures:
    1. Pipeline works on all target Python versions (3.8-3.13)
    2. No unexpected performance regressions from newer versions
    3. Clear baseline for performance comparisons
  </action>
  <verify>
    grep "pyarrow==21.0.0" requirements.txt && grep "DEPENDENCIES.md" requirements.txt
  </verify>
  <done>requirements.txt shows PyArrow 21.0.0 with compatibility comment</done>
</task>

<task type="auto">
  <name>Task 2: Document PyArrow compatibility and performance in DEPENDENCIES.md</name>
  <files>DEPENDENCIES.md</files>
  <action>
    Update DEPENDENCIES.md to add comprehensive PyArrow documentation:

    Add to the "Core Dependencies" section:

    ### PyArrow
    - **Version**: 21.0.0 (pinned)
    - **Purpose**: Parquet file read/write engine (used by pandas)
    - **Compatibility**: Python 3.8+ (supports target range 3.8-3.13)
    - **Rationale**:
      - 23.0.0+ requires Python >= 3.10 (incompatible with target range)
      - 21.0.0 provides stable performance and compatibility
    - **Performance**:
      - Current version: Performs well for typical dataset sizes
      - Future upgrades: Require benchmarking before deployment
      - See UPGRADE_GUIDE.md for performance testing procedure
    - **Usage**: All scripts use pandas.read_parquet() which leverages PyArrow engine
    - **Scripts affected**: All scripts reading/writing Parquet files

    Add to the "Version Pinning Rationale" section:
    - PyArrow pinned to 21.0.0 for maximum Python version compatibility
    - Future upgrades require Python 3.10+ support validation
    - Performance benchmarking required for upgrades (see UPGRADE_GUIDE.md)
  </action>
  <verify>
    grep -q "PyArrow" DEPENDENCIES.md && grep -q "21.0.0" DEPENDENCIES.md && grep -q "Python 3.8" DEPENDENCIES.md
  </verify>
  <done>DEPENDENCIES.md documents PyArrow compatibility and performance considerations</done>
</task>

<task type="auto">
  <name>Task 3: Add PyArrow upgrade procedure to UPGRADE_GUIDE.md</name>
  <files>UPGRADE_GUIDE.md</files>
  <action>
    Update UPGRADE_GUIDE.md to add PyArrow upgrade procedure:

    Add new section:

    ### PyArrow Upgrade Procedure

    Before upgrading PyArrow, verify Python version compatibility and performance:

    1. **Check Python Compatibility**:
       - Review PyArrow release notes for minimum Python version
       - Ensure target Python versions (3.8-3.13) are supported
       - PyArrow 23.0.0+ requires Python >= 3.10 (not compatible with 3.8-3.9)

    2. **Performance Benchmarking**:
       - Benchmark Parquet read/write performance with current version
       - Test with typical dataset sizes from pipeline (1_Inputs/ and 4_Outputs/)
       - Measure: read time, write time, memory usage
       - Record baseline in DEPENDENCIES.md

    3. **Test New Version**:
       - Upgrade to new version in requirements.txt
       - Run benchmarking again
       - Compare with baseline
       - If performance regression > 10%, defer upgrade or investigate

    4. **Full Pipeline Validation**:
       - Run full pipeline with new version
       - Verify no errors or warnings related to Parquet I/O
       - Check output file integrity (checksums match)

    5. **Update Documentation**:
       - Update version in requirements.txt
       - Update Python compatibility note in DEPENDENCIES.md
       - Document performance changes in DEPENDENCIES.md
       - Update UPGRADE_GUIDE.md with findings

    Include example benchmark command for future use.
  </action>
  <verify>
    grep -q "PyArrow Upgrade" UPGRADE_GUIDE.md && grep -q "benchmarking" UPGRADE_GUIDE.md
  </verify>
  <done>UPGRADE_GUIDE.md includes PyArrow upgrade procedure with performance testing</done>
</task>

</tasks>

<verification>
1. Verify requirements.txt has pyarrow==21.0.0 with compatibility comment
2. Verify DEPENDENCIES.md documents PyArrow compatibility rationale
3. Verify UPGRADE_GUIDE.md includes PyArrow upgrade procedure
4. Verify all documentation cross-references each other
</verification>

<success_criteria>
1. PyArrow pinned to 21.0.0 in requirements.txt
2. DEPENDENCIES.md documents Python 3.8-3.13 compatibility
3. Performance considerations documented for future upgrades
4. Upgrade procedure includes benchmarking requirements
</success_criteria>

<output>
After completion, create `.planning/phases/14-dependency-management/14-02-SUMMARY.md`
</output>
