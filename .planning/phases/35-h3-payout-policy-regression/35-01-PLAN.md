---
phase: 35-h3-payout-policy-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/4_Econometric_V2/4.3_H3PayoutPolicyRegression.py
autonomous: true

must_haves:
  truths:
    - "H3 regression script executes 48 regressions without errors"
    - "div_stability (H3a) and payout_flexibility (H3b) DVs tested independently"
    - "Stability regression tests beta1 < 0 and beta3 < 0 (one-tailed)"
    - "Flexibility regression tests beta1 > 0 and beta3 > 0 (one-tailed)"
    - "Results report X/6 measures significant per DV per hypothesis"
  artifacts:
    - path: "2_Scripts/4_Econometric_V2/4.3_H3PayoutPolicyRegression.py"
      provides: "H3 regression execution script"
      min_lines: 800
    - path: "4_Outputs/4_Econometric_V2/4.3_H3PayoutPolicyRegression/latest/H3_Regression_Results.parquet"
      provides: "All 48 regression coefficients and diagnostics"
    - path: "4_Outputs/4_Econometric_V2/4.3_H3PayoutPolicyRegression/latest/stats.json"
      provides: "N, R2, F-stat, hypothesis test outcomes"
    - path: "4_Outputs/4_Econometric_V2/4.3_H3PayoutPolicyRegression/latest/H3_RESULTS.md"
      provides: "Human-readable summary with significance counts"
  key_links:
    - from: "4.3_H3PayoutPolicyRegression.py"
      to: "H3_PayoutPolicy.parquet"
      via: "load_h3_variables() with div_stability and payout_flexibility"
      pattern: "H3_PayoutPolicy\\.parquet"
    - from: "4.3_H3PayoutPolicyRegression.py"
      to: "H1_CashHoldings.parquet"
      via: "load_h1_leverage() for moderator variable"
      pattern: "H1_CashHoldings\\.parquet"
    - from: "4.3_H3PayoutPolicyRegression.py"
      to: "shared/panel_ols.py"
      via: "run_panel_ols() for fixed effects regression"
      pattern: "run_panel_ols"
---

<objective>
Create and execute H3 Payout Policy Regression script (4.3_H3PayoutPolicyRegression.py) to test whether speech uncertainty affects dividend policy stability and flexibility, with leverage as a moderator.

Purpose: Complete the third hypothesis test (H3) following the established H1/H2 pattern, running 48 regressions (2 DVs × 6 uncertainty measures × 4 specifications).

Output: H3_Regression_Results.parquet, stats.json, and H3_RESULTS.md with hypothesis test outcomes (X/6 measures significant per DV).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-h3-payout-policy-regression/35-CONTEXT.md
@.planning/phases/34-h2-investment-efficiency-regression/34-01-SUMMARY.md
@.planning/phases/31-h3-payout-policy-vars/31-01-SUMMARY.md

# Reference scripts for mirroring
@2_Scripts/4_Econometric_V2/4.2_H2InvestmentEfficiencyRegression.py
@2_Scripts/4_Econometric_V2/4.1_H1CashHoldingsRegression.py

# Econometric infrastructure
@2_Scripts/shared/panel_ols.py
@2_Scripts/shared/centering.py
@2_Scripts/shared/diagnostics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and execute H3 Payout Policy regression script</name>
  <files>
    2_Scripts/4_Econometric_V2/4.3_H3PayoutPolicyRegression.py
    4_Outputs/4_Econometric_V2/4.3_H3PayoutPolicyRegression/{timestamp}/H3_Regression_Results.parquet
    4_Outputs/4_Econometric_V2/4.3_H3PayoutPolicyRegression/{timestamp}/stats.json
    4_Outputs/4_Econometric_V2/4.3_H3PayoutPolicyRegression/{timestamp}/H3_RESULTS.md
    3_Logs/4_Econometric_V2/4.3_H3PayoutPolicyRegression/{timestamp}_H3.log
  </files>
  <action>
    **Mirror 4.2_H2InvestmentEfficiencyRegression.py exactly**, adapting for H3 hypothesis.

    **Data Loading:**
    1. Load H3 variables from `H3_PayoutPolicy.parquet` (16,616 obs):
       - DVs: `div_stability`, `payout_flexibility`
       - Controls: `earnings_volatility`, `fcf_growth`, `firm_maturity`, `firm_size`, `roa`, `tobins_q`, `cash_holdings`
    2. Load leverage from `H1_CashHoldings.parquet` (same pattern as H2)
    3. Load speech uncertainty from linguistic_variables_*.parquet files
    4. Aggregate speech data to firm-year level (mean of uncertainty measures)

    **Regression Configuration:**
    - UNCERTAINTY_MEASURES = 6 measures (same as H1/H2)
    - DV_VARS = ['div_stability', 'payout_flexibility']
    - CONTROL_VARS = ['earnings_volatility', 'fcf_growth', 'firm_maturity', 'firm_size', 'roa', 'tobins_q', 'cash_holdings']
    - SPECS = 4 specifications (primary, pooled, year_only, double_cluster)

    **Model Specification:**
    ```
    DV_{t+1} = beta0 + beta1*Uncertainty_t + beta2*Leverage_t
              + beta3*(Uncertainty_t * Leverage_t)
              + gamma*Controls + Firm FE + Year FE + epsilon
    ```

    **Hypothesis Tests (CRITICAL - different directions for each DV):**
    
    For div_stability:
    - H3a_stability: beta1 < 0 (vagueness REDUCES stability)
    - H3b_stability: beta3 < 0 (leverage AMPLIFIES negative effect on stability)
    
    For payout_flexibility:
    - H3a_flexibility: beta1 > 0 (vagueness INCREASES flexibility)
    - H3b_flexibility: beta3 > 0 (leverage AMPLIFIES positive effect on flexibility)
    
    One-tailed p-value calculation:
    - For beta < 0 tests: p_one = p_two/2 if coef < 0, else 1 - p_two/2
    - For beta > 0 tests: p_one = p_two/2 if coef > 0, else 1 - p_two/2

    **Output Files:**
    - `H3_Regression_Results.parquet` (long-format: 48 regressions × ~11 coefficients each)
    - `stats.json` with N, R², F-stat, hypothesis test outcomes
    - `H3_RESULTS.md` with tables showing "X/6 measures significant" per DV

    **Null Result Handling:**
    - If 0/6 significant: "No support found for H3" framing (same as H1/H2)
    - Document results factually

    **After creating script, execute:**
    ```bash
    python 2_Scripts/4_Econometric_V2/4.3_H3PayoutPolicyRegression.py
    ```
  </action>
  <verify>
    1. Script compiles without syntax errors: `python -c "import sys; sys.path.insert(0, '2_Scripts'); exec(open('2_Scripts/4_Econometric_V2/4.3_H3PayoutPolicyRegression.py').read().split('if __name__')[0])"`
    2. Script executes with --dry-run if available
    3. Full execution produces all output files:
       - H3_Regression_Results.parquet exists and has ~528 rows (48 regressions × 11 coefficients)
       - stats.json contains N, R², coefficients for all specifications
       - H3_RESULTS.md shows complete tables with hypothesis test outcomes
    4. Log file shows clean execution with 48 regression messages
    5. No VIF or multicollinearity errors (threshold 5.0)
  </verify>
  <done>
    - 48 regressions complete (2 DVs × 6 measures × 4 specs)
    - H3a tested: beta1 < 0 for stability, beta1 > 0 for flexibility
    - H3b tested: beta3 < 0 for stability, beta3 > 0 for flexibility
    - Results documented with "X/6 measures significant" per DV
    - Requirements H3-06, H3-07, H3-08, H3-09 satisfied
  </done>
</task>

</tasks>

<verification>
**Requirements Coverage:**
- [ ] H3-06: Stability regression runs with expected signs (beta1 < 0, beta3 < 0)
- [ ] H3-07: Flexibility regression runs with expected signs (beta1 > 0, beta3 > 0)
- [ ] H3-08: Both DVs (Stability and Flexibility) tested independently
- [ ] H3-09: Output coefficient table and stats.json

**Quality Checks:**
- [ ] Script follows 4.2_H2InvestmentEfficiencyRegression.py pattern exactly
- [ ] Leverage merged from H1 data (H3 variables lack leverage column)
- [ ] Mean-centering applied before interaction term creation
- [ ] One-tailed hypothesis tests correctly applied per DV direction
- [ ] Results parquet has complete coefficient data for all 48 regressions
</verification>

<success_criteria>
1. **Script Created:** 4.3_H3PayoutPolicyRegression.py exists (~800-1000 lines)
2. **Execution Complete:** All 48 regressions run without errors
3. **Outputs Generated:**
   - H3_Regression_Results.parquet with coefficient data
   - stats.json with summary statistics and hypothesis tests
   - H3_RESULTS.md with human-readable tables
4. **Hypothesis Tests Documented:**
   - div_stability: "X/6 measures significant for H3a", "Y/6 for H3b"
   - payout_flexibility: "X/6 measures significant for H3a", "Y/6 for H3b"
5. **Log Clean:** No errors or warnings in execution log
</success_criteria>

<output>
After completion, create `.planning/phases/35-h3-payout-policy-regression/35-01-SUMMARY.md`
</output>
