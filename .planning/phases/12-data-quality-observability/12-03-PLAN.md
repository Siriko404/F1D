---
phase: 12-data-quality-observability
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - 2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py
  - 2_Scripts/3_Financial/3.1_FirmControls.py
  - 2_Scripts/3_Financial/3.2_MarketVariables.py
  - 2_Scripts/3_Financial/3.3_EventFlags.py
  - 2_Scripts/4_Econometric/4.1_EstimateCeoClarity.py
  - 2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py
  - 2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py
  - 2_Scripts/4_Econometric/4.1.3_EstimateCeoClarity_Regime.py
  - 2_Scripts/4_Econometric/4.1.4_EstimateCeoTone.py
  - 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py
  - 2_Scripts/4_Econometric/4.3_TakeoverHazards.py
  - 2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py
  - 4_Outputs/observability_summary.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All Step 3 scripts (3.0-3.3) track memory, throughput, output checksums"
    - "All Step 4 scripts (4.1-4.4) track memory, throughput, output checksums"
    - "Financial variables checked for anomalies (returns, market data, controls)"
    - "Econometric outputs checked for anomalies (regression coefficients, statistics)"
    - "Observability summary report generated (aggregated metrics across all scripts)"
  artifacts:
    - path: "2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py"
      provides: "Financial features with observability"
      contains: "import psutil", "memory", "throughput"
    - path: "2_Scripts/3_Financial/3.1_FirmControls.py"
      provides: "Firm controls with observability"
      contains: "memory", "throughput", "quality_anomalies"
    - path: "2_Scripts/3_Financial/3.2_MarketVariables.py"
      provides: "Market variables with observability"
      contains: "memory", "throughput", "quality_anomalies"
    - path: "2_Scripts/3_Financial/3.3_EventFlags.py"
      provides: "Event flags with observability"
      contains: "memory", "throughput"
    - path: "2_Scripts/4_Econometric/4.1_EstimateCeoClarity.py"
      provides: "CEO clarity regressions with observability"
      contains: "memory", "throughput"
    - path: "2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py"
      provides: "CEO-specific regressions with observability"
      contains: "memory"
    - path: "2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py"
      provides: "Extended regressions with observability"
      contains: "memory"
    - path: "2_Scripts/4_Econometric/4.1.3_EstimateCeoClarity_Regime.py"
      provides: "Regime regressions with observability"
      contains: "memory"
    - path: "2_Scripts/4_Econometric/4.1.4_EstimateCeoTone.py"
      provides: "CEO tone regressions with observability"
      contains: "memory"
    - path: "2_Scripts/4_Econometric/4.2_LiquidityRegressions.py"
      provides: "Liquidity regressions with observability"
      contains: "memory", "throughput", "quality_anomalies"
    - path: "2_Scripts/4_Econometric/4.3_TakeoverHazards.py"
      provides: "Takeover hazards with observability"
      contains: "memory", "throughput"
    - path: "2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py"
      provides: "Summary stats with observability"
      contains: "memory", "throughput"
    - path: "4_Outputs/observability_summary.json"
      provides: "Aggregated observability metrics"
      contains: "peak_memory_by_script", "throughput_by_script", "total_anomalies"
  key_links:
    - from: "All modified scripts"
      to: "12-01 helper functions"
      via: "Inline copy-paste of observability helpers"
      pattern: "def get_process_memory_mb"
    - from: "4_Outputs/observability_summary.json"
      to: "All stats.json files"
      via: "Aggregation script"
      pattern: "Load stats.json from all output directories"
---

<objective>
Roll out observability features to Step 3 (Financial) and Step 4 (Econometric) scripts, completing coverage of all 19 core pipeline scripts. Generate aggregated observability summary report.

Purpose: Add memory tracking, throughput metrics, output checksums, and data quality anomaly detection to the remaining 11 scripts. Create summary report to provide cross-script visibility into resource usage and data quality.

Output: 11 modified scripts with observability features, observability summary report with aggregated metrics.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-data-quality-observability/12-data-quality-observability-RESEARCH.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-data-quality-observability/12-01-PLAN.md
@.planning/phases/12-data-quality-observability/12-02-PLAN.md

# Existing scripts
@2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py
@2_Scripts/4_Econometric/4.1_EstimateCeoClarity.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add observability to Step 3 scripts (3.0-3.3)</name>
  <files>2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py, 2_Scripts/3_Financial/3.1_FirmControls.py, 2_Scripts/3_Financial/3.2_MarketVariables.py, 2_Scripts/3_Financial/3.3_EventFlags.py</files>
  <action>
    For each script in Step 3 (3.0, 3.1, 3.2, 3.3), add observability features using same pattern as Plan 02:

    **Step 3.1: Add imports**
    - Add import psutil
    - Preserve import order

    **Step 3.2: Add inline helper functions**
    Copy same 5 helper functions from 12-01 template:
    - get_process_memory_mb()
    - calculate_throughput()
    - compute_file_checksum()
    - detect_anomalies_zscore()
    - detect_anomalies_iqr()

    **Step 3.3: Add memory tracking**
    Track memory at key points (start, after data loads/merges, after processing, end).

    **Step 3.4: Add throughput calculation**
    Calculate rows/second using existing timing infrastructure.

    **Step 3.5: Add output checksums**
    Compute SHA-256 checksums for all output files.

    **Step 3.6: Add anomaly detection**
    For numeric columns:
    - 3.0_BuildFinancialFeatures: Check merged variables, Compustat/IBES/CCLM/CCM/SDC merge results
    - 3.1_FirmControls: Check Size, BM, Lev, ROA, and other financial controls
    - 3.2_MarketVariables: Check stock returns, Amihud liquidity, market data
    - 3.3_EventFlags: Check takeover event flags, binary variables

    **Script-specific considerations:**
    - 3.0_BuildFinancialFeatures: Multiple data sources merged, track memory after each merge
    - 3.1_FirmControls: Large CRSP data (3.5M observations/year), track memory during joins
    - 3.2_MarketVariables: Computationally intensive (stock returns, liquidity), track memory during calculations
    - 3.3_EventFlags: Lighter script, standard memory tracking sufficient

    **Anomaly detection notes:**
    - Use z-score method with threshold=3.0 (consistent across all scripts)
    - Focus on key regression variables: returns, liquidity measures, financial controls
    - Skip binary/dummy variables for anomaly detection (flag variables are by design 0/1)

    **Important:**
    - DO NOT modify existing stats.json nested structures
    - Add NEW top-level sections only (memory, throughput, output_checksums, quality_anomalies)
    - Preserve all existing functionality
  </action>
  <verify>
    For each script:
    python -c "
import ast
import sys

script = sys.argv[1]
with open(script, 'r') as f:
    tree = ast.parse(f.read())

imports = [node.names[0].name for node in ast.walk(tree) if isinstance(node, ast.Import)]
if 'psutil' not in imports:
    print(f'{script}: Missing psutil import')
    sys.exit(1)

funcs = {node.name for node in ast.walk(tree) if isinstance(node, ast.FunctionDef)}
required = {
    'get_process_memory_mb',
    'calculate_throughput',
    'compute_file_checksum',
    'detect_anomalies_zscore'
}
missing = required - funcs
if missing:
    print(f'{script}: Missing functions: {missing}')
    sys.exit(1)

print(f'{script}: Observability features present')
" 2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py
  </verify>
  <done>All 4 Step 3 scripts have psutil import, helper functions, and memory/throughput/checksum/anomaly tracking</done>
</task>

<task type="auto">
  <name>Task 2: Add observability to Step 4 scripts (4.1-4.4)</name>
  <files>2_Scripts/4_Econometric/4.1_EstimateCeoClarity.py, 2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py, 2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py, 2_Scripts/4_Econometric/4.1.3_EstimateCeoClarity_Regime.py, 2_Scripts/4_Econometric/4.1.4_EstimateCeoTone.py, 2_Scripts/4_Econometric/4.2_LiquidityRegressions.py, 2_Scripts/4_Econometric/4.3_TakeoverHazards.py, 2_Scripts/4_Econometric/4.4_GenerateSummaryStats.py</files>
  <action>
    For each script in Step 4 (4.1 main script and 4.1.* subscripts, 4.2, 4.3, 4.4), add observability features:

    **Step 4.1: Add imports**
    - Add import psutil
    - Preserve import order

    **Step 4.2: Add inline helper functions**
    Copy same 5 helper functions from 12-01 template.

    **Step 4.3: Add memory tracking**
    Track memory at key points.

    **Step 4.4: Add throughput calculation**
    Calculate rows/second for scripts with data operations.

    **Step 4.5: Add output checksums**
    Compute SHA-256 checksums for output files.

    **Step 4.6: Add anomaly detection**
    For numeric columns:
    - 4.1_EstimateCeoClarity: Check regression results, coefficient estimates
    - 4.1.1_EstimateCeoClarity_CeoSpecific: Check CEO-specific regression outputs
    - 4.1.2_EstimateCeoClarity_Extended: Check extended regression outputs
    - 4.1.3_EstimateCeoClarity_Regime: Check regime-specific regression outputs
    - 4.1.4_EstimateCeoTone: Check CEO tone regression outputs
    - 4.2_LiquidityRegressions: Check IV regression results, coefficients
    - 4.3_TakeoverHazards: Check survival model outputs, hazard ratios
    - 4.4_GenerateSummaryStats: Check summary statistics (may skip anomaly detection for aggregates)

    **Script-specific considerations:**

    **4.1_EstimateCeoClarity.py** (main script):
    - This is a wrapper script that calls 4.1.* subscripts
    - Track memory at script start/end and before/after each subprocess call
    - Add memory tracking to subprocess scripts (4.1.*) for granularity

    **4.1.* subscripts** (CeoSpecific, Extended, Regime, Tone):
    - These are standalone regression scripts
    - Track memory during regression estimation (statsmodels models can be memory-intensive)
    - Track throughput (rows processed per regression run)

    **4.2_LiquidityRegressions.py**:
    - IV regressions (2-stage least squares)
    - Track memory during first and second stage regressions
    - Check anomaly detection on instrument variables and regression coefficients

    **4.3_TakeoverHazards.py**:
    - Cox proportional hazards models
    - Track memory during survival model fitting
    - Check anomaly detection on hazard ratios

    **4.4_GenerateSummaryStats.py**:
    - Generates final descriptive statistics and correlations
    - Track memory for large summary operations
    - May skip anomaly detection (aggregating data, not analyzing raw values)

    **Anomaly detection on regression outputs:**
    - Focus on coefficient estimates (regression coefficients, standard errors)
    - Check for extreme values in coefficients (e.g., > 100 or < -100 for log-transformed variables)
    - Check for unrealistic p-values (should be 0-1)
    - Skip for dummy variables/indicator variables

    **Important:**
    - DO NOT modify existing stats.json nested structures
    - Add NEW top-level sections only
    - Preserve all existing functionality
    - For wrapper script (4.1), coordinate with subprocess scripts to avoid duplicate tracking
  </action>
  <verify>
    For each script:
    python -c "
import ast
import sys

script = sys.argv[1]
with open(script, 'r') as f:
    tree = ast.parse(f.read())

imports = [node.names[0].name for node in ast.walk(tree) if isinstance(node, ast.Import)]
if 'psutil' not in imports:
    print(f'{script}: Missing psutil import')
    sys.exit(1)

funcs = {node.name for node in ast.walk(tree) if isinstance(node, ast.FunctionDef)}
required = {
    'get_process_memory_mb',
    'calculate_throughput',
    'compute_file_checksum'
}
missing = required - funcs
if missing:
    print(f'{script}: Missing functions: {missing}')
    sys.exit(1)

print(f'{script}: Observability features present')
" 2_Scripts/4_Econometric/4.1_EstimateCeoClarity.py
  </verify>
  <done>All 8 Step 4 scripts have psutil import, helper functions, and memory/throughput/checksum tracking</done>
</task>

<task type="auto">
  <name>Task 3: Generate observability summary report</name>
  <files>4_Outputs/observability_summary.json</files>
  <action>
    Create a Python script that aggregates observability metrics from all stats.json files and generates a summary report.

    **Script: 2_Scripts/12_generate_observability_summary.py**

    **Functionality:**
    1. Scan all output directories in 4_Outputs/ for stats.json files
    2. Load each stats.json file
    3. Extract observability metrics:
       - memory (start_mb, end_mb, peak_mb, delta_mb)
       - throughput (rows_per_second, total_rows, duration_seconds)
       - output_checksums (list of checksums)
       - quality_anomalies (sum of anomaly counts across all columns)

    4. Generate aggregated metrics:
       - peak_memory_by_script: {script_id: peak_mb}
       - total_memory_by_stage: {stage: sum_peak_mb}
       - throughput_by_script: {script_id: rows_per_second}
       - average_throughput_by_stage: {stage: avg_rows_per_sec}
       - total_anomalies_by_script: {script_id: total_anomalies}
       - total_anomalies_by_stage: {stage: sum_anomalies}
       - checksum_count_by_script: {script_id: num_checksums}

    5. Generate summary statistics:
       - Total peak memory across all scripts (MB)
       - Average peak memory per script (MB)
       - Total rows processed across all scripts
       - Total duration across all scripts (seconds)
       - Overall throughput (total rows / total duration)
       - Total anomalies detected across all scripts
       - Total output files with checksums

    6. Save summary to 4_Outputs/observability_summary.json:
    ```json
    {
      "summary_generated": "ISO_TIMESTAMP",
      "total_scripts": 19,
      "aggregated_metrics": {
        "total_peak_memory_mb": ...,
        "average_peak_memory_mb": ...,
        "total_rows_processed": ...,
        "total_duration_seconds": ...,
        "overall_throughput_rows_per_sec": ...,
        "total_anomalies_detected": ...,
        "total_output_checksums": ...
      },
      "by_stage": {
        "1_Sample": {
          "scripts": [...],
          "total_peak_memory_mb": ...,
          "average_throughput_rows_per_sec": ...,
          "total_anomalies": ...
        },
        "2_Text": {...},
        "3_Financial": {...},
        "4_Econometric": {...}
      },
      "by_script": {
        "1.1_CleanMetadata": {
          "peak_memory_mb": ...,
          "throughput_rows_per_sec": ...,
          "output_checksums": 2,
          "total_anomalies": ...
        },
        ...
      }
    }
    ```

    7. Print summary to console:
       - Overall statistics
       - Stage-by-stage breakdown
       - Top 5 scripts by peak memory
       - Top 5 scripts by throughput
       - Scripts with most anomalies detected

    **Execution:**
    - Run after all scripts have been modified and executed at least once
    - If stats.json files don't exist for some scripts, handle gracefully (report missing)
    - If observability sections missing from some stats.json files, report gracefully

    **Output:**
    - Save to 4_Outputs/observability_summary.json
    - Print to console with formatted summary tables
  </action>
  <verify>
    python 2_Scripts/12_generate_observability_summary.py && cat 4_Outputs/observability_summary.json | jq '.aggregated_metrics'
  </verify>
  <done>Observability summary report generated with aggregated metrics across all 19 scripts</done>
</task>

</tasks>

<verification>
Verify observability features work in Step 3 and Step 4 scripts:

1. Check imports: grep -r "import psutil" 2_Scripts/3_Financial 2_Scripts/4_Econometric
2. Run a sample script: python 2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py
3. Check stats.json: cat 4_Outputs/3.0_BuildFinancialFeatures/latest/stats.json | jq '.memory, .throughput, .output.checksums, .quality_anomalies'
4. Generate summary report: python 2_Scripts/12_generate_observability_summary.py
5. Verify summary: cat 4_Outputs/observability_summary.json | jq '.aggregated_metrics, .by_stage'
</verification>

<success_criteria>
1. All 11 Step 3-4 scripts have psutil import and inline helper functions
2. All Step 3 scripts track memory, throughput, output checksums, and financial variable anomalies
3. All Step 4 scripts track memory, throughput, output checksums, and regression output anomalies
4. Observability summary report is generated with:
   - Aggregated metrics (total peak memory, average throughput, total anomalies)
   - Stage-by-stage breakdown
   - Per-script breakdown
5. All 19 core pipeline scripts now have observability features
6. Existing stats.json structure is preserved (backward compatible)
7. Phase 12 success criteria met:
   - All scripts track memory usage (peak, average per operation) ✓
   - All scripts track throughput (rows/second) ✓
   - Output files have checksums (SHA-256) ✓
   - Data quality anomalies are detected and flagged ✓
</success_criteria>

<output>
After completion, create `.planning/phases/12-data-quality-observability/12-03-SUMMARY.md`
</output>
