---
phase: 12-data-quality-observability
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - 2_Scripts/1_Sample/1.1_CleanMetadata.py
  - 2_Scripts/1_Sample/1.2_LinkEntities.py
  - 2_Scripts/1_Sample/1.3_BuildTenureMap.py
  - 2_Scripts/1_Sample/1.4_AssembleManifest.py
  - 2_Scripts/2_Text/2.1_TokenizeAndCount.py
  - 2_Scripts/2_Text/2.2_ConstructVariables.py
  - 2_Scripts/2_Text/2.3_VerifyStep2.py
  - tests/integration/test_observability_integration.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All Step 1 scripts (1.1-1.4) track memory, throughput, output checksums"
    - "All Step 2 scripts (2.1-2.3) track memory, throughput, output checksums"
    - "stats.json includes new sections: memory, throughput, output_checksums"
    - "Numeric columns checked for anomalies (z-score method, threshold=3.0)"
    - "Integration tests verify observability features work end-to-end"
  artifacts:
    - path: "2_Scripts/1_Sample/1.1_CleanMetadata.py"
      provides: "Sample construction with observability"
      contains: "import psutil", "memory", "throughput", "output_checksums"
    - path: "2_Scripts/1_Sample/1.2_LinkEntities.py"
      provides: "Entity linking with observability"
      contains: "memory", "throughput"
    - path: "2_Scripts/1_Sample/1.3_BuildTenureMap.py"
      provides: "Tenure map with observability"
      contains: "memory", "throughput"
    - path: "2_Scripts/1_Sample/1.4_AssembleManifest.py"
      provides: "Sample manifest with observability"
      contains: "memory", "throughput"
    - path: "2_Scripts/2_Text/2.1_TokenizeAndCount.py"
      provides: "Tokenization with observability"
      contains: "memory", "throughput"
    - path: "2_Scripts/2_Text/2.2_ConstructVariables.py"
      provides: "Text variables with observability"
      contains: "memory", "throughput"
    - path: "2_Scripts/2_Text/2.3_VerifyStep2.py"
      provides: "Step 2 verification with observability"
      contains: "memory", "throughput"
    - path: "tests/integration/test_observability_integration.py"
      provides: "Integration tests for observability"
      exports: ["test_1.1_observability", "test_2.1_observability", "test_stats_json_schema"]
  key_links:
    - from: "2_Scripts/1_Sample/1.1_CleanMetadata.py"
      to: "12-01 helper functions"
      via: "Inline copy-paste of get_process_memory_mb, calculate_throughput, etc."
      pattern: "def get_process_memory_mb"
    - from: "All modified scripts"
      to: "requirements.txt"
      via: "psutil import"
      pattern: "import psutil"
---

<objective>
Roll out observability features to Step 1 (Sample) and Step 2 (Text) scripts, adding memory tracking, throughput metrics, output checksums, and data quality anomaly detection.

Purpose: Add observability to 8 core data processing scripts following the infrastructure established in Plan 01. Each script becomes self-contained with inline helper functions, enabling reviewers to trace resource usage and data quality at each pipeline stage.

Output: 8 modified scripts with observability features, integration tests verifying correctness.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-data-quality-observability/12-data-quality-observability-RESEARCH.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-data-quality-observability/12-01-PLAN.md

# Existing stats structure
@2_Scripts/1_Sample/1.1_CleanMetadata.py
@2_Scripts/2_Text/2.1_TokenizeAndCount.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add observability to Step 1 scripts (1.1-1.4)</name>
  <files>2_Scripts/1_Sample/1.1_CleanMetadata.py, 2_Scripts/1_Sample/1.2_LinkEntities.py, 2_Scripts/1_Sample/1.3_BuildTenureMap.py, 2_Scripts/1_Sample/1.4_AssembleManifest.py</files>
  <action>
    For each script in Step 1 (1.1, 1.2, 1.3, 1.4), add observability features:

    **Step 1.1: Add imports**
    - Add import psutil to existing imports
    - Preserve existing import order (stdlib first, then third-party)

    **Step 1.2: Add inline helper functions**
    Copy helper functions from 12-01 template:
    - get_process_memory_mb()
    - calculate_throughput(rows, seconds)
    - compute_file_checksum(filepath, algorithm="sha256")
    - detect_anomalies_zscore(df, columns, threshold=3.0)
    - detect_anomalies_iqr(df, columns, multiplier=3.0)

    Place before main() function, after existing utility functions.

    **Step 1.3: Add memory tracking**
    Track memory at key points:
    - Script start (before data load)
    - After major data operations (filtering, merging)
    - Script end (before stats save)

    Add to stats dictionary:
    stats["memory"] = {
        "start_mb": mem_start["rss_mb"],
        "end_mb": mem_end["rss_mb"],
        "peak_mb": max(all_memory_values),
        "delta_mb": mem_end["rss_mb"] - mem_start["rss_mb"]
    }

    **Step 1.4: Add throughput calculation**
    Use existing timing infrastructure (time.perf_counter() already in all scripts):
    throughput = calculate_throughput(stats["output"]["final_rows"], duration_seconds)

    Add to stats dictionary:
    stats["throughput"] = {
        "rows_per_second": round(throughput, 2),
        "total_rows": stats["output"]["final_rows"],
        "duration_seconds": round(duration_seconds, 3)
    }

    **Step 1.5: Add output checksums**
    After saving output files, compute checksums:
    stats["output"]["checksums"] = {}
    for output_file in output_files:
        checksum = compute_file_checksum(output_file)
        stats["output"]["checksums"][output_file.name] = checksum

    **Step 1.6: Add anomaly detection**
    For numeric columns in final DataFrame:
    - Identify numeric columns (df.select_dtypes(include=[np.number]).columns)
    - Apply detect_anomalies_zscore() with threshold=3.0
    - Skip text/categorical columns

    Add to stats dictionary:
    stats["quality_anomalies"] = detect_anomalies_zscore(
        df_final,
        numeric_columns=list_of_numeric_cols,
        threshold=3.0
    )

    **Anomaly detection per script:**
    - 1.1_CleanMetadata: Check financial control columns (if any in output)
    - 1.2_LinkEntities: Check match scores, tenure values
    - 1.3_BuildTenureMap: Check tenure ranges, CEO counts
    - 1.4_AssembleManifest: Check final sample counts, industry distributions

    **Important:**
    - DO NOT modify existing stats.json nested structures (input, output, processing, timing, missing_values)
    - Add NEW top-level sections only (memory, throughput, output_checksums, quality_anomalies)
    - Preserve all existing functionality and stats
    - Follow inline helper pattern (copy-paste ready)
  </action>
  <verify>
    For each script:
    python -c "
import ast
import sys

script = sys.argv[1]
with open(script, 'r') as f:
    tree = ast.parse(f.read())

# Check psutil import
imports = [node.names[0].name for node in ast.walk(tree) if isinstance(node, ast.Import)]
if 'psutil' not in imports:
    print(f'{script}: Missing psutil import')
    sys.exit(1)

# Check helper functions
funcs = {node.name for node in ast.walk(tree) if isinstance(node, ast.FunctionDef)}
required = {
    'get_process_memory_mb',
    'calculate_throughput',
    'compute_file_checksum',
    'detect_anomalies_zscore'
}
missing = required - funcs
if missing:
    print(f'{script}: Missing functions: {missing}')
    sys.exit(1)

print(f'{script}: Observability features present')
" 2_Scripts/1_Sample/1.1_CleanMetadata.py
  </verify>
  <done>All 4 Step 1 scripts have psutil import, helper functions, and memory/throughput/checksum/anomaly tracking</done>
</task>

<task type="auto">
  <name>Task 2: Add observability to Step 2 scripts (2.1-2.3)</name>
  <files>2_Scripts/2_Text/2.1_TokenizeAndCount.py, 2_Scripts/2_Text/2.2_ConstructVariables.py, 2_Scripts/2_Text/2.3_VerifyStep2.py</files>
  <action>
    For each script in Step 2 (2.1, 2.2, 2.3), add observability features using the same pattern as Task 1:

    **Step 2.1: Add imports**
    - Add import psutil
    - Preserve import order

    **Step 2.2: Add inline helper functions**
    Copy the same 5 helper functions from 12-01 template.

    **Step 2.3: Add memory tracking**
    Track memory at key points (start, after major ops, end).

    **Step 2.4: Add throughput calculation**
    Calculate rows/second using existing timing infrastructure.

    **Step 2.5: Add output checksums**
    Compute SHA-256 checksums for all output files.

    **Step 2.6: Add anomaly detection**
    For numeric columns:
    - 2.1_TokenizeAndCount: Check token counts, word counts, vocabulary hits
    - 2.2_ConstructVariables: Check linguistic variables (clarity, tone measures)
    - 2.3_VerifyStep2: May not have numeric columns to check (validation script)

    **Script-specific considerations:**
    - 2.1_TokenizeAndCount: Large data processing (27.8M input rows), track memory at year-level loop checkpoints
    - 2.2_ConstructVariables: 110 columns per year, track memory after variable construction
    - 2.3_VerifyStep2: May skip anomaly detection if no numeric output (validation script)

    **Important:**
    - DO NOT modify existing stats.json nested structures
    - Add NEW top-level sections only
    - Preserve all existing functionality
  </action>
  <verify>
    For each script:
    python -c "
import ast
import sys

script = sys.argv[1]
with open(script, 'r') as f:
    tree = ast.parse(f.read())

imports = [node.names[0].name for node in ast.walk(tree) if isinstance(node, ast.Import)]
if 'psutil' not in imports:
    print(f'{script}: Missing psutil import')
    sys.exit(1)

funcs = {node.name for node in ast.walk(tree) if isinstance(node, ast.FunctionDef)}
required = {
    'get_process_memory_mb',
    'calculate_throughput',
    'compute_file_checksum',
    'detect_anomalies_zscore'
}
missing = required - funcs
if missing:
    print(f'{script}: Missing functions: {missing}')
    sys.exit(1)

print(f'{script}: Observability features present')
" 2_Scripts/2_Text/2.1_TokenizeAndCount.py
  </verify>
  <done>All 3 Step 2 scripts have psutil import, helper functions, and memory/throughput/checksum/anomaly tracking</done>
</task>

<task type="auto">
  <name>Task 3: Create integration tests for observability</name>
  <files>tests/integration/test_observability_integration.py</files>
  <action>
    Create pytest integration tests to verify observability features work end-to-end in modified scripts:

    **Test structure:**
    - Use pytest.mark.integration (mark as integration test)
    - Mark with pytest.mark.slow (these tests may take time to run)
    - Use fixtures from conftest.py (test data paths, config loading)

    **Tests to implement:**

    1. test_1.1_observability():
       - Run 1.1_CleanMetadata.py (or import and call main())
       - Load resulting stats.json
       - Verify new sections exist: memory, throughput, output_checksums, quality_anomalies
       - Verify memory.start_mb > 0, memory.end_mb > 0, memory.peak_mb >= memory.end_mb
       - Verify throughput.rows_per_second > 0
       - Verify output_checksums is non-empty dict
       - Verify quality_anomalies is dict (may be empty if no numeric columns)

    2. test_2.1_observability():
       - Run 2.1_TokenizeAndCount.py
       - Load stats.json and verify observability sections
       - Verify memory tracking for long-running process
       - Verify throughput (should be high for 27.8M rows)

    3. test_stats_json_schema():
       - Verify backward compatibility (existing sections unchanged)
       - Verify new sections are properly structured
       - Test with actual stats.json from modified scripts

    **Test patterns:**
    - Use subprocess.run() to execute scripts (or import functions if possible)
    - Parse JSON output and validate schema
    - Check for expected keys and data types
    - Verify no regressions (existing stats still present)

    **Notes:**
    - These are integration tests (end-to-end), may require test data
    - Mark as @pytest.mark.integration
    - May skip if test data not available (use pytest.skip with reason)
  </action>
  <verify>
    cd tests && python -m pytest integration/test_observability_integration.py -v -m integration --tb=short
  </verify>
  <done>Integration tests verify observability features work in modified scripts</done>
</task>

</tasks>

<verification>
Verify observability features work in Step 1 and Step 2 scripts:

1. Check imports: grep -r "import psutil" 2_Scripts/1_Sample 2_Scripts/2_Text
2. Run a sample script: python 2_Scripts/1_Sample/1.1_CleanMetadata.py
3. Check stats.json: cat 4_Outputs/1.1_CleanMetadata/latest/stats.json | jq '.memory, .throughput, .output.checksums, .quality_anomalies'
4. Run integration tests: pytest tests/integration/test_observability_integration.py -v -m integration
</verification>

<success_criteria>
1. All 8 scripts have psutil import and inline helper functions
2. All scripts track memory (start, end, peak, delta)
3. All scripts calculate throughput (rows/second)
4. All scripts have output file checksums
5. All scripts with numeric data detect anomalies (z-score, threshold=3.0)
6. Integration tests pass
7. Existing stats.json structure is preserved (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/12-data-quality-observability/12-02-SUMMARY.md`
</output>
