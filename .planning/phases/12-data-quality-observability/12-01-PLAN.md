---
phase: 12-data-quality-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - tests/unit/test_observability_helpers.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "psutil>=7.2.1 is installed via requirements.txt"
    - "Inline helper functions exist for memory, throughput, checksums, and anomaly detection"
    - "Unit tests verify helper functions work correctly"
    - "Helper functions follow Phase 1 inline pattern (copy-paste ready)"
  artifacts:
    - path: "requirements.txt"
      provides: "Python dependencies with psutil added"
      contains: "psutil>=7.2.1"
    - path: "tests/unit/test_observability_helpers.py"
      provides: "Unit tests for observability helpers"
      exports: ["test_get_process_memory_mb", "test_calculate_throughput", "test_detect_anomalies_zscore", "test_detect_anomalies_iqr", "test_compute_file_checksum"]
  key_links:
    - from: "tests/unit/test_observability_helpers.py"
      to: "requirements.txt"
      via: "psutil import"
      pattern: "import psutil"
---

<objective>
Create observability infrastructure with psutil dependency and inline helper functions for memory tracking, throughput calculation, output checksums, and data quality anomaly detection.

Purpose: Establish the foundation for adding observability to all 19 core pipeline scripts in subsequent plans. This follows the Phase 1 inline helper pattern to keep scripts self-contained and copy-paste ready for replication reviewers.

Output: Updated requirements.txt with psutil, inline helper functions template, and unit tests verifying correctness.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-data-quality-observability/12-data-quality-observability-RESEARCH.md
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Prior phase patterns
@.planning/phases/11-data-quality-observability/11-*-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add psutil dependency to requirements.txt</name>
  <files>requirements.txt</files>
  <action>
    1. Read existing requirements.txt to understand current format (pinned versions, alphabetical order)
    2. Add psutil with pinned version: psutil==7.2.1 (from research)
    3. Insert in alphabetical order to maintain consistency
    4. Do NOT modify any other existing dependencies
    5. If file doesn't exist in root, create it with psutil==7.2.1 as the first entry

    Rationale from RESEARCH.md:
    - psutil is cross-platform (Windows/Linux/macOS)
    - Production-stable, last release Dec 2025
    - De facto standard for system metrics
    - Alternative: resource module is Unix-only, memory_profiler is deprecated
  </action>
  <verify>
    grep -q "psutil==7.2.1" requirements.txt && echo "psutil added successfully" || echo "psutil not found"
  </verify>
  <done>requirements.txt contains psutil==7.2.1 in correct format and location</done>
</task>

<task type="auto">
  <name>Task 2: Create inline helper functions for observability</name>
  <files>tests/unit/test_observability_helpers.py</files>
  <action>
    Create a Python file with inline helper functions that can be copy-pasted into any script. These follow the Phase 1 inline helper pattern for self-contained replication.

    Include these functions (based on RESEARCH.md patterns):

    1. get_process_memory_mb():
       - Import psutil
       - Get current process memory in MB (RSS, VMS, percent)
       - Return dict with rss_mb, vms_mb, percent

    2. calculate_throughput(rows_processed, duration_seconds):
       - Calculate rows/second
       - Handle division by zero
       - Return float rounded to 2 decimals

    3. compute_file_checksum(filepath, algorithm="sha256"):
       - Use hashlib (existing pattern)
       - Read in 8KB chunks for memory efficiency
       - Return hexdigest string

    4. detect_anomalies_zscore(df, columns, threshold=3.0):
       - Check column dtype is numeric (skip non-numeric)
       - Drop NaN for calculation
       - Calculate mean and std
       - Handle std == 0 (no anomalies)
       - Return dict with count, sample_anomalies (top 10), threshold, mean, std

    5. detect_anomalies_iqr(df, columns, multiplier=3.0):
       - Check column dtype is numeric
       - Drop NaN
       - Calculate Q1, Q3, IQR
       - Handle IQR == 0
       - Return dict with count, sample_anomalies, iqr_bounds

    Pattern notes:
    - Use type hints for clarity
    - Include docstrings with usage examples
    - Make functions deterministic (no randomness)
    - Handle edge cases (empty DataFrames, all NaN, zero variance)
    - Follow existing code style (from scripts like 1.1_CleanMetadata.py)

    Store in tests/unit/test_observability_helpers.py as the "template file" for copy-paste.
  </action>
  <verify>
    python -c "
import ast
import sys
from pathlib import Path

# Parse file to check all functions exist
with open('tests/unit/test_observability_helpers.py', 'r') as f:
    tree = ast.parse(f.read())

funcs = {node.name for node in ast.walk(tree) if isinstance(node, ast.FunctionDef)}
required = {
    'get_process_memory_mb',
    'calculate_throughput',
    'compute_file_checksum',
    'detect_anomalies_zscore',
    'detect_anomalies_iqr'
}

missing = required - funcs
if missing:
    print(f'Missing functions: {missing}')
    sys.exit(1)

print('All helper functions present')
"
  </verify>
  <done>test_observability_helpers.py contains all 5 observability helper functions with proper implementation</done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for observability helpers</name>
  <files>tests/unit/test_observability_helpers.py</files>
  <action>
    Add pytest unit tests to tests/unit/test_observability_helpers.py (append to the helper functions).

    Tests to implement:

    1. test_get_process_memory_mb():
       - Call function and verify it returns dict with keys: rss_mb, vms_mb, percent
       - Verify values are numeric (float/int)
       - Verify rss_mb > 0 (process is running)

    2. test_calculate_throughput():
       - Test normal case: calculate_throughput(1000, 10) == 100.0
       - Test division by zero: calculate_throughput(100, 0) == 0.0
       - Test rounding: calculate_throughput(1, 3) == 0.33 (2 decimals)

    3. test_compute_file_checksum():
       - Create a temporary file with known content
       - Verify checksum matches expected SHA-256
       - Test with algorithm="sha256" (default)

    4. test_detect_anomalies_zscore():
       - Create test DataFrame with known outliers (values far from mean)
       - Test with threshold=3.0
       - Verify count of anomalies detected
       - Test with all NaN column (should return count=0)
       - Test with zero std column (should return count=0)
       - Test with non-numeric column (should skip it)

    5. test_detect_anomalies_iqr():
       - Create test DataFrame with known outliers
       - Test with multiplier=3.0
       - Verify count of anomalies detected
       - Verify iqr_bounds in result
       - Test edge cases (empty DataFrame, all NaN, zero IQR)

    Test patterns:
    - Use pytest fixtures for setup (test DataFrame, temp file)
    - Use @pytest.mark.parametrize for multiple test cases
    - Test deterministic behavior (same input = same output)
    - Test edge cases thoroughly
  </action>
  <verify>
    cd tests && python -m pytest unit/test_observability_helpers.py -v --tb=short
  </verify>
  <done>All unit tests pass (5 tests, covering all observability helper functions)</done>
</task>

</tasks>

<verification>
Run all tests to verify observability infrastructure works:

1. Verify psutil is available: python -c "import psutil; print(psutil.__version__)"
2. Run unit tests: pytest tests/unit/test_observability_helpers.py -v
3. Check test coverage: pytest tests/unit/test_observability_helpers.py --cov=tests/unit/test_observability_helpers --cov-report=term-missing
</verification>

<success_criteria>
1. requirements.txt includes psutil==7.2.1
2. Helper functions exist and are well-documented
3. All unit tests pass (5 tests, 100% coverage for helpers)
4. Helper functions follow Phase 1 inline pattern (copy-paste ready)
5. Functions are deterministic (same input produces same output)
</success_criteria>

<output>
After completion, create `.planning/phases/12-data-quality-observability/12-01-SUMMARY.md`
</output>
