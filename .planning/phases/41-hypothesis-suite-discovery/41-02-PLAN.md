---
phase: 41-hypothesis-suite-discovery
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - .planning/phases/41-hypothesis-suite-discovery/41-02-SUMMARY.md
  - .planning/phases/41-hypothesis-suite-discovery/41-02-DATA_FEASIBILITY_MATRIX.md
autonomous: true

must_haves:
  truths:
    - Data availability verified for all candidate hypothesis variables (IV, DV, controls)
    - Sample size documented for each hypothesis (sufficient for 80% power)
    - Variable construction requirements specified (merges, calculations, filters)
    - Feasibility matrix scores each hypothesis on data availability
  artifacts:
    - path: .planning/phases/41-hypothesis-suite-discovery/41-02-DATA_FEASIBILITY_MATRIX.md
      provides: Complete data availability assessment for all candidate hypotheses
      contains: IV availability, DV availability, sample size, power calculation
    - path: .planning/phases/41-hypothesis-suite-discovery/41-02-SUMMARY.md
      provides: Hypothesis selection based on data feasibility and power analysis
      min_lines: 30
  key_links:
    - from: 41-02-DATA_FEASIBILITY_MATRIX.md
      to: 41-03-STATISTICAL_POWER_ANALYSIS.md
      via: Sample sizes and effect size assumptions for power calculations
      pattern: "sample.*size.*N=|power.*="
---

<objective>
Verify data availability for all candidate hypotheses from Plan 01. Create a data feasibility matrix documenting which variables (IV, DV, controls) are available in existing data sources, estimate sample sizes for each hypothesis, and confirm that sufficient data exists to conduct tests with adequate statistical power.

Purpose: A hypothesis is only worth pursuing if the data is available. This plan prevents dead ends by confirming data availability BEFORE investing in hypothesis development.

Output: (1) Data feasibility matrix scoring each hypothesis on IV/DV/control availability, (2) Sample size estimates for each hypothesis, (3) Variable construction requirements (what merges, calculations, filters are needed), (4) Recommendations on which hypotheses are feasible vs. infeasible
</objective>

<execution_context>
@C:\Users\sinas\.claude\get-shit-done/workflows/execute-plan.md
@C:\Users\sinas\.claude\get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-hypothesis-suite-discovery/41-01-SUMMARY.md
@.planning/phases/41-hypothesis-suite-discovery/41-01-EVIDENCE_MATRIX.md
@.planning/phases/41-hypothesis-suite-discovery/41-RESEARCH.md

# Available data sources
- speaker_data_2002-2018.parquet (112,968 earnings call transcripts)
- Linguistic measures from Step 2 outputs (uncertainty %, weak modal %, etc.)
- tr_ibes.parquet (355MB - analyst forecasts)
- comp_execucomp.parquet (44MB - executive compensation)
- sdc-ma-merged.parquet (25MB - M&A deals)
- CEO Dismissal Data 2021.02.03.xlsx
- CRSP DSF directory (daily returns)
- CRSPCompustat_CCM (linking table)
- Existing V2 outputs: 4_Outputs/3_Financial_V2/, 4_Outputs/4_Econometric_V2/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory Available Data Variables</name>
  <files>.planning/phases/41-hypothesis-suite-discovery/41-02-DATA_FEASIBILITY_MATRIX.md</files>
  <action>
Create a comprehensive inventory of ALL available variables in existing data sources:

1. **Text Measures (IV candidates)** - verify from Step 2 outputs:
   Check 4_Outputs/2_Textual_Analysis/2.2_Variables/latest/ for:
   - Manager_QA_Uncertainty_pct
   - CEO_QA_Uncertainty_pct
   - Manager_QA_Weak_Modal_pct
   - CEO_QA_Weak_Modal_pct
   - Manager_Pres_Uncertainty_pct
   - CEO_Pres_Uncertainty_pct
   - Analyst_QA_Uncertainty_pct
   Document: Years covered, observations, firms

2. **Financial Controls** - verify from Step 3 outputs:
   Check 4_Outputs/3_Financial_Features/latest/ for:
   - Cash holdings, Leverage, Tobin's Q, ROA, Capex intensity
   - Dividend payer dummy, Firm size, Cash flow volatility
   Document: Years covered, observations

3. **Market Measures**:
   - Stock returns (CRSP DSF)
   - Volatility, abnormal returns
   Document: Years available

4. **Event Data**:
   - M&A announcements (sdc-ma-merged.parquet)
   - CEO dismissal (CEO Dismissal Data 2021.02.03.xlsx)
   Document: Event counts, years covered

5. **Analyst Data** (tr_ibes.parquet):
   - Forecast dispersion, forecast error, NUMEST, MEANEST
   Document: Available variables, coverage period

6. **Executive Compensation** (comp_execucomp.parquet):
   - Salary, bonus, equity awards, total compensation
   Document: Coverage, merge feasibility

7. **Linking Tables**:
   - CCM (GVKEY-PERMNO links)
   Document: Link coverage for merging datasets

Create DATA_FEASIBILITY_MATRIX.md with:
- Complete variable inventory table
- Data source, file location, coverage, observations
- Merge feasibility notes
  </action>
  <verify>
File 41-02-DATA_FEASIBILITY_MATRIX.md exists with:
1. Complete variable inventory (text measures, controls, DVs)
2. Data source locations for each variable
3. Coverage periods and observation counts
4. Merge feasibility notes
  </verify>
  <done>
Data inventory documents all available variables with sources, coverage, and merge feasibility
  </done>
</task>

<task type="auto">
  <name>Task 2: Assess Hypothesis Data Feasibility</name>
  <files>.planning/phases/41-hypothesis-suite-discovery/41-02-DATA_FEASIBILITY_MATRIX.md</files>
  <action>
For each candidate hypothesis from Plan 01, assess data feasibility:

1. **IV Feasibility** (text measures):
   - Check if required text measure exists in Step 2 outputs
   - Document coverage (years, observations)

2. **DV Feasibility** (outcome variables):
   - Identify required data source
   - Check if DV variable exists or can be constructed
   - Document coverage and event counts (for event studies)

3. **Controls Feasibility**:
   - List required control variables
   - Verify each is available in existing outputs
   - Flag any missing controls

4. **Sample Size Estimation**:
   - For each hypothesis, estimate N = min(IV coverage, DV coverage)
   - Estimate unique firms based on existing regression samples
   - Calculate: average observations per firm (T = N / firms)

5. **Merge Complexity Assessment**:
   - Document required merges (e.g., earnings calls -> CCM -> Execucomp)
   - Flag high-complexity merges (many-to-many, fuzzy matching)

Create feasibility matrix table:

| Hypothesis | IV_avail | DV_avail | Controls_avail | Est_N | Est_firms | Est_T | Merge_complexity | Feasible |
|------------|----------|----------|----------------|-------|-----------|-------|------------------|----------|
| H6: Hedging -> M&A | YES (Weak Modal) | YES (SDC) | YES (8/8) | 25,000 | 2,500 | 10 | LOW | HIGH |
| H7: Vagueness -> Turnover | YES (Uncertainty) | NEEDS_CHECK | YES (6/8) | ? | ? | ? | MED | TBD |
| ...

Append to DATA_FEASIBILITY_MATRIX.md with:
- Feasibility matrix table
- Detailed notes for each hypothesis
- Flags for missing data or high complexity
- Preliminary feasible/infeasible classification
  </action>
  <verify>
File 41-02-DATA_FEASIBILITY_MATRIX.md includes:
1. Feasibility matrix with all candidate hypotheses
2. IV, DV, controls availability for each
3. Sample size estimates (N, firms, T)
4. Feasibility classification (HIGH/MED/LOW/NOT)
  </verify>
  <done>
Feasibility matrix scores each hypothesis on data availability with preliminary sample size estimates
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify High-Risk Data Sources</name>
  <files>.planning/phases/41-hypothesis-suite-discovery/41-02-DATA_FEASIBILITY_MATRIX.md</files>
  <action>
For data sources with uncertain availability, perform direct verification:

1. **IBES Analyst Data** (tr_ibes.parquet):
   - Use Python to load and inspect:
   ```python
   import pandas as pd
   ibes = pd.read_parquet("1_Inputs/tr_ibes/tr_ibes.parquet")
   print(ibes.columns.tolist())
   print(ibes['analysts'].head())  # Key column
   print(ibes['fpi'].describe())  # Forecast price indicator
   ```
   - Check for: STDEV (dispersion), MEANEST, NUMEST, actual eps
   - Document: Available variables, years, N

2. **Execucomp** (comp_execucomp.parquet):
   - Load and inspect:
   ```python
   execomp = pd.read_parquet("1_Inputs/Execucomp/comp_execucomp.parquet")
   print(execcomp.columns.tolist())
   print(execcomp[['gvkey', 'fyear', 'salary', 'bonus', 'tdc1']].head())
   ```
   - Check for: salary, bonus, equity awards, total comp (tdc1)
   - Estimate merge rate with earnings call sample

3. **CEO Dismissal** (CEO Dismissal Data 2021.02.03.xlsx):
   - Load and inspect:
   ```python
   dismissal = pd.read_excel("1_Inputs/CEO Dismissal Data 2021.02.03.xlsx")
   print(dismissal.columns.tolist())
   print(dismissal.shape)
   print(dismissal['dismissal_year'].value_counts().sort_index())
   ```
   - Count dismissal events in 2002-2018 period
   - Check for: firm identifiers for merging

4. **M&A Deals** (sdc-ma-merged.parquet):
   - Verify deal count and coverage

5. **CRSP Daily Returns**:
   - Check directory contents for coverage period

Update DATA_FEASIBILITY_MATRIX.md with:
- Verification results for each high-risk source
- Actual variable lists (not assumed)
- Event counts for event-study hypotheses
- Merge feasibility confirmed
  </action>
  <verify>
File 41-02-DATA_FEASIBILITY_MATRIX.md includes:
1. IBES verification: actual columns, coverage
2. Execucomp verification: columns, estimated merge rate
3. CEO dismissal: event count in 2002-2018
4. M&A: deal count confirmation
5. CRSP: coverage period confirmed
  </verify>
  <done>
High-risk data sources verified with actual variable lists and coverage confirmed
  </done>
</task>

</tasks>

<verification>
After completion, verify:
1. Data feasibility matrix exists with all candidate hypotheses scored
2. IV, DV, and control availability confirmed for each hypothesis
3. Sample sizes estimated (N, firms, T) for each hypothesis
4. High-risk data sources (IBES, Execucomp, CEO dismissal) verified with actual inspection
5. Feasibility classification (HIGH/MED/LOW/NOT) justified for each hypothesis
</verification>

<success_criteria>
1. Every candidate hypothesis from Plan 01 has a documented feasibility assessment
2. Data sources verified with actual file inspection (not assumptions)
3. Sample size estimates support power analysis in Plan 03
4. Infeasible hypotheses identified and documented with reasons
5. Output feeds directly into Plan 03 (power analysis) and hypothesis selection
</success_criteria>

<output>
After completion, create `.planning/phases/41-hypothesis-suite-discovery/41-02-SUMMARY.md` with:
- Data feasibility summary (which hypotheses are feasible)
- Variable inventory reference
- Sample size estimates
- Recommendations for Plan 03 (power analysis prioritization)
</output>
