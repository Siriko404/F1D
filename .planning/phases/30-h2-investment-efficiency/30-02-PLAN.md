---
phase: 30-h2-investment-efficiency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/3_Financial_V2/3.2a_AnalystDispersionPatch.py
  - 4_Outputs/3_Financial_V2/{timestamp}/H2_InvestmentEfficiency.parquet
  - 4_Outputs/3_Financial_V2/{timestamp}/stats.json
  - 3_Logs/3_Financial_V2/{timestamp}_H2_AnalystDispersion.log
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Analyst Dispersion linked from IBES via CCM CUSIP-GVKEY mapping"
    - "CCM filtering uses LINKPRIM in ['P','C'] and LINKTYPE in ['LU','LC'] for valid links"
    - "IBES filtered to NUMEST >= 2 for meaningful dispersion estimates"
    - "analyst_dispersion = STDEV / |MEANEST| where |MEANEST| >= 0.01"
    - "analyst_dispersion column present in H2 output parquet"
    - "analyst_dispersion statistics recorded in stats.json"
  artifacts:
    - path: "2_Scripts/3_Financial_V2/3.2a_AnalystDispersionPatch.py"
      provides: "CUSIP-GVKEY linking and analyst dispersion patch script"
      min_lines: 150
    - path: "4_Outputs/3_Financial_V2/{timestamp}/H2_InvestmentEfficiency.parquet"
      provides: "H2 variables dataset with analyst_dispersion"
      contains: "analyst_dispersion column"
    - path: "4_Outputs/3_Financial_V2/{timestamp}/stats.json"
      provides: "Variable distribution statistics"
      contains: "analyst_dispersion"
  key_links:
    - from: "3.2a_AnalystDispersionPatch.py"
      to: "1_Inputs/CRSPCompustat_CCM/CRSPCompustat_CCM.parquet"
      via: "CCM data loading for CUSIP-GVKEY mapping"
      pattern: "CCM.*parquet"
    - from: "3.2a_AnalystDispersionPatch.py"
      to: "1_Inputs/tr_ibes/tr_ibes.parquet"
      via: "IBES analyst forecast loading"
      pattern: "tr_ibes.*parquet"
    - from: "3.2a_AnalystDispersionPatch.py"
      to: "4_Outputs/3_Financial_V2/{timestamp}/H2_InvestmentEfficiency.parquet"
      via: "Merge analyst_dispersion into existing H2 output"
      pattern: "H2_InvestmentEfficiency.*parquet"
---

<objective>
Close the verification gap for H2-05 (Analyst Dispersion control variable) by implementing CUSIP-GVKEY linking via CCM and computing analyst_dispersion from IBES data.

Purpose: The original 30-01 plan skipped analyst dispersion because CCM linking was unavailable. CCM is now available, enabling proper CUSIP-to-GVKEY mapping to link IBES forecast data to Compustat firm identifiers.

Output: Updated `H2_InvestmentEfficiency.parquet` with 14 columns (adding analyst_dispersion), and updated `stats.json` with analyst_dispersion statistics.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-h2-investment-efficiency/30-01-VERIFICATION.md
@.planning/phases/30-h2-investment-efficiency/30-01-SUMMARY.md

# Existing H2 script (contains helper functions to reuse)
@2_Scripts/3_Financial_V2/3.2_H2Variables.py

# Input data sources
# CCM: gvkey (int64), cusip (8-char), LINKPRIM, LINKTYPE, LINKDT, LINKENDDT
# IBES: CUSIP (8-char), STATPERS (date string), NUMEST, MEANEST, STDEV
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3.2a_AnalystDispersionPatch.py Script</name>
  <files>2_Scripts/3_Financial_V2/3.2a_AnalystDispersionPatch.py</files>
  <action>
Create a standalone patch script that:

**1. Setup and Configuration:**
- Read paths from config/project.yaml (standard pattern)
- Set up dual-writer logging to 3_Logs/3_Financial_V2/{timestamp}_H2_AnalystDispersion.log
- Import standard helpers from existing pipeline

**2. Load CCM with Proper Filters:**
- Read `1_Inputs/CRSPCompustat_CCM/CRSPCompustat_CCM.parquet`
- Filter to LINKPRIM in ['P', 'C'] AND LINKTYPE in ['LU', 'LC'] for valid links
- Keep columns: gvkey, cusip (8-char)
- Handle gvkey: CCM has int64 gvkey, H2 output has string gvkey (zero-padded to 6 chars)
- Create cusip8 → gvkey_str mapping (deduplicate, keep first for ties)

**3. Load and Process IBES:**
- Read `1_Inputs/tr_ibes/tr_ibes.parquet` with columns: CUSIP, STATPERS, NUMEST, MEANEST, STDEV
- Extract year from STATPERS: `df['year'] = pd.to_datetime(df['STATPERS']).dt.year`
- Filter: NUMEST >= 2 (meaningful dispersion requires 2+ analysts)
- Filter: abs(MEANEST) >= 0.01 (avoid near-zero denominators)
- Compute: analyst_dispersion = STDEV / abs(MEANEST)

**4. Map IBES to GVKEY:**
- Use cusip8 column (IBES CUSIP is already 8-char) to merge with CCM mapping
- Aggregate by gvkey_str + year: take MEDIAN of analyst_dispersion (robust to outliers)

**5. Load Existing H2 Output and Merge:**
- Find latest H2 output: `4_Outputs/3_Financial_V2/*/H2_InvestmentEfficiency.parquet` (sort by timestamp, take newest)
- Merge analyst_dispersion on gvkey + fiscal_year
- Apply winsorization (1%, 99%) consistent with other continuous vars

**6. Update Output:**
- Save updated parquet to SAME timestamped folder (overwrite existing H2_InvestmentEfficiency.parquet)
- Load existing stats.json, add analyst_dispersion stats (mean, std, min, max, n, missing_count)
- Update stats.json with processing info and new checksum

**Key Implementation Details:**
- CCM cusip is 8-char string; IBES CUSIP is 8-char string; match directly
- CCM gvkey is int64 (e.g., 1004); H2 gvkey is string (e.g., "001004") - convert with str(gvkey).zfill(6)
- STATPERS format is "YYYY-MM-DD" string - extract year with `.str[:4].astype(int)` or via pd.to_datetime
- Include CLI --help and --dry-run options (pipeline convention)
  </action>
  <verify>
- Script exists at 2_Scripts/3_Financial_V2/3.2a_AnalystDispersionPatch.py
- `python 2_Scripts/3_Financial_V2/3.2a_AnalystDispersionPatch.py --help` shows usage
- `python 2_Scripts/3_Financial_V2/3.2a_AnalystDispersionPatch.py --dry-run` loads CCM/IBES and reports match counts without modifying files
  </verify>
  <done>
Script created with CCM loading (LINKPRIM/LINKTYPE filters), IBES processing (NUMEST >= 2, |MEANEST| >= 0.01 filters), CUSIP-GVKEY mapping, and H2 output merging logic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute Patch and Verify Output</name>
  <files>
    4_Outputs/3_Financial_V2/{timestamp}/H2_InvestmentEfficiency.parquet
    4_Outputs/3_Financial_V2/{timestamp}/stats.json
    3_Logs/3_Financial_V2/{timestamp}_H2_AnalystDispersion.log
  </files>
  <action>
**1. Run the Patch Script:**
```bash
python 2_Scripts/3_Financial_V2/3.2a_AnalystDispersionPatch.py
```

**2. Verify Output Structure:**
Check that H2_InvestmentEfficiency.parquet now has 14 columns (was 13):
- Verify analyst_dispersion column exists
- Verify gvkey, fiscal_year, and 11 other columns preserved
- Verify row count matches original (28,887 or close - some NaN allowed for analyst_dispersion)

**3. Verify stats.json Updated:**
- Check analyst_dispersion entry in "variables" section
- Check mean/std/min/max/n/missing_count populated
- Check processing info includes CCM match rate

**4. Verify Log File:**
- Log shows CCM loaded with filter counts
- Log shows IBES processing counts
- Log shows CUSIP-GVKEY match rate
- Log shows merge success counts
  </action>
  <verify>
```python
import pandas as pd
import json
from pathlib import Path

# Find latest output
out_dirs = sorted(Path('4_Outputs/3_Financial_V2').glob('2026-*'))
latest = out_dirs[-1]

# Check parquet
df = pd.read_parquet(latest / 'H2_InvestmentEfficiency.parquet')
assert 'analyst_dispersion' in df.columns, "analyst_dispersion column missing"
assert len(df.columns) == 14, f"Expected 14 columns, got {len(df.columns)}"
print(f"Columns: {list(df.columns)}")
print(f"analyst_dispersion: n={df['analyst_dispersion'].notna().sum()}, missing={df['analyst_dispersion'].isna().sum()}")

# Check stats.json
with open(latest / 'stats.json') as f:
    stats = json.load(f)
assert 'analyst_dispersion' in stats['variables'], "analyst_dispersion not in stats.json"
print(f"analyst_dispersion stats: {stats['variables']['analyst_dispersion']}")

print("VERIFICATION PASSED")
```
  </verify>
  <done>
H2_InvestmentEfficiency.parquet has 14 columns including analyst_dispersion. stats.json includes analyst_dispersion statistics. H2-05 requirement is NOW SATISFIED.
  </done>
</task>

</tasks>

<verification>
1. **Column Check:** `H2_InvestmentEfficiency.parquet` has exactly 14 columns (added analyst_dispersion)
2. **Coverage Check:** analyst_dispersion has > 0 non-null values (expect 5,000-15,000 matches based on CCM/IBES overlap)
3. **Stats Check:** stats.json "variables" section includes analyst_dispersion with mean/std/n populated
4. **Log Check:** 3_Logs/3_Financial_V2/{timestamp}_H2_AnalystDispersion.log shows CCM filter counts, IBES filter counts, and merge success
5. **H2-05 Requirement:** Analyst Dispersion control variable NOW AVAILABLE for H2 regression (Phase 34)
</verification>

<success_criteria>
- [ ] 3.2a_AnalystDispersionPatch.py created and runnable
- [ ] CCM loading uses LINKPRIM in ['P','C'] AND LINKTYPE in ['LU','LC'] filters
- [ ] IBES processing applies NUMEST >= 2 AND |MEANEST| >= 0.01 filters
- [ ] analyst_dispersion = STDEV / |MEANEST| computed correctly
- [ ] CUSIP-GVKEY mapping via CCM cusip8 → gvkey_str
- [ ] analyst_dispersion column present in H2 output (14 total columns)
- [ ] stats.json updated with analyst_dispersion statistics
- [ ] Log file documents match rates and processing counts
- [ ] H2-05 requirement now SATISFIED
</success_criteria>

<output>
After completion, create `.planning/phases/30-h2-investment-efficiency/30-02-SUMMARY.md`
</output>
