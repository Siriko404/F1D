---
phase: 39-leverage-speech-discipline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/4_Econometric_V2/4.4_H4_LeverageDiscipline.py (create)
  - 4_Outputs/4_Econometric_V2/4.4_H4_LeverageDiscipline/{timestamp}/H4_Analysis_Dataset.parquet
  - 4_Outputs/4_Econometric_V2/4.4_H4_LeverageDiscipline/{timestamp}/stats.json
autonomous: true

must_haves:
  truths:
    - H4 analysis dataset exists with all required variables for 6 regressions
    - Lagged leverage (t-1) properly computed within firm boundaries
    - All 6 uncertainty DVs, analyst uncertainty controls, and presentation controls available
    - Financial controls (firm_size, tobins_q, roa, cash_holdings, dividend_payer, firm_maturity, earnings_volatility) merged
    - VIF diagnostics run on base specification, all VIF < 5
  artifacts:
    - path: 2_Scripts/4_Econometric_V2/4.4_H4_LeverageDiscipline.py
      provides: Complete H4 data preparation and regression script
      min_lines: 400
    - path: 4_Outputs/4_Econometric_V2/4.4_H4_LeverageDiscipline/{timestamp}/H4_Analysis_Dataset.parquet
      provides: Analysis dataset with all variables for 6 regressions
    - path: 4_Outputs/4_Econometric_V2/4.4_H4_LeverageDiscipline/{timestamp}/stats.json
      provides: Data merging stats, variable availability, VIF diagnostics
  key_links:
    - from: H1_CashHoldings.parquet
      to: H4 analysis dataset
      via: leverage variable and financial controls merge
    - from: linguistic_variables_*.parquet
      to: H4 analysis dataset
      via: 6 uncertainty measures + analyst uncertainty merge by (gvkey, fiscal_year)
    - from: analysis dataset
      to: VIF diagnostics
      via: check_multicollinearity() from shared.diagnostics
---

<objective>
Create complete H4 analysis dataset by merging existing data sources and preparing variables for the leverage discipline hypothesis regressions.

Purpose: Test the reverse causal direction—whether higher leverage disciplines managers and reduces speech uncertainty. This requires creating a clean analysis dataset with lagged leverage and all required controls.

Output: 
- Analysis dataset with 6 uncertainty DVs, lagged leverage (t-1), analyst uncertainty controls, speaker presentation controls, and financial controls
- VIF diagnostics confirming no multicollinearity concerns
- Execution stats documenting merge success and variable coverage
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/39-leverage-speech-discipline/39-CONTEXT.md
@.planning/phases/39-leverage-speech-discipline/39-RESEARCH.md
@.planning/phases/32-econometric-infrastructure/32-01-SUMMARY.md

# Key files from prior phases
@2_Scripts/shared/panel_ols.py
@2_Scripts/shared/diagnostics.py
@2_Scripts/4_Econometric_V2/4.1_H1CashHoldingsRegression.py (reference pattern)
@2_Scripts/4_Econometric_V2/4.3_H3PayoutPolicyRegression.py (reference pattern for control variables)

# Variable sources
- H1_CashHoldings.parquet: leverage, firm_size, tobins_q, roa, cash_holdings, dividend_payer
- H3_PayoutPolicy.parquet: firm_maturity, earnings_volatility
- linguistic_variables_*.parquet: 6 uncertainty measures + analyst_qa_uncertainty
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create H4 data preparation and regression script</name>
  <files>2_Scripts/4_Econometric_V2/4.4_H4_LeverageDiscipline.py</files>
  <action>
Create comprehensive H4 regression script following existing 4.X pattern with these key sections:

**Contract Header (lines 1-45):**
- ID: 4.4_H4_LeverageDiscipline
- Description: Panel OLS for H4 (Leverage → Speech Uncertainty)
- Model: Uncertainty_t = β₀ + β₁·Leverage_{t-1} + β₂·Analyst_Uncertainty_t + β₃·Presentation_Uncertainty_t + γ·Controls_t + Firm_FE + Year_FE + Industry_FE + ε
- Hypothesis: H4: β₁ < 0 (one-tailed)
- Deterministic: true

**Configuration Section:**
- UNCERTAINTY_MEASURES: 6 DVs from H1-H3 pattern
- CONTROL_VARS: analyst_qa_uncertainty + financial controls from H3
- PRESENTATION_CONTROL_MAP: Dictionary mapping each DV to its presentation control
  - Manager_QA_* → Manager_Pres_Uncertainty_pct
  - CEO_QA_* → CEO_Pres_Uncertainty_pct
  - *_Pres_* → None (no control for presentation DVs)

**Data Loading Functions:**
1. `load_h1_variables(h1_dir)` - Load leverage and base financial controls
2. `load_h3_variables(h3_dir)` - Load firm_maturity, earnings_volatility
3. `load_speech_uncertainty(speech_dir)` - Load all 6 uncertainty measures + analyst uncertainty
4. Merge all on (gvkey, fiscal_year) using outer join, then dropna for complete cases

**Variable Preparation:**
- `create_lagged_leverage(df)` - Group by gvkey, shift leverage by 1 year
- Validate: No cross-entity leakage, warn if gaps > 1 year
- Drop first year for each firm (no lag available)

**Control Variable Assembly:**
- Base: ['firm_size', 'tobins_q', 'roa', 'cash_holdings', 'dividend_payer', 'firm_maturity', 'earnings_volatility']
- Always: ['analyst_qa_uncertainty', 'leverage_lag1']
- Conditional: Add presentation control based on DV type (skip for presentation DVs)

**VIF Diagnostics:**
- Run check_multicollinearity() on base specification (all continuous vars)
- Threshold: 5.0
- Log all VIF values, flag any exceeding threshold

**Output Functions:**
- `save_analysis_dataset(df, output_path)` - Save as parquet with stats
- `save_vif_results(vif_df, output_path)` - Save VIF diagnostics

Follow exact patterns from 4.1_H1CashHoldingsRegression.py for:
- Path resolution using get_latest_output_dir()
- DualWriter setup for progress logging
- stats.json structure with execution metadata
- Error handling and validation
  </action>
  <verify>
    python -c "import sys; sys.path.insert(0, '2_Scripts'); from pathlib import Path; exec(Path('2_Scripts/4_Econometric_V2/4.4_H4_LeverageDiscipline.py').read_text().split('if __name__')[0]); print('Import OK')"
  </verify>
  <done>
    Script created with:
    - Contract header with H4 model specification
    - Data loading functions for H1, H3, and speech uncertainty sources
    - Lagged leverage creation with proper groupby shift
    - VIF diagnostics integration
    - All 6 uncertainty DVs and control variable mappings defined
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute data preparation and validation</name>
  <files>
    - 2_Scripts/4_Econometric_V2/4.4_H4_LeverageDiscipline.py
    - 4_Outputs/4_Econometric_V2/4.4_H4_LeverageDiscipline/{timestamp}/H4_Analysis_Dataset.parquet
    - 4_Outputs/4_Econometric_V2/4.4_H4_LeverageDiscipline/{timestamp}/stats.json
  </files>
  <action>
Execute the data preparation portion of the H4 script:

1. **Resolve input paths:**
   - H1_CashHoldings.parquet (leverage + base controls)
   - H3_PayoutPolicy.parquet (firm_maturity, earnings_volatility)
   - linguistic_variables_*.parquet (6 uncertainty measures, analyst uncertainty)

2. **Execute data merge:**
   - Load all datasets
   - Merge on (gvkey, fiscal_year)
   - Log merge statistics (rows before/after, match rate)

3. **Create lagged leverage:**
   - Group by gvkey, shift leverage by 1
   - Validate no cross-firm leakage
   - Drop observations where lag is missing (first firm-year)

4. **Run VIF diagnostics:**
   - Continuous vars: leverage_lag1, analyst_qa_uncertainty, firm_size, tobins_q, roa, cash_holdings, firm_maturity, earnings_volatility
   - Check VIF < 5.0 for all
   - If any exceed threshold, log warning but continue (script handles via check)

5. **Validate variable availability:**
   - Confirm all 6 uncertainty DVs present and non-missing
   - Confirm analyst_qa_uncertainty available
   - Confirm presentation controls available for QA DVs
   - Log availability stats per variable

6. **Save outputs:**
   - H4_Analysis_Dataset.parquet with all prepared variables
   - stats.json with merge stats, VIF results, variable availability, N, git SHA
   - H4_DATA_SUMMARY.md (auto-generated by script)

7. **Update latest/ symlink:**
   - Create symlink pointing to timestamped output directory
  </action>
  <verify>
    python 2_Scripts/4_Econometric_V2/4.4_H4_LeverageDiscipline.py --prepare-only
  </verify>
  <done>
    - H4_Analysis_Dataset.parquet created with N > 0 observations
    - stats.json contains merge_stats, vif_diagnostics, variable_availability
    - All VIF values < 5.0 (or flagged if any exceed)
    - latest/ symlink points to new timestamp directory
    - Log file shows successful data preparation with no errors
  </done>
</task>

</tasks>

<verification>
- [ ] Script 4.4_H4_LeverageDiscipline.py exists with contract header and proper structure
- [ ] H4_Analysis_Dataset.parquet contains: gvkey, fiscal_year, 6 uncertainty DVs, leverage_lag1, analyst_qa_uncertainty, all financial controls
- [ ] Lagged leverage created with proper within-firm shift (no cross-entity leakage)
- [ ] VIF diagnostics saved, all values < 5.0
- [ ] stats.json documents N observations, merge success rate, variable coverage
- [ ] latest/ symlink created pointing to output directory
</verification>

<success_criteria>
1. Analysis dataset ready for 6 H4 regressions with all required variables
2. VIF diagnostics confirm no multicollinearity concerns
3. Variable availability documented - all DVs and controls present
4. Data preparation reproducible via script execution
</success_criteria>

<output>
After completion, create `.planning/phases/39-leverage-speech-discipline/39-01-SUMMARY.md`
</output>
