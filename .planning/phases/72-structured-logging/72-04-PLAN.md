---
phase: 72-structured-logging
plan: 04
type: execute
wave: 1
depends_on: [72-03]
files_modified:
  - 2_Scripts/3_Financial_V2/3.1_H1Variables.py
  - 2_Scripts/3_Financial_V2/3.2_H2Variables.py
  - 2_Scripts/3_Financial_V2/3.3_H3Variables.py
  - 2_Scripts/3_Financial_V2/3.5_H5Variables.py
  - 2_Scripts/3_Financial_V2/3.6_H6Variables.py
  - 2_Scripts/3_Financial_V2/3.7_H7IlliquidityVariables.py
  - 2_Scripts/3_Financial_V2/3.8_H8TakeoverVariables.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All Financial V2 scripts use structlog for logging"
    - "Financial V2 scripts call configure_script_logging() at startup"
    - "Log entries include operation_id and script_name context"
  artifacts:
    - path: "2_Scripts/3_Financial_V2/3.1_H1Variables.py"
      provides: "H1 variables with structured logging"
      contains: "from f1d.shared.logging import"
    - path: "2_Scripts/3_Financial_V2/3.2_H2Variables.py"
      provides: "H2 variables with structured logging"
      contains: "from f1d.shared.logging import"
    - path: "2_Scripts/3_Financial_V2/3.3_H3Variables.py"
      provides: "H3 variables with structured logging"
      contains: "from f1d.shared.logging import"
  key_links:
    - from: "3.*_H*Variables.py"
      to: "f1d.shared.logging"
      via: "import"
      pattern: "from f1d\\.shared\\.logging import"
---

<objective>
Migrate all 7 Financial V2 hypothesis scripts from legacy logging to structured logging with structlog.

Purpose: These scripts are the core hypothesis testing scripts that must use the new structured logging infrastructure to achieve the Phase 72 goal "All logging is structured, JSON-formatted, with request/operation context binding."

Output: 7 migrated scripts using f1d.shared.logging with configure_script_logging() at startup and OperationContext wrapping for main operations.
</objective>

<execution_context>
@C:/Users/sinas/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sinas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Reference existing logging infrastructure
@src/f1d/shared/logging/__init__.py
@src/f1d/shared/logging/handlers.py

# Existing SUMMARYs showing what was built
@.planning/phases/72-structured-logging/72-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate H1, H2, H3 hypothesis scripts (3.1, 3.2, 3.3)</name>
  <files>
    2_Scripts/3_Financial_V2/3.1_H1Variables.py
    2_Scripts/3_Financial_V2/3.2_H2Variables.py
    2_Scripts/3_Financial_V2/3.3_H3Variables.py
  </files>
  <action>
    For each of the 3 scripts (3.1_H1Variables.py, 3.2_H2Variables.py, 3.3_H3Variables.py), perform these changes:

    1. **Replace import statement:**
       - Remove: `import logging`
       - Add: `from f1d.shared.logging import get_logger, configure_script_logging, OperationContext`

    2. **Replace logger initialization:**
       - Remove: `logger = logging.getLogger(__name__)`
       - Add: `logger = get_logger(__name__)` (move this AFTER configure_script_logging call)

    3. **Add logging configuration at start of main():**
       At the beginning of the `main()` function, add:
       ```python
       # Configure structured logging
       script_id = Path(__file__).stem  # e.g., "3.1_H1Variables"
       configure_script_logging(script_name=script_id, log_level="INFO")
       ```

    4. **Wrap main operation with OperationContext:**
       After configure_script_logging, wrap the main execution:
       ```python
       with OperationContext(
           operation_name="financial_variable_construction",
           script_name=script_id,
       ):
           # existing main code here (excluding return statement)
       ```

    5. **Ensure logger is initialized after configure_script_logging:**
       The `logger = get_logger(__name__)` line must come after `configure_script_logging()` call.
       Move it from module-level to inside main() after the configure call.

    DO NOT:
    - Remove the DualWriter import from observability_utils (still used for stats)
    - Change any business logic or variable calculations
    - Modify the observability_utils imports (save_stats, print_stat, etc.)
  </action>
  <verify>
    grep -l "from f1d.shared.logging import" 2_Scripts/3_Financial_V2/3.{1,2,3}_H*Variables.py | wc -l
    # Should return 3

    grep -c "configure_script_logging" 2_Scripts/3_Financial_V2/3.{1,2,3}_H*Variables.py
    # Each file should show count >= 1

    grep -c "import logging$" 2_Scripts/3_Financial_V2/3.{1,2,3}_H*Variables.py
    # Should return 0 (no standalone "import logging")
  </verify>
  <done>
    All 3 scripts (3.1, 3.2, 3.3) import from f1d.shared.logging and call configure_script_logging() at startup.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate H5, H6 hypothesis scripts (3.5, 3.6)</name>
  <files>
    2_Scripts/3_Financial_V2/3.5_H5Variables.py
    2_Scripts/3_Financial_V2/3.6_H6Variables.py
  </files>
  <action>
    For each of the 2 scripts (3.5_H5Variables.py, 3.6_H6Variables.py), perform the same migration pattern as Task 1:

    1. Replace `import logging` with `from f1d.shared.logging import get_logger, configure_script_logging, OperationContext`
    2. Move `logger = get_logger(__name__)` inside main() after configure_script_logging
    3. Add `configure_script_logging(script_name=Path(__file__).stem)` at start of main()
    4. Wrap main execution with `OperationContext(operation_name="financial_variable_construction", script_name=Path(__file__).stem)`

    These scripts follow the same pattern as 3.1, 3.2, 3.3 - apply the same changes.
  </action>
  <verify>
    grep -l "from f1d.shared.logging import" 2_Scripts/3_Financial_V2/3.{5,6}_H*Variables.py | wc -l
    # Should return 2

    grep -c "configure_script_logging" 2_Scripts/3_Financial_V2/3.{5,6}_H*Variables.py
    # Each file should show count >= 1
  </verify>
  <done>
    Both H5 and H6 scripts import from f1d.shared.logging and call configure_script_logging() at startup.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate H7, H8 hypothesis scripts (3.7, 3.8)</name>
  <files>
    2_Scripts/3_Financial_V2/3.7_H7IlliquidityVariables.py
    2_Scripts/3_Financial_V2/3.8_H8TakeoverVariables.py
  </files>
  <action>
    For each of the 2 scripts (3.7_H7IlliquidityVariables.py, 3.8_H8TakeoverVariables.py), perform the same migration pattern:

    1. Replace `import logging` with `from f1d.shared.logging import get_logger, configure_script_logging, OperationContext`
    2. Move `logger = get_logger(__name__)` inside main() after configure_script_logging
    3. Add `configure_script_logging(script_name=Path(__file__).stem)` at start of main()
    4. Wrap main execution with `OperationContext(operation_name="financial_variable_construction", script_name=Path(__file__).stem)`

    These scripts follow the same pattern - apply the same changes.
  </action>
  <verify>
    grep -l "from f1d.shared.logging import" 2_Scripts/3_Financial_V2/3.{7,8}_H*Variables.py | wc -l
    # Should return 2

    # Verify NO scripts in Financial_V2 still use legacy logging
    grep -l "^import logging$" 2_Scripts/3_Financial_V2/3.*.py | wc -l
    # Should return 0
  </verify>
  <done>
    All 7 Financial V2 scripts (3.1, 3.2, 3.3, 3.5, 3.6, 3.7, 3.8) migrated to structured logging. No scripts remain using legacy `import logging`.
  </done>
</task>

</tasks>

<verification>
1. Run grep to verify all 7 scripts import from f1d.shared.logging:
   ```bash
   grep -l "from f1d.shared.logging import" 2_Scripts/3_Financial_V2/3.*_H*Variables.py | wc -l
   ```
   Expected: 7

2. Run grep to verify no scripts still use standalone `import logging`:
   ```bash
   grep -l "^import logging$" 2_Scripts/3_Financial_V2/3.*.py
   ```
   Expected: empty output

3. Verify configure_script_logging is called in all scripts:
   ```bash
   grep -l "configure_script_logging" 2_Scripts/3_Financial_V2/3.*_H*Variables.py | wc -l
   ```
   Expected: 7

4. Test one script to verify logging works:
   ```bash
   python 2_Scripts/3_Financial_V2/3.1_H1Variables.py --dry-run
   ```
   Should produce colored console output with operation_id context.
</verification>

<success_criteria>
- [ ] All 7 Financial V2 scripts import from f1d.shared.logging
- [ ] All scripts call configure_script_logging() at startup
- [ ] All scripts use get_logger() instead of logging.getLogger()
- [ ] No scripts in 2_Scripts/3_Financial_V2/ use standalone `import logging`
- [ ] At least one script tested with --dry-run produces structured log output
</success_criteria>

<output>
After completion, create `.planning/phases/72-structured-logging/72-04-SUMMARY.md`
</output>
