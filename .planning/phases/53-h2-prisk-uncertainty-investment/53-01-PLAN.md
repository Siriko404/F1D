---
phase: 53-h2-prisk-uncertainty-investment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - 2_Scripts/3_Financial_V3/4.1_H2_BiddleInvestmentResidual.py
  - 4_Outputs/3_Financial_V3/4.1_H2_BiddleInvestmentResidual/
  - 3_Logs/3_Financial_V3/4.1_H2_BiddleInvestmentResidual/
autonomous: true

must_haves:
  truths:
    - "Correct Biddle (2009) investment residual constructed (NOT Phase 30's roa_residual)"
    - "First-stage regression Investment ~ Tobin's_Q + Sales_Growth run by FF48-year"
    - "Investment variable constructed as (CapEx + R&D + Acquisitions - AssetSales) / lagged(Assets)"
    - "Output InvestmentResidual available at firm-year level for merge with PRisk"
    - "stats.json documents sample size, residuals distribution, first-stage R2 by industry-year"
  artifacts:
    - path: "2_Scripts/3_Financial_V3/4.1_H2_BiddleInvestmentResidual.py"
      provides: "Correct Biddle (2009) investment residual construction"
      min_lines: 250
      contains: "def construct_investment", "def run_first_stage_regressions", "ff48"
    - path: "4_Outputs/3_Financial_V3/4.1_H2_BiddleInvestmentResidual/{timestamp}/H2_InvestmentResiduals.parquet"
      provides: "Firm-year level investment residuals for regression"
      contains: "gvkey", "year", "InvestmentResidual", "Investment", "TobinQ_lag", "SalesGrowth_lag"
    - path: "4_Outputs/3_Financial_V3/4.1_H2_BiddleInvestmentResidual/{timestamp}/stats.json"
      provides: "Summary statistics and regression metadata"
      contains: "n_firm_years", "mean_residual", "first_stage_r2"
  key_links:
    - from: "2_Scripts/3_Financial_V3/4.1_H2_BiddleInvestmentResidual.py"
      to: "1_Inputs/comp_na_daily_all/comp_na_daily_all.parquet"
      via: "load_compustat_investment() reads CapEx, R&D, Sales, Assets fields"
      pattern: "capx|xrd|aqcy|sppey|at|sale"
    - from: "2_Scripts/3_Financial_V3/4.1_H2_BiddleInvestmentResidual.py"
      to: "4_Outputs/1.4_AssembleManifest/latest/master_sample_manifest.parquet"
      via: "load_manifest() for sample universe filtering"
      pattern: "master_sample_manifest"
    - from: "4.1_H2_BiddleInvestmentResidual.py"
      to: "4.2_H2_PRiskUncertaintyMerge.py (Plan 53-02)"
      via: "InvestmentResiduals.parquet output is input for PRisk merge"
      pattern: "InvestmentResiduals.parquet"
---

<objective>
**Objective:** Construct the correct Biddle (2009) investment residual (InvestmentResidual) as the dependent variable for H2 regression.

**Purpose:** Phase 30's `roa_residual` is INCORRECT for testing investment efficiency (it regresses Delta ROA on CapEx, which tests investment -> profitability). Phase 53 must implement the correct Biddle (2009) specification: regress Investment on Tobin's Q and Sales Growth by industry-year, where the residual represents deviation from expected investment given growth opportunities.

**Output:**
- `4_Outputs/3_Financial_V3/4.1_H2_BiddleInvestmentResidual/{timestamp}/H2_InvestmentResiduals.parquet`: Firm-year level dataset with InvestmentResidual (DV), Investment (raw), TobinQ_lag, SalesGrowth_lag, and control variables
- `4_Outputs/3_Financial_V3/4.1_H2_BiddleInvestmentResidual/{timestamp}/stats.json`: Sample statistics, residual distributions, first-stage regression summary
- `3_Logs/3_Financial_V3/4.1_H2_BiddleInvestmentResidual/{timestamp}_Biddle.log`: Execution log with dual-writer output

**Critical Difference from Phase 30:**
- Phase 30: `Delta_ROA_t+2 ~ CapEx_t/AT_t` (tests if investment leads to profitability)
- Phase 53: `Investment_t ~ Tobin's_Q_t-1 + Sales_Growth_t-1` by FF48-year (residual = investment inefficiency)
</objective>

<execution_context>
@C:\Users\sinas\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\sinas\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-h2-prisk-uncertainty-investment/53-CONTEXT.md
@.planning/phases/53-h2-prisk-uncertainty-investment/53-RESEARCH.md
@2_Scripts/3_Financial_V2/3.2_H2Variables.py (for Compustat loading patterns - DO NOT reuse roa_residual logic)
@2_Scripts/shared/industry_utils.py (FF48 classification)
@2_Scripts/shared/panel_ols.py (for first-stage OLS regressions)
</context>

<tasks>

<task type="auto">
  <name>Create V3 folder structure and script header</name>
  <files>2_Scripts/3_Financial_V3/4.1_H2_BiddleInvestmentResidual.py</files>
  <action>
Create folder structure:
```
2_Scripts/3_Financial_V3/
4_Outputs/3_Financial_V3/
3_Logs/3_Financial_V3/
```

Create script `4.1_H2_BiddleInvestmentResidual.py` with:
- Contract header: id, description, declared inputs/outputs, deterministic: true
- Imports: sys, os, argparse, Path, datetime, pandas, numpy, yaml, json, time, psutil, warnings
- Shared utilities imports: path_utils, observability_utils (DualWriter), industry_utils (parse_ff_industries)
- Statsmodels import for first-stage OLS (statsmodels.api.OLS or similar)
- CLI support with --help and --dry-run flags

Rationale: V3 folder separates external risk (PRisk) interaction hypotheses from V2 (linguistic uncertainty main effects). Script follows existing V2 patterns for consistency.
  </action>
  <verify>
    - Directory `2_Scripts/3_Financial_V3/` exists
    - Script file exists with contract header
    - `python 2_Scripts/3_Financial_V3/4.1_H2_BiddleInvestmentResidual.py --help` returns usage
  </verify>
  <done>V3 folder structure created, script template with header exists, CLI functional</done>
</task>

<task type="auto">
  <name>Load Compustat and construct Investment variable</name>
  <files>2_Scripts/3_Financial_V3/4.1_H2_BiddleInvestmentResidual.py</files>
  <action>
Implement data loading and Investment construction:

**load_compustat_investment():**
- Read from `1_Inputs/comp_na_daily_all/comp_na_daily_all.parquet`
- Required fields (check availability, handle missing):
  - Investment components: CAPXY (CapEx), XRDY (R&D), AQCY (Acquisitions - optional), SPPEY (Asset Sales - optional)
  - Assets: AT (Total Assets, lagged for denominator)
  - Tobin's Q components: AT, MKVALT, CEQ (Market Equity)
  - Sales: SALE (for Sales Growth)
  - Identifiers: gvkey, datadate, fyear (fiscal year)
- Filter to sample firms from manifest (merge on gvkey-year)
- Exclude financials (SIC 6000-6999) and utilities (SIC 4900-4999)
- Require positive AT

**construct_investment():**
```
Investment = (CapEx + R&D + Acquisitions - AssetSales) / lag(AT)
```
- If AQCY or SPPEY missing, use simplified: Investment = (CapEx + R&D) / lag(AT)
- Winsorize Investment at 1% and 99% by year (document in log)

**construct_tobins_q():**
```
Tobin's Q = (AT + MKVALT - CEQ) / AT
```
- Create lagged version: TobinQ_lag = lag(Tobin's Q) by gvkey

**construct_sales_growth():**
```
SalesGrowth = (SALE_t - SALE_t-1) / |SALE_t-1|
```
- Winsorize at 1% and 99% by year

**assign_ff48():**
- Load FF48 from `1_Inputs/Siccodes48.zip` via `parse_ff_industries()`
- Map SIC to FF48 industry code
- Verify minimum 20 observations per FF48-year cell (document thin cells in log)
  </action>
  <verify>
    - load_compustat_investment() loads data successfully
    - Investment variable constructed with N > 20,000 observations
    - TobinQ_lag and SalesGrowth_lag created with proper lags
    - FF48 codes assigned, thin cells logged if any exist
  </verify>
  <done>Compustat data loaded, Investment/TobinQ/SalesGrowth constructed, FF48 assigned, >20K obs</done>
</task>

<task type="auto">
  <name>Run first-stage regressions by FF48-year and extract residuals</name>
  <files>2_Scripts/3_Financial_V3/4.1_H2_BiddleInvestmentResidual.py</files>
  <action>
Implement correct Biddle (2009) first-stage:

**run_first_stage_regressions():**
- Group by FF48 industry x fiscal_year
- For each cell with >= 20 observations:
  ```
  Investment = beta0 + beta1 * TobinQ_lag + beta2 * SalesGrowth_lag + epsilon
  ```
- Use statsmodels OLS (sm.OLS with constant)
- Extract residuals: epsilon = InvestmentResidual
  - Positive (> 0): Overinvestment (investing more than expected)
  - Negative (< 0): Underinvestment (investing less than expected)
- Track first-stage statistics: R2, N, F-stat per cell

**validate_residuals():**
- Report residual distribution: mean, std, min, max, skewness
- Verify mean ~ 0 (residuals from OLS have zero mean by construction)
- Count over/under investment years (positive/negative residuals)

**Output format:**
- Columns: gvkey, year, ff48_code, Investment, InvestmentResidual, TobinQ_lag, SalesGrowth_lag
- One row per firm-year with valid first-stage residual

Fall back to FF12 if FF48 cells too thin (< 5 observations):
- Use `parse_ff_industries(Siccodes12.zip, 12)` for FF12 mapping
- Document fallback in log

CRITICAL: DO NOT reuse Phase 30's roa_residual logic. Phase 30 regresses Delta ROA on CapEx (tests investment profitability). Phase 53 regresses Investment on Tobin's Q + Sales Growth (tests investment efficiency).
  </action>
  <verify>
    - First-stage regressions run for FF48-year cells
    - InvestmentResidual has mean ~ 0
    - stats.json contains first_stage_r2 by industry-year summary
    - Output parquet has gvkey, year, InvestmentResidual columns
  </verify>
  <done>First-stage regressions complete, InvestmentResidual extracted, stats documented</done>
</task>

<task type="auto">
  <name>Implement Biddle controls, output generation, and CLI execution</name>
  <files>2_Scripts/3_Financial_V3/4.1_H2_BiddleInvestmentResidual.py</files>
  <action>
Implement control variables and output:

**construct_biddle_controls():**
- CashFlow = OANCFY / AT (Operating Cash Flow / Assets)
- Size = log(AT) (Firm Size)
- Leverage = (DLTT + DLC) / (DLTT + DLC + MKVALT) (Market Leverage)
- SalesGrowth (already computed)
- All winsorized at 1% and 99% by year

**main() function:**
- Load config, setup paths, initialize DualWriter
- Load manifest, load Compustat, merge on gvkey-year
- Construct all variables
- Run first-stage regressions, extract residuals
- Output to timestamped directory: `H2_InvestmentResiduals.parquet`
- Save stats.json with:
  - n_firm_years, n_firms, n_industries, year_range
  - residual_stats: mean, std, min, max, pct_overinvest, pct_underinvest
  - first_stage_summary: mean_r2, median_r2, n_cells, n_thin_cells
- Update `latest/` symlink

**CLI:**
- `--dry-run`: Validate inputs, print summary, exit
- `--output-dir`: Override output directory
- Standard dual-writer logging to console and log file

**Output structure:**
```
4_Outputs/3_Financial_V3/4.1_H2_BiddleInvestmentResidual/
  {timestamp}/
    H2_InvestmentResiduals.parquet
    stats.json
  latest/ -> {timestamp}/
```
  </action>
  <verify>
    - `python 4.1_H2_BiddleInvestmentResidual.py --dry-run` completes without error
    - Full execution creates H2_InvestmentResiduals.parquet with expected columns
    - stats.json contains all required statistics
    - Log file exists with dual-writer output
  </verify>
  <done>Script executable, outputs generated, stats.json complete, log created</done>
</task>

</tasks>

<verification>
## Overall Phase Verification

After completing Plan 53-01, verify:

1. **Correct Biddle Implementation:**
   - [ ] Investment variable constructed per Biddle (2009): (CapEx + R&D +/- Acq/AssetSales) / lag(AT)
   - [ ] First-stage regression: Investment ~ Tobin's_Q_lag + Sales_Growth_lag by FF48-year
   - [ ] NOT Phase 30's specification (Delta ROA ~ CapEx)

2. **Data Quality:**
   - [ ] N >= 20,000 firm-year observations (expected ~30,000)
   - [ ] InvestmentResidual mean ~ 0 (OLS property)
   - [ ] FF48-year cells have min 20 obs (thin cells logged)

3. **Outputs:**
   - [ ] H2_InvestmentResiduals.parquet has gvkey, year, InvestmentResidual, controls
   - [ ] stats.json documents sample, residuals, first-stage R2
   - [ ] Log file captures full execution trace

4. **Reproducibility:**
   - [ ] Script runs with `python 4.1_H2_BiddleInvestmentResidual.py`
   - [ ] --dry-run flag validates inputs without execution
   - [ ] Timestamp-based output resolution with latest/ symlink
</verification>

<success_criteria>
**Plan 53-01 Complete When:**
1. Script `4.1_H2_BiddleInvestmentResidual.py` exists and executes without error
2. Output `H2_InvestmentResiduals.parquet` contains InvestmentResidual DV at firm-year level
3. stats.json confirms N >= 20,000, first-stage R2 documented, residual distribution valid
4. Log file exists with dual-writer output capturing all operations
5. InvestmentResidual ready for merge with PRisk (Plan 53-02)
</success_criteria>

<output>
After completion, create `.planning/phases/53-h2-prisk-uncertainty-investment/53-01-SUMMARY.md` with:
- Actual observations constructed
- First-stage R2 statistics (mean, median, min, max)
- Residual distribution summary
- Any issues with thin FF48-year cells and fallbacks applied
- Execution time and memory usage
</output>
