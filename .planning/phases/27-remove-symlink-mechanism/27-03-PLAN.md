---
phase: 27-remove-symlink-mechanism
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - 2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py
  - 2_Scripts/3_Financial/3.1_FirmControls.py
  - 2_Scripts/3_Financial/3.2_MarketVariables.py
  - 2_Scripts/3_Financial/3.3_EventFlags.py
  - 2_Scripts/4_Econometric/4.1_EstimateCeoClarity.py
  - 2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py
  - 2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py
autonomous: true

must_haves:
  truths:
    - "Step 3 scripts (3.0-3.3) use get_latest_output_dir() for reading prerequisite outputs"
    - "Step 4 scripts (4.1, 4.1.1, 4.1.2) use get_latest_output_dir() for reading prerequisite outputs"
    - "No hardcoded /latest/ paths remain in reader scripts for Steps 3-4.1.2"
  artifacts:
    - path: "2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py"
      contains: "get_latest_output_dir"
    - path: "2_Scripts/3_Financial/3.1_FirmControls.py"
      contains: "get_latest_output_dir"
    - path: "2_Scripts/3_Financial/3.2_MarketVariables.py"
      contains: "get_latest_output_dir"
    - path: "2_Scripts/3_Financial/3.3_EventFlags.py"
      contains: "get_latest_output_dir"
    - path: "2_Scripts/4_Econometric/4.1_EstimateCeoClarity.py"
      contains: "get_latest_output_dir"
    - path: "2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py"
      contains: "get_latest_output_dir"
    - path: "2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py"
      contains: "get_latest_output_dir"
  key_links:
    - from: "Step 3-4 scripts"
      to: "shared.path_utils.get_latest_output_dir"
      via: "import and function call"
      pattern: "from shared.path_utils import get_latest_output_dir"
---

<objective>
Update Step 3 and Step 4.1.x reader scripts to use timestamp-based path resolution.

Purpose: Replace hardcoded /latest/ paths with get_latest_output_dir() calls in setup_paths() functions for Step 3 (3.0-3.3) and first batch of Step 4 scripts (4.1, 4.1.1, 4.1.2).
Output: 7 scripts updated to use dynamic path resolution for prerequisite inputs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-remove-symlink-mechanism/27-RESEARCH.md
@.planning/phases/27-remove-symlink-mechanism/27-01-SUMMARY.md

@2_Scripts/shared/path_utils.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Step 3 scripts (3.0-3.3) to use get_latest_output_dir()</name>
  <files>2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py, 2_Scripts/3_Financial/3.1_FirmControls.py, 2_Scripts/3_Financial/3.2_MarketVariables.py, 2_Scripts/3_Financial/3.3_EventFlags.py</files>
  <action>
For each of the 4 Step 3 scripts:

**3.0_BuildFinancialFeatures.py:**
1. Add import: `from shared.path_utils import get_latest_output_dir`
2. In setup_paths(), replace paths reading from 1.4_AssembleManifest and related outputs:
```python
manifest_dir = get_latest_output_dir(
    root / "4_Outputs" / "1.0_BuildSampleManifest",
    required_file="master_sample_manifest.parquet"
)
paths["manifest"] = manifest_dir / "master_sample_manifest.parquet"
```
3. Search for all `/ "latest" /` patterns for reading inputs and update

**3.1_FirmControls.py:**
1. Add import: `from shared.path_utils import get_latest_output_dir`
2. In setup_paths(), replace paths reading from 1.0 manifest and 3.0 outputs:
```python
manifest_dir = get_latest_output_dir(
    root / "4_Outputs" / "1.0_BuildSampleManifest",
    required_file="master_sample_manifest.parquet"
)
```
3. Update any paths reading from 3.0_BuildFinancialFeatures outputs

**3.2_MarketVariables.py:**
1. Add import: `from shared.path_utils import get_latest_output_dir`
2. In setup_paths(), replace all /latest/ paths reading from prerequisites
3. This script may read from 1.0 manifest and 3.0/3.1 outputs - update all

**3.3_EventFlags.py:**
1. Add import: `from shared.path_utils import get_latest_output_dir`
2. In setup_paths(), replace all /latest/ paths reading from prerequisites
3. Update paths for manifest and any 3.x outputs it reads

For all scripts: Carefully search for `/ "latest" /` patterns throughout the file (not just setup_paths) and update each reading occurrence.
  </action>
  <verify>
```bash
python -c "import ast; ast.parse(open('2_Scripts/3_Financial/3.0_BuildFinancialFeatures.py').read()); print('3.0 OK')"
python -c "import ast; ast.parse(open('2_Scripts/3_Financial/3.1_FirmControls.py').read()); print('3.1 OK')"
python -c "import ast; ast.parse(open('2_Scripts/3_Financial/3.2_MarketVariables.py').read()); print('3.2 OK')"
python -c "import ast; ast.parse(open('2_Scripts/3_Financial/3.3_EventFlags.py').read()); print('3.3 OK')"
```
  </verify>
  <done>Step 3 scripts (3.0-3.3) use get_latest_output_dir() for reading prerequisite outputs</done>
</task>

<task type="auto">
  <name>Task 2: Update Step 4.1.x scripts to use get_latest_output_dir()</name>
  <files>2_Scripts/4_Econometric/4.1_EstimateCeoClarity.py, 2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py, 2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py</files>
  <action>
For each of the 3 Step 4.1.x scripts:

**4.1_EstimateCeoClarity.py:**
1. Add import: `from shared.path_utils import get_latest_output_dir`
2. In setup_paths() or data loading, replace paths reading from:
   - 1.0_BuildSampleManifest (manifest)
   - 2.2_Variables (linguistic variables)
   - 3_Financial_Features (firm controls, market variables)
   
Example pattern:
```python
manifest_dir = get_latest_output_dir(
    root / "4_Outputs" / "1.0_BuildSampleManifest",
    required_file="master_sample_manifest.parquet"
)
lv_dir = get_latest_output_dir(
    root / "4_Outputs" / "2_Textual_Analysis" / "2.2_Variables"
)
fc_dir = get_latest_output_dir(
    root / "4_Outputs" / "3_Financial_Features"
)
```

**4.1.1_EstimateCeoClarity_CeoSpecific.py:**
1. Add import: `from shared.path_utils import get_latest_output_dir`
2. Replace all /latest/ paths for reading prerequisites
3. This script likely reads from 2.2_Variables and 3_Financial_Features

**4.1.2_EstimateCeoClarity_Extended.py:**
1. Add import: `from shared.path_utils import get_latest_output_dir`
2. Replace all /latest/ paths for reading prerequisites
3. Similar pattern to 4.1 and 4.1.1

Note: These scripts may use shared/data_loading.py which was already updated in Plan 01. If they call load_all_data(), they may need fewer direct changes. Check if they have additional inline /latest/ references.
  </action>
  <verify>
```bash
python -c "import ast; ast.parse(open('2_Scripts/4_Econometric/4.1_EstimateCeoClarity.py').read()); print('4.1 OK')"
python -c "import ast; ast.parse(open('2_Scripts/4_Econometric/4.1.1_EstimateCeoClarity_CeoSpecific.py').read()); print('4.1.1 OK')"
python -c "import ast; ast.parse(open('2_Scripts/4_Econometric/4.1.2_EstimateCeoClarity_Extended.py').read()); print('4.1.2 OK')"
```
  </verify>
  <done>Step 4.1.x scripts use get_latest_output_dir() for reading prerequisite outputs</done>
</task>

</tasks>

<verification>
```bash
# All 7 scripts compile without syntax errors
for f in 2_Scripts/3_Financial/3.*.py 2_Scripts/4_Econometric/4.1*.py; do
  python -c "import ast; ast.parse(open('$f').read())" && echo "OK: $f"
done

# Check for remaining /latest/ patterns in reader paths (reading context only)
grep -rn '/ "latest" /' 2_Scripts/3_Financial/3.*.py 2_Scripts/4_Econometric/4.1*.py 2>/dev/null | head -20
```
</verification>

<success_criteria>
- All 7 Step 3 and Step 4.1.x scripts compile without syntax errors
- 3.0, 3.1, 3.2, 3.3 import and use get_latest_output_dir() for reading prerequisites
- 4.1, 4.1.1, 4.1.2 import and use get_latest_output_dir() for reading prerequisites
- No hardcoded /latest/ path patterns remain for READING data in these scripts
- Note: /latest/ for WRITING (symlink creation) addressed in Plan 05
</success_criteria>

<output>
After completion, create `.planning/phases/27-remove-symlink-mechanism/27-03-SUMMARY.md`
</output>
