% SUBSECTION 2.3 "Estimation of the Main Variable" -- DRAFT for advisor, then user (PDF). DASH-FREE (no '---'/'--').
% Markers %%Pn%% split paragraphs. Equations eq-2/eq-4 transcribed VERBATIM from the verified primary source
% (tmp/nlm_dwz_equations.json: eq-2 UncAnsCEO=UnctWordsAnsCEO/WordsAnsCEO; eq-4 controls Catch-3-closed:
% Speech={UncPreCEO,UncQue,NegCall}, FirmChars={SurpDec,EPSgrowth,StockRet,MarketRet}, Year FE; ClarityCEO_i=-gamma_i; UncRes=eps).
% Phase A: equations VERIFIED (primary source) + Catch-3 closed. pagan1984 (generated-regressand cite) OMITTED:
%   unverified + not in bib + cannot write bibitem from memory -> prose stands without it (fallback); flag to NLM-verify later.
% P2.5 FALLBACK taken: the net-of-UncPre 'isolation' is stated as a residual PROPERTY (mechanical/true), NOT attributed
%   to a DWZ 'design intent' (that attribution was answer-only/provisional). 'would surface' not 'must'; conservative floor kept.
% Phase C self-scan: no '---'/'--'; Catch-1 honored (DWZ CONSTRUCT+USE the residual, never 'discarded'); RULE-COH-4
%   (one-clause callback to 2.1 P4), COH-7 (refinement not different object), COH-8 (net of UncQue/NegCall -> 2.5); no numbers; boundary (mechanics->3.1).

%%P1%%
The dependent variable is built from the language of the call. Following the apparatus adopted in Section~2.1, we measure uncertainty with the Loughran and McDonald finance-specific uncertainty word list, applied as a word share. For a CEO's answers in the question-and-answer session, the raw measure is the share of uncertainty words among all the words the CEO speaks when answering analysts,
\begin{equation}
\mathrm{UncAnsCEO} = \frac{\mathrm{UnctWordsAnsCEO}}{\mathrm{WordsAnsCEO}},
\end{equation}
which is the quantity the decomposition below operates on. The same word-share form is computed for the scripted presentation, $\mathrm{UncPreCEO}$, and for the analysts' own questions, $\mathrm{UncQue}$; these enter the decomposition as controls, not as the dependent variable. How the transcripts are tokenized and the words matched is a construction detail we defer to Section~3.1; here the measure is taken as given, and the question is which part of it can carry an anticipatory signal.

%%P2%%
An anticipatory, deal-tracking signal must vary from call to call within a single CEO's tenure, so, as established in Section~2.1, it can reside only in the call-varying part of this measure and not in the CEO's persistent speaking style. To separate the two, we adopt the decomposition of \citet{dwz}, estimated on our sample:
\begin{equation}
\mathrm{UncAnsCEO}_{i,t} = \alpha + \gamma_i\,\mathrm{CEO}_{i,t} + \beta_s\,\mathrm{Speech}_{s,i,t} + \beta_k\,\mathrm{FirmChars}_{k,i,t} + \mathrm{Year}_t + \varepsilon_{i,t},
\end{equation}
where $\mathrm{CEO}_{i,t}$ is an indicator for manager $i$ being CEO in quarter $t$, $\mathrm{Speech}_{s,i,t}=\{\mathrm{UncPreCEO},\mathrm{UncQue},\mathrm{NegCall}\}$ collects other linguistic features of the same call, $\mathrm{FirmChars}_{k,i,t}=\{\mathrm{SurpDec},\mathrm{EPSgrowth},\mathrm{StockRet},\mathrm{MarketRet}\}$ collects contemporaneous performance, and $\mathrm{Year}_t$ absorbs common annual conditions. The persistent component is the CEO fixed effect, summarized as $\mathrm{ClarityCEO}_i=-\gamma_i$, and the object of interest is the residual, $\mathrm{UncResCEO}=\varepsilon_{i,t}$. This is DWZ's specification estimated on our data, not their published residuals. Because $\mathrm{UncResCEO}$ is taken net of the persistent style and of these observable call-level controls, it is narrower than a style-only residual: a sharper version of the call-level component located in Section~2.1, not a different object. DWZ themselves construct this residual and use it as an explanatory variable in their own market-response model; our contribution is not the measure but the anticipatory, disclosure-state reading we give it around undisclosed deals. Finally, because the residual is net of the scripted presentation, $\mathrm{UncPreCEO}$, it isolates the unscripted component, the place where the anticipation of an undisclosed deal would surface; should some of that anticipation also reach the prepared remarks, the residual understates it and so reads as a conservative floor. By the same construction it is net of analyst-question uncertainty, $\mathrm{UncQue}$, and of call negativity, $\mathrm{NegCall}$, a property Section~2.5 relies on when it separates the signal from analyst scrutiny.

%%P3%%
One feature of this construction deserves note. Because $\mathrm{UncResCEO}$ is itself the residual from a first-stage regression and then serves as the dependent variable in the analyses that follow, the design is a standard two-step one, and conventional second-stage standard errors can be understated unless a two-step correction is applied. We flag the property rather than treat it as a defect: it is generic to two-step estimation, DWZ themselves use the residual as a regressor, and where it matters a bootstrap that re-estimates both stages supplies a remedy. The deeper question, whether the residual reflects genuine uncertainty rather than merely the analyst scrutiny a call attracts, is not settled by construction; we treat it as an empirical matter and take it up in Section~2.5.
%%END%%
