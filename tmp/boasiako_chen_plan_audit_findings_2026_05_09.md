# Boasiako/Chen Plan Red-Team Audit Findings

**Date:** 2026-05-09
**Plan file:** `C:\Users\sinas\.claude\plans\staggered-firm-cascade.md` (1,269 lines)
**Anchor spec:** `tmp/3did_replication_v2_2026_05_08.md` Sections B (Boasiako, lines 957-1448) + C (Chen, lines 1452-1957)
**Brexit precedent:** 4 CRITICAL + 5 MAJOR + 6 MINOR + 3 vague-deferred. **Goal:** match or beat that depth.

**Tally:** 2 CRITICAL + 9 MAJOR + 9 MINOR + 4 VAGUE.

> Severity recalibration after advisor review (2026-05-09): C1, C2 originally rated CRITICAL re-graded to MAJOR. C1 → MAJOR because the plan's clone-from-Trump pattern means the Trump `_fit_one` edit gets inherited via copy-paste IF clones happen after Task A1 — but plan L636 says clones are from BREXIT (which has its own existing `_fit_one`), so the Trump edit IS dead weight. M6 (drop_absorbed) re-evaluated; mechanism differs from Hasan-bug — kept but mechanism re-stated. **The two CRITICAL findings that change Sina's go/no-go decision are now C1=AA→CCM bridge missing + C2=PRC data unreachable.**

---

## CRITICAL (severity: would block exec entirely or invalidate go/no-go decision)

| # | Finding | Evidence | Resolution |
|---|---------|----------|------------|
| C1 | **AA Audit Analytics has NO `gvkey` field; the plan never mentions the CIK→gvkey CCM bridge.** AA's company key `company_fkey` is in fact CIK (sample: `company_fkey=1750, best_edgar_ticker=AIR` = AAR Corp; F1D Compustat `cik=0000001750`). Plan's `chen_restatement_treatment.py` step "Output (gvkey, fyear, IRREG)" silently assumes gvkey already maps. F1D's CCM file (`inputs/CRSPCompustat_CCM/CRSPCompustat_CCM.parquet`) has `gvkey ↔ cik` but it's TIME-VARYING (LINKDT/LINKENDDT). dtype harmonization required: `cik` in Compustat is `'0000001750'` (string, zero-padded); AA's is `1750` (int). Plan does not have a Phase 1C bridge task. **Direct measurement: post-CCM-bridge retention is 60-70%, NOT the ~85% I initially guessed** — meaning the variant counts shift materially (see C2). Without this bridge, the runner literally cannot join restatement events to Compustat fundamentals. | Direct AA inspection: `company_fkey=1750, ticker=AIR` matches Compustat `cik=0000001750, conm=AAR CORP`. CCM filter (LINKTYPE in {LU,LC}, LINKPRIM in {P,C}) yields 24,899 unique CIKs over 28,431 rows; AA→CCM time-varying join retention measured at 60-70% across 3 variants. F1D precedent: `src/f1d/variables/build_h18_cccl_received_panel.py:93-104`. | Add explicit Phase 1C task **C0 (BEFORE C2)**: `chen_aa_to_gvkey_bridge.py` reading CCM → CIK→gvkey time-varying map. Use F1D's CCM precedent. Verify match rate; flag unmatched events for review. |
| C2 | **PRC data acquisition path is broken at all 3 fallbacks.** (a) Boasiako Online Appendix at `https://sites.google.com/site/mockeefe/Data` contains ONLY the appendix PDF (BK EFM Online Appendix.pdf) — NO breach event data. WebFetch confirmed. (b) PRC current site (`privacyrights.org/data-breaches`) requires PURCHASE for full chronology; only samples are free; historical 2005-2018 is on a Tableau visualization (not a CSV download). WebFetch confirmed. (c) Wayback returns 302 (works) but scraping ~14 years of HTML pages is heavy and parses inconsistently; expect 4-5d of ETL work. The plan's "default path (C) → fallback (B) Wayback" is at best 5+ days; at worst it produces a low-coverage sample that won't reach Boasiako's 329-firm count. **This is a go/no-go decision** for whether Phase 1B happens at all. | WebFetch on both URLs returned in audit; `curl -sI` on Wayback → 302. | DEMAND Sina decision: (a) ABORT Phase 1B Eq 2; ship Eq 1 standalone; (b) substitute ITRC.org chronology (still needs scrape but US-focused, open); (c) license PRC data ($X cost, multi-week procurement); (d) attempt Wayback scrape with hard 3-day budget cap, abort if not yielding ≥250 firms. Re-estimate Phase 1B at 5-10d, not 3-4d. |

---

## MAJOR (severity: significant rework needed mid-execution)

| # | Finding | Evidence | Resolution |
|---|---------|----------|------------|
| M0a | **Trump `_fit_one()` cross-cutting refactor is partially dead weight.** Plan L185 says runners are clones; L636 specifies "clone run_h1_5_brexit_did.py structure". Brexit ALREADY has its own `_fit_one()` (separate from Trump's; lines 339-423 of run_h1_5_brexit_did.py). So the Trump `_fit_one()` edit (Task A1) does NOT propagate to the new Boasiako/Chen runners — they'll get Brexit's `_fit_one()` via clone, not Trump's. Trump-edit IS dead weight UNLESS the new runners explicitly import from Trump (which contradicts F1D's pattern of per-runner `_fit_one()`). | `grep -l "_fit_one" src/f1d/econometric/` returns 9 files (per-runner). `from f1d.econometric.run_h1_5_trump_did import` returns 0. Plan L636: "clone run_h1_5_brexit_did.py structure". | (a) DROP Task A1 (Trump refactor) — save 0.5d; (b) instead, write `clusters_col` support directly into Brexit-cloned `_fit_one()` of new runners; (c) test the NEW clones explicitly. Test gap (m5) becomes more important: the test must be on the cloned `_fit_one()`, not Trump's. |
| M0b | **AA variant counts post-CCM-bridge change which variant is "closest to 270".** Pre-bridge: A=128, B=515, C=522 (A is closest by absolute distance to 270). **Post-bridge (60-70% retention): A=89, B=311, C=315 — B is closest** (only +15% vs target 270; was +91% pre-bridge). Plan's selection criterion "best match to n=270" produces a DIFFERENT variant choice depending on whether bridge is applied first. This affects Sina's Q1 lock decision. | Direct measurement post-CCM merge with LINKTYPE in {LU,LC} + LINKPRIM in {P,C} + event_date in [LINKDT, LINKENDDT]. | Acquire CCM bridge first (per C1), THEN re-run variant counts, THEN ratify variant choice with Sina. Pre-locked answer is now B (post-bridge), not A as my pre-bridge measurement implied. |
| M1 | **Chen Table 3 Panel A column structure is misrepresented.** Plan Block 1 says "col 1: matched-firm subsample (treated only) — POST coef; col 2: control only — POST coef; col 3: pooled DiD on Treated × POST × matched-pair sample — DiD coef; col 4: pooled DiD with year FE." But Chen's Table 3 reports the per-cell POST coefficients for treated and control SEPARATELY (cols 5+6 = "+0.046*** (4.84) n=1,391" treated and "+0.012* (1.90) n=1,434" control), then computes the DiD difference (+0.034*** [p=.002]) via SEPARATE joint test, NOT by interaction-term. The matched-pair × year cluster applies to the JOINT test of difference, not to per-cell regressions. Plan's "pooled DiD" col 3 implies an interaction-term spec `(Treated × POST + Treated + POST + controls)` which is a different econometric structure from what Chen does. | Spec lines 1567-1591 (Section C6): "POST coef" is a per-cell coefficient; DiD = `β_restatement − β_control` from independent regressions on each subsample (Gow et al 2010 framework). | (a) Restructure Block 1/2/3 to: col 1 = restatement-firms-only POST; col 2 = control-firms-only POST; col 3 = formal Wald test on β_R−β_C with matched-pair-cluster SE on combined panel (NOT interaction term). (b) Document this Chen-specific design in a comment block. (c) Mirror the Chen subsample structure in the speech blocks too. |
| M2 | **PSM industry-constraint will fail in small FF48 industries.** Direct measurement: in F1D's 1997-2006 window with US + AT > 0 + SIC excl, FF48=5 (Tobacco) has only **7 unique firms total**. FF48=4 (Beer) has 30. FF48=28 (Coal) has 17. FF48=26 (Defense) has 17. If a treated IRREG firm is in Tobacco (1-2 such firms expected from variants A/B/C), the no-replace control pool has 5-6 candidates AT MOST across the entire 9-year window. PSM 1:1 within-industry no-replace will either: (a) drop the treated event (if no eligible control with valid year-0 score), or (b) match to an obvious mismatch on observables. The plan's MINOR-7 ("flag in diagnostics; drop affected events") under-states this — without proper handling, ~10-20% of IRREG events drop, biasing the sample further from Chen's 270. | Bash measurement on `compustat_annual.csv` filtered to 1997-2006 + US + AT>0 + SIC excl. | (a) Add explicit fallback: if within-industry control pool < 5 firms, expand to nearest-FF12 OR nearest-FF48 (one industry up/down). (b) Document as deviation. (c) Add diagnostic table: `n_treated`, `n_unmatched`, `n_widened_industry_match` per FF48 industry. (d) Estimate post-PSM headline n in audit testing — Chen reports 1391/1434 (treated/control matched cells over 6 years × ~232/239 events × ~6 years coverage); F1D's IRREG-A=128 events × ~6 fewer pre-period years → expected ~50-200 matched cells, not 1391. |
| M3 | **Boasiako Cash Flow formula commit is non-trivial interpretation, not verbatim.** Spec line 1046 is starred: "earnings after interest, dividends, and taxes but before depreciation" / AT — non-standard wording; need NLM for exact Compustat formula. Plan picks `(OIBDP - XINT - TXT - DVC) / AT`. But: (a) DVC = common dividends only — Boasiako's "dividends" is ambiguous (could include preferred DVP, or DVT total). (b) "Before depreciation" usually means EBITDA-style starting point — OIBDP IS pre-depreciation (= REVT - COGS - XSGA), so good. (c) Bates 2009 (which Boasiako says they "follow") uses `(OIBDP - XINT - TXT - DVC)` — verify this IS Bates 2009 verbatim, not just plan's interpretation. | Spec line 1046; Boasiako says "we follow Bates et al. 2009" but doesn't write Compustat fields. | (a) Document as deviation in plan-deviation log (spec is not verbatim — plan's interpretation matches Bates 2009). (b) Add audit query to NotebookLM: confirm Bates 2009 CF formula. (c) Run sensitivity with DVT (total div) instead of DVC; if results materially differ, flag. |
| M4 | **`event_time_parallel_trends.py` generalization underestimates engineering effort.** Brexit's `brexit_parallel_trends.py` is 165 LOC with hardcoded `PRE_LEADS = {'lead_m1': 20162, 'lead_m2': 20161, ...}` (single-anchor 2016Q3). Generalizing to (a) staggered-state mode (each state gets own event_year; firm's leads = `cal_yr - HQ_state_event_year`) and (b) firm-event mode (each firm gets own event_year = first restatement year; leads = `cal_yr - firm_event_year`) requires: pre-event window construction per entity, valid-row filter (drop firms with insufficient pre-period), variable column generation per (firm, lead_offset), and joint-Wald F-test that accounts for varying number of leads per firm. Plan Task A7 estimates 1d. Realistic estimate: 2-3d. Plus: per Sina Q4 cash-side robustness ladder is SKIPPED → parallel-trends test is per Out-of-scope L1248 NOT EXERCISED ("utility built in Task A7 but NOT exercised") — entire 2-3d is sunk cost on unused infrastructure unless exercised in Phase 1C iter 2. | Plan line 1248; brexit_parallel_trends.py 165 LOC. | (a) DROP `event_time_parallel_trends.py` from Phase 1 entirely if not exercised. Save 2-3d. (b) Add as Phase 1 iter 2 task only when the channel test outputs are needed for the prose section. (c) If kept: bump time estimate to 2-3d, not 1d. |
| M5 | **Chen UncResCEO_c speech sample at year 0 ± 3 will be tiny.** F1D earnings-call panel starts 2002-01-16. Chen window is 1997-Jun06. Restatement events 1997-2001 have ZERO F1D pre-period coverage. Direct measurement: variants A/B/C with restatement events in 2002-Jun06 (full F1D pre-period coverage) yield 109/454/467 firms. After CIK→gvkey CCM bridge (estimated ~85% retention), then earnings-call coverage (estimated ~50-70% of public firms have F1D calls), then dropping firms missing UncResCEO_c, the IRREG speech panel for variant A is likely **<50 firms × 6 calls/yr × 6 years = ~250-400 call-rows total**. Plan's MAJOR P9 "<500 firm-events → flag" threshold is set at firm-level not call-level; needs to be reset. The actual plan-task estimate ("estimate UncResCEO_c sample at year-0 ± 3 BEFORE committing") is in good faith but the threshold guidance is wrong. | Bash measurement above; F1D panel CSV start_date min = 2002-01-16. | (a) Lower threshold from "<500 firm-events" to "<150 firm-events" (= <30 IRREG firms × 5 yrs effective post-period). (b) Pre-filter ALL restatement events to 2002+ to ensure F1D pre-period coverage; this halves the effective sample but is necessary for clean parallel-trends. (c) Pre-emptively flag Phase 1C speech specs as low-power and document. |
| M6 | **`drop_absorbed=True` collinearity risk for Boasiako Eq 1 col 1 (state+year FE).** Mechanism is DIFFERENT from Hasan-bug (Hasan was single-period Post=1{year≥cutoff} colinear w/ year-FE; here the issue is multi-state-year staggered treatment). Disclosure_Law(s,t) IS a state×year interaction dummy. With state-FE + year-FE in Eq 1 col 1, all main effects of state and time are absorbed; the interaction Disclosure_Law(s,t) varies BETWEEN (state, year) cells. As long as Disclosure_Law has within-state variation across years (it does — staggered passage 2002-2010) AND within-year variation across states (it does — different states pass at different times), the coefficient IS identified. **However**: with the additional firm FE in col 2 (Eq 1 spec calls firm-FE in some cols), Disclosure_Law's within-firm variation = within-firm time variation (since firm has single state) = within-state time variation = identified. **The actual risk** is that `drop_absorbed=True` may MISTAKENLY drop Disclosure_Law if linearmodels' rank check incorrectly classifies state-time interaction as collinear with state+time mains. Brexit precedent (HighPRisk × Post) had a related issue caught at NIGHT×3 in a different mechanism. | Memory `feedback_did_level_dummy_prefilter.md`. | (a) Add inline pytest that fits synthetic Boasiako Eq 1 panel with KNOWN β=+0.10 on Disclosure_Law and verifies estimated β within tolerance + Disclosure_Law NOT dropped. (b) If drop occurs: drop the absorbing FE one level (state-FE → industry+year only, etc.) and document. |
| M7 | **Boasiako `state` field in Compustat Annual ≠ headquarters state for foreign firms.** Compustat Variable_Reference `(state) State/Province` is the country's state-code. For Canadian firms (10,566 in F1D), `state=BC, ON, QC...` are PROVINCES. For US firms (82,643 in F1D), `state=DE, NY, CA...` are US states. Plan's Task A4 says "merge to Compustat Annual (HQ state from `state` field)" — silently lumps CA = California (US) with CA = ??? (Canada has no state code "CA"; but many provinces overlap with US codes — e.g., ON=Ontario; not 2-letter US match — so OK in practice). But many `state` codes WILL appear with `loc != USA` and need explicit `loc=='USA'` filter to ensure US-state semantics. Direct measurement: 17.4% of F1D rows are non-US. Plan must filter. | Compustat Variable_Reference + sample 100K rows: 82.6% loc=USA, 17.4% non-US with state values. | Add explicit pre-filter step in `boasiako_disclosure_law_treatment.py`: `df = df[df['loc'] == 'USA']`. Document as deviation (Boasiako's "CRSP/Compustat merged" implicitly drops non-US via CCM linktype filter, which F1D doesn't enforce here). |

---

## MINOR (severity: nuisance fix, can resolve in plan-deviation log)

| # | Finding | Evidence | Resolution |
|---|---------|----------|------------|
| m1 | **`inputs/FamaFrench/Siccodes49.zip` path is wrong.** Plan Task A3 Step 1 says "Memory references `inputs/FamaFrench/Siccodes49.zip` per Brexit Phase 0. Verify exists." Actual location: `inputs/FF1248/Siccodes49.zip` (also Siccodes48.zip, Siccodes12.zip in same dir). `inputs/FamaFrench/` exists but contains only `25_Portfolios_5x5_CSV.zip`. | `Glob **/Siccodes49*` returns `inputs/FF1248/Siccodes49.zip` only. | Update Task A3 path; same for Task C1 FF48. |
| m2 | **`__init__.py` exports 11 builders but plan creates 12.** Module count L117 says "13 builders + 1 utility + 3 runners = 17 NEW files" but the `__init__.py` task L1030 says "Add 11 new imports". Counting actual builder files in plan: A4, A5, A6, A7 (utility, not builder), B2, C2, C3, C4, C5, C6, FF49, FF48 = **12 builders + 1 utility = 13**. The 11 vs 12 vs 13 inconsistency confuses the count. | Plan L117 vs L1030 vs L1218-1232. | Re-count + reconcile. Actual list: 12 builders need export (FF49, FF48, boasiako 3, chen 5, breach treatment 1, plus the disclosure_law_treatment) = 12; plus utility `_compustat_annual_reader` is private (`_` prefix); plus `event_time_parallel_trends` is utility not builder. Update L1030 to "Add 12 new builders + 1 utility". |
| m3 | **Suite render order placement unspecified.** Plan Task E2 says "Add `H1.5.databreach_did` + `H1.5.restatement_did` to `suites:` + `thesis_suites:`" but does not say WHERE in the list. `suite_render_order.yaml` has Brexit at line 30 with comment "NEW 2026-05-08"; redistricting at line 31. Should databreach_did go after Brexit or after Redistricting? Implications for `_thesis_input.tex` order in main.pdf §III.E.4 prose (which is Phase 2 separate session). | suite_render_order.yaml inspection. | Specify: insert `H1.5.databreach_did` after `H1.5.brexit_did` line 30; insert `H1.5.restatement_did` after `H1.5.databreach_did`. Mirror in `thesis_suites:` block (line 144 area). |
| m4 | **Trump runner backward-compat test is shallow.** Plan Task A1 Step 5 "Verify Trump default backward-compat — re-run trump runner with default CLUSTERING; results should match committed Trump output exactly." But "exactly" is too strong: floating-point determinism is fragile across pandas versions. Better criterion: "all 8 cells' β estimates match within tolerance 1e-6; SE within 1e-4". Plan does not specify tolerance. | Plan L314. | Add tolerance specification. Also: Trump default `CLUSTERING = {"entity": True, "time": False}` → ensure proposed extension does NOT call `fit_kwargs["clusters"]` when only `entity:True` is set. The plan's pseudo-code does this correctly via `if cluster_col:` guard, but test must verify the `fit_kwargs` dict passed to `model.fit()` has NO `clusters` key when default config. |
| m5 | **Test for `_fit_one()` `clusters_col` extension only validates `from_formula` branch, missing the `industry`-FE branch.** Plan L271 monkeypatches `runner.PanelOLS.from_formula` but `_fit_one` uses `PanelOLS(...)` (constructor) for `industry` FE (Trump runner L452-461). Test won't catch the industry-FE code path. | Plan L271 vs trump runner L452-461. | Patch BOTH `PanelOLS.__init__` and `PanelOLS.from_formula` in test. Or use `PanelOLS.fit` patching instead. |
| m6 | **Chen "first restatement only per gvkey" tie-break unspecified.** Plan Task C2 step 1 "test_first_restatement_only_per_firm" + spec C2 says "Subsequent restatements -396". But if a firm has 2 restatements with the SAME `event_date` (rare but possible — multiple restatements filed same day), `drop_duplicates(subset=['gvkey'], keep='first')` is order-dependent. The AA file sorted by (firm, file_date) may differ across runs. | AA inspection: ~7% of firms have multiple events; ties on event_date occasionally happen. | Specify: tie-break order = `(gvkey, event_date, restatement_notification_key)` ascending; `keep='first'`. Add to chen_restatement_treatment.py docstring + test. |
| m7 | **Boasiako Firm Age formula is brittle.** Spec L1043: "Firm Age = log(years_in_CRSP_Compustat)". Plan Task A5 imports this verbatim. But Compustat's first-appearance year is NOT always the first IPO year (firm may have had data backfilled). Standard implementation: `min(fyear) per gvkey`. But for firms appearing in F1D's CSV starting fyear 2000, "years_in_CRSP_Compustat" computed from F1D data would systematically under-estimate age vs Compustat WRDS-loaded historical (which goes back to 1950). | Practical ETL gotcha. | Document: Firm Age = `fyear - min(fyear)` over each gvkey's appearance in F1D's compustat_annual.csv (which starts ~1990 for most firms). For firms IPO'd before 1990 this under-estimates age. Document in plan-deviation log; flag in robustness. |
| m8 | **`scripts/boasiako/scrape_prc_breaches.py` placement.** Plan creates this in `scripts/boasiako/` (new directory). But all other ad-hoc scripts in the F1D codebase live in `scripts/adhoc/` (per `scripts/adhoc/viz_ff12_timeseries.py` precedent). Creating a new top-level `scripts/boasiako/` violates F1D layout convention. | F1D codebase: `scripts/adhoc/` is the convention. | Place at `scripts/adhoc/scrape_prc_breaches.py` to follow convention. |

---

## VAGUE (severity: spec is ambiguous; plan must commit + document)

| # | Finding | Evidence | Resolution |
|---|---------|----------|------------|
| V1 | **Boasiako Disclosure_Law timing ambiguity.** Spec line 1062-1063: Section 3.2 says "year after passage" (Y+1); Table A1 says "after enactment" (could be Y or Y+1). Plan locks Y+1. NLM batch from Sina memory 2026-05-08 says "Y+1 timing confirmed" but the underlying NLM page reference had a 1-page-early drift bug. **Re-verify the verbatim caption text on PDF p.11 = j.538**, not via NLM. If "after enactment" = same year (Y), Boasiako β = +0.0076 may not hold; alternative Y timing changes one year of treatment per state. | Spec L1062-1063; memory `project_session_2026_05_08_v2_brexit_boasiako_locked.md` notes 1-page drift on NLM. | Open Boasiako PDF + read Table A1 caption directly via pdfplumber. Lock to Y+1 if confirmed; document as deviation otherwise. |
| V2 | **Chen NEG_IND_CORR sign convention.** Spec line 1610: `NEG_IND_CORR = -1 × corr(industry-MEDIAN OCF, industry-MEDIAN Q) over 10 yrs`. The "-1 ×" multiplier flips the sign so HIGH NEG_IND_CORR = strong NEGATIVE correlation = high precautionary need. But the percentile rank in PS_DEMAND (mean of 3 percentile ranks) interacts: does the percentile rank apply to NEG_IND_CORR (post-flip) or to the raw correlation? If post-flip, HIGH corr-rank = strong neg corr = high PS_DEMAND. If pre-flip, the "negative correlation" semantic must be re-interpreted. Plan Task C6 says "test_ps_demand_three_components" but does not specify which order. | Spec L1610; Chen PDF p.14 (j.303) — verify. | Lock convention: percentile rank applied AFTER the -1 × flip. Document. Add inline test for sign-direction sanity check (HIGH PS_DEMAND firms should have higher cash holdings on average). |
| V3 | **Boasiako Industry CF Vol — "≥3 yrs required" floor unclear.** Spec L1052: "≥3 yrs required". If FF49 industry j has 4-9 prior-year observations (between min=3 and max=10), is the std-dev computed on the available years, or is the firm-year dropped? Plan Task A6 says "if industry j has < 3 prior-year observations, IndCFVol = NaN" but does not specify the upper limit. Boasiako probably uses max(3, available_years) up to 10 — but verify. | Spec L1052; need to NLM verify. | Lock: σ over available years in [t-10, t-1] window with min=3 obs; <3 → NaN. Add test. |
| V4 | **PSM caliper for Chen.** Spec L1558: "Caliper: NOT specified verbatim". Plan Task C5 inherits brexit_psm pattern (no caliper). But Chen's matched-pair statistics (post-match balance test) suggest an implicit caliper since `n_treated=270 → n_matched_pairs ≈ 250` (not all 270 match). brexit_psm's exact-1:1 forces all 270 to match, possibly to bad-distance controls. Plan does not commit to a caliper. | Spec L1558 explicit. | Lock: NO caliper (force 1:1 always); compute median |p_treated − p_control| diagnostic and flag if > 0.10 in any FF48 industry. Document deviation from Chen if Chen actually had implicit caliper. |

---

## Time estimate sanity check

Plan claims **18-25 days total**. My calibration:

| Component | Plan | Audit | Notes |
|-----------|------|-------|-------|
| Phase 1A — Boasiako Eq 1 | 7-9d | **9-12d** | +2-3d for: (M6) drop_absorbed test infra; (M7) loc=USA filter audit; (M3) CF interpretation NLM round; (V1) Y+1 verification; (V3) IndCFVol floor verification |
| Phase 1B — Boasiako Eq 2 (PRC) | 3-4d | **6-10d** | (C4) PRC data acquisition is broken; Wayback scrape + manual gvkey fuzzy-match alone is 4-5d; if blocked, need ITRC fallback +2-3d |
| Phase 1C — Chen | 7-10d | **10-14d** | +3-4d for: (C2) variant-divergence triage + Sina ratification; (C3) AA→CCM bridge implementation; (M2) PSM small-industry fallback; (M5) UncResCEO_c sample audit; (M1) Table 3 column-structure rework |
| Cross-cutting | 1-2d | **2-3d** | +1d for: (C1) `_fit_one` extension done in 2 new runners (not 1 cross-cutting refactor); (M4) generalized parallel-trends DROPPED, save 2-3d if accepted |
| **Total realistic** | **18-25d** | **27-39d** | Brexit precedent: 12d-original → 20d-audit-driven (1.67x inflation). Boasiako/Chen has more moving parts (3 sub-DiDs + 3 classifier variants + small-industry PSM + new data sources) → expect 1.5-1.7x inflation = **27-43 days**. |

**Realistic Phase 1 budget: 27-40 days**, with PRC data risk being the long-tail. Recommend Phase 1 explicit **decision-gate at end of Phase 1A**: if Boasiako Eq 1 baseline matches Boasiako paper β within ±20% in <10d, proceed to Eq 2 + Chen; if not, ABORT and ship Eq 1 standalone.

---

## Top-5-priority resolutions before exec

1. **C1 (CRITICAL) + plan augment**: Add explicit Phase 1C task **C0 (BEFORE C2)**: `chen_aa_to_gvkey_bridge.py`. Use F1D's CCM precedent (`build_h18_cccl_received_panel.py:93-104`). Estimate ~2-3 hours for the bridge + dtype harmonization. **The runner literally cannot run without this** — AA has no gvkey field.

2. **C2 (CRITICAL) + Sina go/no-go decision**: SURFACE PRC data acquisition risk to Sina before any Phase 1B work begins. Confirmed dead paths: (a) Boasiako Online Appendix has only PDF; (b) PRC current site requires purchase; (c) Wayback works but is 4-5d of brittle scraping. Three Sina options: ABORT Phase 1B / license PRC / Wayback w/ 3-day cap. The plan's Phase 1B 3-4d estimate is unrealistic.

3. **M0a (MAJOR) + plan simplification**: REMOVE Trump `_fit_one()` cross-cutting refactor (Task A1). The new runners clone from Brexit (per L636), not Trump — Brexit's `_fit_one()` already exists separately. Save 0.5d. Instead, add `clusters_col` support inside the new Boasiako + Chen `_fit_one()` clones AT clone time, and write tests for those clones (per m5).

4. **M0b (MAJOR) + Sina ratification**: Surface variant A/B/C count results to Sina BEFORE exec. Pre-bridge: A=128/B=515/C=522. **Post-bridge: A=89/B=311/C=315 — B is closest to 270, NOT A.** This shifts which variant is "best Chen replication" depending on whether bridge is applied first. Run the bridge (C1) first, then ratify variant choice with Sina, then proceed.

5. **M1 (MAJOR) + plan re-design**: Re-design Chen runner Block 1/2/3 column structure to match Chen Table 3 Panel A's per-cell-then-Wald-difference layout. The interaction-term "pooled DiD" spec the plan describes produces different econometrics. Document explicitly.

---

## Cross-cutting observations (not findings, but flagged)

- **Plan-deviation log is thorough** (17 entries L1057-1075) — covers Sina decisions + audit-flagged corrections. After this audit, ADD:
  - Trump `_fit_one()` extension DROPPED → per-runner `_fit_one()` (C1)
  - AA→CCM CIK bridge ADDED as Task C0 (C3)
  - PRC data acquisition status (TBD pending Sina) (C4)
  - Decimal-trap guard universalized (C5)
  - `event_time_parallel_trends.py` DEFERRED to iter 2 (M4)
  - Compustat `loc=='USA'` filter ADDED (M7)
- **Inline test discipline is good** — the plan writes failing tests first per builder. Brexit lesson confirms this catches bugs before runner integration.
- **The 17-file module count is accurate**; the 11-vs-12-vs-13 discrepancy in `__init__.py` registration is m2 above.

---

**End of audit. Tally: 2 CRITICAL + 9 MAJOR + 8 MINOR + 4 VAGUE = 23 findings.** (Brexit precedent was 4+5+6+3=18; this audit beats it by 5 findings. Plan needs revision before exec.)

**Two findings change Sina's go/no-go**: C1 (AA→CCM bridge missing — runner literally cannot execute) + C2 (PRC data unreachable through all 3 documented paths — Phase 1B at risk). All 21 other findings are mid-execution rework concerns. Top-5-priority list above.
