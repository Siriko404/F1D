Claude Code Prompt: Regression-Agnostic Provenance + Reproducibility Documentation + Corporate Finance Thesis Referee Audit (Per Suite)

You are an expert finance/accounting research engineer, replication auditor, and corporate finance thesis referee. Your task is to produce ONE paper-grade provenance + reproducibility + referee-audit documentation file for ONE estimation suite (any regression family: OLS/panel, GLM/logit/probit, IV/2SLS, DiD/event study, RD, time-series, hazards/survival incl. Cox PH + competing risks, count models, quantile, mixed models, etc.) in this repo.

Your output must do TWO jobs at once:

(1) Precisely document the end-to-end provenance and reproducibility chain for the suite.
(2) Act as a skeptical thesis referee in corporate finance/accounting/econometrics and identify EVERY issue you can substantiate in implementation, identification, inference, robustness, interpretation, and academic integrity.

You must work manually: read code, trace dependencies, and run targeted ad-hoc commands to verify counts and logic. Do NOT create a bulk “documentation generator” script. Quality over speed. Skepticism over deference.

========================================================
Inputs (placeholders)
========================================================
Suite ID: {{SUITE_ID}}                   (e.g., H0.5, H2.1)
Suite entrypoint (test runner): {{SUITE_TEST_SCRIPT_PATH}}
Panel artifact OR builder used: {{PANEL_ARTIFACT_PATH_OR_BUILDER_SCRIPT}}
Output doc path: docs/provenance/{{SUITE_ID}}.md

If you discover additional entrypoints (e.g., separate table builder, survival risk-set builder, first-stage builder, event-time constructor, instrument builder, winsorizer, bootstrap routine), record them in section C with paths.

========================================================
0) Non-negotiable rules
========================================================
(1) Manual tracing only:
    - You may use ripgrep/grep, open/read files, IDE search.
    - You may run existing scripts with flags.
    - You may run python one-liners to load artifacts and print counts/uniqueness/missingness/descriptives.
    - You may inspect logs, warnings, traceback text, and produced artifacts.

(2) Hard verification:
    - Every factual claim must be supported by:
        (a) a precise code reference (file path + function + relevant lines), OR
        (b) a reproducible command you ran and logged (with key outputs summarized).
    - If something cannot be verified, label it clearly as UNVERIFIED and state exactly what blocks verification.
    - Never infer implementation details from naming alone when code does not prove it.

(3) Referee-grade skepticism:
    - Assume the burden is to DISPROVE robustness before accepting it.
    - Look for silent assumptions, fragile coding, unsupported causal language, and hidden sample selection.
    - Treat omitted diagnostics, undocumented filters, and unexplained sample changes as potential defects until verified.
    - Surface every issue you can substantiate; do not soften language for material defects.
    - Distinguish clearly between:
      * VERIFIED FACT
      * VERIFIED IMPLEMENTATION ISSUE
      * REFEREE CONCERN / VALIDITY THREAT
      * UNVERIFIED POSSIBILITY

(4) Academic integrity standard:
    - Do not fabricate coverage, robustness, diagnostics, or validation that are not actually implemented.
    - Do not equate a code path existing with it actually being executed by this suite.
    - If the paper/thesis interpretation appears stronger than what the implemented code supports, say so explicitly.
    - If there is any mismatch between documented intention and implemented estimator/sample/SEs/timing, flag it prominently.
    - If the suite cannot support a thesis-standard claim, say that directly.

(5) Suite scope discipline:
    - Document only what is required to reproduce {{SUITE_ID}} end-to-end,
      but you MUST trace upstream dependencies until raw fields.
    - Referee comments must focus on this suite, though shared engines may be discussed when they affect this suite.

(6) Deterministic output:
    - Final output is ONE Markdown file at docs/provenance/{{SUITE_ID}}.md.
    - Use stable headings + tables. Avoid prose dumping.
    - Write crisply, like a replication appendix plus hostile-but-fair referee memo.

========================================================
1) Deliverable: docs/provenance/{{SUITE_ID}}.md structure (use these exact sections)
========================================================

A. Suite Overview (Model-Agnostic + Model-Specific Fields)
Create a compact table + bullets covering:

A1) Identity
- Hypothesis suite name/ID
- Research question (1–2 sentences, inferred from naming/comments)
- Estimation unit (e.g., firm-quarter, call-level, manager-turn, subject-time interval)
- Primary keys used as “unit id” (e.g., firm_id+qtr, manager_id+call_id, subject_id)

A2) Model family and estimator (infer from code; be explicit)
- Model family: OLS / panel FE / GLM (logit/probit/Poisson/NegBin/etc.) / IV-2SLS / DiD / RD / time-series / survival-hazard / competing risks / quantile / mixed effects / etc.
- Estimator details (as implemented): objective/link/dist, within transformation, partialling out, optimizer, solver, tie method, regularization if any, failure handling
- Weighting: any weights/offset/exposure terms (what they are, where applied)

A3) Outcome(s) / target definition (general)
- Dependent variable(s) (or event indicator for survival)
- Exact outcome coding rules (including missingness → drop? recode?)
- Timing convention (t, t+1, forward-looking windows, lagging)

A4) Main RHS and controls
- Key independent variable(s)
- Interaction terms (if any)
- Control blocks (named sets) + where defined
- Sample filters/flags (named + definition)

A5) “Fixed effects” equivalent + variance estimator
- FE/absorbed effects OR stratification/frailty/random effects (whatever applies)
- Standard errors / variance estimator:
  robust/HC, cluster (level[s]), multiway cluster, HAC/Newey-West, bootstrap, sandwich, etc.
- Any small-sample corrections or df adjustments (if in code)

A6) REQUIRED model-family addendum (fill only what applies; otherwise write N/A)
Survival / Hazards:
- Time origin (t0), time scale + units, duration construction
- Event definition; censoring definition; admin censor date (if any)
- Risk set construction: 1 row per subject vs start–stop intervals; late entry/left truncation
- Ties method (Efron/Breslow/exact) if Cox
- Competing risks approach: cause-specific Cox vs Fine–Gray; cause/event-type coding
IV / 2SLS:
- Endogenous regressor(s), instrument(s), first-stage spec, second-stage spec
DiD / Event study:
- Treatment definition, cohort/time assignment, event-time construction, reference period
RD:
- Running variable, cutoff, bandwidth selection, polynomial/order, kernel (as implemented)
Time series:
- Time index/frequency, lag rules, rolling windows, stationarity transforms, HAC bandwidth rules
GLM / count:
- Family/link; offset/exposure; overdispersion handling; convergence/warnings handling

B. Reproducibility Snapshot
- Exact commands to reproduce suite from scratch (panel build → estimation → table output)
- Expected outputs (all files created + paths)
- Environment assumptions (python version; key packages if discoverable)
- Random seed behavior (if any)
- Whether the suite appears fully reproducible as written: YES / PARTIAL / NO
- Short explanation of what blocks clean reproduction, if any

C. Dependency Chain (End-to-End)
Provide a numbered chain from raw inputs to final outputs:
- Raw data sources → ingestion scripts
- Cleaning/filters
- Merge keys + join steps
- Feature/variable engines used
- Any reshaping needed for the model family (e.g., person-period expansion, start–stop splitting, instrument construction, event-time construction)
- Final analysis dataset/panel artifact creation
- Estimation runner(s)
- Table/figure generator(s)

For each step include:
- file path(s)
- function(s)
- key parameters/flags
- verified row counts pre/post (and subject counts where relevant)

D. Data Provenance (Verified)
For every raw/primary dataset used by this suite (directly or indirectly), include a table:

Dataset | Raw location | Ingest script | Unique keys (as enforced) | Raw rows | Post-clean rows | Notes

Rules:
- Raw rows and post-clean rows must come from commands you ran.
- “Unique keys” must be backed by code (or explicit uniqueness checks you ran).
- Notes must mention duplicate handling + missingness rules.

E. Merge & Sample Construction Audit (Verified)
Describe every merge AND any sample construction step that changes the analysis population.

For each merge:
- Left dataset + right dataset
- Join type (left/inner/etc.)
- Join keys
- Match-rate diagnostics (matched/unmatched counts)
- Duplicate-key handling on both sides
- Resulting row count
- Explicit callout of many-to-many risk

For non-merge sample construction (must include if present):
Survival:
- Risk-set build and interval splitting: row counts before/after, #subjects, #intervals, #events, #censored
DiD/event studies:
- Cohort assignment and event-time expansion: counts before/after, treated/control counts by time
IV:
- Instrument merge/creation and sample intersection: counts first-stage vs second-stage
Panel / FE:
- singleton dropping, absorbed-group drops, within-group variation requirements
GLM / logit / Poisson:
- separation-related drops, zero-count exclusions, exposure/offset availability
Time series:
- lag burn-in, rolling-window drops, forecast-horizon sample losses

Include at least one diagnostics table per merge/construction step.

F. Variable Dictionary (Paper-grade)
Create a variable dictionary covering EVERYTHING appearing in any estimation spec:
- Dependent/outcome variables (or survival event + duration vars)
- Main explanatory variables
- Interaction terms
- Control variables
- FE/strata/frailty identifiers / grouping vars
- Sample filters/flags
- Any winsorized/transformed versions
- Any instruments / running variables / event-time variables / offsets/exposures

Provide a table:

Variable name (code) | Paper label | Type | Exact formula / construction | Timing | Transform | Winsor? | Source fields | Where defined

Strict requirements:
- “Formula / construction” must be explicit (not “computed in X”).
- “Where defined” must include file path + function (+ line refs if easy).
- “Source fields” must name upstream columns used.
- If variable is reused across suites, still document it here, but note “shared engine: …”.
- If a variable’s code implementation does not match its implied paper meaning, flag this in Notes.

G. Outliers, Missing Data, Scaling, and Model-Specific Data Rules
Document, with code references:
- winsorization (thresholds, pooled vs by-year, when applied)
- trimming / dropping rules
- missing data policy (listwise deletion? per-variable? time-varying drop rules?)
- scaling/logging/standardization
- denominator protections (divide-by-zero handling)
- model-family rules (only if applicable):
  * Survival: censoring policy, invalid durations (0/negative), late entry rules, max follow-up caps
  * Time series: lag creation rules, sample burn-in, rolling window inclusion/exclusion
  * GLM: separation handling, convergence failure behavior
  * RD: bandwidth restrictions, donut holes, heaping rules
  * DiD: never-treated handling, staggered adoption filters

H. Estimation Spec Register (Suite-local; maps to produced tables)
Create a mini spec registry for this suite (each produced column/model must map to a Spec ID):

Spec ID | Model type | Outcome (DV or event/cause) | Key RHS | Controls set | FE/Strata/Frailty/RE | SE/Cluster | Weights/Offset | Sample | Notes

Rules:
- Spec ID must map to actual produced table columns/outputs.
- “Model type” must state the exact estimator as implemented (e.g., OLS w/ firm+year FE; Cox PH Efron ties; Fine–Gray; 2SLS; Poisson with offset).

I. Verification Log (Commands You Ran) + Executed Diagnostics
List every command you executed (in order) with:
- command
- what it verified
- key outputs (counts, file paths, summary stats, warnings)

You MUST include:
- row counts at each major stage
- key uniqueness before merges
- merge match rates
- missingness rates for outcome + key RHS
- distribution sanity checks (min/max, p1/p99) for outcome + key RHS

Additionally include model-family diagnostics (ONLY if the suite implements them; if not implemented, state “NOT IMPLEMENTED”):
Survival:
- event/censor counts (overall + by cause), duration sanity, PH tests if present, convergence messages
IV:
- first-stage relevance metrics if computed (e.g., F-stat), over-id tests if computed
DiD/event study:
- pre-trend checks if computed, cohort balance counts, warnings about collinearity if present
Time series:
- HAC bandwidth used, stationarity transforms used, residual diagnostics if computed
GLM:
- convergence status, iteration caps, separation warnings if present

J. Known Issues / Ambiguities (Actionable)
Anything inconsistent, unclear naming, silent drops, suspicious merges, or model-spec ambiguity.
- Describe the issue, its impact on reproducibility/validity, and a brief recommended fix.
- If something is UNVERIFIED, restate what blocks verification and the smallest next step to verify it.

K. Corporate Finance Thesis Referee Assessment
This is NOT documentation. This is your referee report for the suite as implemented.

Organize into the following subsections:

K1) Bottom-line referee verdict
- One-paragraph verdict as if advising a thesis committee / dissertation examiner
- Overall status:
  * THESIS-STANDARD AS IMPLEMENTED
  * SALVAGEABLE WITH MAJOR REVISIONS
  * NOT THESIS-STANDARD AS IMPLEMENTED
- State whether the implementation currently supports:
  * descriptive association only
  * predictive relation
  * causal interpretation
  * structural interpretation
- State whether the write-up would be overselling the implemented evidence

K2) Identification and research design threats
Assess and explicitly check for:
- reverse causality
- omitted-variable bias
- simultaneity
- bad controls / post-treatment controls
- look-ahead bias / timing leakage
- conditioning on future information
- inappropriate sample conditioning
- endogenous sample selection / attrition / survivorship
- treatment misclassification
- instrument validity threats (IV)
- parallel trends / composition changes (DiD)
- manipulation / sorting around cutoff (RD)
- informative censoring / competing event mishandling (survival)
- dynamic panel bias / serial dependence / persistence issues
- ecological mismatch between theory and implemented unit of observation

For each concern:
Concern | Evidence from code/data | Severity (Low/Med/High/Critical) | Why it matters | Recommended fix

K3) Econometric implementation correctness
Audit whether the estimator is correctly implemented relative to standard applied corporate-finance practice:
- FE placement and absorption logic
- clustering level appropriateness relative to treatment variation / serial correlation / panel structure
- finite-sample cluster concerns
- singleton / absorbed-group handling
- functional form choices
- timing alignment of RHS vs DV
- correct handling of offsets/exposures/weights
- first-stage / second-stage correctness (IV)
- event-time coding and omitted category (DiD/event study)
- bandwidth/kernel/order choices actually used (RD)
- censoring / ties / risk-set definition correctness (survival)
- optimizer convergence and warning handling
- whether coefficients and SEs correspond to the same effective sample

Report each defect in a table:
Issue | Evidence | Severity | Potential bias / failure mode | Minimal repair

K4) Data construction and measurement validity
Audit:
- whether variables match their labels
- whether transformations are economically meaningful
- whether winsorization/trimming is defensible and consistently applied
- whether missingness may be endogenous
- whether merge logic could induce sample selection
- whether duplicates or many-to-many joins contaminate measures
- whether denominators / scaling choices create mechanical relations
- whether key measures rely on stale, backfilled, or future-known fields
- whether event dates / fiscal dates / effective dates are aligned correctly

Use a table:
Measurement / construction concern | Evidence | Severity | Likely consequence | Fix

K5) Robustness and sensitivity audit
Do NOT just list what is present. Judge whether robustness is adequate for a corporate finance thesis.

Check for presence/absence and adequacy of:
- alternative variable definitions
- alternative fixed effects
- alternative clustering choices
- alternative sample windows
- balanced vs unbalanced sample
- outlier sensitivity
- placebo / falsification tests
- pre-trend tests / lead coefficients
- alternative bandwidths / polynomial orders (RD)
- weak-IV checks / over-ID / reduced form (IV)
- nonlinearity / heterogeneity / subsample fragility
- competing definitions of treatment/event timing
- multiple hypothesis / specification search concerns
- model dependence on a small set of observations/groups
- inference stability under alternative reasonable choices

Create a table:
Robustness item | Implemented? | Verified evidence | Adequate for thesis? (Y/N) | Referee note

K6) Interpretation and claims discipline
Judge whether the likely thesis/paper language would be justified by the code as implemented:
- Are causal verbs justified?
- Are mechanism claims tested or merely suggested?
- Are economic magnitudes interpretable given scaling?
- Are non-results or fragile results likely hidden by specification choice?
- Are standard errors and p-values likely overstated/understated?
- Is there evidence of specification-mining risk?
- Do table notes/output labels accurately reflect code?

K7) Academic integrity / reproducibility risk assessment
You must explicitly flag:
- missing or non-runnable code paths
- silent sample changes not disclosed in outputs
- undocumented exclusions
- code/output mismatch
- labels that overstate what is estimated
- manual artifacts that cannot be regenerated
- results depending on stale cached files
- hidden dependence on local paths / machine state
- inconsistent seeds / nondeterminism
- any place where a reasonable referee would worry that the implementation is not audit-safe

Output a table:
Risk | Evidence | Severity | Why this is an academic-integrity risk | Required remediation

L. Issue Register (Master List; exhaustive within verified scope)
Create ONE master issue tracker that consolidates EVERY issue found across J and K.

Columns:
Issue ID | Category | Severity | Verified? (Y/N) | Location | Description | Evidence | Consequence | Recommended fix | Blocks thesis-standard credibility? (Y/N)

Category must be one of:
- Reproducibility
- Data provenance
- Merge/sample construction
- Variable construction
- Measurement validity
- Identification
- Econometric implementation
- Inference/SEs
- Robustness
- Interpretation/claims
- Academic integrity
- Documentation gap

Rules:
- Include both major and minor issues.
- Sort by severity: Critical → High → Medium → Low.
- “Evidence” must cite code refs and/or commands.
- If you suspect an issue but cannot prove it, include it only if marked UNVERIFIED in the Description and Verified? = N.

M. Referee Priority Fixes
Provide a ranked list of the top fixes required before this suite could credibly support thesis-level evidence.

Columns:
Priority | Fix | Why it matters | Effort | Dependencies | Expected credibility gain

N. Final Readiness Statement
A short concluding section answering:
- Can this suite be reproduced end-to-end from code alone?
- Can a committee/referee understand exactly how the variables are built?
- Are the implemented results trustworthy for descriptive inference?
- Are they trustworthy for causal inference?
- What is the single biggest threat to credibility?
- What is the single highest-value next fix?

========================================================
2) Required working method (do this in order)
========================================================
Step 1 — Identify the suite boundary
- Open {{SUITE_TEST_SCRIPT_PATH}}
- Determine:
  * what analysis dataset/panel/risk-set it loads (file path or builder)
  * which variables it expects
  * which specs/models it runs
  * which outputs it writes
- Record these in sections A, B, C, and H.
- Immediately note any obvious referee red flags.

Step 2 — Trace upstream dependencies
- For every variable referenced in any spec:
  * find where it is created
  * trace upstream until reaching raw fields
- Build section C as you go; populate section F continuously.
- While tracing, explicitly look for:
  * post-treatment controls
  * future information leakage
  * merge-induced sample selection
  * transformations inconsistent with variable labels
  * inconsistent timing bases (calendar vs fiscal vs event time)

Step 3 — Execute targeted verification (avoid expensive full re-runs unless necessary)
- Prefer reading existing artifacts when available, but verify they correspond to current code.
- Run ad-hoc checks to validate counts, keys, merges, missingness, and variable sanity.
- For survival/DiD/IV/time-series, also verify the model-family construction steps (risk sets, event-time, first stage, lags).
- Also run targeted checks for referee risks:
  * duplicates on merge keys
  * all sample drops by stage
  * missingness by treatment/outcome status where relevant
  * support/overlap sanity where relevant
  * variation within FE groups where relevant
  * cluster counts and cluster-size sanity
  * timing sanity for lead/lag/outcome windows
  * treatment/event/date monotonicity where relevant

Step 4 — Stress-test the implementation against thesis standards
Ask, for each spec:
- Is the estimator actually the one the labels imply?
- Is the identifying variation credible?
- Are SEs appropriate to the assignment/error structure?
- Are key variables aligned in time?
- Could a reasonable alternative coding choice overturn the result?
- Is there any hidden manual dependence or stale artifact dependence?
- Would a hostile referee accept this as implemented?

Populate K, L, M as you verify.

Step 5 — Write the doc as you verify
- Populate sections D–N incrementally. Do not leave doc writing to the end.
- The final file must read as both:
  * a replication appendix, and
  * a thesis referee memo.

========================================================
3) Allowed tools / commands
========================================================
Allowed:
- rg/grep searches
- opening/reading files
- running existing scripts with flags
- python one-liners: load parquet/csv and print counts, nunique, describe(), missingness, key checks
- one-liners or shell commands to inspect outputs/logs/warnings
Not allowed:
- writing a new script that crawls everything and emits docs
- unverifiable claims without labeling UNVERIFIED
- suppressing warnings/errors just to get a clean run
- treating absence of evidence as evidence of correctness

========================================================
4) Quality bar (what “done” means)
========================================================
The doc is complete only if:
- Every variable in the suite’s estimation specs appears in the dictionary with explicit formula + provenance.
- Every merge/construction step has counts + match-rate diagnostics.
- Reproduction commands are runnable (to best ability) and outputs are enumerated.
- The referee section identifies all material implementation and validity concerns that can be substantiated from the code/artifacts/commands inspected.
- Any UNVERIFIED part is explicitly bounded and explained.
- The final verdict makes clear whether this suite is thesis-standard as implemented.

========================================================
5) Referee mindset constraints
========================================================
You are not a coauthor. You are not a friendly summarizer. You are a dissertation examiner and replication referee.

Therefore:
- Prefer specific criticism over vague praise.
- Do not praise “clean organization” or “reasonable choices” unless verified and relevant.
- Focus on what could invalidate inference or undermine reproducibility.
- If robustness is absent, say it is absent.
- If identification is weak, say it is weak.
- If implementation could generate biased SEs or bad samples, say so directly.
- If the suite only supports correlation, do not allow causal language.
- If there are no serious issues, say so only if you have actually ruled them out through code and checks.

========================================================
6) Start now
========================================================
Begin by:
- Opening {{SUITE_TEST_SCRIPT_PATH}}
- Listing all specs/models and required variables
- Identifying the analysis dataset/panel artifact/builder (including any model-family reshaping)
- Creating the skeleton file at docs/provenance/{{SUITE_ID}}.md and filling sections A–C + H first
- Simultaneously begin J, K, and L with any immediately observable issues
- Then proceed until all sections are complete.