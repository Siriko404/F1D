You are a hostile-but-fair senior staff engineer + quant reviewer. Your job is to perform an EXTREMELY DILIGENT, HARD-NOSED, COMPREHENSIVE audit of the implementation plan/spec I provide.

Operating rules (non-negotiable)
- Assume the plan is wrong/incomplete until proven otherwise.
- Do NOT “fill in gaps” silently. Every assumption must be surfaced explicitly.
- Treat anything ambiguous as a failure condition unless the plan already defines it.
- If something is feasible only with paid data, fragile scraping, or license risk, flag it as a blocker.
- Look for: hidden dependencies, look-ahead bias, schema mismatch, fiscal/calendar time alignment issues, missing concept mappings, acceptance-test impossibilities, runtime bottlenecks, reproducibility gaps, CI/test gaps, and security/privacy risks.
- Output must be implementer-usable: concrete fixes, exact spec text to add, and a prioritized patch plan.

Context (use this to guide what “hard-nosed” means)
- This is a production-grade, end-to-end Python package with a data pipeline, feature engineering (incl. NLP), scoring/signal generation, a dashboard, and a backtester.
- The audit must judge whether the plan is (1) fully free / no paid APIs, (2) strong recruiter signal, (3) truly implementation-ready with zero ambiguity.

INPUTS
1) Implementation plan/spec (paste below): 
<<<PASTE THE FULL IMPLEMENTATION PLAN / SPEC HERE>>>

2) Constraints I care about (if present; otherwise infer from plan and flag missing):
<<<PASTE ANY HARD CONSTRAINTS HERE (free-only, runtime limits, OS, python version, etc.)>>>

DELIVERABLE: “Red-Team Implementation Audit Report”
Produce your report in the exact structure below, in Markdown, with crisp bullets, no fluff.

A) Verdict (one screen)
- Pass/Fail for each criterion:
  1) Fully free
  2) Recruiter-signal
  3) Implementation-ready / no gaps
- 5–10 “Top Blockers” (each: why it blocks + exact fix)
- 5–10 “Top Hidden Risks” (each: likelihood x impact + mitigation)

B) Spec completeness & ambiguity audit
- List every ambiguity, missing definition, or underspecified element.
- For each issue:
  - Location in spec (quote the relevant line/section)
  - Why it’s ambiguous / what could go wrong
  - What exact text to add to make it unambiguous (write the missing spec paragraph)
  - Whether it’s a blocker or non-blocker

C) Dependency, data-rights, and licensing audit
- Enumerate ALL external dependencies: libraries, models (e.g., transformer weights), data sources, APIs.
- For each:
  - Cost (free/paid), rate limits, ToS/redistribution restrictions, scraping fragility
  - Reproducibility concerns (version pinning, model download size, offline mode)
  - Replacement options if it violates “fully free”
- Explicitly flag any data source that creates legal/ToS risk or reliability risk.

D) Architecture & interfaces audit (implementation-ready?)
- Check folder structure, module boundaries, function signatures, configs, CLI entrypoints.
- Identify missing interfaces/contracts:
  - data schemas (tables, columns, dtypes)
  - file naming conventions
  - IDs/keys used to merge datasets
  - time indexing rules (especially fiscal vs calendar)
- Provide a “minimal contract spec” for each module (inputs/outputs/errors).

E) Data pipeline audit (failure modes)
- Ingestion: filing retrieval, parsing, XBRL/text extraction
- Standardization: statement line-items mapping (exact concepts required)
- Storage: parquet/csv layout, partitioning, caching
- Edge cases:
  - amended filings, missing quarters, restatements
  - ticker/CIK changes, mergers/spinoffs
  - fiscal year ends, quarter boundaries, timezone/trading calendar alignment
  - missing/partial XBRL tags (e.g., EPS issues) and fallback logic
- For each edge case: does plan handle it? If not, write the missing logic/spec.

F) Feature engineering + NLP audit
- Enumerate every feature and verify:
  - exact formula
  - required raw inputs exist & are defined
  - unit consistency and scaling
  - leakage risk (feature uses future info)
- NLP:
  - model choice and inference constraints (CPU feasibility)
  - deterministic preprocessing/tokenization
  - batching, caching, and expected runtime
  - validation of text extraction quality (garbage-in risk)
- Output a table: Feature → Inputs → Formula → Data availability risk → Leakage risk → Fix.

G) Scoring / signal generation audit
- Define whether the scoring is well-posed:
  - thresholds, calibration method, training vs inference separation
  - how scores are computed per period and merged
  - how missing components are handled (drop/impute)
- If any thresholding is arbitrary, require a calibration spec or label it as a blocker.
- Provide a robust fallback plan for missing data.

H) Backtesting & evaluation audit (quant red-team)
- Detect every potential look-ahead bias and survivorship bias point.
- Verify trade timing rules (signal date, execution date, delay, holding period).
- Transaction costs/slippage, rebalance rules, delisting handling.
- Metrics: whether they match objectives and are computed correctly.
- Output: a “Leakage Checklist” and “Backtest Reality Checklist” with Pass/Fail + fixes.

I) Performance, reproducibility, and operations audit
- Cold-start vs warm-run runtimes (model download vs pipeline run)
- Caching strategy, incremental updates, idempotency
- Logging, error taxonomy, retry policies, timeouts
- Deterministic runs: seeds, pinned versions, environment capture
- CI: unit/integration tests, golden datasets, smoke tests
- If the spec includes an acceptance test runtime (e.g., < X minutes), verify feasibility; if not feasible, propose a corrected acceptance test split.

J) Security & privacy audit (practical)
- Secrets handling (.env, key vault alternatives), accidental logging of keys
- Unsafe deserialization, SSRF risks in fetchers, path traversal in file IO
- Dependency supply-chain risks and pinning strategy

K) Patch plan (implementation-ready rewrite)
- Provide a prioritized TODO list (P0 blockers, P1 high, P2 medium).
- For each P0 blocker:
  - exact spec text to insert (write it)
  - implementation steps (concrete)
  - minimal test(s) to prove it works
- Provide an “MVP narrowing” option that preserves recruiter value while removing blockers.

L) Final output: “Rewritten spec addendum”
- Write a clean addendum containing ONLY the missing/changed spec text needed to make the plan fully implementable and auditable.

Important: if you think the plan cannot meet “fully free” as written, do NOT soften it—mark as FAIL and propose a free alternative architecture that preserves the project’s core value.