================================================================================
PROMPT: Adversarial Audit of Provenance Document for Suite {{SUITE_ID}}
================================================================================

You are a hostile auditor. Your job is to verify that the provenance document
at docs/provenance/{{SUITE_ID}}.md was created correctly, completely, and
accurately by checking EVERY claim against the actual code.

You have two reference documents:
  (1) The PROMPT that was used to create the provenance doc:
      docs/Prompts/Suite Provenance Doc.txt
  (2) The PROVENANCE DOC that was produced:
      docs/provenance/{{SUITE_ID}}.md

Your job is to audit (2) against BOTH (1) and the actual codebase.

================================================================================
INPUTS (fill before running)
================================================================================

Suite ID:            {{SUITE_ID}}
Runner script:       src/f1d/econometric/run_{{RUNNER_NAME}}.py
Panel builder:       src/f1d/variables/build_{{BUILDER_NAME}}_panel.py
Provenance doc:      docs/provenance/{{SUITE_ID}}.md
Creation prompt:     docs/Prompts/Suite Provenance Doc.txt

================================================================================
NON-NEGOTIABLE RULES
================================================================================

(1) ASSUME EVERYTHING IN THE PROVENANCE DOC IS WRONG until you prove it correct.
    Do not give benefit of the doubt. Do not skim. Read every line.

(2) MANUAL VERIFICATION ONLY.
    - Read the provenance doc line by line.
    - For every factual claim, open the referenced source file and verify.
    - Use grep for targeted searches. Run python one-liners to check counts.
    - Do NOT write scripts that auto-validate. Check each claim by hand.

(3) CODE IS THE ONLY SOURCE OF TRUTH.
    - If the provenance doc says X and the code says Y, the doc is WRONG.
    - If the provenance doc says X and the code is ambiguous, mark UNVERIFIABLE.
    - If the provenance doc says X and you cannot find supporting code, mark UNSUPPORTED.

(4) THE CREATION PROMPT IS THE SPECIFICATION.
    - Every section required by the creation prompt MUST exist in the provenance doc.
    - Every quality gate in the creation prompt MUST be met.
    - Missing sections = FAIL. Incomplete tables = FAIL. Placeholder text = FAIL.

================================================================================
AUDIT PHASE 1: STRUCTURAL COMPLETENESS
================================================================================

Read docs/Prompts/Suite Provenance Doc.txt to extract the required sections.
Then read docs/provenance/{{SUITE_ID}}.md and check:

For EACH required section (A through L), answer:

| Section | Required by Prompt | Present in Doc | Complete | Notes |
|---------|-------------------|----------------|----------|-------|
| A. Suite Identity | Yes | ? | ? | |
| B. Model Specification | Yes | ? | ? | |
| B1. Regression Equation | Yes | ? | ? | |
| B2. Dependent Variable(s) | Yes | ? | ? | |
| B3. Independent Variable(s) | Yes | ? | ? | |
| B4. Control Variables | Yes | ? | ? | |
| B5. Fixed Effects | Yes | ? | ? | |
| B6. Standard Errors | Yes | ? | ? | |
| B7. Hypothesis Test | Yes | ? | ? | |
| C. Spec Register | Yes | ? | ? | |
| D. Sample Construction | Yes | ? | ? | |
| D1. Population | Yes | ? | ? | |
| D2. Exclusion Criteria | Yes | ? | ? | |
| D3. Sample Counts per Spec | Yes | ? | ? | |
| E. Variable Dictionary | Yes | ? | ? | |
| F. Data Pipeline | Yes | ? | ? | |
| F1. Dependency Chain | Yes | ? | ? | |
| F2. Data Engines | Yes | ? | ? | |
| F3. Merge Operations | Yes | ? | ? | |
| G. Outputs | Yes | ? | ? | |
| G1. Stage 3 Outputs | Yes | ? | ? | |
| G2. Stage 4 Outputs | Yes | ? | ? | |
| G3. Summary Statistics | Yes | ? | ? | |
| H. Outlier/Missing Treatment | Yes | ? | ? | |
| I. generate_all_tables Entry | Yes | ? | ? | |
| J. Reproduction Commands | Yes | ? | ? | |
| K. Model-Family Addendum | Yes | ? | ? | |
| L. Known Issues | Yes | ? | ? | |

For each section marked incomplete or missing, record exactly what is missing.

================================================================================
AUDIT PHASE 2: FACTUAL ACCURACY — SECTION A (Suite Identity)
================================================================================

Read Section A of the provenance doc. For each field:

A-1. Suite ID
     - Does it match the actual suite?
     VERIFY: trivial.

A-2. Title
     - Open the runner. Read the docstring or caption.
     - Does the provenance doc title match?

A-3. Hypothesis
     - Read the runner docstring's hypothesis statement.
     - Does the provenance doc's hypothesis match?

A-4. Direction (tail test)
     - Open the runner. Find the tail test implementation.
     - Grep for `p_two / 2`, `p_one`, or equivalent.
     - Does the provenance doc's direction match the code?

A-5. Model Family
     - Open the runner. Find what estimator class is instantiated.
     - Grep for `PanelOLS`, `CoxTimeVaryingFitter`, `Logit`, `smf.ols`, etc.
     - Does the provenance doc's model family match?

A-6. Estimator
     - Exact class name. Verify against import statements in the runner.

A-7. Unit of Observation
     - Read the panel builder. What does each row represent?
     - Does the doc's claim match?

A-8. Panel Index
     - Find `set_index(...)` in the runner.
     - Does the doc's panel index match the actual code?

A-9. Columns (number of model specs)
     - Count entries in MODEL_SPECS (or equivalent) in the runner.
     - Does the doc's column count match?

A-10. Runner and Panel Builder paths
      - Do the file paths exist on disk?

Record: PASS or FAIL for each, with evidence.

================================================================================
AUDIT PHASE 3: FACTUAL ACCURACY — SECTION B (Model Specification)
================================================================================

B1-CHECK: Regression Equation
    - Read the runner's actual regression formula construction.
    - Does the provenance doc's equation include every term that appears in code?
    - Does the provenance doc's equation exclude terms that do NOT appear in code?
    - For moderation suites: is the interaction term correctly documented?
    - For Cox PH: is the hazard function correctly written?

B2-CHECK: Dependent Variable(s)
    - For each DV listed in the provenance doc:
      a) Grep the runner for the DV column name. Does it appear as the LHS?
      b) Open the builder/engine that creates it. Does the formula match?
      c) Is the timing (t, t+1, lag) correct?
    - Are ANY DVs used in the runner MISSING from the provenance doc?

B3-CHECK: Independent Variable(s)
    - For each IV listed:
      a) Grep the runner for the IV column name. Is it in the regression formula?
      b) Is the source engine correct?
      c) For centered IVs: is the centering documented? Is it correct?
    - Are ANY IVs used in the runner MISSING from the provenance doc?

B4-CHECK: Control Variables
    - Read BASE_CONTROLS and EXTENDED_CONTROLS from the runner.
    - Does every control in the code appear in the provenance doc?
    - Does every control in the provenance doc appear in the code?
    - Is Lagged_DV documented? Is its construction correct?
    - Are there dynamic controls (e.g., H11's Pres auto-add)? Are they documented?

B5-CHECK: Fixed Effects
    - Find the FE specification in the runner (entity_effects, time_effects,
      other_effects, strata, C(year), etc.)
    - Does the provenance doc's FE table match?
    - Is the time FE column correct (cal_yr vs cal_yr_qtr vs year vs fyearq_int)?
    - For Cox PH: are strata correctly documented?

B6-CHECK: Standard Errors
    - Find cov_type and cluster arguments in the runner.
    - Does the provenance doc match?

B7-CHECK: Hypothesis Test
    - Find the p-value computation in the runner.
    - Is the direction correct?
    - Is the conversion from two-tailed to one-tailed correct?
    - For mixed-tail suites: is each variable's tail correctly documented?

Record: PASS or FAIL for each, with evidence.

================================================================================
AUDIT PHASE 4: FACTUAL ACCURACY — SECTION C (Spec Register)
================================================================================

- Count rows in the spec register table.
- Does it match the number of MODEL_SPECS in the runner?
- For each row, verify:
  a) DV matches the spec's actual DV
  b) Entity FE matches (industry vs firm)
  c) Time FE matches (cal_yr vs cal_yr_qtr)
  d) Controls set matches (base vs extended)
- Are any specs in the code MISSING from the table?
- Are any specs in the table NOT in the code?

================================================================================
AUDIT PHASE 5: FACTUAL ACCURACY — SECTION D (Sample Construction)
================================================================================

D1-CHECK: Population
    - Are the stated totals (calls, firms, year range) plausible?
    - Cross-reference against project scope if known (112,968 calls, 2,429 firms, 2002-2018).

D2-CHECK: Exclusion Criteria
    - Read the runner's sample filtering code step by step.
    - Does the attrition cascade in the provenance doc match the code's filter order?
    - Are the filter descriptions accurate?
    - For Cox PH: is interval construction, capping, event censoring documented?
    - For moderation: is moderator data merge documented?

D3-CHECK: Sample Counts per Spec
    - If N varies across specs, is this documented?
    - Are the counts plausible?

================================================================================
AUDIT PHASE 6: FACTUAL ACCURACY — SECTION E (Variable Dictionary)
================================================================================

This is the MOST IMPORTANT section to audit. Check EVERY row:

For each variable in the dictionary:
    a) Does the variable name match what the code uses? (exact string match)
    b) Is the formula correct? Open the builder or engine and verify.
    c) Is the source engine correct?
    d) Is the winsorization claim correct?
    e) Is the timing correct?

Then check COMPLETENESS:
    f) Read the runner's MODEL_SPECS. List EVERY variable referenced.
    g) Read BASE_CONTROLS and EXTENDED_CONTROLS. List every variable.
    h) Is EVERY variable from (f) and (g) in the dictionary?
    i) Are FE columns (gvkey, ff12_code, cal_yr, cal_yr_qtr) in the dictionary?

Record: PASS or FAIL for each variable, with evidence for failures.

================================================================================
AUDIT PHASE 7: FACTUAL ACCURACY — SECTIONS F, G, H
================================================================================

F-CHECK: Data Pipeline
    - Is the dependency chain complete? (raw -> engine -> builder -> runner -> table)
    - Are all engines listed that this suite actually uses?
    - Are all merges in the panel builder documented with correct keys and types?

G-CHECK: Outputs
    - Open the runner. Find every file-write operation (to_csv, to_parquet, open().write, etc.)
    - Is every output file listed in the provenance doc?
    - Is any file listed in the doc NOT actually written by the code?

H-CHECK: Outlier/Missing Treatment
    - Is winsorization correctly described (level, scope, when applied)?
    - Is the missing data policy correct (complete-case deletion)?
    - Are any transformations (log, z-score, centering) documented?
    - Grep the runner for `inf`, `replace`, `dropna`, `fillna` — are these documented?

================================================================================
AUDIT PHASE 8: FACTUAL ACCURACY — SECTION I (Table Generator Entry)
================================================================================

- Open outputs/generate_all_tables.py
- Find the SUITES entry for this suite
- Compare EVERY field against what the provenance doc claims:
  a) "id" matches?
  b) "tail" / "hyp_dir" / "key_tails" matches?
  c) "cols" matches?
  d) "dvs" matches?
  e) "key_vars" matches?
- Does the provenance doc flag any discrepancies?
- If the suite has NO entry in generate_all_tables.py, does the doc say so?

================================================================================
AUDIT PHASE 9: FACTUAL ACCURACY — SECTION K (Model-Family Addendum)
================================================================================

- Which model family was identified in Section A?
- Is the CORRECT subsection of K filled out (K1 for PanelOLS, K2 for Cox PH, etc.)?
- Are the OTHER subsections marked N/A or omitted?
- For the filled subsection, verify each claim:

  PanelOLS (K1):
  - entity_effects: verify against code
  - other_effects: verify against code
  - drop_absorbed: verify against code

  Cox PH (K2):
  - Time origin: verify against code
  - Event definition: verify against code
  - Censoring rules: verify against code
  - Ties method: verify against code
  - Strata: verify against code
  - Concordance computation: verify against code
  - EPV rules: verify against code

  (Adapt for other model families)

================================================================================
AUDIT PHASE 10: QUALITY GATE CHECKLIST
================================================================================

The creation prompt specifies 10 quality gates. Verify each:

| # | Quality Gate | Met? | Evidence |
|---|-------------|------|----------|
| 1 | Every variable in every regression spec appears in Variable Dictionary with explicit formula and source engine | ? | |
| 2 | The model equation matches what the code actually estimates | ? | |
| 3 | The specification register accounts for every model column | ? | |
| 4 | The attrition cascade has row counts for each filter step | ? | |
| 5 | The tail test direction matches between runner code and generate_all_tables.py | ? | |
| 6 | The FE specification matches between docstring, code, and this document | ? | |
| 7 | Every merge in the panel builder is documented with join keys and type | ? | |
| 8 | The output file list matches what the runner actually writes | ? | |
| 9 | The model-family addendum is filled for the correct family only | ? | |
| 10 | Any claim marked [UNVERIFIED] has an explanation of what blocks verification | ? | |

================================================================================
AUDIT PHASE 11: CROSS-REFERENCE CONSISTENCY
================================================================================

Check internal consistency WITHIN the provenance doc:

1. Do the DVs in Section B2 match the DVs in Section C (spec register)?
2. Do the DVs in Section C match the DVs in Section I (table generator entry)?
3. Do the controls in Section B4 match the variables in Section E (dictionary)?
4. Does the column count in Section A match the rows in Section C?
5. Does the column count in Section A match "cols" in Section I?
6. Does the tail direction in Section A match Section B7 match Section I?
7. Does the FE in Section B5 match Section C match Section K?
8. Does the panel index in Section A match the set_index in Section K?

Flag any internal contradictions.

================================================================================
OUTPUT REQUIREMENTS
================================================================================

You MUST write your complete audit report to a file. Do NOT only return it
in your response. The audit report must be saved to:

    docs/provenance/Audits/{{SUITE_ID}}_audit.md

Create the docs/provenance/Audits/ directory if it does not exist.

The file must contain your FULL analysis — every phase, every check, every
verification result, every piece of evidence. Do not summarize or abbreviate.
A reader of the audit file must be able to see exactly what you checked,
what the provenance doc claimed, what the code actually says, and whether
each claim passed or failed.

Structure the audit report as follows:

### AUDIT SUMMARY

| Category | Total Checks | Passed | Failed | Score |
|----------|-------------|--------|--------|-------|
| Structural Completeness (Phase 1) | | | | |
| Suite Identity (Phase 2) | | | | |
| Model Specification (Phase 3) | | | | |
| Spec Register (Phase 4) | | | | |
| Sample Construction (Phase 5) | | | | |
| Variable Dictionary (Phase 6) | | | | |
| Pipeline/Outputs/Treatment (Phase 7) | | | | |
| Table Generator Entry (Phase 8) | | | | |
| Model-Family Addendum (Phase 9) | | | | |
| Quality Gates (Phase 10) | | | | |
| Cross-Reference Consistency (Phase 11) | | | | |
| **TOTAL** | | | | |

### VERDICT

One of:
- **PASS**: All checks pass. Document is accurate and complete.
- **PASS WITH NOTES**: Minor issues that do not affect accuracy. List them.
- **FAIL — INCOMPLETE**: Missing required sections or content. List what's missing.
- **FAIL — INACCURATE**: Factual errors found. List every error with evidence.
- **FAIL — BOTH**: Missing content AND factual errors.

### FAILURES (detailed)

For each failure:
| Phase | Check | Provenance Doc Claims | Actual Code Says | Severity | Fix Required |
|-------|-------|----------------------|-----------------|----------|-------------|

### CORRECTIONS REQUIRED

Numbered list of exact edits needed to bring the provenance doc to PASS status.
Each correction must specify:
- Which section of the provenance doc to edit
- What the current (wrong) text says
- What it should say instead
- The code reference proving the correction

================================================================================
BEGIN
================================================================================

Start by:
1. Read the creation prompt (docs/Prompts/Suite Provenance Doc.txt) to understand
   what was required
2. Read the provenance doc (docs/provenance/{{SUITE_ID}}.md) end to end
3. Begin Phase 1 (structural completeness) — check every required section exists
4. Proceed through Phases 2-11 sequentially
5. Write the COMPLETE audit report to docs/provenance/Audits/{{SUITE_ID}}_audit.md
   - Create the directory if needed (mkdir -p docs/provenance/Audits)
   - The file must contain ALL phase results, ALL verification evidence,
     ALL tables, the summary, verdict, failures, and corrections
   - This is your PRIMARY deliverable — do NOT skip writing the file

Do not skip any phase. Do not assume any claim is correct without verification.
