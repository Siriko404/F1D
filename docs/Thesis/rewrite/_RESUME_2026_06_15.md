# RESUME STATE — 2026-06-15 (compaction-safe)

## Status: the fix pass is COMPLETE. No open decisions.
Live draft = `docs/Thesis/thesis_draft_uottawa.tex`. Compiles clean (0 undefined refs/citations,
no write error). All work committed + pushed to origin/debug/campello-did-supervisor-interrogation.
HEAD = local == remote (run `git log -1` for the current SHA).

## DONE this pass (commits 548e26ea → 33847e23)
- All 5 MAJOR audit findings (M1 thread §4.2 through front/back matter; M2 drop DWZ self-contradiction
  in §4.2 PARA3; M3 Gelman-Stern -> per-component + "no between-component test" in §4.2 PARA4;
  M4 Nickell disclosure in §2.4 P5, empirical-first; M5 completed-only-POST disclosure in §3.3 + §4.3 pointer).
- §4.3 (withdrawal-as-resolution) + §4.4 (static-FE) robustness: ledgers + prose (4 paras each, author voice)
  + two full tables (tab:empire_drop_resolution, tab:empire_drop_staticfe) built from summary.json.
- Minors: L1/W1 causal verbs, L2 "independent", L3 "public"=gap, H1 "four parts", H2 call-level->call-varying,
  C4 DWZ-repl ground-truth log, W2 cash/stock >=50% overlap (212 deals disclosed), W3 MNPI-at-e=-1 caveat,
  C6 thewissen purpose-gloss trimmed.
- Removed the "Empire-Building Run-Up Test -- Regression Specification" page (stale framing); moved the
  control-variable formulas to a new Appendix II; simplified Appendix I to the cash-scrutiny measure + word lists.
- Standardized ALL 14 table notes to one format (extract_draft_tables.py NOTE_BY_LABEL + inject_note).

## RESOLVED -- run-up table significance tail (2026-06-15, user chose two-tailed)
- tab:empire_building_did treatment stars are now TWO-tailed, matching §2.4 P5, the abstract/intro, and the
  other three Section 3 timing tables. Changed: gen_empire_did_table.py write_tex uses p2 (was p1) for the
  treatment cell + note now "(two-tailed)"; extract_draft_tables.py NOTE_BY_LABEL[tab:empire_building_did]
  now "(two-tailed)". Re-ran the script, re-extracted, recompiled (0 undefined refs/citations, no write error).
- Headline cash UncResCEO run-up 0.0461 SURVIVES two-tailed (p2=0.007 -> ***); CashRatio *** and
  HighCashScrutiny ** also survive; the stock placebo arm stays null. No key-cell star loss.
- The validity/spread tables (h11_prisk, h24_us_epu, h24b_global_epu, cash_scrutiny_validity) KEEP their
  one-tailed-IV / two-tailed-controls note by design (directional IV) -- not changed.

## DEFERRED (optional, not defects) -- C2/C3 citation hardening
- The DWZ price/volume-null and BGT 25-day-window claims (§4.2) rest on our own NotebookLM captures, which carry
  no admissible structured cited_text span (memory flags NLM captures as unverified). Airtightening = a
  primary-source PDF span capture (a page/line quote), NOT another query. Substance not contradicted; fine to ship.

## PIPELINES (how prose/tables reach the draft -- NEVER hand-author .tex prose)
- Body §3/§4: section{3.1..4.4}_paragraph_ledger.json -> `tmp/build_sec34_body.py` -> sec34_body_from_ledgers.tex.
- Intro/abstract/conclusion: section{1,abstract,5}_paragraph_ledger.json -> `tmp/build_introconcl_body.py`.
- §2 is INLINE in uottawa.tex (no assembler): edit the section2.x ledger, then mirror verbatim into the .tex
  (e.g., `tmp/push_sec24_p5.py` for §2.4 P5); keep ledger == .tex.
- Tables: bible `docs/Draft/thesis_tables.tex` + fragments (docs/Draft/_*.tex from the regression scripts)
  -> `tmp/extract_draft_tables.py` (FRAGMENT + THESIS_TABLES + NOTE_BY_LABEL) -> _tables_from_bible.tex.
- Robustness numbers: `outputs/econometric/robustness_drop_sec43_44/2026-06-15_070702/summary.json` (AUTHORITATIVE; gitignored).
- Compile: kill Acrobat (it locks the PDF), then `pdflatex -interaction=nonstopmode thesis_draft_uottawa.tex` x3
  from docs/Thesis. Verify the log: 0 undefined refs/citations, no "I can't write".

## REGISTER LOCKS (do not relitigate)
- The §4.2 channel split = two per-component facts ("consistent with" a difference), NEVER a tested
  between-component difference; the "no between-component test was run" sentence stays.
- §4.3 = "consistent with" not "robust" (adds only +89 fq / +1 firm). §4.4 = lag stays in the MAIN spec;
  the static spec SIDESTEPS the Nickell bias (no lagged DV), it does not "fix" it; timing preserved, magnitudes larger (level vs change).
- Author voice everywhere; NEVER write "audit"/"referee"/"reader"/"one might worry" in thesis prose
  (the work has NOT been refereed). NO em-dash/en-dash in prose (tables may use --- for not-applicable).
