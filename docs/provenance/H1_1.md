# H1.1 Suite Provenance -- TNIC-Moderated Cash Holdings

> **Version note (v2, 2026-03-21):** This document was revised following a red-team audit (`docs/provenance/H1_1_red_team.md`). Key changes: (1) updated to reflect IV mean-centering now implemented in code (`Manager_Pres_Unc_c = IV - mean`); (2) coefficients updated to match the centered run `2026-03-19_001554`; (3) multicollinearity issue J1 and centering issue J5 marked as resolved; (4) corrected the falsified SE-reduction prediction; (5) all code line-number references updated to current working-tree version; (6) refocused blocking issues on marginal interaction significance, lack of robustness, and Moulton problem.

**Suite ID:** H1.1
**Document version:** v2
**Generated:** 2026-03-19 (revised 2026-03-21)
**Verified against run:** `2026-03-19_001554` (post-centering; supersedes `2026-03-18_235755`)
**Panel builder run:** `2026-03-18_184753` (reuses H1 panel)
**Auditor mode:** Independent code-and-output verification, referee-grade

---

## A. Suite Overview

### A.1 Identity

| Field | Value |
|-------|-------|
| Hypothesis suite name/ID | H1.1 -- TNIC-Moderated Speech Uncertainty and Cash Holdings |
| Research question | Does product-market similarity (TNIC3TSIMM, Hoberg & Phillips JPE 2016) moderate the relationship between managerial presentation uncertainty and corporate cash holdings? |
| Estimation unit | Individual earnings call (`file_name`) |
| Primary keys | `gvkey` (entity) + `fyearq_int` (time index, Compustat fiscal year) |
| Prediction | Positive main effect of uncertainty on cash (b1 > 0); interaction b3 tested two-tailed |
| Parent suite | H1 (Cash Holdings) -- Manager_Pres_Uncertainty_pct significant under Industry+FY FE |

### A.2 Model Family and Estimator

| Field | Value |
|-------|-------|
| Model family | Panel FE (OLS with absorbed effects); single FE configuration |
| Estimator | `linearmodels.panel.PanelOLS`, `drop_absorbed=True`, `check_rank=False` |
| FE config | Industry FE (FF12, absorbed via `other_effects`) + Fiscal Year FE (`time_effects=True`); no entity FE |
| Within transformation | Automatic via PanelOLS (demeaning within absorbed groups) |
| Solver | Default `linearmodels` OLS solver |
| Weighting | None |
| Regularization | None |

### A.3 Outcome / Target Definition

| Field | Value |
|-------|-------|
| Dependent variables (2) | `CashHoldings` (contemporaneous, t) and `CashHoldings_lead` (next-fiscal-year, t+1) |
| Construction | `cheq / atq` from Compustat. Lead: latest-call-per-firm-fiscal-year aggregation, shifted by -1 within gvkey, consecutive-year validated. Inherited from H1 panel. |
| Winsorization | 1%/99% per fiscal year at Compustat engine level |
| Missing policy | Listwise deletion at Stage 4 (`prepare_regression_data()` lines 264-300) |

### A.4 Main RHS, Moderator, and Controls

**Key independent variable (1, per moderation memo):**

| Variable | Description |
|----------|-------------|
| `Manager_Pres_Uncertainty_pct` | All managers' uncertainty language in presentation section, % of words |

Source: `run_h1_1_cash_tsimm.py` line 70.

**Moderator:**

| Variable | Raw Name | Transformation | Description |
|----------|----------|---------------|-------------|
| `z_log_tnic3tsimm` | `tnic3tsimm` | log then z-score on Main sample | Hoberg-Phillips TNIC3 total product similarity |

Source: `run_h1_1_cash_tsimm.py` lines 78-79, `transform_moderator_and_center_iv()` lines 199-247.

**Interaction term:**

| Variable | Construction |
|----------|-------------|
| `MgrPresUnc_x_zlogTSIMM` | `Manager_Pres_Unc_c * z_log_tnic3tsimm` (uses mean-centered IV) |

Source: `prepare_regression_data()` line 284.

**Control variables -- Extended (11):**

| Variable | Source |
|----------|--------|
| `BookLev` | (dlcq + dlttq) / atq from Compustat |
| `Size` | log(atq) from Compustat |
| `TobinsQ` | (cshoq*prccq + dlcq + dlttq) / atq from Compustat |
| `ROA` | iby_annual / avg_assets from Compustat |
| `CapexAt` | capxy_Q4_annual / atq_lag from Compustat |
| `DividendPayer` | 1(dvy_annual > 0) from Compustat |
| `OCF_Volatility` | Rolling 5-year std of (oancfy / atq_lag) |
| `SalesGrowth` | (saley_t - saley_{t-1}) / abs(saley_{t-1}) |
| `RD_Intensity` | xrdq / atq from Compustat |
| `CashFlow` | oancfy / avg_assets from Compustat |
| `Volatility` | Annualized std of daily stock returns from CRSP |

Source: `run_h1_1_cash_tsimm.py` lines 72-76.

**Additional control (Col 2 only):**

| Variable | Purpose |
|----------|---------|
| `CashHoldings` (contemporaneous) | Lagged DV control to address AR(1) persistence (median=0.595) |

Source: `MODEL_SPECS` line 87.

**Sample:** Main only (FF12 codes not in {8, 11}). Source: `filter_main_sample()` line 255.

**Minimum calls filter:** >= 5 calls per firm after complete-case filtering. Source: `MIN_CALLS_PER_FIRM = 5` at line 83.

### A.5 Fixed Effects + Variance Estimator

| Field | Value |
|-------|-------|
| Industry FE (both cols) | FF12 industry classification, absorbed via `other_effects` parameter (10 groups in Main sample) |
| Time FE (both cols) | `fyearq_int` via `time_effects=True` |
| Entity FE | **Not used** -- only Industry FE. Design decision: parent H1 effect disappears under Firm FE; only 8.3% of z(log(TSIMM)) variation is within-firm |
| Standard errors | Firm-clustered (`cov_type="clustered"`, `cluster_entity=True`) |
| Hypothesis test (IV) | One-tailed: b1 > 0 (higher uncertainty --> more cash) |
| Hypothesis test (Interaction) | Two-tailed: b3 != 0 |
| One-tailed transform | `p_one = p_two / 2 if beta > 0 else 1 - p_two / 2` (lines 363-364) |
| Stars | `***` p<0.01, `**` p<0.05, `*` p<0.10 |

### A.6 Model-Family Addendum: Two-Model Design

| Col | DV | Extra Controls | Rationale |
|-----|-----|----------------|-----------|
| (1) | CashHoldings_t | None | Contemporaneous association; comparable to H1 cols 3/7 |
| (2) | CashHoldings_lead | CashHoldings_t | Forward-looking + lagged DV to control persistence |

Only Industry+FY FE used (no Firm FE columns) because:
1. Parent effect (Mgr_Pres_Unc on CashHoldings) is significant only under Industry FE in H1.
2. Only 8.3% of z(log(TSIMM)) variance is within-firm (verified: Section I), so Firm FE would absorb nearly all moderator variation.

---

## B. Reproducibility Snapshot

| Field | Value |
|-------|-------|
| Stage 3 command | `python -m f1d.variables.build_h1_cash_holdings_panel` (builds H1 panel, reused by H1.1) |
| Stage 4 command | `python -m f1d.econometric.run_h1_1_cash_tsimm` |
| Stage 3 verified run | `2026-03-18_184753` (112,968 rows x 31 columns) |
| Stage 4 verified run | `2026-03-19_001554` (2/2 regressions completed, 10 output files; supersedes `2026-03-18_235755`) |
| Deterministic | Yes (OLS is deterministic given fixed inputs) |
| Git commit | `8f5e9296fcbedf778455d4b02efa806a377dcf1d` |
| Panel hash | `3aaee0a0196cddc01e93dc10639d5400deaf851e8bf8e7ed38cd585f0b65f1b0` |
| TNIC hash | `68fe0867f9be889ebadc2cd4e03919b0d429d7c2e96a9965c844c2e72ef76870` |

**Commands to reproduce from scratch:**

```bash
# Stage 1-2: Prerequisites (one-time)
python -m f1d.sample.assemble_manifest
python -m f1d.text.build_linguistic_variables

# Stage 3: Build H1 panel (reused by H1.1)
python -m f1d.variables.build_h1_cash_holdings_panel

# Stage 4: Run H1.1 regressions
python -m f1d.econometric.run_h1_1_cash_tsimm
```

**Expected outputs (Stage 4, 10 files):**

| File | Format | Content |
|------|--------|---------|
| `regression_results_col1.txt` | Text | Full PanelOLS summary, Col (1) |
| `regression_results_col2.txt` | Text | Full PanelOLS summary, Col (2) |
| `model_diagnostics.csv` | CSV | 2 rows, betas/SEs/p-values, N, R2 |
| `h1_1_cash_tsimm_table.tex` | LaTeX | 2-column table with stars+SE |
| `summary_stats.csv` / `.tex` | CSV/LaTeX | Descriptive statistics |
| `sample_attrition.csv` / `.tex` | CSV/LaTeX | 4-stage attrition table |
| `report_step4_H1_1.md` | Markdown | Results summary |
| `run_manifest.json` | JSON | Reproducibility metadata with input hashes |

---

## C. Dependency Chain (End-to-End)

### C.1 Raw Data Sources

| # | Source | Location | Format | Role in H1.1 |
|---|--------|----------|--------|---------------|
| 1 | Compustat Quarterly | `inputs/comp_na_daily_all/comp_na_daily_all.parquet` | Parquet | DV, controls |
| 2 | Earnings Call Transcripts | `inputs/Earnings_Calls_Transcripts/` | Parquet | Linguistic IV |
| 3 | Execucomp | `inputs/Execucomp/` | Parquet | Speaker identification (inherited, not used in H1.1 -- only Manager, not CEO) |
| 4 | CRSP-Compustat CCM | `inputs/CRSPCompustat_CCM/CRSPCompustat_CCM.parquet` | Parquet | PERMNO linkage for Volatility |
| 5 | CRSP Daily Stock Files | `inputs/CRSP_DSF/` | Parquet | Volatility control |
| 6 | FF12 SIC Codes | `inputs/FF1248/Siccodes48.zip` | ZIP/TXT | Industry classification |
| 7 | LM Dictionary | `inputs/LM_dictionary/` | CSV | Uncertainty word list |
| 8 | **TNIC3HHI Data** | `inputs/TNIC3HHIdata/TNIC3HHIdata.txt` | Tab-separated | **Moderator** (TSIMM) |

### C.2 Pipeline Flow

```
Stage 1: assemble_manifest.py -> master_sample_manifest.parquet (112,968 calls)
Stage 2: build_linguistic_variables.py -> linguistic_variables_{year}.parquet
Stage 3: build_h1_cash_holdings_panel.py -> h1_cash_holdings_panel.parquet (112,968 x 31)
Stage 4: run_h1_1_cash_tsimm.py:
    (a) Load H1 panel (112,968 rows, 15 columns selected)
    (b) Load TNIC3HHIdata.txt (188,422 rows)
    (c) Left-join TNIC on (_gvkey_int, fyearq_int) -> 98.3% match
    (d) Log-transform + z-score TSIMM on Main sample
    (e) Filter Main sample (88,205 rows)
    (f) For each spec: complete-case + min-calls filter -> regression
```

### C.3 H1.1-Specific Data Loading

The runner at `run_h1_1_cash_tsimm.py` lines 150-157 selects only 15 columns from the H1 panel:

```
gvkey, year, fyearq_int, ff12_code, CashHoldings, CashHoldings_lead,
Manager_Pres_Uncertainty_pct, BookLev, Size, TobinsQ, ROA, CapexAt,
DividendPayer, OCF_Volatility, SalesGrowth, RD_Intensity, CashFlow, Volatility
```

This is narrower than H1 (which loads all 4 IVs). H1.1 uses only 1 IV (Manager_Pres_Uncertainty_pct) per the moderation memo.

---

## D. Data Provenance (Verified)

### D.1 TNIC3HHI Data (Hoberg-Phillips)

| Field | Value |
|-------|-------|
| File | `inputs/TNIC3HHIdata/TNIC3HHIdata.txt` |
| Format | Tab-separated, 4 columns: `gvkey`, `year`, `tnic3tsimm`, `tnic3hhi` |
| Rows | 188,422 (header + 188,422 data rows) |
| Year range | 1988-2023 |
| Unique gvkey | 19,162 |
| Duplicates on (gvkey, year) | **0** (verified) |
| TSIMM range | [1.0000, 143.2506] |
| TSIMM mean | 11.2984 |
| TSIMM skewness (raw) | 2.8145 |
| TSIMM skewness (log) | 1.0635 |
| TSIMM = 1 (no rivals in TNIC3) | 10,650 firm-years (5.7% of TNIC data) |
| Reference | Hoberg & Phillips, "Text-Based Network Industries and Endogenous Product Differentiation," JPE 2016 |
| Data source URL | http://hobergphillips.tuck.dartmouth.edu |

**TSIMM interpretation:** Total similarity for firm i is the sum of pairwise product-space similarities between firm i and all other firms in its TNIC3 peer group. TSIMM = 1 means the firm has no TNIC3 peers above the threshold -- it operates in a unique product space.

### D.2 H1 Panel (Inherited)

| Field | Value |
|-------|-------|
| File | `outputs/variables/h1_cash_holdings/2026-03-18_184753/h1_cash_holdings_panel.parquet` |
| Shape | 112,968 rows x 31 columns |
| Unique gvkey | 2,429 |
| Year range | 2000-2019 (fyearq_int) |
| Build script | `src/f1d/variables/build_h1_cash_holdings_panel.py` |
| Full provenance | See `docs/provenance/H1.md` |

### D.3 Merge Overlap (Panel x TNIC)

| Metric | Value |
|--------|-------|
| Full panel TNIC match | 111,090 / 112,968 (98.3%) |
| Main sample TNIC match | 86,807 / 88,205 (98.4%) |
| Unmatched Main calls | 1,398 (1.6%) |
| Size of unmatched (mean log AT) | 7.46 (vs 7.47 for matched -- negligible selection) |

---

## E. Merge & Sample Construction Audit (Verified)

### E.1 TNIC Merge Implementation

Source: `load_and_merge_tnic()` lines 163-194.

| Step | Implementation | Verification |
|------|---------------|--------------|
| 1 | Panel `gvkey` (string, zero-padded) -> `_gvkey_int` via `pd.to_numeric()` | Converts "001234" to 1234 to match TNIC integer gvkey |
| 2 | TNIC columns renamed: `gvkey` -> `_gvkey_int`, `year` -> `fyearq_int` | Aligns join keys |
| 3 | Left merge on `(_gvkey_int, fyearq_int)` | `how='left'` preserves all panel rows |
| 4 | Row count assertion: `len(panel) == before` (line 189) | Prevents fan-out from TNIC duplicates |
| 5 | Drop `_gvkey_int` helper column | Cleanup |

**Merge integrity verified:** TNIC has 0 duplicate (gvkey, year) rows, so the left-merge cannot fan out. The assertion at line 189 is a hard guard.

### E.2 Moderator Transformation and IV Centering

Source: `transform_moderator_and_center_iv()` lines 199-247.

| Step | Operation | Verified Value |
|------|-----------|---------------|
| 1 | Filter to Main sample: `~ff12_code.isin([8, 11])` | 86,807 non-null TSIMM in Main |
| 2 | Drop NaN TSIMM | Computes on non-null only |
| 3 | `log_tsimm_main = np.log(tsimm_main)` | Log transform |
| 4 | `mu = log_tsimm_main.mean()` | **0.7517** (verified independently) |
| 5 | `sd = log_tsimm_main.std()` | **0.8065** (verified independently) |
| 6 | Apply to **full panel**: `(log(TSIMM) - mu) / sd` | z-score uses Main sample params |
| 7 | `z_main.mean() ~ 0`, `z_main.std() ~ 1` | By construction |
| 8 | `iv_mu = iv_main.mean()` (Main sample IV mean) | **0.8631** |
| 9 | `panel[IV_CENTERED] = panel[IV] - iv_mu` | `Manager_Pres_Unc_c` = IV minus Main-sample mean |
| 10 | Verify: `panel.loc[main_mask, IV_CENTERED].mean() ~ 0` | By construction |

**IV centering rationale:** The IV (`Manager_Pres_Uncertainty_pct`) is mean-centered on the Main sample before forming the interaction term. This ensures the moderator's "main effect" coefficient (b2) represents the moderator effect at the mean IV value (0.863), rather than at IV=0 (which is at the boundary of the IV distribution). Centering also reduces Corr(moderator, interaction) from ~0.93 (uncentered) to ~0.12 (centered), improving interpretability. However, centering does NOT change the interaction coefficient, its SE, or its p-value (algebraic invariance of OLS interaction tests to mean-centering).

**Critical design choice:** Both z-score and centering parameters are computed on the Main sample pre-regression, NOT on the regression sample (which is smaller after complete-case deletion). This ensures both models use identical transformed values, but the z-scores are not exactly mean-zero in the regression sample.

### E.3 Stage 4 Sample Attrition -- Verified Run `2026-03-19_001554`

| Stage | N | N Lost | % of Manifest |
|-------|---|--------|---------------|
| Full panel (H1) | 112,968 | 0 | 100.0% |
| Main sample (excl Finance/Utility) | 88,205 | -24,763 | 78.1% |
| TNIC3TSIMM matched | 86,807 | -1,398 | 76.8% |
| After complete-case + min-calls (Col 1) | 76,239 | -10,568 | 67.5% |

Source: `sample_attrition.csv` from run `2026-03-19_001554` (identical to previous run; centering does not change the sample).

**Col 2 attrition (lead DV):**

| Stage | N |
|-------|---|
| Col 1 regression sample | 76,239 |
| Col 2 regression sample (lead requires t+1) | 72,618 |
| Additional loss for lead | -3,621 |

**Attrition comparison with parent H1:** H1.1 Col 1 has N=76,239 vs H1 Col 3 (Industry+FY, Extended) N=54,915. The H1.1 sample is LARGER because H1 uses 4 simultaneous IVs (requiring all 4 non-null, with ~30% CEO missingness as binding constraint), while H1.1 uses only 1 IV (Manager_Pres_Uncertainty_pct at 97.5% coverage). This is a significant sample composition change between parent and child suites.

### E.4 Panel FE Notes

- `entity_effects=False` in PanelOLS constructor (line 344). No firm FE.
- `time_effects=True` (line 345): absorbs fiscal year effects via `fyearq_int`.
- `other_effects=df_panel["ff12_code"]` (line 346): absorbs 10 industry groups in Main sample.
- `drop_absorbed=True` (line 347): handles singleton groups.
- `check_rank=False` (line 348): suppresses rank deficiency warnings.
- No explicit intercept in constructor API (PanelOLS absorbs the grand mean when `time_effects=True` + `other_effects`).

---

## F. Variable Dictionary (Paper-grade)

### F.1 Dependent Variables

Inherited from H1 panel. Full construction chain documented in `docs/provenance/H1.md` Section 6. Summary:

#### F.1.1 `CashHoldings` (contemporaneous, t)

| Field | Value |
|-------|-------|
| Formula | `cheq / atq` |
| Winsorization | 1%/99% per fyearq |
| Matching | `merge_asof` backward (call start_date to Compustat datadate) |
| Summary stats (Main sample) | N=87,990, mean=0.1708, sd=0.1806, range=[0.0000, 0.9938] |

#### F.1.2 `CashHoldings_lead` (forward, t+1)

| Field | Value |
|-------|-------|
| Formula | CashHoldings of latest-call in next fiscal year within gvkey |
| Consecutive-year validation | Lead nulled if fyearq gap > 1 |
| Summary stats (Main sample) | N=82,236, mean=0.1715, sd=0.1748, range=[0.0000, 0.9938] |

### F.2 Key Independent Variable

#### F.2.1 `Manager_Pres_Uncertainty_pct`

| Field | Value |
|-------|-------|
| Paper label | Mgr Pres Uncertainty |
| Speaker filter | `is_manager == True` (any company-side speaker with managerial role keyword or matching employer) |
| Section filter | `context == "pres"` (presentation section) |
| Formula | `sum(Uncertainty_count for Manager pres turns) / sum(total_tokens for Manager pres turns) * 100` |
| Winsorization | 0%/99% upper-only per calendar year |
| Coverage (Main sample) | 86,038 / 88,205 = 97.5% |
| Summary stats (Main sample) | N=86,038, mean=0.8631, sd=0.3526, range=[0.0000, 2.0959] |

Full construction chain: see `docs/provenance/H1.md` Section 6.2.4.

#### F.2.2 `Manager_Pres_Unc_c` (mean-centered IV)

| Field | Value |
|-------|-------|
| Paper label | Mgr Pres Uncertainty (centered) |
| Construction | `Manager_Pres_Uncertainty_pct - mean(Manager_Pres_Uncertainty_pct)` on Main sample |
| Centering mean | 0.8631 (Main sample mean of raw IV) |
| Purpose | Used in regression and interaction formation; ensures moderator "main effect" is interpretable at mean IV |
| Summary stats (Main sample) | N=86,038, mean=0.0000, sd=0.3526 |

Source: `run_h1_1_cash_tsimm.py` line 80 (constant), lines 232-236 (centering logic).

### F.3 Moderator Variable

#### F.3.1 `tnic3tsimm` (raw)

| Field | Value |
|-------|-------|
| Paper label | TNIC3TSIMM (raw) |
| Source | Hoberg-Phillips TNIC3 database (`inputs/TNIC3HHIdata/TNIC3HHIdata.txt`) |
| Unit | Firm-year (sum of pairwise product similarities for firm i) |
| Range in Main sample | [1.0000, 80.1876] |
| Summary stats (Main sample) | N=86,807, mean=3.4005, sd=5.4910, skewness=2.81 |
| TSIMM=1 (no TNIC3 peers) | 4,385 calls (5.0% of Main sample) |

#### F.3.2 `z_log_tnic3tsimm` (transformed)

| Field | Value |
|-------|-------|
| Paper label | z(log(TSIMM)) |
| Transformation | log(tnic3tsimm), then z-scored on Main sample |
| z-score base | mu(log(TSIMM)) = 0.7517, sd(log(TSIMM)) = 0.8065 |
| z-score base sample | 86,807 Main sample calls with non-null TSIMM |
| Summary stats (Main sample) | N=86,807, mean=0.0000, sd=1.0000, range=[-0.9321, 4.5040] |
| Skewness (log, pre-z) | 1.0635 (reduced from 2.81 raw) |
| Within-firm variance share | **8.3%** (verified: only 8.3% of z(log(TSIMM)) variance is within-firm) |

**Note on TSIMM=1:** log(1) = 0, so z(log(1)) = (0 - 0.7517) / 0.8065 = -0.9321, which is the minimum z-score. These are firms with no TNIC3 peers -- they cluster at the floor of the moderator distribution.

### F.4 Interaction Term

#### F.4.1 `MgrPresUnc_x_zlogTSIMM`

| Field | Value |
|-------|-------|
| Construction | `Manager_Pres_Unc_c * z_log_tnic3tsimm` (uses mean-centered IV) |
| Created at | `prepare_regression_data()` line 284 |
| Correlation with z_log_tnic3tsimm | ~0.12 (after IV centering; was 0.9331 before centering) |
| Correlation with centered IV | ~0.11 |

**Note on centering and multicollinearity:** Prior to IV centering, Corr(moderator, interaction) was 0.9331 -- severe multicollinearity. After centering (`Manager_Pres_Unc_c = IV - mean`), this correlation drops to ~0.12. However, the interaction coefficient, SE, and p-value are algebraically invariant to mean-centering in OLS. The SE remained 0.0052 in both centered and uncentered runs. Centering improves interpretability (the moderator coefficient now represents the effect at mean uncertainty) but does not change inference on the interaction.

### F.5 Control Variables (11)

All inherited from H1 panel. Full construction chains in `docs/provenance/H1.md` Sections 6.3-6.4. Winsorization summary:

| Variable | Lower | Upper | Grouping | Binary? |
|----------|-------|-------|----------|---------|
| BookLev | 1% | 99% | fyearq | No |
| Size | 1% | 99% | fyearq | No |
| TobinsQ | 1% | 99% | fyearq | No |
| ROA | 1% | 99% | fyearq | No |
| CapexAt | 1% | 99% | fyearq | No |
| DividendPayer | -- | -- | -- | Yes (not winsorized) |
| OCF_Volatility | 1% | 99% | fyearq | No |
| SalesGrowth | 1% | 99% | fyearq | No |
| RD_Intensity | 1% | 99% | fyearq | No |
| CashFlow | 1% | 99% | fyearq | No |
| Volatility | 1% | 99% | calendar year | No |

### F.6 Identifiers

| Variable | Type | Role in H1.1 |
|----------|------|-------------|
| `gvkey` | String (6 digits, zero-padded) | Entity index for PanelOLS, cluster variable |
| `fyearq_int` | Integer | Time index for PanelOLS, merge key for TNIC |
| `ff12_code` | Integer | Industry FE via `other_effects`, sample filter |
| `file_name` | String | Call-level identifier (not used in regression) |

---

## G. Outliers, Missing Data, Scaling, and Model-Specific Data Rules

### G.1 Moderator Transformation Rationale

Raw TNIC3TSIMM has heavy right skew (2.81) and extreme range [1, 143]. The log transform reduces skewness to 1.06, improving the linear-interaction assumption. Z-scoring centers at zero and scales to unit variance for interpretability: a one-SD increase in log(TSIMM) maps to a one-unit change in the moderator.

### G.2 TSIMM = 1 Handling

5.0% of Main sample calls have TSIMM = 1 (no TNIC3 peers). These are NOT dropped; log(1) = 0 is valid, producing z = -0.9321. These firms form a mass point at the moderator floor. The interaction effect for these firms is partially identified but may be unstable. No sensitivity analysis excluding TSIMM=1 firms is implemented.

### G.3 Inf/NaN Handling

- `prepare_regression_data()` line 279: `df.replace([np.inf, -np.inf], np.nan)` -- all infinities converted to NaN before complete-case deletion.
- TSIMM is always >= 1 in the TNIC data, so `log(TSIMM) >= 0` -- no log-of-zero or negative-log risk.
- Missing TSIMM (1.6% of Main sample) produces NaN in z_log_tnic3tsimm and interaction, causing complete-case dropout.

### G.4 No Additional Winsorization

The moderator (z_log_tnic3tsimm) is NOT winsorized -- neither before nor after the z-score transformation. The max z-score of 4.50 represents a firm with very high product similarity. Whether this constitutes an outlier is debatable. Robustness to trimming at |z| > 3 is not tested.

### G.5 Summary Statistics Scope

Summary statistics (`summary_stats.csv`) are computed on the full Main sample (88,205 calls) AFTER the Industry/Utility filter but BEFORE complete-case deletion and TNIC filtering. The z(log(TSIMM)) shows N=86,807 (TNIC-matched subset of Main). These statistics do NOT exactly match the regression sample (76,239 for Col 1, 72,618 for Col 2).

---

## H. Estimation Spec Register

### H.1 Full Results -- Verified Run `2026-03-19_001554` (centered IV)

| | Col (1) | Col (2) |
|---|---------|---------|
| **DV** | CashHoldings | CashHoldings_lead |
| **FE** | Industry(FF12) + FY | Industry(FF12) + FY |
| **Controls** | Extended (11) | Extended (11) + CashHoldings_t |
| **N (calls)** | 76,239 | 72,618 |
| **N (firms)** | 1,825 | 1,768 |
| **N (firm-years)** | 20,681 | 18,978 |
| **Time periods** | 17 | 17 |
| **R-squared** | 0.3457 | 0.7161 |
| **Within-R2** | -0.0809 | 0.0724 |
| | | |
| **Mgr Pres Unc (centered IV)** | | |
| beta | 0.0074 | 0.0001 |
| SE | 0.0050 | 0.0018 |
| p (two-tailed) | 0.1369 | 0.9544 |
| p (one-tailed) | 0.0685* | 0.4772 |
| | | |
| **z(log(TSIMM))** | | |
| beta | 0.0361 | 0.0070 |
| SE | 0.0034 | 0.0012 |
| p (two-tailed) | 0.0000*** | 0.0000*** |
| | | |
| **Interaction** | | |
| beta | 0.0090 | 0.0001 |
| SE | 0.0052 | 0.0020 |
| p (two-tailed) | 0.0851* | 0.9415 |
| | | |
| **CashHoldings_t** | -- | |
| beta | | 0.7532 |
| SE | | 0.0094 |
| p (two-tailed) | | 0.0000*** |

**Note on centering effects:** The IV coefficient and interaction coefficient/SE/p are identical to the uncentered run (`2026-03-18_235755`), as expected from OLS algebra. The moderator coefficient changed from 0.0283 (uncentered) to 0.0361 (centered) because it now represents the moderator effect at the mean IV value (0.863) rather than at IV=0. The moderator SE decreased from 0.0058 to 0.0034 as the multicollinearity with the interaction was removed.

### H.2 Selected Control Coefficients (Col 1)

| Control | beta | SE | p |
|---------|------|----|---|
| BookLev | -0.1472 | 0.0176 | 0.0000 |
| Size | -0.0134 | 0.0020 | 0.0000 |
| TobinsQ | 0.0293 | 0.0025 | 0.0000 |
| ROA | 0.0241 | 0.0186 | 0.1938 |
| CapexAt | -0.2754 | 0.0345 | 0.0000 |
| DividendPayer | -0.0123 | 0.0049 | 0.0130 |
| OCF_Volatility | 0.0360 | 0.0150 | 0.0164 |
| SalesGrowth | 0.0104 | 0.0041 | 0.0123 |
| RD_Intensity | 1.8685 | 0.1754 | 0.0000 |
| CashFlow | -0.0283 | 0.0252 | 0.2625 |
| Volatility | 0.0002 | 0.0001 | 0.0009 |

### H.3 Key Patterns

1. **Interaction is marginal in Col 1 only:** b3 = 0.0090, p = 0.085 (two-tailed, just outside 5%). This suggests firms in more crowded product markets hold marginally more cash in response to managerial uncertainty language, but the evidence is weak.

2. **Interaction vanishes in Col 2:** b3 = 0.0001, p = 0.94. After controlling for CashHoldings_t (which absorbs b = 0.753), neither the IV nor the interaction have any predictive power for lead cash holdings. This suggests the contemporaneous effect in Col 1 does not persist.

3. **Main IV significance:** Manager_Pres_Uncertainty_pct is marginally significant in Col 1 (p_one = 0.069) but not in Col 2 (p_one = 0.48). This is weaker than the parent H1 result (where p_one < 0.005 in Industry+FY specs), likely because H1.1 requires TNIC match (removing some calls) and the moderator absorbs some variance.

4. **Moderator (z(log(TSIMM))) is highly significant as a direct effect** in both columns (p < 0.001). Firms in more crowded product markets hold more cash (b = 0.036 in Col 1, evaluated at mean IV), consistent with the competitive-threat-precautionary-savings channel (Haushalter et al. 2007; Fresard 2010).

5. **Negative within-R2 in Col 1:** Same as parent H1 -- expected for Industry FE specs. The overall R2 = 0.35 is more meaningful.

---

## I. Verification Log (Commands Ran)

| # | Check | Result | Method |
|---|-------|--------|--------|
| 1 | Panel shape | 112,968 x 31 | `pd.read_parquet().shape` |
| 2 | TNIC row count | 188,422 | `wc -l` on TNIC file |
| 3 | TNIC year range | 1988-2023 | `tnic["year"].min()/.max()` |
| 4 | TNIC unique gvkey | 19,162 | `tnic["gvkey"].nunique()` |
| 5 | TNIC duplicate (gvkey,year) | **0** | `tnic.duplicated(subset=["gvkey","year"])` |
| 6 | TNIC TSIMM range | [1.0000, 143.2506] | `.min()/.max()` |
| 7 | TNIC TSIMM skewness (raw) | 2.8145 | `.skew()` |
| 8 | TNIC TSIMM skewness (log) | 1.0635 | `np.log(tsimm).skew()` |
| 9 | TNIC TSIMM=1 count | 10,650 (5.7%) | `(tsimm==1).sum()` |
| 10 | Panel-TNIC merge: row delta | 0 | `len(merged) - len(panel)` |
| 11 | Main sample TNIC match | 86,807 / 88,205 (98.4%) | `notna().sum()` after merge on Main |
| 12 | z-score params | mu=0.7517, sd=0.8065 | Independent computation on Main sample |
| 13 | Within-firm variance share (z_log_TSIMM) | 8.3% | `groupby("gvkey").std()**2).mean() / total_var` |
| 14 | Corr(z_log_TSIMM, Interaction) | ~0.12 (centered); was 0.9331 (uncentered) | `scipy.stats.pearsonr` |
| 15 | Corr(IV, z_log_TSIMM) | 0.1128 | `scipy.stats.pearsonr` |
| 16 | Unmatched firms: Size diff | 7.46 vs 7.47 (negligible) | Mean log(AT) comparison |
| 17 | Calls per firm-year | mean=3.85, median=4, max=39 | `groupby(["gvkey","fyearq_int"]).size()` |
| 18 | CashHoldings AR(1) | median=0.595, mean=0.550 | `groupby("gvkey")["CashHoldings"].autocorr()` |
| 19 | Col 1 N = 76,239 | Confirmed | `model_diagnostics.csv` and `regression_results_col1.txt` |
| 20 | Col 2 N = 72,618 | Confirmed | `model_diagnostics.csv` and `regression_results_col2.txt` |
| 21 | Col 1 firms = 1,825, firm-years = 20,681 | Confirmed | `regression_results_col1.txt` |
| 22 | Col 1 FE: "Time, Other Effect (ff12_code)", 10 groups | Confirmed | `regression_results_col1.txt` |
| 23 | Col 2 FE: "Time, Other Effect (ff12_code)", 10 groups | Confirmed | `regression_results_col2.txt` |
| 24 | Col 2 CashHoldings_t coefficient = 0.7532 | Confirmed | `regression_results_col2.txt` |
| 25 | Clustered SEs in both cols | Confirmed | "Cov. Estimator: Clustered" in both outputs |
| 26 | One-tailed p transform correct | `p_two/2 if beta>0 else 1-p_two/2` | Code lines 363-364 |
| 27 | Output file count = 10 | Confirmed | `ls` on output directory |
| 28 | Git commit in manifest | `8f5e929...` | `run_manifest.json` |
| 29 | Panel hash matches H1 build | `3aaee0a0...` | `run_manifest.json` |
| 30 | TSIMM=1 in Main sample | 4,385 calls (5.0%) | `(main["tnic3tsimm"]==1).sum()` |
| 31 | TNIC year 2019: 4,127 firms | Confirmed | `(tnic["year"]==2019).sum()` |
| 32 | Panel fyearq_int 2019: 76 calls | Confirmed | Panel year distribution check |

---

## J. Known Issues / Ambiguities

| ID | Severity | Description | Blocks thesis? |
|----|----------|-------------|----------------|
| J1 | ~~High~~ **Resolved** | ~~Severe multicollinearity from non-centered IV.~~ Code now mean-centers IV (`Manager_Pres_Unc_c = IV - mean`). Corr(moderator, interaction) drops from 0.93 to ~0.12. However, the interaction SE (0.0052) and p-value (0.085) are unchanged -- centering improves interpretability but not inference on the interaction, because the OLS interaction test is algebraically invariant to mean-centering. The original audit's prediction of a "2-3x SE reduction" was empirically falsified. | No (resolved) |
| J2 | **High** | **Interaction significance is marginal and fragile:** b3 = 0.009, p = 0.085 (two-tailed) in Col 1. This does NOT meet conventional 5% significance. At 10% two-tailed it is marginal. Vanishes completely in Col 2 (p = 0.94). The moderation hypothesis is not supported at standard thresholds. This is the suite's fundamental problem. | Yes (blocking -- must be honestly reported; no robustness checks corroborate it) |
| J3 | **High** | **Sample composition differs from parent:** H1.1 N=76,239 vs H1 N=54,915 (Industry+FY Extended). H1.1 drops 3 of the 4 IVs and gains ~21k observations. The "parent effect" (Mgr_Pres_Unc significant at p<0.005) is tested on a DIFFERENT sample in the child suite. Ideally the child should replicate the parent effect on the H1.1 sample before testing moderation. | No (but weakens chain of evidence) |
| J4 | **High** (upgraded from Medium-High) | **Moulton problem (double):** Both the DV (CashHoldings, constant within firm-quarter) and the moderator (TSIMM, constant within firm-year) are repeated across calls. With ~3.85 calls per firm-year, the effective N for the moderator and interaction is closer to ~20k firm-years than ~76k calls. Firm-clustered SEs address within-firm correlation but may understate the precision inflation from firm-year-level variables in call-level regressions. With J1 resolved, this is now the primary econometric concern. | Yes (needs firm-year collapse robustness) |
| J5 | ~~Medium~~ **Resolved** | ~~No centering of IV before interaction.~~ IV is now mean-centered (code lines 80, 232-236, 284). The moderator coefficient now represents the effect at mean IV (0.863), which is economically interpretable. | No (resolved) |
| J6 | **Medium** | **Within-firm moderator variation = 8.3%:** Under Industry FE, the moderator retains between-firm variation, so this is not fatal. But it means the interaction is primarily identified from between-firm differences in TSIMM, not from within-firm changes over time. Causal interpretation of the interaction is very limited. | No (but limits causal claims) |
| J7 | **High** (upgraded from Medium) | **No robustness checks:** No alternative moderator (e.g., TNIC3HHI), no subsample splits (high vs low TSIMM), no trimming of TSIMM=1 firms (5% mass point), no Firm FE comparison, no firm-year collapse, no alternative functional forms. A single fragile result in one specification with no corroboration. With J1 resolved, this is the binding constraint for thesis readiness. | Yes (blocking for thesis quality) |
| J8 | **Low** (downgraded from Medium) | **Lead DV AR(1) concern partially addressed:** Col 2 includes CashHoldings_t as lagged DV control (b=0.753), which is appropriate. The Nickell (1981) bias concern for lagged-DV + FE is negligible with T=17 and Industry (not Firm) FE. | No |
| J9 | **Low-Medium** | **z-score on pre-regression sample:** z-score parameters computed on 86,807 Main-sample calls, not on the 76,239 regression sample. The z-scores in the regression are NOT exactly mean-zero, sd-one. The discrepancy is likely small but is a minor methodological inelegance. | No |
| J10 | **Low-Medium** | **Summary stats computed on wrong sample:** Summary statistics cover the full Main sample (N~87-88k), not the regression sample (N~76k). Readers comparing summary stats to regression results may be confused. | No |
| J11 | **Low** | **No test for symmetric vs asymmetric moderation:** The linear interaction assumes moderation is linear and symmetric. In practice, the effect of uncertainty on cash may be stronger for firms in VERY competitive markets (top decile) but absent for the rest. Subsample splits or polynomial interactions would test this. | No |
| J12 | **Low** | **TSIMM temporal alignment:** TNIC3TSIMM is based on 10-K filings, which are annual with a filing lag. The TNIC year may not perfectly align with the earnings call's fiscal year, especially for non-December fiscal year-end firms. The code merges on fyearq_int = TNIC year, which is a reasonable but imperfect approximation. | No |
| J13 | **Low** | **No heteroskedasticity test:** White's test or Breusch-Pagan not reported. Clustered SEs are robust to within-cluster heteroskedasticity but the appropriateness of the cluster structure (firm only, not firm+year) is not tested. | No |

---

## K. Corporate Finance Thesis Referee Assessment

### K1. Identification

**Assessment: Weak for causal claims; adequate for descriptive moderation**

The suite tests whether a cross-sectional moderator (product-market similarity) modifies a descriptive association (uncertainty language and cash holdings). There is no exogenous variation in either the IV or the moderator. The interaction b3 = 0.009 (p = 0.085) in Col 1 is at best suggestive.

The identification strategy relies on conditional correlations within Industry+FY FE. With only 8.3% within-firm variation in the moderator, the interaction is primarily identified from between-firm differences in product-market similarity -- firms that are permanently in more crowded markets are compared to firms in less crowded markets. This is inherently confounded by all other between-firm differences correlated with product-market structure (industry dynamics, firm strategy, management quality).

**No instrumental variable, no exogenous shock, no RDD.** Standard for exploratory moderation analysis in corporate finance, but the reader should understand this is a conditional correlation, not a causal estimate.

### K2. Inference

**Assessment: Marginal significance is fragile and potentially overstated**

1. **The headline result (b3 p=0.085) does not meet the conventional 5% threshold.** The 10% threshold is occasionally used in finance but is increasingly frowned upon (Harvey, Liu, and Zhu 2016). Reporting this as a "significant moderation effect" would be misleading.

2. **Multicollinearity (resolved but instructive):** Corr(moderator, interaction) was 0.93 before IV centering, now ~0.12 after centering. However, the interaction SE (0.0052) is identical in both centered and uncentered runs. This is because the interaction coefficient and its SE are algebraically invariant to mean-centering in OLS -- centering only changes the interpretation of the moderator's "main effect." The original concern that the SE was "inflated by 2-3x" was empirically falsified. Under cluster-robust SEs, the high pairwise correlation did not inflate inference on the interaction. The multicollinearity concern was textbook-correct for classical OLS SEs but practically irrelevant under clustered inference.

3. **Multiple testing:** If this is the "secondary moderator" from a moderation memo, how many moderators were tested? If TSIMM was selected after observing results from a primary moderator (or from data mining across moderator candidates), the effective p-value is higher than reported.

4. **Moulton problem:** The moderator varies at firm-year level but is repeated across ~3.85 calls per firm-year. Firm-clustered SEs account for within-firm correlation but the precision for the interaction coefficient specifically may be overstated. A firm-year-level regression would be the definitive test.

5. **One-tailed test on the IV:** The one-tailed p=0.069 for the main IV is reported with a star (*) at the 10% level. This is legitimate given the directional hypothesis, but combined with the two-tailed 10% interaction, the table gives a misleading impression of robust significance.

### K3. Robustness

**Assessment: No robustness checks implemented -- this is a single-specification result**

A referee would expect at minimum:
- Firm-year collapse (to address Moulton)
- ~~Mean-centering the IV before forming interaction~~ (done -- implemented in current code)
- Alternative moderator (TNIC3HHI, which is available in the same data file)
- Subsample splits: high vs low TSIMM (above/below median)
- Trimming TSIMM=1 firms (5% mass point at the moderator floor)
- Replication of the parent effect on the H1.1 sample (to confirm the main effect exists in this sample before testing moderation)
- Firm FE specification (even if expected to fail, the comparison is informative)

**None of these are implemented.** A marginal result (p=0.085) with zero robustness checks would not survive peer review.

### K4. Interpretation / Economic Magnitude

**Assessment: Not discussed; difficult to interpret given the specification**

The interaction coefficient b3 = 0.0090 means: a one-SD increase in log(TSIMM) (from a moderately competitive firm to a highly competitive firm) is associated with a 0.9 percentage-point increase in the marginal effect of Manager_Pres_Uncertainty_pct on CashHoldings. Given that the mean CashHoldings = 0.17 (17% of assets), this is a ~5% relative effect on the margin.

With IV mean-centering, the "direct effect" of the moderator (b2 = 0.036) now represents the moderator effect at the mean IV value (0.863), which is economically interpretable. The decomposition into "direct moderator effect" and "interaction effect" is now meaningful: at average uncertainty, a one-SD increase in log(TSIMM) is associated with a 3.6 percentage-point increase in cash holdings (relative to mean CashHoldings = 0.17, a ~21% relative effect).

### K5. Academic Integrity

**Assessment: No fabrication detected; documentation is transparent about limitations**

- All coefficients, p-values, and N counts verified against output artifacts.
- The run_manifest.json records input hashes, enabling full reproducibility.
- The report honestly labels the interaction as marginal (stars at 10% level).
- The LaTeX table notes correctly distinguish one-tailed (IV) from two-tailed (interaction) tests.

**One concern:** The LaTeX table shows the IV with a * (one-tailed 10%) and the interaction with a * (two-tailed 10%), using the same star symbol for different test types. A careful reader will catch the footnote distinction, but a casual reader may be misled. Best practice would be to use different notation or to report only two-tailed p-values throughout.

### K6. Relation to Literature

The TNIC total similarity measure (TSIMM) has been used in the product-market competition literature (Hoberg & Phillips JPE 2016, Hoberg & Phillips RFS 2010). The precautionary savings motivation for cash holdings in competitive markets is well-established (Haushalter, Klasa, and Maxwell 2007; Fresard JFE 2010).

The direct effect of TSIMM on CashHoldings at mean IV (b = 0.036, p < 0.001) is consistent with the competitive-threat-precautionary-savings channel and is well-supported in the literature. This is actually a more interesting finding than the interaction, but it is treated as a control rather than a result.

The interaction (does TSIMM moderate the effect of managerial uncertainty language?) is novel but weakly supported by theory. Why would speech uncertainty predict cash holdings more strongly for firms in competitive markets? The mechanism is unclear. Is it that competitive pressure makes managers more uncertain (a main effect, not an interaction)? Or that markets punish uncertain speech more when competition is fierce (a capital market channel, not a cash-holdings channel)?

### K7. Overall Assessment

**Verdict: Not thesis-ready in current form. Marginal evidence with no robustness and unclear mechanism.**

The suite produces one marginally significant interaction (p=0.085) that vanishes in the lead specification (p=0.94). IV centering has been implemented (resolving the interpretability issue) but did not change the interaction's significance. This is insufficient to support a claim that product-market similarity moderates the uncertainty-cash relationship. The direct moderator effect (TSIMM -> CashHoldings) is strong and well-established, but that is not the suite's research question.

To become thesis-ready, the suite needs:
1. ~~Mean-center the IV before interaction~~ (done -- did not change interaction significance)
2. Firm-year collapse robustness (addresses Moulton -- the #1 remaining issue)
3. At least one alternative moderator or subsample split
4. Confirmation that the parent main effect holds in the H1.1 sample
5. Honest framing: if the interaction remains marginal after robustness checks, discuss it as suggestive evidence, not a confirmed finding

---

## L. Issue Register (Master List)

| ID | Section | Severity | Category | Description | Disposition |
|----|---------|----------|----------|-------------|-------------|
| J1 | F.4, K2 | ~~High~~ Resolved | Inference | ~~Corr(moderator, interaction) = 0.93 -- multicollinearity from non-centered IV.~~ IV now centered; interaction SE unchanged (algebraic invariance). | Resolved in code |
| J2 | H.3, K2 | **High** | Inference | Interaction p=0.085 does not meet 5% threshold; vanishes in Col 2 (p=0.94). No robustness checks corroborate it. | Blocking: requires honest reporting |
| J3 | E.3, K1 | **High** | Design | Sample differs from parent (N=76k vs 55k); parent effect not replicated on child sample | Requires replication check |
| J4 | G, K2 | **High** (upgraded) | Inference | Double Moulton: DV at firm-quarter, moderator at firm-year, repeated across calls. Now the #1 econometric concern. | Blocking: requires firm-year collapse |
| J5 | F.4, K4 | ~~Medium~~ Resolved | Interpretation | ~~IV not mean-centered.~~ Implemented (code lines 80, 232-236). Moderator coefficient now interpretable at mean IV. | Resolved in code |
| J6 | F.3, K1 | Medium | Identification | 8.3% within-firm moderator variation; interaction identified from between-firm | Acknowledge |
| J7 | K3 | **High** (upgraded) | Robustness | Zero robustness checks for a marginal result. Binding constraint for thesis. | Blocking: requires minimum 3 checks |
| J8 | H, K2 | Low (downgraded) | Inference | Nickell bias negligible with T=17 and Industry (not Firm) FE | Acknowledge |
| J9 | E.2, G | Low-Medium | Design | z-score on pre-regression sample, not regression sample | Minor; acknowledge |
| J10 | G.5, K5 | Low-Medium | Documentation | Summary stats on wrong sample (Main vs regression) | Fix or document |
| J11 | K4 | Low | Interpretation | No test for nonlinear/asymmetric moderation | Future work |
| J12 | F.3 | Low | Data | TNIC year-to-fyearq alignment imperfect | Acknowledge |
| J13 | K2 | Low | Inference | No heteroskedasticity test beyond clustering | Minor |

---

## M. Referee Priority Fixes

### Priority 1 (Must-fix before thesis submission)

1. ~~**Mean-center the IV**~~ **DONE.** IV centering implemented in current code (lines 232-236). Result: moderator coefficient changed from 0.0283 to 0.0361 (now interpretable at mean IV). Interaction coefficient, SE, and p-value unchanged (algebraic invariance). Centering improved interpretability but did not improve the interaction's significance.

2. **Run firm-year collapsed regression** as a robustness check. Average all call-level variables within (gvkey, fyearq_int) and re-run both models. This eliminates the Moulton problem entirely and provides the definitive test of the interaction. This is now the #1 remaining priority.

3. **Replicate the parent main effect on the H1.1 sample.** Before the moderation table, show a single-column result: DV=CashHoldings, IV=Manager_Pres_Unc_c, Industry+FY FE, Extended controls, same sample as Col 1 (N~76k). Confirm that the main effect is significant in this sample. If it is not, the moderation analysis is moot.

### Priority 2 (Should-fix for robustness)

4. **Add at least one alternative moderator.** TNIC3HHI is available in the same data file. Run the same specification with z(log(TNIC3HHI)) as moderator to test whether competition concentration (rather than total similarity) moderates the effect.

5. **Subsample split:** high vs low TSIMM (above/below median). Report the main-effect coefficient separately for each subsample. This provides intuitive evidence of whether the effect differs across competitive environments.

### Priority 3 (Nice-to-have)

6. Test sensitivity to trimming TSIMM=1 firms (5% mass point).
7. Report summary statistics on the regression sample, not the full Main sample.

---

## N. Final Readiness Statement

**Status: NOT READY for thesis inclusion.**

The H1.1 suite produces a single marginally significant interaction (p=0.085, two-tailed) that does not meet the conventional 5% threshold and vanishes completely in the lead specification (p=0.94). IV centering has been implemented (resolving the interpretability and multicollinearity concerns from v1), but it had no effect on the interaction's significance -- as expected from OLS algebra. The remaining blocking issues are: (1) marginal interaction with no robustness checks, (2) Moulton problem from call-level repetition of firm-year variables, and (3) sample composition differs from parent without a replication check.

The technical implementation is sound: the TNIC merge is clean (0 duplicates, 98.4% match, row-count-preserving), the moderator transformation and IV centering are correctly computed and verified, the regression mechanics are correct, and the output artifacts are fully reproducible. The problems are econometric substance, not coding errors.

**Path to readiness:** Implement remaining Priority 1 fixes (firm-year collapse, parent-effect replication). If the interaction survives the firm-year collapse, the suite can be included with appropriate caveats about marginal significance. If it does not, honestly report the null result as supplementary evidence.

---

## Revision History

- **2026-03-21 (v2):** Updated following red-team audit (`docs/provenance/H1_1_red_team.md`). IV centering now reflected as implemented; coefficients updated to centered run `2026-03-19_001554`; J1/J5 marked resolved; J4/J7 upgraded to High; falsified SE prediction corrected; all code line-number references updated; moderator direct effect updated from 0.0283 to 0.0361; AR(1) corrected from ">0.9" to median=0.595.
- **2026-03-19 (v1):** Initial provenance document. Written from scratch against verified run `2026-03-18_235755` and independently verified data artifacts. Includes full referee assessment.
