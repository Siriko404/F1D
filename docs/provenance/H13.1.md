# H13.1 Provenance Document

## A. SUITE IDENTITY

```yaml
Suite ID:        H13.1
Title:           Product Market Competition--Moderated Speech Uncertainty and Capital Expenditure
Hypothesis:      Does product-market structure (Hoberg-Phillips TNIC3TSIMM and TNIC3HHI) moderate the
                 Manager QA Uncertainty -> Capital Expenditure relationship?
Direction:       two-tailed (all coefficients, matching parent H13)
Model Family:    PanelOLS
Estimator:       linearmodels.panel.PanelOLS
Unit of Obs:     call-level (individual earnings call)
Panel Index:     (gvkey, cal_yr) for cols 1-4; (gvkey, cal_yr_qtr) for cols 5-8
Columns:         8
Reference:       Hoberg and Phillips (2016) — TNIC3TSIMM and TNIC3HHI moderators
Runner:          src/f1d/econometric/run_h13_1_competition.py
Panel Builder:   src/f1d/variables/build_h13_capex_panel.py
```

---

## B. MODEL SPECIFICATION

### B1. Regression Equation

For each model column, the estimator fits:

```
DV_{i,t} = b1 * Manager_QA_Unc_c + b2 * z(log(MOD)) + b3 * (Manager_QA_Unc_c x z(log(MOD)))
          + b4 * Lagged_DV + Controls + Industry_FE(ff12) + Time_FE + epsilon_{i,t}
```

Where:
- `MOD` = TNIC3TSIMM (cols 1-2, 5-6) or TNIC3HHI (cols 3-4, 7-8)
- `Time_FE` = Calendar Year (cols 1-4) or Calendar Year-Quarter (cols 5-8)
- `b3` is the coefficient of interest (moderation effect)
- `Manager_QA_Unc_c` is the mean-centered IV (centered on Main sample mean)

Source: `run_h13_1_competition.py`, lines 12-13 (docstring), lines 398, 410-418 (estimation).

### B2. Dependent Variable(s)

| Variable Name (code) | Description | Formula | Source Engine | Timing |
|----------------------|-------------|---------|---------------|--------|
| CapexAt | Capital expenditure scaled by lagged total assets | capxy_annual (Q4 full-year) / atq_annual_lag1 | CompustatEngine: capxy, atq, fqtr, fyearq | Contemporaneous (t) |
| CapexAt_lead | Capital expenditure intensity in fiscal year t+1 | Shift of CapexAt by -1 within gvkey on fiscal year, requiring consecutive fiscal years | CompustatEngine via panel builder lead construction | Lead (t+1) |

Source: `_compustat_engine.py`, lines 994-999 (CapexAt); `build_h13_capex_panel.py`, lines 215-337 (CapexAt_lead).

### B3. Independent Variable(s)

| Variable Name (code) | Description | Formula | Source Engine | Timing |
|----------------------|-------------|---------|---------------|--------|
| Manager_QA_Uncertainty_pct | Raw Manager Q&A Uncertainty percentage | Word-level uncertainty percentage from Q&A section of earnings calls | LinguisticEngine: Stage 2 textual analysis outputs | Contemporaneous |
| Manager_QA_Unc_c | Mean-centered Manager Q&A Uncertainty | Manager_QA_Uncertainty_pct - mean(Manager_QA_Uncertainty_pct) on Main sample | Computed in runner at load time | Contemporaneous |
| z_log_tnic3tsimm | Standardized log TNIC3 Total Similarity (moderator, primary) | z-score of log(tnic3tsimm), computed on Main sample: (log(tnic3tsimm) - mu) / sd | TNIC3HHIdata.txt: tnic3tsimm | Fiscal year (firm-year) |
| z_log_tnic3hhi | Standardized log TNIC3 HHI (moderator, robustness) | z-score of log(tnic3hhi), computed on Main sample: (log(tnic3hhi) - mu) / sd | TNIC3HHIdata.txt: tnic3hhi | Fiscal year (firm-year) |
| MgrQAUnc_x_zlogTSIMM | Interaction: centered IV x z(log(TSIMM)) | Manager_QA_Unc_c * z_log_tnic3tsimm | Computed in runner prepare_regression_data() | Contemporaneous |
| MgrQAUnc_x_zlogHHI | Interaction: centered IV x z(log(HHI)) | Manager_QA_Unc_c * z_log_tnic3hhi | Computed in runner prepare_regression_data() | Contemporaneous |

Centering and standardization details (from `run_h13_1_competition.py`, lines 249-298):
- IV centering: `Manager_QA_Unc_c = Manager_QA_Uncertainty_pct - mean(Main sample)`
- Moderator standardization: `z = (log(raw) - mu_Main) / sd_Main`
- Interaction: product of centered IV and z-scored moderator
- Cross-moderator correlation: Corr(z(log(HHI)), z(log(TSIMM))) = -0.7004

### B4. Control Variables

All specs use the same unified control block (no Base/Extended split):

| Variable Name (code) | Description | Formula | Source Engine |
|----------------------|-------------|---------|---------------|
| Lagged_DV | Lagged dependent variable (CapexAt_lag) | CapexAt from fiscal year t-1 (consecutive-year validated) | CompustatEngine via panel builder lag construction |
| Size | Firm size | ln(atq) for atq > 0; NaN otherwise | CompustatEngine: atq |
| TobinsQ | Tobin's Q | (atq + cshoq * prccq - ceqq) / atq | CompustatEngine: atq, cshoq, prccq, ceqq |
| ROA | Return on assets | iby_annual (Q4) / avg_assets where avg = (atq_t + atq_{t-1}) / 2 | CompustatEngine: iby, atq |
| BookLev | Book leverage | (dlcq + dlttq) / atq; missing debt treated as 0 | CompustatEngine: dlcq, dlttq, atq |
| CashHoldings | Cash and equivalents ratio | cheq / atq | CompustatEngine: cheq, atq |
| DividendPayer | Dividend payer indicator | 1 if dvy_annual (Q4 full-year) > 0, else 0 | CompustatEngine: dvy, fqtr, fyearq |
| OCF_Volatility | Operating cash flow volatility | Rolling 5-year std (min 3 yrs) of oancfy / atq_{t-1} per gvkey | CompustatEngine: oancfy, atq |
| SalesGrowth | Year-over-year revenue growth | (saley_t - saley_{t-1}) / abs(saley_{t-1}); saley fallback to saleq | CompustatEngine: saley, saleq |
| RD_Intensity | R&D intensity | xrdq / atq; missing xrdq treated as 0 | CompustatEngine: xrdq, atq |
| CashFlow | Operating cash flow to assets | oancfy / avg_assets where avg = (atq_t + atq_{t-1}) / 2 | CompustatEngine: oancfy, atq |
| Volatility | Annualized stock return volatility | std(daily_ret) * sqrt(252) * 100 over [prev_call+5, call-5] window, min 10 days | CRSPEngine: daily returns |

Source: `run_h13_1_competition.py`, lines 86-91 (CONTROLS list).

Lagged_DV construction: In `prepare_regression_data()` (lines 325-329), `Lagged_DV = CapexAt_lag` (for both CapexAt and CapexAt_lead DVs). The lag column `CapexAt_lag` is built in the panel builder (`build_h13_capex_panel.py`, lines 340-408) as the previous fiscal year's CapexAt, consecutive-year validated.

### B5. Fixed Effects / Stratification / Random Effects

| FE Type | Column Used | Description | Cols |
|---------|-------------|-------------|------|
| Industry | ff12_code | Fama-French 12-industry classification, absorbed via `other_effects` | All (1-8) |
| Time (Calendar Year) | cal_yr | Calendar year derived from call start_date | 1-4 |
| Time (Year-Quarter) | cal_yr_qtr | Calendar year-quarter (cal_yr * 10 + cal_qtr) from start_date | 5-8 |

Source: `run_h13_1_competition.py`, lines 410-418:
```python
model_obj = PanelOLS(
    dependent=df_panel[dv],
    exog=df_panel[exog],
    entity_effects=False,
    time_effects=True,
    other_effects=df_panel["ff12_code"],
    drop_absorbed=True,
    check_rank=False,
)
```

Panel index is set at line 407: `df_panel = df_prepared.set_index(["gvkey", time_col])`.
- `time_col = "cal_yr"` for `fe="industry"` (cols 1-4)
- `time_col = "cal_yr_qtr"` for `fe="industry_yq"` (cols 5-8)

`entity_effects=False` + `time_effects=True` means PanelOLS absorbs time FE from the panel index time dimension. `other_effects=df_panel["ff12_code"]` absorbs industry FE. No firm FE are used.

### B6. Standard Errors and Clustering

- `cov_type="clustered"` with `cluster_entity=True`
- Clustering dimension: entity (gvkey), i.e., firm-clustered standard errors
- No small-sample corrections beyond what PanelOLS applies by default

Source: `run_h13_1_competition.py`, line 419: `model = model_obj.fit(cov_type="clustered", cluster_entity=True)`.

### B7. Hypothesis Test

- **Direction:** Two-tailed for ALL coefficients (IV, moderator, interaction, controls)
- **P-value computation:** Two-tailed p-values used directly from PanelOLS output; no one-tailed conversion
- **Significance thresholds:** `*** p<0.01`, `** p<0.05`, `* p<0.10`
- **Per-variable tail specification:** All variables are two-tailed (confirmed by `key_tails: ["two", "two", "two", "two", "two"]` in generate_all_tables.py)

Source: `run_h13_1_competition.py`, lines 426-443 (p-value extraction uses `model.pvalues` directly); lines 474-484 (`_sig_stars` function uses two-tailed thresholds). The runner docstring (line 42) states: "Hypothesis: All two-tailed (matching parent H13)."

---

## C. MODEL SPECIFICATION REGISTER

| Col | DV | Moderator | Entity FE | Time FE | Controls | Notes |
|-----|-----|-----------|-----------|---------|----------|-------|
| 1 | CapexAt | z(log(TSIMM)) | Industry (FF12) | Cal Yr | Extended + Lagged_DV | Primary moderator |
| 2 | CapexAt_lead | z(log(TSIMM)) | Industry (FF12) | Cal Yr | Extended + Lagged_DV | Primary moderator |
| 3 | CapexAt | z(log(HHI)) | Industry (FF12) | Cal Yr | Extended + Lagged_DV | Robustness moderator |
| 4 | CapexAt_lead | z(log(HHI)) | Industry (FF12) | Cal Yr | Extended + Lagged_DV | Robustness moderator |
| 5 | CapexAt | z(log(TSIMM)) | Industry (FF12) | Cal Yr-Qtr | Extended + Lagged_DV | Primary moderator |
| 6 | CapexAt_lead | z(log(TSIMM)) | Industry (FF12) | Cal Yr-Qtr | Extended + Lagged_DV | Primary moderator |
| 7 | CapexAt | z(log(HHI)) | Industry (FF12) | Cal Yr-Qtr | Extended + Lagged_DV | Robustness moderator |
| 8 | CapexAt_lead | z(log(HHI)) | Industry (FF12) | Cal Yr-Qtr | Extended + Lagged_DV | Robustness moderator |

Source: `run_h13_1_competition.py`, lines 116-127 (MODEL_SPECS).

---

## D. SAMPLE CONSTRUCTION

### D1. Population

- Starting dataset: `master_sample_manifest.parquet`
- Total calls: 112,968
- Year range: 2002-2018

### D2. Exclusion Criteria (Attrition Cascade)

From `sample_attrition.csv` (latest run 2026-03-27):

| Step | Filter | N | N Lost | % Retained |
|------|--------|---|--------|------------|
| 1 | Full panel (H13) | 112,968 | 0 | 100.0% |
| 2 | Main sample (excl Finance FF12=8, Utility FF12=11) | 88,205 | -24,763 | 78.1% |
| 3 | TNIC3TSIMM matched | 86,807 | -1,398 | 76.8% |
| 4 | TNIC3HHI matched | 86,766 | -41 | 76.8% |
| 5 | After complete-case + min-calls (TSIMM, col 1) | 73,083 | -13,683 | 64.7% |
| 6 | After complete-case + min-calls (HHI, col 3) | 73,070 | -13 | 64.7% |

Note: Steps 5-6 include DV non-missing, all variables non-missing, inf-to-NaN replacement, and min 5 calls per firm.

Source: `outputs/econometric/h13_1_competition/2026-03-27_095014/sample_attrition.csv`.

### D3. Sample Counts per Specification

From `model_diagnostics.csv`:

| Col | DV | Moderator | N (calls) | N (firms) | N (firm-years) |
|-----|-----|-----------|-----------|-----------|----------------|
| 1 | CapexAt | TSIMM | 73,083 | 1,790 | 19,780 |
| 2 | CapexAt_lead | TSIMM | 69,028 | 1,730 | 17,985 |
| 3 | CapexAt | HHI | 73,070 | 1,790 | 19,776 |
| 4 | CapexAt_lead | HHI | 69,017 | 1,730 | 17,982 |
| 5 | CapexAt | TSIMM | 73,083 | 1,790 | 19,780 |
| 6 | CapexAt_lead | TSIMM | 69,028 | 1,730 | 17,985 |
| 7 | CapexAt | HHI | 73,070 | 1,790 | 19,776 |
| 8 | CapexAt_lead | HHI | 69,017 | 1,730 | 17,982 |

N differs between CapexAt and CapexAt_lead specs because CapexAt_lead has additional NaN from fiscal year gaps and last-year censoring. N differs slightly between TSIMM and HHI specs because 13 additional calls lack HHI data where TSIMM is available.

---

## E. VARIABLE DICTIONARY

| Variable (code) | Label | Type | Formula | Source | Winsorized | Timing |
|-----------------|-------|------|---------|--------|------------|--------|
| CapexAt | CapEx / Assets | DV | capxy_annual (Q4) / atq_annual_lag1 | CompustatEngine: capxy, atq, fqtr, fyearq | 1%/99% by fiscal year | Contemporaneous (t) |
| CapexAt_lead | CapEx / Assets lead | DV | CapexAt shifted -1 fiscal year within gvkey (consecutive validated) | Panel builder construction from CompustatEngine | Inherits CapexAt winsorization | Lead (t+1) |
| CapexAt_lag | CapEx / Assets lag | Source for Lagged_DV | CapexAt shifted +1 fiscal year within gvkey (consecutive validated) | Panel builder construction from CompustatEngine | Inherits CapexAt winsorization | Lag (t-1) |
| Manager_QA_Uncertainty_pct | Mgr QA Uncertainty (raw) | IV (raw) | Word-level uncertainty % in Q&A section, manager utterances | LinguisticEngine: Stage 2 textual analysis | 0%/99% upper-only by year | Contemporaneous |
| Manager_QA_Unc_c | Mgr QA Uncertainty (centered) | IV (centered) | Manager_QA_Uncertainty_pct - mean(Main sample) | Runner-time computation | Inherits raw winsorization | Contemporaneous |
| tnic3tsimm | TNIC3 Total Similarity (raw) | Moderator (raw) | Hoberg-Phillips TNIC3 total similarity score | TNIC3HHIdata.txt | No | Fiscal year |
| z_log_tnic3tsimm | z(log(TSIMM)) | Moderator | (log(tnic3tsimm) - mu_Main) / sd_Main | Runner-time: log + z-score on Main sample | No (standardized) | Fiscal year |
| tnic3hhi | TNIC3 HHI (raw) | Moderator (raw) | Hoberg-Phillips TNIC3 text-based Herfindahl index | TNIC3HHIdata.txt | No | Fiscal year |
| z_log_tnic3hhi | z(log(HHI)) | Moderator | (log(tnic3hhi) - mu_Main) / sd_Main | Runner-time: log + z-score on Main sample | No (standardized) | Fiscal year |
| MgrQAUnc_x_zlogTSIMM | IV x TSIMM interaction | Interaction | Manager_QA_Unc_c * z_log_tnic3tsimm | Runner-time computation | No | Contemporaneous |
| MgrQAUnc_x_zlogHHI | IV x HHI interaction | Interaction | Manager_QA_Unc_c * z_log_tnic3hhi | Runner-time computation | No | Contemporaneous |
| Lagged_DV | Lagged dependent variable | Lagged_DV | CapexAt_lag (previous fiscal year CapexAt) | Panel builder lag construction | Inherits CapexAt winsorization | Lag (t-1) |
| Size | Firm size | Control | ln(atq) for atq > 0 | CompustatEngine: atq | 1%/99% by fiscal year | Contemporaneous |
| TobinsQ | Tobin's Q | Control | (atq + cshoq*prccq - ceqq) / atq | CompustatEngine: atq, cshoq, prccq, ceqq | 1%/99% by fiscal year | Contemporaneous |
| ROA | Return on assets | Control | iby_annual (Q4) / avg_assets | CompustatEngine: iby, atq | 1%/99% by fiscal year | Contemporaneous |
| BookLev | Book leverage | Control | (dlcq + dlttq) / atq; missing debt = 0 | CompustatEngine: dlcq, dlttq, atq | 1%/99% by fiscal year | Contemporaneous |
| CashHoldings | Cash / assets | Control | cheq / atq | CompustatEngine: cheq, atq | 1%/99% by fiscal year | Contemporaneous |
| DividendPayer | Dividend payer indicator | Control | 1 if dvy_annual (Q4 full-year) > 0, else 0 | CompustatEngine: dvy, fqtr, fyearq | No (binary) | Contemporaneous |
| OCF_Volatility | Cash flow volatility | Control | Rolling 5-yr std (min 3) of oancfy / atq_{t-1} | CompustatEngine: oancfy, atq | 1%/99% by fiscal year | Rolling window |
| SalesGrowth | Revenue growth | Control | (saley_t - saley_{t-1}) / abs(saley_{t-1}); saleq fallback | CompustatEngine: saley, saleq | 1%/99% by fiscal year (in Biddle engine) | Contemporaneous |
| RD_Intensity | R&D intensity | Control | xrdq / atq; missing xrdq = 0 | CompustatEngine: xrdq, atq | 1%/99% by fiscal year | Contemporaneous |
| CashFlow | Operating cash flow / assets | Control | oancfy / avg_assets | CompustatEngine: oancfy, atq | 1%/99% by fiscal year (in Biddle engine) | Contemporaneous |
| Volatility | Stock return volatility | Control | std(daily_ret) * sqrt(252) * 100 over inter-call window | CRSPEngine: daily returns | No | Pre-call window |
| ff12_code | Fama-French 12 industry code | FE | Mapped from SIC codes | ManifestFieldsBuilder / CompustatEngine | N/A | Time-invariant |
| cal_yr | Calendar year | FE | start_date.dt.year | Derived from call start_date | N/A | Time index |
| cal_yr_qtr | Calendar year-quarter | FE | cal_yr * 10 + cal_qtr | Derived from call start_date | N/A | Time index |

---

## F. DATA PIPELINE

### F1. Dependency Chain

1. **Raw inputs:**
   - `outputs/1.4_AssembleManifest/latest/master_sample_manifest.parquet` (112,968 calls)
   - `outputs/2_Textual_Analysis/2.2_Variables/latest/linguistic_variables_{year}.parquet` (uncertainty measures)
   - Compustat quarterly data (financial variables)
   - CRSP daily stock files (volatility)
   - `inputs/TNIC3HHIdata/TNIC3HHIdata.txt` (Hoberg-Phillips competition measures)

2. **Engine loading:**
   - CompustatEngine (singleton): loads Compustat quarterly, computes all financial controls + CapexAt, winsorizes by fiscal year
   - LinguisticEngine (singleton): loads Stage 2 linguistic variables, winsorizes pct columns by year
   - CRSPEngine (singleton): loads CRSP daily returns, computes inter-call volatility

3. **Panel builder** (`build_h13_capex_panel.py`):
   - Loads manifest as base (one row per call)
   - Merges 16 builder outputs on file_name (zero-row-delta enforced)
   - Creates CapexAt_lead via fiscal-year shift (t+1, consecutive validated)
   - Creates CapexAt_lag via fiscal-year shift (t-1, consecutive validated)
   - Assigns industry sample (Main/Finance/Utility from ff12_code)
   - Outputs: `h13_capex_panel.parquet`

4. **Runner loading** (`run_h13_1_competition.py`):
   - Loads H13 panel parquet (selects specific columns)
   - Builds cal_yr_qtr index from start_date
   - Merges TNIC3HHIdata on (gvkey_int, fyearq_int) -- left join
   - Log-transforms + z-scores both moderators on Main sample
   - Mean-centers IV on Main sample

5. **Sample filtering** (per spec):
   - Exclude Finance (FF12=8) and Utility (FF12=11)
   - Replace inf/-inf with NaN
   - Create interaction term (centered IV x z-scored moderator)
   - Drop rows with NaN in DV
   - Complete-case deletion (all required variables non-null)
   - Min 5 calls per firm

6. **Regression estimation:**
   - PanelOLS: 8 specs (2 DVs x 2 moderators x 2 time FE)
   - Outputs per-column regression text, diagnostics CSV, LaTeX table

7. **Table generation:**
   - `generate_all_tables.py` reads `col_files` mapping, applies `generate_moderation_table()` with key_tails all "two"

### F2. Data Engines Used

| Engine | Source Data | Variables Provided to This Suite |
|--------|------------|--------------------------------|
| CompustatEngine | WRDS Compustat quarterly | CapexAt, Size, TobinsQ, ROA, BookLev, CashHoldings, DividendPayer, OCF_Volatility, RD_Intensity, CashFlow, SalesGrowth |
| LinguisticEngine | Stage 2 textual analysis parquets | Manager_QA_Uncertainty_pct |
| CRSPEngine | CRSP daily stock files | Volatility |
| TNIC3HHIdata (file) | Hoberg-Phillips TNIC3HHIdata.txt | tnic3tsimm, tnic3hhi |

### F3. Merge Operations

**Panel Builder (Stage 3):**

| Left | Right | Keys | Type | Notes |
|------|-------|------|------|-------|
| manifest | CEOQAUncertaintyBuilder | file_name | left | Zero-row-delta enforced |
| manifest | CEOPresUncertaintyBuilder | file_name | left | Zero-row-delta enforced |
| manifest | ManagerQAUncertaintyBuilder | file_name | left | Zero-row-delta enforced |
| manifest | ManagerPresUncertaintyBuilder | file_name | left | Zero-row-delta enforced |
| manifest | SizeBuilder | file_name | left | Zero-row-delta enforced |
| manifest | TobinsQBuilder | file_name | left | Zero-row-delta enforced |
| manifest | ROABuilder | file_name | left | Zero-row-delta enforced |
| manifest | BookLevBuilder | file_name | left | Zero-row-delta enforced |
| manifest | CashHoldingsBuilder | file_name | left | Zero-row-delta enforced |
| manifest | CapexIntensityBuilder | file_name | left | Zero-row-delta enforced |
| manifest | DividendPayerBuilder | file_name | left | Zero-row-delta enforced |
| manifest | OCFVolatilityBuilder | file_name | left | Zero-row-delta enforced |
| manifest | SalesGrowthBuilder | file_name | left | Zero-row-delta enforced |
| manifest | RDIntensityBuilder | file_name | left | Zero-row-delta enforced |
| manifest | CashFlowBuilder | file_name | left | Zero-row-delta enforced |
| manifest | VolatilityBuilder | file_name | left | Zero-row-delta enforced |
| panel | fyearq (CompustatEngine) | merge_asof gvkey+start_date->datadate | left | Via attach_fyearq() |
| panel | CapexAt_lead lookup | gvkey, fyearq_int | left | Zero-row-delta enforced |
| panel | CapexAt_lag lookup | gvkey, fyearq_int | left | Zero-row-delta enforced |

**Runner (Stage 4):**

| Left | Right | Keys | Type | Notes |
|------|-------|------|------|-------|
| H13 panel | TNIC3HHIdata | _gvkey_int, fyearq_int | left | Row count preserved (assert) |

Source: `run_h13_1_competition.py`, lines 229-237 (TNIC merge).

---

## G. OUTPUTS

### G1. Stage 3 Outputs (Panel Builder)

| File | Description |
|------|-------------|
| `h13_capex_panel.parquet` | Call-level panel with all variables (112,968 rows) |
| `summary_stats.csv` | Variable summary statistics from builders |
| `run_manifest.json` | Reproducibility manifest |
| `report_step3_h13_capex.md` | Build report |

Output directory: `outputs/variables/h13_capex/{timestamp}/`

### G2. Stage 4 Outputs (Runner)

| File | Description |
|------|-------------|
| `h13_1_capex_tnic_table.tex` | Publication LaTeX table (8 columns, Panel A: TSIMM, Panel B: HHI) |
| `model_diagnostics.csv` | Per-model regression metadata (8 rows) |
| `summary_stats.csv` | Main sample summary statistics |
| `summary_stats.tex` | Summary statistics LaTeX table |
| `sample_attrition.csv` | Attrition cascade |
| `sample_attrition.tex` | Attrition cascade LaTeX table |
| `regression_results_col{1-8}_{dv}_{mod}.txt` | Individual model outputs (8 files) |
| `report_step4_H13_1.md` | Results report |
| `run_manifest.json` | Reproducibility manifest |

Output directory: `outputs/econometric/h13_1_competition/{timestamp}/`

Regression result file naming convention: `regression_results_col{col}_{dv}_{moderator}.txt`
(e.g., `regression_results_col1_CapexAt_tsimm.txt`)

Source: `run_h13_1_competition.py`, lines 652-680 (save_outputs), lines 786-793 (summary stats), lines 822-833 (attrition).

### G3. Summary Statistics

Variables included in summary stats (`SUMMARY_STATS_VARS`, lines 134-154):
- CapexAt, CapexAt_lead
- Manager_QA_Uncertainty_pct, Manager_QA_Unc_c
- tnic3tsimm, z_log_tnic3tsimm
- tnic3hhi, z_log_tnic3hhi
- Size, TobinsQ, ROA, BookLev, CashHoldings, DividendPayer, OCF_Volatility
- SalesGrowth, RD_Intensity, CashFlow, Volatility

Metrics computed: N, Mean, SD, Min, P25, Median, P75, Max (via `make_summary_stats_table`).

---

## H. OUTLIER AND MISSING DATA TREATMENT

### H1. Winsorization

**Compustat financial variables (CompustatEngine level):**
- Level: 1%/99% by fiscal year (`_winsorize_by_year`, min 10 obs per year group)
- Applied to: Size, TobinsQ, ROA, BookLev, CashHoldings, CapexAt, OCF_Volatility, RD_Intensity
- NOT applied to: DividendPayer (binary), CashFlow/SalesGrowth (winsorized separately inside Biddle engine to avoid double-winsorization)

**Linguistic variables (LinguisticEngine level):**
- Level: 0%/99% upper-only by calendar year
- Applied to: Manager_QA_Uncertainty_pct (and all _pct columns)
- Rationale: Linguistic percentages are bounded [0, ~2%]; only upper tail needs trimming

**TNIC moderators:** Not winsorized (log-transformed and z-scored instead)

**Volatility (CRSPEngine level):** Not winsorized at engine level

Source: `_compustat_engine.py`, lines 1115-1136 (Compustat winsorization); `_linguistic_engine.py`, lines 254-258 (linguistic winsorization).

### H2. Missing Data Policy

- Complete-case deletion: rows dropped if any required variable is NaN (`run_h13_1_competition.py`, lines 353-357)
- Inf/-Inf replaced with NaN before regression (`run_h13_1_competition.py`, line 343)
- TNIC merge is left join; unmatched calls get NaN moderator (dropped at complete-case step)
- CapexAt_lead/lag set to NaN for non-consecutive fiscal years or last/first fiscal year per firm

### H3. Transformations

| Variable | Transformation | Details |
|----------|---------------|---------|
| Manager_QA_Uncertainty_pct | Mean-centering | Subtract Main sample mean (iv_mu) |
| tnic3tsimm | Log + z-score | log(raw), then (log - mu_Main) / sd_Main |
| tnic3hhi | Log + z-score | log(raw), then (log - mu_Main) / sd_Main |
| Size | Log | ln(atq) applied at CompustatEngine level |

---

## I. GENERATE_ALL_TABLES.PY ENTRY

```python
{
    "id": "H13.1",
    "type": "moderation",
    "dir": "h13_1_competition/2026-03-27_095014",
    "caption": "H13.1: Product Market Competition--Moderated Speech Uncertainty and Capital Expenditure",
    "label": "tab:h13_1",
    "cols": 8,
    "col_files": {
        1: "regression_results_col1_CapexAt_tsimm.txt",
        2: "regression_results_col2_CapexAt_lead_tsimm.txt",
        3: "regression_results_col5_CapexAt_tsimm.txt",
        4: "regression_results_col6_CapexAt_lead_tsimm.txt",
        5: "regression_results_col3_CapexAt_hhi.txt",
        6: "regression_results_col4_CapexAt_lead_hhi.txt",
        7: "regression_results_col7_CapexAt_hhi.txt",
        8: "regression_results_col8_CapexAt_lead_hhi.txt",
    },
    "dvs": [
        ("TSIMM", 4),
        ("HHI", 4),
    ],
    "col_dv_labels": [
        "CapexAt", r"CapexAt\_lead",
        "CapexAt", r"CapexAt\_lead",
        "CapexAt", r"CapexAt\_lead",
        "CapexAt", r"CapexAt\_lead",
    ],
    "lagged_dv_var": "Lagged_DV",
    "lagged_dv_label": r"Lagged\_DV",
    "key_vars": [
        "Manager_QA_Unc_c",
        "z_log_tnic3tsimm",
        "MgrQAUnc_x_zlogTSIMM",
        "z_log_tnic3hhi",
        "MgrQAUnc_x_zlogHHI",
    ],
    "key_labels": [
        r"Manager\_QA\_Unc\_c",
        r"z\_log\_tnic3tsimm",
        r"MgrQAUnc\_x\_zlogTSIMM",
        r"z\_log\_tnic3hhi",
        r"MgrQAUnc\_x\_zlogHHI",
    ],
    "key_tails": ["two", "two", "two", "two", "two"],
}
```

Source: `outputs/generate_all_tables.py`, lines 306-350.

**Verification:**

| Check | Expected | Actual | Match |
|-------|----------|--------|-------|
| Tail direction | two-tailed | key_tails all "two" | Yes |
| Number of columns | 8 (MODEL_SPECS) | cols: 8 | Yes |
| DV column names | CapexAt, CapexAt_lead | col_dv_labels alternate CapexAt/CapexAt_lead | Yes |
| Key variable names | Match runner code names | key_vars match IV_CENTERED, z_col, int_col names | Yes |
| Table type | moderation | type: "moderation" | Yes |
| Lagged DV | Lagged_DV | lagged_dv_var: "Lagged_DV" | Yes |

**Note on col_files mapping:** The generate_all_tables entry remaps runner column numbers to table column numbers. Cols 1-4 in the table correspond to TSIMM specs (runner cols 1,2,5,6), and cols 5-8 in the table correspond to HHI specs (runner cols 3,4,7,8). This groups moderators contiguously: TSIMM first, HHI second — each with Cal Yr then Yr-Qtr FE pairs.

---

## J. REPRODUCTION COMMANDS

```bash
# Stage 3: Build panel (shared with H13)
python -m f1d.variables.build_h13_capex_panel

# Stage 4: Run regressions
python -m f1d.econometric.run_h13_1_competition

# Generate tables (if applicable)
python outputs/generate_all_tables.py
```

---

## K. MODEL-FAMILY ADDENDUM

### K1. PanelOLS Specifics

- **Entity effects:** Not used (`entity_effects=False`). No firm FE.
- **Time effects:** Absorbed (`time_effects=True`). The panel index time dimension provides the time FE:
  - `cal_yr` for cols 1-4 (Calendar Year FE)
  - `cal_yr_qtr` for cols 5-8 (Calendar Year-Quarter FE)
- **other_effects usage:** `other_effects=df_panel["ff12_code"]` absorbs Fama-French 12-industry FE
- **drop_absorbed:** `True` — absorbed fixed effect variables are dropped from the exogenous matrix
- **check_rank:** `False` — rank check disabled
- **Singleton handling:** Handled by PanelOLS default behavior with `drop_absorbed=True`

Source: `run_h13_1_competition.py`, lines 410-418.

### K2. Cox Proportional Hazards Specifics
N/A

### K3. Logit/Probit/LPM Specifics
N/A

### K4. IV/2SLS Specifics
N/A

### K5. OLS (non-panel) Specifics
N/A

### K6. Other Model Family
N/A

---

## L. KNOWN ISSUES AND NOTES

1. **Report docstring FE label inconsistency:** The report file (`report_step4_H13_1.md`, line 8) labels cols 1-4 as "Industry(FF12) + FiscalYear" and uses "FY" shorthand in the table. However, the code uses `cal_yr` (calendar year from start_date, line 335) as the time index, not fiscal year. The calendar year designation in the runner code (line 44: `Panel index: ["gvkey", "cal_yr"]`) and `build_cal_yr_qtr_index()` confirm these are calendar-year FE. **Code is truth: Calendar Year FE, not Fiscal Year FE.**

2. **TNIC measures are firm-year, repeated across calls:** TNIC3TSIMM and TNIC3HHI are annual firm-level measures merged to calls via (gvkey, fyearq_int). Multiple calls from the same firm in the same fiscal year share identical moderator values. This does not violate the specification but means within-firm-year variation in the interaction term comes entirely from the centered IV.

3. **Cross-moderator correlation:** Corr(z(log(HHI)), z(log(TSIMM))) = -0.7004. The moderators are NOT independent. TSIMM and HHI are run in separate regressions (not jointly), so multicollinearity between them is not a concern within any single specification. However, the high negative correlation means HHI results should be interpreted cautiously as they may partly reflect the TSIMM channel in reverse.

4. **Lagged_DV always uses CapexAt_lag:** For both CapexAt and CapexAt_lead DVs, `Lagged_DV = CapexAt_lag` (the previous fiscal year's CapexAt). This is set in `prepare_regression_data()` line 326-329 where `base_dv = dv.replace("_lead_qtr", "").replace("_lead", "")` extracts "CapexAt", and `lag_col = "CapexAt_lag"`.

5. **H13.1 redesign:** This suite replaces an earlier H13.1 design that had uncentered interactions, HHI-only moderator, 16 models with 4 separate IVs, and only 1/16 significant results that failed Bonferroni correction. The redesigned version uses only Manager_QA_Uncertainty (the strongest IV from parent H13), centers the interaction, and tests both TSIMM (primary) and HHI (robustness). See runner docstring lines 37-39.

6. **No CapexIntensity control exclusion note:** CapexIntensityBuilder provides the CapexAt DV. The DV itself (CapexAt) does not appear in the CONTROLS list — Lagged_DV (CapexAt_lag) serves as the lagged-DV control. No bad-control issue exists because the lagged DV is temporally prior.

7. **Panel builder also loads 4 IVs (CEO_QA, CEO_Pres, Manager_QA, Manager_Pres):** The H13 shared panel builder loads all 4 uncertainty measures, but the H13.1 runner only uses Manager_QA_Uncertainty_pct. The other three IVs are available in the panel but unused by this suite.
