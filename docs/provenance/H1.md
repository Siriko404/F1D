# H1 Cash Holdings -- Provenance Document

**Generated:** 2026-03-30 (manual trace)
**Auditor method:** Manual file reading, grep, line-level verification.
Every claim cites file path + line number. No documentation generators used.

---

## A. SUITE IDENTITY

```yaml
Suite ID:        H1
Title:           Speech Uncertainty and Cash Holdings
Hypothesis:      Higher managerial speech uncertainty during earnings calls is
                 associated with higher corporate cash holdings (contemporaneous
                 and one-year-ahead).
Direction:       One-tailed (beta > 0)
Model Family:    Linear panel regression with absorbed fixed effects
Estimator:       linearmodels.panel.PanelOLS
Unit of Obs:     Individual earnings call (file_name)
Panel Index:     (gvkey, cal_yr) for Year FE specs; (gvkey, cal_yr_qtr) for YQ FE specs
Columns:         12
Reference:       "--"
Runner:          src/f1d/econometric/run_h1_cash_holdings.py
Panel Builder:   src/f1d/variables/build_h1_cash_holdings_panel.py
```

---

## B. MODEL SPECIFICATION

### B1. Regression Equation

**Industry FE specs (cols 1, 3, 5, 7, 9, 11):**

$$
\text{DV}_{i,t} = \beta_1 \cdot \text{CEO\_QA\_Uncertainty\_pct}_{i,t}
+ \beta_2 \cdot \text{CEO\_Pres\_Uncertainty\_pct}_{i,t}
+ \beta_3 \cdot \text{Manager\_QA\_Uncertainty\_pct}_{i,t}
+ \beta_4 \cdot \text{Manager\_Pres\_Uncertainty\_pct}_{i,t}
+ \gamma \mathbf{X}_{i,t}
+ \alpha_{\text{Industry}(j)} + \delta_t + \varepsilon_{i,t}
$$

where $\alpha_{\text{Industry}(j)}$ is absorbed via `other_effects` (FF12 industry) and $\delta_t$ is absorbed via `time_effects=True`.

**Firm FE specs (cols 2, 4, 6, 8, 10, 12):**

$$
\text{DV}_{i,t} = \beta_1 \cdot \text{CEO\_QA\_Uncertainty\_pct}_{i,t}
+ \beta_2 \cdot \text{CEO\_Pres\_Uncertainty\_pct}_{i,t}
+ \beta_3 \cdot \text{Manager\_QA\_Uncertainty\_pct}_{i,t}
+ \beta_4 \cdot \text{Manager\_Pres\_Uncertainty\_pct}_{i,t}
+ \gamma \mathbf{X}_{i,t}
+ \alpha_i + \delta_t + \varepsilon_{i,t}
$$

where $\alpha_i$ is `EntityEffects` and $\delta_t$ is `TimeEffects`.

All 4 IVs enter simultaneously in every specification.

*Source: run_h1_cash_holdings.py, lines 343, 360--381.*

### B2. Dependent Variable(s)

| Variable Name | Description | Formula | Source Engine | Timing |
|---|---|---|---|---|
| `CashRatio` | Cash and equivalents scaled by total assets | `cheq / atq` | CompustatEngine | Contemporaneous (most recent Compustat filing <= call date, via merge_asof backward) |
| `CashRatio_lead` | Next-fiscal-year end-of-year cash holdings | CashRatio from firm's latest call in fiscal year t+1, shifted back to year t | Panel builder (fiscal-year shift) | t+1 lead |

*Source: `_compustat_engine.py` line 981 (CashRatio formula); `build_h1_cash_holdings_panel.py` lines 239--376 (lead construction).*

### B3. Independent Variable(s)

| Variable Name | Description | Formula | Source Engine | Timing |
|---|---|---|---|---|
| `UncAnsCEO` | CEO Q&A section uncertainty word percentage | Count of LM uncertainty words by CEO in Q&A / total CEO Q&A words * 100 | LinguisticEngine | Contemporaneous (call-level) |
| `UncPreCEO` | CEO presentation section uncertainty word percentage | Count of LM uncertainty words by CEO in presentation / total CEO presentation words * 100 | LinguisticEngine | Contemporaneous (call-level) |
| `UncAnsMgr` | All managers' Q&A section uncertainty word percentage | Count of LM uncertainty words by all managers in Q&A / total manager Q&A words * 100 | LinguisticEngine | Contemporaneous (call-level) |
| `UncPreMgr` | All managers' presentation section uncertainty word percentage | Count of LM uncertainty words by all managers in presentation / total manager presentation words * 100 | LinguisticEngine | Contemporaneous (call-level) |

*Source: LinguisticEngine (`_linguistic_engine.py` lines 165--264); individual builders (`ceo_qa_uncertainty.py` line 27, `ceo_pres_uncertainty.py` line 27, `manager_qa_uncertainty.py` line 33, `manager_pres_uncertainty.py` line 27).*

### B4. Control Variables

**Base Controls (8 variables):**

| Variable Name | Description | Formula | Source Engine |
|---|---|---|---|
| `Leverage` | Book leverage | `(dlcq.fillna(0) + dlttq.fillna(0)) / atq` | CompustatEngine |
| `lnAssets` | Firm size | `ln(atq)` for atq > 0; else NaN | CompustatEngine |
| `TobinsQ` | Tobin's Q | `(cshoq * prccq + debt_book) / atq` where debt_book = clipped(dlcq) + clipped(dlttq); requires atq > 0 and mktcap non-null | CompustatEngine |
| `ROA` | Return on assets | `iby_annual / avg_assets` where avg_assets = (atq_Q4 + atq_Q4_lag1) / 2; iby_annual = Q4 iby | CompustatEngine |
| `Capex` | Capital expenditure intensity | `capxy_annual / atq_annual_lag1` (Q4 YTD capex / lagged total assets); requires atq_lag > 0 | CompustatEngine |
| `DivDummy` | Dividend payer indicator | `float(dvy_annual > 0)` where dvy_annual = Q4 full-year YTD common dividends; missing -> 0 | CompustatEngine |
| `sCFO` | Operating cash flow volatility | Rolling 5-year std of `(oancfy / atq_lag)` per gvkey, min 3 periods, using Q4 annual observations; atq_lag = prior year's atq | CompustatEngine |
| `Lagged_DV` | Lagged dependent variable | `CashRatio_lag` (previous fiscal year's end-of-year CashRatio, requiring consecutive fiscal years) | Panel builder |

*Source: `_compustat_engine.py` lines 938, 943, 988--992, 954--964, 994--999, 1004--1007, 303--352; `build_h1_cash_holdings_panel.py` lines 379--447; `run_h1_cash_holdings.py` lines 256--260.*

**Extended Controls (Base + 4 additional = 12 variables):**

| Variable Name | Description | Formula | Source Engine |
|---|---|---|---|
| `SalesGrowth` | Revenue growth | `(sale_t - sale_lag) / abs(sale_lag)` where sale = saley (annual, Q4 YTD; fallback saleq); requires consecutive fiscal years | CompustatEngine (via Biddle) |
| `RDSales` | R&D intensity | `xrdq.fillna(0) / atq` | CompustatEngine |
| `CashFlowAt` | Operating cash flow | `oancfy / avg_assets` where avg_assets = (atq_t + atq_lag) / 2; Q4 annual; fallback to end-of-year atq if lag missing | CompustatEngine (via Biddle) |
| `DailyVola` | Annualized stock return volatility | `std(daily_ret) * sqrt(252) * 100` over window [start_date + 1 day, next_call_date - 5 days]; min 10 trading days | CRSPEngine |

*Source: `_compustat_engine.py` lines 644--688 (SalesGrowth, CashFlowAt); line 967 (RDSales); `_crsp_engine.py` lines 247, 255 (DailyVola).*

### B5. Fixed Effects

| FE Type | Column Used | Description | How Absorbed |
|---|---|---|---|
| Industry FE (FF12) | `ff12_code` | Fama-French 12-industry classification | `other_effects=industry_data` in PanelOLS constructor (absorbed, not dummies) |
| Firm FE | `gvkey` | Firm entity effects | `EntityEffects` in PanelOLS.from_formula |
| Calendar Year FE | `cal_yr` | Calendar year from call start_date | `time_effects=True` (Year FE specs) via MultiIndex second level |
| Calendar Year-Quarter FE | `cal_yr_qtr` | Calendar year * 10 + quarter (e.g., 20103) | `time_effects=True` (YQ FE specs) via MultiIndex second level |

*Source: `run_h1_cash_holdings.py` lines 346--381; `panel_utils.py` lines 195--218 (cal_yr_qtr construction).*

### B6. Standard Errors and Clustering

| Parameter | Value |
|---|---|
| `cov_type` | `"clustered"` |
| `cluster_entity` | `True` |
| Cluster dimension | Firm (`gvkey`) |

*Source: `run_h1_cash_holdings.py` lines 375, 381.*

### B7. Hypothesis Test

| Parameter | Value |
|---|---|
| Direction | One-tailed: beta > 0 |
| Two-tailed p-value source | `model.pvalues` (from PanelOLS fit) |
| One-tailed conversion | `p_one = p_two / 2` if `beta > 0`; else `p_one = 1 - p_two / 2` |
| Significance thresholds | `***` p < 0.01, `**` p < 0.05, `*` p < 0.10 |

*Source: `run_h1_cash_holdings.py` lines 403--421.*

---

## C. MODEL SPECIFICATION REGISTER

| Col | DV | Entity FE | Time FE | Controls | Notes |
|-----|---|-----------|---------|----------|-------|
| 1 | CashRatio | Industry (FF12) | Calendar Year | Base (8) | `other_effects` + `time_effects` |
| 2 | CashRatio | Firm | Calendar Year | Base (8) | `EntityEffects` + `TimeEffects` |
| 3 | CashRatio | Industry (FF12) | Calendar Year | Extended (12) | `other_effects` + `time_effects` |
| 4 | CashRatio | Firm | Calendar Year | Extended (12) | `EntityEffects` + `TimeEffects` |
| 5 | CashRatio | Industry (FF12) | Cal Year-Quarter | Extended (12) | `other_effects` + `time_effects`; panel index uses `cal_yr_qtr` |
| 6 | CashRatio | Firm | Cal Year-Quarter | Extended (12) | `EntityEffects` + `TimeEffects`; panel index uses `cal_yr_qtr` |
| 7 | CashRatio_lead | Industry (FF12) | Calendar Year | Base (8) | `other_effects` + `time_effects` |
| 8 | CashRatio_lead | Firm | Calendar Year | Base (8) | `EntityEffects` + `TimeEffects` |
| 9 | CashRatio_lead | Industry (FF12) | Calendar Year | Extended (12) | `other_effects` + `time_effects` |
| 10 | CashRatio_lead | Firm | Calendar Year | Extended (12) | `EntityEffects` + `TimeEffects` |
| 11 | CashRatio_lead | Industry (FF12) | Cal Year-Quarter | Extended (12) | `other_effects` + `time_effects`; panel index uses `cal_yr_qtr` |
| 12 | CashRatio_lead | Firm | Cal Year-Quarter | Extended (12) | `EntityEffects` + `TimeEffects`; panel index uses `cal_yr_qtr` |

*Source: `run_h1_cash_holdings.py` lines 105--119 (MODEL_SPECS list).*

---

## D. SAMPLE CONSTRUCTION

### D1. Population

| Parameter | Value |
|---|---|
| Starting dataset | `outputs/variables/h1_cash_holdings/latest/h1_cash_holdings_panel.parquet` |
| Source manifest | `outputs/1.4_AssembleManifest/latest/master_sample_manifest.parquet` |
| Year range | 2002--2018 (from `config/project.yaml` lines 6--7) |
| Unit of observation | Individual earnings call (`file_name`) |

### D2. Exclusion Criteria (Attrition Cascade)

The runner applies filters sequentially per model spec. The exact row counts depend on the data; the attrition stages are:

| Step | Filter | Applied Where | Notes |
|------|--------|---------------|-------|
| 1 | Full panel load | `load_panel()` (line 213) | All calls from Stage 3 parquet |
| 2 | Main sample filter | `filter_main_sample()` (line 229) | Exclude ff12_code in {8, 11} (Utility, Finance) |
| 3 | Replace inf with NaN | `prepare_regression_data()` (line 276) | All inf -> NaN |
| 4 | DV non-null | `prepare_regression_data()` (line 286) | Drop rows where DV column is NaN |
| 5 | Complete cases | `prepare_regression_data()` (line 290--291) | Require all of: DV, 4 IVs, all controls, gvkey, fyearq_int, ff12_code (+ cal_yr_qtr for YQ specs) non-null |
| 6 | Min calls per firm | `prepare_regression_data()` (lines 295--297) | >= 5 calls per gvkey (`MIN_CALLS_PER_FIRM = 5`) |

*Source: `run_h1_cash_holdings.py` lines 122, 226--303.*

The runner records an attrition table at lines 820--829, saved to `sample_attrition.csv`.

**Numeric attrition counts** (actual counts are runtime-determined per spec; representative col-1 counts from latest run):

| Step | Filter | Rows Before | Rows After | Dropped |
|------|--------|-------------|------------|---------|
| 1 | Full panel load | — | see parquet | — |
| 2 | Main sample (excl Finance/Utility, ff12_code in {8,11}) | see parquet | see attrition CSV | see attrition CSV |
| 3 | Replace inf with NaN | — | — | 0 |
| 4 | DV non-null (per spec) | see attrition CSV | see attrition CSV | see attrition CSV |
| 5 | Complete cases (DV + IVs + controls + required cols) | see attrition CSV | see attrition CSV | see attrition CSV |
| 6 | Min >= 5 calls per firm | see attrition CSV | see attrition CSV | see attrition CSV |

*Exact per-column counts are in `outputs/econometric/h1_cash_holdings/2026-03-27_094942/sample_attrition.csv` (generated at runner line 829 via `generate_attrition_table()`).*

### D3. Sample Counts per Spec

N varies across specs because:
- Cols 7--12 use `CashRatio_lead` which has additional NaN from last-fiscal-year firms and fiscal year gaps.
- YQ FE specs (cols 5--6, 11--12) additionally require `cal_yr_qtr` non-null.
- Extended control specs may drop more rows due to missing `SalesGrowth`, `RDSales`, `CashFlowAt`, or `DailyVola`.

The `model_diagnostics.csv` output contains the exact `n_obs` and `n_firms` per column.

---

## E. VARIABLE DICTIONARY

| Variable (code) | Label | Type | Formula | Source | Winsorized | Timing |
|---|---|---|---|---|---|---|
| `CashRatio` | Cash Holdings | DV | `cheq / atq` | CompustatEngine (`_compustat_engine.py` L981) | 1%/99% by fiscal year | Contemporaneous (merge_asof backward) |
| `CashRatio_lead` | Cash Holdings t+1 | DV | End-of-FY cash for fiscal year t+1, shifted to t; requires consecutive FY | Panel builder (`build_h1_cash_holdings_panel.py` L239--376) | Inherits winsorization from CashRatio | t+1 lead (fiscal year) |
| `CashRatio_lag` | Cash Holdings t-1 | Intermediate | End-of-FY cash for fiscal year t-1, shifted to t; requires consecutive FY | Panel builder (`build_h1_cash_holdings_panel.py` L379--447) | Inherits winsorization from CashRatio | t-1 lag (fiscal year) |
| `Lagged_DV` | Lagged DV | Control | Alias for `CashRatio_lag` (dynamically assigned at regression time from `{base_dv}_lag`) | Runner (`run_h1_cash_holdings.py` L256--260) | Same as CashRatio | t-1 |
| `UncAnsCEO` | CEO QA Uncertainty | IV | LM uncertainty word count by CEO in Q&A / total CEO Q&A words * 100 | LinguisticEngine (`_linguistic_engine.py`) -> Stage 2 parquets | 0%/99% upper-only per year (L255--258) | Contemporaneous (call-level) |
| `UncPreCEO` | CEO Pres Uncertainty | IV | LM uncertainty word count by CEO in presentation / total CEO presentation words * 100 | LinguisticEngine | 0%/99% upper-only per year | Contemporaneous (call-level) |
| `UncAnsMgr` | Mgr QA Uncertainty | IV | LM uncertainty word count by all managers in Q&A / total manager Q&A words * 100 | LinguisticEngine | 0%/99% upper-only per year | Contemporaneous (call-level) |
| `UncPreMgr` | Mgr Pres Uncertainty | IV | LM uncertainty word count by all managers in presentation / total manager presentation words * 100 | LinguisticEngine | 0%/99% upper-only per year | Contemporaneous (call-level) |
| `Leverage` | Leverage | Control | `(dlcq.fillna(0) + dlttq.fillna(0)) / atq` | CompustatEngine (`_compustat_engine.py` L943) | 1%/99% by fiscal year | Contemporaneous |
| `lnAssets` | Firm Size (log AT) | Control | `ln(atq)` for atq > 0; NaN otherwise | CompustatEngine (`_compustat_engine.py` L938) | 1%/99% by fiscal year | Contemporaneous |
| `TobinsQ` | Tobin's Q | Control | `(cshoq * prccq + debt_book) / atq`; debt_book = clipped(dlcq>=0).fillna(0) + clipped(dlttq>=0).fillna(0); NaN if both dlcq and dlttq missing, atq<=0, or mktcap missing | CompustatEngine (`_compustat_engine.py` L982--992) | 1%/99% by fiscal year | Contemporaneous |
| `ROA` | Return on Assets | Control | `iby_annual / avg_assets`; iby_annual = Q4 iby (full-year income); avg_assets = (atq_Q4 + atq_Q4_lag1) / 2 | CompustatEngine (`_compustat_engine.py` L954--964) | 1%/99% by fiscal year | Contemporaneous (annual, joined to all quarters via fyearq) |
| `Capex` | CapEx / Assets | Control | `capxy_annual / atq_annual_lag1`; capxy_annual = Q4 YTD capex (full fiscal year); denominator = prior-year total assets | CompustatEngine (`_compustat_engine.py` L994--999) | 1%/99% by fiscal year | Contemporaneous (annual, joined to all quarters) |
| `DivDummy` | Dividend Payer | Control | `float(dvy_annual.fillna(0) > 0)`; dvy_annual = Q4 full-year common dividends YTD | CompustatEngine (`_compustat_engine.py` L1004--1007) | Not winsorized (binary; in skip_winsorize set, L1124) | Contemporaneous (annual, joined to all quarters) |
| `sCFO` | OCF Volatility | Control | Rolling 5-year std of `(oancfy / atq_lag)` per gvkey, min 3 periods; Q4-only annual observations; dummy_date rolling window = 1826 days | CompustatEngine (`_compustat_engine.py` L303--352) | 1%/99% by fiscal year | Contemporaneous (annual, joined to all quarters) |
| `SalesGrowth` | Sales Growth | Control (Ext.) | `(sale_t - sale_lag) / abs(sale_lag)`; sale = saley (annual YTD; fallback saleq); requires consecutive fiscal years; inf -> NaN | CompustatEngine via Biddle (`_compustat_engine.py` L644--661) | 1%/99% by fiscal year (inside Biddle; NOT double-winsorized post-join, L1125--1126) | Contemporaneous (annual, joined to all quarters) |
| `RDSales` | R&D Intensity | Control (Ext.) | `xrdq.fillna(0) / atq` | CompustatEngine (`_compustat_engine.py` L967) | 1%/99% by fiscal year | Contemporaneous |
| `CashFlowAt` | Cash Flow | Control (Ext.) | `oancfy / avg_assets`; avg_assets = (atq + at_lag) / 2 (fallback atq if lag missing); Q4 annual; inf -> NaN | CompustatEngine via Biddle (`_compustat_engine.py` L674--688) | 1%/99% by fiscal year (inside Biddle; NOT double-winsorized, L1125--1126) | Contemporaneous (annual, joined to all quarters) |
| `DailyVola` | Stock Volatility | Control (Ext.) | `std(daily_RET) * sqrt(252) * 100` over [start_date + 1 day, next_call_date - 5 days]; min 10 trading days | CRSPEngine (`_crsp_engine.py` L255) | 1%/99% by year (per-year winsorization at L445--447) | Inter-call window |
| `ff12_code` | FF12 Industry Code | Classification | Fama-French 12-industry code from manifest | ManifestFieldsBuilder | N/A | Static per firm-period |
| `gvkey` | Firm Identifier | Index | Compustat permanent company key | ManifestFieldsBuilder | N/A | Static |
| `cal_yr` | Calendar Year | FE Index | `start_date.dt.year` | `panel_utils.build_cal_yr_qtr_index()` L215 | N/A | Call date |
| `cal_yr_qtr` | Calendar Year-Quarter | FE Index | `cal_yr * 10 + cal_qtr` (e.g., 20103) | `panel_utils.build_cal_yr_qtr_index()` L217 | N/A | Call date |
| `fyearq_int` | Fiscal Year | Intermediate | Compustat fiscal year from merge_asof (call start_date -> Compustat datadate) | `panel_utils.attach_fyearq()` -> CompustatEngine | N/A | Compustat fiscal year |

---

## F. DATA PIPELINE

### F1. Dependency Chain

1. **Raw data** -- Compustat quarterly (`inputs/comp_na_daily_all/comp_na_daily_all.parquet`), CRSP daily (`inputs/CRSP_DSF/CRSP_DSF_{year}_Q{q}.parquet`), LM Dictionary, Earnings call transcripts, CCM linktable
2. **Stage 1** -- Manifest assembly -> `outputs/1.4_AssembleManifest/latest/master_sample_manifest.parquet`
3. **Stage 2** -- Textual analysis -> `outputs/2_Textual_Analysis/2.2_Variables/latest/linguistic_variables_{year}.parquet`
4. **Stage 3 (Panel Builder)** -- `build_h1_cash_holdings_panel.py` merges manifest + linguistic + Compustat + CRSP variables into one call-level panel -> `outputs/variables/h1_cash_holdings/{timestamp}/h1_cash_holdings_panel.parquet`
5. **Stage 4 (Runner)** -- `run_h1_cash_holdings.py` loads panel, filters, runs 12 PanelOLS regressions, outputs LaTeX table + diagnostics -> `outputs/econometric/h1_cash_holdings/{timestamp}/`
6. **Table Generation** -- `outputs/generate_all_tables.py` reads regression `.txt` files and produces final formatted LaTeX tables

### F2. Data Engines Used

| Engine | Source Data | Variables Provided |
|--------|------------|-------------------|
| CompustatEngine (`_compustat_engine.py`) | `inputs/comp_na_daily_all/comp_na_daily_all.parquet` | CashRatio, Leverage, lnAssets, TobinsQ, ROA, Capex, DivDummy, sCFO, RDSales, CashFlowAt, SalesGrowth |
| LinguisticEngine (`_linguistic_engine.py`) | `outputs/2_Textual_Analysis/2.2_Variables/latest/linguistic_variables_{year}.parquet` | UncAnsCEO, UncPreCEO, UncAnsMgr, UncPreMgr |
| CRSPEngine (`_crsp_engine.py`) | `inputs/CRSP_DSF/CRSP_DSF_{year}_Q{q}.parquet` | DailyVola |
| ManifestFieldsBuilder (`manifest_fields.py`) | `outputs/1.4_AssembleManifest/latest/master_sample_manifest.parquet` | file_name, gvkey, ceo_id, ceo_name, ff12_code, ff12_name, start_date |

All three engines (Compustat, Linguistic, CRSP) are module-level singletons. Data is loaded once per process and cached.

### F3. Merge Operations

| Step | Left | Right | Keys | Type | Notes |
|------|------|-------|------|------|-------|
| 1 | Manifest (base) | Each builder result | `file_name` | left | Zero-row-delta enforced (ValueError if row count changes). Builder outputs must have unique file_name. |
| 2 | Manifest (inside each Compustat builder) | CompustatEngine cache | `gvkey` (by), `start_date` <= `datadate` (backward) | merge_asof | Links call to most recent Compustat filing |
| 3 | Manifest (inside CRSP builder) | CRSPEngine cache | `file_name` (map) | map | File_name-keyed volatility lookup |
| 4 | Panel | fyearq attachment | `gvkey` (by), `start_date` <= `datadate` (backward) | merge_asof | Attaches Compustat fiscal year to each call |
| 5 | Panel | CashRatio_lead lookup | `gvkey`, `fyearq_int` | left | Firm-year-level lead merged to call-level; zero-row-delta enforced |
| 6 | Panel | CashRatio_lag lookup | `gvkey`, `fyearq_int` | left | Firm-year-level lag merged to call-level; zero-row-delta enforced |

*Source: `build_h1_cash_holdings_panel.py` lines 182--216 (Step 1), 210 (left merge); `_compustat_engine.py` lines 1222--1229 (Step 2); `build_h1_cash_holdings_panel.py` lines 278--284, 357--367, 432--443 (Steps 4--6).*

---

## G. OUTPUTS

### G1. Stage 3 Outputs (Panel Builder)

| File | Description |
|------|-------------|
| `outputs/variables/h1_cash_holdings/{timestamp}/h1_cash_holdings_panel.parquet` | Call-level panel with all variables |
| `outputs/variables/h1_cash_holdings/{timestamp}/summary_stats.csv` | Per-builder summary statistics |
| `outputs/variables/h1_cash_holdings/{timestamp}/report_step3_h1.md` | Build report with coverage stats |
| `outputs/variables/h1_cash_holdings/{timestamp}/run_manifest.json` | Reproducibility manifest |

*Source: `build_h1_cash_holdings_panel.py` lines 521--546.*

### G2. Stage 4 Outputs (Runner)

| File | Description |
|------|-------------|
| `outputs/econometric/h1_cash_holdings/{timestamp}/h1_cash_holdings_table.tex` | 12-column LaTeX regression table |
| `outputs/econometric/h1_cash_holdings/{timestamp}/model_diagnostics.csv` | Per-column metadata: N, R2, adj_R2, per-IV betas/SEs/p-values |
| `outputs/econometric/h1_cash_holdings/{timestamp}/summary_stats.csv` | Summary statistics (main sample) |
| `outputs/econometric/h1_cash_holdings/{timestamp}/summary_stats.tex` | Summary statistics LaTeX table |
| `outputs/econometric/h1_cash_holdings/{timestamp}/report_step4_H1.md` | Results report with all IV coefficients |
| `outputs/econometric/h1_cash_holdings/{timestamp}/sample_attrition.csv` | Attrition cascade counts |
| `outputs/econometric/h1_cash_holdings/{timestamp}/sample_attrition.tex` | Attrition cascade LaTeX |
| `outputs/econometric/h1_cash_holdings/{timestamp}/run_manifest.json` | Reproducibility manifest |
| `outputs/econometric/h1_cash_holdings/{timestamp}/regression_results_col{N}.txt` | Full PanelOLS summary per column (1--12) |

*Source: `run_h1_cash_holdings.py` lines 39--46 (docstring), 593--596, 610--638, 716--719, 782--793, 829, 833--844.*

### G3. Summary Statistics

Generated by `make_summary_stats_table()` on the Main sample (post ff12 filter, pre-regression).

Variables included (from `SUMMARY_STATS_VARS`, lines 131--151):

| Column | Label |
|--------|-------|
| CashRatio | Cash Holdings$_t$ |
| CashRatio_lead | Cash Holdings$_{t+1}$ |
| UncAnsCEO | CEO QA Uncertainty |
| UncPreCEO | CEO Pres Uncertainty |
| UncAnsMgr | Mgr QA Uncertainty |
| UncPreMgr | Mgr Pres Uncertainty |
| Leverage | Leverage |
| lnAssets | Firm Size (log AT) |
| TobinsQ | Tobin's Q |
| ROA | ROA |
| Capex | CapEx / Assets |
| DivDummy | Dividend Payer |
| sCFO | OCF Volatility |
| SalesGrowth | Sales Growth |
| RDSales | R&D Intensity |
| CashFlowAt | Cash Flow |
| DailyVola | Stock Volatility |

---

## H. OUTLIER AND MISSING DATA TREATMENT

### H1. Winsorization

| Scope | Level | Percentiles | Where Applied |
|-------|-------|-------------|---------------|
| Compustat variables (CashRatio, Leverage, lnAssets, TobinsQ, ROA, Capex, sCFO, RDSales) | Per fiscal year (fyearq) | 1% / 99% | CompustatEngine `_compute_and_winsorize()` lines 1129--1136; `_winsorize_by_year()` lines 439--463 |
| CashFlowAt, SalesGrowth | Per fiscal year (fyearq) | 1% / 99% | Inside `_compute_biddle_residual()` (lines 661, 688); excluded from post-OLS winsorization to avoid double-winsorization (skip_winsorize set, lines 1123--1126) |
| DivDummy | Not winsorized | N/A | Binary variable; in skip_winsorize set (line 1124) |
| Linguistic variables (all 4 IVs) | Per calendar year | 0% / 99% (upper-only) | LinguisticEngine `get_data()` lines 255--258; uses `winsorize_by_year()` with `lower=0.0, upper=0.99` |
| CRSP variables (DailyVola) | Per calendar year | 1% / 99% | CRSPEngine lines 445--447 |

Minimum observations for winsorization: 10 per year group (all engines).

*Source: `_compustat_engine.py` lines 439--463, 1123--1136; `_linguistic_engine.py` lines 255--258; `_crsp_engine.py` lines 445--447; `winsorization.py` lines 23--84.*

### H2. Missing Data Policy

- **Complete-case deletion**: All regression specifications require non-null values for: DV, all 4 IVs, all controls in the spec, gvkey, fyearq_int, ff12_code (and cal_yr_qtr for YQ specs). Applied in `prepare_regression_data()` lines 289--291.
- **Inf replacement**: All inf values replaced with NaN before filtering (line 276).
- **Minimum calls per firm**: Firms with < 5 calls in the post-filter sample are dropped entirely (line 296).
- **Missing debt in Leverage**: dlcq and dlttq individually filled with 0 before summing (line 943 of `_compustat_engine.py`).
- **Missing R&D in RDSales**: xrdq filled with 0 (line 967 of `_compustat_engine.py`).
- **Missing dvy**: Filled with 0 for DivDummy (line 1006).

### H3. Transformations

| Transformation | Variable | Details |
|---|---|---|
| Natural log | lnAssets | `ln(atq)` for atq > 0 |
| Annualization | DailyVola | `std(daily_ret) * sqrt(252) * 100` |
| Percentage scaling | All linguistic IVs | Already expressed as percentages (word count / total words * 100) at Stage 2 |

No z-scoring, centering, or standardization is applied.

---

## I. GENERATE_ALL_TABLES.PY ENTRY

```python
{
    "id": "H1",
    "dir": "h1_cash_holdings/2026-03-27_094942",
    "caption": "H1: Speech Uncertainty and Cash Holdings",
    "label": "tab:h1",
    "cols": 12,
    "dvs": [
        ("CashRatio", 6),
        (r"CashRatio\_lead", 6),
    ],
    "tail": "one",
    "hyp_dir": ">",
},
```

*Source: `outputs/generate_all_tables.py` lines 20--32.*

**Verification against runner:**

| Field | generate_all_tables.py | Runner | Match? |
|-------|----------------------|--------|--------|
| `cols` | 12 | `len(MODEL_SPECS)` = 12 | YES |
| `tail` | `"one"` | One-tailed (lines 31, 403--411) | YES |
| `hyp_dir` | `">"` | `beta > 0` (line 32, 411) | YES |
| DV split | CashRatio (6), CashRatio_lead (6) | Cols 1--6 = CashRatio, Cols 7--12 = CashRatio_lead | YES |

---

## J. REPRODUCTION COMMANDS

```bash
# From project root (F1D/)

# Step 1: Build the Stage 3 panel
python -m f1d.variables.build_h1_cash_holdings_panel

# Step 2: Run the Stage 4 regressions
python -m f1d.econometric.run_h1_cash_holdings

# Step 3 (optional): Regenerate formatted tables
cd outputs && python generate_all_tables.py && cd ..

# Dry-run validation (no execution)
python -m f1d.econometric.run_h1_cash_holdings --dry-run
python -m f1d.variables.build_h1_cash_holdings_panel --dry-run

# Override panel path
python -m f1d.econometric.run_h1_cash_holdings --panel-path outputs/variables/h1_cash_holdings/2026-03-27_094942/h1_cash_holdings_panel.parquet
```

---

## K. MODEL-FAMILY ADDENDUM

### K1. PanelOLS

| Parameter | Industry FE specs (cols 1,3,5,7,9,11) | Firm FE specs (cols 2,4,6,8,10,12) |
|---|---|---|
| Constructor | `PanelOLS(dependent, exog, entity_effects=False, time_effects=True, other_effects=industry_data, drop_absorbed=True, check_rank=False)` | `PanelOLS.from_formula(formula, data=df_panel, drop_absorbed=True)` |
| `entity_effects` | `False` | Implicit `True` via `EntityEffects` in formula |
| `time_effects` | `True` | Implicit `True` via `TimeEffects` in formula |
| `other_effects` | `df_panel["ff12_code"]` (FF12 industry code) | Not used |
| `drop_absorbed` | `True` | `True` |
| `check_rank` | `False` | Default (`True`) |
| Formula | N/A (constructor API) | `"{dv} ~ 1 + {exog_str} + EntityEffects + TimeEffects"` |
| Singleton handling | `drop_absorbed=True` drops singleton groups | Same |
| R-squared | `model.rsquared` (within R-squared from PanelOLS; R² on demeaned data after absorbing FE; NOT overall R²; use `model.rsquared_overall` for overall R²) | Same |
| Adj. R-squared | Manually computed: `1 - (1 - R2) * (nobs - 1) / df_resid` | Same |

*Source: `run_h1_cash_holdings.py` lines 360--381 (model construction), 388--400 (R2 computation).*

### K2. Cox PH
N/A

### K3. Logit/Probit
N/A

### K4. IV/2SLS
N/A

### K5. OLS
N/A

---

## L. KNOWN ISSUES AND NOTES

1. **Contemporaneous DV is constant within firm-quarter.** CashRatio is matched via merge_asof from the most recent Compustat filing. All calls in the same firm-quarter receive the same CashRatio value. The table footnote (line 586) acknowledges this and directs interpretation alongside the lead DV.

2. **Industry FE uses `check_rank=False`.** For the industry FE specs, `check_rank=False` is passed to `PanelOLS` (line 373). This suppresses rank-deficiency warnings that can arise from collinear absorbed effects. The firm FE specs do not set this flag (use default `True`).

3. **Lagged DV construction uses fiscal year, not calendar year.** The `Lagged_DV` control is `CashRatio_lag`, which is the previous fiscal year's end-of-year CashRatio (from the latest call in that fiscal year). This correctly handles non-December fiscal year-end firms (~30% of sample). The lag requires consecutive fiscal years; gaps produce NaN.

4. **Lead variable fiscal year continuity validation.** `CashRatio_lead` is set to NaN if the firm's next fiscal year is not exactly fyearq + 1 (line 331 of the panel builder). This prevents assigning misleading lead values across fiscal year gaps.

5. **Docstring comment mismatch.** Runner line 794 says "8 model specifications" but there are 12 MODEL_SPECS (verified at line 105--119). This is a stale comment.

6. **Attrition table labels partially misleading.** The runner's attrition table (lines 823--828) labels step 3 as "After lead filter (col 5-8 only)" but the lead DV actually applies to cols 7--12 (not 5--8). The label is inaccurate.

7. **Linguistic winsorization is upper-only (0%/99%).** The LinguisticEngine uses `lower=0.0, upper=0.99` (line 257 of `_linguistic_engine.py`). This means the lower bound is 0th percentile (no left-tail clipping), and only the 99th percentile upper bound is applied. The Compustat and CRSP engines use symmetric 1%/99%.

8. **CRSP DailyVola window definition.** The DailyVola window runs from [call start_date + 1 day] to [next_call_date - 5 days], not from [prev_call_date + 5 days] as stated in `volatility.py`'s docstring (line 7). The docstring is misleading; the actual code at `_crsp_engine.py` lines 361--366 uses `start_date + DAYS_AFTER_CURRENT_CALL` (1 day) and `next_call_date - DAYS_BEFORE_NEXT_CALL` (5 days). This represents a POST-call window looking forward to the next call.

9. **Computed-but-unused variables in panel.** The panel builder (`build_h1_cash_holdings_panel.py`) imports and executes three builders that produce columns in the Stage 3 parquet file but are not referenced in any H1 regression specification:
   - `CurrentRatioBuilder` (lines 78, 152): produces `CurrentRatio = actq / lctq`
   - `AnalystQAUncertaintyBuilder` (lines 137--139): produces `AnalystQA_Uncertainty_pct`
   - `NegativeSentimentBuilder` (lines 140--142): produces `NegativeSentiment_pct`

   None of these appear in `run_h1_cash_holdings.py` (`KEY_IVS`, `BASE_CONTROLS`, or `EXTENDED_CONTROLS`). They are present in the parquet and do not affect regression results. They may be retained for downstream reuse by other suites.

10. **Stale module-level docstring in panel builder.** Lines 16--18 of `build_h1_cash_holdings_panel.py` describe the `CashRatio_lead` construction as "Average CashRatio within (gvkey, call_year) -> firm-year mean". This description is stale and incorrect relative to the actual code. The function `create_lead_variable()` (lines 308--334) uses `idxmax()` to select the LATEST call within each `(gvkey, fyearq_int)` group, not an average. The module-level docstring has not been updated to reflect this fiscal-year fix.

11. **IBES listed as raw input in builder docstring but not used.** Builder docstring line 31 lists `inputs/tr_ibes/tr_ibes.parquet` (IBES data) as a raw input dependency. No IBES engine, IBES builder, or `IbesEngine` class is imported or called anywhere in `build_h1_cash_holdings_panel.py`. This is a stale artifact. The H1 suite does not use IBES data.

---

*End of provenance document.*
