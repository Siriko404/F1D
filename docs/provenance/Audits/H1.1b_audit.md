================================================================================
ADVERSARIAL AUDIT REPORT: Suite H1.1b
Provenance document: docs/provenance/H1.1b.md
Runner: src/f1d/econometric/run_h1_1b_cash_tsimm_binary.py
Panel builder: src/f1d/variables/build_h1_cash_holdings_panel.py
Auditor: Claude Sonnet 4.6 (adversarial mode)
Date: 2026-03-31
================================================================================

---

## AUDIT SUMMARY

| Category | Total Checks | Passed | Failed | Score |
|----------|-------------|--------|--------|-------|
| Structural Completeness (Phase 1) | 27 | 25 | 2 | 93% |
| Suite Identity (Phase 2) | 10 | 10 | 0 | 100% |
| Model Specification (Phase 3) | 7 | 7 | 0 | 100% |
| Spec Register (Phase 4) | 6 | 6 | 0 | 100% |
| Sample Construction (Phase 5) | 4 | 4 | 0 | 100% |
| Variable Dictionary (Phase 6) | 18 | 18 | 0 | 100% |
| Pipeline/Outputs/Treatment (Phase 7) | 12 | 11 | 1 | 92% |
| Table Generator Entry (Phase 8) | 5 | 4 | 1 | 80% |
| Model-Family Addendum (Phase 9) | 6 | 6 | 0 | 100% |
| Quality Gates (Phase 10) | 10 | 8 | 2 | 80% |
| Cross-Reference Consistency (Phase 11) | 8 | 7 | 1 | 88% |
| **TOTAL** | **113** | **106** | **7** | **94%** |

---

## VERDICT

**FAIL -- INACCURATE**: The provenance document contains one critical factual error
that propagates through two sections:

1. Section I claims H1.1b has NO entry in generate_all_tables.py. This is WRONG.
   An entry exists and was verified (id="H1.1b", type="moderation", cols=2,
   key_tails=["one_pos","two","two"]).
2. Section F1 Step 7 repeats the same false claim ("does NOT have an entry").
3. Quality gates 4 and 5 are partially met: gate 4 lacks actual row counts (runtime-
   dependent, appropriately flagged with [UNVERIFIED]), and gate 5 cannot be fully
   asserted because the doc denies the existence of the generate_all_tables.py entry
   that would allow verification.
4. One minor line-range discrepancy in B7 source citation (391-393 should be 391-394).

All other claims -- model spec, variable dictionary, controls, FE, SE, sample
construction logic, output files, panel builder -- verified as accurate.

---

## FAILURES (detailed)

| Phase | Check | Provenance Doc Claims | Actual Code Says | Severity | Fix Required |
|-------|-------|----------------------|-----------------|----------|-------------|
| 8/I | Section I: existence of generate_all_tables.py entry | "H1.1b does NOT have an entry in `outputs/generate_all_tables.py` as of 2026-03-30. The parent suite H1.1 has an entry, but H1.1b (binary) does not." | H1.1b DOES have an entry: id="H1.1b", type="moderation", dir="h1_1b_cash_tsimm_binary/2026-03-31_155928", cols=2, key_vars=["Manager_QA_Unc_c","HighTSIMM","MgrQAUnc_x_HighTSIMM"], key_tails=["one_pos","two","two"] | CRITICAL | Rewrite Section I with the actual entry |
| 7/F1 | F1 Step 7 (table generation) | "H1.1b does NOT have an entry in `outputs/generate_all_tables.py`. The LaTeX table is generated directly by the runner's `_save_latex_table()` function (lines 462-581)." | Both statements are partially true: the runner DOES write a LaTeX table directly, AND generate_all_tables.py DOES have an entry for H1.1b. The claim "does NOT have an entry" is factually wrong. | HIGH | Update F1 Step 7 to reflect that the entry exists |
| 3/B7 | Source line range for one-tailed p-value | "lines 391-393 (one-tailed IV)" | The relevant code block spans lines 391-394 (4 lines: if-condition plus p_two/2 else 1-p_two/2) | MINOR | Change "391-393" to "391-394" |
| 10/Gate5 | Quality gate 5: tail direction match between runner and generate_all_tables.py | Doc cannot assert match because it claims no entry exists | Actual entry has key_tails=["one_pos","two","two"] which matches runner lines 391-394 (one_pos for IV) and lines 398-402 (two for moderator and interaction). The underlying match is correct but the doc cannot verify it. | MEDIUM (consequence of Section I error) | Fixed automatically once Section I is corrected |

---

================================================================================
PHASE 1: STRUCTURAL COMPLETENESS
================================================================================

Read docs/Prompts/Suite Provenance Doc.txt to identify required sections A through L.
Then verified presence and completeness in docs/provenance/H1.1b.md.

| Section | Required by Prompt | Present in Doc | Complete | Notes |
|---------|-------------------|----------------|----------|-------|
| A. Suite Identity | Yes | YES | YES | YAML block present; all 12 fields populated |
| B. Model Specification | Yes | YES | YES | All sub-sections present |
| B1. Regression Equation | Yes | YES | YES | LaTeX equation with component explanations |
| B2. Dependent Variable(s) | Yes | YES | YES | Table with all 5 required columns |
| B3. Independent Variable(s) | Yes | YES | YES | Table + centering note + 6-step HighTSIMM construction |
| B4. Control Variables | Yes | YES | YES | Single extended block table; Lagged_DV construction documented |
| B5. Fixed Effects | Yes | YES | YES | 3-row table with FE type, column, description, specification |
| B6. Standard Errors | Yes | YES | YES | cov_type and cluster documented with line reference |
| B7. Hypothesis Test | Yes | YES | YES | Per-variable tail direction table with p-value computation |
| C. Spec Register | Yes | YES | YES | 2-row table matching 2 MODEL_SPECS |
| D. Sample Construction | Yes | YES | YES | All sub-sections present |
| D1. Population | Yes | YES | YES | Starting dataset, scope stated |
| D2. Exclusion Criteria | Yes | YES | PARTIAL | 7-step cascade described; row counts [UNVERIFIED] with explanation |
| D3. Sample Counts per Spec | Yes | YES | YES | Notes N may differ; references model_diagnostics.csv |
| E. Variable Dictionary | Yes | YES | YES | 17 rows with all required columns |
| F. Data Pipeline | Yes | YES | YES | All sub-sections present |
| F1. Dependency Chain | Yes | YES | FAIL | Step 7 incorrectly states no generate_all_tables.py entry exists |
| F2. Data Engines | Yes | YES | YES | 4 engines listed with source and variables |
| F3. Merge Operations | Yes | YES | YES | Stage 3 and Stage 4 merges documented with keys and join types |
| G. Outputs | Yes | YES | YES | All sub-sections present |
| G1. Stage 3 Outputs | Yes | YES | YES | 4 files listed (parquet, summary_stats.csv, report, run_manifest.json) |
| G2. Stage 4 Outputs | Yes | YES | YES | 10 files listed |
| G3. Summary Statistics | Yes | YES | YES | 16 variables listed; metrics noted |
| H. Outlier/Missing Treatment | Yes | YES | YES | All 3 sub-sections present |
| I. generate_all_tables Entry | Yes | YES | FAIL | Claims no entry exists -- entry verified in code |
| J. Reproduction Commands | Yes | YES | YES | 3 bash commands present |
| K. Model-Family Addendum | Yes | YES | YES | K1 filled; K2-K6 all marked N/A |
| L. Known Issues | Yes | YES | YES | 7 numbered notes |

STRUCTURAL FAILURES:
- Section I: Claims no entry -- entry verified in generate_all_tables.py (CRITICAL)
- Section F1 Step 7: Propagates the same false claim (HIGH)

================================================================================
PHASE 2: FACTUAL ACCURACY -- SECTION A (Suite Identity)
================================================================================

A-1. Suite ID
     Doc: "H1.1b"
     File: src/f1d/econometric/run_h1_1b_cash_tsimm_binary.py. Trivial match.
     RESULT: PASS.

A-2. Title
     Doc: "Binary Product Similarity--Moderated Speech Uncertainty and Cash Holdings"
     Runner LaTeX caption (line 490):
       r"\caption{Binary Product Similarity--Moderated Speech Uncertainty and Cash Holdings}"
     Exact match. PASS.

A-3. Hypothesis
     Doc: "Does product-market similarity (Hoberg-Phillips TNIC3TSIMM), binarized at
     per-fiscal-year median, moderate the Manager QA Uncertainty -> Cash Holdings
     relationship?"
     Runner docstring (lines 8-10): "Test whether product-market similarity (Hoberg-Phillips
     TNIC3TSIMM), binarized at per-fiscal-year median, moderates the
     Manager_QA_Uncertainty -> CashHoldings relationship."
     Match: Yes. PASS.

A-4. Direction (tail test)
     Doc: "Mixed (one-tailed beta > 0 on main IV; two-tailed on interaction and moderator)"
     Runner line 33: "Hypothesis: Two-tailed on interaction (b3 != 0); one-tailed on main IV (b1 > 0)."
     Runner lines 391-394: p_one_iv = p_two_iv / 2 if beta_iv > 0 else 1 - p_two_iv / 2
     Runner lines 398, 402: p_two_mod and p_two_int used directly (no one-tailed conversion)
     Match: Yes. PASS.

A-5. Model Family
     Doc: "PanelOLS"
     Runner line 61: `from linearmodels.panel import PanelOLS`
     Runner line 369: `model_obj = PanelOLS(...)`
     Match: Yes. PASS.

A-6. Estimator
     Doc: "linearmodels.panel.PanelOLS"
     Runner import: `from linearmodels.panel import PanelOLS`
     Match: Yes. PASS.

A-7. Unit of Observation
     Doc: "Call-level (individual earnings call)"
     Builder docstring line 24: "Unit of observation: the individual earnings call (file_name)."
     Runner line 34: "Unit: Call-level (loads H1 panel, merges TNIC at load time)."
     Match: Yes. PASS.

A-8. Panel Index
     Doc: "(gvkey, cal_yr) for Col 1; (gvkey, cal_yr_qtr) for Col 2"
     Runner line 366: `df_panel = df_prepared.set_index(["gvkey", time_col])`
     Runner lines 290, 344: time_col = "cal_yr_qtr" if fe.endswith("_yq") else "cal_yr"
     MODEL_SPECS lines 91-93:
       Col 1: fe="industry" (no "_yq") -> time_col="cal_yr"
       Col 2: fe="industry_yq" -> time_col="cal_yr_qtr"
     Match: Yes. PASS.

A-9. Columns
     Doc: "2"
     Runner MODEL_SPECS lines 90-93: 2 entries exactly.
     Match: Yes. PASS.

A-10. File paths
      Doc Runner: "src/f1d/econometric/run_h1_1b_cash_tsimm_binary.py" -- file read above. PASS.
      Doc Panel Builder: "src/f1d/variables/build_h1_cash_holdings_panel.py" -- file read above. PASS.

PHASE 2 SUMMARY: 10 PASS, 0 FAIL.

================================================================================
PHASE 3: FACTUAL ACCURACY -- SECTION B (Model Specification)
================================================================================

B1-CHECK: Regression Equation
    Doc equation (B1):
      CashHoldings_{i,t} = beta1 * MgrQAUnc_c + beta2 * HighTSIMM
                          + beta3 * (MgrQAUnc_c x HighTSIMM) + gamma*Controls
                          + alpha_j + delta_t + epsilon_{i,t}
    Runner exog list (line 357):
      exog = [IV_CENTERED, MODERATOR, INTERACTION] + all_controls
           = ["Manager_QA_Unc_c", "HighTSIMM", "MgrQAUnc_x_HighTSIMM"] + CONTROLS
    PanelOLS(dependent=df_panel[dv], exog=df_panel[exog],
             entity_effects=False, time_effects=True, other_effects=df_panel["ff12_code"])
    FE components:
      alpha_j = ff12_code (other_effects) = FF12 industry FE. Correct.
      delta_t = time_effects=True absorbed via panel time index = cal_yr or cal_yr_qtr. Correct.
    Equation in doc captures all terms. PASS.

B2-CHECK: Dependent Variable(s)
    Doc: CashHoldings | cheq / atq | CompustatEngine: cheq, atq | Contemporaneous (t)
    Runner MODEL_SPECS: dv="CashHoldings" for both specs.
    Runner lines 284-285: dv = spec["dv"]; panel["Lagged_DV"] = panel[lag_col]
    DV is CashHoldings (not CashHoldings_lead or CashHoldings_lag). Confirmed contemporaneous.
    Formula cheq/atq: consistent with CashHoldingsBuilder from CompustatEngine. PASS.

B3-CHECK: Independent Variable(s)
    Doc lists 3 regression IVs: Manager_QA_Unc_c, HighTSIMM, MgrQAUnc_x_HighTSIMM.
    Runner line 357: exog = [IV_CENTERED, MODERATOR, INTERACTION] + all_controls
      IV_CENTERED = "Manager_QA_Unc_c" (line 85)
      MODERATOR = "HighTSIMM" (line 84)
      INTERACTION = "MgrQAUnc_x_HighTSIMM" (line 86)
    All 3 match. No undocumented IVs. PASS.

    Centering: Doc B3 says "Manager_QA_Uncertainty_pct - mean(Main sample)"
    Runner lines 248-251:
      main_mask = ~panel["ff12_code"].isin([8, 11])
      iv_main = panel.loc[main_mask, IV].dropna()
      iv_mu = iv_main.mean()
      panel[IV_CENTERED] = panel[IV] - iv_mu
    PASS.

    HighTSIMM construction (doc B3, 6 steps):
    Step 1: Load from TNIC3HHIdata.txt -- runner line 186. VERIFIED. PASS.
    Step 2: Merge by (_gvkey_int, fyearq_int), left join -- runner lines 190-200. VERIFIED. PASS.
    Step 3: Main sample mask -- runner line 226. VERIFIED. PASS.
    Step 4: Per-fiscal-year median -- runner line 230. VERIFIED. PASS.
    Step 5: Binary comparison -- runner line 233. VERIFIED. PASS.
    Step 6: NaN handling -- runner lines 234-235. VERIFIED. PASS.
    Source reference: "function transform_moderator_and_center_iv(), lines 210-263" --
    function starts at line 210 (def statement) and returns at line 262-263. VERIFIED. PASS.

B4-CHECK: Control Variables
    Doc CONTROLS: BookLev, Size, TobinsQ, ROA, CapexAt, DividendPayer, OCF_Volatility,
    SalesGrowth, RD_Intensity, CashFlow, Volatility, Lagged_DV (12 items total).
    Runner CONTROLS (lines 76-81): exact same 12 items in same order. PASS.

    Lagged_DV:
    Doc: "CashHoldings_lag = prior fiscal year's end-of-year CashHoldings"
    Doc reference: "create_lag_variable(), lines 379-447"
    Builder: create_lag_variable() def at line 379, final return at line 446-447. VERIFIED. PASS.
    Runner lines 293-295: lag_col = f"{dv}_lag" = "CashHoldings_lag";
      panel["Lagged_DV"] = panel[lag_col]. PASS.

    No dynamic controls in runner. PASS.

B5-CHECK: Fixed Effects
    Doc B5 table:
    | Industry | ff12_code | FF12 industry FE (via other_effects) | Both Cols 1-2 |
    | Time (Col 1) | cal_yr | Calendar Year FE (via time_effects=True) | Col 1 only |
    | Time (Col 2) | cal_yr_qtr | Cal Yr-Qtr FE (via time_effects=True) | Col 2 only |

    Runner lines 369-377:
      PanelOLS(
          entity_effects=False,       -- no firm FE; correct (doc says Industry FE only)
          time_effects=True,          -- absorbs panel time index; correct
          other_effects=df_panel["ff12_code"],  -- FF12 industry FE; correct
          drop_absorbed=True,
          check_rank=False,
      )

    Panel index varies:
      Col 1: set_index(["gvkey", "cal_yr"]) -- time_effects absorbs cal_yr
      Col 2: set_index(["gvkey", "cal_yr_qtr"]) -- time_effects absorbs cal_yr_qtr
    Match: Yes. PASS.

    cal_yr_qtr derivation: Doc says "cal_yr * 10 + cal_qtr (e.g., 20103 = 2010 Q3)"
    Runner line 169: build_cal_yr_qtr_index(panel) from panel_utils.
    Doc references "panel_utils.py, function build_cal_yr_qtr_index(), lines 195-218". PASS.

B6-CHECK: Standard Errors
    Doc: cov_type="clustered", cluster_entity=True, line 378.
    Runner line 378: `model = model_obj.fit(cov_type="clustered", cluster_entity=True)`
    Exact match including line number. PASS.

B7-CHECK: Hypothesis Test
    Doc table:
    | Manager_QA_Unc_c | One-tailed (beta>0) | p_one = p_two/2 if beta>0, else 1-p_two/2 |
    | HighTSIMM | Two-tailed | Direct from pvalues |
    | MgrQAUnc_x_HighTSIMM | Two-tailed | Direct from pvalues |

    Runner lines 391-394:
      if not np.isnan(p_two_iv) and not np.isnan(beta_iv):
          p_one_iv = p_two_iv / 2 if beta_iv > 0 else 1 - p_two_iv / 2
    Matches doc description of one-tailed conversion. PASS.

    Runner line 398: p_two_mod = float(model.pvalues.get(MODERATOR, np.nan)) -- no conversion.
    Runner line 402: p_two_int = float(model.pvalues.get(INTERACTION, np.nan)) -- no conversion.
    Both two-tailed. PASS.

    Stars functions: _sig_stars_one (line 431-441) and _sig_stars_two (line 444-454)
    use same thresholds (p<0.01/0.05/0.10). Doc says "***p<0.01, **p<0.05, *p<0.10". PASS.

    MINOR: Doc says "lines 391-393 (one-tailed IV)". Actual block spans lines 391-394.
    Not a meaningful error but technically inaccurate line count.

PHASE 3 SUMMARY: 7 PASS, 0 FAIL. One minor line-range note (391-393 vs 391-394).

================================================================================
PHASE 4: FACTUAL ACCURACY -- SECTION C (Spec Register)
================================================================================

Doc spec register (2 rows):
| Col | DV | Entity FE | Time FE | Controls | Notes |
| 1 | CashHoldings | Industry (ff12_code) | Calendar Year (cal_yr) | Extended (11 controls + Lagged_DV) | Panel index: (gvkey, cal_yr) |
| 2 | CashHoldings | Industry (ff12_code) | Calendar Year-Quarter (cal_yr_qtr) | Extended (11 controls + Lagged_DV) | Panel index: (gvkey, cal_yr_qtr) |

Runner MODEL_SPECS (lines 90-93):
  {"col": 1, "dv": "CashHoldings", "fe": "industry",    "extra_controls": []}
  {"col": 2, "dv": "CashHoldings", "fe": "industry_yq", "extra_controls": []}

Checks:
- Row count: 2 rows in doc = 2 specs in code. PASS.
- Col 1 DV: CashHoldings. Matches. PASS.
- Col 2 DV: CashHoldings. Matches. PASS.
- Entity FE: Industry (ff12_code), both cols. Code: other_effects=ff12_code, entity_effects=False. PASS.
- Time FE Col 1: cal_yr. Code: fe="industry" (no "_yq") -> time_col="cal_yr". PASS.
- Time FE Col 2: cal_yr_qtr. Code: fe="industry_yq" -> time_col="cal_yr_qtr". PASS.
- Controls: "11 controls + Lagged_DV" = 12 total. CONTROLS list has 11 named controls +
  Lagged_DV as 12th element. Description accurate. PASS.
- Source line reference: "lines 90-93 (MODEL_SPECS)" -- verified. PASS.
- No specs in code missing from table. No specs in table not in code. PASS.

PHASE 4 SUMMARY: 6 PASS, 0 FAIL.

================================================================================
PHASE 5: FACTUAL ACCURACY -- SECTION D (Sample Construction)
================================================================================

D1-CHECK: Population
    Doc: "112,968 total calls, ~2,429 firms, 2002-2018"
    Project scope (memory): 112,968 calls, 2,429 firms, 2002-2018.
    Match: PASS.

D2-CHECK: Exclusion Criteria
    Doc lists 7 steps (steps 1-7 with descriptions, no row counts).

    Runner main() execution order (lines 718-784):
      Line 719: load_panel() -- loads H1 panel (Step 1)
      Line 722: load_and_merge_tnic() -- TNIC merge (Step 2)
      Line 725: transform_moderator_and_center_iv() -- moderator + IV centering (Step 3)
      Line 729: filter_main_sample() -- main sample filter (Step 4)
      prepare_regression_data():
        Line 311: df[dv].notna() filter -- DV non-missing (Step 5)
        Lines 316-317: notna().all(axis=1) -- complete cases (Step 6)
        Lines 321-323: gvkey.value_counts() >= MIN_CALLS_PER_FIRM -- min calls (Step 7)

    Step order in doc matches code execution order. PASS.
    Step descriptions accurate. PASS.

    Row counts: Marked [UNVERIFIED] with explanation:
    "The attrition table generated by the runner (lines 776-784) records only 4
    aggregate stages, not the full 7-step cascade. Exact per-step counts require
    executing the runner."

    Runner lines 776-784 attrition_stages:
      ("Full panel (H1)", full_n),
      ("Main sample (excl Finance/Utility)", main_n),
      ("TNIC3TSIMM matched", tnic_matched),
      ("After complete-case + min-calls (col 1)", first["n_obs"]),
    Exactly 4 stages -- doc's characterization ACCURATE. [UNVERIFIED] label appropriate.

D3-CHECK: Sample Counts per Spec
    Doc: N may differ between Col 1 and Col 2 due to panel index change and
    drop_absorbed=True. References model_diagnostics.csv.
    Plausible and not falsifiable without runtime. PASS.

PHASE 5 SUMMARY: 4 PASS, 0 FAIL. D2 row counts [UNVERIFIED] is correctly flagged.

================================================================================
PHASE 6: FACTUAL ACCURACY -- SECTION E (Variable Dictionary)
================================================================================

Checking all 17 rows plus completeness.

CashHoldings:
  Code name: "CashHoldings" -- used as dv in MODEL_SPECS. PASS.
  Formula: cheq / atq -- standard Compustat cash/assets. PASS (consistent with
    CashHoldingsBuilder, CompustatEngine).
  Type: DV. PASS.
  Winsorized: 1%/99% by fiscal year -- at CompustatEngine level. PASS.
  Timing: Contemporaneous (t) -- runner uses CashHoldings not CashHoldings_lead. PASS.

Manager_QA_Uncertainty_pct:
  Code name: IV = "Manager_QA_Uncertainty_pct" (line 74). PASS.
  Formula: uncertainty word count / total words in Manager Q&A * 100. Consistent
    with LinguisticEngine Stage 2 textual analysis. PASS.
  Type: "--" (raw pre-centering; not in regression directly). PASS.
  Winsorized: "0%/99% per calendar year (upper-only)" -- LinguisticEngine level.
    Consistent with project practice. PASS.

Manager_QA_Unc_c:
  Code name: IV_CENTERED = "Manager_QA_Unc_c" (line 85). PASS.
  Formula: "Manager_QA_Uncertainty_pct - mean(Main sample)" -- runner lines 248-251. PASS.
  Type: IV -- this is the regression variable (in exog list). PASS.
  Winsorized: "Inherits from raw" (centering shifts mean only). PASS.

tnic3tsimm:
  Code name: MODERATOR_RAW = "tnic3tsimm" (line 83). PASS.
  Formula: Hoberg-Phillips TNIC3 product-market similarity (external data). PASS.
  Source: "TNIC3HHIdata.txt" -- runner line 186. PASS.
  Type: "--" (raw before binary transform). PASS.
  Winsorized: "No (external data)". PASS.

HighTSIMM:
  Code name: MODERATOR = "HighTSIMM" (line 84). PASS.
  Formula: "1 if tnic3tsimm > per-fiscal-year median (Main sample), else 0" --
    runner lines 230-235. PASS.
  Type: Moderator. PASS.
  Winsorized: "No (binary)". PASS.

MgrQAUnc_x_HighTSIMM:
  Code name: INTERACTION = "MgrQAUnc_x_HighTSIMM" (line 86). PASS.
  Formula: "Manager_QA_Unc_c * HighTSIMM" -- runner line 307:
    df[INTERACTION] = df[IV_CENTERED] * df[MODERATOR]. PASS.
  Type: Interaction. PASS.
  Winsorized: "No". PASS.

Lagged_DV:
  Code name: "Lagged_DV" -- CONTROLS list, last element (line 81). PASS.
  Formula: "CashHoldings_lag = prior fiscal year's end-of-year CashHoldings"
  Runner line 294: lag_col = f"{dv}_lag" = "CashHoldings_lag"
  Runner line 295: panel["Lagged_DV"] = panel[lag_col]. PASS.
  Builder: create_lag_variable(), lines 379-447. VERIFIED. PASS.
  Type: Lagged_DV. PASS.

BookLev:
  Code name: "BookLev" -- in CONTROLS. PASS.
  Formula: "(dlcq.fillna(0) + dlttq.fillna(0)) / atq" -- standard book leverage. PASS.
  Source: CompustatEngine: dlcq, dlttq, atq. PASS.
  Winsorized: 1%/99% by fiscal year. PASS.

Size:
  Code name: "Size" -- in CONTROLS. PASS.
  Formula: ln(atq) for atq > 0; else NaN. PASS.
  Winsorized: 1%/99% by fiscal year. PASS.

TobinsQ:
  Code name: "TobinsQ" -- in CONTROLS. PASS.
  Formula: (atq + cshoq*prccq - ceqq) / atq. PASS.
  Winsorized: 1%/99% by fiscal year. PASS.

ROA:
  Code name: "ROA" -- in CONTROLS. PASS.
  Formula: "iby_annual (Q4) / avg_assets; avg = (atq_t + atq_{t-1})/2"
  ROABuilder docstring (roa.py, lines 20-23): "Build ROA = iby_annual / avg_assets
  from Compustat quarterly data... annual income before extraordinary items (iby) from
  Q4 filing, divided by average total assets (atq_t + atq_{t-1}) / 2." MATCH. PASS.
  Source: roa.py metadata line 55: "Compustat/iby,atq". PASS.
  Winsorized: 1%/99% by fiscal year. PASS.

CapexAt:
  Code name: "CapexAt" -- in CONTROLS. PASS.
  Formula: "capxy_annual (Q4-only full FY) / atq_lag". PASS.
  Winsorized: 1%/99% by fiscal year. PASS.

DividendPayer:
  Code name: "DividendPayer" -- in CONTROLS. PASS.
  Formula: "1 if dvy_annual (Q4-only full FY) > 0, else 0". PASS.
  Winsorized: No (binary). PASS.

OCF_Volatility:
  Code name: "OCF_Volatility" -- in CONTROLS. PASS.
  Formula: "Rolling 5-year std (min 3 yrs) of oancfy/atq_{t-1} per gvkey". PASS.
  Winsorized: 1%/99% by fiscal year. PASS.

SalesGrowth:
  Code name: "SalesGrowth" -- in CONTROLS. PASS.
  Formula: "(saley_t - saley_{t-1}) / |saley_{t-1}|; Q4-only annual; saley with saleq
  fallback". PASS.
  Winsorized: "1%/99% by fiscal year (in Biddle engine)". PASS.

RD_Intensity:
  Code name: "RD_Intensity" -- in CONTROLS. PASS.
  Formula: "xrdq.fillna(0) / atq". PASS.
  Winsorized: 1%/99% by fiscal year. PASS.

CashFlow:
  Code name: "CashFlow" -- in CONTROLS. PASS.
  Formula: "oancfy / avg_assets; avg = (atq_t + atq_{t-1})/2; fallback to atq_t". PASS.
  Winsorized: "1%/99% by fiscal year (in Biddle engine)". PASS.

Volatility:
  Code name: "Volatility" -- in CONTROLS. PASS.
  Formula: "std(daily_ret) * sqrt(252) * 100 over [prev_call+5d, call-5d], min 10 days". PASS.
  Source: CRSPEngine: CRSP daily returns. PASS.
  Winsorized: No (bounded by construction). PASS.

FE / Index variables (gvkey, cal_yr, cal_yr_qtr, ff12_code): All listed. PASS.

COMPLETENESS CHECK:
  Runner exog = [Manager_QA_Unc_c, HighTSIMM, MgrQAUnc_x_HighTSIMM,
                 BookLev, Size, TobinsQ, ROA, CapexAt, DividendPayer, OCF_Volatility,
                 SalesGrowth, RD_Intensity, CashFlow, Volatility, Lagged_DV]
  DV: CashHoldings
  = 16 regression variables + 1 DV = 17 total variables in regressions.
  Dict has 17 regression rows + 4 FE/Index rows = 21 rows total. All 17 covered. PASS.
  FE columns all listed. PASS.

PHASE 6 SUMMARY: 18 PASS, 0 FAIL.

================================================================================
PHASE 7: FACTUAL ACCURACY -- SECTIONS F, G, H
================================================================================

F-CHECK: Data Pipeline

F1. Dependency Chain:
  Step 1 -- Raw inputs: Doc lists 5 sources:
    - master_sample_manifest.parquet -- used by builder. PASS.
    - comp_na_daily_all.parquet -- used via CompustatEngine. PASS.
    - CRSP_DSF_{year}_Q{q}.parquet -- used via CRSPEngine. PASS.
    - linguistic_variables_{year}.parquet -- used via LinguisticEngine. PASS.
    - TNIC3HHIdata.txt -- loaded in runner (line 186). PASS.

    NOTE: Builder docstring (lines 29-35) lists tr_ibes.parquet and
    CRSPCompustat_CCM.parquet as inputs but the builders dict (lines 121-158) does NOT
    include any IBESBuilder or CCMLinktableBuilder. These inputs appear in the docstring
    for historical reasons but are not consumed by H1.1b's actual variable builders.
    The doc's omission of tr_ibes/CCM from Step 1 is defensible and accurate.

  Steps 2-6: All verified above in Phases 3-6. PASS.

  Step 7: "H1.1b does NOT have an entry in generate_all_tables.py."
    WRONG. Entry exists. See Phase 8. FAIL.

F2. Data Engines:
  CompustatEngine, CRSPEngine, LinguisticEngine, TNIC3 flat file -- all correct.
  "CurrentRatio (built but unused)" noted -- accurate (CurrentRatioBuilder in builders
  dict, line 154, but CurrentRatio not in CONTROLS). PASS.

F3. Merge Operations:
  Stage 3: manifest + each builder by file_name, left join, zero-row-delta enforced.
    Code lines 182-216: builder loop with panel.merge(data, on="file_name", how="left")
    plus row-delta assertion. PASS.
  Stage 4: H1 panel + TNIC3HHIdata by (_gvkey_int, fyearq_int), left join.
    Code lines 193-200: panel.merge(tnic[...].rename(...), on=["_gvkey_int","fyearq_int"],
    how="left") plus assert len(panel)==before. PASS.
  Join keys and types correct. PASS.

G-CHECK: Outputs

G1. Stage 3 Outputs (4 files):
  h1_cash_holdings_panel.parquet -- line 521. VERIFIED. PASS.
  summary_stats.csv -- line 531. VERIFIED. PASS.
  report_step3_h1.md -- line 612. VERIFIED. PASS.
  run_manifest.json -- lines 535-546 (generate_manifest call). VERIFIED. PASS.
  Doc lists all 4. PASS.

G2. Stage 4 Outputs (10 files):
  h1_1b_cash_tsimm_binary_table.tex -- line 583. VERIFIED. PASS.
  model_diagnostics.csv -- line 622. VERIFIED. PASS.
  summary_stats.csv -- line 743. VERIFIED. PASS.
  summary_stats.tex -- line 744. VERIFIED. PASS.
  sample_attrition.csv -- via generate_attrition_table (line 782). VERIFIED. PASS.
  sample_attrition.tex -- via generate_attrition_table (line 782). VERIFIED. PASS.
  regression_results_col1.txt -- line 604 (col_num=1 loop). VERIFIED. PASS.
  regression_results_col2.txt -- line 604 (col_num=2 loop). VERIFIED. PASS.
  report_step4_H1_1b.md -- line 684. VERIFIED. PASS.
  run_manifest.json -- lines 788-797 (generate_manifest call). VERIFIED. PASS.
  Doc lists all 10. PASS.
  No file in doc NOT written by code. PASS.

H-CHECK: Outlier/Missing Treatment

H1. Winsorization:
  Compustat variables at 1%/99% by fiscal year (fyearq) in CompustatEngine. PASS.
  Applied to: CashHoldings, BookLev, Size, TobinsQ, ROA, CapexAt, OCF_Volatility,
  RD_Intensity. PASS.
  NOT winsorized (Compustat): DividendPayer (binary). PASS.
  Biddle engine: SalesGrowth, CashFlow at 1%/99% by fiscal year. PASS.
  LinguisticEngine: Manager_QA_Uncertainty_pct at 0%/99% per calendar year. PASS.
  CRSPEngine: Volatility not winsorized. PASS.
  TNIC data: tnic3tsimm not winsorized. PASS.

H2. Missing Data:
  Complete-case deletion -- runner lines 316-317. VERIFIED. PASS.
  Inf/-Inf replaced -- runner line 304:
    `df = df.replace([np.inf, -np.inf], np.nan)`. Doc cites "line 304". EXACT MATCH. PASS.
  Min-calls filter -- runner lines 321-323. VERIFIED. PASS.

H3. Transformations:
  Mean-centering of Manager_QA_Uncertainty_pct -- runner lines 248-251. PASS.
  Median split for tnic3tsimm -> HighTSIMM -- runner lines 230-235. PASS.
  Natural log for Size (ln(atq)). PASS.
  Multiplication for interaction -- runner line 307. PASS.

PHASE 7 SUMMARY: 11 PASS, 1 FAIL (F1 Step 7 generate_all_tables.py claim).

================================================================================
PHASE 8: FACTUAL ACCURACY -- SECTION I (Table Generator Entry)
================================================================================

DOC CLAIM: "H1.1b does NOT have an entry in `outputs/generate_all_tables.py` as of
2026-03-30. The parent suite H1.1 (continuous TNIC moderation) has an entry, but H1.1b
(binary) does not. This means H1.1b's LaTeX table is generated directly by the runner's
`_save_latex_table()` function (lines 462-581) rather than through the shared table
generator pipeline. If H1.1b is to be included in the thesis table set, an entry must be
added to the SUITES list in generate_all_tables.py."

VERIFICATION: Grep for "H1.1b" and "h1_1b" in outputs/generate_all_tables.py.

ACTUAL ENTRY FOUND (outputs/generate_all_tables.py):
```python
{
    "id": "H1.1b",
    "type": "moderation",
    "dir": "h1_1b_cash_tsimm_binary/2026-03-31_155928",
    "caption": "H1.1b: Binary Product Similarity--Moderated Speech Uncertainty and Cash Holdings",
    "label": "tab:h1_1b",
    "cols": 2,
    "dvs": [
        ("CashHoldings", 2),
    ],
    "key_vars": [
        "Manager_QA_Unc_c",
        "HighTSIMM",
        "MgrQAUnc_x_HighTSIMM",
    ],
    "key_labels": [
        r"Manager\_QA\_Unc\_c",
        "HighTSIMM",
        r"MgrQAUnc\_x\_HighTSIMM",
    ],
    "key_tails": ["one_pos", "two", "two"],
    "lagged_dv_var": "Lagged_DV",
    "lagged_dv_label": r"Lagged\_DV",
},
```

ENTRY FIELD VERIFICATION:
a) "id": "H1.1b" -- matches suite. PASS.
b) "key_tails": ["one_pos", "two", "two"]:
   - Manager_QA_Unc_c: "one_pos" (one-tailed positive)
     Runner lines 391-394: p_one_iv = p_two_iv/2 if beta>0 else 1-p_two_iv/2.
     Doc Section B7: one-tailed beta>0 for main IV. MATCH. PASS.
   - HighTSIMM: "two" (two-tailed)
     Runner line 398: p_two_mod direct from pvalues. MATCH. PASS.
   - MgrQAUnc_x_HighTSIMM: "two" (two-tailed)
     Runner line 402: p_two_int direct from pvalues. MATCH. PASS.
c) "cols": 2 -- matches MODEL_SPECS count. PASS.
d) "dvs": [("CashHoldings", 2)] -- CashHoldings is DV in both cols (all 2 specs). PASS.
e) "key_vars": ["Manager_QA_Unc_c", "HighTSIMM", "MgrQAUnc_x_HighTSIMM"] -- all 3 match
   runner exog (line 357) and B3. PASS.

CRITICAL FAILURE: The doc's claim that no entry exists is factually wrong. The entry
exists, has a specific run directory timestamp (2026-03-31_155928 -- meaning it has
already been run), and all entry fields are consistent with the runner code. The doc was
apparently written before the entry was added, but was not updated after.

PHASE 8 SUMMARY: 4 PASS, 1 FAIL (existence claim).

================================================================================
PHASE 9: FACTUAL ACCURACY -- SECTION K (Model-Family Addendum)
================================================================================

Model family: PanelOLS (identified in Phase 2, A-5).
K1 should be filled; K2-K6 should be N/A.

K1. PanelOLS Specifics:
  entity_effects: Doc says "False -- NO firm FE absorbed."
    Runner line 372: entity_effects=False. EXACT MATCH. PASS.

  time_effects: Doc says "True -- Calendar Year (Col 1) or Calendar Year-Quarter (Col 2)
    FE absorbed, depending on the panel index used when calling set_index(['gvkey', time_col])."
    Runner line 373: time_effects=True.
    Runner line 366: df_panel = df_prepared.set_index(["gvkey", time_col]).
    EXACT MATCH. PASS.

  other_effects: Doc says "df_panel['ff12_code'] -- Fama-French 12 industry."
    Runner line 374: other_effects=df_panel["ff12_code"]. EXACT MATCH. PASS.

  drop_absorbed: Doc says "True."
    Runner line 375: drop_absorbed=True. PASS.

  check_rank: Doc says "False (line 376)."
    Runner line 376: check_rank=False. EXACT LINE MATCH. PASS.

  Singleton handling: "Handled by linearmodels default behavior with drop_absorbed=True"
    No explicit singleton suppression code found in runner. Consistent. PASS.

  Source: "lines 369-378" -- PanelOLS instantiation block runs lines 369-378. VERIFIED. PASS.

K2. Cox Proportional Hazards: N/A -- PASS (correct to omit).
K3. Logit/Probit/LPM: N/A -- PASS (correct to omit).
K4. IV/2SLS: N/A -- PASS (correct to omit).
K5. OLS (non-panel): N/A -- PASS (correct to omit).
K6. Other Model Family: N/A -- PASS (correct to omit).

PHASE 9 SUMMARY: 6 PASS, 0 FAIL.

================================================================================
PHASE 10: QUALITY GATE CHECKLIST
================================================================================

| # | Quality Gate | Met? | Evidence |
|---|-------------|------|----------|
| 1 | Every variable in every regression spec appears in Variable Dictionary with explicit formula and source engine | YES | 17 regression variables + DV documented. Phase 6: all 18 checks passed. |
| 2 | The model equation matches what the code actually estimates | YES | B1 equation = runner exog + PanelOLS FE structure. Phase 3 B1: PASS. |
| 3 | The specification register accounts for every model column | YES | 2 rows in C = 2 MODEL_SPECS. Phase 4: all 6 checks passed. |
| 4 | The attrition cascade has row counts for each filter step | PARTIAL | No actual row counts. Doc marks [UNVERIFIED] with explanation citing runtime dependency. The [UNVERIFIED] flag is appropriate but gate is not fully met. |
| 5 | The tail test direction matches between runner code and generate_all_tables.py | PARTIAL | Runner and generate_all_tables.py DO match (one_pos/two/two). However, the doc incorrectly claims no entry exists so it cannot assert this match. The underlying match is verified by auditor but not by the doc. |
| 6 | The FE specification matches between docstring, code, and this document | YES | B5 matches runner PanelOLS args; K1 matches; C matches. Phase 3 B5, Phase 4, Phase 9: all PASS. |
| 7 | Every merge in the panel builder is documented with join keys and type | YES | F3 documents Stage 3 (file_name, left) and Stage 4 ((_gvkey_int, fyearq_int), left). Phase 7 F3: PASS. |
| 8 | The output file list matches what the runner actually writes | YES | G1 (4 files) and G2 (10 files) all verified against code. Phase 7 G: all PASS. |
| 9 | The model-family addendum is filled for the correct family only | YES | K1 filled for PanelOLS; K2-K6 N/A. Phase 9: all PASS. |
| 10 | Any claim marked [UNVERIFIED] has an explanation of what blocks verification | YES | D2 [UNVERIFIED] note explains runtime dependency. Phase 5: PASS. |

PHASE 10 SUMMARY: 8 PASS, 2 PARTIAL (gates 4 and 5).
Both partials trace to the Section I error: gate 4 is inherent (runtime data needed),
gate 5 would become PASS once Section I is corrected.

================================================================================
PHASE 11: CROSS-REFERENCE CONSISTENCY
================================================================================

1. DVs in Section B2 match DVs in Section C (spec register)?
   B2: CashHoldings.
   C: CashHoldings for Col 1 and Col 2.
   MATCH. PASS.

2. DVs in Section C match DVs in Section I (table generator entry)?
   C: CashHoldings (both cols).
   I: Claims no entry exists. Actual entry has dvs=[("CashHoldings", 2)].
   If corrected, C and actual I would match perfectly.
   FAIL (consequence of Section I error).

3. Controls in Section B4 match variables in Section E (dictionary)?
   B4: BookLev, Size, TobinsQ, ROA, CapexAt, DividendPayer, OCF_Volatility,
   SalesGrowth, RD_Intensity, CashFlow, Volatility, Lagged_DV (12 items).
   E: All 12 present with Type=Control (or Lagged_DV). MATCH. PASS.

4. Column count in Section A (2) matches rows in Section C (2)?
   A: "Columns: 2". C: 2 rows. MATCH. PASS.

5. Column count in Section A (2) matches "cols" in Section I?
   A: 2. I claims no entry. Actual entry has cols=2.
   If corrected, A and actual I would match.
   FAIL (same root cause as #2 above -- counted once).

6. Tail direction in A matches B7 matches Section I?
   A: "Mixed (one-tailed beta>0 on main IV; two-tailed on interaction and moderator)"
   B7: One-tailed positive for Manager_QA_Unc_c; two-tailed for HighTSIMM and
     MgrQAUnc_x_HighTSIMM. MATCH with A.
   Actual I: key_tails=["one_pos","two","two"]. MATCH with A and B7.
   A and B7 are internally consistent. Section I error tracked separately. PASS.

7. FE in Section B5 matches Section C matches Section K?
   B5: Industry FF12 (both cols); Cal Year (Col 1); Cal Yr-Qtr (Col 2).
   C: Industry FE + Cal Year (Col 1); Industry FE + Cal Yr-Qtr (Col 2). MATCH.
   K1: entity_effects=False; time_effects=True; other_effects=ff12_code. MATCH.
   PASS.

8. Panel index in Section A matches set_index in Section K?
   A: "(gvkey, cal_yr) for Col 1; (gvkey, cal_yr_qtr) for Col 2"
   K1: "depending on the panel index used when calling set_index(['gvkey', time_col])"
   Runner line 366: set_index(["gvkey", time_col]). MATCH. PASS.

PHASE 11 SUMMARY: 7 PASS, 1 FAIL (cross-reference between C and I fails due to Section I
error; checks #2 and #5 are the same root cause, counted as 1 failure).

---

## CORRECTIONS REQUIRED

### Correction 1 (CRITICAL) -- Rewrite Section I completely

**Section:** I. GENERATE_ALL_TABLES.PY ENTRY

**Current text (wrong):**
```
H1.1b does NOT have an entry in `outputs/generate_all_tables.py` as of 2026-03-30.
The parent suite H1.1 (continuous TNIC moderation) has an entry, but H1.1b (binary)
does not. This means H1.1b's LaTeX table is generated directly by the runner's
`_save_latex_table()` function (lines 462-581) rather than through the shared table
generator pipeline.

If H1.1b is to be included in the thesis table set, an entry must be added to the
SUITES list in generate_all_tables.py.
```

**What it should say:**
```
H1.1b has an entry in `outputs/generate_all_tables.py`. The entry is of type
"moderation" with cols=2 and key_tails=["one_pos","two","two"], consistent with
the runner's one-tailed test on the main IV and two-tailed tests on the moderator
and interaction term.

Entry (verified against outputs/generate_all_tables.py):

    {
        "id": "H1.1b",
        "type": "moderation",
        "dir": "h1_1b_cash_tsimm_binary/2026-03-31_155928",
        "caption": "H1.1b: Binary Product Similarity--Moderated Speech Uncertainty
                    and Cash Holdings",
        "label": "tab:h1_1b",
        "cols": 2,
        "dvs": [("CashHoldings", 2)],
        "key_vars": [
            "Manager_QA_Unc_c",
            "HighTSIMM",
            "MgrQAUnc_x_HighTSIMM",
        ],
        "key_labels": [
            r"Manager\_QA\_Unc\_c",
            "HighTSIMM",
            r"MgrQAUnc\_x\_HighTSIMM",
        ],
        "key_tails": ["one_pos", "two", "two"],
        "lagged_dv_var": "Lagged_DV",
        "lagged_dv_label": r"Lagged\_DV",
    }

Verification:
- key_tails=["one_pos","two","two"] matches runner lines 391-394 (one-tailed IV)
  and lines 398-402 (two-tailed moderator and interaction). Consistent with B7.
- cols=2 matches MODEL_SPECS count (lines 90-93).
- dvs=[("CashHoldings", 2)] matches both specs using CashHoldings as DV.
- key_vars match runner exog (line 357).

Note: The runner also directly writes a LaTeX table via `_save_latex_table()`
(lines 462-581). The generate_all_tables.py entry is the thesis table pipeline entry.
```

**Code reference:** `outputs/generate_all_tables.py`, SUITES list, entry id="H1.1b".

---

### Correction 2 (HIGH) -- Update Section F1 Step 7

**Section:** F. DATA PIPELINE > F1. Dependency Chain > Step 7

**Current text (wrong):**
```
7. **Table generation:**
   - H1.1b does NOT have an entry in `outputs/generate_all_tables.py`. The LaTeX table is generated directly by the runner's `_save_latex_table()` function (lines 462-581).
```

**What it should say:**
```
7. **Table generation:**
   - H1.1b has an entry in `outputs/generate_all_tables.py` (type="moderation", cols=2).
     The runner also directly writes a LaTeX table via `_save_latex_table()` (lines 462-581)
     for standalone use. The generate_all_tables.py entry references run directory
     "h1_1b_cash_tsimm_binary/2026-03-31_155928" and uses the moderation table generator
     with key_tails=["one_pos","two","two"].
```

**Code reference:** `outputs/generate_all_tables.py` H1.1b entry; runner line 583.

---

### Correction 3 (MINOR) -- Fix B7 source line range

**Section:** B. MODEL SPECIFICATION > B7. Hypothesis Test

**Current text:** "Source: `run_h1_1b_cash_tsimm_binary.py`, lines 391-393 (one-tailed IV)"

**What it should say:** "Source: `run_h1_1b_cash_tsimm_binary.py`, lines 391-394 (one-tailed IV)"

**Code reference:** The one-tailed conversion block:
  Line 391: `if not np.isnan(p_two_iv) and not np.isnan(beta_iv):`
  Line 392: `    p_one_iv = p_two_iv / 2 if beta_iv > 0 else 1 - p_two_iv / 2`
  Line 393: (else branch)
  Line 394: `    p_one_iv = np.nan`
The block spans lines 391-394 inclusive.

---

## NOTES ON ITEMS THAT PASSED BUT DESERVE ATTENTION

1. **Builder docstring lists tr_ibes and CRSPCompustat_CCM as inputs** (build_h1_cash_holdings_panel.py
   lines 29-35) but the builders dict (lines 121-158) does not include any IBES or CCM builder.
   These appear in the docstring for historical reasons but are not consumed by H1.1b's variable
   builders. The doc's F1 correctly omits them. A note in Section L could clarify this
   discrepancy between the docstring and the actual builders dict.

2. **D2 row counts are [UNVERIFIED]** -- appropriately flagged. The 4-stage attrition table
   from the runner (Full panel, Main sample, TNIC matched, After complete-case+min-calls)
   combines the DV filter and complete-case filter steps. The doc's 7-step cascade is a more
   detailed logical description. This is acceptable.

3. **The generate_all_tables.py entry uses a hardcoded timestamp dir** ("2026-03-31_155928").
   If the runner is re-executed, the dir field will need updating. This is a project
   maintenance concern, not a provenance doc error.

4. **CurrentRatio** is noted in L#1 and F2 as built but unused. This is correct and well-documented.

5. **CashHoldings_lead** is noted in L#3 as built but unused. Correct -- H1.1b uses
   contemporaneous CashHoldings, not the lead. Well-documented.

================================================================================
END OF AUDIT REPORT
================================================================================
