# Adversarial Audit: Suite H1.1b Provenance Document

**Audit Date:** 2026-03-30
**Auditor:** Hostile Audit Agent (Claude Opus 4.6)
**Subject:** `docs/provenance/H1.1b.md`
**Runner:** `src/f1d/econometric/run_h1_1b_cash_tsimm_binary.py`
**Panel Builder:** `src/f1d/variables/build_h1_cash_holdings_panel.py`
**Creation Prompt:** `docs/Prompts/Suite Provenance Doc.txt`

------------------------------------------------------------------------

## AUDIT SUMMARY

| Category | Total Checks | Passed | Failed | Score |
|----------|-------------|--------|--------|-------|
| Structural Completeness (Phase 1) | 26 | 25 | 1 | 96% |
| Suite Identity (Phase 2) | 10 | 10 | 0 | 100% |
| Model Specification (Phase 3) | 7 | 7 | 0 | 100% |
| Spec Register (Phase 4) | 4 | 4 | 0 | 100% |
| Sample Construction (Phase 5) | 3 | 2 | 1 | 67% |
| Variable Dictionary (Phase 6) | 22 | 22 | 0 | 100% |
| Pipeline/Outputs/Treatment (Phase 7) | 8 | 8 | 0 | 100% |
| Table Generator Entry (Phase 8) | 2 | 0 | 2 | 0% |
| Model-Family Addendum (Phase 9) | 6 | 5 | 1 | 83% |
| Quality Gates (Phase 10) | 10 | 8 | 2 | 80% |
| Cross-Reference Consistency (Phase 11) | 8 | 6 | 2 | 75% |
| **TOTAL** | **106** | **97** | **9** | **92%** |

------------------------------------------------------------------------

## VERDICT

**FAIL -- INACCURATE**: Section I (generate_all_tables.py entry) is entirely fabricated -- H1.1b has no entry in the SUITES list. The provenance doc presents a fake code block, fake line numbers, and a fake verification table for a non-existent entry. Additionally, the attrition cascade in Section D2 lacks the required row counts per step, and one line number reference in Section K1 is off by one.

------------------------------------------------------------------------

## Phase 1: Structural Completeness

Checked every section required by `docs/Prompts/Suite Provenance Doc.txt` against the provenance doc.

| Section | Required by Prompt | Present in Doc | Complete | Notes |
|---------|-------------------|----------------|----------|-------|
| A. Suite Identity | Yes | Yes | Yes | All YAML fields present |
| B. Model Specification | Yes | Yes | Yes | -- |
| B1. Regression Equation | Yes | Yes | Yes | LaTeX equation with subscript notation |
| B2. Dependent Variable(s) | Yes | Yes | Yes | Table format correct |
| B3. Independent Variable(s) | Yes | Yes | Yes | Centering note, median-split construction |
| B4. Control Variables | Yes | Yes | Yes | Single extended block documented |
| B5. Fixed Effects | Yes | Yes | Yes | Table format correct |
| B6. Standard Errors | Yes | Yes | Yes | cov_type + clustering documented |
| B7. Hypothesis Test | Yes | Yes | Yes | Per-variable tail table |
| C. Spec Register | Yes | Yes | Yes | 2 rows, matching MODEL_SPECS |
| D. Sample Construction | Yes | Yes | Partial | See Phase 5 |
| D1. Population | Yes | Yes | Yes | -- |
| D2. Exclusion Criteria | Yes | Yes | **Incomplete** | Missing row counts (Rows Before / Rows After / Dropped columns absent) |
| D3. Sample Counts per Spec | Yes | Yes | Yes | Documents that N may differ between specs |
| E. Variable Dictionary | Yes | Yes | Yes | 22 rows covering all variables |
| F. Data Pipeline | Yes | Yes | Yes | -- |
| F1. Dependency Chain | Yes | Yes | Yes | 7-step chain |
| F2. Data Engines | Yes | Yes | Yes | 4 engines documented |
| F3. Merge Operations | Yes | Yes | Yes | Stage 3 and Stage 4 merges |
| G. Outputs | Yes | Yes | Yes | -- |
| G1. Stage 3 Outputs | Yes | Yes | Yes | 4 files listed |
| G2. Stage 4 Outputs | Yes | Yes | Yes | 10 files listed |
| G3. Summary Statistics | Yes | Yes | Yes | 16 variables listed |
| H. Outlier/Missing Treatment | Yes | Yes | Yes | H1, H2, H3 subsections |
| I. generate_all_tables Entry | Yes | Yes | **FABRICATED** | H1.1b does NOT exist in SUITES list |
| J. Reproduction Commands | Yes | Yes | Yes | 3 commands |
| K. Model-Family Addendum | Yes | Yes | Yes | K1 filled, K2-K6 N/A |
| L. Known Issues | Yes | Yes | Yes | 7 issues documented |

**Phase 1 result:** 25 PASS, 1 FAIL (Section D2 missing row counts; Section I fabricated but structurally present)

------------------------------------------------------------------------

## Phase 2: Factual Accuracy -- Section A (Suite Identity)

**A-1. Suite ID: "H1.1b"**
- VERIFY: Runner docstring line 4 says "STAGE 4: Test H1.1b Binary TNIC-Moderated Cash Holdings Hypothesis"
- **PASS**

**A-2. Title: "Binary Product Similarity--Moderated Speech Uncertainty and Cash Holdings"**
- VERIFY: Runner line 490 (LaTeX caption): `"Binary Product Similarity--Moderated Speech Uncertainty and Cash Holdings"`
- **PASS** -- exact match

**A-3. Hypothesis: "Does product-market similarity (Hoberg-Phillips TNIC3TSIMM), binarized at per-fiscal-year median, moderate the Manager QA Uncertainty -> Cash Holdings relationship?"**
- VERIFY: Runner docstring lines 7-9 match this question.
- **PASS**

**A-4. Direction: "Mixed (one-tailed beta > 0 on main IV; two-tailed on interaction and moderator)"**
- VERIFY: Runner line 33: "Two-tailed on interaction (b3 != 0); one-tailed on main IV (b1 > 0)."
- Runner line 391-392: `p_one_iv = p_two_iv / 2 if beta_iv > 0` (one-tailed positive for IV)
- Runner line 402: `p_two_int` used directly (two-tailed for interaction)
- Runner line 398: `p_two_mod` used directly (two-tailed for moderator)
- **PASS** -- "Mixed" correctly summarizes the per-variable directions

**A-5. Model Family: "PanelOLS"**
- VERIFY: Runner line 61: `from linearmodels.panel import PanelOLS`; line 369: `model_obj = PanelOLS(...)`
- **PASS**

**A-6. Estimator: "linearmodels.panel.PanelOLS"**
- VERIFY: Runner line 61 import statement: `from linearmodels.panel import PanelOLS`
- **PASS**

**A-7. Unit of Observation: "Call-level (individual earnings call)"**
- VERIFY: Runner docstring line 34: "Unit: Call-level"
- Panel builder docstring line 24: "Unit of observation: the individual earnings call (file_name)."
- **PASS**

**A-8. Panel Index: "(gvkey, cal_yr) for Col 1; (gvkey, cal_yr_qtr) for Col 2"**
- VERIFY: Runner line 290: `time_col = "cal_yr_qtr" if fe.endswith("_yq") else "cal_yr"`
- Runner line 366: `df_panel = df_prepared.set_index(["gvkey", time_col])`
- MODEL_SPECS col 1 has `"fe": "industry"` (no `_yq`), so time_col = "cal_yr" -> index = (gvkey, cal_yr)
- MODEL_SPECS col 2 has `"fe": "industry_yq"`, so time_col = "cal_yr_qtr" -> index = (gvkey, cal_yr_qtr)
- **PASS**

**A-9. Columns: 2**
- VERIFY: Runner lines 90-93: `MODEL_SPECS` has 2 entries (col 1 and col 2).
- **PASS**

**A-10. Runner and Panel Builder paths**
- VERIFY: Runner path `src/f1d/econometric/run_h1_1b_cash_tsimm_binary.py` -- EXISTS on disk (file read successfully)
- Panel builder path `src/f1d/variables/build_h1_cash_holdings_panel.py` -- EXISTS on disk (file read successfully)
- **PASS**

**Phase 2 result:** 10/10 PASS

------------------------------------------------------------------------

## Phase 3: Factual Accuracy -- Section B (Model Specification)

**B1-CHECK: Regression Equation**

The provenance doc writes:
```
CashHoldings_{i,t} = b1 * MgrQAUnc_c + b2 * HighTSIMM + b3 * (MgrQAUnc_c x HighTSIMM) + gamma * Controls + alpha_j + delta_t + epsilon
```

Verified against runner code:
- Line 357: `exog = [IV_CENTERED, MODERATOR, INTERACTION] + all_controls`
  - IV_CENTERED = "Manager_QA_Unc_c" (b1)
  - MODERATOR = "HighTSIMM" (b2)
  - INTERACTION = "MgrQAUnc_x_HighTSIMM" (b3)
  - all_controls = CONTROLS (12 controls including Lagged_DV)
- Line 370: `dependent=df_panel[dv]` where dv = "CashHoldings"
- Line 372-374: entity_effects=False, time_effects=True, other_effects=ff12_code
  - alpha_j = industry FE via other_effects
  - delta_t = time FE via time_effects=True (absorbed)

The equation accurately represents what the code estimates.
- **PASS**

**B2-CHECK: Dependent Variable(s)**

Doc claims: CashHoldings = cheq / atq, CompustatEngine, contemporaneous.
- VERIFY: `_compustat_engine.py` line 981: `comp["CashHoldings"] = comp["cheq"] / comp["atq"]`
- Runner MODEL_SPECS: both specs use `"dv": "CashHoldings"` -- contemporaneous (not lead)
- **PASS**

**B3-CHECK: Independent Variable(s)**

Doc lists 3 IVs: Manager_QA_Unc_c, HighTSIMM, MgrQAUnc_x_HighTSIMM.
- VERIFY: Runner line 85: `IV_CENTERED = "Manager_QA_Unc_c"`
- Runner line 84: `MODERATOR = "HighTSIMM"`
- Runner line 86: `INTERACTION = "MgrQAUnc_x_HighTSIMM"`
- Runner line 357: `exog = [IV_CENTERED, MODERATOR, INTERACTION] + all_controls` -- all 3 present
- No IVs missing from the doc.
- **PASS**

**B4-CHECK: Control Variables**

Doc lists 12 controls: BookLev, Size, TobinsQ, ROA, CapexAt, DividendPayer, OCF_Volatility, SalesGrowth, RD_Intensity, CashFlow, Volatility, Lagged_DV.

Code CONTROLS (lines 76-82):
```python
CONTROLS = [
    "BookLev", "Size", "TobinsQ", "ROA", "CapexAt",
    "DividendPayer", "OCF_Volatility",
    "SalesGrowth", "RD_Intensity", "CashFlow", "Volatility",
    "Lagged_DV",
]
```
Count: 12 controls. Every control in code is in doc. Every control in doc is in code.

Lagged_DV construction: doc says `panel["Lagged_DV"] = panel["CashHoldings_lag"]` at line 295.
Code line 295: `panel["Lagged_DV"] = panel[lag_col]` where lag_col = `f"{dv}_lag"` = `"CashHoldings_lag"`. CORRECT.

No dynamic controls in this suite. Doc correctly does not mention any.
- **PASS**

**B5-CHECK: Fixed Effects**

Doc claims:
- Industry FE via ff12_code (other_effects) for both cols
- Time FE: cal_yr (Col 1), cal_yr_qtr (Col 2) via time_effects=True

Code:
- Line 372: `entity_effects=False`
- Line 373: `time_effects=True`
- Line 374: `other_effects=df_panel["ff12_code"]`
- Time column determined by line 290/344: cal_yr for "industry" FE, cal_yr_qtr for "industry_yq" FE
- set_index at line 366 uses [gvkey, time_col]

cal_yr and cal_yr_qtr derivation:
- `panel_utils.py` line 215: `panel["cal_yr"] = dt.dt.year.astype("Int64")`
- `panel_utils.py` line 217: `panel["cal_yr_qtr"] = (panel["cal_yr"] * 10 + panel["cal_qtr"]).astype("Int64")`
- Doc claims `cal_yr = start_date.dt.year` and `cal_yr_qtr = cal_yr * 10 + cal_qtr`. CORRECT.
- Doc cites `panel_utils.py`, function `build_cal_yr_qtr_index()`, lines 195-218. Code confirms function at lines 195-218. CORRECT.
- **PASS**

**B6-CHECK: Standard Errors**

Doc claims: `cov_type="clustered"`, `cluster_entity=True`, default linearmodels behavior.
Code line 378: `model = model_obj.fit(cov_type="clustered", cluster_entity=True)`
- **PASS**

**B7-CHECK: Hypothesis Test**

Doc claims:
- Manager_QA_Unc_c: one-tailed (beta > 0), `p_one = p_two / 2` if beta > 0, else `1 - p_two / 2`
- HighTSIMM: two-tailed, direct from PanelOLS pvalues
- MgrQAUnc_x_HighTSIMM: two-tailed, direct from PanelOLS pvalues

Code:
- Lines 391-392: `p_one_iv = p_two_iv / 2 if beta_iv > 0 else 1 - p_two_iv / 2` -- matches
- Line 398: `p_two_mod = float(model.pvalues.get(MODERATOR, np.nan))` -- direct, two-tailed
- Line 402: `p_two_int = float(model.pvalues.get(INTERACTION, np.nan))` -- direct, two-tailed
- Line 404: `stars_iv = _sig_stars_one(p_one_iv)` -- one-tailed stars
- Line 405: `stars_int = _sig_stars_two(p_two_int)` -- two-tailed stars
- Line 518: moderator uses `_sig_stars_two(p_two_moderator)` in LaTeX table -- two-tailed

All match the doc.

Doc references "lines 391-393", "lines 400-402", "lines 404-405". Verified: all correct.
- **PASS**

**Phase 3 result:** 7/7 PASS

------------------------------------------------------------------------

## Phase 4: Factual Accuracy -- Section C (Spec Register)

**Row count:** Doc has 2 rows (Col 1, Col 2). Code has 2 MODEL_SPECS entries. **PASS**

**Col 1 verification:**
- DV: CashHoldings -- code `"dv": "CashHoldings"` -- **PASS**
- Entity FE: Industry (ff12_code) -- code `"fe": "industry"` + `other_effects=df_panel["ff12_code"]` -- **PASS**
- Time FE: Calendar Year (cal_yr) -- code: `time_col = "cal_yr"` for "industry" FE -- **PASS**
- Controls: Extended (11 controls + Lagged_DV) -- code: CONTROLS has 12 entries, no extra_controls -- **PASS**

**Col 2 verification:**
- DV: CashHoldings -- code `"dv": "CashHoldings"` -- **PASS**
- Entity FE: Industry (ff12_code) -- same -- **PASS**
- Time FE: Calendar Year-Quarter (cal_yr_qtr) -- code: `"fe": "industry_yq"` -> `time_col = "cal_yr_qtr"` -- **PASS**
- Controls: Extended (11 controls + Lagged_DV) -- same -- **PASS**

No specs missing from table. No specs in table not in code.

**Phase 4 result:** 4/4 PASS

------------------------------------------------------------------------

## Phase 5: Factual Accuracy -- Section D (Sample Construction)

**D1-CHECK: Population**
- Doc claims: 112,968 total calls, ~2,429 firms, 2002-2018
- Cross-reference: project scope confirms 112,968 calls, 2,429 firms, 2002-2018
- **PASS**

**D2-CHECK: Exclusion Criteria**

The creation prompt at Section D2 requires:
```
| Step | Filter | Rows Before | Rows After | Dropped |
```

The provenance doc provides:
```
| Step | Filter | Description |
```

This is a **3-column table** instead of the required **5-column table**. The "Rows Before", "Rows After", and "Dropped" columns are entirely absent. The doc adds a note: "the exact row counts at each step depend on the runtime attrition" -- but the creation prompt explicitly requires row counts.

Logical step verification against code:

| Doc Step | Code Location | Accurate? |
|----------|--------------|-----------|
| 1. Load H1 panel | `load_panel()` line 138 | Yes |
| 2. TNIC3 merge | `load_and_merge_tnic()` line 176 | Yes |
| 3. Moderator + IV centering | `transform_moderator_and_center_iv()` line 210 | Yes |
| 4. Main sample (excl ff12=8,11) | `filter_main_sample()` line 271 | Yes |
| 5. DV non-missing | `prepare_regression_data()` line 311 | Yes |
| 6. Complete cases | `prepare_regression_data()` line 316-317 | Yes |
| 7. Min calls per firm | `prepare_regression_data()` lines 321-323 | Yes |

The step descriptions and ordering match the code. But the missing row counts column is a **FAIL** per the creation prompt specification.

- **FAIL** -- missing required row count columns

**D3-CHECK: Sample Counts per Specification**
- Doc notes N may differ between Col 1 and Col 2 due to panel index changes affecting singleton handling.
- This is plausible since set_index changes from (gvkey, cal_yr) to (gvkey, cal_yr_qtr), and drop_absorbed=True can remove different observations.
- **PASS**

**Phase 5 result:** 2/3 PASS, 1 FAIL

------------------------------------------------------------------------

## Phase 6: Factual Accuracy -- Section E (Variable Dictionary)

Checked every variable row in the dictionary.

**DV:**
| Variable | Doc Formula | Code Formula | Match? |
|----------|------------|-------------|--------|
| CashHoldings | cheq / atq | `comp["CashHoldings"] = comp["cheq"] / comp["atq"]` (engine line 981) | **PASS** |

**IVs:**
| Variable | Doc Formula | Code | Match? |
|----------|------------|------|--------|
| Manager_QA_Uncertainty_pct | Count uncertainty words / total words * 100 | LinguisticEngine loads from Stage 2 parquet | **PASS** (formula matches Stage 2 construction) |
| Manager_QA_Unc_c | Manager_QA_Uncertainty_pct - mean(Main sample) | Runner line 251: `panel[IV_CENTERED] = panel[IV] - iv_mu` | **PASS** |
| tnic3tsimm | Product-market similarity from Hoberg-Phillips TNIC3 | Runner line 186-187: loaded from `TNIC3HHIdata.txt` | **PASS** |
| HighTSIMM | 1 if tnic3tsimm > per-fiscal-year median, else 0 | Runner line 233: `panel[MODERATOR] = (panel[MODERATOR_RAW] > panel["_tsimm_yr_median"]).astype(float)` | **PASS** |
| MgrQAUnc_x_HighTSIMM | Manager_QA_Unc_c * HighTSIMM | Runner line 307: `df[INTERACTION] = df[IV_CENTERED] * df[MODERATOR]` | **PASS** |

**Controls:**
| Variable | Doc Formula | Code/Engine | Match? |
|----------|------------|-------------|--------|
| Lagged_DV | CashHoldings_lag = prior FY end-of-year CashHoldings | Builder `create_lag_variable()` lines 379-447; Runner line 295: `panel["Lagged_DV"] = panel[lag_col]` | **PASS** |
| BookLev | (dlcq.fillna(0) + dlttq.fillna(0)) / atq | Engine lines 983-985: `debt_c = comp["dlcq"].clip(lower=0).fillna(0)`, `debt_t = comp["dlttq"].clip(lower=0).fillna(0)`, `comp["BookLev"] = (debt_c + debt_t) / comp["atq"]` | **PASS** |
| Size | ln(atq) for atq > 0 | Engine: `np.log(comp["atq"])` with atq<=0 -> NaN handling | **PASS** |
| TobinsQ | (atq + cshoq*prccq - ceqq) / atq | Engine: `comp["TobinsQ"] = (comp["atq"] + mktcap - comp["ceqq"]) / comp["atq"]` where mktcap = cshoq * prccq | **PASS** |
| ROA | iby_annual / avg_assets | Engine (Biddle): uses iby annual from Q4 / average assets | **PASS** |
| CapexAt | capxy_annual / atq_lag | Engine (Biddle): Q4-only capxy annual / lagged atq | **PASS** |
| DividendPayer | 1 if dvy > 0, else 0 | Engine: binary from dvy annual (Q4) | **PASS** |
| OCF_Volatility | Rolling 5-yr std of oancfy/atq_{t-1} | Engine: rolling window with min 3 years | **PASS** |
| SalesGrowth | (saley_t - saley_{t-1}) / abs(saley_{t-1}) | Engine (Biddle): Q4-only annual, saleq fallback | **PASS** |
| RD_Intensity | xrdq.fillna(0) / atq | Engine: `xrdq.fillna(0) / atq` | **PASS** |
| CashFlow | oancfy / avg_assets | Engine (Biddle): oancfy / average assets with fallback | **PASS** |
| Volatility | std(daily_ret) * sqrt(252) * 100 | CRSPEngine: inter-call window, min 10 days | **PASS** |

**FE/Index columns:**
| Variable | Doc | Code | Match? |
|----------|-----|------|--------|
| gvkey | Compustat permanent ID | Manifest field | **PASS** |
| cal_yr | start_date.dt.year | `panel_utils.py` line 215 | **PASS** |
| cal_yr_qtr | cal_yr * 10 + cal_qtr | `panel_utils.py` line 217 | **PASS** |
| ff12_code | FF12 industry classification | Manifest field | **PASS** |

**Winsorization claims:**
| Variable | Doc Claim | Code | Match? |
|----------|----------|------|--------|
| CashHoldings | 1%/99% by fiscal year | In COMPUSTAT_COLS list (engine line 115), winsorized by `_winsorize_by_year` | **PASS** |
| Manager_QA_Uncertainty_pct | 0%/99% per calendar year (upper-only) | LinguisticEngine: `winsorize_by_year(..., lower=0.0, upper=0.99)` | **PASS** |
| Volatility | Not winsorized | Not a Compustat variable; no winsorization in VolatilityBuilder | **PASS** |
| DividendPayer | No (binary) | In `skip_winsorize` set (engine line 1124) | **PASS** |

**Completeness check:**
- Runner MODEL_SPECS + CONTROLS: CashHoldings, Manager_QA_Unc_c, HighTSIMM, MgrQAUnc_x_HighTSIMM, BookLev, Size, TobinsQ, ROA, CapexAt, DividendPayer, OCF_Volatility, SalesGrowth, RD_Intensity, CashFlow, Volatility, Lagged_DV
- All 16 regression variables present in dictionary
- FE columns (gvkey, ff12_code, cal_yr, cal_yr_qtr) present in dictionary
- Raw variables (Manager_QA_Uncertainty_pct, tnic3tsimm) also documented
- No variables missing

**Phase 6 result:** 22/22 PASS

------------------------------------------------------------------------

## Phase 7: Factual Accuracy -- Sections F, G, H

### F-CHECK: Data Pipeline

**F1. Dependency Chain:**

7-step chain documented. Verified against code:

1. Raw inputs -- doc lists manifest, Compustat, CRSP, linguistic, TNIC3. Code confirms all five sources.
   - Manifest: `load_panel()` line 147-151
   - TNIC3: `load_and_merge_tnic()` line 182
   - Others: via panel builder engines
   - **PASS**

2. Engine loading -- doc lists CompustatEngine, CRSPEngine, LinguisticEngine. Code confirms via panel builder imports (lines 60-87).
   - **PASS**

3. Panel builder -- doc describes merge sequence, join keys (file_name, left join), output parquet. Code at `build_call_level_panel()` lines 103-236.
   - **PASS**

4. Runner loading -- doc describes column selection, cal_yr_qtr build, TNIC merge. Runner lines 138-207.
   - **PASS**

**F2. Data Engines:**

Doc lists 4 engines: CompustatEngine, CRSPEngine, LinguisticEngine, TNIC3 (flat file).
Panel builder imports confirm all four. TNIC3 is loaded at runner level (not engine level), correctly documented as "flat file".
- **PASS**

**F3. Merge Operations:**

Stage 3 merge: doc says manifest + each builder on file_name, left join, zero row-delta. Code lines 209-216 confirm.
Stage 4 merge: doc says panel + TNIC3 on (_gvkey_int, fyearq_int), left join, row count assertion. Runner lines 190-200 confirm.
- **PASS**

### G-CHECK: Outputs

**G1. Stage 3 Outputs:**

| Doc File | Code Evidence | Present? |
|----------|--------------|----------|
| h1_cash_holdings_panel.parquet | Builder line 522: `panel.to_parquet(panel_path, index=False)` | **PASS** |
| summary_stats.csv | Builder line 531: `stats_df.to_csv(stats_path, index=False)` | **PASS** |
| report_step3_h1.md | Builder line 612-614: `report_path = out_dir / "report_step3_h1.md"` | **PASS** |
| run_manifest.json | Builder line 534-546: `generate_manifest(...)` | **PASS** |

**G2. Stage 4 Outputs:**

| Doc File | Code Evidence | Present? |
|----------|--------------|----------|
| h1_1b_cash_tsimm_binary_table.tex | Runner line 583: `tex_path = out_dir / "h1_1b_cash_tsimm_binary_table.tex"` | **PASS** |
| model_diagnostics.csv | Runner line 622: `diag_df.to_csv(out_dir / "model_diagnostics.csv", ...)` | **PASS** |
| summary_stats.csv | Runner line 743: `output_csv=out_dir / "summary_stats.csv"` | **PASS** |
| summary_stats.tex | Runner line 744: `output_tex=out_dir / "summary_stats.tex"` | **PASS** |
| sample_attrition.csv/.tex | Runner line 782-784: `generate_attrition_table(...)` | **PASS** |
| regression_results_col1.txt | Runner line 604: `fname = f"regression_results_col{col_num}.txt"` | **PASS** |
| regression_results_col2.txt | Same pattern, col_num=2 | **PASS** |
| report_step4_H1_1b.md | Runner line 684: `out_dir / "report_step4_H1_1b.md"` | **PASS** |
| run_manifest.json | Runner line 788-796: `generate_manifest(...)` | **PASS** |

No file listed in doc that code does not write. No file written by code that doc omits.
- **PASS**

### H-CHECK: Outlier/Missing Treatment

**H1. Winsorization:**
- Doc claims Compustat 1%/99% by fiscal year. Code: `_winsorize_by_year()` at 1%/99% using `fyearq` as year_col. **PASS**
- Doc claims Linguistic 0%/99% per calendar year (upper-only). Code: `winsorize_by_year(..., lower=0.0, upper=0.99)`. **PASS**
- Doc claims Volatility not winsorized. Code: VolatilityBuilder does not call any winsorize function. **PASS**

**H2. Missing Data Policy:**
- Doc claims complete-case deletion + inf replacement. Code line 304: `df = df.replace([np.inf, -np.inf], np.nan)`. Line 316-317: `complete_mask = df[all_required].notna().all(axis=1)`. **PASS**
- Doc claims min 5 calls per firm. Code line 88: `MIN_CALLS_PER_FIRM = 5`, lines 321-323. **PASS**

**H3. Transformations:**
- Doc lists: mean-centering (IV), median split (TSIMM), natural log (Size), multiplication (interaction). All verified in runner code. **PASS**

**Phase 7 result:** 8/8 PASS

------------------------------------------------------------------------

## Phase 8: Factual Accuracy -- Section I (Table Generator Entry)

This is the most critical failure in the audit.

**The provenance doc claims H1.1b has an entry in `outputs/generate_all_tables.py` and presents an entire code block:**

```python
{
    "id": "H1.1b",
    "type": "moderation",
    "dir": "h1_1b_cash_tsimm_binary/2026-03-28_193207",
    ...
}
```

**ACTUAL CODE:** `outputs/generate_all_tables.py` SUITES list (lines 18-405) was searched exhaustively. The complete list of suite IDs present is:

H0.3, H1, H1.1, H1.2, H4a, H4b, H5, H5b, H5b-Wang, H7, H11, H11-Lag, H11-Lead, H12, H13, H13.1, H14, H16, H17, H18

**H1.1b is NOT in the list.** There is no entry for H1.1b anywhere in `generate_all_tables.py`. Verified by:
1. Case-insensitive grep for `h1_1b` in the file: NO MATCHES
2. Case-insensitive grep for `H1.1b` in the file: NO MATCHES
3. Case-insensitive grep for `tsimm_binary` in the file: NO MATCHES
4. Full reading of SUITES list lines 18-405: H1.1b absent

The provenance doc further claims "Source: `outputs/generate_all_tables.py`, lines 87-109" -- but lines 87-109 in the actual file contain the **H1.2** entry (the financial constraint moderation suite), NOT H1.1b.

The provenance doc also presents a "Verification" table claiming every field matches between `generate_all_tables.py` and the runner code. This entire verification table is checking a non-existent entry.

**Check 1: Does H1.1b have an entry?** -- **FAIL** -- NO, it does not.
**Check 2: Is the provenance doc's code block accurate?** -- **FAIL** -- The code block is entirely fabricated; no such entry exists.

**Phase 8 result:** 0/2 PASS, 2 FAIL

------------------------------------------------------------------------

## Phase 9: Factual Accuracy -- Section K (Model-Family Addendum)

**Correct family identified:** PanelOLS (verified in Phase 2 A-5). K1 filled, K2-K6 marked N/A. **PASS**

**K1 PanelOLS checks:**

| Claim | Code | Match? |
|-------|------|--------|
| entity_effects=False | Line 372: `entity_effects=False` | **PASS** |
| time_effects=True | Line 373: `time_effects=True` | **PASS** |
| other_effects=df_panel["ff12_code"] | Line 374: `other_effects=df_panel["ff12_code"]` | **PASS** |
| drop_absorbed=True | Line 375: `drop_absorbed=True` | **PASS** |
| check_rank=False (line 377) | Line 376: `check_rank=False,` | **FAIL** -- doc says "line 377", actual is line 376. The closing paren `)` is at line 377. |

Doc says "Source: `run_h1_1b_cash_tsimm_binary.py`, lines 369-378." The range 369-378 includes both the PanelOLS constructor and the .fit() call, which is correct as a range. But the specific claim about `check_rank=False` being at line 377 is off by one.

**Phase 9 result:** 5/6 PASS, 1 FAIL (minor line number error)

------------------------------------------------------------------------

## Phase 10: Quality Gate Checklist

| # | Quality Gate | Met? | Evidence |
|---|-------------|------|----------|
| 1 | Every variable in every regression spec appears in Variable Dictionary with explicit formula and source engine | **YES** | Phase 6: all 16 regression variables + 4 FE/index columns + 2 raw variables documented with formulas |
| 2 | The model equation matches what the code actually estimates | **YES** | Phase 3 B1: equation verified against exog list and PanelOLS call |
| 3 | The specification register accounts for every model column | **YES** | Phase 4: 2 rows for 2 MODEL_SPECS entries |
| 4 | The attrition cascade has row counts for each filter step | **NO** | Phase 5 D2: table has Step/Filter/Description columns but lacks Rows Before/Rows After/Dropped columns. The doc defers to runtime attrition CSV. |
| 5 | The tail test direction matches between runner code and generate_all_tables.py | **NO** | Phase 8: H1.1b has no entry in generate_all_tables.py, so this gate cannot be satisfied. The doc fabricates a match. |
| 6 | The FE specification matches between docstring, code, and this document | **YES** | Phase 3 B5: docstring says "Calendar Year FE", code uses time_effects=True with cal_yr/cal_yr_qtr, doc matches |
| 7 | Every merge in the panel builder is documented with join keys and type | **YES** | Phase 7 F3: Stage 3 (file_name, left) and Stage 4 (_gvkey_int+fyearq_int, left) documented |
| 8 | The output file list matches what the runner actually writes | **YES** | Phase 7 G1/G2: all files verified |
| 9 | The model-family addendum is filled for the correct family only | **YES** | Phase 9: K1 filled for PanelOLS, K2-K6 N/A |
| 10 | Any claim marked [UNVERIFIED] has an explanation of what blocks verification | **YES** | No [UNVERIFIED] claims present in the doc |

**Phase 10 result:** 8/10 PASS, 2 FAIL

------------------------------------------------------------------------

## Phase 11: Cross-Reference Consistency

| # | Check | Result | Evidence |
|---|-------|--------|----------|
| 1 | DVs in B2 match DVs in C | **PASS** | B2: CashHoldings. C: CashHoldings for both cols. |
| 2 | DVs in C match DVs in I | **FAIL** | C: CashHoldings. I: fabricated entry claims CashHoldings. Since Section I is fabricated, this cross-reference is meaningless. |
| 3 | Controls in B4 match variables in E | **PASS** | B4 lists 12 controls. E has all 12 with formulas. |
| 4 | Column count in A matches rows in C | **PASS** | A: 2 columns. C: 2 rows. |
| 5 | Column count in A matches "cols" in I | **FAIL** | A: 2 columns. I: fabricated entry claims cols=2. Since the entry does not exist, this cannot be verified. |
| 6 | Tail direction in A matches B7 matches I | **PASS** (A vs B7 only) | A: "Mixed (one-tailed beta > 0 on main IV; two-tailed on interaction and moderator)". B7: same directions per variable. I: fabricated, so A-B7 match only. |
| 7 | FE in B5 matches C matches K | **PASS** | B5: Industry + CalYr/CalYrQtr. C: same. K1: entity_effects=False, time_effects=True, other_effects=ff12_code. |
| 8 | Panel index in A matches set_index in K | **PASS** | A: (gvkey, cal_yr) for Col 1, (gvkey, cal_yr_qtr) for Col 2. K1: set_index uses gvkey + time_col. |

**Phase 11 result:** 6/8 PASS, 2 FAIL (both due to fabricated Section I)

------------------------------------------------------------------------

## FAILURES (detailed)

| Phase | Check | Provenance Doc Claims | Actual Code Says | Severity | Fix Required |
|-------|-------|----------------------|-----------------|----------|-------------|
| 5 | D2 attrition cascade | Step/Filter/Description table (3 columns, no row counts) | Creation prompt requires Rows Before/Rows After/Dropped columns | Medium | Add row count columns or mark as [UNVERIFIED -- runtime dependent] with explicit explanation |
| 8 | I-1 Entry existence | H1.1b has an entry in generate_all_tables.py at lines 87-109 | H1.1b is NOT in the SUITES list. Lines 87-109 contain H1.2. | **Critical** | Replace entire Section I with a statement that H1.1b has no entry in generate_all_tables.py |
| 8 | I-2 Verification table | 10-row verification table showing field-by-field match | The entry does not exist; the entire verification is fabricated | **Critical** | Remove fabricated verification table |
| 9 | K1 check_rank line | "check_rank=False (line 377)" | check_rank=False is at line 376; line 377 is the closing paren `)` | Minor | Change "line 377" to "line 376" |
| 10 | QG4 Attrition row counts | Attrition cascade "has row counts for each filter step" | Row counts deferred to runtime CSV; not present in doc | Medium | Add row counts or add [UNVERIFIED] explanation |
| 10 | QG5 Tail test direction match | Tail test direction "matches between runner code and generate_all_tables.py" | Cannot match -- no entry exists in generate_all_tables.py | **Critical** | Acknowledge absence |
| 11 | XR-2 DVs C vs I | Cross-reference checks DVs between spec register and table generator | Table generator entry does not exist | Medium | Remove cross-reference to non-existent Section I |
| 11 | XR-5 Cols A vs I | Cross-reference checks column count between identity and table generator | Table generator entry does not exist | Medium | Remove cross-reference to non-existent Section I |

------------------------------------------------------------------------

## CORRECTIONS REQUIRED

1. **[CRITICAL] Section I -- Replace entire section.**

   Current (wrong) text: A Python dict code block followed by a 10-row verification table, with source citation "lines 87-109".

   Should say instead:
   ```
   ## I. GENERATE_ALL_TABLES.PY ENTRY

   H1.1b does NOT have an entry in `outputs/generate_all_tables.py` as of 2026-03-30.
   The parent suite H1.1 (continuous TNIC moderation) has an entry, but H1.1b (binary)
   does not. This means H1.1b's LaTeX table is generated directly by the runner's
   `_save_latex_table()` function (lines 462-581) rather than through the shared table
   generator pipeline.

   If H1.1b is to be included in the thesis table set, an entry must be added to the
   SUITES list in generate_all_tables.py.
   ```

   Code reference: `outputs/generate_all_tables.py` SUITES list (lines 18-405) -- H1.1b absent. The closest entry is H1.1 at lines 62-85.

2. **[MEDIUM] Section D2 -- Add row count columns or mark as [UNVERIFIED].**

   Current (incomplete) text: 3-column table (Step | Filter | Description) with a note deferring to runtime.

   Should say instead: Either add the 5-column attrition table with actual row counts from the latest run, or add an explicit [UNVERIFIED] marker:

   ```
   [UNVERIFIED -- Row counts at each filter step are runtime-dependent and saved to
   sample_attrition.csv. The attrition table generated by the runner (lines 776-784)
   records only 4 aggregate stages, not the full 7-step cascade. Exact per-step counts
   require executing the runner.]
   ```

3. **[MINOR] Section K1 -- Fix check_rank line number.**

   Current (wrong) text: "check_rank: False -- Rank check disabled (line 377)."

   Should say: "check_rank: False -- Rank check disabled (line 376)."

   Code reference: `run_h1_1b_cash_tsimm_binary.py` line 376: `check_rank=False,`

------------------------------------------------------------------------

## ADDITIONAL NOTES

The following items are not failures but are worth recording for completeness:

1. **Section F1 step 7 says "Suite has entry in `outputs/generate_all_tables.py` (type: moderation)"** -- this is also incorrect since the entry does not exist. This sentence should be removed or corrected.

2. **Section L (Known Issues) item 7** references "lines 407, 420" for R-squared computation. Verified: line 407 has the print statement with the formula, and line 420 has the meta dict assignment. Both are correct.

3. **The provenance doc is otherwise of high quality.** The variable dictionary, model specification, FE documentation, merge operations, and output listings are thorough and accurate. The fabricated Section I is the only critical defect.
